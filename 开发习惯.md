# yiyiaddon 开发习惯

> 本文件是 yiyiaddon 项目的长期强制规则。
> 以后任何 AI 智能体开始工作前，第一步必须读取本文件，然后才能扫描代码、修改代码或开发新功能。

---

## 零、项目身份（先记住这个）

| 项目 | 路径 | 定位 |
|------|------|------|
| yiyiaddon（新，当前） | `D:/mcaddon/yiyiaddon` | 独立 Minecraft 26.1.2 Fabric 客户端模组。包根 `com.yiyiaddon`，Mod ID `yiyiaddon`。**不依赖任何第三方客户端框架** |
| 旧项目 | `D:/mcaddon/26.1.2` | 仅作为业务逻辑、算法、状态机、数据、资源、配置的提取来源，其框架代码不作为 yiyiaddon 的运行时依赖 |

开发参考资料统一放在 `01-开发参考库/`，开发报告统一放在 `02-开发报告/项目开发报告/`。

---

## 一、代码分类与目录

1. 所有代码必须按照职责工整分类，目录必须容易理解、容易查找。

2. 禁止为了省事把大量不同职责代码全部挤在一个 Java 文件中。

3. 一个 Java 文件应该有明确职责。

4. 复杂功能应该根据真实职责合理拆分，例如：
   - 功能入口
   - 流程控制
   - 状态机
   - 任务
   - 服务
   - 数据模型
   - Minecraft 适配
   - 配置

5. 不要求机械拆分。简单功能保持简单，复杂功能才进行合理拆分。

6. 禁止把不知道放哪里的代码全部塞进 `utils`、`helper`、`common`、`manager`。

7. 如果不知道代码应该放在哪里：先检查调用关系和实际职责，再决定目录。

8. 正式 Java 代码必须保持工整。可以根据实际职责使用类似 `client/`、`core/`、`platform/`、`service/`、`modules/`、`integration/`、`ui/`、`mixin/`、`dev/` 这样的分类。

9. 不要为了目录漂亮创建大量空包。具体业务目录必须根据真实代码自行建立，不要提前猜用户有什么模块。

10. 正式代码只能位于 `src/main/java`、`src/main/resources`。`01-开发参考库/` 不参与编译、不进入 jar、不作为运行时依赖。

---

## 二、用户可见内容与语言

11. 用户可见内容统一使用中文，包括：界面、设置、按钮、提示、状态、错误、开发报告。

12. Java 类名、API、包名、Identifier 等技术名称允许使用英文，禁止翻译代码标识符。

13. 用户可见内容禁止使用 Emoji。

---

## 三、开发报告

14. 开发报告必须使用中文，禁止使用 Emoji。

15. 开发报告必须带日期。

16. 开发报告统一放在 `02-开发报告/项目开发报告/`，按阶段先后顺序排列。禁止散落在 `02-开发报告/` 根目录，禁止新建并列或按主题分叉的报告目录。

17. 报告文件名格式：`YYYY-MM-DD-第NN阶段-报告名称.md`。NN 为两位阿拉伯数字（01、02、…、10），保证文件名字典序等于阶段顺序。禁止使用中文数字（一/二/三/四/五）作为阶段编号，禁止跳号。

18. 以后所有开发报告都要明确写清楚新项目与旧项目的关系，避免混淆。

---

## 四、验证标准（严禁混淆）

19. 不允许把「编译通过」当成功能已经验证。必须严格区分以下状态：

    - 代码完成
    - 编译通过
    - 客户端启动验证
    - 实际功能验证
    - 待真实环境验证

    报告中说明某个功能时，必须写明它当前处于上述哪一个状态。

---

## 五、旧代码迁移

20. 旧项目代码迁移时，不允许根据 AI 记忆猜功能，必须查看真实源码。

21. 迁移时不得提前依赖人工模块列表，也不得依赖旧开发报告。必须自己分析真实旧源码，分清：
    - 什么是真实业务
    - 什么是公共代码
    - 什么是第三方框架代码（一律不迁移）
    - 什么是 Minecraft API
    - 什么是第三方依赖
    - 什么是数据和资源

    然后再整合进 `D:/mcaddon/yiyiaddon`。

---

## 六、Minecraft 26.1.2 API

22. Minecraft 26.1.2 API 必须参考 `01-开发参考库/` 中的真实 Mojang 26.1.2 源码，禁止凭旧版本经验猜 API。

23. 禁止使用 Yarn 旧名。26.1 起 Yarn 退役，统一使用 Mojang 官方映射名。

24. 参考库中查不到某个 API，不能直接判定其不存在，必须继续在原始源码中搜索确认。

---

## 七、版权工作

25. 当前开发阶段不把时间花在版权整理、许可证整理、NOTICE 整理、第三方版权报告上，不因这些工作中断当前开发。

26. 现有版权文件不需要主动重构。发布相关事项以后再单独处理。

    当前重点是：代码、结构、稳定性、迁移、验证。

---

## 八、工作前置动作（硬性）

27. 以后所有 AI 智能体开始工作前，必须先读取本文件。

28. 开发前先查 `01-开发参考库/README.md` 的查询顺序铁律，按顺序查证，禁止跳步猜测。

---

## 九、旧项目与迁移铁律（第四阶段新增，2026-09-14）

29. yiyiaddon 是独立 Fabric 自研项目。不依赖任何第三方客户端框架，架构完全自研。

30. `D:/mcaddon/26.1.2` 是旧 Meteor 项目。它的 Mod 入口继承 MeteorAddon，全部模块继承 Meteor 的 Module。

31. 旧项目只作为参考来源。它提供的是业务逻辑、算法、数据、配置、资源，不提供运行时依赖。

32. 新代码禁止依赖 Meteor。新写的任何代码不得出现 `meteordevelopment` 包的 import 或调用。

33. 迁移必须提取业务逻辑。只取识别算法、状态机、数据模型、文件持久化、协议封装、资源解析；框架代码一律不取。

34. 禁止直接复制旧框架。模块外壳、Setting 系统、Event 总线、GUI 组件、指令注册、汉化 Mixin、accessor 接口都属于框架层，必须按 yiyiaddon 自研架构重写。

35. 开发前必须扫描真实源码。禁止依赖人工描述、旧报告摘要或 AI 记忆来推断旧项目结构。

36. 不根据人工描述猜项目结构。功能清单、模块清单、依赖清单必须以真实文件内容与调用关系为准。

37. 代码必须分类整理。迁移进来的代码也要按 yiyiaddon 的 `client/`、`core/`、`platform/`、`service/`、`modules/`、`integration/`、`ui/`、`mixin/`、`dev/` 分类摆放。

38. 禁止大量逻辑堆在单个 Java 文件。旧项目中超过 1,000 行的类（共 13 个）必须先按职责拆分，再迁移；不得整类搬入。

39. 用户可见内容中文。界面、设置、按钮、提示、状态、错误、开发报告一律中文；代码标识符保持英文。

40. 禁止 Emoji。Java 源码、注释、游戏内文本、开发报告全部禁止 Emoji，需要图形时使用 Unicode 符号（✓ ✗ ⚠ ▸ ■ ├ └ ─）。

41. 报告必须中文并带日期。文件名格式 `YYYY-MM-DD-第NN阶段-报告名称.md`（NN 为两位阿拉伯数字），统一放在 `02-开发报告/项目开发报告/`，按阶段顺序排列。

42. 输出内容禁止寒暄和无意义文字。只输出结论、方案与技术内容；报错时只输出问题、原因、修复方法。

43. 严禁迁移的旧代码（第四阶段扫描确认）：`utils/YiyiaddonPasswordInterceptorService`（明文密码外发）、`utils/YiyiaddonWatermark`、`mixin/ClientCommandSourceMixin`（指令名上报）、`hud/HudExample`、`theme/ThemeModule`，以及全部 recognition/adapter 死代码层（24 个文件）与 `stardew/profile/StardewProfileStore`。

44. 迁移顺序固定：先基础设施（`farm/ContainerBroker`、`farm/FarmPacketOps`、`resourcepack/ResourcePackDownloader`、`resourcepack/BlockStateModelResolver`、`core/world/*`、播报排版器），再识别体系（`itemid`、`worldblock`），再业务模块（农业主线、星露谷、其他自动化模块）。禁止倒序迁移。

45. 迁移前的统一规范（第四阶段扫描发现的三处不一致，迁移时必须先统一）：
    - 服务器键统一使用 `ResourcePackDownloader.canonicalKey` 的规范化实现（小写 + 补默认端口），禁止裸 `ip.replaceAll`。
    - 维度键统一使用 `dimension().identifier().toString()`，禁止 `ResourceKey[...]` 包装格式。
    - 文件持久化统一抽象为一个 `JsonFileStore`（临时文件 + 备份 + `ATOMIC_MOVE` + 失败回滚），禁止每个管理器各写一份。

46. 迁移验证状态必须写明。区分：代码完成 / 编译通过 / 客户端启动验证 / 实际功能验证 / 待真实环境验证。禁止把编译通过当成功能已验证。

---

## 十、迁移代码分类规范（2026-09-14 追加）

47. 迁移进来的代码必须按 yiyiaddon 新项目结构分类整理。禁止所有代码堆在一个目录，禁止一个 Java 文件承担多个职责，禁止保留旧 Meteor 的混乱结构。

48. 分类目录按实际职责划定，固定八类（2026-09-14 第六阶段新增 `command/`）。八类是「模块内部」的分类；顶层通用目录 `core/`（入口、事件总线、模块运行时、全局排版）、`command/`（客户端指令系统）、`ui/`（界面与渲染）、`mixin/`（Mixin 声明）、`client/`（客户端生命周期）、`dev/`（开发期调试）与之并存，不冲突：模块内部用八类，跨模块共用的东西才放顶层。

| 目录 | 职责 | 放什么 | 不放什么 |
| --- | --- | --- | --- |
| `model/` | 数据模型 | record、值对象、枚举、状态快照、DTO | 任何业务动作、任何 Minecraft 调用 |
| `service/` | 业务服务 | 状态机、协调器、识别器、规划器、任务执行体、业务算法 | 文件读写、Minecraft API 直接调用 |
| `repository/` | 数据存储 | 持久化读写、文件事务、序列化、缓存与索引管理 | 业务判定、状态机推进 |
| `platform/` | Fabric/Minecraft 适配 | Minecraft API 封装、发包、容器交互、资源读取、事件桥接、Mixin | 业务规则、硬编码判定逻辑 |
| `integration/` | 第三方兼容 | Baritone 集成、第三方 Mod 对接、外部协议与远程接口 | 本项目自有业务逻辑 |
| `config/` | 配置管理 | 设置项定义、配置读写、配置校验、默认值 | 业务状态、运行期缓存 |
| `command/` | 模块指令 | 模块自带的客户端指令（参数解析、补全候选、中文回显） | 功能实现本身（功能放 service） |
| `utils/` | 通用工具 | 无业务语义的纯函数工具（字符串、加密、像素对齐、文件名净化） | 不知道放哪里的代码 |

49. 一个模块一个包。模块内的类按上述八类分子目录摆放，禁止把不同模块的类放进同一个包。模块专属界面放模块内的 `ui/`。

50. 禁止把代码塞进 `utils/`、`helper/`、`common/`、`manager/` 作为兜底。不确定归属时，先查调用关系与实际职责再决定目录。

51. 旧项目的包名与目录结构一律不复用（例如 `com.example.addon.autofarm.controller`）。迁移后按 yiyiaddon 的包规范重新组织：框架层留在 `com.yiyiaddon.<框架目录>`，业务模块统一放进 `com.yiyiaddon.feature.<模块名>.<职责>`。

52. 迁移前先列分类对照表：旧类路径 → 新类路径 → 新职责目录 → 是否需要拆分。拆分依据是职责数量，不是行数。

---

## 十一、迁移记录报告规范（2026-09-14 追加）

53. 每次迁移完成必须生成中文迁移记录报告，禁止跳过、禁止事后补写。

54. 位置：`D:/mcaddon/yiyiaddon/02-开发报告/项目开发报告/`，与阶段报告同目录，按阶段顺序排列。

55. 文件名：`日期-第NN阶段-迁移记录.md`（NN 为两位阿拉伯数字）。同一阶段有多次迁移时追加模块名区分，例如 `2026-09-15-第05阶段-迁移记录-容器基础设施.md`。

56. 报告固定九节，顺序不变：

```
1. 本次迁移目标
2. 迁移文件列表
3. 新目录结构
4. 分类调整说明
5. Meteor 依赖清理情况
6. 修改内容
7. 编译结果
8. 测试结果
9. 遗留问题
10. 下一阶段开发规划（见第十二章）
```

57. 各节写法要求：

- 第 2 节：旧路径 → 新路径 → 类型（直接迁移 / 拆分迁移 / 重写 / 新建），逐文件列出，不得只写数量。
- 第 4 节：说明每个类为什么归到该目录，以及发生了哪些拆分（旧类 → 新类拆分关系）。
- 第 5 节：列出清理掉的 `meteordevelopment` 引用点，以及哪些类因此重写；若本次无 Meteor 相关内容，写明「本次无」。
- 第 6 节：只写实际改动。
- 第 7 节：写真实的构建命令与真实输出结果。
- 第 8 节：写真实的验证状态（代码完成 / 编译通过 / 客户端启动验证 / 实际功能验证 / 待真实环境验证），未做的写未做。
- 第 9 节：如实记录发现的问题与未完成项。

58. 严禁编造。没有修改就写未修改，没有测试就写未测试，没有编译就写未编译。禁止用推测、估计、预期结果填充报告。

59. 报告中文，无 Emoji，无寒暄，无废话，只记录实际开发内容。

60. 全部开发报告（阶段报告、迁移记录、专项报告）集中在 `D:/mcaddon/yiyiaddon/02-开发报告/项目开发报告/`，按阶段顺序排列，禁止散落在 `02-开发报告/` 根目录或另建并列目录。

61. 阶段编号一律用两位阿拉伯数字（第01阶段、第02阶段……），使文件名字典序等于阶段顺序，目录列表即按阶段排列。禁止使用中文数字编号。

62. 新增报告前先确认上一阶段报告是否已存在。阶段报告缺失时必须先补写或明确说明缺失原因，禁止跳号后沉默不提。

---

## 十二、下一阶段规划与路线图规范（2026-09-14 追加）

63. 每完成一个开发阶段，除当前阶段报告外，必须在同一份阶段报告的正文最后增加 `## 下一阶段开发规划` 章节。目的：保证项目持续推进，避免阶段完成后没有明确方向。

64. 该章节固定六节，顺序与标题不变：

```
一、下一阶段目标
二、执行原因
三、前置条件检查
四、任务拆分
五、验收标准
六、禁止事项
```

65. 第三节「前置条件检查」必须列出两类内容：当前阶段已完成的能力、下一阶段依赖哪些已有代码。无法确认的依赖必须写明「需先确认」，禁止想当然。

66. 第四节「任务拆分」按顺序编号，每个任务必须写满四项：目标、涉及目录、影响范围、预计修改内容。缺项视为不合格。任务粒度要求可独立验证，禁止把多个模块塞进一个任务。

67. 第五节「验收标准」必须包含：编译通过、客户端启动成功、功能验证通过、无旧框架依赖、报告完成。功能验证项逐条列出，验证后必须标注真实状态（代码完成 / 编译通过 / 客户端启动验证 / 实际功能验证 / 待真实环境验证），未做的写未做。

68. 第六节「禁止事项」必须写清下一阶段不做什么，防止扩大范围、提前迁移、破坏已有结构。已迁移代码的包结构默认冻结，只允许调用，不允许重排。

69. 全部阶段报告（含迁移记录）末尾必须维护 `## 项目迁移路线图`，固定四段格式：

```
已完成：√ 阶段名称
当前：→ 当前阶段
下一步：→ 下一阶段
未来：→ 后续阶段列表
```

70. 路线图随项目推进持续更新，不得只在新报告里写而不回改旧报告。各报告中的路线图必须相互一致，禁止出现某份报告仍把已完成阶段列为「下一步」。

71. 阶段报告缺失时必须在路线图「当前」段写明缺失事实（例如第 03 阶段报告不存在），禁止静默略过。

72. 每个阶段结束必须形成「当前状态 → 下一目标 → 执行方案」三段闭环。只记录「完成了什么」而不记录「下一步做什么」的报告视为不合格。

---

## 十三、目录与包结构规范（2026-09-14 追加）

73. 顶层包清单固定如下，新增顶层包必须经确认，禁止随手新增：

```
command/      客户端指令系统（前缀、注册表、内置指令、补全）
config/       配置管理
core/         框架层：入口、事件总线、模块运行时、全局排版
feature/      业务模块（每个模块一个子包）
integration/  第三方兼容（如 Baritone）
mixin/        Mixin 声明
model/        跨模块共用的数据模型
module/       框架层：模块注册表、分类注册表、模块条目、引导注册
platform/     Fabric / Minecraft 适配
repository/   数据存储
service/      跨模块共用的业务服务
ui/           界面与渲染
utils/        通用工具
```

74. 框架层与业务层严格分离。`com.yiyiaddon.module` 属框架层（注册表、分类、模块条目、引导注册），禁止把任何业务模块类放进该包；业务模块包根固定为 `com.yiyiaddon.feature.<模块名>`，模块内部按七类（`model` / `service` / `repository` / `platform` / `integration` / `config` / `utils`）分子目录，模块专属界面放模块内的 `ui`。

75. 业务模块的注册统一走唯一引导入口 `com.yiyiaddon.module.AddonModules`。业务模块只提供注册数据与实现，禁止各自在客户端入口类里注册，禁止新增第二个引导入口。引导顺序固定为：分类 → 模块 → 指令 → 控件工厂 → 资源服务。

76. 报告中的代码规模、文件数量、调用点数量等统计必须注明统计时点。项目被并发修改导致历史报告数字过期时，不逐次回改历史报告，改为在下一份报告的「状态变更」段写明差异、原因与影响；仅当原表述会误导后续开发（例如报告写「某框架不存在」而实际已存在）时才回改。

77. 规划中若出现需要新增结构约定（顶层包、模块包根、引导入口等）的决定，必须同步写入本文件对应条目，禁止只在报告里写规划而不落规则。

---

## 十四、模块独立页面规则（2026-09-14 追加）

78. yiyiaddon 所有功能模块必须拥有独立配置页面。禁止在模块列表页直接展开大量设置，禁止把所有模块配置堆叠在同一个页面，禁止用一个巨大滚动页面管理全部功能。

79. 交互链路固定，禁止跳步：

```
模块中心
  ↓
选择功能分类
  ↓
显示模块列表
  ↓
点击模块
  ↓
进入该模块独立页面
```

80. 模块列表页只负责三件事：展示模块、显示模块状态、提供进入入口。禁止在列表页承载模块业务配置、运行数据或日志。

81. 每个模块页面必须与模块业务绑定，由模块自身负责其页面内容，禁止把模块页面集中堆在顶层 `ui/` 而与业务脱节。

82. 模块页面内容按模块真实职责确定，不预先规定模板。示例：

| 模块 | 页面负责内容 |
| --- | --- |
| 自动农场 | 自动农场配置、当前运行状态、任务信息、仓库信息、点位管理、日志查看 |
| 自动箱子 | 扫描设置、目标物品、箱子记录、取物规则、运行状态 |
| 星露谷农场 | 种子管理、作物状态、浇水设置、农田信息 |
| 自动附魔 | 装备目标、附魔模板、材料状态、执行流程 |

83. 模块页面代码放在模块包内的 `ui` 目录，与模块业务同包：

```
feature/<模块名>/
 ├─ model/        数据模型
 ├─ service/      业务逻辑
 ├─ config/       模块配置
 ├─ repository/   数据存储
 ├─ platform/     Minecraft 适配
 └─ ui/           模块独立页面
```

顶层 `com.yiyiaddon.ui` 只放跨模块共用的界面基础设施（屏幕基类、渲染工具、控件工厂），禁止放具体模块页面。

84. 只参考传统客户端「点击模块进入独立界面」这一交互方式。禁止复用旧 Meteor 的设置系统、UI 代码、模块继承方式与页面结构。

85. 当前阶段只确定页面独立结构，不提前固定视觉方案。页面风格、主题、组件、动画、布局后续统一设计，禁止各模块自造风格。

86. 最终目标：每个复杂功能模块都拥有独立的入口、页面、配置、状态、日志，形成「模块中心 → 模块页面 → 模块功能」的完整架构。

---

## 十五、模块运行时与指令系统规范（2026-09-14 第六阶段追加）

87. 所有功能模块必须继承 `com.yiyiaddon.core.module.Module`。模块只实现自己的功能，生命周期调度、状态持久化、事件订阅退订、异常隔离一律由 `ModuleManager` 负责。

88. 模块元数据与能力声明只写在自己的类里：ID、中英文名、分类 ID、说明、图标、权重、版本、生命周期（`onInitialize` / `onEnable` / `onDisable` / `onTick` / `selfCheck`）、事件（`subscribedEvents` / `onEvent`）、设置（`loadSettings` / `saveSettings`）、界面（`page`）、指令（`commands`）。

89. 模块禁止自行注册 Fabric 事件、禁止自行读写状态文件、禁止自行捕获异常后静默吞掉、禁止在 `onTick` 里做重活。异常由运行时捕获：中文播报 + 自动关闭该模块，其余模块不受影响。

90. 模块注册只改 `com.yiyiaddon.module.AddonModules` 的 `createModules()`。禁止新增第二个引导入口，禁止各模块自行在客户端入口注册。引导顺序固定为：分类 → 事件入口 → 模块 → 指令。

91. 状态与配置分家：模块启用状态、模块快捷键、模块设置存 `config/module-state.json`（模块运行数据）；界面外观与界面快捷键存 `yiyiaddon.json`（界面配置）。两者禁止互相写入；界面配置重置不得清掉模块开关与模块快捷键。

92. 客户端 Fabric 事件的注册点只有一个：`core/event/EventDispatcher`。模块通过 `subscribedEvents()` 声明需要的事件类型，运行时在启用时订阅、关闭时整批退订。禁止模块绕过事件总线直接注册 Fabric 事件。

93. 收包 / 发包事件必须经队列由主线程派发（网络线程只允许入队），禁止在网络线程回调模块。

94. 客户端指令统一走 `com.yiyiaddon.command`：前缀 `.`，指令名与别名都要支持中文，参数补全与错误提示必须实现；新增指令实现 `ClientCommand` 并在引导入口注册（模块自带指令由运行时自动注册）。玩家可见文本一律中文。

95. 模块自检 `selfCheck()` 返回缺项列表：非空时不得启用。因条件未满足（如未进入世界）被拦下的模块进入等待队列，条件满足后自动开启，且不得因此把玩家的开启状态写回关闭。

---

## 十六、用户交互兼容铁律（2026-09-14 追加）

> 本章为最高优先级规则。与其它章节冲突时，以本章为准。

96. **代码可以重构，架构可以升级，目录可以重排；但旧项目的用户交互资产禁止修改。** 旧项目用户交互资产包含五类，全部属不可随意修改资产：
    - 指令（指令名、别名、子命令、参数、帮助文本、错误提示、成功提示）
    - 状态通知与播报（聊天消息、Toast、ActionBar、状态提示、成功/失败提示、过程播报、任务状态文字）
    - 中文显示文本（模块名、分类名、设置名、按钮文字、页面标题、配置说明）
    - 配置名称（Setting 名称、配置字段、中文 JSON 键名、模板名称、参数说明）
    - 用户操作方式（指令交互路径、参数顺序、页面层级）

97. **迁移任何模块前，必须先查旧项目真实源码里的用户交互原文，逐字复制，不得改写。** 禁止「用更通顺的说法重写」。禁止「优化措辞」。禁止「改变语气」。例如旧文为 `XXX成功`，迁移后必须仍是 `XXX成功`。

98. **禁止 AI 自行创造指令名、状态播报、中文提示、配置显示名称。** 新增任何用户可见文本前，必须先在旧项目源码中确认是否已有对应文本；已有则必须使用旧文本，禁止另起一套。

99. **禁止用英文替代旧项目已有的中文。** 旧项目已有中文显示名的，迁移后必须仍是中文。技术标识符（类名、包名、API 名、Identifier）保持英文不受此限。

100. **禁止自行修改指令名称、禁止自行设计替代指令。** 旧项目已有指令必须保持同一名称。例如旧指令 `.id`，迁移后必须仍是 `.id`；旧子命令 `物品`，不得改成 `item` 后把中文降级为别名。

101. **指令回执必须保持旧项目的统一排版格式。** 旧项目所有指令回执经统一排版器输出（标题 + 字段 + 状态行，字段分隔 ` §8▸ `，状态行五级 `成功`/`处理中`/`警告`/`失败`/`信息`，空值兜底 `无`）。迁移后实现方式可以不同，**输出文本与格式不得不同**。禁止各模块自行拼接字符串。状态级别与用色按第十七章第 114 条执行（旧项目的 `警告` 对应新规范的 `异常`，同为橙色 `§6`）。

102. **模块播报前缀规则以第十七章为准。** 旧项目前缀为 `§c§l[yiyiaddon]§r§f§l[模块名]§r`，含模组自身名称；第十七章第 110 条明令禁止显示 `yiyiaddon`，故前缀改为 `[模块名] 正文`，模块名白色 `§f`、整条消息加粗 `§l`（第 111、112 条）。**本条原文（要求保留 yiyiaddon 段）已作废**，此处保留是为记录变更原因。

103. **中文 JSON 键名（写盘字段）禁止改动。** 旧项目用中文键写盘（如 `物品ID`、`显示名称`、`方块状态`、`成熟阶段`）。迁移后必须沿用同一键名，否则旧存档无法读取，属破坏性变更。

104. **迁移时必须逐条核对，核对依据是《2026-09-14-用户交互资产迁移规范报告》**（`02-开发报告/项目开发报告/`）。该报告是旧项目用户交互资产的唯一权威基准，包含：旧项目指令清单、状态通知清单、中文文本清单、配置名称清单、当前项目差异、发现的问题、后续迁移规则。核对项缺少任何一项视为迁移不合格。

105. **只迁移逻辑、不迁移文本，属不合格迁移。** 每个模块的验收必须包含「用户交互文本逐字比对」这一步。

106. **旧项目已发现的两套不一致文案，迁移时必须先二选一定稿，禁止原样带入新项目。** 具体不一致项记录在上述规范报告第 6.4 节。

107. **已迁移代码中若发现用户交互文本被改写，应在该模块下一次改动时一并回改为旧原文，并单独记录「回改项」。** 禁止在没有记录的情况下顺手改文本。

108. **确需修改旧原文时必须单独说明理由并留档。** 只有在旧文本存在事实错误、或旧文本在新架构下无法成立时，才允许修改；修改必须记录：旧原文、新原文、修改原因、影响范围。禁止以「更通顺」「更统一」「更简洁」为理由修改。

109. **本章适用于全部后续迁移**，包括但不限于自动农场、星露谷、自动箱子、自动附魔、村民交易、图书馆管理员、传送、自动登入、自动采矿、绕过类模块、用户统计、界面汉化、界面主题、水源显示、指令说明。

---

## 十七、用户交互消息显示规范（固定铁律，2026-09-14 追加）

> 本章为固定铁律，优先级高于第十六章中与之冲突的条目。
> 第 102 条（模块播报前缀必须保留 `§c§l[yiyiaddon]§r§f§l[模块名]§r`）**已被本章取代**，前缀规则以本章第 110-113 条为准。

### 一、前缀显示规则

110. **消息前缀统一使用「当前模块名称」。** 禁止显示以下三类内容作为前缀或出现在前缀中：
    - `yiyiaddon`（模组自身名称）
    - 旧项目插件前缀
    - 内部项目名称

111. **模块名称作为前缀时的样式固定**：

    | 项 | 规定 |
    | --- | --- |
    | 颜色 | 白色（`§f`） |
    | 字体 | 加粗（`§l`） |

112. **加粗作用于整条消息，不限于模块名段。** 前缀与正文一律加粗（`§l`），模块名用白色 `§f`，正文按状态色（第 114 条）。

    示例：`[自动农场] 收割完成`
    其中 `[自动农场]` 与 `收割完成` **均为加粗**；`[自动农场]` 白色，`收割完成` 取成功色绿色。

    实现口径：`§f§l[模块名] ` 之后直接接状态色即可保持加粗（`§` 颜色码不重置加粗），无需重复写 `§l`；确需在正文中途解除加粗时才能使用 `§r`，且必须重新指定颜色。

113. **前缀格式固定为 `[模块名] 正文`**，方括号与模块名同属前缀，不得替换为其它标识。未归属任何模块的框架级消息（如指令框架自身提示），前缀使用承载该消息的功能名，同样禁止出现 `yiyiaddon`。

### 二、状态颜色规则

114. **颜色属于用户交互资产，禁止 AI 自行修改颜色含义。** 固定语义如下，八种状态与其用色一一对应，不得混用、不得互换、不得新增：

    | 状态 | 颜色 | 用色代码 | 适用范围 |
    | --- | --- | --- | --- |
    | 成功 | 绿色 | `§a` | 完成、成功、验证通过 |
    | 失败 | 红色 | `§c` | 执行失败、错误、无法完成 |
    | 异常 | 橙色 | `§6` | 异常情况、风险提醒、需要处理的问题 |
    | 信息 | 蓝色 | `§b` | 普通状态、流程说明、信息提示 |
    | 处理中 | 黄色 | `§e` | 等待、加载、执行中 |
    | 调试 | 紫色 | `§d` | 调试信息、开发诊断 |
    | 禁用 | 灰色 | `§7` | 未启用、不可用、无数据 |
    | 重要 | 青色 | `§3` | 重要提醒、核心状态 |

115. 上表用色代码与旧项目实际用色一致（旧项目已按此语义使用：成功 `§a`、失败 `§c`、异常 `§6`、信息 `§b`、处理中 `§e`、调试 `§d`、禁用 `§7`、重要 `§3`），且旧项目状态文字本就加粗（如 `§a§l已开启`），与本规范第 111、112 条一致。**禁止以「更好看」「更协调」为由更换配色或去掉加粗。**

116. **一条消息只用一个状态色。** 不得为了强调在同一条消息里堆叠多个状态色。字段值使用白色 `§f`，字段标签使用灰色 `§7`，同样保持加粗。

### 三、迁移规则

117. **旧项目已有的消息格式、前缀、颜色、状态文字，迁移时必须优先保持旧项目。** 禁止 AI 自行重写、禁止自行优化措辞、禁止自行改变颜色。

118. **只有旧项目不存在的新功能提示，才使用本章规范。** 新功能提示同样必须遵守第 110-116 条。

119. **前缀是本条唯一的例外**：旧项目前缀含 `yiyiaddon`，与第 110 条直接冲突，故按第 110 条执行（不显示 `yiyiaddon`），其余部分（模块名、白色、加粗、整条加粗）保持旧项目样式。

### 四、用户交互资产范围

120. 以下全部属于固定资产，禁止随意修改：

    - 指令名称
    - 指令参数
    - 状态播报
    - 聊天消息
    - 通知文本
    - 颜色
    - 前缀格式
    - 配置显示名称
    - UI 显示文字

121. **迁移时必须扫描旧项目。** 代码可以重构，用户体验不能随意改变。

122. **分类（模块分组）不属迁移范围，禁止按旧项目改动（2026-09-14 定稿）。** 分类是 yiyiaddon 自己界面的分组结构：功能模块最终都归入本项目的模块分组，旧项目的 5 个分类（工具 / 自动化 / 绕过 / 辅助 / 星露谷）属旧框架的界面产物，不是要搬运的用户交互资产。因此：

    - 禁止按旧项目增删分类、改分类名、改分类顺序、改分类样式；
    - 分类的划分、名称、样式与顺序一律由本项目界面设计决定；
    - 分类名中禁止出现 `yiyiaddon`（第 110 条）；
    - 本报告第四章「分类名称」清单仅作旧项目现状记录，不作为迁移依据。

    配套要求：模块的 `categoryId` 必须指向本项目已注册的分类，不得为迁就旧项目分类而新增分类。


