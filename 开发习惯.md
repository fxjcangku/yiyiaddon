# yiyiaddon 开发习惯

> **本文件是 yiyiaddon 的规则正本（36 章 / 234 条）。入口钩子见根目录 [`AGENTS.md`](AGENTS.md)。**
>
> **项目性质：移植项目（移植阶段已收口）。** 旧项目 `D:/mcaddon/26.1.2` 的功能、算法、状态机、数据、
> 配置与用户文案**已逐模块搬进本项目，只换 UI 外壳**；迁移口径正本为
> `02-开发报告/项目开发报告/00-总纲与基准/01-总纲-迁移口径与复刻原则.md`。
>
> **当前状态：修 bug 阶段**（2026-09-19 用户确认「我的移植已经完成了 … 现在在修 bug 的阶段了」）。
> 全量迁移已完成，产出以「用户实机报缺陷 → 定位 → 修复 → 复测」为主：**不要再把「搬新模块」当默认任务**，
> 优先做用户点名的缺陷修复与既有能力加固。缺陷流程走第二十八章、埋点流程走第三十三章；
> 动任何模块前仍先查进度清单 `02-开发报告/项目开发报告/00-总纲与基准/30-移植总览-已移植与未移植全清单.md`。
>
> 以后任何 AI 智能体开始工作前，**第一步必须完整读取本文件**，然后才能扫描代码、修改代码或开发新功能。
> 与其它任何报告、文档冲突时，一律以本文件为准（其中第二十三章「改动禁区」优先级最高）。

---

## 零、项目身份（先记住这个）

| 项目 | 路径 | 定位 |
|------|------|------|
| yiyiaddon（新，当前） | `D:/mcaddon/yiyiaddon` | 独立 Minecraft 26.1.2 Fabric 客户端模组。包根 `com.yiyiaddon`，Mod ID `yiyiaddon`。**不依赖任何第三方客户端框架** |
| 旧项目 | `D:/mcaddon/26.1.2` | 仅作为业务逻辑、算法、状态机、数据、资源、配置的提取来源，其框架代码不作为 yiyiaddon 的运行时依赖 |

开发参考资料统一放在 `01-开发参考库/`，开发报告统一放在 `02-开发报告/项目开发报告/`，
埋点修复案例统一放在 `04-埋点修复/{进行中,已修复}/`（方案见第三十三章）。

根目录 `AGENTS.md` 是**智能体入口钩子**：只放「项目性质 + 必读清单 + 条号速查」，**规则内容只写在本文件里**；
两处冲突以本文件为准。本文件增删章节时，必须同步核对 `AGENTS.md` 第三节的速查条号。

---

## 一、代码分类与目录

1. 所有代码必须按照职责工整分类，目录必须容易理解、容易查找。

2. 禁止为了省事把大量不同职责代码全部挤在一个 Java 文件中。

3. 一个 Java 文件应该有明确职责。

4. 复杂功能应该根据真实职责合理拆分，例如：
   - 功能入口
   - 流程控制
   - 状态机
   - 任务
   - 服务
   - 数据模型
   - Minecraft 适配
   - 配置

5. 不要求机械拆分。简单功能保持简单，复杂功能才进行合理拆分。

6. 禁止把不知道放哪里的代码全部塞进 `utils`、`helper`、`common`、`manager`。

7. 如果不知道代码应该放在哪里：先检查调用关系和实际职责，再决定目录。

8. 正式 Java 代码必须保持工整。可以根据实际职责使用类似 `client/`、`core/`、`platform/`、`service/`、`modules/`、`integration/`、`ui/`、`mixin/`、`dev/` 这样的分类。

9. 不要为了目录漂亮创建大量空包。具体业务目录必须根据真实代码自行建立，不要提前猜用户有什么模块。

10. 正式代码只能位于 `src/main/java`、`src/main/resources`。`01-开发参考库/` 不参与编译、不进入 jar、不作为运行时依赖。

---

## 二、用户可见内容与语言

11. 用户可见内容统一使用中文，包括：界面、设置、按钮、提示、状态、错误、开发报告。

12. Java 类名、API、包名、Identifier 等技术名称允许使用英文，禁止翻译代码标识符。

13. 用户可见内容禁止使用 Emoji。

---

## 三、开发报告

14. 开发报告必须使用中文，禁止使用 Emoji。

15. 开发报告必须带日期。

16. 开发报告统一放在 `02-开发报告/项目开发报告/`，按阶段先后顺序排列。禁止散落在 `02-开发报告/` 根目录，禁止新建并列或按主题分叉的报告目录。

17. 报告文件名格式：`YYYY-MM-DD-第NN阶段-报告名称.md`。NN 为两位阿拉伯数字（01、02、…、10），保证文件名字典序等于阶段顺序。禁止使用中文数字（一/二/三/四/五）作为阶段编号，禁止跳号。

18. 以后所有开发报告都要明确写清楚新项目与旧项目的关系，避免混淆。

---

## 四、验证标准（严禁混淆）

19. 不允许把「编译通过」当成功能已经验证。必须严格区分以下状态：

    - 代码完成
    - 编译通过
    - 客户端启动验证
    - 实际功能验证
    - 待真实环境验证

    报告中说明某个功能时，必须写明它当前处于上述哪一个状态。

---

## 五、旧代码迁移

20. 旧项目代码迁移时，不允许根据 AI 记忆猜功能，必须查看真实源码。

21. 迁移时不得提前依赖人工模块列表，也不得依赖旧开发报告。必须自己分析真实旧源码，分清：
    - 什么是真实业务
    - 什么是公共代码
    - 什么是第三方框架代码（一律不迁移）
    - 什么是 Minecraft API
    - 什么是第三方依赖
    - 什么是数据和资源

    然后再整合进 `D:/mcaddon/yiyiaddon`。

---

## 六、Minecraft 26.1.2 / 26.2 API

22. Minecraft API 必须以**目标版本的真实官方源码 / 字节码**为准，禁止凭旧版本经验猜 API。

    - **26.1.2 主线（`master`）**：查 `01-开发参考库/Minecraft-26.1.2-Mojang源码/`（6882 个 .java）与
      `Minecraft-26.1.2-映射参考/`。
    - **26.2 版本线（`port/26.2`）**：查本库的 `01-开发参考库/Minecraft-26.2-Mojang源码/`
      （7055 个 .java，由 26.2 merged JAR 用 Loom `genSources` 反编译）与 `Minecraft-26.2-映射参考/`；
      **禁止**拿 26.1.2 那份当 26.2 的依据 —— 两版差异清单见第三十六章与 136 号复盘。
    - 索引与查询脚本按线取用：26.1.2 用顶层 `01-开发参考库/工具/`，26.2 用
      `Minecraft-26.2-映射参考/工具/`。顶层 `工具/审计-*.js`（mixin 提取/注册/验证、负面清单、
      import 对账、baritone 命令名）六个脚本已改为**按线自动取材**：运行时读 `gradle.properties`
      的 `minecraft_version` 自动选本线源码 / 索引 / jar，资料不齐直接报错退出，两条线都可直接跑。
    - 查不到时以本线源码为准；**官方博客只作索引不作依据**（26.2 实测三处与博客不一致：`EntityTypeIds`、
      `ColorCollection`、`BlockItemTags`）。

23. 禁止使用 Yarn 旧名。26.1 起 Yarn 退役，统一使用 Mojang 官方映射名。

24. 参考库中查不到某个 API，不能直接判定其不存在，必须继续在原始源码中搜索确认。

---

## 七、版权工作

25. 当前开发阶段不把时间花在版权整理、许可证整理、NOTICE 整理、第三方版权报告上，不因这些工作中断当前开发。

26. 现有版权文件不需要主动重构。发布相关事项以后再单独处理。

    当前重点是：代码、结构、稳定性、迁移、验证。

---

## 八、工作前置动作（硬性）

27. 以后所有 AI 智能体开始工作前，必须先读取本文件。

28. 开发前先查 `01-开发参考库/README.md` 的查询顺序铁律，按顺序查证，禁止跳步猜测。

---

## 九、旧项目与迁移铁律（第四阶段新增，2026-09-14）

29. yiyiaddon 是独立 Fabric 自研项目。不依赖任何第三方客户端框架，架构完全自研。

30. `D:/mcaddon/26.1.2` 是旧项目，它建立在第三方客户端框架之上。它的 Mod 入口继承该框架的基类，全部模块继承该框架的 Module。

31. 旧项目只作为参考来源。它提供的是业务逻辑、算法、数据、配置、资源，不提供运行时依赖。

32. 新代码禁止依赖旧框架。新写的任何代码不得出现 `meteordevelopment` 包的 import 或调用。

33. 迁移必须提取业务逻辑。只取识别算法、状态机、数据模型、文件持久化、协议封装、资源解析；框架代码一律不取。

34. 禁止直接复制旧框架。模块外壳、Setting 系统、Event 总线、GUI 组件、指令注册、汉化 Mixin、accessor 接口都属于框架层，必须按 yiyiaddon 自研架构重写。

35. 开发前必须扫描真实源码。禁止依赖人工描述、旧报告摘要或 AI 记忆来推断旧项目结构。

36. 不根据人工描述猜项目结构。功能清单、模块清单、依赖清单必须以真实文件内容与调用关系为准。

37. 代码必须分类整理。迁移进来的代码也要按 yiyiaddon 的 `client/`、`core/`、`platform/`、`service/`、`modules/`、`integration/`、`ui/`、`mixin/`、`dev/` 分类摆放。

38. 禁止大量逻辑堆在单个 Java 文件。旧项目中超过 1,000 行的类（共 13 个）必须先按职责拆分，再迁移；不得整类搬入。

39. 用户可见内容中文。界面、设置、按钮、提示、状态、错误、开发报告一律中文；代码标识符保持英文。

40. 禁止 Emoji。Java 源码、注释、游戏内文本、开发报告全部禁止 Emoji，需要图形时使用 Unicode 符号（✓ ✗ ⚠ ▸ ■ ├ └ ─）。

41. 报告必须中文并带日期。**文件名格式 `NN-类别-名称.md`**（NN 为两位序号；类别 ∈ 总纲 / 基准 / 规范 / UI / 阶段 / 追加 / 复盘 / 参考），统一放在 `02-开发报告/项目开发报告/`，**按 NN 升序即为阅读顺序**；正文首行必须写「日期：YYYY-MM-DD」。2026-09-14 已把全部历史报告统一改为该命名（原名与新编号对照见 `00-AI阅读指引.md` 与各报告正文内的引用）。

42. 输出内容禁止寒暄和无意义文字。只输出结论、方案与技术内容；报错时只输出问题、原因、修复方法。

43. 严禁迁移的旧代码（第四阶段扫描确认）：`utils/YiyiaddonPasswordInterceptorService`（明文密码外发）、`utils/YiyiaddonWatermark`、`mixin/ClientCommandSourceMixin`（指令名上报）、`hud/HudExample`、`theme/ThemeModule`，以及全部 recognition/adapter 死代码层（24 个文件）与 `stardew/profile/StardewProfileStore`。

44. 迁移顺序固定：先基础设施（`farm/ContainerBroker`、`farm/FarmPacketOps`、`resourcepack/ResourcePackDownloader`、`resourcepack/BlockStateModelResolver`、`core/world/*`、播报排版器），再识别体系（`itemid`、`worldblock`），再业务模块（农业主线、星露谷、其他自动化模块）。禁止倒序迁移。

45. 迁移前的统一规范（第四阶段扫描发现的三处不一致，迁移时必须先统一）：
    - 服务器键统一使用 `ResourcePackDownloader.canonicalKey` 的规范化实现（小写 + 补默认端口），禁止裸 `ip.replaceAll`。
    - 维度键统一使用 `dimension().identifier().toString()`，禁止 `ResourceKey[...]` 包装格式。
    - 文件持久化统一抽象为一个 `JsonFileStore`（临时文件 + 备份 + `ATOMIC_MOVE` + 失败回滚），禁止每个管理器各写一份。

46. 迁移验证状态必须写明。区分：代码完成 / 编译通过 / 客户端启动验证 / 实际功能验证 / 待真实环境验证。禁止把编译通过当成功能已验证。

---

## 十、迁移代码分类规范（2026-09-14 追加）

47. 迁移进来的代码必须按 yiyiaddon 新项目结构分类整理。禁止所有代码堆在一个目录，禁止一个 Java 文件承担多个职责，禁止保留旧项目框架的混乱结构。

48. 分类目录按实际职责划定，固定八类（2026-09-14 第六阶段新增 `command/`）。八类是「模块内部」的分类；顶层通用目录 `core/`（入口、事件总线、模块运行时、全局排版）、`command/`（客户端指令系统）、`ui/`（界面与渲染）、`mixin/`（Mixin 声明）、`client/`（客户端生命周期）、`dev/`（开发期调试）与之并存，不冲突：模块内部用八类，跨模块共用的东西才放顶层。

| 目录 | 职责 | 放什么 | 不放什么 |
| --- | --- | --- | --- |
| `model/` | 数据模型 | record、值对象、枚举、状态快照、DTO | 任何业务动作、任何 Minecraft 调用 |
| `service/` | 业务服务 | 状态机、协调器、识别器、规划器、任务执行体、业务算法 | 文件读写、Minecraft API 直接调用 |
| `repository/` | 数据存储 | 持久化读写、文件事务、序列化、缓存与索引管理 | 业务判定、状态机推进 |
| `platform/` | Fabric/Minecraft 适配 | Minecraft API 封装、发包、容器交互、资源读取、事件桥接、Mixin | 业务规则、硬编码判定逻辑 |
| `integration/` | 第三方兼容 | Baritone 集成、第三方 Mod 对接、外部协议与远程接口 | 本项目自有业务逻辑 |
| `config/` | 配置管理 | 设置项定义、配置读写、配置校验、默认值 | 业务状态、运行期缓存 |
| `command/` | 模块指令 | 模块自带的客户端指令（参数解析、补全候选、中文回显） | 功能实现本身（功能放 service） |
| `utils/` | 通用工具 | 无业务语义的纯函数工具（字符串、加密、像素对齐、文件名净化） | 不知道放哪里的代码 |

49. 一个模块一个包。模块内的类按上述八类分子目录摆放，禁止把不同模块的类放进同一个包。模块专属界面放模块内的 `ui/`。

50. 禁止把代码塞进 `utils/`、`helper/`、`common/`、`manager/` 作为兜底。不确定归属时，先查调用关系与实际职责再决定目录。

51. 旧项目的包名与目录结构一律不复用（例如 `com.example.addon.autofarm.controller`）。迁移后按 yiyiaddon 的包规范重新组织：框架层留在 `com.yiyiaddon.<框架目录>`，业务模块统一放进 `com.yiyiaddon.feature.<模块名>.<职责>`。

52. 迁移前先列分类对照表：旧类路径 → 新类路径 → 新职责目录 → 是否需要拆分。拆分依据是职责数量，不是行数。

---

## 十一、迁移记录报告规范（2026-09-14 追加）

53. 每次迁移完成必须生成中文迁移记录报告，禁止跳过、禁止事后补写。

54. 位置：`D:/mcaddon/yiyiaddon/02-开发报告/项目开发报告/`，与阶段报告同目录，按阶段顺序排列。

55. 文件名：`日期-第NN阶段-迁移记录.md`（NN 为两位阿拉伯数字）。同一阶段有多次迁移时追加模块名区分，例如 `2026-09-15-第05阶段-迁移记录-容器基础设施.md`。

56. 报告固定九节，顺序不变：

```
1. 本次迁移目标
2. 迁移文件列表
3. 新目录结构
4. 分类调整说明
5. 旧框架依赖清理情况
6. 修改内容
7. 编译结果
8. 测试结果
9. 遗留问题
10. 下一阶段开发规划（见第十二章）
```

57. 各节写法要求：

- 第 2 节：旧路径 → 新路径 → 类型（直接迁移 / 拆分迁移 / 重写 / 新建），逐文件列出，不得只写数量。
- 第 4 节：说明每个类为什么归到该目录，以及发生了哪些拆分（旧类 → 新类拆分关系）。
- 第 5 节：列出清理掉的 `meteordevelopment` 引用点，以及哪些类因此重写；若本次无相关内容，写明「本次无」。
- 第 6 节：只写实际改动。
- 第 7 节：写真实的构建命令与真实输出结果。
- 第 8 节：写真实的验证状态（代码完成 / 编译通过 / 客户端启动验证 / 实际功能验证 / 待真实环境验证），未做的写未做。
- 第 9 节：如实记录发现的问题与未完成项。

58. 严禁编造。没有修改就写未修改，没有测试就写未测试，没有编译就写未编译。禁止用推测、估计、预期结果填充报告。

59. 报告中文，无 Emoji，无寒暄，无废话，只记录实际开发内容。

60. 全部开发报告（阶段报告、迁移记录、专项报告）集中在 `D:/mcaddon/yiyiaddon/02-开发报告/项目开发报告/`，按阶段顺序排列，禁止散落在 `02-开发报告/` 根目录或另建并列目录。

61. 阶段编号一律用两位阿拉伯数字（第01阶段、第02阶段……），使文件名字典序等于阶段顺序，目录列表即按阶段排列。禁止使用中文数字编号。

62. 新增报告前先确认上一阶段报告是否已存在。阶段报告缺失时必须先补写或明确说明缺失原因，禁止跳号后沉默不提。

---

## 十二、下一阶段规划与路线图规范（2026-09-14 追加）

63. 每完成一个开发阶段，除当前阶段报告外，必须在同一份阶段报告的正文最后增加 `## 下一阶段开发规划` 章节。目的：保证项目持续推进，避免阶段完成后没有明确方向。

64. 该章节固定六节，顺序与标题不变：

```
一、下一阶段目标
二、执行原因
三、前置条件检查
四、任务拆分
五、验收标准
六、禁止事项
```

65. 第三节「前置条件检查」必须列出两类内容：当前阶段已完成的能力、下一阶段依赖哪些已有代码。无法确认的依赖必须写明「需先确认」，禁止想当然。

66. 第四节「任务拆分」按顺序编号，每个任务必须写满四项：目标、涉及目录、影响范围、预计修改内容。缺项视为不合格。任务粒度要求可独立验证，禁止把多个模块塞进一个任务。

67. 第五节「验收标准」必须包含：编译通过、客户端启动成功、功能验证通过、无旧框架依赖、报告完成。功能验证项逐条列出，验证后必须标注真实状态（代码完成 / 编译通过 / 客户端启动验证 / 实际功能验证 / 待真实环境验证），未做的写未做。

68. 第六节「禁止事项」必须写清下一阶段不做什么，防止扩大范围、提前迁移、破坏已有结构。已迁移代码的包结构默认冻结，只允许调用，不允许重排。

69. 全部阶段报告（含迁移记录）末尾必须维护 `## 项目迁移路线图`，固定四段格式：

```
已完成：√ 阶段名称
当前：→ 当前阶段
下一步：→ 下一阶段
未来：→ 后续阶段列表
```

70. 路线图随项目推进持续更新，不得只在新报告里写而不回改旧报告。各报告中的路线图必须相互一致，禁止出现某份报告仍把已完成阶段列为「下一步」。

71. 阶段报告缺失时必须在路线图「当前」段写明缺失事实（例如第 03 阶段报告不存在），禁止静默略过。

72. 每个阶段结束必须形成「当前状态 → 下一目标 → 执行方案」三段闭环。只记录「完成了什么」而不记录「下一步做什么」的报告视为不合格。

---

## 十三、目录与包结构规范（2026-09-14 追加）

73. 顶层包清单固定如下，新增顶层包必须经确认，禁止随手新增：

```
command/      客户端指令系统（前缀、注册表、内置指令、补全）
config/       配置管理
core/         框架层：入口、事件总线、模块运行时、全局排版
feature/      业务模块（每个模块一个子包）
integration/  第三方兼容（如 Baritone）
mixin/        Mixin 声明
model/        跨模块共用的数据模型
module/       框架层：模块注册表、分类注册表、模块条目、引导注册
platform/     Fabric / Minecraft 适配
repository/   数据存储
service/      跨模块共用的业务服务
ui/           界面与渲染
utils/        通用工具
```

74. 框架层与业务层严格分离。`com.yiyiaddon.module` 属框架层（注册表、分类、模块条目、引导注册），禁止把任何业务模块类放进该包；业务模块包根固定为 `com.yiyiaddon.feature.<模块名>`，模块内部按七类（`model` / `service` / `repository` / `platform` / `integration` / `config` / `utils`）分子目录，模块专属界面放模块内的 `ui`。

75. 业务模块的注册统一走唯一引导入口 `com.yiyiaddon.module.AddonModules`。业务模块只提供注册数据与实现，禁止各自在客户端入口类里注册，禁止新增第二个引导入口。引导顺序固定为：分类 → 模块 → 指令 → 控件工厂 → 资源服务。

76. 报告中的代码规模、文件数量、调用点数量等统计必须注明统计时点。项目被并发修改导致历史报告数字过期时，不逐次回改历史报告，改为在下一份报告的「状态变更」段写明差异、原因与影响；仅当原表述会误导后续开发（例如报告写「某框架不存在」而实际已存在）时才回改。

77. 规划中若出现需要新增结构约定（顶层包、模块包根、引导入口等）的决定，必须同步写入本文件对应条目，禁止只在报告里写规划而不落规则。

---

## 十四、模块独立页面规则（2026-09-14 追加）

78. yiyiaddon 所有功能模块必须拥有独立配置页面。禁止在模块列表页直接展开大量设置，禁止把所有模块配置堆叠在同一个页面，禁止用一个巨大滚动页面管理全部功能。

79. 交互链路固定，禁止跳步：

```
模块中心
  ↓
选择功能分类
  ↓
显示模块列表
  ↓
点击模块
  ↓
进入该模块独立页面
```

80. 模块列表页只负责三件事：展示模块、显示模块状态、提供进入入口。禁止在列表页承载模块业务配置、运行数据或日志。

81. 每个模块页面必须与模块业务绑定，由模块自身负责其页面内容，禁止把模块页面集中堆在顶层 `ui/` 而与业务脱节。

82. 模块页面内容按模块真实职责确定，不预先规定模板。示例：

| 模块 | 页面负责内容 |
| --- | --- |
| 自动农场 | 自动农场配置、当前运行状态、任务信息、仓库信息、点位管理、日志查看 |
| 自动箱子 | 扫描设置、目标物品、箱子记录、取物规则、运行状态 |
| 星露谷农场 | 种子管理、作物状态、浇水设置、农田信息 |
| 自动附魔 | 装备目标、附魔模板、材料状态、执行流程 |

83. 模块页面代码放在模块包内的 `ui` 目录，与模块业务同包：

```
feature/<模块名>/
 ├─ model/        数据模型
 ├─ service/      业务逻辑
 ├─ config/       模块配置
 ├─ repository/   数据存储
 ├─ platform/     Minecraft 适配
 └─ ui/           模块独立页面
```

顶层 `com.yiyiaddon.ui` 只放跨模块共用的界面基础设施（屏幕基类、渲染工具、控件工厂），禁止放具体模块页面。

84. 只参考传统客户端「点击模块进入独立界面」这一交互方式。禁止复用旧项目框架的设置系统、UI 代码、模块继承方式与页面结构。

85. 当前阶段只确定页面独立结构，不提前固定视觉方案。页面风格、主题、组件、动画、布局后续统一设计，禁止各模块自造风格。

86. 最终目标：每个复杂功能模块都拥有独立的入口、页面、配置、状态、日志，形成「模块中心 → 模块页面 → 模块功能」的完整架构。

---

## 十五、模块运行时与指令系统规范（2026-09-14 第六阶段追加）

87. 所有功能模块必须继承 `com.yiyiaddon.core.module.Module`。模块只实现自己的功能，生命周期调度、状态持久化、事件订阅退订、异常隔离一律由 `ModuleManager` 负责。

88. 模块元数据与能力声明只写在自己的类里：ID、中英文名、分类 ID、说明、图标、权重、版本、生命周期（`onInitialize` / `onEnable` / `onDisable` / `onTick` / `selfCheck`）、事件（`subscribedEvents` / `onEvent`）、设置（`loadSettings` / `saveSettings`）、界面（`page`）、指令（`commands`）。

89. 模块禁止自行注册 Fabric 事件、禁止自行读写状态文件、禁止自行捕获异常后静默吞掉、禁止在 `onTick` 里做重活。异常由运行时捕获：中文播报 + 自动关闭该模块，其余模块不受影响。

90. 模块注册只改 `com.yiyiaddon.module.AddonModules` 的 `createModules()`。禁止新增第二个引导入口，禁止各模块自行在客户端入口注册。引导顺序固定为：分类 → 事件入口 → 模块 → 指令。

91. 状态与配置分家：模块启用状态、模块快捷键、模块设置存 `config/module-state.json`（模块运行数据）；界面外观与界面快捷键存 `yiyiaddon.json`（界面配置）。两者禁止互相写入；界面配置重置不得清掉模块开关与模块快捷键。

92. 客户端 Fabric 事件的注册点只有一个：`core/event/EventDispatcher`。模块通过 `subscribedEvents()` 声明需要的事件类型，运行时在启用时订阅、关闭时整批退订。禁止模块绕过事件总线直接注册 Fabric 事件。

93. 收包 / 发包事件必须经队列由主线程派发（网络线程只允许入队），禁止在网络线程回调模块。

94. 客户端指令统一走 `com.yiyiaddon.command`：默认前缀 `.`（可在设置页「指令前缀」修改，非法值回落默认前缀），指令名与别名都要支持中文，参数补全与错误提示必须实现；新增指令实现 `ClientCommand` 并在引导入口注册（模块自带指令由运行时自动注册）。玩家可见文本一律中文。

    - 前缀取值只允许一处来源：`CommandManager.prefix()`。禁止在指令、界面或提示文本里硬编码前缀字符，用法与提示一律拼接 `CommandManager.prefix()`；拦截与补全也必须走该方法，否则改前缀后会出现「能输入但不生效」的错位。

95. 模块自检 `selfCheck()` 返回缺项列表：非空时不得启用。因条件未满足（如未进入世界）被拦下的模块进入等待队列，条件满足后自动开启，且不得因此把玩家的开启状态写回关闭。

---

## 十六、用户交互兼容铁律（2026-09-14 追加）

> 本章为最高优先级规则。与其它章节冲突时，以本章为准。

96. **代码可以重构，架构可以升级，目录可以重排；但旧项目的用户交互资产禁止修改。** 旧项目用户交互资产包含五类，全部属不可随意修改资产：
    - 指令（指令名、别名、子命令、参数、帮助文本、错误提示、成功提示）
    - 状态通知与播报（聊天消息、Toast、ActionBar、状态提示、成功/失败提示、过程播报、任务状态文字）
    - 中文显示文本（模块名、分类名、设置名、按钮文字、页面标题、配置说明）
    - 配置名称（Setting 名称、配置字段、中文 JSON 键名、模板名称、参数说明）
    - 用户操作方式（指令交互路径、参数顺序、页面层级）

97. **迁移任何模块前，必须先查旧项目真实源码里的用户交互原文，逐字复制，不得改写。** 禁止「用更通顺的说法重写」。禁止「优化措辞」。禁止「改变语气」。例如旧文为 `XXX成功`，迁移后必须仍是 `XXX成功`。

98. **禁止 AI 自行创造指令名、状态播报、中文提示、配置显示名称。** 新增任何用户可见文本前，必须先在旧项目源码中确认是否已有对应文本；已有则必须使用旧文本，禁止另起一套。

99. **禁止用英文替代旧项目已有的中文。** 旧项目已有中文显示名的，迁移后必须仍是中文。技术标识符（类名、包名、API 名、Identifier）保持英文不受此限。

100. **禁止自行修改指令名称、禁止自行设计替代指令。** 旧项目已有指令必须保持同一名称。例如旧指令 `.id`，迁移后必须仍是 `.id`；旧子命令 `物品`，不得改成 `item` 后把中文降级为别名。

101. **指令回执必须保持旧项目的统一排版格式。** 旧项目所有指令回执经统一排版器输出（标题 + 字段 + 状态行，字段分隔 ` §8▸ `，状态行五级 `成功`/`处理中`/`警告`/`失败`/`信息`，空值兜底 `无`）。迁移后实现方式可以不同，**输出文本与格式不得不同**。禁止各模块自行拼接字符串。状态级别与用色按第十七章第 114 条执行（旧项目的 `警告` 对应新规范的 `异常`，同为橙色 `§6`）。

102. **模块播报前缀规则以第十七章为准。** 旧项目前缀为 `§c§l[yiyiaddon]§r§f§l[模块名]§r`，含模组自身名称；第十七章第 110 条明令禁止显示 `yiyiaddon`，故前缀改为 `[模块名] 正文`，模块名白色 `§f`、整条消息加粗 `§l`（第 111、112 条）。**本条原文（要求保留 yiyiaddon 段）已作废**，此处保留是为记录变更原因。

103. **中文 JSON 键名（写盘字段）禁止改动。** 旧项目用中文键写盘（如 `物品ID`、`显示名称`、`方块状态`、`成熟阶段`）。迁移后必须沿用同一键名，否则旧存档无法读取，属破坏性变更。

104. **迁移时必须逐条核对，核对依据是《2026-09-14-用户交互资产迁移规范报告》**（`02-开发报告/项目开发报告/`）。该报告是旧项目用户交互资产的唯一权威基准，包含：旧项目指令清单、状态通知清单、中文文本清单、配置名称清单、当前项目差异、发现的问题、后续迁移规则。核对项缺少任何一项视为迁移不合格。

105. **只迁移逻辑、不迁移文本，属不合格迁移。** 每个模块的验收必须包含「用户交互文本逐字比对」这一步。

106. **旧项目已发现的两套不一致文案，迁移时必须先二选一定稿，禁止原样带入新项目。** 具体不一致项记录在上述规范报告第 6.4 节。

107. **已迁移代码中若发现用户交互文本被改写，应在该模块下一次改动时一并回改为旧原文，并单独记录「回改项」。** 禁止在没有记录的情况下顺手改文本。

108. **确需修改旧原文时必须单独说明理由并留档。** 只有在旧文本存在事实错误、或旧文本在新架构下无法成立时，才允许修改；修改必须记录：旧原文、新原文、修改原因、影响范围。禁止以「更通顺」「更统一」「更简洁」为理由修改。

109. **本章适用于全部后续迁移**，包括但不限于自动农场、星露谷、自动箱子、自动附魔、村民交易、图书馆管理员、传送、自动登入、自动采矿、绕过类模块、用户统计、界面汉化、界面主题、水源显示、指令说明。

---

## 十七、用户交互消息显示规范（固定铁律，2026-09-14 追加）

> 本章为固定铁律，优先级高于第十六章中与之冲突的条目。
> 第 102 条（模块播报前缀必须保留 `§c§l[yiyiaddon]§r§f§l[模块名]§r`）**已被本章取代**，前缀规则以本章第 110-113 条为准。

### 一、前缀显示规则

110. **消息前缀统一使用「当前模块名称」。** 禁止显示以下三类内容作为前缀或出现在前缀中：
    - `yiyiaddon`（模组自身名称）
    - 旧项目插件前缀
    - 内部项目名称

111. **模块名称作为前缀时的样式固定**：

    | 项 | 规定 |
    | --- | --- |
    | 颜色 | 白色（`§f`） |
    | 字体 | 加粗（`§l`） |

112. **加粗作用于整条消息，不限于模块名段。** 前缀与正文一律加粗（`§l`），模块名用白色 `§f`，正文按状态色（第 114 条）。

    示例：`[自动农场] 收割完成`
    其中 `[自动农场]` 与 `收割完成` **均为加粗**；`[自动农场]` 白色，`收割完成` 取成功色绿色。

    实现口径：`§f§l[模块名] ` 之后直接接状态色即可保持加粗（`§` 颜色码不重置加粗），无需重复写 `§l`；确需在正文中途解除加粗时才能使用 `§r`，且必须重新指定颜色。

113. **前缀格式固定为 `[模块名] 正文`**，方括号与模块名同属前缀，不得替换为其它标识。未归属任何模块的框架级消息（如指令框架自身提示），前缀使用承载该消息的功能名，同样禁止出现 `yiyiaddon`。

### 二、状态颜色规则

114. **颜色属于用户交互资产，禁止 AI 自行修改颜色含义。** 固定语义如下，八种状态与其用色一一对应，不得混用、不得互换、不得新增：

    | 状态 | 颜色 | 用色代码 | 适用范围 |
    | --- | --- | --- | --- |
    | 成功 | 绿色 | `§a` | 完成、成功、验证通过 |
    | 失败 | 红色 | `§c` | 执行失败、错误、无法完成 |
    | 异常 | 橙色 | `§6` | 异常情况、风险提醒、需要处理的问题 |
    | 信息 | 蓝色 | `§b` | 普通状态、流程说明、信息提示 |
    | 处理中 | 黄色 | `§e` | 等待、加载、执行中 |
    | 调试 | 紫色 | `§d` | 调试信息、开发诊断 |
    | 禁用 | 灰色 | `§7` | 未启用、不可用、无数据 |
    | 重要 | 青色 | `§3` | 重要提醒、核心状态 |

115. 上表用色代码与旧项目实际用色一致（旧项目已按此语义使用：成功 `§a`、失败 `§c`、异常 `§6`、信息 `§b`、处理中 `§e`、调试 `§d`、禁用 `§7`、重要 `§3`），且旧项目状态文字本就加粗（如 `§a§l已开启`），与本规范第 111、112 条一致。**禁止以「更好看」「更协调」为由更换配色或去掉加粗。**

116. **一条消息只用一个状态色。** 不得为了强调在同一条消息里堆叠多个状态色。字段值使用白色 `§f`，字段标签使用灰色 `§7`，同样保持加粗。

### 三、迁移规则

117. **旧项目已有的消息格式、前缀、颜色、状态文字，迁移时必须优先保持旧项目。** 禁止 AI 自行重写、禁止自行优化措辞、禁止自行改变颜色。

118. **只有旧项目不存在的新功能提示，才使用本章规范。** 新功能提示同样必须遵守第 110-116 条。

119. **前缀是本条唯一的例外**：旧项目前缀含 `yiyiaddon`，与第 110 条直接冲突，故按第 110 条执行（不显示 `yiyiaddon`），其余部分（模块名、白色、加粗、整条加粗）保持旧项目样式。

### 四、用户交互资产范围

120. 以下全部属于固定资产，禁止随意修改：

    - 指令名称
    - 指令参数
    - 状态播报
    - 聊天消息
    - 通知文本
    - 颜色
    - 前缀格式
    - 配置显示名称
    - UI 显示文字

121. **迁移时必须扫描旧项目。** 代码可以重构，用户体验不能随意改变。

122. **分类（模块分组）不属迁移范围，禁止按旧项目改动（2026-09-14 定稿）。** 分类是 yiyiaddon 自己界面的分组结构：功能模块最终都归入本项目的模块分组，旧项目的 5 个分类（工具 / 自动化 / 绕过 / 辅助 / 星露谷）属旧框架的界面产物，不是要搬运的用户交互资产。因此：

    - 禁止按旧项目增删分类、改分类名、改分类顺序、改分类样式；
    - 分类的划分、名称、样式与顺序一律由本项目界面设计决定；
    - 分类名中禁止出现 `yiyiaddon`（第 110 条）；
    - 本报告第四章「分类名称」清单仅作旧项目现状记录，不作为迁移依据。

    配套要求：模块的 `categoryId` 必须指向本项目已注册的分类，不得为迁就旧项目分类而新增分类。

---

## 十八、界面控件规范（2026-09-14 追加）

123. **禁止滑块控件。** 本项目全部数值型设置项一律使用「输入框带加减」控件（`ui/widget/SettingNumberBox`，结构 `[-] [ 数值框 ] [+]`：点两侧按钮按步进增减，也可点击数值框直接键入数字）。禁止使用滑块、禁止使用拖动条、禁止使用进度条式取值。旧项目框架的滑块类设置不属迁移资产，一律不搬。

124. 数值控件的四项参数必须与设置语义一致：

    | 参数 | 要求 |
    | --- | --- |
    | 最小值 / 最大值 | 必须覆盖该项设置的全部合法取值，不得靠使用处自行兜底 |
    | 步进 | 整数型为 1；比例型取最小可感知增量（如 `0.05`、`0.1`） |
    | 显示格式 | 带单位时把单位写进格式串（如 `%.1fx`、`%.2f`），数值框按该格式显示与回读 |

125. 已存在的滑块控件必须删除，连同其拖动状态的转发链路一并清理，禁止保留废弃控件类或空转发方法。新增设置项时，若需要数值输入，只能使用第 123 条规定的控件。

---

## 十九、界面自研铁律与参考边界（2026-09-14 追加）

> 本章解决一个反复出现的混淆：**旧项目的「文字与功能」要照搬，旧项目的「外观」禁止照搬。**
> 与第十六章（用户交互兼容铁律）、第十七章（消息显示规范）配合使用，三者冲突时以本章第 128 条的分层表为准。

126. **主题与按钮样式保持现状，不重新设计。** 本次界面工作的全部范围只有两件事：

    - 把旧项目的**按钮文字**（及页面全部文案）套入本项目现有主题；
    - 实现与旧项目**相同的交互方式**。

    | 资产 | 状态 | 禁止事项 |
    | --- | --- | --- |
    | 主题与配色（`ui/theme/ClickGuiThemeColors` 及各主题类） | **固定不变** | 禁止重新设计主题、禁止新增主题、禁止调整配色、禁止引入旧项目框架的主题体系（`GuiTheme` 系列） |
    | 按钮样式与尺寸（`ui/widget/Button`、`IconButton`、`ui/component/ButtonRow`） | **固定不变** | 禁止改动其视觉规格，禁止复制仿制旧项目框架的按钮控件（`WButton`）；**按钮大小由本项目自动适配，不参照旧项目的固定宽度与展开方式** |
    | 组件（`ui/component`、`ui/widget`、`ui/page`） | **固定不变** | 只准复用，禁止引入旧项目框架的任何界面组件（`WindowScreen` / `WTable` / `WMinus` / `WPlus` 等） |

127. **参考旧项目框架源码的边界固定为两点，超出即违规。**

    - **允许**：理解**交互结构与功能逻辑**。例如——选择器是「双栏 + 加减」还是「分组 + 勾选」；搜索按名称与技术 ID 哪些字段过滤；点选后是否立即生效、是否需要整表重建；有无「全选」「折叠」这类结构。
    - **禁止**：照搬**视觉与代码**。配色、圆角、字号、间距、控件尺寸、图标资源、类结构、方法命名，一律不得进入本项目。
    - **判定口径**：出现「旧框架是这样画的，所以我们也这样画」即为违规；出现「旧框架是这样交互的，所以我们也这样交互」即为合规。

128. **配置页面公式：旧项目的文字与交互，套入本项目现有主题。**

    | 层 | 来源 | 要求 |
    | --- | --- | --- |
    | 按钮文字、字段标签、区块标题、页面标题、提示与确认文案 | 旧项目源码 | 逐字复刻，含 `§` 颜色码，改一个字即不合格（第十六章） |
    | 交互方式（按钮点击行为、页面层级、跳转路径、操作对象） | 旧项目源码 | 逐项对齐，功能不得增删 |
    | 主题、配色、按钮样式、控件样式 | 本项目现有 | **保持现状**，不重新设计、不参照旧项目外观 |

    按钮尺寸与排布一律由本项目控件自动适配（`Button` 的尺寸档位、`ButtonRow` 的满宽或等宽平分），**不参照旧项目按钮的大小、固定宽度与展开方式**。

    一句话：**文字换、交互对齐，壳不动。**

129. **页面文本必须经 `ui/render/MinecraftText` 渲染。** 旧项目文案带 `§` 颜色码，禁止在绘制前手工剥离颜色码、禁止改写为纯文本、禁止把 `§` 原文替换为本项目的其它配色方案。

130. **禁止以「界面优化」「参考旧框架」「更统一」等任何理由重新设计主题或按钮。** 本次界面工作的边界仅限第 126 条的两件事。任何「顺便把主题重做一下」「顺手换个更好看的配色」「把按钮改成旧项目那样」的改动都属越界。若某交互在旧框架中有而本项目没有对应控件，做法固定为：按本项目现有设计新增自研控件，或判定该交互无需支持；禁止把旧框架的控件移植进来。

---

## 二十、独立窗口动画与跳转规范（2026-09-14 追加）

> 本章解决一个具体问题：**旧项目里「点一下按钮就弹出一个新窗口」的交互，新项目必须一一对应，且弹出与关闭的动画细节必须做全。**
> 与第十九章（界面自研铁律）配合使用：第十九章管「窗口里写什么字」，本章管「窗口怎么出现、怎么消失、谁能开谁」。
> 完整规格见 `02-开发报告/项目开发报告/01-规范与UI规格/13-UI-04-独立窗口动画与跳转规范.md`。

131. **独立窗口必须继承 `ui/screen/PanelScreen`，全项目无例外。** 该类负责面板几何、玻璃材质、圆角、标题栏、返回按钮、滚动、输入分发与开合动画；子类只允许在构造末尾调用内容助手（`addSectionTitle` / `addField` / `addButton` / `addButtons` / `addGap` / `addDivider`）填内容。

    子类禁止：自己画面板、自己算坐标、自己写 `canvas.save/restore`、自己处理滚动与命中。

132. **打开动画固定为「缩放 + 上浮 + 淡入」三件套，参数不得改动。**

    | 通道 | 起点 | 终点 | 曲线 |
    | --- | --- | --- | --- |
    | 缩放 | `OPEN_MIN_SCALE = 0.88` | `1.00` | `Easing.easeOutCubic` |
    | 上浮 | 下移 `ENTER_RISE = 16f`（设计单位） | 归位 | 同一进度 |
    | 透明度 | `0.0` | `1.0` | 同一进度 |

    时长 `PanelFrame.OPEN_DURATION = 0.16s`；圆角按 `CARD_RADIUS / animationScale` 反向补偿，缩放期间圆角视觉不变粗。上浮只作用于绘制，命中几何仍用 `PanelFrame.scale()`，不得引入第二套坐标。

133. **关闭必须是打开动画的严格反向播放，禁止瞬时切屏。**

    - 唯一入口是 `requestClose()`（返回按钮、`ESC`、操作成功后的自动关闭都走它），内部带 `closingRequested` 卫语句，重复调用不重复播放。
    - 关闭期间 `mouseClicked` 直接返回 `false`，不接受新输入。
    - 返回按钮不得在关闭瞬间消失：`backVisible = alpha > 0.01f`，跟随整体淡出。
    - 面板的 `animationAlpha()` 必须同步作用到全部元素（玻璃、边框、标题、返回按钮、内容、滚动条），不允许出现「面板淡出但某个元素还亮着」。
    - **禁止**用 `client.setScreen(xxx)` 直接切屏来「返回」。唯一例外是「打开另一个窗口」。

134. **独立窗口的内容必须逐项错峰入场。**

    `CompactStack.enterAnimation(true)` 提供该行为；参数固定为单项 `ENTER_DURATION = 0.18s`、错峰 `ENTER_STAGGER = 0.018s`、错峰序号上限 `MAX_STAGGER_INDEX = 10`。

    三条硬约束：

    - **只改绘制透明度**（`alpha * progress`），高度、绘制坐标、命中坐标一律不受动画影响，杜绝「看得到点不到」；
    - **默认关闭**，只有独立窗口显式开启；模块页等常驻界面保持即时呈现，避免每次切页整体闪一下；
    - **宿主必须每帧按 `update(dt)` → `draw(...)` 的顺序执行**；若从未 `update` 过，进度直接返回 `1`，保证内容正常显示（不允许出现整屏不可见的兜底失败）。

135. **控件动画只准复用既有实现，禁止为单个窗口另写一份。**

    | 动画 | 唯一实现 |
    | --- | --- |
    | 按钮/图标按钮悬停过渡 | `ui/widget/Button`、`ui/widget/IconButton` 内建（时间常数 `14f`） |
    | 按压回弹 | `ui/anim/PressState`（按下 `0.09s` 到 `0.97`，松开 `0.26s` 用 `easeOutBack` 回弹） |
    | 滚动缓动 | `ui/anim/ScrollTracker`（时间常数 `0.085s`，拖动滚动条走 `jumpTo` 跳过缓动） |
    | 缓动曲线 | `ui/anim/Easing` |

    需要新的动画语义时改公共控件，禁止在页面里内联动画状态。

136. **旧项目中「点击后弹出独立窗口」的按钮，新项目必须同样弹出独立窗口。**

    禁止改成同屏展开、禁止改成聊天栏输出、禁止改成内嵌面板。已扫描旧项目全部 `setScreen(new …)` 调用点，触发按钮与目标窗口固定为：

    | 触发按钮（逐字） | 目标窗口 | 出现位置 |
    | --- | --- | --- |
    | `更多管理` | `ID 更多管理` | `IdConfigModule` |
    | `§e使用说明` | `<模块名> - 使用说明` | `IdConfigModule` |
    | `§e查看使用说明` | `<模块名> - 使用说明` | 共 11 处：`AutoMinerModule.java:1509`、`AutoEnchantBook.java:606`、`AutoLoginModule.java:217`、`AutoVillagerTradeModule.java:693`、`AutoLibrarianModule.java:81`、`AutoFarmMatrix.java:700`、`FlightBypass.java:180`、`AntiKickBypass.java:338`、`ServerDetector.java:149`、`PacketInstantBreak.java:310`、`StardewResourceSetting.java:208` |
    | `手动添加物品 ID` | `添加物品` | `IdManagementScreen` |
    | `§c数据清理 >` | `ID 数据清理` | `IdManagementScreen` |
    | 6 项清空（含 `§c§l【最高危险】清空全部 ID 数据`） | 二次确认窗 | `IdDataCleanScreen` |
    | `清空全部点位` / `清除当前维度处理记录` / `清除全部处理记录` | 二次确认窗 | `AutoChestModule` |
    | `§c清空全部点位` | 二次确认窗 | `StardewConsoleScreen` |
    | `配置每种物品数量` | `ItemQuantityScreen` | `ItemQuantitySetting.java:119` |
    | `选择容器类型` | `ContainerTypeSelectScreen` | `ContainerTypeSetting.java:77` |
    | `选择附魔` | `EnchantmentSelectScreen` | `EnchantmentSelectSetting.java:55` |
    | `选择目标物品` | `ItemTargetSelectScreen` | `ItemTargetSetting.java:91` |
    | `配置装备附魔` | `GearEnchantScreen` | `GearEnchantSetting.java:55`（走 `Minecraft.getInstance().execute(...)` 延迟一 tick 再弹屏） |
    | `设置` | `StardewRenderObjectScreen` | `StardewRenderSetting.java:66` |
    | `点击选择` | `StardewTargetSelectScreen` | `StardewTargetSetting.java:160` |
    | 识别（聊天复制/显示模式） | `识别结果` / `实体识别结果` / `方块识别结果` | `IdIdentifyModule`、`IdCommand` |

    通用二次确认窗固定使用 `ui/screen/ConfirmPanelScreen`；确认按钮文案默认 `§c§l确认`、取消 `§7取消`（旧项目 `ConfirmScreen` 原文，清空类操作用 `§c§l确认清空` / `§c§l确认全部清空`）。

    **落地状态（2026-09-14 核对，禁止当成「已全部对齐」）**：

    - 已在新项目落地：`更多管理`、`§e使用说明`、`手动添加物品 ID`、`§c数据清理 >`、6 项清空（二次确认）、识别结果三窗。
    - **未落地：`选择目标物品`**。该按钮与新项目对应的双栏多选窗**尚不存在**；数据层 `config/identity/IdentityTargetConfig` 已齐（含计数文案与失效清理），缺的是 UI 入口。已登记为第 08 阶段任务，见《2026-09-14-追加类-世界渲染基建与独立窗口体系记录》3.3 问题 1。
    - **补充（2026-09-14 更正）**：`选择目标物品` 属「ID 三件套 ⇄ 自动箱子」联动链的一环，**不是可选项**。旧项目该控件定义在 `itemid/ItemTargetSetting.java:91`，但由自动箱子的「目标物品」配置项承载（`autochest/config/AutoChestSettings.java:191-195`），自动箱子取物时按选中身份匹配；ID 增删后由 `AutoChestModule.java:107-108` 的监听器 → `ItemTargetSetting.pruneInvalid():48-57` 自动同步。契约全文见《2026-09-14-旧项目基准-05-逐模块依赖全量清单》§0.8。
    - 其余各行（`配置每种物品数量`、`选择容器类型`、`选择附魔`、`配置装备附魔`、`设置`、`点击选择` 等）的触发模块本身尚未迁移，随对应模块落地时一并接入，当前既不得视为已对齐，也不得提前实现。

137. **「使用说明」窗口固定使用 `ui/screen/HelpPanelScreen`，格式不得改动。**

    旧项目全部模块共用同一个 `ui/HelpScreen`：标题 `module.title + " - 使用说明"`，正文由 `buildHelpContent(sections)` 生成，底部一条分隔线加满宽 `关闭` 按钮。

    正文格式固定为（已逐字核对旧项目源码）：

    ```
    §8┏ + ━ × 51 + ┓
    §8┃ §3§l> §b§l使用说明 + 空格 × 38 + §8┃
    §8┗ + ━ × 51 + ┛
    （空行 → 分隔线）
    §3[§b#§3] §f<章节标题>
      正文行…
    ```

    每条章节在生成数组中占 `1 空行 + 1 标题 + 正文行数`。禁止改动框线长度与标题行空格数、禁止省略空行对应的分隔线。

    **两套「说明」格式必须分清，不得混用**（来源：旧项目规范正本 `convention/YiyiaddonConvention.java` 第 5.7 / 5.13 节）：

    | 场合 | 载体 | 格式 |
    | --- | --- | --- |
    | 模块面板内的**内嵌信息块** | `getWidget()` 返回的 `buildInfoWidget(...)` | 标题 `§l模块名 · 使用说明`；小节 `§{色}§l▌ 小节名`（黄 `§e` 准备 / 绿 `§a` 功能 / 青 `§b` 参数 / 粉 `§d` 模式 / 红 `§c` 注意）；条目 `§f  1. 步骤`、`§f  · 要点`；按钮一律铺满整行（`expandX().minWidth(200)`） |
    | 点击按钮弹出的**独立说明窗口** | 旧项目 `ui/HelpScreen` → 本项目 `ui/screen/HelpPanelScreen` | 本节上方的框线 + `§3[§b#§3]` 章节格式 |

    即：`▌` 只属于内嵌信息块，`[#]` 只属于独立说明窗口，禁止把两者格式对调。旧项目规范正本第 5.13 节同时规定了说明窗口的配色语义：`§3` 标题与章节标识、`§b` 副标题与重点、`§f` 正文、`§7` 补充、`§8` 树形线条、`§e` 数值与方式标识、`§a` 流程步骤、`§6` 参数建议、`§d` 可选功能、`§c` 注意事项。

138. **窗口跳转关系只准写在一处导航类里，且必须与旧项目逐条对齐。**

    每个功能模块建一个 `XxxScreens`（现有：`feature/identity/ui/IdScreens`），集中定义「哪个窗口打开哪个窗口」「哪个窗口返回哪个窗口」。

    - 业务模块、页面、指令只调导航类的静态方法，禁止 `new XxxScreen(...)` 直接建窗口；
    - 跳转目标必须来自旧项目源码，禁止自行设计层级。旧项目本身就不对称，必须照抄——例如 `IdManagementScreen` 的 `§7返回` 是 `setScreen(null)`（回游戏），而 `IdDataCleanScreen` 的 `§7返回` 是 `openManagementScreen()`（回更多管理）；
    - 关闭语义二选一，不得混用：**回游戏**的窗口在构造里调用 `PanelScreen.exitToGame()`（使用说明、添加物品、二次确认、三个识别结果屏）；**返回上级窗口**的窗口按默认行为回到构造时传入的 `parent`（仅 `ID 数据清理` 一处）；
    - 关闭动作统一走 `requestClose()`（先播关闭动画再切屏），禁止直接 `setScreen` 切屏。

139. **禁止凭印象或凭既有报告转述来「对齐旧项目」。**

    任何以「与旧项目一致」为理由的改动，动手前必须先读旧项目的源码原文；报告与铁律中的每一条文案、按钮文字、字段标签、跳转关系，都必须能指回旧项目的源文件与行号。

    判定口径：说不出「这句话来自哪个文件的哪一行」即为不合格，必须先扫描源码再写结论。

    两个必读位置：

    - 旧项目源码根：`D:/mcaddon/26.1.2/src/main/java/com/example/addon`
    - 旧项目**规范正本**：`convention/YiyiaddonConvention.java`（纯注释类，写明了消息格式、面板结构、按钮铺满规则、说明窗口规范、配色总表、符号库等既有约定）。动手前必须先读它，它是旧项目自己的成文规范，优先级高于任何二手报告。

140. **`§` 文案里的符号必须先确认字体覆盖，缺字形由 `FontRenderer` 兜底。**

    实测结果（`harmony.ttf` / `harmony_bold.ttf`）：

    | 符号 | 码点 | 鸿蒙字体（实测该字符本身） | 用途 |
    | --- | --- | --- | --- |
    | `━ ┃ ┏ ┓ ┗ ┛ ┣ ┫ ┠ ┨ ├ └ ─ │` | 制表符区 U+2500–257F 内的这 13 个 | **有** | 使用说明标题框、章节树状缩进 |
    | `▸` | U+25B8 | **缺** | 字段行分隔符 `§7标签 §8▸ §f值` |
    | `✓` `✗` | U+2713 / U+2717 | **缺** | 反馈前缀 `§a§l✓` / `§c✗` |
    | `⚠` | U+26A0 | **缺** | 使用说明警示行 |

    上表为**逐字符实测结果**，不是区块结论：制表符区只验证了列出的 13 个字符，其他制表符、其他符号一律按「未验证」对待，新增前必须实测。

    `ui/render/FontRenderer` 已内建逐字符兜底：主字体无该字形时经 `FontMgr.matchFamilyStyleCharacter` 取系统兜底字族，且**绘制与测量共用同一套分段规则**（否则会出现文本超宽或按钮文字不居中）。

    禁止的处理方式：把 `▸` 换成 `>`、把 `✓` 改成 `[OK]`、把 `⚠` 去掉——都属于改写旧项目文案（违反第 129 条）。新增符号前先用字体覆盖工具确认，不得凭感觉使用。

141. **界面配色只准来自 `ClickGuiThemeColors`，组件与页面内禁止出现颜色字面量。**

    颜色的唯一入口是 `ui/theme/ClickGuiThemeColors`；组件、页面、独立窗口一律通过 `ClickGuiThemeColors.current()` 取色。

    - **颜色字面量只允许出现在两处**：各主题类（`DarkTheme` / `WhiteTheme` / `VapeTheme` …）与 `ClickGuiThemeColors` 本身。
    - **组件私有配色必须先派生为具名色槽**，再由组件读取。禁止在组件里写 `tc.dark ? 0x777777 : 0x555555` 这类内联三元，也禁止把同一组颜色在多个组件里各抄一份（曾出现 `KeybindBadge` 与 `SettingModule` 重复同一组快捷键徽章配色，已收编为 `keybindBackground` / `keybindHoverBackground` / `keybindUnbindBackground`）。
    - **默认色必须跟随主题**。组件里表示「未指定颜色」用哨兵值（如 `-1`），绘制时回落到主题色槽；禁止写死默认灰（曾出现 `ListRow.badgeColor = 0xAAAAAA`，已改为回落 `tc.secondaryText`）。
    - 位掩码（`0x00FFFFFF`）与材质合成参数（如 `SkiaBlurRenderer.FROST_DARKEN`）不是配色，但必须提为具名常量，不得散落魔法数。
    - **主题与配色固定不变**（第 126 条）：本条的「统一」是「收拢到主题入口」，不是「重新调色」。禁止借统一之名改动任何色槽的取值。

    **两套颜色的边界**（不得互相污染）：

    | 用途 | 来源 | 规则 |
    | --- | --- | --- |
    | 界面配色（面板、按钮、滚动条、徽章…） | `ClickGuiThemeColors` | 本条 |
    | 文案颜色（`§a` `§c` `§e` …） | 旧项目原文的 `§` 色码 | 第 114 条八级状态色 + 第 129 条 |

    禁止用主题色去改文案颜色，也禁止用 `§` 色码去充当界面配色。

---

## 二十一、世界空间渲染基建（2026-09-14 追加）

142. **世界空间渲染基建固定放在 `ui/render/world/`，禁止各模块自写一套投影与画布管理。**

    ```
    ui/render/world/
        ShapeMode.java        渲染模式枚举（线框 / 面 / 两者）
        ColorPresets.java     颜色预设（9 色）+ 填充默认透明度
        EspColor.java         ESP 颜色值：RGB + 透明度 + 彩虹开关（可变对象）
        Rainbow.java          彩虹色相循环，绘制瞬间按单调时钟取值
        Dir.java              方块面方向位掩码，供面剔除使用
        Visibility.java       方块视线遮挡判定（射线近似）
        RenderCamera.java     每帧相机快照：位置 + 视图×投影矩阵 + 屏幕尺寸
        ScreenPoint.java      投影结果：屏幕坐标 / 可见性 / 深度
        WorldProjector.java   世界→屏幕投影，含近平面裁剪
        EspRenderer.java      绘制 API：方框 / 面 / 线条 / 射线 / 文字
        WorldOverlay.java     注册中心 + 帧钩子
    ```

    模块只调 `WorldOverlay.register(模块ID, renderer -> ...)`，在 `onEnable` 注册、`onDisable` 注销。
    禁止模块自行获取 Framebuffer、自行建 Skija 后端、自行算投影矩阵、自行处理近平面裁剪。

143. **`ShapeMode` 的设置项标签逐字取自旧项目 `stardew/render/EspShapeMode`：`线框` / `面` / `两者`。**
    禁止改写为「线条 / 填充 / 线条+填充」等自拟文案（第 128 条：设置项显示中文属用户交互资产）。

144. **渲染时机固定由 `mixin/client/GuiRendererMixin` 注入 `GuiRenderer#render` 的 HEAD。**
    此时世界已画完、原版 GUI 未画，ESP 因此压在世界之上、被 GUI 正常遮挡。
    禁止改到 `RenderTarget#blitToScreen`——那会让 ESP 盖在原版 GUI 之上。

    画布必须用 `SkiaGlBackend.begin(SkiaGlBackend.mainFramebufferId())` 显式指定主 RenderTarget 的
    Framebuffer，禁止用无参 `begin()`（即 Framebuffer 0）。此刻默认 Framebuffer 不是呈现目标，
    画上去的内容会被帧末的 `blitToScreen` 整体覆盖，表现为「层已注册、画面什么都没有」。这一点与
    本项目全部界面窗口的用法一致（第 142 条：画布管理只有一个入口）。

145. **世界空间渲染的坐标系口径固定为 GUI 缩放坐标。**
    `SkiaGlBackend` 已在画布上应用 `guiScale`，投影输出必须与之对齐。
    禁止混用实际像素坐标，禁止在模块里再做一次缩放换算。

146. **ESP 类模块的颜色必须走 `ColorPresets`，禁止在模块里写 RGB 字面量。**
    这是第 141 条在渲染侧的对应规则：颜色只有一个入口。

147. **世界空间渲染的既定技术路线：投影 + Skija，禁止改用自建 RenderPipeline。**
    依据（26.1.2 实测，非推测）：`RenderType.create` 是**包私有**（无法创建自定义 RenderType）；
    `RenderSetup.RenderSetupBuilder` **没有深度设置**；原版 `RenderPipelines` 里的 `LINES` /
    `DEBUG_FILLED_BOX` 等**全是带深度的，没有无深度版本**；自建管线必须自带 GLSL 着色器资源并
    调 `device.precompilePipeline` 预编译。

    本路线的已知代价（必须如实告知使用者，禁止掩饰）：

    - 线宽是**屏幕像素常量**，不随距离变化（不是世界单位线宽）；
    - 填充面之间不做深度排序（单个盒子的六个面已处理，多盒交叠时压盖顺序可能不理想）；
    - 无法做到「被方块遮挡时隐藏」（透视是天然结果，也是唯一形态）。

    将来若确实需要世界单位线宽、模型描边或后处理轮廓，**必须作为独立阶段先立规则再动手**，
    禁止以「顺便优化」为名改掉路线。

148. **新增模块分类必须同时改三处**，缺一不可（第 77 条）：

    1. `com.yiyiaddon.module.AddonModules#registerCategories()`；
    2. 该模块构造器里的 `categoryId`；
    3. 本文件对应条目（记录分类 ID、中文名、图标、权重）。

    **已注册分类「工具」**：`utility` / `工具` / 图标 `\uE869` / 权重 `10` / 说明「管理通用工具功能」，
    承载管理员检测等通用工具模块。权重 10 排在最前（现有：工具 10 / 辅助 20 / 自动化 30 /
    Baritone设置 40 / 战斗 50 / 星露谷 60 / ESP 80）。图标 `\uE869` = Material Symbols 的 `build`（扳手），
    已按第 140 条用脚本解析 `MaterialSymbolsRounded.ttf` 的 cmap（format 4）与 `post` 表字形名验真存在。

    **已注册分类「自动化」**：`automation` / `自动化` / 图标 `\uE5D5` / 权重 `30` / 说明「管理全自动挂机功能」，
    承载自动挖矿、自动重生等常驻自动化模块。

    **已注册分类「战斗」**：`combat` / `战斗` / 图标 `\uE3E7` / 权重 `50` / 说明「管理战斗辅助功能」，
    承载杀戮光环等战斗辅助模块。

    **已注册分类「视觉」**（2026-09-18 随首个承载模块「透视」启用，按上述三处一并登记）：
    `visuals` / `视觉` / 图标 `\uE3B4` / 权重 `70` / 说明「管理世界标记与显示功能」，
    承载透视等世界标记与显示类模块。图标原预留值 `\uE8F4` 与已注册的「ESP 全局设置」分类重复，
    故改用 `\uE3B4`（Material Symbols：`center_focus_strong`；已按第 140 条用脚本解析
    `MaterialSymbolsRounded.ttf` 的 `cmap` 与 `post` 表验真，并与既有 12 个模块图标、7 个分类图标
    比对无占用冲突）。

    禁止在没有承载模块时提前注册分类——`ModuleCenterPage` 显示 `CategoryRegistry.all()` 的**全部**分类，
    不过滤空分类，提前注册会让界面出现点进去什么都没有的空分组。

149. **「被遮挡时隐藏」用 `EspRenderer.occlusion(true)` 逐目标开启，默认关闭（透视）。**

    本路线没有深度缓冲，遮挡只能靠 `Visibility` 从相机向目标点做体素射线检测
    （`ClipContext.Block.VISUAL`）。这是**近似**：单点判定，半掩在墙后的目标会被整体判为被遮挡。

    旧项目与绝大多数 ESP 的默认语义都是透视，禁止把遮挡做成全局默认。

    性能口径：判定成本为「每个图形一次射线」。目标成百上千时，必须在模块侧先做距离裁剪或分帧，
    不得把无上限的集合直接丢给渲染层。

150. **ESP 颜色统一用 `EspColor` 承载，彩虹在绘制瞬间解析，禁止模块为变色自己维护计时器。**

    `EspColor` = RGB + 透明度 + 彩虹开关 + 彩虹速度 / 相位偏移。`argb()` 在调用时按单调时钟算色相，
    因此刷新率跟随帧率、不受刻率限制，模块也不需要写 `onTick` 去更新颜色。
    未开彩虹时 `argb()` 是纯位运算，无时间开销。

    禁止各模块自建 `Color` / `SettingColor` 之类的第二套颜色类（第 146 条：颜色只有一个入口）。

151. **调色板固定用 `ui/screen/ColorPickerScreen`，设置项固定用 `ui/widget/SettingColorPicker`。**

    内容顺序固定：**预览 → 色相条 → 饱和度亮度面板 → RGBA 数值 → 彩虹开关与速度 → 关闭**。
    拾色组件固定在 `ui/component/`：`ColorPreview` / `HueStrip` / `SaturationBrightnessPad`。

    调色板直接改传入的 `EspColor`，**没有「确定」按钮**，关闭窗口即生效。

152. **手动取色必须同时关闭彩虹。**

    点色相条、点饱和度亮度面板、改任一 RGBA 通道，都要把 `rainbow` 置为 `false`。

    理由：彩虹开启时颜色由时间驱动，手动调的值会被下一帧立刻覆盖，用户看到的是「调了没反应」。
    这条是必须消除的交互歧义——宁可让用户手动调色时失去彩虹，也不能让取色看起来失效。

153. **新增界面控件前，必须先找到项目中同类的既有控件，逐项对齐尺寸、圆角、边框取色与内边距。**

    判定口径：说不出「对齐的是哪个类的哪一处」即为不合格。

    反面案例（2026-09-14 实际发生，已修）：调色板组件被写成**直角矩形 + 硬编码纯白边框**，
    而项目既有的 `SettingColorPreview` 用的是 `theme.border` 的 24% 透明度 + 圆角 `8f`。
    这是第 130 / 141 条被违反的具体形态——**「我觉得这样更好看」就是重新设计主题**。

    同类控件对照关系（新增前先看这些）：

    | 要做的控件 | 先看 | 对齐什么 |
    | --- | --- | --- |
    | 颜色类 | `ui/widget/SettingColorPreview` | 圆角 8、边框 `border×0.24`、尺寸 100×24 |
    | 开关类 | `ui/widget/SettingToggle` | 轨道/手柄取色与尺寸 |
    | 输入类 | `ui/widget/SettingTextBox` | 弱底 `field`、下划线 `border`、光标取色 |
    | 按钮类 | `ui/widget/Button` | 变体与尺寸档位，尺寸自动适配不设固定宽度 |
    | 独立窗口 | `ui/screen/PanelScreen` 子类 | 骨架、开合动画、返回按钮 |

154. **界面与渲染的「配色」禁止出现字面量，含 `0xFFFFFF`、`0x080808` 这类通用黑/白。**

    「白色高光」「黑色描边」不是豁免理由。项目已在主题里提供对应语义色槽：
    高光用 `rim`、阴影/描边用 `shadow`、硬对比前景用 `pickerIndicator`、
    透明度棋盘格用 `checkerLight` / `checkerDark`。确需新语义时，先在
    `ClickGuiThemeColors.of()` 里加派生色槽（按明暗自适应），再在组件里引用。

    以下三类**不算配色**，允许出现，但必须具名并注明用途：

    - **材质合成参数**：玻璃冰霜高光（`GlassPanel` 的白色渐变）、模糊压暗（`SkiaBlurRenderer.FROST_DARKEN`）；
    - **位掩码**：`0x00FFFFFF` 这类取通道的位运算；
    - **API 兜底默认值**：颜色对象为 null 时的返回值，须提为具名常量
      （如 `EspRenderer.FALLBACK_LINE` / `FALLBACK_SIDE`）。

    文字颜色属于另一套，边界见第 141 条的表。

155. **滚动偏移的口径固定为「宿主并入 y」，禁止用 `canvas.translate` 平移内容区。**

    既有体系的口径是：元素起点（屏幕坐标）= 内容顶部 − 滚动偏移。

    - `BasePage`：`draw/onClick/onDrag` 都接收 `scrollOffset`，内部用 `cy = y - scrollOffset` 算位置；
    - `CompactModulePage`：把 `y - scrollOffset` 直接传给 `CompactStack`。

    鼠标坐标与可见边界一律用**屏幕坐标**，不做换算。禁止出现第二套语义
    （例如「画布平移 + 内容坐标视口」）——同一个组件被两种口径调用必然出错。

156. **`CompactStack` 的坐标口径：`y` 与 `visibleBottom` 都是屏幕坐标。**

    宿主负责把滚动偏移并入 `y`；鼠标坐标原样传入、不做任何换算。
    新增调用方前先看 `CompactModulePage` 怎么传参，禁止自行解释参数含义。

157. **拖动事件只发给「按下时命中的元素」，禁止按当前鼠标位置重新查找。**

    `CompactStack` 在 `onClick` 命中时锁定 `active` 与 `activeY`，`onDrag` 只转发给它；
    宿主必须在 `mouseReleased` 调用 `releaseDrag()` 解锁。

    反面案例（2026-09-14 实际发生，已修）：拖动色相条时鼠标划到下方的饱和度面板，
    事件被转给了面板，用户看到的是「拖一下就改了另一个值」。

158. **注释必须与实现一致；说出「复用/避免分配/已优化」就必须真的做到。**

    反面案例（2026-09-14，已修）：`EspRenderer.drawPolygon` 注释写「用复用数组构造，避免逐面分配点对象」，
    实际每次调用仍 `new Point` 四次——复用的只是数组外壳。（已确认 Skija 的 `Point`/`Path` 不可变，
    做不到零分配，于是改为如实描述。）

    评审口径：注释里的性能声明一律当作断言，与实现不符即缺陷，不是「笔误」。

159. **查 API 必须按 `01-开发参考库/README.md` 第三章的查询顺序，禁止跳步。**

    顺序固定：**快速索引 → API参考 → 开发机制 → 原始源码**。任一步找到答案就停下，不得跳到猜。
    冲突裁决：原始源码 > API 文档 > 快速索引 > 模型记忆。
    资料库查不到某个 API 不代表它不存在，须继续 Grep 原始源码；查到了也建议复核一次。

    反面案例（2026-09-14 实际发生，已补修）：做世界渲染时跳过前三步，直接读源码、还用了资料库未指定的旧工具，
    漏掉资料库早已写明的现成结论（`API参考/` 渲染篇列全了双色 `line` / `boxSides` /
    `gradientQuadVertical` / `quadVertical` / `quadHorizontal`），既缺能力又差点把参数顺序写反。

    资料库指定工具（不要用别的）：

    ```powershell
    node "01-开发参考库/工具/查JARAPI.js" LocalPlayer          # 查类全路径 + 字段 + 方法
    node "01-开发参考库/工具/查JARAPI.js" LocalPlayer sendSys  # 类内搜方法
    node "01-开发参考库/工具/查JARAPI.js" --找 sendCommand     # 全局搜方法
    ```

160. **渲染 API 的参数顺序对齐既定基准：盒子类一律「先填充色、后描边色」。**

    `EspRenderer` 的 `box` / `box2D` / `blockBox` / `boxSides` 全部沿用该顺序
    （形如 `box(AABB, sideColor, lineColor, mode, excludeDir)`），迁移旧模块时可照抄调用。

    禁止以「本项目自己设计 API」为理由调换参数顺序：**颜色传反是静默错误**——
    不报错、不崩溃，只在画面上显示成错的颜色，排查代价极高。

161. **新项目的代码、注释、界面与对外文案禁止出现旧框架的名称。**

    提旧项目时统一写「旧项目」「旧项目框架」，不得写出具体框架名。
    参考只发生在开发过程中，产物里不留痕迹。

    三类例外（必须原样保留）：

    - `meteordevelopment` 包名——它是「零依赖检查」的检索目标（第 32 条），抹掉就没法 grep 验证；
    - 旧项目的**类名**与**用户可见原文**——属用户交互资产，按第 98 条逐字保留，禁止改写；
    - 开发报告里作为**事实依据**的引用（资料库路径、旧项目文件路径）。

---

## 二十二、迁移总纲：全量复刻 + 只换外壳（2026-09-14 定稿）

> 本章为迁移口径正本。与其它章节或任何报告冲突时，以本章为准。完整展开见 `02-开发报告/项目开发报告/00-总纲与基准/01-总纲-迁移口径与复刻原则.md`。

162. **旧项目的功能、模块、互相关联全部复刻，一样不少；唯一变化是 UI 外壳。新项目自研，彻底脱离旧框架。**

    展开：

    - **搬什么（1:1 复刻）**：模块与分类、模块间关联（依赖边 / 监听器 / 回调链 / 共享底座 / 数据同源）、用户交互文本、指令、自检、数据与结构（身份库 / 点位 / 快照 / 记录 / 资源索引 / 报告落盘）、行为（状态机 / 触发时机 / 坐标归一化 / 跨服跨维度隔离语义）、配置项。逐字依据为六份基准（`02-基准-01` ~ `07-基准-06`）。
    - **变什么（仅外壳）**：渲染、页面骨架、控件、主题换成本项目自研体系；**按钮文字与点击作用逐字照旧，一个字不改、作用不差**（按第 96、100、105 条）。
    - **不复刻什么（唯一例外）**：第 43 条清单、第三方框架层（第 32、34 条）、旧项目 `run/` 与 `build/` 目录。
    - **作废声明**：此前报告中「建议不迁移」「暂不迁移」「按口径排除」某模块的表述（含星露谷 / 自动箱子 / 自动附魔 / 自动农场 / 战术等）均为定稿前的待决项，自本条起**一律作废**，相关模块全部纳入迁移范围。
    - **关联不变**：基准里登记过的依赖与回调，迁移后语义与效果仍然成立（可用新架构表达，但不得改变）。

---

## 二十三、改动禁区（2026-09-14 用户指令，最高优先级）

> 本章为硬约束，**优先级高于本文件其它所有章节**，也高于任何报告与既有实现。与本章冲突的一切做法一律作废。

163. **项目完成之前，禁止擅自新增任何东西。**

    禁止新增的范围包括但不限于：功能、模块、指令与子命令、服务、工具类、界面页面与控件、设置项、用户可见文案、脚手架与测试代码、依赖。

    - 旧项目已有的 → 按基准复刻，不算新增。
    - 旧项目没有、但框架运行绝对必需的 → **必须先向用户说明「旧项目对应物是什么 / 为什么没有它就跑不起来」，取得同意后才能写**。
    - 判定标准：**说不清旧项目对应物、也说不清框架必需性的代码，一律不得进入代码库。**

164. **非必要不得擅自修改；确需修改必须先经用户同意。**

    只有两种情形可以提修改：

    - 修复缺陷（bug）；
    - 因 API 变更、编译或运行所必需的适配。

    即便属于这两种情形，也必须先说明「改什么、为什么、影响面」，**得到用户同意后**再动手。

165. **一切改动都必须先经用户同意。**

    新增、删除、重命名、重构、结构调整、文案改动、依赖变更 —— 动手前必须先提出并取得用户明确同意；未获同意的改动一律视为违规，须立即回滚或改为旧项目原样。

    - 「顺手优化」「顺便整理」「统一风格」「我觉得这样更好」**都不是理由**。
    - 存疑时一律先问，不得自作判断。

166. **违规处置。**

    发现已发生的擅自改动，立即停手，产出清单（文件:行 + 原状 + 现状态 + 旧项目对应物），按用户指示回滚或改为旧项目原样，并在 `02-开发报告/项目开发报告/` 登记。

171. **移植不得夹带新功能；要新加必须先问我，不许乱搞。**（2026-09-15 用户指令追加）

    以「移植 / 迁移 / 复刻」为名义的一切改动，只允许把旧项目已有的东西搬过来，**不允许顺手加任何旧项目没有的功能**。

    - **判定基准只有一条**：旧项目的真实源码里有没有（按第 35、36 条，以真实文件内容与调用关系为准，不以报告摘要、清单或 AI 记忆为准）。
    - **「旧项目没有，但它更好用 / 更现代 / 更统一 / 别人都这么做」不是理由**，一律不加。
    - **要加就必须先问**：先说清「旧项目对应物是什么（没有就直说没有）／为什么非加不可／影响面（哪些文件、哪些用户可见文本、哪些落盘结构）」，**取得用户明确同意后才能写**。未获同意就写 = 违规，按第 166 条处置。
    - **已发生过的典型违规形态**（逐条自查）：为某个动作自造界面按钮；自造统计块与状态文案；把一次性逻辑拆成额外的服务类；为「一致性」给所有物品补一条分类判据；自造设置项与设置页控件；移植时顺手「补一个更完整的实现」。
    - **正确动作是留白并登记**：移植途中遇到旧项目没有的东西，写进该阶段报告的「未搬 / 差异」清单并提请用户拍板，**不要自己补一个**。

> 已发生并已处理的违规：`.resource` 指令（越权新增，已删，见 `26-审计-自造内容清单与处置.md`）、3 个未注册主题类（已删）、ID 模块 20 条自造文案（待处理）、分类结构擅自改动（2026-09-14 已改回旧项目名与顺序）。

---

## 二十四、差异核对与取证铁律（2026-09-15 追加）

> 本章针对一类反复出现的事故：**凭印象判定「新项目与旧项目不一致」，然后按印象改代码。**
> 触发事例：用户一次性报出 6 条「ID 三件套 bug」，逐条取证后只有 2 条是真移植偏差，3 条旧项目本来就一样，1 条是新项目自建能力的副作用。
> 完整对账记录见 `02-开发报告/项目开发报告/03-复盘/34-复盘-ID三件套差异核对与修复.md`。

167. **修复前必须取证。** 任何以「与旧项目不一致」为由的改动，动手前必须拿出硬证据，证据来源限定为四类：

    | 证据类型 | 用途 |
    | --- | --- |
    | 旧项目源码 `文件:行号` ＋ 原文摘录 | 判定两边的判据是否同源 |
    | 旧项目 git 历史（`git log -S` / `git show`） | 判定「旧项目一直如此」还是「被改过」 |
    | 真实运行产物（存档 JSON、日志、截图） | 判定现象的确切成因 |
    | 可复现实验（例如用真实 Brigadier 复刻命令树跑候选） | 判定机制，而不是猜机制 |

    判定口径：说不出「这句话 / 这个行为来自哪个文件的哪一行、哪次提交、或哪个真实文件」即为不合格，必须先取证再动手。

168. **每项差异必须强制二分：真移植偏差 / 旧项目本来就一样。**

    - **真移植偏差** → 按旧项目口径修正，属第 164 条的「修复缺陷」情形。
    - **旧项目本来就一样** → **不得当作移植 bug 静默修掉**。这意味着「修」等于修改旧项目既有行为，越过第 163~165 条，必须单独列出并提请用户拍板。
    - 报告必须逐条写明属于哪一类及其依据，**禁止只写「已修复」**。

169. **同源逻辑两边必须逐字比对，禁止各自演化。**

    两个项目共有的关键判据（如 `ItemIdentity#isCustom`、`BlockIdentifier#identify`、识别模式分流）属同源逻辑：任一方改动后必须同步另一方，并逐字校对一遍。判定同源的方法是**比对方法体**，不是比对类名。

    已发生的事故形态：新项目在识别模式之外自造第二份判据 `IdentityModuleConfig#savesToLibrary()`，与旧项目的单一判据方向相反，两个入口各判一次、互相矛盾。同类问题一律按本条处理——**收敛为唯一判据**，并删除多余那一份。

170. **本轮定稿的实现口径（覆盖新旧两个项目，后续一律照此执行）：**

    - **中文参数**：客户端指令的含中文字段只能使用自研整词 `ArgumentType`（读到下一个空格或行尾，不做字符校验）。**禁止使用 `StringArgumentType.word()` / `string()`**——它们只认 ASCII `[0-9A-Za-z_.+-]`，中文整段标红，且解析失败会让补全上下文塌回上一层节点，导致 TAB 把已输入的整词替换成列表里被高亮的别的条目。
    - **补全候选**：一种解析形式只给一套候选，**禁止为同一目标额外生成同义重复候选**（如既给 `黄瓜` 又给 `"黄瓜"`）。否则打全之后再按 TAB 会把已输入的文本改写成另一种形式，表现为「TAB 把我的字改掉了」。
    - **识别模式分流**：只有「自动保存」落盘；其余模式（聊天复制/显示、准星方块识别）一律弹对应结果窗口、识别本身不写盘。该判据**只允许存在一处**（新项目 `IdIdentifyModule`，旧项目 `IdConfigModule#identifyMode`），指令、面板按钮等所有入口一律复用。
    - **方块命中**：`BlockIdentifier#identify()` 必须同时拦截 `hit.getType() != HitResult.Type.BLOCK` 与 `state.isAir()`。准星朝天空 / 朝空气时 `mc.hitResult` 仍是 `BlockHitResult` 且类型为 `MISS`，只判 `instanceof` 会把空气当成命中方块写出 `minecraft:air`。
    - **原版 / 自定义判据**：`itemName`（服务器显示名组件）**与注册表默认名相同时不构成自定义证据**。大量服务器会给物品挂上与默认中文名完全相同的 `item_name`，只看「非空」会把整片原版物品误判成自定义。
    - **列表分组**：分组标题在**空分组时也必须渲染**，并在组内补一行空态（旧项目 `ItemTargetSelectScreen` 口径）。禁止「有内容才建组」，那会让标题随内容一起消失。

---

## 二十五、移植模块设置持久化铁律（2026-09-15 用户指令追加）

> 触发事例：星露谷渲染对象（控制台「点位」→「显示与颜色」）改完设置，重启客户端后回到默认；用户判定「所有移植过来的模块都这样」，要求写成铁律。
> 已取证的现场证据：`run/config/module-state.json` 里星露谷的设置块仍是**旧对象名**（`render.农田边界.*` / `render.农田起点.*` / `render.农田终点.*`，个别还有已删除的 `regionPlanting`），而当前代码里的对象名是「种植区域」等——**写入键与读取键不是同一套**，属第 174 条形态。

172. **移植过来的每个模块，改过的设置必须重启客户端后仍然生效。** 本项对全部移植模块一律适用（ID 三件套、自动箱子、星露谷，以及后续的自动挖矿 / 自动附魔 / 村民交易 / 传送 / 登录等）。

    验收口径（唯一口径，不接受「界面上看着是好的」）：

    1. 任选一项设置改成非默认值（勾选、数值、颜色、渲染模式都算）；
    2. **完全关掉客户端**，重新打开；
    3. 该设置仍是改后的值 —— 做不到即视为该模块的**移植缺陷**，必须修到能用为止，不得记为「已知问题」放过。

    运行期状态（点位、区域、选择集合、画面设置之外的会话数据）按各自的 ServerKey 存档隔离，不适用本条；本条只管**模块设置**。

173. **界面上的每一次设置改动都必须走到落盘，禁止「只改内存」。**

    - 统一走 `ModuleManager.saveSettings(module)`（模块实现 `loadSettings` / `saveSettings` 两个字段编解码方法），禁止模块自己另写一套文件读写。
    - 新增任何可改设置项时，**改动的那个回调里必须调用保存**；只 `object.show = value` 而不保存，等于重启即丢，属缺陷。
    - 模块的 `saveSettings` 必须覆盖**全部**设置项，`loadSettings` 必须与之一一对应（同一批键名、同一批默认值）；两端键名不对称同样是缺陷。

174. **改设置键名 / 增删设置项，必须同时写迁移。**

    本项目的设置键是中文名（`render.<对象名>.show`、`<作物名>` 等），**改名等于换键**：老存档读不回来，表现就是「重启恢复默认」。

    - 对象 / 字段改名时，`load` 必须**先读旧键、兼容期内双读**，`save` 写新键；
    - 删掉设置项时，旧键留在文件里无害，但**不得让旧键参与读取**（以免复活已删除的语义，如已去实验化的 `regionPlanting`）；
    - 需要一次性升级时保留版本号字段（现有先例：`paletteRevision`），并按「只迁移一次」实现，禁止每次启动都迁移。

175. **「设置重启后丢」的固定排查顺序（四步，禁止跳步猜测）：**

    1. **看落盘文件里有没有这一项的新键**（`run-<版本>/config/module-state.json` 的 `模块 → <模块ID> → 设置`，以及模块自己的存档目录）。**没有键** → 保存路径没走到：该界面的改动回调没调保存，或模块没实现 `saveSettings`。
    2. **有键、值仍是默认** → 保存发生在读取之后，或存在第二个写入者把文件覆盖（例如另一份内存副本 / 另一个配置文件被后写）。
    3. **有键有值，界面仍显示默认** → 读写键名不一致（第 174 条），或模块读了另一个 settings 实例。
    4. **前三步都正常** → 在 `loadSettings` 与 `saveSettings` 两端各打一行日志，核对实际读写值；**禁止凭猜测改代码**（与第 167 条取证要求同源）。

    取证产物必须记录：文件路径、键名、值、时间戳，以及「改的哪一项、怎么改的」。

---

## 二十六、启动自检提示铁律（2026-09-15 用户指令追加）

> 触发事例：星露谷农场因资源未就绪被自检拦下，只有聊天里那几条，玩家点完开关「像没反应」；用户要求弹到屏幕中间，且**提示文字要红色加粗**；并要求「自动箱子模块也要弄这样的提示」。故立为通用条款，对所有模块生效。

176. **启动自检的结论必须同时给两份：聊天 + 屏幕中间弹窗。**

    - 聊天那份留档、可回看（沿用各模块原有文案与配色，一字不改）；
    - 弹窗那份走项目现成的面板窗（`ConfirmPanelScreen.notice`，屏幕正中、底部一个「知道了」，点掉回游戏）。
    - **只发聊天＝缺陷**：聊天会被后续状态播报刷走，实机反馈就是「点了没反应、不知道哪里不对」。

177. **弹窗里的提示项一律红色加粗（`§c§l`），排版只在组件里做一份。**

    - 条目自带的颜色码（如 `§a容器类型§f·…`）在弹窗内**先剥离再统一上红色**，否则原文配色会把红色盖掉；**文字本身一字不改**。
    - 条目里带 `▸`（结论 + 出路）的拆成两行，避免一行顶出面板宽度。
    - 排版与配色统一由 `ConfirmPanelScreen.notice(title, headline, items, parent)` 承担，**禁止各模块各写一份**（包括拆行、剥色码这类细节）。

178. **入口必须走公共路径，移植新模块时必须核对。**

    - 框架自检（`Module.selfCheck()`）失败：统一由 `ModuleManager.tryEnable` 的缺项分支弹窗，模块**不许**自造弹窗；
    - 模块自己实现启动自检流程的（如星露谷农场），也必须调用同一个 `ConfirmPanelScreen.notice`；
    - 现状覆盖：星露谷（启动自检未通过 / 启动提醒）、全部走 `selfCheck()` 的模块（自动箱子、ID 三件套…）。
    - 移植验收项：开关一次就能看到——失败有聊天 + 弹窗，提醒类（不拦启动）也有聊天 + 弹窗，弹窗提示项是红色加粗。

179. **自检弹窗必须带「打开设置」，一键直达那个模块的设置页。**

    - 触发事例：玩家点「知道了」之后，还得自己再进模块中心 → 找到那个模块 → 点进设置，三跳才到出路所在页。
    - 做法：`ConfirmPanelScreen.notice(title, headline, items, parent, settingsTarget)` 的 `settingsTarget`
      返回 `new ModuleScreen(ModuleEntries.of(module), null)`；按钮与跳转时机只在这个组件里实现一次。
    - 跳转时机是**关闭动画结束**（`closing()` 里换屏），不是点击瞬间——面板的「回游戏」语义会覆盖点击瞬间的设置屏。
    - 模块侧只需给 `settingsTarget`，**禁止**自己拼按钮 / 自己 `setScreen`。
    - 验收项：故意把某模块的必填项清空 → 开模块 → 弹窗点「打开设置」→ **直接**落在该模块设置页；点「知道了」仍回游戏。

180. **模块页面（`Module.page()`）的构建必须推迟到「真正打开页面」时，禁止在构造函数里碰游戏注册表。**（2026-09-16 实机崩溃后追加）

    - 事实：`module.page()` 在**模组初始化阶段**就会被调用一次（`ModuleEntries.of:35` 用它判空），此时注册表组件尚未绑定；
      该阶段任何构造 `ItemStack`、取物品/方块显示名、建候选列表的行为都会抛
      `NullPointerException: Components not bound yet`，表现为**客户端一启动就崩**。
      实测证据：自动挖矿配置页，`run/crash-reports/crash-2026-09-16_01.58.11-client.txt`
      （`AutoMinerPage$ItemEntry.title() → Item.getDefaultInstance()`）。
    - 做法：页面构造函数只存引用（模块、设置载体）；把 `build()` 与读盘（如点位 `reloadStore()`）移进
      `createPage(ModuleEntry)`（`ModuleScreen:84-91` 只在打开时调用），并用 `built` 标志保证只建一次。
      参考实现：`feature/mining/ui/AutoMinerPage.java`。
    - 禁止的规避方式：把注册表调用包进 `try/catch`、或以「`mc.level == null` 就跳过构建」为由绕过——
      前者会把真错误吞掉，后者会让页面在打开时缺内容。
    - 验收项（新增或移植任何带页面的模块后必做，光编译通过不算）：真跑一次 `runClient` → 进一个世界 →
      日志确认 `yiyiaddon initialised`、无新崩溃报告、页面能打开；
      改过 Mixin 的还要在 `run-<版本>/logs/debug.log` 里看到 `Mixing <Mixin 名> … into <目标类>`。

---

## 二十七、模块控制台铁律（2026-09-16 用户指令追加）

> 触发事例：阶段11 自动附魔侦察提出「装备附魔设置页（`GearEnchantScreen` 311 行）/ 附魔选择页
> （`EnchantmentSelectScreen` 77 行）要不要一比一复刻旧交互」。用户裁定：**移植模块一律做成控制台样式
> （参照星露谷农场）**，并立为通用铁律，后续所有移植模块一律适用。

181. **凡是移植过来的模块，都必须带一个控制台（整屏分页）。**

    - 已落地参照实现：星露谷农场 `StardewConsoleScreen` + `feature/stardew/ui/console/`、
      自动挖矿 `MiningConsoleScreen` + `feature/mining/ui/console/`。
    - 页面构件统一用 `ui/console/ConsoleWidgets`（`ConsoleRow` / `Ctl` / `Note` / `Table` /
      `FoldSection` / `ButtonStrip`），窗口骨架统一用 `PanelScreen` + `ConsoleHost`，
      **禁止各模块另写一套分页壳**。
    - 控制台按用途分页（概览 / 点位 / 各设置组…），页签顺序与分组名沿用旧项目原有的分组，
      不新造分类。

182. **「查看使用说明」必须排在「打开控制台」下面。**

    - 顺序固定：先控制台入口（可带一行用途提示），再使用说明按钮。
    - 参照实现：`AutoMinerPage`（`addCore(控制台行)` → `addCore(ButtonRow(查看使用说明))`）、
      `StardewResourcePanelPage`（② 打开控制台 → ④ 查看使用说明）。

183. **旧项目的独立设置页 / 选择页，交互形态不再一比一复刻，统一改造成控制台页。**

    - 界线同 §二十二「全量复刻 + 只换外壳」与第 122 条：**内容**是资产——设置项名称、描述、
      默认值、取值域、可见性条件、按钮与回执文案逐字照抄；**承载它们的界面结构**由本项目决定，
      改成控制台页。
    - 阶段11 附魔的具体裁定：`GearEnchantSetting` 的 18 个分组做成控制台页；
      装备附魔设置页与附魔选择页**不做**一比一交互复刻。
    - 其他模块的独立窗口（如自动挖矿的点位卡片、星露谷的渲染对象设置）同此处理。

---

## 二十八、用户报 bug 的排查与修复流程（2026-09-16 追加）

> 收到 bug 反馈后固定照走本流程，禁止跳步。
> 本流程建立在**开发客户端**上：日志直连终端、崩溃报告自动落 `run-<版本>/crash-reports/`，
> 排查全程不需要任何外部工具或服务。

### 固定流程（十步，禁止跳步）

```
用户报 bug
  ↓
① 复现      跑开发客户端，直达现场
  ↓
② 取样      先读崩溃报告 / 日志
  ↓
③ 判断      ├─ 有证据 → 直接定性（跳到 ⑥）
            └─ 无证据 → ④
  ↓
④ 埋点      在「可疑那一环」加一次性日志
  ↓
⑤ 重跑      再走一遍复现路径，grep 到关键词
  ↓
⑥ 定性      本项目 bug / 上游第三方 / 配置资源包
  ↓
⑦ 排除法    一次只改一个变量，逐项实测
  ↓
⑧ 修复      只改必要那一处
  ↓
⑨ 验收      复现路径 + 埋点日志 + 无新崩溃报告
  ↓
⑩ 收尾      临时埋点删除，留档埋点登记
```

184. **第一步是复现，不是改代码。** 按用户描述的操作路径在开发客户端上重现一次；按菜单逐步走太慢时，可用启动参数直达该场景：

```powershell
.\gradlew runClient --console=plain --args='<直达该场景的启动参数>'
```

    复现不出来就不许动代码——改一个没复现的问题等于赌。现象与前提条件（版本、资源包、维度、服务器）都要先对齐。

185. **第二步是取样，不是猜机制。** 证据优先级固定，禁止跳步：

    `run-<版本>/crash-reports/` 崩溃报告（真实栈） > `run-<版本>/logs/debug.log` 埋点日志 > 代码静态推断。

    两个都没有时，进入第 186 条**去加埋点**，禁止凭印象下结论（第 167 条取证口径）。

186. **没有证据时，在「可疑那一环」加一次性埋点，然后重跑复现。**

    - 埋点位置选在**失败点的上游**，而不是把失败点整个包起来；
    - 禁止用「多试几次」「换个版本试试」这类无记录的操作代替一次有埋点的复现。

187. **埋点三条硬要求，缺一条就等于没埋：**

    | 要求 | 说明 |
    | --- | --- |
    | **唯一中文关键词** | 能一次 grep 到，全项目不得重复 |
    | **一次性** | 静态 `announced` 标志，一个进程只打一行；禁止在 render / tick 里刷 |
    | **正文含具体值** | 标识符、坐标、数量、版本号——没有具体值就无法定性 |

```powershell
Select-String -Path run\logs\debug.log -Pattern '<关键词>'
```

188. **埋点必须证明「路径走到了」，不能只证明「没崩」。**

    「没崩」是**结果**，不是证据——它分不清「修补生效了」和「压根没进这段代码」。
    日志正文写全三样：**触发了什么 + 具体的参数值 + 归因**；标识符后必须补中文释义，
    禁止只输出裸标识符（事后只剩日志文件时，裸标识符读不懂）。

189. **埋点代码自己不许成为新的崩溃源。** 四条约束：

    - 先做**零开销快路径**判断，确认要动手才碰 Minecraft API；
    - 只读，不改业务状态、不写盘；
    - 整段包 `catch (Throwable)`，失败即置一次性 `disabled` **永久停用**，只打一次 warn；
    - **禁止在 catch 里再抛、再重试、再递归**——「修不好也不能更糟」是底线，否则排查工具本身成了新 bug。

190. **定性必须二分，且必须靠实测。** 结论只能落在三类之一：

    | 类别 | 处置 |
    | --- | --- |
    | 本项目 bug | 直接修 |
    | 上游 / 第三方（依赖的 Mod、库、服务端） | 本项目内做兼容兜底，并记录上游链接 |
    | 配置 / 资源包 | 不改代码，告知用户改哪一项 |

    定性写不出来时，先补埋点，不许写「可能是……」。

191. **排除法：一次只改一个变量。** 升级 Mod、换资源包、改配置，**一次只动一项**，跑一次复现，记录结果，再动下一项。

    禁止一次性改多项——那样即使「好了」，也说不清是谁起的作用，等于没定性。
    每项排除结果都要留档（崩溃报告文件名或日志时间戳）。

192. **修复只改必要那一处。** 不动周边的代码、不顺手重构、不「顺便优化」（第 163–165 条）。补丁能多小就多小。

193. **验收三样齐了才算修好，缺一不可：**

    1. 按用户原操作路径复现一次，问题不再出现；
    2. grep 到那条埋点日志，**打印次数与内容**符合预期；
    3. `run-<版本>/crash-reports/` 无新增崩溃报告（对比时间戳）。

    禁止把「这次没崩」或「编译通过」当作修复完成（第 19、46 条）。

194. **收尾：临时埋点删除，留档埋点登记。**

    | 类型 | 判断口径 | 处置 |
    | --- | --- | --- |
    | 临时取证埋点 | 问题修好之后就不再触发的 | **提交修复时必须一并删除**，禁止留在代码库 |
    | 留档埋点 | 问题修好之后仍会长期触发的（兜底生效、降级启用这类行为宣告） | 保留，并在阶段报告写明**关键词、含义、触发条件** |

    判定问句：**这个埋点在问题修好之后还会不会触发？** 会 → 留档；不会 → 临时。

### 配套：埋点代码骨架

新增排查埋点时照抄此骨架：

```java
private static final Logger LOGGER = LoggerFactory.getLogger("yiyiaddon/<域>");

private static boolean disabled;   // 失败即永久停用（第 189 条）
private static boolean announced;  // 一次性播报（第 187 条）

public static void apply(...) {
    if (disabled) {
        return;
    }
    try {
        // 1. 零开销快路径：不满足条件立刻 return
        if (!needFix()) {
            return;
        }
        // 2. 只读判断 + 修补
        // 3. 一次性播报：具体值 + 中文释义 + 归因（第 188 条）
        announce(...);
    } catch (Throwable throwable) {
        disabled = true;
        LOGGER.warn("<唯一中文关键词>：<归因>；本进程内不再尝试", throwable);
    }
}
```

模块内的业务 bug 也可以直接用模块状态播报（`status.critical(键, 任务, 详情)`），
它在**聊天栏 + 控制台「日志」页**可见，比 `debug.log` 更贴近玩家现场；键名格式 `大类:稳定标识`，
同一键只播一次。

---

## 二十九、多目标选择器铁律（2026-09-16 用户指令追加）

> 用户原话：**「以后这种多目标的选择器做成 分类好 可以折叠的。」**
> 起因：自动附魔「原版装备」原本是一个 75 件装备平铺的长列表选择器，用户来回滚很烦
> （同日原话「现在上下滑动真的很烦」「做成跟旧项目一样分类好的那种界面」）。

195. **候选多到需要滚动时，必须先分类。** 一次装不满一屏的候选（装备、方块、物品、附魔、实体、玩家、
     生物群系…），禁止平铺成长列表丢给用户滚。判定口径：**候选数 > 8，或任意一次打开选择器需要滚动，
     即视为必须先分类。**

196. **每个分类必须可折叠，且默认收起。** 分类标题只作章节标记：不铺底色、不做成按钮样式、
     不与普通条目撞脸。本项目用控制台折叠分组承载（`ui/console/ConsoleWidgets.FoldSection`，
     点标题整组收起/展开），折叠状态必须记在会话级集合里（`owner.collapsedSections()`），
     禁止每次打开页面都重置回展开。

197. **分类维度取业务自身的维度，不取 UI 方便。** 例：装备按「大类 / 品质 / 类型」、实体按
     「玩家 / 怪物 / 动物 / 水生 / 环境 / 其他」、物品按「工具 / 方块 / 食物 / 材料」。
     禁止按字母序、按注册表顺序、按「每行 8 个硬切」分组。

198. **天然多级定位的目标：点击后必须先看到候选列表，再落定。** 例：装备定位天然是
     「大类 → 品质 → 类型」三级。用户 2026-09-16 明确否掉了「点一下直接跳下一档」的循环控件
     （原话「为什么点击就直接帮我选择了？我都没看见列表」）—— 点击的**唯一合法反馈**是弹出候选，
     由用户在候选里挑。参照实现：`feature/enchant/ui/console/GearEnchantConfigScreen`
     （独立窗口 + 按大类分组的装备候选列表 + 极品方案 + 目标附魔）。

199. **改造既有选择器的口径。** 已经存在的平铺长列表一律按本章改造：能分类 → 补分组 + 折叠；
     天然多级 → 换多级控件；确实无法分类（搜索结果、临时候选）→ 保留搜索框并限制候选量，同时
     在阶段报告登记「为何不分类」。改造后必须同步改掉指向旧入口的文案（例如帮助页里
     「点击卡片中的设置按钮」这类句子），禁止让文档描述一个已经不存在的界面。

200. **禁止用「加了搜索框」替代分类。** 搜索是补充手段，不是分类的替代：用户要先一眼看出
     「这里有哪些类」，再决定类里挑哪一项。

201. **一组强耦合的设置超过 3 行时，收进独立窗口，不要平铺在控制台页面里。** 判断口径：
     控制台某一页超过 5 行，或其中一组设置必须「先选中同一个对象（装备 / 方块 / 玩家…）才能配」，
     就把它做成「一行摘要 + 入口按钮」+ 独立窗口。用户 2026-09-16 原话：
     **「我要的是点击选择原版附魔装备单独弹出一个窗口配置，我的旧项目就是这样的。」**
     实现约定：窗口继承 `ui/screen/PanelScreen`，自己管理重建（`content().clear()` 后重排），
     写配置后同时通知控制台 `reload()`，保证关窗后主页面摘要与状态条同步。
     参照实现：`feature/enchant/ui/console/GearEnchantConfigScreen`（配套的入口页只留两行：
     合成策略 + 装备附魔配置摘要）。

---

## 三十、仓库与发布通道铁律（2026-09-17 用户指令追加）

> 用户原话：**「https://github.com/fxjcangku/yiyiaddon 这里到时候是项目发布页混淆版不公开源码
> 然后帮我新建一个单独放源码的私人仓库」**。2026-09-17 已按要求建好私有源码仓库。

202. **两个通道，职责严格分开，禁止混推：**

     | 远程 | 仓库 | 定位 | 允许推什么 |
     |------|------|------|-----------|
     | `source` | `https://github.com/fxjcangku/yiyiaddon-source`（**private**） | **源码仓库（日常开发唯一目标）** | 全部源码、开发报告、文档、脚本 |
     | `origin` | `https://github.com/fxjcangku/yiyiaddon`（public） | **发布页：只放混淆版产物，不公开源码** | 混淆后的发布产物、Release、面向用户的说明 |

203. **日常提交一律推 `source`**：`git push source master`（已设上游，`git push` 即指 `source`）。
     禁止再把源码提交推到 `origin`；要给 `origin` 发布，只推混淆后的产物与面向用户的说明，
     并且必须使用单独的发布流程 / 目录，不要在源码工作区直接 `git push origin`。

204. **禁止把以下内容推到 `origin`**：`src/` 源码、`01-开发参考库/`、`02-开发报告/`（含侦察记录、
     移植记录、差异清单）、`开发习惯.md`、任何含有内部实现细节的文档。

205. **`origin` 现有历史里的源码不做改写**（不改写历史、不 force push）；从本规则生效之日起，
     新增提交一律只进 `source`。

206. 私有仓库里的代码与文档同样受本文件全部规则约束（不因「私有」而放宽分类、命名、文案、
     取证与验证标准）。

---

## 三十一、代码质量与 API 查证铁律（2026-09-17 用户指令追加）

> 用户原话：**「api找不到就去我的新项目有官网源码参考 铁律加上 以后新发或者移植都要代码美观
> 带详细中注释」**。

207. **API 查不到的兜底路径固定为参考库的官网源码，禁止猜。** `01-开发参考库/` 收录 26.1.2
     官网原版源码参考。第 159 条的查询顺序（快速索引 → API参考 → 开发机制 → 原始源码）不变，
     本条补强兜底：前三步都查不到时，**必须继续在参考库的官网源码全文里 grep 确认**；
     参考库与官网源码都查不到，才能判定该 API 不存在或必须换实现。
     禁止因「查不到」就按旧版本经验猜 API、猜参数顺序或改用 Yarn 旧名（第 22~24 条不变）。

208. **所有新写与移植的代码必须代码美观、带详细中文注释。** 本条为硬性验收项，
     与编译通过同级，缺一不算完成：

     - **代码美观**：命名清晰、职责单一、缩进分行工整、禁止超长方法与魔法数（常量必须具名）、
       与周边既有代码风格一致（第 1~9 条的延续与强化）；能复用底座的不另造第二份（第 34 条）。
     - **详细中文注释**：每个类必须有中文 javadoc 写清职责与关键设计；每个非平凡方法说明
       「做什么、为什么、关键判据」；状态机、发包、容器同步、线程、坐标口径等易错点必须点明
       （照搬旧项目注释的按原文保留，旧项目没有的补齐）；注释与实现必须一致（第 158 条），
       禁止在注释里自夸（说「已优化 / 已复用」就必须真的做到）。
     - **范围**：新开发、移植、修复、重构一律适用。
     - **补注释的边界**：为存量代码补中文注释时只加注释、不改任何代码逻辑（第 164 条不变），
       逐文件补完后必须重新编译验证。


---

## 三十二、界面承载与说明时效铁律（2026-09-17 用户指令追加）

> 用户原话（同日实机反馈，逐条对应下方条目）：
> 「控制台里面已经有点位了 为什么控制台外面还有  自动农村也有这个问题」／
> 「把点击查看使用说明 删掉这个按钮 然后把使用说明 写在 控制台按钮的下面？超出界面的话可以上下滑动查看」／
> 「剩下的模块点击查看按钮之后 点返回会直接关掉 gui  我想返回还是查看按钮的那个界面」／
> 「检查一下各个的模组 看看事实说明是不是都是最新的 不是最新的 要改正」／
> 「运行模式 目标职业旁边没用中文注释 用户根本不知道怎么点击切换」／
> 「快熟停止键 没有刷新按钮 绑定了导致无法取消」／
> 「快速停止键落模块快捷键 不能自研一个吗 meteor的源代码也有参考价值吧」。
> 落地记录见 `02-开发报告/项目开发报告/02-阶段施工与落盘/60-跨模块-界面口径统一-点位去重与说明内嵌.md`。

209. **同一份设置 / 点位只能有一处承载，禁止模块页与控制台各摆一份。**
     模块页结构固定为「顶部信息块 → 打开控制台入口 → 使用说明正文」；设置项、点位卡、日志
     一律只在控制台对应页签出现。同一份信息在模块页与控制台**各写一份渲染**算缺陷
     （第 169 条「同源逻辑不留两份」的界面侧延伸）：已按本条订正自动村民交易（两张点位卡）、
     自动农场（六张点位卡）、自动箱子（标点区整体收敛，两套渲染代码合一）。

210. **使用说明一律内嵌，不再单摆「查看使用说明」按钮。**
     说明正文去掉独立窗口的窗框三行（`§8┏━┓ / §8┃ 使用说明 ┃ / §8┗━┛`；
     见 `HelpPanelScreen#inlineContent`），章节标题与全部正文**逐字不动**，直接铺在
     「打开控制台」入口正下方；超出一屏由模块页自身的滚动条上下查看，不新增滚动实现。
     尚未改的模块（星露谷、身份配置）保持现状，要动必须用户点名。

211. **子窗口的「返回 / 关闭」必须回到打开它的那个界面，禁止直接关掉整个 GUI。**
     实现口径：关闭走 `requestClose()` → `SkiaScreen#closing()` → `setScreen(parent)`，
     传 parent 时必须传「打开它的那个页面」（模块页传自身、控制台内开的传控制台）。
     旧项目的 `exitToGame()`（`setScreen(null)`）语义只保留给用户明确要求的窗口
     （如二次确认窗），其余窗口沿用本条。

212. **使用说明必须与实现保持最新，发现过时立即改正**（本条为交付前必跑项，与编译同级）：
     - **判定标准**：说明里凡是「指向界面位置 / 入口 / 按钮 / 页签名 / 指令名 / 节点名 / 默认值」
       的表述，必须与当前实现一致。**页签名与指令字面量必须去源码核实，禁止凭印象**
       （第 207 条的同一精神：查得到才算数）。
     - **交付前必检**：本批改动涉及或影响到的模块，说明里指向的元素是否还存在、是否已搬家、
       指令是否仍可执行得到。发现即改，不许「照抄原文」当挡箭牌 —— 旧文案与实况冲突时，
       以实况为准并登记差异（第 105 条的「有意不带界面」同理）。
     - **全量核对**：全部带说明正文的模块（用 `Grep "HelpSection("` 可穷举）逐行比对
       旧项目原文 ↔ 本项目实况；本项目已落地的新能力（如新增开关、新增页签）也必须写进说明。
     - **修正边界**：只改与实况不符 / 已失效的部分，其余照抄旧原文；每处改动登记报告，
       写明改前 → 改后 + 依据 `文件:行号`。
     - **硬伤示例**（本次已修）：`.fumo 设置 <节点>` 的节点表里写「装备箱」→ 实际节点是
       `工具护甲箱`（照着打会报错）；挖矿说明写「点击『检测假矿』按钮」→ 本项目该能力留白，
       按钮与指令都不存在；星露谷写「状态提示默认关闭」→ 实际默认开启。

213. **可点击项必须有看得见的中文提示，不许只靠 tooltip。**
     循环 / 选择 / 键位这类**没有按钮外观**的控件，行尾必须给可见灰字提示
     （例：`§7点击切换`、`§7点击后按任意键绑定`）；旧项目的设置描述仍逐字留在 tooltip 里，
     两者并存。判据：新玩家不看 tooltip 也应知道「这个东西点了会变」。

214. **已绑定的东西必须能取消，且取消入口要看得见。**
     键位类设置必须在该项旁边提供显式「清空」按钮（未绑定时为禁用态），
     与键位块同一行；**控件内嗅右键之类的交互算缺陷**（行构件 `ConsoleRow#onClick` 只把左键
     转给控件，右键根本到不了控件，等于做了个点不到的交互）。

215. **壳件缺失时优先自研，且可以参考库里的 Meteor 原始源码**（第 19 章「界面自研铁律与参考边界」
     的补充口径）：缺少的控件自研实现，参考 Meteor 源码是**合法**的；每条实现必须在 javadoc 里
     写明参考了哪个文件、对齐了什么语义（键名表、`canBindTo` 判据、`None` 显示等），
     并顺手收敛重复实现（如键名表 / 鼠标名 / 按下判定统一到一处，第 169 条）。

216. **模块页不留顶部状态行；状态文字 + 快捷键徽章 + 模块开关三件压进控制台顶栏**（用户 2026-09-17
     两次口径合并：「按钮开关 能不能移动到控制台里面？所有包含控制台的模块」→「未启用这些 还有快捷键设置的
     都没有移动过去」「不可以那么长 压缩到控制台里面的跟启动按钮的旁边」）。

     - **模块页顶栏整条撤掉**：原来的 `ModuleStatusBar`（状态圆点 + 状态文字 + 快捷键徽章 + 开关）
       已整体删除，13 个模块页不再设头部；`CompactModulePage#setHeader` 保留但当前无调用。
       别再把这条长卡加回模块页 —— 它横贯整屏，用户明确说「不可以那么长」。
     - **控制台内容第一行 = 压缩的右对齐横排**：`[状态圆点 + 状态文字] [快捷键徽章] [模块开关]`，
       实现只有一处 `ui/console/ConsoleHeaderBar`（构造只传 `Module`，状态文案、启用语义、快捷键 ID
       全部从它取，与模块页原来同源），9 个控制台（含没有状态格的水源显示）一律照此接线。
     - **排布口径固定**：整排右对齐、右内缩 14、件间距 12；开关尺寸一律读 `SettingToggle#getWidth/getHeight`
       （44×24），不许为塞进某处另定尺寸；状态文字仍是「运行中 / 未启用」，绿 / 红走主题色槽
       `stateOn` / `stateOff`（第 141 条）。
     - **可点的开关全项目只剩控制台这一处**：状态**显示**点可以有多处（模块中心行、首页、控制台状态格），
       但同一个开关不许摆第二份（第 209 条）。模块中心的行内开关、首页的功能开关不受影响。
     - **带颜色码文本的截断只有一份实现**：`MinecraftText.fit(text, size, maxWidth)`
        （按可见宽度算、不把 `§x` 计入宽度）；禁止再写私有 `fit` 副本（本条落地时收敛了 5 份）。

217. **控制台窗口标题下面必须显示所属模块的说明**（用户 2026-09-17 指令：「控制台外面的模块标题
     下面有说明 但是控制台里面没有 修复一下 所有带控制台的都要」）。

     - **来源不许另写一份**：控制台的说明就是**模块自己的说明**，在构造器里写
       `setSubtitle(module::description);` —— 与模块页标题下那行是同一个字符串源，改一处两边同步。
     - **实现只有一处**：`ui/screen/PanelScreen#setSubtitle(Supplier<String>)`（可选，不调用则不画），
       9 个控制台全部调用；新增控制台（第 156 条那类骨架）时这一行属**必做项**。
     - **度量与模块页逐项相同**（`ModuleScreen#drawHeader` 那套）：x = 标题 x、y = 44、
       字号 11、次要文字色、超宽按「内容宽 − 120」省略号截断 —— 两处说明看起来要像同一行字。
     - 说明过长一律走省略号，不许压到右侧内容上，也不许换行把标题区撑高。

---

## 三十三、运行时埋点修复规范（2026-09-17 用户指令追加）

> 用户原话：**「把这种弄一个 `04-埋点修复` 然后你写一个 js；然后开发习惯写上这次标准的埋点规范代码
> （要汉化的）；用户提出的 bug 实在无法修好才启动这个方案；修复的单独建一个文件夹、已修复 / 进行中
> 都放在 `04-埋点修复`，分类好按文件夹。」**
>
> 本方案是第二十八章（用户报 bug 排查修复流程）的**重武器档位**：那边靠 `debug.log` 打点，
> 这边靠**运行时事件探针 + HTTP 采集 + JS 分析**，能看清「每个刻的实际状态与发包裁决」。
> 首个案例：`04-埋点修复/已修复/2026-09-17-卸货后RTP秒破连锁失效/`，复盘见报告 79。

218. **启用门槛：三条同时成立才允许启动本方案，缺一条都不许开。**

    | 条件 | 说明 |
    | --- | --- |
    | ① 是**用户报的** bug | 不是自己顺手做的优化、不是「我觉得可能有问题」 |
    | ② 第二十八章十步走完仍**定性不了** | 已复现、已看崩溃报告 / `debug.log`、已完成静态分析与排除法，结论仍写不出来 |
    | ③ **用户同意** | 先说明「要用埋点修复方案 + 为什么必须用它」，取得同意（第 165 条） |

    禁止的用法：把它当常规调试手段；当「多打几行日志再看看」的借口。
    **每个探针都必须对应案例档里的一条判据**，说不清判据的探针不许埋。

219. **目录约定：一案一个文件夹，`进行中` / `已修复` 两类，工具与案例分离。**

    ```
    04-埋点修复/
    ├── README.md                  总纲（目录约定 + 工具用法 + 结案三件事）
    ├── 案例模板.md                 新开一案照抄成 进行中/<案名>/README.md
    ├── 工具/ 公共.js 采集服务.js 分析.js
    ├── 进行中/<YYYY-MM-DD-一句话现象>/   README.md（案例档）+ 日志-<会话名>.ndjson
    └── 已修复/<YYYY-MM-DD-一句话现象>/   结案后整体搬到这里
    ```

    - 文件夹名 `YYYY-MM-DD-一句话现象`，同日多案加 `-2`、`-3`。
    - 案例档必须逐项填全（现象 / 复现路径 / 假设清单 / 埋点清单 / 证据 / 根因 / 修复 / 验证 / 收尾核对）。
    - 原始日志按**会话名**分开存（`日志-修复前.ndjson`、`日志-修复后.ndjson`），
      两个会话的横向对比就是结案判据。

220. **埋点代码规范（骨架唯一权威版本，照抄并只改要改的字段）。**

    落点 `src/main/java/com/yiyiaddon/debug/DebugProbe.java`，**一案一份，结案整文件删除**。
    本方案经用户 2026-09-17 指令授权，属第 163 条允许的例外（排查用临时物，随案删除）。

    ```java
    // ═══════════ 埋点修复 · 临时探针（结案必须整体删除，见 开发习惯.md 第三十三章） ═══════════
    package com.yiyiaddon.debug;

    import java.net.URI;
    import java.net.http.HttpClient;
    import java.net.http.HttpRequest;
    import java.net.http.HttpResponse;
    import java.nio.charset.StandardCharsets;
    import java.time.Duration;
    import java.util.Map;
    import java.util.Set;
    import java.util.concurrent.ConcurrentHashMap;

    /**
     * 运行时埋点：把「这一刻发生了什么」发到本地采集服务，供《04-埋点修复/工具/分析.js》出结论。
     *
     * <p>三条铁律：<b>不阻塞游戏线程</b>（异步发）、<b>绝不抛异常</b>（出过错就永久停用）、
     * <b>失败静默</b>（采集服务没开就当作没埋点，绝不因此崩游戏）。</p>
     *
     * <p>结案动作：删除本文件 + 全部调用点，然后 grep 校验
     * {@code debug-point|DebugProbe} 为 0 命中，再编译一次确认 EXIT=0。</p>
     */
    public final class DebugProbe {

        /** 总开关：停采集时改 false（结案仍要整段删除，不许留在树里）。 */
        private static final boolean ENABLED = true;
        /** 采集服务地址，端口与《工具/采集服务.js》固定一致。 */
        private static final String ENDPOINT = "http://127.0.0.1:7777/event";
        /** 会话名：一次采集一个名字（修复前 / 修复后 / 复现2），换采集就换名字。 */
        private static final String RUN = "修复前";

        private static final HttpClient CLIENT = HttpClient.newHttpClient();
        /** 出过错就永久停用：排查工具自己绝不许成为新的崩溃源（第 189 条）。 */
        private static boolean disabled;
        /** 心跳节流表：位置 → 上次发送的刻数。 */
        private static final Map<String, Integer> LAST_TICKS = new ConcurrentHashMap<>();
        /** 一次性标记表：同一个键一个进程只发一次（第 187 条精神）。 */
        private static final Set<String> ANNOUNCED = ConcurrentHashMap.newKeySet();

        private DebugProbe() {
        }

        /**
         * 发一条事件。
         *
         * @param tag      探针代号（A / B / C…），用于按探针聚类
         * @param location 埋点位置，建议「模块.方法」（如 FastBreak.begin）
         * @param msg      事件名，英文短标识（如 heartbeat / detail / verdict）
         * @param dataJson {@link #data} 拼好的 JSON 对象；没有内容传 "{}"
         */
        public static void send(String tag, String location, String msg, String dataJson) {
            if (!ENABLED || disabled) {
                return;
            }
            try {
                String body = "{\"run\":\"" + RUN + "\",\"tag\":\"" + escape(tag)
                        + "\",\"location\":\"" + escape(location) + "\",\"msg\":\"" + escape(msg)
                        + "\",\"data\":" + (dataJson == null || dataJson.isBlank() ? "{}" : dataJson) + "}";
                HttpRequest request = HttpRequest.newBuilder(URI.create(ENDPOINT))
                        .timeout(Duration.ofMillis(500))                     // 采集服务没开也别拖住游戏
                        .header("Content-Type", "application/json; charset=utf-8")
                        .POST(HttpRequest.BodyPublishers.ofString(body, StandardCharsets.UTF_8))
                        .build();
                // 异步发送 + 静默失败：异常在 whenComplete 里被吞掉，不进游戏线程
                CLIENT.sendAsync(request, HttpResponse.BodyHandlers.discarding())
                        .whenComplete((response, error) -> {
                            if (error != null) {
                                // 采集服务没开是常态，不打日志刷屏
                            }
                        });
            } catch (Throwable throwable) {
                disabled = true;   // 只停一次，不抛、不重试、不递归（第 189 条）
            }
        }

        /** 心跳节流：同一位置每 intervalTicks 刻最多发一条，避免把日志刷爆。 */
        public static boolean due(String location, int tickCount, int intervalTicks) {
            Integer last = LAST_TICKS.get(location);
            if (last != null && tickCount - last < intervalTicks) {
                return false;
            }
            LAST_TICKS.put(location, tickCount);
            return true;
        }

        /** 一次性事件：同一个 location+msg 一个进程只发一次（初始化 / 换实体这类转折点用）。 */
        public static void once(String tag, String location, String msg, String dataJson) {
            if (ANNOUNCED.add(location + "|" + msg)) {
                send(tag, location, msg, dataJson);
            }
        }

        /** 拼 JSON 对象：交替传「键, 值, 键, 值…」，字符串自动转义，可直接喂给 {@link #send}。 */
        public static String data(Object... pairs) {
            StringBuilder json = new StringBuilder("{");
            for (int i = 0; i + 1 < pairs.length; i += 2) {
                if (i > 0) {
                    json.append(',');
                }
                json.append('"').append(escape(String.valueOf(pairs[i]))).append("\":").append(value(pairs[i + 1]));
            }
            return json.append('}').toString();
        }

        private static String value(Object raw) {
            if (raw == null) {
                return "null";
            }
            if (raw instanceof Number || raw instanceof Boolean) {
                return String.valueOf(raw);
            }
            return '"' + escape(String.valueOf(raw)) + '"';
        }

        private static String escape(String text) {
            return text == null ? "" : text.replace("\\", "\\\\").replace("\"", "\\\"");
        }
    }
    ```

    调用点一律包在唯一标记里，便于一次 grep 清干净：

    ```java
    // #region debug-point A:秒破主循环心跳
    if (DebugProbe.due("FastBreak.tick", player.tickCount, 5)) {
        DebugProbe.send("A", "FastBreak.tick", "heartbeat",
                DebugProbe.data("tick", player.tickCount, "gap", player.tickCount - nextStartTick,
                        "phase", phase, "pending", pending.size(), "blocked", blockedBefore.size(),
                        "brokenAge", player.tickCount - lastBrokenTick, "ready", ready));
    }
    // #endregion
    ```

    埋点自身的要求（与第 187~189 条同源，缺一条等于没埋）：

    | 要求 | 做法 |
    | --- | --- |
    | 唯一标记 | `// #region debug-point <探针>:<用途>` … `// #endregion`，全项目只有这一种标记 |
    | 单一出口 | 只走 `DebugProbe.send`，调用点禁止自己拼 HTTP / 自己拼 JSON（用 `data(...)`） |
    | 不阻塞 | 一律异步；**渲染线程里的探针必须 `due(...)` 节流**，禁止每帧发 |
    | 不出错 | 失败静默 + 永久停用；禁止在探针里改业务状态、写盘、调用会影响玩法的方法 |
    | 带具体值 | data 里必须有能定性的数值（坐标、刻数、计数、判定结果），禁止只发「到了这里」 |
    | 汉化 | 注释、案例档、工具输出一律中文；标识符与事件名用英文短标识（脚本聚合友好） |

221. **采集与分析必须用 `04-埋点修复/工具/` 里现成的两个 JS，禁止每次另写一次性脚本。**

    ```powershell
    # 采集（Ctrl+C 停服；案例目录不存在会自动建并复制模板）
    node 04-埋点修复/工具/采集服务.js --案例 <文件夹名>
    # 分析（默认挑最新日志；--列表 先看有哪些会话）
    node 04-埋点修复/工具/分析.js --列表
    node 04-埋点修复/工具/分析.js --案例 <文件夹名> --会话 修复前
    node 04-埋点修复/工具/分析.js --事件 begin --时间线
    node 04-埋点修复/工具/分析.js --分组 data.pos --前N 10
    node 04-埋点修复/工具/分析.js --空档 ts --最小间隔 500
    ```

    - 会话名固定用 `修复前` / `修复后`（同一案例下并存才能做会话对比）。
    - 复现开始 / 结束各打一个 `POST /mark`，分析时标记会单独列出。
    - 结论必须落到**具体数值**上（次数、刻数、坐标、比例），禁止「可能是」「看起来像」。

222. **结案三件事，缺一件不许归档。**

    1. **删插桩**：`DebugProbe.java` 与全部 `#region debug-point` 段落、全部调用点一起删；
       grep 校验 `debug-point|DebugProbe` 为 **0 命中**；`.\gradlew build --console=plain -q` → **EXIT=0**；
       重启客户端确认功能仍然正常（第 180 条验收项照旧适用）。
    2. **停采集服务**：Ctrl+C；确认端口（默认 7777）不再监听。
    3. **归档**：案例文件夹整体从 `进行中/` 移到 `已修复/`；案例档填完并勾上收尾核对；
       同时在 `02-开发报告/项目开发报告/` 写一份复盘（编号接续，第 16~17 条格式不变），
       案例档里互相写上编号。

223. **埋点代码永远不许进入任何发布产物。**

    jar / 混淆版 / `origin` 公开发布仓库（第 202~204 条）里**不得出现** `DebugProbe`、
    `#region debug-point`、`127.0.0.1:7777`。结案前若手边有构建产物，重新构建一次再交付。

224. **假设先写、数据后采，判据先定、结论后落。**

    案例档第 3 节的假设清单必须在**第一次采集之前**写好（假设 / 判据 / 数据来源），
    采集后逐条判定「成立 / 不成立」；事后新增的假设必须注明「采集后补充」。
    禁止先看数据再编一个能自圆其说的故事——那是伪装成证据的猜测（第 158、167 条）。

---

## 三十四、窗口标题与返回键对齐铁律（2026-09-19 用户指令追加）

> 用户原话：「主世界矿石标题 没跟返回键对齐看了吗 对齐一下偏上了现在 所有标题都要这样对齐返回键
> 除了底下有介绍的不用」。
> 落地位置：`PanelScreen#titleBaselineY` / `ModuleScreen#drawHeader`（两个骨架各一处判据）。

225. **窗口标题必须与左侧返回键垂直居中；只有「标题下方真有说明文字」的窗口才可以不下移。**

    - **几何判据**：返回键固定 `BACK_Y = 16`、边长 `BackButton.SIZE = 28`（垂直中心 **30**）；
      标题基线取同轴值 `TITLE_Y_ALIGNED = BACK_Y + SIZE / 2 + 标题字号 × 0.36`
      （0.36 是 `TextLine` / 选择器分组标题同一套「基线 → 视觉中心」比例，19 号字算出 ≈ 36.8）。
      **禁止再把标题基线写死成与返回键无关的数字**——本条落地前 `PanelScreen` 与 `ModuleScreen`
      都写死 27，比返回键中心偏上 10px，正是用户截图里的现象。
    - **有副标题的窗口保持原位**（`TITLE_Y = 27`）：标题与其下的说明是上下成组的一行，占同一个标题区，
      下移只会把标题和说明挤到一起（用户口径「除了底下有介绍的不用」）。
    - **判定必须看「副标题能否取到非空文本」，不是「有没有调 `setSubtitle`」**：有些窗口塞进去的是空串，
      那种也按无副标题处理，否则会留一段空白还把标题顶在上面。
    - **覆盖现状**（全项目只有 `BackButton` 一个返回键组件，图标码点 `\uE5C4` 全库唯一命中，
      不存在自绘箭头，所以覆盖它即可全覆盖）：
      - `PanelScreen` 系（41 个窗口）：**无副标题的 21 个已对齐**——选择器、帮助、确认框、调色盘、
        Baritone 名单 / 方块映射、自动箱子两个子页、身份识别 5 个、星露谷 5 个列表页、农场作物配置、
        装备附魔配置；**有副标题的 20 个保持原位**——各模块整屏控制台 + 挖矿配置存档页。
      - `ModuleScreen`（模块页）：副标题 = 模块描述，全库模块描述均非空 → 按本条口径全部保持原位。
      - `ClickGuiScreen`（主界面页头）：各页都带副标题 → 不动。
    - **新增窗口照此接线**：新窗口若没有说明文字，标题必须与返回键同轴；有说明文字才允许用高位基线。

## 三十五、自动化模块与玩家界面共存铁律（静默容器标准，2026-09-19 用户指令追加）

> 用户原话：「把 自动附魔的打开 gui 发包那些功能 不关闭菜单能打开箱子 不抢鼠标的 代码 全部同步到 所有模块里面
> 凡是有开箱子的模块 那些都要」「都不会抢鼠标了是吧 运行过程中模块都可以打开背包吗」
> 「都可以后台挂机 不取消我的那个返回界面吧」「写进开发习惯更新 以后开发的插件都按照这一套标准」。
> 落地位置：`platform/container/SilentContainer`（唯一闸门）+ 8 个开箱模块的 `onOpenScreen`；
> 机制来历见 `02-开发报告/项目开发报告/03-复盘/128-复盘-挂机时界面被抢（静默容器时机与传送收尾）.md`，
> 全模块同步见 `134-复盘-静默容器机制全模块同步（统一闸门 SilentContainer）.md`。

226. **凡是「自己发包开方块容器」的模块，必须走共用闸门 `SilentContainer`，禁止各模块自写界面判据。**

    - **四条判据只有一份**（第 169 条落地版；改造前这套判据在六个模块里各写了一遍，已全部删除，
      今后禁止再新写一份 `isContainerScreen`）：
      `isPlayerInventory(类名)`（生存背包 + 创造背包，两个都必须放行）、`isContainerScreen(类名)`、
      `isLevelLoadingHijack(类名)`、`releaseSilentContainer()`。
    - **`onOpenScreen` 骨架顺序固定，不许调换**：

      ```java
      String cls = event.payload();
      if (SilentContainer.isLevelLoadingHijack(cls)) { event.cancel(); return; }      // ① 传送不顶掉玩家界面
      if (SilentContainer.isPlayerInventory(cls)) { 收我方容器 + 复位本模块相位; return; } // ② 背包永远放行
      if (本模块正在做容器操作() && SilentContainer.isContainerScreen(cls)) event.cancel(); // ③ 只静默自己的
      ```

    - **③ 的状态门控是硬性要求，不是可选项**：判据必须是「本模块当前**正在**用容器 / 菜单」
      （挖矿 `MiningContainer#isOperatingContainer`、村民 `VillagerTradeFSM#isUsingContainerScreen`、
      图书管理员 `LibrarianOrchestrator#isUsingTradeScreen`、农场 `task.exclusive()`、
      星露谷 `coordinator.usingSilentContainer()`、自动箱子 `isInteractingWithTarget()`、
      自动附魔容器操作状态集合）。
      **禁止写成「模块一开着就拦所有容器界面」** —— 那样玩家挂机时手动开自己的箱子，界面根本不出现
      （用户 2026-09-19 报的就是这个）。**也不能拿 `containerMenu.containerId != 0` 当判据**：
      玩家自己开的箱子同样满足，会绕回同一个毛病。
    - **优先级口径（用户 2026-09-19 拍板：模块运行优先）**：模块**没在**做容器操作时，
      玩家手动开的箱子必须照常显示；模块**正在**用容器 / 菜单的那段时间里，玩家的箱子界面被压掉、
      **模块继续跑**（不会停机、不会播报）。**禁止擅自改成「玩家优先 / 让模块让路」**。
    - **已知待修风险（本次登记，不要当已修）**：模块正在用容器时玩家手动开箱，服务端那边玩家的箱子
      其实已经开了（`containerMenu` 被换掉），模块只看 `containerId != 0` 会把它当成自己的目标容器去
      搬东西。建议修法（要动 6 个模块）见 `134-复盘` 第六节「已知风险」：给每个模块打点
      「我方最近一次发包开箱」，之后再出现不同菜单 → 判为玩家手动开的 → 压掉界面 + 掐掉它。
    - **补了门控就必须同时检查「相位推进时会不会撞玩家开着的容器」**：加了门控之后玩家能带着自己的
      箱子界面走过更多相位了，凡是「即将开我方容器 / 交互村民」的相位都要有「玩家自己开着容器界面 →
      只等不做」的守卫（挖矿卸货 / 补给、农场 `ContainerTask`、星露谷 `interactLogistics`、村民
      补给 / 卸货 / 打开交易界面、自动箱子与图书管理员整体冻结状态机）。

    - **玩家手动开箱必须被拒（用户 2026-09-19 拍板）**：模块正在用容器 / 菜单时，玩家手动开的箱子
      **不给打开** —— 压掉界面 + **收掉那个容器**（`SilentContainer#rejectPlayerContainer()`），
      动作栏提示 `§e⚠ 容器界面发包无法打开 §8▸请关闭模块重试`（文案用户给定，禁止改写），
      走 `ClientChat#overlay`（不刷聊天框）。
      **只压界面不收容器是错的**：服务端那边箱子已经开了，`containerMenu` 被换掉，
      模块会把它当成自己的目标容器去搬东西（这正是收掉容器要解决的问题）。
      判据是「我方刚发包开箱」打点 + 40 刻窗口：**每个我方开容器 / 开菜单的入口都必须调
      `SilentContainer#markOwnContainerOpen()`**（挖矿 `MiningContainer#openContainer`、自动箱子
      `ChestInteractionService#open`、农场 `ContainerTask#tryOpen`、星露谷开箱步、村民补给 / 卸货 /
      打开交易界面、图书管理员 `MerchantTradeOps#open`、自动附魔 `EnchantContainer#interactBlock`、
      自动登入两处菜单物品）。漏打点 = 我方自己开箱也被拒（自己把自己的箱子关掉），必须照清单核对。
    - **开箱发包只认 `BlockPacketSender.interactBlock(hand, pos, face)`**：不依赖 `mc.gameMode.useItemOn`，
      窗口失焦 / 无界面时前者会被原版吞掉（旧项目踩过）。静默容器能成立的前提就是「发包与界面无关」。

227. **玩家按 E 开背包时必须「收掉我方静默容器」，绝不允许「干脆不关容器」。**

    - **原因**：静默模式下容器界面被取消，走不到 `AbstractContainerScreen#onClose`，服务端一直认为容器开着；
      此时玩家在背包里的点击会按**容器的 `containerId`** 发出去 → 错位、丢物品、背包点不动。
    - **收容器本身不会动玩家界面**：这条由 `LocalPlayerScreenGuardMixin` +
      `EventDispatcher#closeScreenUnlessPlayerOwned` 保证（见 128 号复盘），所以「关容器但别动玩家界面」是安全的。
    - **收容器后必须复位本模块的容器相位**，否则状态机停在「操作中」无法重开菜单而卡死或超时停机：
      各模块用自己已有的收箱口（如挖矿 `MiningContainer#closeContainer` —— 除关菜单外还要清开箱重试 / 精确补组）。
    - **创造背包不能漏**：创造模式下 `InventoryScreen.init` 会立刻切到 `CreativeModeInventoryScreen`，
      拦掉后者会让前者停在未初始化状态（`RecipeBookComponent.book` 为 null）**闪退**。

228. **玩家自己开着容器界面时，模块一律「只等不做」；等待期间禁止累计超时、禁止记失败。**

    - 判定：`mc.screen instanceof AbstractContainerScreen<?>` —— 我方容器界面一律被 ③ 静默取消，
      所以这时能看到的容器界面就是玩家自己的。
    - **实现方式按模块结构选一种**（三种都合规，禁止发明第四种「一边等一边试探发包」）：
      ① 状态机整体冻结（自动箱子 / 图书管理员：`tick` 里直接 return，**计时一并冻住**）；
      ② 状态级守卫（挖矿卸货 / 补给、农场 `ContainerTask`、星露谷 `interactLogistics`：返回等待态）；
      ③ 原状态重入只清计时（村民补给 / 卸货四态 `holdForPlayerScreen`）。
    - **例外**：依赖界面本身才能继续的相位（村民「交易中」依赖 `MerchantMenu`）按各模块既有语义处理，
      但必须在复盘里写明「这一相位是停机还是等待」。

229. **玩家自己的界面有两层保护，新模块照抄，不许绕过。**

    - 第一层：`SCREEN_OPEN` 只在「原版容器界面」上做取消 —— 游戏菜单 / 本模组控制台（带返回键那个）/
      聊天这类非容器界面根本不进判定，不会被拍掉；
    - 第二层：模块所有收容器动作（`closeContainer()` / `player.closeContainer()`）都被
      `LocalPlayerScreenGuardMixin` 兜住 —— 目标是「关界面」但当前是玩家自己开的非容器界面 → **保留**。
    - **明确例外**（写清预期，别当 bug）：死亡界面照旧关（原版重生语义）；
      自动登入断线重连会主动切屏（那是它的功能本身）；玩家按 ESC 关菜单走 `Screen#onClose`，不经这两层。

230. **新模块交付前的静默容器自查清单（与编译同级，缺一项不许交付）。**

    - 自己开箱 / 开菜单时：界面不弹、鼠标不动、数据走 `mc.player.containerMenu` 正常同步；
    - 挂机时按 E 开背包（生存 + 创造）：背包可正常点击（不错位），关掉后自动化自动续上；
    - 挂机时按 ESC 开游戏菜单 / 打开本模组控制台并点返回：界面不被关、不被取消，自动化照跑；
    - 被传送 / 重生时玩家界面不被「加载地形中」顶掉；
    - 挂机时手动去开一个自己的箱子（模块当前没在做容器操作）：**必须正常显示界面**，
      模块只是启用 / 正在寻路都不许拦（第 226 条 ③ 的状态门控）；
    - 模块正在用容器时手动开箱：界面被压掉、动作栏出现 `§e⚠ 容器界面发包无法打开 §8▸请关闭模块重试`，
      且**那个容器确实被收掉**（模块继续跑自己的）；同时确认**模块自己开箱时不出现提示、不被误收**
      （出现即说明有开箱入口漏打点）。

---

## 三十六、双版本线维护铁律（26.1.2 主线 / 26.2 版本线并行，2026-09-19 用户指令追加）

> 用户原话：「我想先适配26.2」→「等等我想说的是出2个版本 你是不是误会了 26.1.2 保留 出一个26.2的版本
> 懂吗 不是把我的26.12升级成26.2」→「写没错 你的建议很好 让智能体第一件事情就知道这个」。
> 落地形态：**同一个仓库两条分支**（不复制工程目录、不复制源码树）——
> `master` = **26.1.2 主线**（业务唯一真源），`port/26.2` = **26.2 版本线**（只放版本适配差异）。
> 两版 Baritone 构件在 `maven-repo` 并存：`1.18.0` 给主线、`1.19.0` 给 26.2，两条线各取各的。
> 迁移全貌、逐条 API 口径与验证状态见
> `02-开发报告/项目开发报告/05-版本线迁移/136-复盘-迁移到 Minecraft 26.2（双版本并行 port 26.2）.md`。

231. **`master` 是业务唯一真源：功能、判据、文案、渲染、组件一律先在 `master` 落地，再 cherry-pick 到 `port/26.2`；禁止直接在 `port/26.2` 上改业务代码。**

    - **两条线只允许差「官方 API 口径」**：切屏入口、常量搬家（如 `EntityTypeIds` / `ColorCollection` /
      `BlockItemTags`）、Mixin 注入点、依赖与构建坐标。**差到业务逻辑就是违规**
      （第 169 条：同源逻辑禁止留两份 —— 如果放着不管，判据与文案会各跑各的）。
    - **自查口径**：把这次改动拿到 `master` 上看 —— 如果 `master` 上是错的（改动与 MC 版本无关），
      说明改错了分支，必须挪回 `master` 再 cherry-pick。
    - **26.2 线现有的全部适配差异清单**登记在 136 号复盘第四节；后续在这条线上新增任何适配，
      同样要在该报告里登记，不许只存在于 commit 里。
    - **改完 26.2 分支必须回 `master` 复跑一次编译**，确认主线没被牵连（第 180 条同理：改完必须真跑）。
    - **新版本线开工前先取证**：把目标版本的**官方客户端 jar**当正本，用 `javap` 与当前版本 jar 逐类逐成员
      对照；官方博客只作索引、**不作依据**（26.2 就有三处与博客不一致：`EntityTypeIds`、`ColorCollection`、
      `BlockItemTags`）。
    - **`runClient` 验收要先确认测试环境可用**：`run-<版本>/mods` 里那批第三方模组声明了 MC 版本区间，
      升版本后会直接拒绝启动；临时移开做验证可以，但**跑完必须原样还原**，
      并在交付说明里写清「哪些模组还没有目标版本」——环境问题的失败不许当成代码问题的失败。
    - **日常操作三步（照抄即可，忘了就回来看这里）**：
      1. **平时写代码 —— 只在 `master` 上写**（功能、修 bug 都在这条线上，不在 `port/26.2` 上动业务）；
      2. **要出 26.2 —— 提交仍先在 `master` 落，再搬运**：`git checkout port/26.2` →
         `git cherry-pick <master 的 commit>` → `git checkout master` 切回来继续干活；
      3. **参考库不用手工分版本** —— 顶层 `01-开发参考库/工具/审计-*.js` 六个脚本自己读
         `gradle.properties` 的 `minecraft_version` 选本线源码/索引/jar，在两条分支上直接跑即可；
         人工文档（`快速索引/`、`API参考/`、`开发机制/`、`真实代码示例/`）**只保留一份正文**，
         受 26.2 影响的条目已就地标注「26.2 差异」，不要另建 26.2 副本。

232. **开发端启动：两条线同一条命令，运行目录与「开发端专属开关」各归各位。**

    - **启动命令（只有这一条，不用记第二个）**：仓库根目录 `.\gradlew.bat runClient`。
      跑哪个版本、用哪个运行目录，由**当前分支的 `gradle.properties#minecraft_version`**决定
      （`build.gradle` 的 `runs.client.runDir`）：目录名恒为 `run-<minecraft_version>` ——
      `26.1.2` → `run-26.1.2/`，`26.2` → `run-26.2/`。所以「26.1.2 开发客户端」= `master` 上跑，
      「26.2 开发客户端」= `port/26.2` 上跑，互不干扰。
    - **`run-*/` 不进版本库**（`.gitignore` 的 `/run-*/`），里面是本机实测环境：
      模组、存档、配置、资源包、光影、日志。新建版本线目录的口径：从正式版实例
      （如 `C:\Users\Administrator\Desktop\MC\.minecraft\versions\26.2`）1:1 拷 `mods` / `config` /
      `defaultconfigs` / `data` / `saves` / `resourcepacks` / `shaderpacks` / `schematics` / `xaero` /
      `options.txt`，**但必须剔除 `fabric-api-*.jar`** —— 它由 Loom 按 `gradle.properties` 提供
      （26.2 线为 `0.161.0+26.2`），目录里再放一份会重复加载而崩。
    - **开发端专属开关只写在 `run-<版本>/config/`，绝不写进业务代码**：
      `modernfix-mixins.properties` 末尾 user overrides 加 `mixin.devenv=false` —— ModernFix 的
      devenv 混入假定 `Minecraft.createUserApiService` 是 static，26.2 已改签名，`@Overwrite`
      直接抛 `InvalidMixinException` 起不来；正式版不走 devenv 路径，因此只需在开发端关掉。
    - **26.2 开发端有两处渲染兜底属于「代码」，只在 `port/26.2` 存在**（清单登记在 136 号复盘第四节的 E）：
      `MissingGlobalUniformFallback`（在 `RenderSystem#bindDefaultUniforms` 读 `Globals` 之前补建
      全局设置 UBO —— 26.2 首次资源重载排在首帧之前，图集上传取不到 `Globals`，开发端校验直接抛
      `Missing uniform Globals`）与 `VertexFormatBindingsPad`（把顶点格式绑定数组补足 16 槽 ——
      Iris 装包时只返回 1 槽，26.2 新增的 `validateDraw` 写死索引 16 槽而越界）。两者都只作用于
      开发环境才跑的校验路径，正式版行为一字未变。
    - **出包命名（两条线版本号相同，个人版与发布版都靠 MC 版本后缀区分）**：
      个人版 —— `.\gradlew.bat buildBoth` 一条命令出两条线的，都落 `build/libs/`：
      `yiyiaddon-<版本>-personal+26.1.2.jar` 与 `...-personal+26.2.jar`（**个人版一律带 MC 版本**，
      两条线不带就分不清谁能装、buildBoth 也会互相覆盖）；只出本线一份用 `buildPersonal`。
      混淆发布版 —— `.\gradlew.bat buildRelease` 出 `build/release/yiyiaddon-<版本>-<MC 版本>.jar`
      与同名 `.zip`（如 `yiyiaddon-1.0-beta1-26.1.2.jar`）。**发布包名是游戏内更新提示的判据**：
      `ReleaseCatalog.isReleaseAsset` 只认 `yiyiaddon-<版本>-<MC 版本>.zip|.jar`（兼容老的
      `yiyiaddon-<版本>.zip`），**并且只认本线 MC 版本的附件** —— 运行时版本由
      `ClientIdentity.gameVersion()`（Fabric 的 minecraft 容器版本）传入，取不到时退回不筛；
      `-personal…` 一律不算可安装包。两条命名口径是一套，改名字必须同步 `ReleaseCatalog` 与
      `gradle/update-tests/UpdateSelfTest.java`，否则更新提示会哑掉（或给玩家推一个装不上的包）。
      **同一个 `ClientIdentity.gameVersion()` 也用在界面上**：侧栏页头版本行、右上版本条与更新弹窗的
      版本标签都写成 `v1.0-beta1 · 26.1.2`（`ClickGuiScreen.versionLine()` 与
      `UpdateService.versionLabel()`），否则两条线装的是哪个从界面上看不出来。
      **对外发布时两条线的包挂同一个 Release（一个标签两个附件，各带自己的 MC 版本后缀）**，
      也就是公开页面那句「支持 26.x」；两个附件都要传完（`uploaded` 且 size>0），映射各存各的。
      注意同 tag 补传附件不会触发老玩家更新提示（版本号没变），要提示就得递增版本号。
      另一条线的个人版由 `gradle/build-both.ps1` 在系统临时目录建 `git worktree`
      （`--detach`，不占分支名）用一次性 JVM（`--no-daemon`）构建，拷回时换成 `+<它的 MC 版本>`，
      跑完自动撤掉 worktree，不碰当前工作区。
    - **公开页面写的支持范围是文案，不是加载器约束**：`06-发布页面` 首页与发布说明写
      「支持 Minecraft **26.x**（当前 26.1.2 与 26.2，26.3 及以后暂不支持）」，而各线的
      `minecraft_compat` 仍是自己的 `~<本线版本>`（master `~26.1.2`、port/26.2 `~26.2`）。
      **不许为了让页面文案成立去放宽 `minecraft_compat`** —— 两条线源码本就不同（26.2 有 100+ 处
      GUI API 改名与两处渲染兜底），放宽只会把「装错版本被 Fabric 明确拒绝」变成「装错进游戏就崩」。
      改这套口径要同步 `仓库首页/README.md`、`版本说明草稿.md`、`工具/生成预览.mjs`（侧栏注释）、
      `工具/制作动态封面.cjs`（封面底栏），再重跑生成命令，见 `06-发布页面/使用说明.md`。

233. **用户口令「同步仓库」= 提交并推两条线到 `source`，绝不碰 `origin`。**

    用户说「同步仓库」（或「上传仓库」「推一下代码」）时，按下面五步做完再报告，
    不要反过来问他要推哪个分支、要不要提交：

    1. **先看工作区**（`git status`）。有在途改动就按第 202 条的提交口径写成提交，信息写清
       「做什么、为什么」；**用户这一轮明确说了「先别提交」时，跳过提交只做推送**。
    2. **未跟踪文件先判性质**：属于项目产物的（报告、归档、源码、脚本）入库；本机临时产物
       （日志、缓存、构建产物、一次性 dump）不提交也不删——除非用户点名。
    3. **推主线**：`git push source master`（第 203 条：日常只推 `source`）。被拒时说明远端有新提交，
       **只报告，不擅自 `pull`／`rebase`／`force`**。
    4. **拉齐 26.2 线**：`master` 上有新提交，就按《日常开发.md》第 3 节 cherry-pick 到 `port/26.2`
       （当前没有 26.2 工作目录时用临时 worktree，做完 `git worktree remove` 撤掉，不要留在盘上），
       在 26.2 侧 `.\gradlew.bat compileJava` 过关后 `git push source port/26.2`。
    5. **收尾报告**：两条线 HEAD 与远端是否一致（`git branch -vv`）、工作区是否干净、
       本次提交了什么（一行一条）。

    与本条互补的常驻自动推送（**不替代本条**）：`01-开发参考库/工具/同步代码.ps1`
    ＋ 计划任务 `yiyiaddon-sync`，每 15 分钟把两条线**已提交**的内容推到 `source`；
    它只推不提交、不切分支、不碰工作区，所以在途改动仍然要靠「同步仓库」才入库。

234. **混淆归档：目录名带版本线，只删作废的，大块资料进仓外。**

    - **命名**：`05-混淆文件备份/<版本>/02-字符串与控制流加强版-<版本线>-<SHA-256 前 8 位>/`
      （如 `…-26.2-1bcf30d9`）。两条线版本号相同，只写摘要分不出属于 26.1.2 还是 26.2。
      加线标之前建的归档沿用旧名，**不要为整齐去批量改名**（早期那批无从可靠判定属于哪条线）。
      改这里的命名要同步 `gradle/obfuscation.gradle` 与 `05-混淆文件备份/README.md`。
    - **保留**：正常发布的映射一律保留（一份约 1.3 MB，大头就是纯文本 `mapping.txt`，git 压得很好），
      **不按版本删**。只清**作废归档** —— 产物被同名覆盖（同版本重出）或从未对外挂过的，
      映射已经没有对照对象，当场删掉。删不删只看「还有没有对应产物」，不看发了几个版本。
    - **体积**：反编译/反汇编这类一次性人工资料放本机 `d:\mcaddon\归档\`，不进仓库；
      仓库里只留 mapping 四件套与产物信息。要往仓库里加几十上百 MB 之前先问一句放哪儿。

## 三十七、混淆产物契约与生产实例验收铁律（2026-09-20 用户指令追加）

235. **Mixin 里不用「带初值的字段」承载状态；发布包必须在真机（生产实例）上验收。**

    - **契约**：Mixin 类的 `@Unique` 字段初值，要靠 Mixin 把 Mixin 类构造器并进目标类构造器时才会执行。
      混淆产物上这一步实测没生效 —— 字段挂上去了、初值没进去，读出来恒为 `null`。
      （2026-09-20：线上两个发布包进单人世界约 45 秒必崩，`stopDestroyBlock` 原版每帧都会走到，直接 NPE 退出。）
      字节码层面，混淆版**该字段与初值指令都在、注解在、指令序列与未混淆版逐条一致**，
      所以**不能拿「开发端跑过」推断混淆包没问题**。
    - **写法**：Mixin 里要用单例或共享状态，就在注入点直接调取值方法（如 `X.instance()`）；
      不要缓存进 `@Unique` 实例字段，也不要在 Mixin 里做字段/静态初始化。
    - **验收**：`buildRelease` 的静态检查（改名数、资源不变、字节码可加载、常量审计）**只证明能加载，不证明能跑**。
      发布前用独立生产实例验收：真 Fabric Loader + 对应版本 Fabric API + **从发布 ZIP 解出的 JAR**，
      `--quickPlaySingleplayer` 进单人世界，看日志有无异常行与崩溃报告。
      同一实例换未混淆的个人版做对照 —— 用来区分「混淆引入」还是「本来就坏」。
      验收脚本在仓外（当前 `D:\mcv\prod-test.ps1`，实例与 jar 同目录自动搭），要长期保留就挪进 `01-开发参考库/工具/`。
    - **映射与摘要同步**：每条线、每个摘要各存一份映射，不能跨构建混用；
      出完包要同步 `06-发布页面/使用说明.md` 与 `05-混淆文件备份/README.md` 里记的线上摘要。
    - **同标签换附件不会触发更新提示**（版本号没变，已装的玩家收不到），
      所以发布说明里要写明「之前下载过的请重新下载」。








