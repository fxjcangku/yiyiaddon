# AGENTS.md —— 智能体入口（**第一件事读这里**）

> 本文件是**入口钩子**，只有一屏：确认项目性质 + 把你送到规则正本。
>
> **规则正本是 [`开发习惯.md`](开发习惯.md)（36 章 / 233 条）—— 开始任何工作前必须完整读一遍**，
> 读完才允许扫描代码、改代码、写新功能。两者冲突时一律以 `开发习惯.md` 为准
> （其第二十三章「改动禁区」优先级最高）。

---

## 零、分支纪律（**先记住这个，别改错分支**）

本项目**同一个仓库跑两条版本线**，不是两份工程目录：

| 线 | 分支 | 定位 |
| --- | --- | --- |
| 26.1.2 主线 | `master` | **业务唯一真源**（功能 / 判据 / 文案 / 渲染 / 组件） |
| 26.2 版本线 | `port/26.2` | **只放版本适配差异**（官方 API 改名、常量搬家、Mixin 注入点、依赖与构建坐标） |

**业务改动一律先在 `master` 落地，再 cherry-pick 到 `port/26.2`；禁止直接在 `port/26.2` 上改业务代码。**
自查口径：这次改动拿到 `master` 上看是否成立 —— 若与 MC 版本无关（`master` 上就是错的），说明改错了分支。
完整规则见 `开发习惯.md` 第三十六章（第 231 条）。

---

## 一、这是什么项目（先记住）

| 项 | 内容 |
| --- | --- |
| **性质** | **移植项目（移植阶段已收口）**：旧项目 `D:/mcaddon/26.1.2` 的功能、算法、状态机、数据、配置、用户文案**已逐模块搬进本项目，只换 UI 外壳** |
| **状态** | **修 bug 阶段**（2026-09-19 用户确认移植完成）—— 产出以「用户实机报缺陷 → 定位 → 修复 → 复测」为主，**不要再把「搬新模块」当默认任务**；缺陷流程走 `开发习惯.md` 第二十八章 |
| **目标产物** | 独立 Minecraft Fabric 客户端模组，**两条版本线**：26.1.2（`master`）/ 26.2（`port/26.2`）；包根 `com.yiyiaddon`，Mod ID `yiyiaddon`，**不依赖任何第三方客户端框架** |
| **迁移口径正本** | `02-开发报告/项目开发报告/00-总纲与基准/01-总纲-迁移口径与复刻原则.md`（全量复刻 + 只换外壳） |
| **进度清单** | `02-开发报告/项目开发报告/00-总纲与基准/30-移植总览-已移植与未移植全清单.md` —— **动任何模块前先查它在不在清单里** |

## 二、必读顺序（读完第 1 项再往下）

| # | 文件 | 作用 |
| --- | --- | --- |
| 1 | `开发习惯.md` | **规则正本**：代码分类、语言、报告、UI 铁律、改动禁区、取证铁律、埋点修复规范…（36 章 233 条）。**第三十六章是双版本线分支纪律，动代码前先看** |
| 2 | `02-开发报告/项目开发报告/00-总纲与基准/00-AI阅读指引.md` | 报告地图与阅读顺序 |
| 3 | `01-开发参考库/README.md` | API 查询顺序与指定工具（第 159、207 条） |

## 三、最容易踩的硬约束（速查，全文见 `开发习惯.md`）

| 条 | 约束 |
| --- | --- |
| 231 | **双版本线**：业务改动一律先在 `master` 落地再 cherry-pick 到 `port/26.2`；禁止直接在 26.2 分支改业务（两条线只允许差官方 API 口径） |
| 233 | **口令「同步仓库」**：用户说这句 = 提交并推两条线到 `source`（`port/26.2` 侧先 `compileJava` 过关再推），绝不推 `origin`；不要反问他推哪个分支、要不要提交 |
| 163~165 | **项目完成前禁止擅自新增**；非必要不得擅自修改；一切改动先经用户同意 |
| 171 | 移植不得夹带新功能；要加必须先问（并说清「旧项目对应物是什么 / 为什么非加不可 / 影响面」） |
| 167 | **修复前必须取证**：说得出「哪一行代码 / 哪次提交 / 哪个真实文件」，否则不许动手 |
| 169 | 同源逻辑禁止留两份（判据、文案、渲染、组件都算） |
| 180 | 模块页面构建推迟到真正打开时；改 Mixin 后必须真跑一次 `runClient` 验收 |
| 208 | 代码必须美观 + 详细中文注释（与编译通过同级） |
| 212 | 使用说明必须与实现保持最新（交付前必跑） |
| 225 | 窗口标题与左侧返回键垂直居中；**只有标题下方真有说明文字**的窗口才例外（有说明时标题与说明成组，保持高位） |
| 218 | **埋点修复（重武器）只在**「用户报的 bug + 常规流程走完仍定性不了 + 用户同意」时启用 |
| 222/223 | 埋点插桩结案必须删干净；**永不许进发布产物** |

## 四、目录地图

| 路径 | 用途 |
| --- | --- |
| `src/main/java/com/yiyiaddon/` | 正式代码（唯一参与编译的源码树） |
| `01-开发参考库/` | 只读参考资料，**分两套**：`Minecraft-26.1.2-*`（主线）与 `Minecraft-26.2-*`（26.2 线，源码 + 索引 + 查询脚本）。不进 jar、不进版本库（`.gitignore`）。**按分支取用，禁止混用**（第 22 章） |
| `02-开发报告/项目开发报告/` | **开发报告**（中文、带日期、编号接续，禁止散落） |
| `03-后端API/` | 后端 API 与部署脚本 |
| `04-埋点修复/{进行中,已修复}/` | 运行时埋点修复案例（方案见 `开发习惯.md` 第三十三章） |
| `开发习惯.md` / `AGENTS.md` | 规则正本 / 本入口钩子 |

## 五、每轮交付标准（缺一不算完成）

1. **先取证再改**（第 167 条），改动最小化（第 192 条）。
2. `.\gradlew build --console=plain -q` → **EXIT=0**；并明确说明**是否实机验证**，
   未实机必须显式标注「未实机」（第 19、46、193 条）。
3. 在 `02-开发报告/项目开发报告/` 写一份中文复盘报告（编号接续）。
4. 语言：用户可见内容与注释一律中文，禁止 Emoji（第 11~13 条）。
5. 提交：日常只推 `source`（私有源码仓库）；`origin` 只放混淆产物（第 202~205 条）。
6. 用户说「**同步仓库**」（或「上传仓库」「推一下代码」）＝ 提交并推两条线到 `source`：
   按 `开发习惯.md` 第 233 条的五步走（提交口径 → 判未跟踪文件性质 → 推 `master` →
   cherry-pick 到 `port/26.2` 并编译过关后推 → 报告两线状态），**绝不推 `origin`**。
   另有计划任务 `yiyiaddon-sync` 每 15 分钟自动推**已提交**的内容（只推不提交、不切分支），
   它不替代这条口令 —— 在途改动仍要靠「同步仓库」才入库。
