# AGENTS.md —— 智能体入口（**第一件事读这里**）

> 本文件是**入口钩子**，只有一屏：确认项目性质 + 把你送到规则正本。
>
> **规则正本是 [`开发习惯.md`](开发习惯.md)（33 章 / 224 条）—— 开始任何工作前必须完整读一遍**，
> 读完才允许扫描代码、改代码、写新功能。两者冲突时一律以 `开发习惯.md` 为准
> （其第二十三章「改动禁区」优先级最高）。

---

## 一、这是什么项目（先记住）

| 项 | 内容 |
| --- | --- |
| **性质** | **移植项目**：把旧项目 `D:/mcaddon/26.1.2` 的功能、算法、状态机、数据、配置、用户文案**逐模块搬进本项目，只换 UI 外壳** |
| **状态** | **正在进行中，尚未完成** —— 模块是一个一个迁移的，不要假设某个模块已经存在或已经完整 |
| **目标产物** | 独立 Minecraft 26.1.2 Fabric 客户端模组；包根 `com.yiyiaddon`，Mod ID `yiyiaddon`，**不依赖任何第三方客户端框架** |
| **迁移口径正本** | `02-开发报告/项目开发报告/01-总纲-迁移口径与复刻原则.md`（全量复刻 + 只换外壳） |
| **进度清单** | `02-开发报告/项目开发报告/30-移植总览-已移植与未移植全清单.md` —— **动任何模块前先查它在不在清单里** |

## 二、必读顺序（读完第 1 项再往下）

| # | 文件 | 作用 |
| --- | --- | --- |
| 1 | `开发习惯.md` | **规则正本**：代码分类、语言、报告、UI 铁律、改动禁区、取证铁律、埋点修复规范…（33 章 224 条） |
| 2 | `02-开发报告/项目开发报告/00-AI阅读指引.md` | 报告地图与阅读顺序 |
| 3 | `01-开发参考库/README.md` | API 查询顺序与指定工具（第 159、207 条） |

## 三、最容易踩的硬约束（速查，全文见 `开发习惯.md`）

| 条 | 约束 |
| --- | --- |
| 163~165 | **项目完成前禁止擅自新增**；非必要不得擅自修改；一切改动先经用户同意 |
| 171 | 移植不得夹带新功能；要加必须先问（并说清「旧项目对应物是什么 / 为什么非加不可 / 影响面」） |
| 167 | **修复前必须取证**：说得出「哪一行代码 / 哪次提交 / 哪个真实文件」，否则不许动手 |
| 169 | 同源逻辑禁止留两份（判据、文案、渲染、组件都算） |
| 180 | 模块页面构建推迟到真正打开时；改 Mixin 后必须真跑一次 `runClient` 验收 |
| 208 | 代码必须美观 + 详细中文注释（与编译通过同级） |
| 212 | 使用说明必须与实现保持最新（交付前必跑） |
| 218 | **埋点修复（重武器）只在**「用户报的 bug + 常规流程走完仍定性不了 + 用户同意」时启用 |
| 222/223 | 埋点插桩结案必须删干净；**永不许进发布产物** |

## 四、目录地图

| 路径 | 用途 |
| --- | --- |
| `src/main/java/com/yiyiaddon/` | 正式代码（唯一参与编译的源码树） |
| `01-开发参考库/` | 只读参考资料（Minecraft 26.1.2 官方源码、Baritone 源码、索引工具），不进 jar、不进版本库 |
| `02-开发报告/项目开发报告/` | **开发报告**（中文、带日期、编号接续，禁止散落） |
| `03-后端API/` | 后端 API 与部署脚本 |
| `04-埋点修复/{进行中,已修复}/` | 运行时埋点修复案例（方案见 `开发习惯.md` 第三十三章） |
| `开发习惯.md` / `AGENTS.md` | 规则正本 / 本入口钩子 |

## 五、每轮交付标准（缺一不算完成）

1. **先取证再改**（第 167 条），改动最小化（第 192 条）。
2. `.\gradlew build --console=plain -q` → **EXIT=0**；并明确说明**是否实机验证**，
   未实机必须显式标注「未实机」（第 19、46、193 条）。
3. 在 `02-开发报告/项目开发报告/` 写一份中文复盘报告（编号接续）。
4. 语言：用户可见内容与注释一律中文，禁止 Emoji（第 11~13 条）。
5. 提交：日常只推 `source`（私有源码仓库）；`origin` 只放混淆产物（第 202~205 条）。
