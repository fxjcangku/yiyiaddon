# 混淆文件备份里的四件套由构建工具直接写出，verifyObfuscatedJar 会逐字节比对
# （见 gradle/obfuscation.gradle 的 saveBackup：内容不同就拒绝覆盖）。
# 若让 git 做行尾转换，检出的字节与构建输出不一致，同一产物再构建一次就会被误判成
# 「混淆备份已有不同内容，拒绝覆盖」。这里禁用转换，保证逐字节往返。
05-混淆文件备份/** -text
