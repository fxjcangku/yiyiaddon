# 日常开发（双版本线操作手册）

> 一句话记住：**业务只在 `master` 写；要出 26.2 就把 `master` 的提交 `cherry-pick` 到 `port/26.2`；
> 想让智能体传代码，说一句「同步仓库」。**
> 配套规则见 `开发习惯.md` 第 231、232、233 条；26.2 侧差异清单见 136 号复盘第四节。

---

## 0. 两条线

| 分支 | Minecraft | 开发客户端跑在 | 模组环境 |
| --- | --- | --- | --- |
| `master` | 26.1.2 | `run-26.1.2/` | 104 个 26.1.2 模组 |
| `port/26.2` | 26.2 | `run-26.2/` | 103 个 26.2 模组（含 Meteor + 汉化） |

运行目录名 = `run-<Minecraft 版本>`，两个都**不进版本库**（`.gitignore` 的 `/run-*/`），里面是本机实测环境。

---

## 1. 启动开发客户端

命令**只有一条**，跑哪个版本由「当前在哪个分支」决定：

```powershell
.\gradlew.bat runClient
```

```powershell
git checkout master      # 之后 runClient = 26.1.2 客户端
git checkout port/26.2   # 之后 runClient = 26.2 客户端
```

**正写着 26.1.2 的代码、又只想起一次 26.2**（不想把工作区切走）：给 26.2 开一个临时工作目录，
`run-26.2` 用目录联接指回真目录（模组存档共用一份），跑完就撤：

```powershell
git worktree add D:\mcaddon\_wt262 port/26.2
New-Item -ItemType Junction -Path D:\mcaddon\_wt262\run-26.2 -Target D:\mcaddon\yiyiaddon\run-26.2
cd D:\mcaddon\_wt262 ; .\gradlew.bat runClient

# 用完：先撤联接、再撤工作目录 —— 顺序不能反（先删目录会有跟随联接删掉真 run-26.2 的风险）
[System.IO.Directory]::Delete('D:\mcaddon\_wt262\run-26.2', $false)
git -C D:\mcaddon\yiyiaddon worktree remove --force D:\mcaddon\_wt262
```

---

## 2. 日常写功能（占 99% 的时间）

```powershell
git checkout master
# ...写代码...
.\gradlew.bat runClient            # 在 26.1.2 上验证
git add <你改的文件>
git commit -m '说明这次改了什么'
```

**不要**在 `port/26.2` 上写业务代码。

---

## 3. 把新插件 / 新功能同步到 26.2（核心流程）

前提：这次改动**已经在 `master` 上提交过**。然后：

```powershell
git checkout port/26.2
git cherry-pick <master 上的 commit hash>
```

> 手里有**没提交**的改动时别切分支（会一起被切走）。两个替代：先 `git stash`，搬完回来 `git stash pop`；
> 或给 26.2 开临时 worktree（做法见第 1 节末），搬完 `git worktree remove` 撤掉。

- **一次搬多个提交**：`git cherry-pick <最早>^..<最晚>`
- **冲突了怎么解**：冲突几乎只出在官方 API 口径上（`EntityType` 拆类、`setScreen` 搬进 `Gui`、
  `Minecraft.gui.hud`、常量搬家、Mixin 注入点）。按 **26.2 的写法**改完，然后：

```powershell
git add <解完冲突的文件>
git cherry-pick --continue
```

- **想在 26.2 上验收**：

```powershell
.\gradlew.bat compileJava          # 编译
.\gradlew.bat runClient            # 起 26.2 开发客户端（run-26.2/）
node 01-开发参考库\工具\审计-mixin.js
node 01-开发参考库\工具\审计-mixin注册.js
node 01-开发参考库\工具\审计-mixin验证.js
node 01-开发参考库\工具\审计-import对账.js
node 01-开发参考库\工具\审计-负面清单.js
node 01-开发参考库\工具\审计-baritone命令名.js
```

- **搬完切回来继续干活**：

```powershell
git checkout master
```

> 审计脚本会自己读 `gradle.properties` 的 `minecraft_version` 选本线源码/jar，
> 所以**必须先切到对应分支再跑**，不要跨分支跑。

---

## 4. 哪些东西只能写在 `port/26.2`

两条线**只允许差「官方 API 口径」**，不许差业务逻辑：

- 切屏入口（`Gui#setScreen`）、常量来源（`EntityTypeIds` / `ColorCollection` / `BlockItemTags`）
- Mixin 注入点改名、依赖与构建坐标（如 Baritone 1.18.0 → 1.19.0）
- 26.2 专属兜底（`MissingGlobalUniformFallback` / `VertexFormatBindingsPad`，见 136 号复盘第四节 E）

自查口径：把这次改动拿到 `master` 上看——**如果 `master` 上是错的（错误与 MC 版本无关），说明改错分支了**，
必须挪回 `master` 再 cherry-pick。

---

## 5. 往开发端装模组（`run-26.2/mods`）

直接丢 jar 进去，重启客户端即可。两条硬规矩：

1. **不要放 `fabric-api-*.jar`** —— Loom 已按 `gradle.properties` 提供（26.2 线为 `0.161.0+26.2`），
   目录里再放一份会重复加载而崩。
2. **必须用 26.2 官方构建的 jar**。26.1 起 Minecraft 走官方名称体系，运行时命名空间是 `official`，
   老版本那种声明 `intermediary` 的 jar 会直接起不来：

   ```
   ClassTweakerFormatException: Namespace (intermediary) does not match current runtime namespace (official)
   ```

   （实机日志实证：xiaohe66-meteor-lotus 17.2-1.21.11 就是这个报错，改元数据也救不了，
   必须等作者发 26.2 版。）

`run-26.2/config/` 里的**开发端专属开关**不要动，也不要写进业务代码：

- `modernfix-mixins.properties` → `mixin.devenv=false`（ModernFix 假定 `createUserApiService` 是 static，26.2 已改签名）
- `iris.properties` → 目前是 `enableShaders=false`（2026-09-20 为定位流星界面显示问题留下的状态，随时可改回 `true`；
  开光影本身不会崩，顶点格式数组已由 `VertexFormatBindingsPad` 兜住）

流星（Meteor）在 26.2 开发端还有一条**必须**的口径，写在 `run-26.2/meteor-client/config.nbt` 里：

- **`custom-font=false`**（游戏内就是 Config 页把「Use a custom font」关掉）。开着的话流星走自有字体纹理，
  26.2 下这套纹理会在界面绘制前被关掉 —— 开发端直接抛 `Texture view Sampler0 has been closed!` 起不来，
  正式版不做校验则表现为**菜单一片空白**。关掉后走原版字体渲染，中文照常显示。
  这份配置与 `run-26.1.2` 里能用的那份逐字节同款，只改了版本号。

---

## 6. 正版登录（已配好，不用再装）

开发客户端用的是 **DevAuth Neo**，配置全在 `build.gradle` 里，两线都生效：

- 依赖：`localRuntime 'maven.modrinth:dev-auth-neo:1.1.1'`（只进开发运行时，不进发布产物）
- 参数：`-Ddevauth.enabled=1`、`-Ddevauth.account=main`、`-Ddevauth.microsoft.oauth2.grant-flow=auth-code-external`（走系统浏览器登录，不用内嵌 CEF）
- 令牌缓存：`C:\Users\<你>\.dev-auth-neo\microsoft-accounts.json`（账号键 `main`，不进版本库）

**生效标志**：启动日志里出现

```
[main/INFO] (DevAuthNeo) DevAuth active for account 'main'
```

此时 `Setting user: yiyijia` 用的是**正版账号**（你的正版 ID 恰好也叫 `yiyijia`，所以和离线名长得一样，
别靠名字判断——真正的判据是上面那行 DevAuth 日志，以及 `run-<版本>/downloads/<正版UUID>/` 这个目录名）。

**令牌过期怎么办**：`session` 只有几小时，过期后 DevAuth 自动拿 `refreshToken` 续期，正常情况下你什么都不用做。
只有在**改过微软密码 / 撤销过授权**时才需要重登：删掉 `~/.dev-auth-neo/microsoft-accounts.json`，
再 `runClient` 一次，会自动打开浏览器让你登录。

---

## 7. 同步与推送（口令：同步仓库）

跟智能体说 **「同步仓库」** 就行（等价说法「上传仓库」「推一下代码」）—— 它按 `开发习惯.md` 第 233 条执行：
提交在途改动 → 推 `master` → cherry-pick 拉齐 `port/26.2`（编译过关再推）→ 报告两线状态。
**只推 `source`（私有源码仓），永远不推 `origin`（发布仓）。**

三句常用的补充话：

| 你说 | 它做 |
| --- | --- |
| 同步仓库 | 提交 + 推两条线 |
| 同步仓库，先别提交 | 只推已提交的内容，在途改动不动 |
| 别推，我还没写完 | 什么都不做 |

不想每次都喊人，还有常驻兜底（只推不提交、不切分支、不碰工作区）：

```powershell
# 手动跑一轮
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\mcaddon\yiyiaddon\01-开发参考库\工具\同步代码.ps1" -Once

# 计划任务 yiyiaddon-sync：每 15 分钟自动推一轮；日志在 01-开发参考库\工具\同步记录.log
schtasks /query  /tn "yiyiaddon-sync"
schtasks /delete /tn "yiyiaddon-sync" /f      # 不想要了就删掉
```

---

## 8. 常见坑

| 现象 | 处理 |
| --- | --- |
| `git checkout` 报 `untracked working tree files would be overwritten: maven-repo/baritone/baritone-fabric/1.19.0/baritone-fabric-1.19.0.jar` | 先 `Remove-Item` 删掉那个 jar 再切。它在 `port/26.2` 里是**追踪**文件，切过去会从 git 自动恢复（它只属于 26.2 线，`master` 用 1.18.0） |
| 切分支后 `runClient` 起的是另一个版本 | 正常——运行目录跟着 `minecraft_version` 走，确认一下 `git branch --show-current` |
| 26.2 开发端启动崩 `Missing uniform Globals` | 兜底已被 136 号复盘第四节 E 覆盖；若仍出现，说明 `port/26.2` 上少了 `RenderSystemMixin` |
| 装了光影进世界崩 `Index N out of bounds for length 1` | 同上，缺 `GlCommandEncoderValidateMixin` |
| `run-26.1.2/mods` 和 `run-26.2/mods` 混了 | 两套模组环境完全独立，别互相拷，尤其别把 26.1.2 的 jar 放进 `run-26.2` |
| 启动日志**没有** `DevAuth active`，进服提示非正版 | 令牌失效了：删 `~/.dev-auth-neo/microsoft-accounts.json` 后重跑一次，按提示在浏览器登录 |
| 流星菜单打不开：开发端崩 `Texture view Sampler0 has been closed!`，正式版则一片空白 | 流星的「Use a custom font」必须是**关**（`run-26.2/meteor-client/config.nbt` 里 `custom-font=false`），见第 5 节 |
| `git status` 冒出 `?? maven-repo/baritone/baritone-fabric/1.19.0/` | 切分支带出来的残留（它在 `port/26.2` 是追踪文件，`master` 只认 1.18.0）。直接删掉即可，26.2 线版本库里仍有它 |
| 流星菜单能开、中文也在，只是界面偏透 | 正常：底就是半透明，26.2 版流星没有背景模糊（官方 GUI 重写后的现状），不影响使用 |

---

## 9. 出包

两条线的版本号相同，所以**个人版和发布版都靠 MC 版本后缀区分**：
个人版是 `personal+<MC 版本>`，发布版是 `-<MC 版本>`。

### 个人版（两条线一次出齐）

```powershell
.\gradlew.bat buildBoth
```

都落在 `build\libs\`：

| 产物 | 是哪条线 |
| --- | --- |
| `yiyiaddon-1.0-beta1-personal+26.1.2.jar` | 本线的（当前在 `master` 就是 26.1.2） |
| `yiyiaddon-1.0-beta1-personal+26.2.jar` | 另一条线 |

- 只出本线一份：`.\gradlew.bat buildPersonal`。
- 另一条线的构建走 `gradle/build-both.ps1`：在系统临时目录建 `git worktree` 签出另一条分支、
  用一次性 JVM（`--no-daemon`）构建，拷回本线时换成 `+<它的 MC 版本>` 后缀，完事自动撤掉 worktree，
  **全程不碰当前工作区**。

### 混淆发布版（逐条线各出一次）

```powershell
.\gradlew.bat buildRelease
```

产物在 `build\release\`：**`yiyiaddon-1.0-beta1-<MC 版本>.jar`** 与同名 `.zip`
（`master` 上就是 `yiyiaddon-1.0-beta1-26.1.2.jar`），映射自动备份到 `05-混淆文件备份/`。

`buildRelease` 只出当前分支那一条线，另一条线要切过去（或临时 worktree）再跑一次。
发布版的名字是**游戏内更新提示的判据**（`ReleaseCatalog.isReleaseAsset`）：认
`yiyiaddon-<版本>-<MC 版本>.zip|.jar`，**且只认本线 MC 版本的附件**（运行时版本取自 Fabric 的
minecraft 容器；取不到时不筛），**个人版一律不当成可安装包**。所以某次只挂了 26.2 的包时，
26.1.2 的玩家不会被误提示去下一个装不上的包。

### 发布（两条线的混淆包挂同一个 Release）

**一个 Release 页挂两个附件**，页面口径就是「支持 26.x」：

```
v1.0-beta1
  ├─ yiyiaddon-1.0-beta1-26.1.2.zip   ← 给 26.1.2 玩家（master 上 buildRelease 出的）
  └─ yiyiaddon-1.0-beta1-26.2.zip     ← 给 26.2 玩家（port/26.2 上 buildRelease 出的）
```

发布前确认三件事：
1. **两份都构建过、都有映射备份**（`05-混淆文件备份/1.0-beta1/02-字符串与控制流加强版-<各自摘要>/`），
   还原堆栈要按出问题的那个包的 SHA-256 选对应映射，不能混用。
2. **两个附件都上传完**（`state=uploaded` 且 size>0）—— 更新提示只看已传完的包。
3. **页面正文（`版本说明草稿.md`）说清两个附件按游戏版本选**，且 `1 更新日志` 已按
   `07-bug记录/<本次版本>.md` 的台账誊全（发现 bug 就往台账记，发布时才有料可写，见第四十一章）。

⚠️ **同一个 tag 补传附件不会触发老玩家的更新提示**（版本号没变）：`v1.0-beta1` 已经公开过，
这次加挂 26.2 的包，已经装着 beta1 的人不会收到提示。想让老玩家收到提示，下次必须**递增版本号**。

---

## 10. 一分钟速查

```powershell
# 写功能
git checkout master                     ; .\gradlew.bat runClient

# 同步到 26.2
git checkout port/26.2                  ; git cherry-pick <hash>
.\gradlew.bat runClient                 # 26.2 上验收
git checkout master

# 只跑 26.2
git checkout port/26.2                  ; .\gradlew.bat runClient

# 出包：两条线的个人版一次出齐 → build\libs\
.\gradlew.bat buildBoth

# 出包：混淆发布版 → build\release\yiyiaddon-1.0-beta1-<MC 版本>.jar（只出当前分支那条线）
.\gradlew.bat buildRelease

# 传代码：对着智能体说一句就行
#   同步仓库           = 提交 + 推 source 两条线
#   同步仓库，先别提交  = 只推已提交的内容
```
