# 反编译测试归档

日期：2026-09-19

想看别人反编译后能看到什么，请先打开 `01-完整反编译结果-Vineflower/com/yiyiaddon/` 下的 Java 文件。

| 中文目录 | 内容 |
| --- | --- |
| 01-完整反编译结果-Vineflower | 对整个发布 JAR 的反编译输出，包含 760 个 Java 文件及工具输出的其他资源 |
| 02-抽样反编译结果-CFR | 三个真实 class 的反编译结果，用于比较不同工具的还原表现 |
| 03-解压字节码样本 | 从发布包抽取的三个 class 文件，是抽样而非完整解压目录 |
| 04-对应混淆发布包 | 本次接受测试的原始混淆 JAR 副本，可用压缩软件打开 |
| 05-测试日志与参数 | 原测试日志及参数；参数含原机器绝对路径，归档不自动改写，换环境需调整 |

CFR 的参数文件仅对应最后一个抽样，不能当成三个样本的完整参数记录。完整测试范围及边界见开发报告 112。

文件夹和说明已汉化；Java 文件名、包名、类名及工具原始输出保留，便于与 class 和名称映射核对。反编译结果可能含伪代码，不能当成可直接编译的原项目源码。

对应 JAR SHA-256：`6116886369f23dcee385458a24a117349db0f763a5fede6d405d973154bb1a2b`。

本次为复制归档，原 `build/obfuscation/` 输出保留；874 个复制文件已逐项比对 SHA-256，记录在 `文件校验清单.csv`。该清单只列复制的原始文件，不含本说明与清单自身。清理 build 不会删除本目录。

本目录不参与模组编译或运行，不影响功能。本次归档不代表重新做了反编译或游戏验收，也不是以后每次构建自动生成的反编译备份。这里是内部资料，尤其相邻目录中的 mapping.txt 不应公开。
