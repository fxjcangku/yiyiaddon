# 混淆文件备份

日期：2026-09-20（首次公开 Beta 之后更新）

2026-09-19 那轮供人工查看的反编译结果已**移出仓库**，保存在本机 `d:\mcaddon\归档\反编译测试归档-1.0-beta1-61168863\`
（103.9 MB：完整反编译结果、CFR 抽样、解压字节码、对应发布包和测试日志，中文目录说明见其中的 `阅读说明.md`）。
移出原因：它是一次性人工归档，占了大半个目录体积，跟着仓库走没有意义；`buildRelease` 不会自动运行反编译器或更新这些结果。

运行 ` .\gradlew buildRelease --console=plain `，混淆产物通过检查后自动备份到：

```text
05-混淆文件备份/<版本>/02-字符串与控制流加强版-<版本线>-<SHA-256前8位>/
    mapping.txt          类、方法和字段名称映射
    proguard-rules.pro   本次保留及混淆规则
    hardening-stats.json 字符串和控制流覆盖统计
    protection-audit.txt 最终发布包加密常量与字节码审计
    产物信息.txt          JAR 名称、版本、校验值和验证状态
```

发布包在 `build/release/`；本目录独立于 `build/`，`gradlew clean` 不会删除它。
相同产物的备份可重复使用，已有不同内容时构建报错，不覆盖历史。
目录采用中文标签 + 版本线 + 摘要前 8 位（新产物形如 `02-字符串与控制流加强版-26.2-<摘要>`），
完整 SHA-256 保存在各目录的产物信息中；加线标之前建的归档沿用旧名，看不出属于哪条线时看
`产物信息.txt` 的「产物」行或查对应报告。
`01-基础混淆版-21413309` 是历史基线；`02-字符串与控制流加强版-61168863` 是 2026-09-19 加固验收用的产物，
它的反编译测试资料已按上面说的移出仓库，目录里只剩 mapping 四件套。
线标从 2026-09-20 那批新产物起生效（`…-26.1.2-db9a5b72` / `…-26.2-43a3b738`）；更早的归档沿用旧名，
要认属于哪条线看 `产物信息.txt` 的「产物」行（JAR 名带 `-26.1.2` / `-26.2`）或对应的发布记录。
按出问题的 JAR 的 SHA-256 选择对应映射，不能使用其他构建的映射还原堆栈。

**保留规则**：每份归档约 1.3 MB（大头就是 1.34 MB 的 `mapping.txt`，纯文本，git 压得很好），
所以正常发布的映射**一律保留**，不按版本删。要清只清**作废归档** —— 产物被同名覆盖（同版本重出）
或从未对外挂过的那些，映射已经没有对照对象，当场删掉；体积大的反编译/反汇编资料一律放仓外，
不进本目录。删除与否只看「还有没有对应的产物」，不看发布了几版。

本目录是内部资料，不得上传到公开发布仓库 `origin`、Release 附件或发送给普通用户。
映射会暴露原始类名、方法名与字段名。源码提交与推送仍需用户明确授权。

当前方案使用免费的 ASM 9.9.1 自研变换与 ProGuard 7.9.1：
AES-256-GCM 字符串加密、ConstantDynamic 延迟解密、基本块随机重排、加密整数驱动的跳转分派、名称混淆与调试信息清理。
保留 Fabric 入口、Mixin 契约、枚举配置、Gson 字段与原始资源；第三方嵌套依赖不做二次加固。
构造器、异常处理、同步方法等不适合安全重排的代码跳过控制流变换，公开常量与资源仍可能包含明文。
解密材料必须随客户端分发，因此不能保证无法反编译、阻止 AI 还原或达到“免费最高级别”。

`buildRelease` 自动执行行为对照、最终常量审计、类加载和资源检查；
可单独运行 `testHardening` 或 `auditProtectedConstants` 排查问题。
普通 `build` 或 `buildPersonal` 生成 `build/libs/yiyiaddon-<版本>-personal+<MC 版本>.jar`。
`buildRelease` 生成 `build/release/yiyiaddon-<版本>-<MC 版本>.jar` 与同名 `.zip`（如 `yiyiaddon-1.0-beta1-26.1.2.jar`），公开版不带混淆后缀；ZIP 内只有该发布 JAR。两条版本线的版本号相同，个人版与发布版都靠 MC 版本后缀区分，不带就分不清谁能装；发布包名同时是游戏内更新提示的判据，个人版不会被当成可安装包，**而且只认本线 MC 版本的附件**（运行时版本取自 Fabric 的 minecraft 容器），免得某次只挂了一条线的包时另一条线的玩家被误提示。改名字要同时改 `ReleaseCatalog` 与 `gradle/update-tests/UpdateSelfTest.java`。
任务内部名称仍保留 obfuscated，便于维护，不影响对外文件名。历史归档名称保留原状。
每次真正重新加固使用新随机材料，应按实际 JAR 摘要保存对应备份，不能跨构建混用；**因此本地 `build/release/` 里的包不等于线上那份，比对只认线上附件摘要。**
JAR 内附有 `META-INF/反编译声明.txt`，声明不能代替技术保护。

**2026-09-21 线上状态（第六次覆盖）**：把界面这批改动收口后重出这条对 —— ZIP `f3c45857…`（26.1.2，内含 JAR `e0358a4d…`）与 `bbc622d2…`（26.2，内含 JAR `b61cfecc…`），回读线上附件的名称 / 大小 / SHA-256 与本地逐一相等；对应映射 `02-字符串与控制流加强版-26.1.2-e0358a4d` 与 `…-26.2-b61cfecc`。包内改动：模块中心分组展开改整块高度动画（修展开抖动与滚动条挤压）、面板玻璃不再逐帧复用世界帧（修开着「面板模糊」时背景闪烁）、侧栏品牌卡三行居中、动画平滑时钟 `FrameClock`、模块行状态对齐、面板主题默认深色。同步口径：master `03e8c4f` → `port/26.2` `2d09b54`（cherry-pick 干净落地、零冲突）。两份都起独立生产实例（Fabric Loader 0.19.5 + 对应 Fabric API + 从发布 ZIP 解出的 JAR，`--quickPlaySingleplayer`，180 秒）跑满全程：`crash-reports` 0 个、日志无模组包名栈帧、异常只剩离线账号的 401 / Realms 无害行、正常存盘退出（比第五次覆盖的降级验收升了一档）。附件下载次数 0 / 0，发布页正文更新为六条更新日志（按第 240 条未写「之前下载过的请重新下载」），下载表未改。被覆盖那对（映射 `4a18feb1` / `1b3e9325`）按保留规则留着；本目录没有需要清理的作废归档。明细见报告 185。

**2026-09-21 线上状态（第五次覆盖）**：先把 `port/26.2` 线补齐到与 `master` 同一份业务代码（cherry-pick `00232cc` / `4668e64` / `dbb3190`，三笔全部干净落地、无冲突），再重出这条对 —— ZIP `b7dfb42d…`（26.1.2，内含 JAR `4a18feb1…`）与 `e7ba1d32…`（26.2，内含 JAR `1b3e9325…`），回读线上附件的名称 / 大小 / SHA-256 与本地逐一相等；对应映射 `02-字符串与控制流加强版-26.1.2-4a18feb1` 与 `…-26.2-1b3e9325`。包内改动：模组侧接通后台链路（遥测、指令埋点、玩家侧 `.聊天`·`.回复`）、撤掉「更新提示」远程开关、摘除旧项目配置读取层；后台面板与 Worker 的重做在服务端，不随包发布。**本次是降级验收：两线都没起生产实例**（只做构建期检查与两线 `compileJava`），实机行为待玩家确认。覆盖前 26.2 那个附件有 **1 次下载**（26.1.2 为 0），装过旧包的按需重下；发布页正文与下载表未改（同标签换附件不改版本号，已装 beta1 的玩家收不到更新提示）。被覆盖那对（映射 `1b124a97` / `89dfcf52`）按保留规则留着；顺手删掉两处**从未对外挂过**的作废归档 `…-26.2-40edf1e0`、`…-26.2-d8c818c8`（第 234 条，此前一直挂在 26.2 工作树里没清）。明细见报告 181。

**2026-09-21 线上状态（第四次覆盖）**：同一对 ZIP 覆盖为「退出卡死修复」后的产物 —— ZIP `16c249ca…`（26.1.2，内含 JAR `1b124a97…`）与 `9867aa2e…`（26.2，内含 JAR `89dfcf52…`），对应映射 `02-字符串与控制流加强版-26.1.2-1b124a97` 与 `…-26.2-89dfcf52`。两线在清洁生产实例里跑满并走关停路径验收：池线程已是守护线程、1.1 秒退出、无崩溃报告。被覆盖那对（映射 `2f4c20d1` / `61a56a66`）按保留规则留着；**从未对外挂过**的 `…-26.1.2-2b7a04ec` 与 `…-26.2-40edf1e0` 按第 234 条删除。明细见报告 171、172。

**2026-09-21 线上状态（第三次覆盖）**：同一对 ZIP 覆盖为「自动登入两处口径修复」后的产物 —— ZIP `4b290469…`（26.1.2，内含 JAR `2f4c20d1…`）与 `6cf3632b…`（26.2，内含 JAR `61a56a66…`），对应映射 `02-字符串与控制流加强版-26.1.2-2f4c20d1` 与 `…-26.2-61a56a66`。两份都跑了独立生产实例（从发布 ZIP 解出的 JAR + 真 Loader / API，`--quickPlaySingleplayer`，180 秒）跑满全程：日志 0 异常行、无崩溃报告、正常存盘退出。附件下载次数为 0，发布页正文未改；被覆盖那对（映射 `90f84705` / `1f4ba6ab`）按保留规则留着。明细见报告 168（保命断线补抑制窗口）与 169（正版验证服允许开启）。

**2026-09-21 线上状态（第二次覆盖）**：同一对 ZIP 覆盖为「选择器实体名中文 + 26.2 模型图标」修复后的产物 —— ZIP `b1f5f5c7…`（26.1.2，内含 JAR `90f84705…`）与 `dd5dbde4…`（26.2，内含 JAR `1f4ba6ab…`），回读线上摘要与本地逐一相等；对应映射 `02-字符串与控制流加强版-26.1.2-90f84705` 与 `…-26.2-1f4ba6ab`。两份都跑了独立生产实例（从发布 ZIP 解出的 JAR，`--quickPlaySingleplayer`）跑满全程：日志 0 异常行、无崩溃报告、正常存盘退出。附件下载次数为 0，发布页正文未改；被覆盖那对（映射 `a9230fd8` / `7250385f`）按保留规则留着。明细见报告 164。

**2026-09-21 线上状态**：`v1.0-beta1` 仍是那对同名 ZIP，**版本号不变、同标签覆盖**为星露谷这批修复的产物 —— ZIP `354d2e22…`（26.1.2，内含 JAR `a9230fd8…`）与 `6209ba81…`（26.2，内含 JAR `7250385f…`），回读线上摘要与本地逐一相等；对应映射 `02-字符串与控制流加强版-26.1.2-a9230fd8` 与 `…-26.2-7250385f`。两份都跑了独立生产实例（Fabric Loader 0.19.5 + 对应 Fabric API + 从发布 ZIP 解出的 JAR，`--quickPlaySingleplayer`，180 秒），日志 0 异常行、无崩溃报告、正常存盘退出。附件下载次数为 0，发布页正文未改；被覆盖那对（映射 `027ab3cc` / `3b5d2435`）按保留规则留着。明细见报告 163。

**2026-09-20 线上状态**：`v1.0-beta1` 挂两个发布包（26.1.2 与 26.2），对应映射 `027ab3cc` 与 `3b5d2435`；两个 ZIP 的 SHA-256 记录在《06-发布页面/使用说明.md》。
这一版相对上一版只改了包内 `META-INF/反编译声明.txt`（改成六节完整版），**未重跑生产实例**；上一版（映射 `45e928e2` / `dd98d13f`）在发布前各起过一次独立生产实例（Fabric Loader 0.19.5 + 对应 Fabric API + 从发布 ZIP 解出的 JAR）进单人世界：日志 0 异常行、无崩溃报告，但两次都由用户手动退出、进世界只有 6~9 秒，**未跑满全程**。
被覆盖的各对（`45e928e2` / `dd98d13f`、`db9a5b72` / `43a3b738`）的映射按保留规则留着，供还原旧崩溃堆栈用；其中 `db9a5b72` / `43a3b738` 那一版是上一轮实测过的产物 —— 两条线各自用 Fabric Loader 0.19.5 + 对应 Fabric API 起真实客户端（非开发端），`--quickPlaySingleplayer` 进单人世界跑满全程，日志无异常行、正常存盘退出。
更早那两份（映射 `0ec23456` / `de83984a`）在同一实例里进世界约 45 秒必崩 —— 秒破 Mixin 把控制器缓存在「带初值的 `@Unique` 字段」里，混淆产物上初值没生效，而 `stopDestroyBlock` 原版每帧都会走到，直接 NPE 退出；那两份的映射按保留规则留着，供还原旧崩溃堆栈用。
详细覆盖数字、产物摘要与测试边界见开发报告 112；反编译部分的资料见已移出仓库的那份归档（报告 113）。
**今后每个新产物仍需重新验收，不能沿用上一轮的结论。**
