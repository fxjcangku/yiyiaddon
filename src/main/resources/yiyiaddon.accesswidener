accessWidener v2 official

# Skija 需要把 Minecraft 主 RenderTarget 的 GL Framebuffer 名字交给
# BackendRenderTarget.makeGL 使用；26.2 起该 FBO 由 FrameBufferCache 拼装，
# 而取 FBO 的入口仍在包私有的 GlDevice 上（GlDevice#frameBufferCache / #directStateAccess）。
accessible class com/mojang/blaze3d/opengl/GlDevice

# 「Missing sampler」崩溃兜底需要读 GlRenderPass 已绑定的采样器表，
# 并在绑定时校验；这两个类与 samplers 字段在 26.1.2 与 26.2 里都是包私有的。
accessible class com/mojang/blaze3d/opengl/GlCommandEncoder
accessible class com/mojang/blaze3d/opengl/GlRenderPass
accessible field com/mojang/blaze3d/opengl/GlRenderPass samplers Ljava/util/HashMap;

# 「是否正版验证服」判定：26.2 移除了 Connection#isEncrypted()，加密状态只剩管线里有没有
# CipherDecoder 这一种表现，而 channel 是包私有字段。
accessible field net/minecraft/network/Connection channel Lio/netty/channel/Channel;
