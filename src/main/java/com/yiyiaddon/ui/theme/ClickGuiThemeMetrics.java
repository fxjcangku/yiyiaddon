package com.yiyiaddon.ui.theme;

public record ClickGuiThemeMetrics(
        float windowRadius,
        float moduleRadius,
        float subModuleRadius,
        float borderWidth,
        float shadowBlur
) {
}
