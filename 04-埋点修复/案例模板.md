# 埋点修复 · 案例模板
#
# 用法：新开一案时把本文件复制成 `04-埋点修复/进行中/<YYYY-MM-DD-简述>/README.md` 再往下填。
# （用《工具/采集服务.js》创建案例目录时会自动复制本模板。）
# 填完的案例在结案时把整个文件夹移到 `04-埋点修复/已修复/`。
# 本文件本身是模板，不要直接在它上面改。

---

# <日期> <一句话现象>

| 项 | 内容 |
| --- | --- |
| 立案日期 | YYYY-MM-DD |
| 报障原话 | 「<用户原话，逐字>」 |
| 状态 | 进行中 / 已修复 |
| 影响面 | <哪个模块、哪个功能、什么条件下> |
| 相关报告 | `02-开发报告/项目开发报告/NN-复盘-….md` |

## 1. 现象与复现

- 现象：<看到什么、什么时候看到、持续多久>
- 复现路径（逐步）：
  1.
  2.
- 复现难度：必现 / 偶现（约 N 次中 M 次）
- 前提条件：<版本、服务器、维度、坐标、资源包、开关状态>

## 2. 先走常规流程（第二十八章），说明为什么不够用

- 已做的常规排查：<崩溃报告 / debug.log / 静态分析 / 单独复现的结果>
- 为什么定性不了：<例如「四入口裁决看不到，需要每个 tick 的状态快照」>
- 用户是否同意启用埋点修复方案：是 / 否

## 3. 假设清单（先写假设，再采集；禁止事后编故事）

| # | 假设 | 判据（用什么数据判定真假） | 结论 |
| --- | --- | --- | --- |
| H1 | | | 成立 / 不成立 |
| H2 | | | |
| H3 | | | |

## 4. 埋点清单

| 探针 | 位置（location） | 事件（msg） | 关键字段 | 采样策略 |
| --- | --- | --- | --- | --- |
| A | | | | 每刻 / 每 N 刻 / 计数制 |
| B | | | | |

- 落盘会话名：`修复前` / `修复后`（每次采集换名字，便于《工具/分析.js》做会话对比）
- 代码里全部用 `// #region debug-point <探针>:<用途>` 标记，便于一次 grep 清干净

## 5. 采集与证据

采集命令：

```powershell
node 04-埋点修复/工具/采集服务.js --案例 <本文件夹名>
# 另开一个窗口跑客户端复现，然后：
node 04-埋点修复/工具/分析.js --案例 <本文件夹名> --会话 修复前
```

关键证据（贴数据，不要只写「已验证」）：

| 观察项 | 数值 | 说明 |
| --- | --- | --- |
| | | |

## 6. 根因

<机制级说明：哪一行代码、什么条件下、为什么会导致这个现象。附源码 `文件:行号`。>

## 7. 修复

| 文件 | 改动 |
| --- | --- |
| | |

## 8. 验证（修复后同一路径再采一次）

| 指标 | 修复前 | 修复后 | 判定 |
| --- | --- | --- | --- |
| | | | |

- 用户实机确认：<原话>
- `.\gradlew build --console=plain -q` → EXIT=?

## 9. 收尾核对（三件，缺一不许归档）

- [ ] 全部 `#region debug-point` 已删除；`Grep "debug-point|debugProbe|埋点("` → 0 命中
- [ ] 采集服务已停止（端口不再监听）
- [ ] 本文件夹已从 `进行中/` 移到 `已修复/`，并在 `02-开发报告/项目开发报告/` 留一份复盘

## 10. 遗留

- <未验证的部分、同类风险点、需要用户拍板的事>
