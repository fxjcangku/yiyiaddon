# 进行中

正在采集 / 定位中的案例放这里，**一案一个文件夹**，命名 `YYYY-MM-DD-一句话现象`。

- 建案：`node ../工具/采集服务.js --案例 <YYYY-MM-DD-一句话现象>`（会自动建目录并复制 `../案例模板.md` 成 `README.md`）
- 结案：整个文件夹移到 `../已修复/`，并在 `02-开发报告/项目开发报告/` 留复盘。

本文件只用来占位说明，不要往里放案例内容。
