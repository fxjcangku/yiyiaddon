# 91 · 阶段15 · 批次4 · 收口报告

日期：2026-09-18
范围：自动图书管理员批次4 的**静态两件**（① 文案机对、② 自造件审计复核）+ 旧 52 → 新 53 全量映射 + 差异汇总 + **两条拍板项落地** + **实机 12 项清单（待用户执行）**。
代码改动：仅 `LibrarianLogPage.java` 3 行（D-15-15 措辞对齐，2026-09-18 用户拍板后落地，见 §6.2），其余为核对与登记。
前置：[87 号](87-阶段15-自动图书管理员移植侦察与决策.md)（D1~D9）、[88 号](88-阶段15-附录A-逻辑层全量侦察记录.md)（逐态判据正本）、[89 号](89-阶段15-批次2-逻辑层迁移记录.md)、[90 号](90-阶段15-批次3-外围移植记录.md)。
方法学照：[57 号](57-阶段13-批次2与3代码自查报告.md) 第五节（提取字面量 → 逐条新包检索 → 缺口逐条给结论）。

**验证状态：文案机对、自造件审计、静态审计 —— 已完成（静态核对）；D-15-15 改动后 `compileJava` 复验 EXIT=0；客户端启动验证与实际功能验证 —— 未做（见第七节清单，等用户实机）。**

---

## 1. 本次目标

把批次4 拆成「不依赖实机的两件」与「必须实机的一件」，先把前两件做完并给出结论，实机项以清单形式交用户执行：

1. **文案机对**：旧 `librarian` 52 文件的全部用户可见字面量（含 `§` 或含中文），逐条在新包定位；缺口逐条给「已搬 / 有意不搬 / 真缺口」结论；
2. **自造件审计复核**：对 90 号 §9.3 表逐条给「旧项目对应物 / 既有先例」，并补一次**反向机对**（新包 → 旧包 + 本项目其它模块），把「新包写了但两边都没有」的文案逐条裁掉；
3. **实机 12 项**：87 号第七节清单，交用户执行（第 19/46 条：静态分析不得代替实机结论）。

---

## 2. 文案机对

### 2.1 方法与口径（可复现）

| 步 | 做法 |
| --- | --- |
| 1 | 遍历旧 `com/example/addon/librarian/` **52 个 .java**，正则提取全部双引号字符串字面量（`"((?:[^"\\\n]|\\.)*)"`） |
| 2 | 过滤 = **含 `§` 的全部保留**；不含 `§` 的仅保留「含 2 个及以上中日韩字符」的（技术标识符、英文串天然排除） |
| 3 | 去重后得 **344 条**（同一文案在多个类各写一份时合并） |
| 4 | 每条按 `§` 色码切成片段（片段 = 去色码后含中文或长度 ≥ 4 的段落），逐个在新包全文（`feature/librarian/` 53 文件 + `module/AddonModules.java` + `platform/container/MerchantTradeAccess.java`）检索 |
| 5 | 任一片段检索不到即记为「缺失」，逐条人工裁定 |

> 说明：第 4 步切片是为了匹配「拼接构建」的文案——旧项目写成一条整体字面量、新项目可能拆成 `"§7搜索半径　§8▸ "` + 值，切片后仍能对上。

**旧项目字面量覆盖统计**

| 项 | 数 |
| --- | --- |
| 旧文件数 | 52 |
| 提取到的用户可见字面量（去重） | 344 |
| 直接命中 | 337 |
| 记为缺失 | **7** |

### 2.2 七条缺失逐条结论（缺口 = 0）

| # | 旧位置 | 旧原文 | 裁定 | 依据 |
| --- | --- | --- | --- | --- |
| 1 | `AutoLibrarianModule.java:267` | `已暂停` | **有意不搬**（死枝） | 旧 `paused` 字段无处置 true（仅 `:68/:259/:267/:283`），`getInfoString` 的「已暂停」分支不可达；已登记 **D-15-13** |
| 2 | `AutoLibrarianModule.java:325` | `击退` / `精准采集` | **非用户可见**（机对误报） | 出现在**注释**内引号：`// 获取本地化中文名，如"击退"、"精准采集"，回退到命名空间ID`；脚本按字面量提取时无法区分注释，人工判掉 |
| 3 | `orchestrator/AutoLibrarianOrchestrator.java:116` | `暂停重置`（`stateMachine.reset("暂停重置")`） | **有意不搬**（死件） | 承载它的 `softReset()` 在旧项目**全库零调用**（88 号 §7 已取证：`softReset` 仅声明处命中）；同为死件的 `restart()`（`:119`）亦未搬 |
| 4 | `service/EnchantmentService.java:16` | `当前附魔服务尚未提供最高交易等级解析` | **已拍板删除**（D1） | 该文案属 `getMaximumTradeLevel` 默认方法体，三个默认方法零调用且前者被调用即抛异常；D1 拍板删除，新契约只剩 `matches(...)` |
| 5 | `integration/FabricBaritoneMovementService.java:68` | `Baritone 移动启动失败（LinkageError，Baritone API 未正确加载): ` | **真差异**（登记 D-15-14） | 旧项目直连 Baritone，能区分 `LinkageError` / `RuntimeException` 两种成因分别播报；本项目统一走 `FarmNav` 隔离层（第 34 条），异常一律收成「下发失败」，两条成因**合并**为新文案（新 `BaritoneMoveService:53/58/74` 三条分支：「游戏世界尚未就绪」「当前没有可用的 Baritone 实例」「Baritone 移动启动失败。」） |
| 6 | `integration/FabricBaritoneMovementService.java:72` | `Baritone 移动启动失败（` | 同上 | 同上（两条旧文案合并为一条新文案） |
| 7 | —— | —— | —— | （#5/#6 为同一条差异的两条旧字面量，故 7 条 = 2 条误报 + 3 条死代码/已拍板删除 + 2 条同一条真差异） |

**结论**：344 条中，除上表 7 条外全部在新包逐条命中；7 条全部给出结论，**无未处置缺口**。

### 2.3 正向抽查样本（逐字一致的证据样点）

| 旧原文（逐字） | 新位置 |
| --- | --- |
| `§a§l✓ 自动图书管理员 · 启动报告` + 四行 `§7目标附魔　§8▸ …`（全角空格 + `§8▸`） | `AutoLibrarianModule:428-447`（与旧 `:227-249` 逐字，含 `highlightText §a§l` / `highlightNumber §e§l` / `highlightFunction §b§l` 三色） |
| `§d目标附魔§f·未勾选` / `§bBaritone§f·未安装或未启用` / `§a讲台§f·背包没有` / `§a书§f·背包没有` / `§a绿宝石§f·背包没有` | `AutoLibrarianModule#selfCheck`（5 类缺项顺序与旧 `:183-209` 一致） |
| `§6⚠ 未命中目标 §8▸ 准备刷新交易` 等 7 条进度播报 | `AutoLibrarianModule.Messenger`（旧 `broadcastProgress:353-369`） |
| `§c§l已强制结束` / `§c§l已关闭` | 暂停键分支 + 运行时统一关闭播报 |
| 使用说明 5 章正文 | `ui/AutoLibrarianHelpContent`（旧 `buildHelpContent:89-118`，仅去掉独立窗口外框三行，第 210 条） |
| 12 项设置名称 / 描述 / 分组名（`目标/行为/通知/调试/快捷键`） | `config/LibrarianSettings` + `ui/console/LibrarianSettingsPage`（旧 `AutoLibrarianSettings:19-143`） |

---

## 3. 自造件审计复核

### 3.1 双向机对（正向 + 反向）

| 方向 | 范围 | 结果 |
| --- | --- | --- |
| 正向（旧 → 新） | 旧 52 文件 344 条字面量 | 缺失 7 条，已全部结清（§2.2），**缺口 0** |
| 反向（新 → 旧 ∪ 本项目其它模块） | 新包去重后 **443 条**用户可见字面量 | 304 条仅旧包有先例（旧原文复用）+ 86 条两边都有 + 30 条仅本项目其它模块有先例（壳件同款）+ **19 条三方皆无**（逐条裁定见 §3.3） |

反向机对的参照池 = 旧 `librarian` 包 ∪ **本项目除本模块外的全部 java**。这样「本项目壳件文案」（控制台、页签、状态条、日志页、选择器、模块页入口）会被自动识别为「有先例」，只剩真正的新写文案进入人工裁定。

### 3.2 对 90 号 §9.3 表的逐条复核

| 新增件 | 旧项目对应物 / 既有先例 | 复核结论 |
| --- | --- | --- |
| `LibrarianConsoleScreen` + 三页签页 | 旧项目无控制台；铁律 181 要求移植模块带控制台，本项目已有 8 个控制台（星露谷 / 挖矿 / 农场 / 村民 / 附魔 / 自动箱子 / 杀光环 / 管理员检测）同款骨架 | 壳件，非新功能（第 181 条） |
| `AutoLibrarianPage` 状态行 + 控制台入口 | 其他 8 个模块页同款（`§b打开控制台` + 一行用途提示 + 说明内嵌，第 182/209/210 条） | 同上 |
| `AutoLibrarianHelpContent` | 旧 `buildHelpContent:89-118` 5 章逐字 | 逐字搬运 |
| `LibrarianSelectors` | 旧 `EnchantmentListSetting`（旧框架弹窗，不搬）；承载方式是本项目既有选择器窗（`SelectorScreen` 多选，村民 / 农场的选择器行同款） | 承载旧设置项 |
| `statusSummary()` / `countItem()` | 旧 `getInfoString` + `selfCheck` 内部统计 | 同一份数据的对外读口（第 169 条） |
| 其余 | —— | **未发现自造功能 / 自造设置项 / 自造指令** |

### 3.3 反向机对「三方皆无」19 条逐条裁定

| # | 位置 | 文案 | 裁定 |
| --- | --- | --- | --- |
| 1 | `integration/BaritoneMoveService.java:74` | `Baritone 移动启动失败。` | **真新增文案** → 登记 **D-15-14**（与 §2.2 #5/#6 同源，成因合并所致） |
| 2 | `platform/MerchantTradeOps.java:159` | `更完整` | 非用户可见（javadoc 注释内引号：`不做"更完整"的实现`） |
| 3 | `ui/AutoLibrarianPage.java:40` | `整屏承载全部设置项、目标附魔选择器与运行日志` | 模块页用途提示，**同形先例**：村民页 `整屏承载全部设置项、点位与运行日志`（各模块按自身内容改述，属既成做法） |
| 4-6 | `ui/console/LibrarianConsoleScreen.java:300/301/302` | `搜索半径：` / `最高价格：` / `命中策略：` | **旧原文词汇 + 壳件写法**：三个词逐字来自旧启动报告（`§7搜索半径　§8▸`）；状态条「`§7标签：值`」格式与村民 / 挖矿状态条逐格同形，仅分隔符由全角空格改为冒号 |
| 7 | `ui/console/LibrarianLogPage.java:38` | `只清这里的历史记录，不影响运行状态与聊天栏` | 壳件同款文案的**措辞微差**（其余 3 个日志页为「不影响状态与聊天栏」）→ 登记 **D-15-15** |
| 8 | `ui/console/LibrarianLogPage.java:42` | `§8还没有播报：启用模块后，状态与运行结果会出现在这里` | 壳件同款空态的**措辞微差**（村民页为「状态与任务结果会出现在这里」）→ 登记 **D-15-15** |
| 9-12 | `ui/console/LibrarianSettingsPage.java:50-53` | `group:目标` / `group:行为` / `group:通知` / `group:调试` | **非用户可见**：`FoldSection` 的 `stateKey` 只用于折叠状态记忆（`ConsoleWidgets.FoldSection:483-485`），界面上显示的是另传的标题（= 旧组名逐字） |
| 13-19 | `module/AddonModules.java:91/92/93/96/98/99/101` | 7 条分类说明（`管理通用工具功能` / `管理辅助工具功能` / `管理全自动挂机功能` / `Baritone 全部设置：…` / `管理农场模拟功能` / `管理战斗辅助功能` / `全部世界 ESP 的公共选项：…`） | **框架既有注册表**（第 148 条分类三处同步），非本批次改动；本批次只在该文件追加 2 行（import + `new AutoLibrarianModule(),`） |

**结论**：19 条中真新增仅 1 条（Baritone 失败文案，成因合并所致，已登记）；其余为壳件同款 / 旧原文词汇 / 非用户可见（注释、技术键）/ 框架既有。**未发现自造功能、自造设置项、自造指令**。

### 3.4 附魔家族名的来源取证（补强 D-15-11）

`LibrarianSelectors.ENCHANT_FAMILIES` 的 8 个家族名（`武器附魔 / 工具附魔 / 弓箭附魔 / 弩附魔 / 盔甲附魔 / 钓鱼附魔 / 通用附魔 / 三叉戟附魔`）**逐字来自旧项目** `villager/data/VillagerProfessionRegistry.java:163-208` 的注释分节名（旧项目该文件按这 8 节罗列村民可交易附魔池）。

- 旧项目是**注释分节**（非用户可见文本），本项目把它们用作选择器的**分组标题**——这是 D3 拍板「选择器必须分类（第 195 条）」后的承载方式选择，词汇本身不是新造；
- 该表与村民模块的表同源，两处并存已登记 **D-15-11**（待村民模块批次4 收口后收敛为一处，第 68 条包结构冻结期不跨模块抽取）。

---

## 4. 旧 52 → 新 53 全量映射

类型：`重写` = 保留职责、外壳按本项目架构重写；`直接迁移` = 业务逻辑逐行搬、仅换 import；`移动` = 同上但换了分类目录；`不搬` = 死代码（第 43 条或零调用取证）。

| 旧路径（相对 `librarian`） | 新路径（相对 `feature/librarian`） | 类型 |
| --- | --- | --- |
| `AutoLibrarianModule.java` | `AutoLibrarianModule.java` | 重写（外壳四件事 + 播报 + 自检 + 静默容器 + 音效） |
| `fsm/AutoLibrarianState.java` | `fsm/LibrarianState.java` | 直接迁移 |
| `fsm/AutoLibrarianStateMachine.java` | `fsm/LibrarianStateMachine.java` | 直接迁移 |
| `fsm/StateTransition.java` | `fsm/StateTransition.java` | 直接迁移 |
| `orchestrator/AutoLibrarianOrchestrator.java` | `service/LibrarianOrchestrator.java` | 移动（旧 `orchestrator/` 并入 `service/`，第 48 条） |
| `config/AutoLibrarianSettings.java` | `config/LibrarianSettings.java` | 重写（旧框架 Setting 对象 → 设置载体） |
| `config/AutoLibrarianConfig.java` | `config/LibrarianConfig.java` | 直接迁移 |
| `config/SuccessSound.java` | `config/SuccessSound.java` | 直接迁移（22 项） |
| `integration/FabricBaritoneMovementService.java` | `integration/BaritoneMoveService.java` | 重写（改走 `FarmNav` 隔离层，见 D-15-14） |
| `integration/FabricEnchantmentService.java` | `service/EnchantmentMatcher.java` | 移动（去 Minecraft 依赖后归 `service/`） |
| `integration/FabricInventoryService.java` | `platform/InventoryAccess.java` | 移动 |
| `integration/FabricLecternPlacementService.java` | `platform/LecternOps.java` | 移动 |
| `integration/FabricTradeService.java` | `platform/MerchantTradeOps.java` | 移动（三步交易协议收敛到 `platform/container/MerchantTradeAccess`，D6） |
| `integration/FabricVillagerSearchService.java` | `platform/VillagerSearchProbe.java` | 移动 |
| `integration/FabricVillagerStationService.java` | `platform/StationProbe.java` | 移动 |
| `model/` 14 个（`AutoLibrarianContext` / `BlockPosition` / `EnchantmentTarget` / `HorizontalDirection` / `InventorySnapshot` / `LecternStatus` / `StationValidationStatus` / `TargetProgress` / `TradeOfferSnapshot` / `TradeVerificationContext` / `VillagerStation` / `VillagerTarget` / `VillagerTargetStatus` / `VillagerTradeStatus`） | `model/` 14 个（`AutoLibrarianContext` → `LibrarianContext`，其余同名） | 直接迁移 |
| `service/` 17 个（`ActionStatus` / `ActionResult` / `DebugLoggerService` / `DebugSoundEvent` / `DebugSoundService` / `EnchantmentService` / `InventoryService` / `LecternPlacementService` / `MarkerBlockAccess` / `MarkerBlockValidation` / `MarkerBlockValidator` / `MovementService` / `MovementStartResult` / `MovementStatus` / `TradeService` / `VillagerSearchService` / `VillagerStationService`） | `service/` 同 17 个（`EnchantmentService` 删 3 个零调用默认方法，D1） | 直接迁移 |
| `service/LecternService.java` | —— | **不搬**（无实现、零调用，88 号 §7） |
| `service/NoOpMovementService.java` | —— | **不搬**（零使用） |
| `service/AutoLibrarianServices.java` | —— | **不搬**（零消费者死件，89/90 号已订正） |
| `recognition/LibrarianTradeRecognizer.java` | —— | **不搬**（第 43 条：recognition/adapter 死代码层） |
| `recognition/LibrarianTradeResult.java` | —— | 同上 |
| `adapter/LibrarianAdapter.java` | —— | 同上 |
| —— | `ui/AutoLibrarianPage.java` | 新建（本项目模块页壳件，第 209/210 条） |
| —— | `ui/AutoLibrarianHelpContent.java` | 新建（旧 `buildHelpContent` 逐字） |
| —— | `ui/LibrarianSelectors.java` | 新建（旧 `EnchantmentListSetting` 的承载界面） |
| —— | `ui/console/LibrarianConsoleScreen.java` | 新建（控制台骨架，第 181 条） |
| —— | `ui/console/LibrarianOverviewPage.java` / `LibrarianSettingsPage.java` / `LibrarianLogPage.java` | 新建（三页签内容） |
| —— | 顶层 `platform/container/MerchantTradeAccess.java` | 新建（跨模块共用交易原语，D6） |

**映射自洽核对**：旧 52 = 根 1 + fsm 3 + model 14 + config 3 + orchestrator 1 + integration 7 + service 20 + recognition 2 + adapter 1；
新 53 = 根 1 + fsm 3 + model 14 + config 3 + integration 1 + platform 5 + service 19 + ui 3 + ui/console 4。
差额 = 不搬 4 个死件（`LecternService` / `NoOpMovementService` / `AutoLibrarianServices` / `LibrarianAdapter`）+ 不搬 2 个 recognition + 新增 ui 7 个 + 新增 `EnchantmentMatcher` 1 个 + `MerchantTradeAccess` 记在顶层（不计入 53）。**无遗漏、无多余。**

---

## 5. 静态审计（本轮实测）

| # | 检查项 | 方法 | 结果 |
| --- | --- | --- | --- |
| 1 | 编译 + 全量构建 | `.\gradlew compileJava --console=plain -q` / `.\gradlew build --console=plain` | **均 EXIT=0**（`build` 6s，含 `jar` / `assemble`，`processResources` 无变化） |
| 2 | 零旧框架依赖 | 对 `feature/librarian/` + `MerchantTradeAccess` 全文检索 `meteordevelopment` / `com.example.addon` / `WWidget` | **0 命中**；`KeybindSetting` 仅 1 处命中，位于 `LibrarianSettings:100` 的 javadoc（第 161 条例外第三类：旧项目对应物的事实依据） |
| 3 | 类级中文 javadoc | 54 文件逐个检查是否存在 `/** … */` | **54/54 全覆盖**（`noJavadoc` 为空） |
| 4 | 中文注释覆盖 | 逐个检查注释行是否含中文 | **54/54 全覆盖**（`noChineseComment` 为空） |
| 5 | 死代码残留 | 检索 `TODO` / `FIXME` / `placeholder` | **0 命中** |
| 6 | 规模（含空行口径，2026-09-18 实测） | 逐文件计数 | `feature/librarian/` **53 文件 / 6,220 行**；`platform/container/MerchantTradeAccess.java` **77 行**；旧 `librarian` 52 文件（含空行 4,552 / 非空 4,083） |
| 7 | 旧交互资产未被改写 | 正向文案机对 | 344 条中 337 直接命中，7 条全部结清（§2.2） |
| 8 | 无自造功能 | 反向文案机对 + 90 号 §9.3 复核 | 真新增文案 1 条（D-15-14）；无自造功能 / 设置项 / 指令 |
| 9 | 实机（12 项） | —— | **未做**（第七节，等用户执行） |

---

## 6. 差异登记（接续 90 号 D-15-01 ~ 13）

| 编号 | 差异 | 旧项目 | 新项目 | 影响面 / 处置 |
| --- | --- | --- | --- | --- |
| **D-15-14** | Baritone 启动失败文案 | 两条分支分别播报：`Baritone 移动启动失败（LinkageError，Baritone API 未正确加载): <msg>` / `Baritone 移动启动失败（<异常类名>): <msg>` | 统一走 `FarmNav` 隔离层，异常收成「下发失败」；三条文案：`Baritone 移动启动失败：游戏世界尚未就绪。` / `Baritone 移动启动失败：当前没有可用的 Baritone 实例。` / `Baritone 移动启动失败。` | **用户可见**（走 `DebugLoggerService.error` → 错误播报）。成因合并是隔离层的必然结果（第 34 条禁止业务代码直连 Baritone）；新文案反而更具体（能区分「没有实例」与「世界未就绪」）。**用户 2026-09-18 拍板：保留新文案**（代码不动，登记为定稿差异） |
| **D-15-15** | 控制台日志页文案措辞微差 | 旧项目无控制台 | 本项目其余日志页（农场 / 村民）为「只清这里的历史记录，不影响**状态**与聊天栏」「状态与**任务**结果会出现在这里」；本模块原写为「不影响**运行状态**」与「状态与**运行**结果会出现在这里」 | **用户 2026-09-18 拍板改为其余日志页逐字一致 → 已落地**（见 §6.2） |
| **D-15-16** | 旧项目两个零调用入口未搬 | `AutoLibrarianOrchestrator#softReset`（`:112`，含 `暂停重置` 文案）与 `#restart`（`:119`） | 未搬 | 无用户可见差异（旧项目全库零调用，88 号 §7 已取证） |

### 6.1 两条拍板项落地（2026-09-18 用户同意）

| 编号 | 结论 | 落地动作 | 复验 |
| --- | --- | --- | --- |
| D-15-14 | **保留新文案**（不采用旧原文措辞） | 无代码改动；隔离层合并成因是第 34 条的必然结果，登记为定稿差异 | —— |
| D-15-15 | **改为与本项目其余日志页逐字一致** | `ui/console/LibrarianLogPage.java` 3 处：① javadoc `不影响运行状态与聊天栏` → `不影响状态与聊天栏`；② 清空按钮 tooltip `不影响运行状态与聊天栏` → `不影响状态与聊天栏`；③ 空态 `状态与运行结果会出现在这里` → `状态与任务结果会出现在这里` | `compileJava` **EXIT=0**；grep 四处日志页（农场 / 村民 / 星露谷 / 本模块）逐字比对一致 |

> 本次改动属第 164 条「修复缺陷（本项目壳件文案的同源不一致，第 169 条）」且已取得用户同意（第 165 条）；未新增任何文案、未触碰旧项目原文资产。

### 6.2 状态变更（第 76 条）

| 项 | 前记 | 实测 / 现记 | 说明 |
| --- | --- | --- | --- |
| `feature/librarian/` 规模 | 90 号：53 文件 / 6,167 行 | **53 文件 / 6,220 行**（含空行口径，2026-09-18 实测） | 文件数一致；行数差 53，属统计口径与时点差异，不回改 90 号 |
| 旧侧规模口径 | 87 号：52 文件 / 4,552 行 | 52 文件 / 4,552 行（含空行）/ 4,083 行（非空） | 两口径并存说明，避免后续报告混用 |
| 文案机对 | 90 号列为批次4 待办 | **已完成**：344 条 / 缺失 7 条 / 缺口 0 | 本报告 §2 |
| 自造件审计 | 90 号 §9.3 为初版 | **已复核 + 补反向机对**：19 条三方皆无逐条裁定，真新增 1 条 | 本报告 §3 |

---

## 7. 实机验收清单（12 项，待用户执行）

> 环境准备：单机世界 + 已搭「岩浆块 + 相邻空地」工位阵列 + 失业村民 + 背包备讲台 / 书 / 绿宝石 + Baritone 可用。
> 逐项填写结果（已验 / 未验 / 不适用 + 现象），本节由用户执行后补完。
> **三项旧项目原样行为，验收时不要当成 bug**：① 播报抹标点（`Lv.1` → `Lv1`）；② 「找到目标附魔」不发音（只有 `COMPLETE_TARGET` / `FINISH` 映射 `SUCCESS`）；③ 暂停键描述写「切换暂停/继续」实际按下即强制结束（照实现）；④ 静默容器放行背包与创造背包。

| # | 项 | 验收方式 | 结果 |
| --- | --- | --- | --- |
| 1 | 编译 | `.\gradlew build --console=plain -q` → EXIT=0 | **已过**（`build` EXIT=0，含 `jar` / `assemble`） |
| 2 | 文案机对 | 见本报告 §2 | **已完成（静态）** |
| 3 | 自造件审计 | 见本报告 §3 | **已完成（静态）** |
| 4 | 自检双通道 | 造缺项（清空目标附魔 / 无 Baritone / 无讲台 / 无书 / 无绿宝石）→ 聊天 + 屏幕中央弹窗 + 「打开设置」直达 | 待验 |
| 5 | 单村民单轮 | 失业村民 + 岩浆块工位 → 放讲台 → 转职 → 命中附魔 → 买到手（库存 +1 判成交） | 待验 |
| 6 | 未命中刷新循环 | 未命中 → 拆讲台 → 村民失业 → 重新检测工位 → 再放（≥3 轮不漏） | 待验 |
| 7 | 清障流程 | 讲台位预放障碍方块 → `已清除障碍方块，恢复运行` → 继续放置 | 待验 |
| 8 | 多目标 | 勾 2~3 个附魔 → 逐个完成；「找到后移除目标」开 / 关各验一次 | 待验 |
| 9 | 静默容器 | 运行中打开箱子 / 交易界面被取消；按 E 开背包与创造背包**放行** | 待验 |
| 10 | 设置持久化（第 172 条） | 改值 → 完全关客户端 → 重开仍在 | 待验 |
| 11 | 状态串 | 模块列表与控制台状态显示中文状态名，不出现英文枚举名 | 待验 |
| 12 | 使用说明时效（第 212 条） | 说明里指向的入口 / 页签名 / 设置名与实现一致 | 待验（静态核对：说明 5 章逐字旧原文，未指向本项目新增元素，无时效冲突） |

---

## 8. 遗留问题

1. **实机 12 项未做**（第七节）——第 19/46 条：本报告的「编译通过 + 静态核对」不等于功能验证通过。
2. **两条拍板项已落地**（§6.1）：D-15-14 保留新文案（无代码改动）、D-15-15 措辞对齐（3 处已改并复验）。无剩余待拍板项。
3. **跨模块收敛待办**：D-15-03（`MerchantTradeAccess` 村民模块未 adopt）、D-15-11（附魔家族表两份）→ 均在自动村民交易模块批次4 收口后处理。
4. 实机重点（90 号 §9.5）：暂停键携带物同步与强制结束后的状态落盘；静默交易在真实服下的屏取消与报价同步；工位两遍探测对「被埋岩浆块」场地的 fallback；目标附魔选择器在含模组附魔环境下的分组与写回。

---

## 9. 下一阶段开发规划

### 一、下一阶段目标

收尾阶段15：完成实机 12 项验收（用户执行 + 逐项记录），随后产出阶段15 最终结论；拍板项已落地（§6.1），实机若发现新现象则逐条取证二分。之后按路线图推进传送模块。

### 二、执行原因

静态核对已把「文案是否一字不漏、依赖是否干净、有无自造件」证到位；剩下唯一没有证据的部分是**运行期行为**——27 态推进、工位几何、静默交易协议、设置持久化，旧项目本模块历史失败点也集中在这两块，必须实机才能证实。

### 三、前置条件检查

- 已完成：批次1（侦察 87 / 88 / 87-附A）、批次2（逻辑层 44 文件）、批次3（外围 9 文件 + 注册）、批次4 静态两件（本报告），`compileJava` EXIT=0；
- 依赖的实机条件：单机世界 + 工位阵列 + 失业村民 + 背包补给 + Baritone；需要用户启动 `runClient` 并按第七节清单逐项走；
- 需先确认：`build` 全量构建（含打包）在本轮是否有资源侧变化——无资源新增，**已跑：EXIT=0**（6s，`jar` / `assemble` 通过）。

### 四、任务拆分

| # | 任务 | 目标 | 涉及目录 | 影响范围 | 验收方式 |
| --- | --- | --- | --- | --- | --- |
| 1 | 实机 12 项 | 第七节清单逐项过一遍并记录 | 无代码改动 | 无 | 逐项「已验 / 未验 / 不适用」+ 现象记录 |
| 2 | 拍板项落地 | D-15-14 保留新文案 / D-15-15 措辞对齐 | `ui/console` | 用户可见文案 3 处 | **已完成**（§6.1）：编译 EXIT=0 + 四处日志页 grep 逐字一致 |
| 3 | 实机新发现二分 | 每条新现象取证后二分「真移植偏差 / 旧项目本来就一样」 | 视结论 | 视结论 | 报告登记 + 旧项目行号依据 |
| 4 | 阶段15 收口结论 | 汇总全部差异（D-15-01 ~ 16）与实机结果 | `02-开发报告/项目开发报告/` | 报告 | 报告含状态变更与路线图 |
| 5 | `build` 全量构建 | 含资源处理与 remapJar | 无 | 无 | EXIT=0 + jar 内 librarian 类齐全 |

### 五、验收标准

- 编译与构建：`compileJava` / `build` 均 EXIT=0；
- 客户端：进世界无新崩溃，`yiyiaddon initialised` 正常，`run/crash-reports/` 无新增（第 180 条）；
- 功能：12 项全部「已验」，未验项写明原因与替代证据；
- 文案：旧项目全部用户可见文本逐条可定位（缺口 0，本轮已达成）；
- 报告：状态如实标注，禁止把静态核对写成功能验证。

### 六、禁止事项

- 禁止顺手改旧行为（抹标点 / 音效条件 / 快捷键描述不一致 / 静默容器放行背包，四项均为旧项目原样，要改必须用户点名）；
- 禁止新增指令 / 点位 / ESP / 设置项 / 文案；
- 禁止用静态分析代替实机结论；
- 禁止改动 `d:\mcaddon\26.1.2`；
- 禁止在未获同意前落地其它差异项（第 165 条；D-15-14 / D-15-15 已获同意，见 §6.1）。

---

## 项目迁移路线图

已完成：√ 阶段01~12 √ 阶段13 批次1~3（含自查修复）√ 阶段14 批次1~3（村民交易）√ 阶段15 批次1（侦察）√ 批次2（逻辑层）√ 批次3（外围 + 注册）√ **批次4 静态两件 + 拍板项落地（文案机对 + 自造件审计 + D-15-15 措辞对齐，本报告）**
当前：→ 阶段15 批次4 实机 12 项（等用户执行）；阶段14 批次4 收口（村民交易实机验收）并行待办
下一步：→ 传送 → 自动登入 → 战术 → 单文件模块（骨粉 / 用户统计 / 自动断线 / Baritone 引导）→ 全量实机验收与 Mixin 对账
