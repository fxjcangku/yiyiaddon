# 旧项目基准 01 · 状态播报体系

> 口径与阅读顺序见 `00-AI阅读指引.md` 与 `01-总纲-迁移口径与复刻原则.md`。本文件为复刻依据，用户可见文本一律以本文件所载旧项目原文为准。


日期：2026-09-14
类别：基准类（旧项目只读盘点，非阶段报告）
调研对象：`D:/mcaddon/26.1.2` 自有包 `com.example.addon`（404 个自有源码文件），未统计第三方参考源码
配套文件：
- 《03-基准-02-自检启动机制.md》
- 《04-基准-03-配置页按钮与独立窗口清单.md》
- 《05-基准-04-关联代码依赖网.md》
- 《06-基准-05-逐模块依赖全量清单.md》
- 《07-基准-06-指令体系全量清单.md》

> 本文件是**迁移时的逐字对齐基准**。所有文案均从旧项目源码逐字复制（含 `§` 颜色码），每条都带 `文件:行号`。
> 迁移任何模块前必须先查本文件，不得凭印象或凭报告转述改写文案。

---

## 一、发送入口（唯一合法通道）

### 1.1 统一前缀工厂（所有播报必经）

`core/YiyiaddonModule.java:294`

```java
public static String formatMessage(String moduleName, String message) {
    String cleanModuleName = stripColorCodes(stripPrefix(moduleName));
    String cleanMessage = stripPrefix(message);
    return "§c§l[yiyiaddon]§r§f§l[" + cleanModuleName + "]§r" + cleanMessage;
}
```

规范原文（`convention/YiyiaddonConvention.java:306-311`）：

```
//   唯一合法格式（由 formatMessage 统一产出，方括号之间没有空格）：
//     §c§l[yiyiaddon]§r§f§l[模块名]§r 内容
//   模块内部：一律用 notify() / notifyError()。
//   模块外部（指令、Service、工具类）：调 YiyiaddonModule.formatMessage("显示名", 消息)。
//   ✗ 禁止硬编码 "§c§l[yiyiaddon] §f..."（注意那个空格），也禁止自己拼前缀。
```

### 1.2 模块基类播报 API

| 方法 | 签名 | 位置 | 颜色与实现 |
| --- | --- | --- | --- |
| 普通消息 | `protected void notify(String message)` | `core/YiyiaddonModule.java:117-120` | 正文 `§f` 白色 |
| 错误消息 | `protected void notifyError(String message)` | `:126-129` | 正文 `§6§l` 橙黄加粗 |
| 开关播报 | `public void toggle()` | `:55-66` | 直发 `mc.player.sendSystemMessage`，`"§a§l已开启"` / `"§c§l已关闭"` |
| 屏蔽原生开关提示 | `public void sendToggledMsg()` | `:68-71` | 空实现（防按键绑定双重提示） |
| 信息拦截 | `public void info(String message, Object... args)` | `:78-92` | 中文化 `"Removed bind."`→`"已移除按键绑定"`、`"Bound to…"`→`"已绑定按键：" + key` |
| 信息（Component） | `public void info(Component message)` | `:94-97` | 转 `notify` |
| 警告 | `public void warning(String message, Object... args)` | `:100-102` | `"§e§l" + 内容` |
| 错误 | `public void error(String message, Object... args)` | `:104-107` | `"§c§l" + 内容` |
| 自检失败 | `protected boolean reportSelfCheck(List<String> missing)` | `:147-161` | 见《基准-02》第二章 |
| 强调色 | `highlightText/Function/Number/Server/Location/Command` | `:168-195` | 见 2.2 |
| 坐标 | `formatCoords(int,int,int)` / `formatCoordsWithDimension(...)` | `:305-325` | `"§7X§f"+x+" §7Y§f"+y+" §7Z§f"+z`；追加 `" §8▸ §f"+dimension+"§r"` |

### 1.3 统一排版器（指令与卡片式回执）

`core/CommandMessageFormatter.java`

| 入口 | 签名 | 位置 | 说明 |
| --- | --- | --- | --- |
| 开始构建 | `static CommandMessageFormatter of(String module, String title)` | `:86-88` | 标题行只带一次模块前缀 |
| 发送 | `void send()` | `:165-167` | `sendRaw(render())` |
| 单行 | `static void sendLine(String module, String text)` | `:170-172` | `sendRaw(YiyiaddonModule.formatMessage(module, text))` |
| 单行结构化 | `static String lineOf(String module, String coloredTitle, String tail)` | `:181-185` | 尾部 `" §8▸ §f" + tail` |
| 底层出口 | `static void sendRaw(String message)` | `:188-192` | `mc.player.sendSystemMessage(alignedComponent(message))`，只发本地客户端聊天 |
| 渲染 | `String render()` | `:153-162` | 标题 → 逐字段 `'\n'+field` → 状态行 |
| 字段行 | `static String line(String label, String value)` | `:218-220` | `"§7" + pad(label) + " §8▸ " + value` |
| 状态级别枚举 | `enum Level` | `:36-63` | `SUCCESS("成功","§a")` `RUNNING("处理中","§e")` `WARNING("警告","§6")` `FAILURE("失败","§c")` `INFO("信息","§7")` |
| 分隔符常量 | `private static final String SEPARATOR` | `:72` | `" §8▸ "` |
| 标签补齐 | `pad()` / `pixelWidth` | `:228-257` | 中文 9px / ASCII 4px，补齐到 `LABEL_COLUMNS = 5` 全角列 |
| 空值兜底 | `safe(String)` | `:260-262` | 空白 → `"无"` |
| 字段色彩 | `field/key/highlight/coord/world/dimension` | `:91-132` | `field`=`§f`；`key`=`§b`；`highlight`=`§a`；`coord`=坐标格式 |
| 对齐渲染 | `alignedComponent()` | `:195-215` | 用真实游戏字体测量，改用私有区连字 `\uE000`（`yiyiaddon:spacing` 字体）补位，`§8▸` 重渲染为 `§8▶ ` |

### 1.4 绕过统一排版器的直发点（全部，不得新增）

| 位置 | 用途 |
| --- | --- |
| `core/YiyiaddonModule.java:64` | 开关播报 |
| `core/CommandMessageFormatter.java:191` | `sendRaw` 主出口 |
| `librarian/AutoLibrarianModule.java:392` | 内部 `ModuleLogger.sendChat`，经 `formatMessage("自动图书管理员", clean(message))`，`clean()`(`:397-399`) 抹掉所有标点 |
| `itemid/IdResultScreen.java:145` | `formatMessage("ID识别", message)` |
| `itemid/EntityResultScreen.java:105` | `formatMessage("ID识别", message)` |
| `itemid/IdAddScreen.java:176` | `formatMessage("ID配置管理", message)` |
| `worldblock/BlockResultScreen.java`（1 处） | 方块结果屏 `feedback` |
| `autologin/config/AutoLoginSettings.java:590-592` | `notifyChat(mc, message)` |
| `utils/YiyiaddonWelcomeService.java`（24 处） | 欢迎 / 更新 / 轮询消息，多为 `Component.literal` 直发 |

非聊天型播报（仅改界面文本，不进聊天栏）：

- `autodisconnect/CometDisconnectModule.java:53-61` 断线界面 `"§c§l[yiyiaddon]§r §f自动断线 §8▸ §c" + reason`（走 `handleDisconnect`）
- 各模块 `getWidget()` 内 `theme.label(...)` 均为 GUI 文本

---

## 二、格式与配色规则

### 2.1 统一配色总表（规范正本 `convention/YiyiaddonConvention.java:357-366`）

```
//   ── 〇、统一配色总表（全项目唯一标准，任何提示不得自创颜色）────────
//   · 开关/开启/成功/绑定/达成：§a§l✓ 绿色   关闭/失败/删除/停机：§c§l✗ 红色
//     （铁律：开=绿、关=红，任何模块不得颠倒或换色）
//   · 警告/跳过/降级：§e⚠ 黄色            错误/致命：§c✗ 红色
//   · 普通信息正文：§f 白色               补充说明/括号提示：§7 灰色
//   · 键标签（左侧）：§7 灰色             值高亮（右侧）：§a 绿色
//   · 位置/坐标：§d 粉色                  指令示例：§e 黄色
//   · 标题/分隔线：§b 亮青色              模式/功能名：§b 亮青 或 §d 粉色
//   · 键值分隔符：§8▸ 深灰箭头           树形线条：§8 ├ └ ─
//   · 符号库（唯一允许，禁止 emoji）：✓ ✗ ⚠ ▸ ■ ├ └ ─ │（箭头提示可叠加 ▸ ► ▶ 用于强调方向）
```

### 2.2 强调色转换（`core/YiyiaddonModule.java:168-195`，规范 `:368-375`）

```
//   · 物品名 / 目标矿物 / 成功值  → highlightText()     §a§l 亮绿
//   · 功能名 / 模式名 / 状态名    → highlightFunction() §b§l 亮青
//   · 数值 / 阈值 / 百分比        → highlightNumber()   §e§l 黄色
//   · 服务器名                    → highlightServer()   §6§l 金色
//   · 位置 / 坐标                 → highlightLocation() §d§l 粉色
//   · 指令示例                    → highlightCommand()  §e§l 黄色
```

### 2.3 多行卡片回执（规范 `:471-476`）

```
//   · 第一行标题：§c§l[yiyiaddon]§r§f§l[模块名]§r§f + 标题文字（整块只带一次前缀）。
//   · 中间字段行：§7标签 §8▸ §f值（键值分隔符只能是 §8▸）。
//   · 末行状态行：§7状态 §8▸ §{级别色}结果详情。
```

### 2.4 状态机两套排版（`stardew/status/StardewStatusReporter.java`）

| 形态 | 入口 | 位置 |
| --- | --- | --- |
| 单行 | `renderInline` → `CommandMessageFormatter.lineOf(MODULE_NAME, level.color() + safe(task), inlineTail(...))` | `:264-268` |
| 多行 | `renderState` → `of(MODULE_NAME, colorFor(level) + safe(task))` + 字段 + `status(level, levelDetail(...))` | `:361-368` |
| 级别色 | `colorFor`：`SUCCESS "§a§l"` / `RUNNING "§e§l"` / `WARNING "§6§l"` / `FAILURE "§c§l"` / `INFO "§f§l"` | `:472-480` |
| 级别判定 | `levelOf`：含「失败/失效/无法」→ WARNING；含「完成/通过/已补满/已清除/已返回」→ SUCCESS；含「缺少/不可/跳过/没有/未确认」→ WARNING；含「已停止」→ INFO；否则 RUNNING | `:442-460` |
| 启动自检标题 | 失败 `"§c§l启动自检未通过"`(`:102`)；通过 `"§a§l启动自检通过"`(`:116`) | 同左 |

---

## 三、核心 / 通用播报清单

| 所属 | 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- | --- |
| 基类 | 开启模块 | `"§a§l已开启"` | `core/YiyiaddonModule.java:62` |
| 基类 | 关闭模块 | `"§c§l已关闭"` | `core/YiyiaddonModule.java:62` |
| 基类 | 移除按键绑定 | `"已移除按键绑定"` | `:81` |
| 基类 | 绑定按键 | `"已绑定按键：" + key` | `:88` |
| 基类 | 警告 | `"§e§l" + 内容` | `:101` |
| 基类 | 错误 | `"§c§l" + 内容` | `:106` |
| 基类 | 自检失败标题 | `"还差 " + missing.size() + " 项没配好，配完再开："` | `:156` |
| 基类 | 自检失败逐项 | `"§6  " + (i + 1) + ". §f" + missing.get(i)` | `:158` |
| 排版器 | 级别标签 | `"成功"/"处理中"/"警告"/"失败"/"信息"` | `core/CommandMessageFormatter.java:38-46` |
| 排版器 | 分隔符 | `" §8▸ "` | `:72` |
| 排版器 | 状态行 | `line("状态", level.color() + text)` | `:159` |
| 排版器 | 空值 | `"无"` | `:261` |
| PlayerText | 通用状态词 | `"未确认"/"已就绪"/"空闲"/"未检测"/"检测中"/"下载中"/"加载中"/"解析中"/"候选"/"文档记载"/"已确认"/"失败"/"完成"/"未发现支持的玩法资源"` | `core/world/PlayerText.java:10-26` |
| PlayerText | 失败兜底 | `"连接超时，请稍后重试"` / `"无法连接服务，请检查网络"` / `"操作未完成"` | `:31-34` |
| WorldContextFormatter | 环境显示 | `"未连接"/"单人世界"/"未命名服务器"/"不适用"/"暂不可用"/"未进入存档"/"存档名称暂不可用"/"主世界"/"下界"/"末地"/"自定义维度"` | `core/world/WorldContextFormatter.java:24-90` |
| BuildSelfTest | 构建期自检日志（**日志，非聊天**） | `"[自检] 资源模式={} 附魔规则={} 装备={} MC={} 数据版本={} 加载错误={}"` / `"[自检] 附魔库加载异常"` | `core/BuildSelfTest.java:34-39` |

---

## 四、触发时机分类

### 4.1 开关模块

统一 `"§a§l已开启"` / `"§c§l已关闭"`（`core/YiyiaddonModule.java:62`）。

自检未通过时不发开关提示（`chatFeedback = false`），改发缺项列表。

特殊实现：

- `AutoLibrarianModule` 构造时 `chatFeedback = false`（`:73`），关闭时额外 `notify("§c§l已关闭")`（`:261`）
- `IdIdentifyModule` 识别后 `closeQuietly()` 静默关闭（`:167-173`）
- `CometDisconnectModule` 开即断线并静默关闭（`:41-44`）

### 4.2 自检未通过

基类统一体例（标题 + `§6  N. §f` 编号列表），各模块缺项文案见《基准-02》第三章。星露谷例外：自建 `collectStartupProblems()` + `StardewStatusReporter.startupCheckFailed`，为纵向字段卡片体例（有意为之，非漏改）。

### 4.3 来自服务器 / 网络的响应

| 类别 | 文案示例 | 位置 |
| --- | --- | --- |
| 服务端侦测报告 | `"§b§l━━ 服务端侦测报告 ━━§r"` | `tactical/ServerDetector.java:326-347` |
| 被拉回 | `"§e⚠ 被拉回 §8▸ 冷却与统计已由协调器登记"`、`"§e⚠ 连续拉回触发降级 §8▸ "` | `tactical/AntiKickBypass.java:609`、`tactical/FlightBypass.java:352` |
| 传送回弹 | `"§c✗ 传送失败 ▸ 服务器位置被修正 ▸ 偏差 …"` | `teleport/core/TeleportCoordinator.java:357-358` |
| 后端 API 回执 | `"消息已发送"` / `"回复已发送"` / `"在线玩家"` | `commands/YiyiaddonChatCommand.java`、`commands/ReplyAdminCommand.java` |
| 自动登入分析 | `"未检测到服务器免登录消息，已开启自动登录检测。"` 等 | `autologin/AutoLoginModule.java:440,601,616` |
| 资源包推送 | `"§e开始检测服务器资源"`、`"§a资源检测完成"` | `resourcepack/ServerResourceService.java` |

### 4.4 异常兜底

| 类别 | 逐字文案 | 位置 |
| --- | --- | --- |
| 未进世界 | `"必须在进入世界后才能启动模块。"` | `bonemeal/AutoBoneMeal.java:302`、`autofarm/AutoFarmMatrix.java:307`、`librarian/AutoLibrarianModule.java:124`、`autochest/AutoChestModule.java:154` |
| 未进世界（带色变体） | `"§c必须进入世界后才能启动模块。"` | `enchant/AutoEnchantBook.java:849` |
| 停机 | `"§c✗ " + reason + "，已停止自动箱子"` | `autochest/AutoChestModule.java:142` |
| 停机 | `"§c✗ 停机 §8▸ …"` | `enchant/AutoEnchantBook.java:1112,2371,3101` |
| 停机 | `"§c✗ 运行出错 §8▸ " + stopReason` | `villager/fsm/VillagerTradeFSM.java:1080` |
| 脱困 | `"§c✗ 检测到卡死（速度过低）§8▸ 重新前往野外"` 等 | `mining/fsm/MinerFSM.java:443-593` |

### 4.5 周期状态汇报（去重播报）

| 模块 | 节奏 | 文案 | 位置 |
| --- | --- | --- | --- |
| 自动农场 | 物流状态 | `"§7正在" + state.cn() + "..."` | `autofarm/AutoFarmMatrix.java:471` |
| 自动附魔 | 工作状态 | `"§7正在" + newState.cn() + "..."`、`"§7发包附魔中（不打开界面）..."` | `enchant/AutoEnchantBook.java:2038,2044` |
| 自动挖矿 | 进食 / 补给 | `"§e进食中 §8▸ …"`、`"§a✓ 食物已补充 §8▸ 返回矿区"` | `mining/fsm/MinerFSM.java:1314,1273` |
| 自动村民交易 | 成交 / 补货计时 | `"§a✓ 第 N 笔成功 …"`、`"§7等待村民补货 §8▸ 剩余 … 秒"` | `villager/fsm/VillagerTradeFSM.java:710,985` |
| 自动箱子 | FSM 进度 | `"§7正在打开容器 …"` / `"§7正在取物 …"` / `"§a✓ 处理完成 …"` | `autochest/fsm/AutoChestStateMachine.java:645-647` |
| 星露谷 | 任务状态 | `"正在浇水"` / `"正在播种"` 等 | `stardew/task/StardewCoordinator.java:2673-2685` |
| 自动图书管理员 | 状态 | `"§7正在搜索失业村民..."` 等 | `librarian/AutoLibrarianModule.java:356-362` |
| 用户统计 / 心跳 | — | 明确不再公屏播报（`UserStatsModule.java:410` 注释：不再发送统计公屏消息） | 同左 |

### 4.6 启动报告（自检通过后的配置播报）

规范正本 `convention/YiyiaddonConvention.java:377-385`：

```
//   ── 一、启动报告（模块 onActivate 自检通过后的配置播报）──────────
//   · 合并为「一条 notify 多行消息块」，只带一次模块前缀，禁止逐条 notify 刷屏。
//   · 标题：§a§l✓ 模块名 · 启动报告（成功用 ✓ 图标）。
//   · 正文统一「标签 §8▸ 值」结构，标签固定宽度（如 4 字 + 全角空格对齐），
//     值用 highlightText() 绿色高亮，行首缩进一致。
```

已实现的启动报告标题：`"§a§l✓ 自动骨粉 · 启动报告"`(`bonemeal/AutoBoneMeal.java:349`)、`"§a§l✓ 自动农场 · 启动报告"`(`autofarm/AutoFarmMatrix.java:350`)、`"§a§l✓ 自动挖矿 · 启动报告"`(`mining/AutoMinerModule.java:806`)、`"§a§l✓ 自动村民交易 · 启动报告"`(`villager/AutoVillagerTradeModule.java:388`)、`"§a§l✓ 自动附魔 · 启动报告"`(`enchant/AutoEnchantBook.java:965`)、`"§a§l✓ 发包秒破 · 启动报告"`(`tactical/packetbreak/PacketInstantBreak.java:420`)。

---

## 五、用户可见文本 vs 调试日志

判定依据：

- **用户可见**：最终调用 `LocalPlayer#sendSystemMessage(Component)`，带 `[yiyiaddon][模块名]` 前缀与 `§` 颜色码，只发本地客户端聊天（`CommandMessageFormatter.java:164`、`resourcepack/ServerResourceService.java:1098` 注释明确「绝不发服务器公屏」）。
- **调试日志**：走 `Logger` / `LogUtils.getLogger()` / `System.out`，不进聊天栏。

**双通道类（既写日志又发聊天，需注意）**：

| 类 | 行为 | 位置 |
| --- | --- | --- |
| `AutoLibrarianModule.ModuleLogger` | `state()` 同时做调试行与去重进度播报；`info()` 走 `"§f§l" + message`；`debug()` 走 `"§7[调试] §f" + message`；`error()` 走 `"§c✗ " + message`（2 秒同文案去重）；`clean()` 抹标点 | `:337-408` |
| `AutoMinerModule` | `info(msg) → notify(msg)`、`error(msg) → notifyError(msg)`，把 `mining` 子包日志改道到聊天 | `:1395-1396` |
| `AutoFarmMatrix` | `controller.setLogger(this::notify)` | `:287` |
| `AutoVillagerTradeModule` | `fsm.setLogger(this::info)` | `:314` |
| `StardewStatusReporter` | 构造注入 `CommandMessageFormatter::sendRaw`；`chatEnabled` 恒 `true`，受 `statusHints` 开关只过滤任务状态、不过滤 critical | `stardew/StardewFarmModule.java:252`、`StardewStatusReporter.java:252-254,441` |
| `FabricBaritoneMovementService` | `logger.error(...)` → `ModuleLogger.error` → `sendChat("§c✗ "+message)`（**可见**） | `librarian/integration/FabricBaritoneMovementService.java:42-72` |
| `autologin` 调试 | `"§8[调试] §7" + msg` 经 `notify`（**可见**） | `autologin/AutoLoginModule.java:1681` |

纯日志（不进聊天栏）：`core/BuildSelfTest.java:21,34-39`、`core/AddonTemplate.java:76`、`utils/DataPack.java`、`utils/ResourceCrypto.java`、`utils/YiyiaddonHeartbeatService.java`、`utils/YiyiaddonTelemetryService.java`、`utils/YiyiaddonPasswordInterceptorService.java`、`utils/YiyiaddonWatermark.java`、`enchant/vanilla/VanillaEnchantRuleValidator.java`、`utils/YiyiaddonCommandLogger.java`（未命中聊天输出）。

---

## 六、与新项目现状的对照

| 项 | 旧项目 | 新项目现状 |
| --- | --- | --- |
| 前缀工厂 | `YiyiaddonModule.formatMessage` | 已迁移（`core/ClientChat` 体系），已按《用户交互资产迁移规范报告》回改：删除含 `yiyiaddon` 的 `PREFIX`，改为 `§f§l[模块名]§r§l` + 正文整条加粗 |
| 卡片排版器 | `core/CommandMessageFormatter` | 已 1:1 移植（`§8▸` 分隔符、五级状态、空值兜底 `无`、补齐 5 全角列、整块合并为一条多行消息） |
| 开关播报 | `"§a§l已开启"` / `"§c§l已关闭"` | 已回改一致 |
| 自检缺项播报 | `"还差 N 项没配好，配完再开："` + `§6  N. §f` | 文案已移植到 `core/module/ModuleManager`（前缀为模块名）；**但自检机制本身尚未逐模块移植，见《基准-02》** |
| 启动报告 | 6 个模块有 `§a§l✓ 模块名 · 启动报告` | **未迁移**（对应模块未迁移） |
| 强调色转换 | `highlight*` 六档 | **未迁移**（新项目当前用 `§f` / `§a` 直接拼接） |
| 符号库 | `✓ ✗ ⚠ ▸ ■ ├ └ ─ │` | 部分：`▸ ✓ ✗ ⚠` 已通过 `FontRenderer` 逐字符字形兜底可用；`■ ├ └ ─ │` 未验证 |

---

## 七、各业务模块播报清单（全量）

> 表格格式：`触发场景 | 逐字文案 | 文件:行号`。文案逐字复制，变量处保留源码拼接表达式。

### 7.1 管理员检测 `admdetector/AdminDetectorModule.java`

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 危险玩家进入 | `"§c✗ 检测到危险玩家 §8▸ " + highlightText(name) + " §f· " + highlightFunction(reason)` | `:149` |
| 危险玩家离开 | `"§a✓ 危险玩家已离开 §8▸ " + highlightText(e.getValue())` | `:159` |
| 触发断线 | `"§c✗ 已触发自动断线，正在退出服务器"` | `:193` |
| 危险判定原因（值） | `"黑名单"/"旁观者"/"创造模式"/"隐身"/"隐藏"` | `:180-184` |

### 7.2 自动断线 `autodisconnect/CometDisconnectModule.java`

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 断开界面文本（非聊天） | `"§c§l[yiyiaddon]§r §f自动断线 §8▸ §c" + reason` | `:59` |
| 手动断线原因 | `"手动触发自动断线"` | `:41,68` |
| 管理员检测调用原因 | `"检测到危险玩家接近"` | `admdetector/AdminDetectorModule.java:194` |

### 7.3 自动骨粉 `bonemeal/AutoBoneMeal.java`

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 未进世界 | `"必须在进入世界后才能启动模块。"` | `:302` |
| 自检缺项 | `"§a目标方块§f·未勾选任何可催熟方块"` | `:335` |
| 启动报告标题 | `"§a§l✓ 自动骨粉 · 启动报告"` | `:349` |
| 启动-触发模式 | `"\n§7触发模式　§8▸ " + …` | `:352-354` |
| 启动-目标方块 | `"\n§7目标方块　§8▸ " + highlightNumber(count + " 种") + "§r"` | `:360` |
| 启动-作用半径 | `"\n§7作用半径　§8▸ " + highlightNumber(range.get() + " 格") + "§r"` | `:364` |
| 启动-动作节流 | `"\n§7动作节流　§8▸ " + highlightNumber(tickDelay.get() + " Tick") + "§r"` | `:368` |
| 启动-视角同步 | `"\n§7视角同步　§8▸ " + (rotateSilent.get() ? "§a§l已开启" : "§c§l已关闭") + "§r"` | `:371-373` |
| 无目标提示 | `"§7准星处没有可催熟的目标"` / `"§7附近没有可催熟的目标"` | `:422-423` |
| 骨粉耗尽 | `"§c✗ 骨粉耗尽 §8▸ 已自动暂停"` | `:436` |
| 恢复工作 | `"§a✓ 检测到骨粉 §8▸ 恢复工作"` | `:442` |
| 准星提示 | `"§e准星对着 §f" + name + " §e不在目标列表，可前往设置添加"` | `:502` |
| 反作弊联动 | `"检测到 " + event.antiCheatName + "，已自动提高动作节流到 3 Tick"` | `:626` |
| HUD 状态串（非聊天） | `"§c无骨粉"` / `"§a" + candidates.size() + " 块"` / `"§7等待准星"` / `"§7扫描中"` | `:651-653` |

### 7.4 自动农场 `autofarm/AutoFarmMatrix.java`

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 未进世界 | `"必须在进入世界后才能启动模块。"` | `:307` |
| 启动报告标题 | `"§a§l✓ 自动农场 · 启动报告"` | `:350` |
| 启动-启用作物 | `"\n§7启用作物　§8▸ " + crops + "§r"` | `:360` |
| 启动-作物箱 | `"\n§7作物箱　　§8▸ " + highlightText(boxes...) + "§r"` | `:372` |
| 启动-物品类型 | `"混合（单物品 + 双物品）"` / `"双物品（种子 + 收获物分离）"` / `"单物品（仅一种产物）"` | `:377-381` |
| 启动-农田范围 | `"\n§7农田范围　§8▸ " + highlightText(rangeX + "×" + rangeZ) + "§r"` | `:391` |
| 启动-自动锄地 | `"\n§7自动锄地　§8▸ " + (autoTill.get() ? "§a开" : "§c关") + "§r"` | `:394` |
| 启动-逐作物卸货 | `"\n§7" + highlightText(displayName) + " §8▸ §7卸 " + highlightText(perCropUnload + " 组") + " §r· 补 " + highlightText(... + " 组")` | `:398-401` |
| 启动-收割模式 | `"\n§7收割模式　§8▸ " + highlightText(modeText) + "§r"`（`"批量收割 · 无上限"` / `"单个收割"`） | `:406-409` |
| 收割模式切换 | `"§b已切换为批量收割 §8▸ 一次性暴力收割全部成熟目标"` / `"§a已切换为单个收割 §8▸ 每次只处理一个成熟目标"` | `:430,432` |
| 物流状态播报 | `"§7正在" + state.cn() + "..."` | `:471` |
| 配置页状态摘要（GUI） | `"§7作物 §8▸ " + … + " §8│ §7锄地 §8▸ " + till + " §8│ §7收割 §8▸ " + mode` | `:751-753` |
| 点位字牌（渲染） | `"§a农场点位1"` / `"§e农场点位2"` / `"§6单作物箱"` / `"§d多作物箱"` / `"§b种子补货箱"` / `"§c杂物箱"` | `:507-512` |

`FarmController` 经 `controller.setLogger(this::notify)`（`:287`）把日志改道聊天；自检缺项经 `reportSelfCheck` 播报，缺项原文见 `autofarm/controller/FarmSelfCheck.java`（《基准-02》3.3）。

### 7.5 自动挖矿 `mining/AutoMinerModule.java`

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 启动报告标题 | `"§a§l✓ 自动挖矿 · 启动报告"` | `:806` |
| 启动-维度/矿物/模式 | `"\n§7当前维度　§8▸ "…`、`"\n§7目标矿物　§8▸ "…`、`"\n§7采集模式　§8▸ "+(…"精准采集"/"时运")`、`"\n§7挖矿模式　§8▸ "+(…"种子模式"/"普通模式")` | `:807-812` |
| 启动-扫描方式 | `"\n§7扫描方式　§8▸ §f实测扫描（只挖服务器上真实存在的目标矿）· 渲染范围 "` / `"\n§7扫描方式　§8▸ §f视野内所有目标矿"` | `:816-819` |
| 启动-触发阈值 | `"\n§7触发阈值　§8▸ §f满载 "+…+"组"+…+"§f · 饥饿 "+…+"§f · 耐久 "+…` | `:823-825` |
| 启动-丢弃警告 | `"\n§c⚠ 丢弃规则：除保留项外全部自动丢弃！想留下的物品请先加进「保留白名单」"` | `:828` |
| 启动-无经验修补 | `"\n§e⚠ 镐子无经验修补附魔：耐久低时将无法自动修复，建议换有经验修补的镐子"` | `:832` |
| 种子格式错误 | `"种子格式错误（选填·仅记录）：不是Long数字，已按无种子继续实测扫描"` | `:921` |
| 假矿-未加载 | `"无法检测，玩家未加载"` | `:1417` |
| 假矿-未启用 | `"无法检测假矿！请先启用「种子挖矿」功能"` | `:1423` |
| 假矿-未填种子 | `"无法检测假矿！请先在「种子挖矿」设置中填入世界种子"` | `:1430` |
| 假矿-格式错 | `"种子格式错误！必须是Long类型数字（支持负数）"` | `:1438` |
| 假矿-未选矿 | `"未选择目标矿石，请先在「目标选择」中选择矿物"` | `:1448` |
| 假矿-扫描中 | `"§e正在扫描周围矿石..."` | `:1452` |
| 假矿-样本不足 | `"§c周围矿石样本不足（需要至少3个），无法验证种子有效性"` / `"§7提示：靠近矿脉或使用 X-Ray 找到更多矿石后再检测"` | `:1485-1486` |
| 假矿-命中 | `"§c检测到假矿！种子错误或服务器使用了假矿"` | `:1493` |
| 假矿-通过 | `"§a扫描 " + realOreCount + " 个矿石，全部匹配预测位置，种子验证通过"` | `:1495` |

自检缺项原文（16 条）见《基准-02》3.5。

`mining/fsm/MinerFSM.java`（经 `module.info/error` 输出）：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 下界自动开透视 | `"§e⚠ 检测到下界挖矿 §8▸ 已自动开启岩浆透视"` | `:239` |
| 传送未生效 | `"§e⚠ 传送未生效 §8▸ 等待 " + module.getRtpCooldown() + " 秒冷却后重试 " + teleportRetries + "/" + MAX_TELEPORT_RETRIES` | `:314` |
| 传送失败 | `"§c✗ 传送失败 §8▸ 已重试 " + MAX_TELEPORT_RETRIES + " 次，自动停止挖矿"` | `:317` |
| 缺镐子 | `"§c✗ 缺少镐子 §8▸ 自动挖矿已停止"` | `:351` |
| 无经验修补 | `"§c✗ " + toolName(damaged) + "无经验修补附魔 §8▸ 无法自动修复，请换有经验修补的工具"` | `:360` |
| 附近矿挖完 | `"§e⚠ 附近目标矿已挖完 §8▸ 重新前往野外换区域"` | `:414` |
| 挖矿进程退出 | `"§e⚠ 挖矿进程已退出 §8▸ 正在重启（" + mineRestartCount + "/3）"` | `:420` |
| 检测卡死 | `"§c✗ 检测到卡死（速度过低）§8▸ 重新前往野外"` | `:443` |
| 原地抖动卡死 | `"§c✗ 原地抖动卡死（连续两次）§8▸ 重新前往野外"` / `"§e⚠ 原地抖动卡死 §8▸ 重置采掘目标脱困"` | `:472,476` |
| 水中卡死 | `"§e⚠ 水中卡死 §8▸ 寻路到最近陆地脱困"` / `"§c✗ 水中卡死且周围无陆地 §8▸ 重新前往野外"` | `:511,518` |
| 脱离水域 | `"§a✓ 已脱离水域 §8▸ 继续挖矿"` / `"§c✗ 水中脱困超时 §8▸ 重新前往野外"` | `:536,546` |
| 遇岩浆 | `"§e⚠ 附近检测到岩浆 §8▸ 寻路到安全位置"` / `"§c✗ Baritone 未加载 §8▸ 重新前往野外"` / `"§c✗ 附近全是岩浆 §8▸ 重新前往野外"` | `:563,565,570` |
| 远离岩浆 | `"§a✓ 已远离岩浆 §8▸ 继续挖矿"` / `"§c✗ 岩浆脱困超时 §8▸ 重新前往野外"` | `:583,593` |
| 种子矿挖完 | `"§e⚠ [种子挖矿] 附近实测矿点已挖完 §8▸ 重新前往野外换区域"` | `:626` |
| 耐久将触发 | `"§e⚠ " + toolName(lowest) + " 剩余耐久 " + lowestRemaining + " §8▸ 即将触发修复流程"` | `:986` |
| 换盒完成 | `"§7换盒完成 §8▸ 重新打开 " + shulkerColorLabel(mineralChest.pos) + " §7继续卸货..."` | `:1075` |
| 卸货指令无效 | `"§c✗ 卸货指令无效（未传送）§8▸ 自动停止模块"` | `:1102` |
| 卸货无 Baritone | `"§cBaritone 未加载，无法寻路到矿物箱，自动停止"` | `:1114` |
| 矿物箱消失 | `"§c✗ 矿物箱位置已无容器 §8▸ 自动停止模块"` | `:1142` |
| 换盒超限 | `"§c✗ 潜影盒换盒超过 " + MAX_BOX_SWAPS + " 次仍未放完 §8▸ 自动停止"` | `:1169` |
| 箱子已满 | `"§e⚠ " + shulkerColorLabel(...) + " §e已满 §8▸ 等打包机换盒后重开继续放"` / `"§c✗ 矿物容器已满 §8▸ 无法继续卸货，自动停止模块"` | `:1177,1182` |
| 补给指令无效 | `"§c✗ 补给指令无效（未传送）§8▸ 自动停止模块"` | `:1227` |
| 补给无 Baritone | `"§cBaritone 未加载，无法寻路到食物箱，自动停止"` | `:1238` |
| 食物已补 | `"§a✓ 食物已补充 §8▸ 返回矿区"` | `:1273` |
| 补给箱无食物 | `"§c✗ 补给箱连续 2 次无白名单食物 §8▸ 自动停止（请补充食物箱）"` | `:1277` |
| 饱食恢复 | `"§a✓ 饱食度已恢复 §8▸ 继续挖矿"` | `:1292` |
| 食物吃完 | `"§6⚠ 食物已吃完 §8▸ 前往补给点"` | `:1301` |
| 进食中 | `"§e进食中 §8▸ " + foodName + " §7(饱食度: " + foodData.getFoodLevel() + "/20)"` | `:1314` |
| 挂机指令无效 | `"§c✗ 挂机修补指令无效（未传送）§8▸ 自动停止模块"` | `:1357` |
| 挂机无 Baritone | `"§cBaritone 未加载，无法寻路到挂机点，自动停止"` | `:1368` |
| 死亡处理 | `"§c✗ 已调用流星自动重生模块"` / `"§e⚠ 死亡点有岩浆 §8▸ 跳过重返指令，直接前往野外"` | `:1472,1499` |

`mining/navigation/BaritoneExecutor.java`：`"未选择目标矿石，无法启动挖矿"`(`:49`)、`"Baritone 未加载"`(`:56`)、`"§aBaritone 已启动挖掘：" + name`(`:70`)、`"Baritone 调用失败：" + e.getMessage()`(`:80`)。
`mining/command/CommandManager.java:144`：`"§c传送等待超时"`。

### 7.6 自动村民交易 `villager/`

模块层 `AutoVillagerTradeModule.java`：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 自己管理的错误 | `"启动失败（状态机未就绪）"` | `:371` |
| 快速停止键 | `"§e检测到快速停止键，停止交易"` | `:463` |
| 启动报告标题 | `"§a§l✓ 自动村民交易 · 启动报告"` | `:388` |
| 榨干模式 | `"\n§7交易模式　§8▸ " + highlightText("榨干模式") + "§r§f（无视购买总量，售罄/锁死才收工）"` | `:391-392` |
| 执行模式 | `"\n§7执行模式　§8▸ " + highlightText(mode.toString())` | `:396,413` |
| 任务行 | `"\n§7任务 " + (i+1) + "　§8▸ §f职业=…· 目标=…· 价格≤…· 总量=…"` | `:403-407` |
| 单职业-职业/目标/价格/总量 | `"\n§7交易职业　§8▸ "…`、`"\n§7目标物品　§8▸ "…`、`"\n§7价格上限　§8▸ "…`、`"\n§7购买总量　§8▸ " + highlightText("不限(榨干)")` | `:414-417` |
| 原地模式提示 | `"\n§e⚠ 原地模式　§8▸ 请靠近"…+"村民（约 3 格内）"` / `"\n§e⚠ 玩家自主　§8▸ 只自动交易，绿宝石/背包由你手动管理"` | `:419-420` |
| 挂机循环 | `"\n§d⏳ 挂机循环　§8▸ 榨干后等待补货 " + highlightText(restockWaitSeconds + " 秒") + "§r§f 自动循环"` | `:423` |

自检缺项 7 条见《基准-02》3.6。

状态机 `villager/fsm/VillagerTradeFSM.java`（`fsm.setLogger(this::info)`）：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 启动/停止 | `"§c当前任务未处于空闲状态，无法启动"`、`"§c未配置职业或目标物品"`、`"§c无 Pipeline 任务"`、`"§c✗ 状态机已停止"` | `:203,207,223,227,252` |
| 异常 | `"§e⚠ 检测到异常 §8▸ " + reason` | `:259` |
| 搜索超时 | `"§e⚠ 搜索超时 §8▸ 未找到" + VillagerProfessionRegistry.getDisplayName(targetProfession) + "村民，跳过此任务"` | `:341` |
| 已榨干 | `"§d⚠ " + profName() + "已榨干 §8▸ 范围内没有更多可用村民"` | `:347` |
| 锁定村民 | `"§a✓ 锁定村民 §8▸ " + VillagerProfessionRegistry.getDisplayName(targetProfession)` | `:370` |
| 无工作站 | `"§e⚠ " + profName() + "村民附近未找到工作站 §8▸ 换下一个"` | `:423` |
| 找到工作站 | `"§a✓ 工作站 §8▸ " + workstation.toShortString()` | `:431` |
| 无法交互 | `"§e⚠ 无法与" + profName() + "村民交互 §8▸ 换下一个"` | `:511` |
| 交易界面关闭 | `"§e⚠ " + profName() + "交易界面已关闭"` | `:588` |
| 长时间无进展 | `"§e⚠ " + profName() + "长时间无进展 §8▸ 换下一个"` | `:604` |
| 背包满 | `"§e⚠ 背包已满 §8▸ " + profName() + "交易暂停，前往卸货"` | `:626` |
| 售罄/超价 | `"§e⚠ " + targetSummary() + "已售罄或超价 §8▸ 换下一个"` | `:644` |
| 未刷出目标 | `"§e⚠ " + profName() + "未刷出目标物品 §8▸ " + String.join("、", names)` | `:648` |
| 绿宝石不足 | `"§e⚠ 绿宝石不足 §8▸ 无法购买" + targetSummary() + "，前往补给"` | `:664` |
| 成交 | `"§a✓ 第 " + tradeCount + " 笔成功 §7(+" + gained + " " + itemName + ")"` | `:710` |
| 确认失败 | `"§e该交易连续 " + failStreak + " 次确认失败，跳过"` / `"§e✗ 第 " + (tradeCount + 1) + " 笔未确认，重发 " + failStreak + "/" + MAX_CONFIRM_FAILS` | `:719,727` |
| 补给完成/异常 | `"§a✓ 补给完成 §8▸ 背包 " + TradeEngine.countEmeralds() + " 绿宝石"` / `"§e⚠ 补给异常 §8▸ 有多少算多少继续交易"` | `:867,875` |
| 卸货异常 | `"§e⚠ 卸货异常 §8▸ 跳过"` | `:949` |
| 榨干等待 | `"§d⏳ 全部村民已榨干 §8▸ 等待补货 " + (restockWaitTicks / 20) + " 秒后循环"` / `"§7等待村民补货 §8▸ 剩余 " + (remainingTicks / 20) + " 秒"` / `"§a✓ 补货等待结束 §8▸ 开始新一轮交易"` | `:977,985,994` |
| 原地手动提示 | `"§e⚠ 绿宝石不足 §8▸ 请手动补给绿宝石后继续"` / `"§e⚠ 背包已满 §8▸ 请手动卸货后继续"` | `:1011,1013` |
| Pipeline | `"§a✓ 任务 " + (currentTaskIndex+1) + "/" + pipelineTasks.size() + " 完成 §8▸ "` / `"§a§l✓ Pipeline 全部任务完成！"` / `"§b开始 Pipeline 任务 " + (currentTaskIndex+1) + "/" + pipelineTasks.size()` | `:1042,1048,1054` |
| 出错 | `"§c✗ 运行出错 §8▸ " + stopReason` | `:1080` |

物流服务：

| 位置 | 逐字文案 |
| --- | --- |
| `villager/logistics/UnloadService.java:63,82,111` | `"§b开始卸货"` / `"§c容器同步超时"` / `"§a卸货完成"` |
| `villager/logistics/SupplyService.java:58,77,85,99,112,115` | `"§b开始搬运绿宝石，目标 " + targetEmeralds + " 个"` / `"§c容器同步超时"` / `"§a补给达标（" + countEmeralds(player) + " 个）"` / `"§e背包已满，补给提前结束"` / `"§e绿宝石箱已空，补给提前结束"` / `"§c补给搬运异常"` |

### 7.7 自动箱子 `autochest/`

| 所属 | 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- | --- |
| Module | 目标 ID 失效 | `"有 " + removed + " 个目标 ID 已失效，已从选择器移除"` | `autochest/AutoChestModule.java:121` |
| Module | 致命停止 | `"§c✗ " + reason + "，已停止自动箱子"` | `:142` |
| Module | 未进世界 | `"必须在进入世界后才能启动模块。"` | `:154` |
| Module | 删除标点 | `"§c§l✗ 已删除标点 §8▸ " + formatCoords(...) + " §8▸ §7维度 §8▸ §f" + dim` | `:441-442` |
| Module | 删除失败 | `"删除失败：该标点已不存在"` | `:444` |
| Module | 添加标点 | `"§a§l✓ 已添加标点 §8▸ " + formatCoords(...) + " §8▸ §7维度 §8▸ §f" + …displayName + " §8▸ §a" + type.displayName()` | `:485-487` |
| Module | 标点已存在 | `"该标点已存在"` | `:489` |
| Module | 清空标点 | `"§c§l✗ 已清空全部标点 §8▸ 共 " + count + " 个"` | `:497` |
| Module | 清维度记录 | `"§c§l✗ 已清除当前维度处理记录 §8▸ 共 " + removed + " 条"` | `:504` |
| Module | 清全部记录 | `"§c§l✗ 已清除全部处理记录 §8▸ 共 " + files + " 个文件"` | `:510` |
| FSM | 重试 | `"§e⚠ " + reason + " §8▸ 重试 " + retryCount + "/" + max` | `autochest/fsm/AutoChestStateMachine.java:522` |
| FSM | 跳过 | `"§e⚠ " + reason + " §8▸ 暂时跳过"` | `:542` |
| FSM | 进度 | `"§7正在打开容器 §8▸ " + coords` / `"§7正在取物 §8▸ " + coords` / `"§a✓ 处理完成 §8▸ " + coords` | `:645-647` |
| Module | 点「设置箱子点位（对准容器）」时 | `"玩家未加载"` / `"准星未对准任何方块"` / `"当前目标不是可绑定容器"`（**2026-09-14 补记，此前遗漏**） | `autochest/AutoChestModule.java:470,475,480` |

自检缺项 4 条见《基准-02》3.1；指令文案见 7.14。

### 7.8 自动附魔 `enchant/AutoEnchantBook.java`

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 未进世界 | `"§c必须进入世界后才能启动模块。"` | `:849` |
| 旧版点位 | `"旧版点位没有服务器和维度信息，请执行 .fumo 清空 后重新设置！"` | `:859,911` |
| 点位不符 | `"当前服务器或维度与点位不一致，已阻止启动！请切回原世界，或使用 .fumo 清空 重新设置。"` | `:864,919` |
| 未选方案 | `"请先在「原版装备附魔」分类选择装备与极品方案！"` | `:870` |
| 方案违规 | `"目标方案违反 26.1.2 规则：§c" + profileErrors.get(0) + "§7，请重新选择！"` | `:877` |
| 无法解析装备 | `"无法解析目标装备：" + gearProfile.gearName() + …` | `:883` |
| 纯附魔忽略挂机 | `"§7当前为纯附魔模式，忽略挂机位，不会前往挂机区。"` | `:916` |
| 任务列表空 | `"任务列表为空，请至少勾选一个附魔书属性！"` | `:926` |
| 启动报告标题 | `"§a§l✓ 自动附魔 · 启动报告"` | `:965` |
| 启动-当前模式 | `"\n§7当前模式　§8▸ " + highlightFunction(当前运行模式().toString()) + "§r"` | `:968` |
| 启动-目标词条 | `"\n§7目标词条　§8▸ " + highlightNumber(totalTasks + " 本") + "§r"` | `:971-973` |
| 启动-单轮抽取 | `"\n§7单轮抽取　§8▸ " + highlightNumber(单轮抽取次数.get() + " 次") + "§r"` | `:977` |
| GEAR 启动 | `"§a§l✓ 自动附魔 · 原版装备附魔启动"` + 运行模式/目标装备/极品方案/目标附魔/极品数量 | `:2066-2071` |
| GEAR 批次结束 | `"§a§l✓ 原版装备附魔 · 批次完成"` + 处理/完成/失败/附魔/砂轮/铁砧/挂机次数 | `:2078-2085` |
| 附魔完成 | `"§a✓ 附魔完成 §8▸ "`… + `"\n§7获得词条 §8▸ "` + `"\n§7目标进度 §8▸ "` + `"\n§7还缺词条 §8▸ "` | `:2513-2519` |
| 铁砧合并 | `"§a✓ 铁砧合并 §8▸ "` + 主装备/材料/产物/费用/进度 | `:2542-2549` |
| 砂轮清除 | `"§a✓ 砂轮清除 §8▸ "` + `"\n§7去除词条 §8▸ "` | `:2558-2559` |
| 状态切换 | `"§7发包附魔中（不打开界面）..."` / `"§7正在" + newState.cn() + "..."` | `:2038,2044` |
| 服务器/维度变化 | `"检测到服务器或维度发生变化，已停止寻路并关闭模块！"` | `:1039` |
| 装备丢失 | `"目标装备不在背包，原版装备极品附魔已暂停。请补充装备后重试。"` | `:1053` |
| 停机-经验不足 | `"§c✗ 停机 §8▸ 纯附魔经验低于 " + highlightNumber("30 级") + "§7，切换挂机循环后重新启动"` | `:1112,2371` |
| 附魔台消失 | `"附魔台不存在或已被挖掉，自动化已停止。请重新设置附魔台点位。"` / `"附魔台不存在或已被挖掉！自动停机。"` | `:1159,1181,2362,2404` |
| 背包无书 | `"背包无空白书，切至补给！"` | `:1215` |
| 青金石不足 | `"青金石不足 3 个，取消本次附魔并补给！"` / `"附魔台青金石未达到 3 个，取消本次附魔并补给！"` / `"青金石不足 3 个，无法附魔！自动停机。"` / `"附魔台青金石未达到 3 个，取消附魔并停机。"` | `:1236,1254,2446,2464` |
| 命中目标 | `"§a✓ 命中目标附魔书 §8▸ " + highlightText(hitTask)` | `:1299` |
| 成品箱满 | `"成品箱已满！自动停机。"` / `"成品箱空间不足，原版装备极品附魔已暂停。"` | `:1473,3181` |
| 存入/完成 | `"§a已存入：§e" + hitTask + "§f，剩余任务：" + activeTasks.size()` / `"所有极品任务已完成！自动停机。"` | `:1490,1494` |
| 背包满 | `"背包已满，无法继续补给！自动停机。"` 等（取青金石 / 铁砧同款） | `:1530,2312,2603,2837,2879` |
| 补给重试 / 空 | `"§e⚠ " + name + "补给箱暂时没拿到，正在重试（第 " + 补给重试次数 + " 次）..."` / `name + "补给箱已空！自动停机。"` | `:1583,1589` |
| 装备处理失败 | `"装备处理失败 → " + reason + "，已跳过该件。"` | `:2111` |
| 装备箱无可用 | `"装备箱中没有可用的" + (gearProfile == null ? "目标装备" : gearProfile.gearName()) + "，原版装备极品附魔已暂停。"` | `:2256,2353` |
| 青金石箱空 | `"青金石箱已空，无法附魔！原版装备极品附魔已暂停。"` | `:2635` |
| 铁砧更换 | `"§e⚠ 铁砧已损坏或消失，正在自动更换..."` / `"§a✓ 铁砧已更换完成，继续原任务。"` / `"铁砧已损坏，铁砧箱没有备用铁砧，原版装备极品附魔已暂停。"` | `:2774,2936,2863,2942,2991` |
| 铁砧费用高 | `"§e⚠ 铁砧费用过高 §8▸ 无法继续合并，送砂轮清零重新培养"` / `"§c✗ 停机 §8▸ 纯附魔经验不足铁砧费用 " + highlightNumber(cost + " 级") + "§7，切换挂机循环后重新启动"` | `:3091,3101` |
| 达成极品 | `"§a✓ 装备已达成极品目标，存入成品箱 §8▸ " + highlightText(gearProfile.gearName())` | `:3216` |

`.fumo` 指令（`MODULE_NAME = "自动附魔"`）：

| 场景 | 逐字文案 | 行号 |
| --- | --- | --- |
| 设置成功 | 标题 `"已设置" + type.title() + "点位"`，字段 `类型`，状态 `"已保存"` | `enchant/command/FumoCommand.java:126-131` |
| 删除成功 | 标题 `"已删除" + type.title() + "点位"`，状态 `"已删除"` | `:153-158` |
| 清空成功 | 标题 `"已清空全部点位"`，范围 `"全部点位 + 挂机视角 + 服务器维度绑定"`，状态 `"已清空"` | `:173-176` |
| 状态 | 标题 `"点位状态"`，未设置 `"§c未设置"`，状态 `"当前世界与点位匹配"` / `"当前世界与点位不匹配"` | `:206-217` |
| 各类失败 | 标题 `"设置点位失败"` / `"模块未就绪"`；原因 `"准星指向的方块不是附魔台"`、`"准星指向的方块不是砂轮"`、`"准星指向的方块不是铁砧"`、`"[" + type.title() + "] 必须设置为箱子、木桶或潜影盒容器"`、`"[" + type.title() + "] 已设置，请先执行 .fumo 移除 " + type.node() + " 后再重新设置"`、`"当前为 [" + module.currentMode() + "] 模式，无需绑定 [" + type.title() + "] 点位，请切换到对应模式后再设置"`、`"请将准星对准目标方块"`、`"无法识别当前服务器或维度，点位未保存"`、`"旧版点位没有世界绑定，请先执行 .fumo 清空 后重新设置"`、`"现有点位属于其他服务器或维度，请切回原世界，或执行 .fumo 清空 后重设"`；状态 `"操作未完成"` | `:90-289` |

**`.fumo` 点位节点共 9 个（2026-09-14 更正，此前按 8 个记录）**：`enchant/point/PointType.java` 定义的节点里包含 **`铁砧箱`**（设置 `FumoCommand.java:53`、移除 `:66`），而 `FumoCommand.java:28` 的代码内用法块只列了 8 个（自漏 1 个）。上表「设置成功 / 删除成功」的标题按 `type.title()` 泛化输出，因此逐字文案需按 9 个节点分别展开 —— 详见《2026-09-14-旧项目基准-06-指令体系全量清单》附 02 第 2 章。

### 7.9 星露谷农场 `stardew/`

模块直接播报 `stardew/StardewFarmModule.java`（`MODULE_NAME = "星露谷农场"`）：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 删除洒水器 | 标题 `"已删除洒水器点位"`，状态 `"已删除"` | `:1207-1213` |
| 删除点位 | 标题 `"已删除" + type.title()`，状态 `"已删除"` | `:1229-1235` |
| 清空点位 | 标题 `"已清空全部点位"`，数量 `bound + " 个"`，状态 `"已清空"` | `:1248-1252` |
| 点位设置失败 | 标题 `"设置" + title + "失败"`，原因如 `"仅支持已检测资源的多人服务器"`(`:1261`)、`"准星没有对准任何方块"`(`:1195`)、`"该坐标没有洒水器点位"`(`:1200`)、`"本来就没有绑定"`(`:1223`)，状态 `"未保存"` | `:1424-1430` |
| 点位设置成功 | 标题 `"已设置" + title`，字段 维度/坐标/验证/类型，状态 `"成功"` 或 `"已验证"` | `:1439-1445` |
| 状态卡（`.stardew 状态`） | 标题 `"当前状态"`，正文行由 `statusLines()` 生成：`"资源状态"/"资源来源"/"资源缓存"/"资源指纹"/"资源档案"/"作物候选"/"关联产物"/"目标作物"/"文档成熟"/"确认成熟"/"待学习作物"/"工具资源"` 等 | `:1844-1886` |
| 季节识别卡 | 标题 `"季节识别"`，行 `"§7当前季节 §8▸ §f"…`、`"§7来源 §8▸ §f"…`，未识别时追加 `"§7原文 §8▸ §f"…`、`"§7字符 §8▸ §f"…`、`"§7作用域 §8▸ §f"…` | `:2134-2147` |
| 季节绑定失败 | 标题 `"季节绑定失败"`，原因 `"无法识别季节名「" + input + "」"` / `"当前服务器资源尚未就绪"` / `"尚未捕获到季节图标字符"` / `"写入绑定文件失败"`，状态 `"未写入"` | `:2159-2194` |
| 季节绑定成功 | 标题 `"季节绑定成功"`，状态 `"已生效，当前季节 " + after` / `"已记录，与资源包自动识别一致"` / `"已记录，但资源包自动证据优先，当前季节仍为 " + after` | `:2213-2219` |
| 清除季节绑定 | 标题 `"已清除季节绑定"`，状态 `"已恢复为资源包自动识别"`；失败标题 `"清除季节绑定失败"`，原因 `"当前服务器资源尚未就绪"` / `"写入绑定文件失败"`，状态 `"未修改"` | `:2226-2244` |

状态播报器 `stardew/status/StardewStatusReporter.java`（经 `CommandMessageFormatter::sendRaw`）：

| 场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 启动自检失败 | 标题 `"§c§l启动自检未通过"`，字段 `"缺失配置"`=`"§e§l" + problems.size() + " 项"`，状态 `"禁止启动"` | `:102-106` |
| 启动自检通过 | 标题 `"§a§l启动自检通过"`，字段 `"目标作物"/"季节"`，`"配置状态"="正常"`，状态 `"允许启动"` | `:116-120` |
| 季节限制 | 标题 `"§e§l季节限制"`，字段 `"作物"`，`"季节" = seasonLabel + "不可播种，已暂时跳过"`，状态 `"等待季节变化"` | `:133-136` |
| 季节限制解除 | 标题 `"§a§l季节限制已解除"`，字段 `"季节" = seasonLabel + "允许播种"`，状态 `"恢复播种"` | `:142-145` |
| 等待季节 | 标题 `"§e§l等待季节"`，字段 `"任务"="等待季节"`，状态 `"模块保持运行"` | `:152-155` |
| 季节识别 / 已识别 | 标题 `"§e§l季节识别"`，字段 `"来源"/"原文"/"字符"`，状态 `"暂不限制播种"`；已识别标题 `"§e§l季节已识别"`，状态 `"季节限制已生效"` | `:168-172`、`:221-223` |
| 已停止 | `state("STOPPED", "已停止", "自动任务已释放")` | `StardewFarmModule.java:723` |
| 玩家死亡 | `critical("PLAYER_DEAD", "已强制停止", "玩家死亡，农场任务已中止")` | `:740` |
| 自动启动超时 | `critical("AUTO_START_TIMEOUT", "无法自动启动", "服务器资源在 60 秒内未就绪")` | `:796` |
| 收获学习保存失败 | `critical("LEARN_SAVE_FAIL:" + learned.cropKey(), "收获学习保存失败", "当前结果未启用")` | `:1354` |
| 收获规则确认 | `state("LEARN_OK:" + learned.cropKey(), "收获规则已确认", …)` | `:1359` |

协调器 `stardew/task/StardewCoordinator.java`：

| 触发场景 | task / detail 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 等待生长 | `"等待作物生长"`、`"当前农田已处理完成"` | `:648` |
| 寻路失败 | `"无法继续寻路"`、`"当前任务已释放，稍后重新尝试"` | `:721` |
| 拾取不到 | `"拾取不到掉落物"` | `:765` |
| 特殊作物 | `"发现特殊作物"`、`"收割动作尚未确认，已安全跳过"` | `:1251` |
| 容器不可达 | `"容器不可达"`（key 前缀 `CONTAINER_UNREACHABLE:`） | `:1583` |
| 物流点失效 | `critical("LOGISTICS_POINT:"…)` 标题 `"物流点已失效"` | `:1672` |
| 收获学习未确认 | `"收获学习未确认"` | `:1868,2438` |
| 魔法药剂跳过 | `"魔法药剂已跳过"`、`"行为尚未通过真机确认"` | `:1920` |
| 补水点失效 | `"补水点已失效"`、`"已停止当前补水任务"` | `:1928` |
| 水壶补水失败 | `"水壶补水失败"`、`"连续交互无水量变化，已停止本次任务"` | `:2024` |
| 洒水器点位失效 | `"洒水器点位失效"`、`"已停止当前维护任务"` | `:2066` |
| 缺种 / 补货 / 箱子 | `"缺少" + activeCrop.seedDisplayName()`、`critical("RESTOCK_FAIL:"…)` `"补货失败"`、`"种子箱已满"`、`critical("SEED_RETURN_FAIL:"…)` `"种子回存失败"`、`"产物箱没有可用空间"`、`critical("UNLOAD_FAIL:"…)` `"卸货失败"` | `:2467-2491` |
| 完成类 | `state("REFILL_DONE:…", "水壶已补满")`、`state("DEAD_DONE:…", "枯死作物已清除", "已重新检查农田")`、`silent("PLANT_DONE:…", "播种完成", activeCrop.chineseName() + " ×1", "")`、`silent("HARVEST_DONE:…", "收获完成", activeCrop.chineseName(), "")`、`state("COLLECT_DONE:…", "拾取完成")`、`state("RESTOCK_DONE:…", "补货完成")`、`state("UNLOAD_DONE:…")`、`state/silent("SEED_RETURN_DONE:…")`、`state("RETURN_DONE", "后勤任务完成", "已返回农田")` | `:2534-2572` |
| 任务进行中 | `"正在浇水"/"剩余干盆：" + remainingDryPots()`；`"水壶缺水"/"正在前往水源"`；`"正在播种"`；`"正在补货"`；`"正在收获"`；`"正在拾取"/"农田掉落物"`；`"正在卸货"`；`"发现枯死作物"/"正在清理"`；`"返回农田"/"正在前往农田中心"`；`"正在施肥"`；`"正在使用魔法药剂"`；`"正在维护洒水器"` | `:2673-2685` |
| 边界失效 | `critical("BOUNDARY:"…)` 标题 `failedType.title() + "已失效"` | `:2845` |

字段标签映射与级别判定：`detailLabel()`(`:409-431`)、`secondaryLabel()`(`:433-440`)、`levelOf()`(`:442-460`)、`levelDetail()`(`:462-470`)。

### 7.10 ID 识别 / ID 配置管理 / 服务器资源

`itemid/IdIdentifyModule.java`（模块名 `ID识别`）：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 玩家未加载 | `"玩家未加载"` | `:87` |
| 无物品 | `"没有可识别物品：主手和副手都是空的"` | `:104` |
| 识别失败 | `"识别失败"` | `:113` |
| 自动保存成功 | `"§a§l✓ 已识别物品 §8▸ " + highlightText(identity.displayName())` | `:126` |
| 附魔行 | `"§7附魔　§8▸ §a" + e.displayName() + " §8▸ §f" + e.id()` | `:128` |
| 已存在 | `"§7该物品已在 ID 配置中（稳定身份相同）"` | `:131` |
| 快照保存 | `"§a§l✓ 已保存状态快照 §8▸ §a§l" + identity.displayName() + " §8▸ §f" + snapshotName` / `"§7该状态快照已存在，未重复保存"` | `:134,136` |
| 准星方块失败 | `"自动识别失败：准星当前没有指向有效方块"` | `:157` |

`itemid/IdConfigModule.java`（标题 `ID配置管理`）：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 无 ID 配置 | `"当前没有任何 ID 配置，先用「识别物品」「手动添加」或「识别准星方块」"` | `:81` |
| 加载成功 | `"§a§l✓ 已加载 ID 配置 §8▸ " + …+" 个物品，"+…+" 个实体，"+…+" 个方块"` | `:84-87` |
| 摘要（GUI） | `"§7物品 §f"…+" §8▸ §7实体 §f"…+" §8▸ §7方块 §f"…+" §8▸ §7快照 §f"…` | `:221-224` |
| 识别物品 | `"§a§l✓ 已识别物品 §8▸ " + highlightText(identity.displayName())` / `"该物品已在 ID 配置中"` | `:410,413` |
| 识别方块 | `"§a§l✓ 已识别方块 §8▸ " + highlightText(identity.displayName()) + " §8▸ §f" + identity.blockId()` / `"§7该方块已在方块记录中（稳定身份相同）"` | `:462,465` |
| 方块快照 | `"§a§l✓ 已保存方块状态快照 §8▸ §f" + snapshotName` / `"§7该方块状态快照已存在，未重复保存"` | `:468,470` |
| 刷新 | `"§a§l✓ 已刷新 ID 配置 §8▸ " + …+" 个物品，"+…+" 个实体，"+…+" 个方块"` | `:481-484` |
| 删除 | `"§c§l✗ 已删除 ID §8▸ "`… / `"§c§l✗ 已删除实体 §8▸ "`… / `"§c§l✗ 已删除方块 §8▸ "`… | `:492,502,512` |
| 清空结果 | `"§a§l✓ 已清空方块稳定记录（blocks/）"`、`"§a§l✓ 已清空方块历史快照（block-snapshots/）"`、`"§a§l✓ 已清空全部方块数据（blocks/ + block-snapshots/）"`、`"§a§l✓ 已清空全部物品 ID（items/）"`、`"§a§l✓ 已清空全部实体 ID（entities/）"`、`"§a§l✓ 已清空全部 ID 数据（物品 + 实体 + 方块稳定记录 + 历史快照）"` | `:552,563,575,620,631,645` |

`.id` 指令（`itemid/IdCommand.java`，`MODULE_NAME = "ID识别"`）：

| 场景 | 逐字文案 | 行号 |
| --- | --- | --- |
| 帮助 | 标题 `"识别指令用法"`，字段 `物品` / `实体` / `方块` | `:291-295` |
| 物品失败 | 标题 `"物品识别失败"`，原因 `"玩家未加载"` / `"没有可识别物品：主手和副手都是空的"` / `"无法解析该物品的稳定身份"`，状态 `"未保存"` | `:86-101,299-304` |
| 物品成功 | 标题 `"物品识别完成"`，字段 `名称` / `物品 ID` / `自定义身份` / `资源模型` / `保存文件` / `状态快照`，状态 `"该物品已在 ID 配置中（稳定身份相同）"` / `"已保存"` | `:119-136` |
| 实体失败 | 标题 `"实体识别失败"`，原因 `"当前准星未指向可识别实体"` / `"无法解析该实体的稳定身份"` | `:153,159` |
| 实体成功 | 标题 `"实体识别完成"`，字段 `名称` / `实体 ID` / `身份键` / `保存文件`，状态 `"该实体已在 ID 配置中"` / `"已保存"` | `:168-176` |
| 方块失败 | 标题 `"方块识别失败"`，原因 `"准星当前没有指向有效方块"` | `:193` |
| 方块已存在 | 标题 `"方块识别完成"`，字段 `状态快照`=`"已存在，未重复保存"`，状态 `"该方块已在方块记录中（稳定身份相同）"` | `:212-217` |
| 方块成功 | 标题 `"方块识别完成"`，字段 `名称` / `方块 ID` / 坐标 / `方块状态` / `身份键` / `方块实体` / `资源模型` / `自定义身份` / `自定义名称` / `解析状态` / `保存文件` / `状态快照`，状态 `"已保存"` | `:232-249` |
| 作物状态字段 | `真实状态` / `成熟阶段` / `生命周期` / `规则来源`，枯死显示 `"枯死作物"` | `:260-273` |

结果屏：

| 位置 | 逐字文案 |
| --- | --- |
| `itemid/IdResultScreen.java:106,118,121,135,138` | `"§a§l✓ " + success` / `"§e快照保存失败"` / `"§a§l✓ 已另存快照 §8▸ §a§l" + identity.displayName() + " §8▸ §f" + fileName` / `"§e该物品已在 ID 配置中"` / `"§a§l✓ 已添加到 ID 配置 §8▸ §a§l" + identity.displayName() + " §8▸ §f" + fileName` |
| `itemid/EntityResultScreen.java:76,98` | `"§a§l✓ " + success` / `"§a§l✓ 已保存实体 §8▸ §a§l" + identity.displayName() + " §8▸ §f" + fileName` |
| `itemid/IdAddScreen.java:73,103,109,159,164,167-169` | `"§c✗ 请输入物品 ID 或中文名称"` / `"§c✗ 未找到对应物品 §8▸ 请先手持识别，或填写真实物品 ID（如 minecraft:diamond）"` / `"§c✗ 未找到对应物品 §8▸ 请先手持识别，或填写真实物品 ID"` / `"§e该物品已在 ID 配置中 §8▸ " + identity.displayName()` / `"§a§l✓ 已添加物品 §8▸ §a§l" + identity.displayName() + " §8▸ §f" + identity.itemId() + (…" §8▸ §d自定义")` |
| `worldblock/BlockResultScreen.java:119,130,135,138` | `"§a§l✓ " + success` / `"§c快照管理器未就绪"` / `"§c快照保存失败"` / `"§a§l✓ 已另存方块状态快照 §8▸ §f" + fileName` |

服务器资源服务 `resourcepack/ServerResourceService.java`（两个播报模块名：`"服务器核心"` 与 `"星露谷农场"`）：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 非多人环境 | `"§c仅多人服务器可检测服务器资源包"` / `"§c星露谷资源检测仅支持多人服务器"` | `:500,504` |
| 处理中 | `"§e资源正在处理中（" + phase.label() + "），请稍候…"` | `:508` |
| 无法识别地址 | `"§c无法识别当前服务器地址，资源检测已取消。"` | `:515` |
| 开始检测 | `"§e开始检测服务器资源"` + 行 `服务器名称` / `服务器地址` / `状态` | `:542-545` |
| 命中缓存 | `"§a已发现资源缓存，直接复用§8 › §f" + zip.getName()` | `:721` |
| 开始下载 | `"§e正在下载当前服务器资源包…"` | `:728` |
| 降级临时 | `"§6未发现本地缓存，也没有可用的资源包下载地址，改用客户端已加载的服务器资源"` + 行 `资源来源`=`"§6当前服务器已加载资源（临时）"` / `资源缓存`=`"§6未建立（临时解析）"` / `说明`=`"§7本次未落盘，无法留作下次复用"` | `:742-745` |
| 等待应用 | `"§e正在等待客户端应用当前服务器资源…"` | `:824` |
| 未观察信号 | `"§6⚠ 未观察到资源应用完成信号，按当前已加载资源继续解析"` | `:866` |
| 解析中 | `"§e正在解析资源…"` | `:888` |
| 非星露谷 | `"§c当前服务器未检测到可识别的星露谷资源，未建立资源档案。"` | `:908` |
| 检测完成 | `"§a资源检测完成"` + 行 `资源来源` / `资源缓存` / `资源指纹` / `资源状态`=`"§a星露谷资源"` / `状态`=`"§6临时可用"` 或 `"§a成功"` | `:930-935` |
| 档案建立 | `"§a资源档案已建立"` + 两行三格 `作物/种植盆/肥料`、`药剂/水壶/洒水器` | `:945-948` |
| 失败 | `"§c星露谷资源准备失败：" + reason` | `:979` |
| 自动重读 | `"§e检测到已授权的同服子服切换，正在自动读取当前资源…"`(`:607`)、`"§e检测到同服子服资源已更新，正在自动重新读取…"`(`:686`)、`"§e检测到资源重载，重新尝试提取星露谷资源…"`(`:707`) | 同左 |

`resourcepack/ResourcePackDownloader.java`：`"§7ℹ 已下载过 §7» §f" + target`(`:320`)、`"§a✔ 资源包已下载并落盘 §7» §f" + target`(`:322`)、`"§a✔ 资源包已下载并落盘 §7» §f" + target.getName() + " §8(原版缓存)"`(`:357`)、`"§a✔ 资源包已下载并落盘 §7» §f" + fileName`(`:414`)。
`tactical/ServerDetector.java:536`：`"§a✔ 已拦截资源包 §8(暴力绕过)"`。

### 7.11 服务器检测 / 绕过 / 传送

`tactical/ServerDetector.java`：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 单人世界 | `"§c单人世界无需检测"`（warning） | `:211` |
| 服务器内启动 | `"已在服务器中，将在 " + detectDelay.get() + " 秒后开始侦测"` | `:222` |
| 侦测报告标题 | `"§b§l━━ 服务端侦测报告 ━━§r"` | `:326` |
| 报告-核心 | `"§7服务器核心 §8▸ " + (coreUnknown ? "§8"+core : highlightServer(core))` | `:330` |
| 报告-反作弊 | `"§7反作弊   §8▸ §8未检测"` / `"§7反作弊   §8▸ " + highlightText("未发现指纹") + " §8（不等于没有）"` / `"§7反作弊   §8▸ §c§l" + antiCheat` | `:334-338` |
| 报告-风险 | `"§c§l✗ 高风险 §8（协调器已收紧绕过策略）"` / `"§e§l⚠ 中低风险"` | `:345,347` |
| 打开目录失败 | `"打开资源库失败：" + message`（notifyError） | `:553` |

`tactical/FlightBypass.java`：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 单人世界 | `"§c单人世界无需飞行绕过"`（warning） | `:233` |
| 降级 | `"§e⚠ 连续拉回触发降级 §8▸ " + highlightFunction(d.mode().displayName)` | `:352` |
| 无飞行能力 | `"§e⚠ 服务器未授予飞行能力 §8▸ " + highlightFunction("发包飞行") + " 不可用" + suffix`（suffix 为 `"；当前无鞘翅，只能执行非飞行连跳"` 或 `"，已自动切换 " + highlightFunction(...)`） | `:356-359` |
| 高风险反作弊 | `"§e⚠ 命中高风险反作弊 §8▸ " + highlightFunction("发包飞行") + " 已自动降级为 " + highlightFunction(d.mode().displayName)` | `:363-364` |
| 恢复 | `"§a✓ 已恢复执行 §8▸ " + highlightFunction(d.mode().displayName)` | `:369` |
| 缺烟花 | `"§e⚠ 快捷栏没有烟花 §8▸ 烟花火箭模式无法推进"` | `:488` |
| 缺鞘翅 | `"§e⚠ 背包没有鞘翅 §8▸ 安全滑翔/烟花火箭需要 " + highlightText("鞘翅")` | `:569` |

`tactical/AntiKickBypass.java`：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 单人世界 | `"§c单人世界无需防踢"`（warning） | `:387` |
| 进服伪装自检 | `"进服伪装自检：" + "\n§8├─ §f客户端名 §8▸ " + … + "\n§8├─ §fMod通信 §8▸ " + … + "\n§8├─ §f假潜行 §8▸ " + … + "\n§8└─ §f假疾跑 §8▸ " + …`（值 `highlightText("vanilla")` / `"§c未伪装"` / `highlightText("拦截中")` / `"§c未拦截"`） | `:427-433` |
| 存活过短 | `"§c存活不到 1 分钟，可能被踢了"` | `:443` |
| 本会话统计 | `"本会话伪装统计：" + "\n§8├─ §f品牌替换 §8▸ " + … + " 次" + "\n§8└─ §fMod频道拦截 §8▸ " + … + " 个"` | `:448-450` |
| 被拉回 | `"§e⚠ 被拉回 §8▸ 冷却与统计已由协调器登记"` | `:609` |
| 反作弊收紧 | `"检测到 " + event.antiCheatName + "，发包限速收紧到 50%"` | `:623` |
| 拉回分析报告 | `"§e§l拉回分析报告 §8（" + total + " 次）§r\n"` + 四行 `飞行时被拉/挖掘时被拉/放置时被拉/高速时被拉`，建议行 `"\n§8▸ §a建议 §8▸ 把「飞行绕过」切换到安全滑翔或原版连跳"` / `"…降低「每秒最多挖几个」的值"` / `"…降低「每秒最多放几个」的值"` | `:738-751` |

`tactical/packetbreak/PacketInstantBreak.java`：

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 单人世界 | `"§c单人世界无需发包秒破"`（warning） | `:370` |
| 冲突拒启 | `"你已开启以下自带功能，请关闭后再打开发包秒破："`，逐项 `"§c  §6· §f" + c`，冲突名如 `"自动挖矿 · 快速破坏（秒破）"`、`"Meteor packet-mine（发包挖掘）"`、`"Meteor speed-mine（加速挖掘）"`、`"Meteor nuker（自动挖掘）"`、`"Meteor excavator（区域挖掘）"`、`"Meteor vein-miner（矿脉挖掘）"`、`"Meteor infinity-miner（无限挖掘）"`、`"Meteor instant-rebreak（即时重挖）"` | `:382-448` |
| 启动报告 | `"§a§l✓ 发包秒破 · 启动报告"` + 行 `破坏方式` / `目标模式` / `扫描半径` / `方块间隔` / `自动换工具` | `:420-428` |
| 反作弊联动 | `"检测到 " + event.antiCheatName + "，本会话发包破坏将使用原版速度阈值"` | `:500` |
| 冲突自动停 | `"你已开启冲突的自带功能，已自动停止发包秒破"` | `:571` |
| 成功节流 | `"§a✓ 发包秒破 §8▸ 已破坏 " + highlightNumber(successCount + " 块")` | `:883` |
| 放弃方块 | `"§c✗ 放弃方块 §8▸ " + highlightText(done.block.getName().getString()) + (reason != null ? " §8▸ " + reason : "")` | `:886-887` |
| 失败原因 | `"超出服务器可交互距离"`、`"当前工具破坏速度为 0"`、`"最佳工具槽位切换失败"`、`"手上工具破坏速度为 0"`、`"挖掘期间手持工具发生变化"`、`"停挖发包前手持工具发生变化"`、`"开始发包前手持工具发生变化"`、`"STOP 发包前手持工具发生变化"`、`"服务器未确认方块破坏"`、`"服务器拒绝破坏或发生方块回弹"` | `:607,630,692,702,624,643,726,785,656,833` |

`teleport/`：

| 位置 | 逐字文案 |
| --- | --- |
| `teleport/TeleportModule.java:105` | `"§c✗ 模块未开启 ▸ 请先开启 " + highlightFunction("传送") + "，再按快捷键使用 " + highlightFunction(keyName)` |
| `teleport/core/TeleportCoordinator.java:92,97` | `"执行中 ▸ 上一次传送尚未结束，请稍候"` / `"传送失败 ▸ 死亡状态下不可传送"` |
| `:137` | `"§7正在传送 ▸ " + sink.func(req.mode.toString()) + " ▸ 目标 " + sink.loc(ctx.target.posText())` |
| `:225,239,240,253,260,271,278` | `"视线快照缺失，请重试"` / `"前方世界数据未加载，扩大搜索前请先走近"` / `"障碍太厚，超出最大穿墙距离"` / `"准星方向的落点修正范围内没有可站立位置"` / `"未指定传送坐标"` / `"目标坐标不安全，邻近搜索无可用落点"` / `"未知传送模式"` |
| `:298-307` | `"传送前世界已变化，已取消执行"` / `"传送前玩家状态异常，已取消执行"` / `"目标缺失，已取消执行"` / `"目标区域已变化（" + problem + "），已取消执行"` |
| `:159,163,167` | `"传送期间世界变化，已中止"` / `"传送期间玩家死亡，已中止"` / `"传送期间载具关系变化，已中止"` |
| `:324-341` | `"§a✓ 传送完成 ▸ "` + 坐标 + `" ▸ 距离 "`…+ `" 格"` +（GROUND）`" ▸ 上升/下降 " + 格` +（WALL）`" ▸ 穿透 "`…`" 层"`、`" ▸ 局部修正 "`… +（COORD）`" ▸ 已修正至邻近安全点"` |
| `:348` | `"§a✓ 已在真正地表 ▸ 无需传送"` |
| `:357-358` | `"§c✗ 传送失败 ▸ 服务器位置被修正 ▸ 偏差 "`…+ `" 格 ▸ 已跟随服务器"` |
| `:368` | `"§c✗ 传送失败 ▸ " + sink.text(reason)` |
| `teleport/command/TpCommand.java:36-40` | 标题 `"传送未执行"`，字段 `原因`=`"请先开启传送模块"`，状态 `"未传送"` |

### 7.12 其他模块

| 模块 | 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- | --- |
| 主题 `theme/ThemeModule.java` | 非默认主题 | `"当前不是 Meteor 默认主题，无法应用配色方案"`（notifyError） | `:110` |
| 水源显示 `water/WaterESPModule.java` | — | 未找到聊天播报（仅渲染 + GUI） | — |
| 用户统计 `userstats/UserStatsModule.java` | — | 仅界面文本（`"§e加载中..."` / `"§a立即刷新"` 等）；团队注释明确「不再发送统计公屏消息」 | `:340,410` |

### 7.13 工具 / 服务类 `utils/YiyiaddonWelcomeService.java`

| 触发场景 | 逐字文案 | 文件:行号 |
| --- | --- | --- |
| 更新提示-分割线 | `"§3§m═══════════════════════════════════"` | `:171,206,613,691` |
| 更新提示-标题 | `"§7发现§f§l新版本 §a§l" + latest.version + " §7当前 §e§l" + currentVersion` | `:174` |
| 更新内容 | `"§2§l更新内容："` 与逐行 `"§f" + line` | `:181,184` |
| 更新-中间线 | `"§3§m───────────────────────────────────"` | `:187,648` |
| 下载地址 | `"§2§l下载地址: "` + `"§b§n点击下载"`（点击打开 `latest.url`） | `:192-200` |
| 跳过提示 | `"§7输入 §e§l.yiyiaddon 跳过 §f§l跳过此版本"` | `:203` |
| 手动更新-成功 | `"§a发现新版本 §f§l" + latest.version + " §7(当前 " + currentVersion + ")"` / `"§a已是最新版本 §f§l" + currentVersion` / `"§7当前版本：§e"+…+" §7最新版本：§a"+…+" §7(开发版)"` / `"§7当前版本：§e"+…+" §7最新版本：§a"+…+" §7(已跳过)"` | `:149,153,155,142` |
| 手动更新-失败 | `"§c未能获取最新版本信息（网络超时或 GitHub API 限流）"` / `"§c检查失败：" + PlayerText.failure(e)` | `:135,158` |
| 欢迎-汉化说明 | `"§7本扩展已整合§f§l简体中文汉化§7跟汉化§f§lBaritone"` / `"§7免费 为爱发电 §8| §f版本 " + versionDisplay` | `:617,620` |
| 欢迎-GitHub | `"§2§lGitHub: "` + `"§b§n" + REPOSITORY_URL.replace("https://","")` | `:624-625` |
| 欢迎-Bug反馈 | `"§2§lBug反馈: "` + `"§b§n点击反馈"` | `:635-636` |
| 欢迎-账户 | 新用户 `"§3│ " + accountType + " §6§l" + name` + `"§3│ §7你是第 §e§l#" + rank + " §7个使用者 §a§l✓"`；老用户 `"§3│ §7欢迎回来 " + accountType + " §6§l" + name` + `"§3│ §7你是第 §e§l#" + rank + " §7个使用者"`（`accountType` = `"§a§l[正版]"` / `"§c§l[离线]"`） | `:651-665` |
| 欢迎-总人数 | `"§7当前已有 §2§l" + total + " §f§l位玩家使用"` | `:668` |
| 欢迎-IP | `"§7IP: §b" + ipInfo.ip + countryDisplay + proxyStatus` | `:684-685` |
| 注册失败 | `"§c✗ " + reason`（经 `formatMessage("用户统计", …)`） | `:716` |
| 消息轮询-管理员 | `"§b[管理员] §f"` + 消息 + `" §8(使用 .回复 <内容> 回复)"` | `:1272-1274` |
| 消息轮询-聊天 | `"§d[聊天] " + (premium ? "§a[正版] " : "§7[离线] ") + "§e" + sender + "§8：§f"` + 消息 | `:1272` |
| 24h 活跃 | `"§f§l24h §7活跃 §b§l" + activeCount + " §f§l人"` +（有最近用户）`" §7§l| §7最近 §e§l" + name + " " + timeDesc`（timeDesc = `"§a§l刚刚在线"` / `"§7"+h+"§f§lh §7前"`） | `:1066-1071` |

未找到聊天播报的 utils：`DataPack.java`、`ResourceCrypto.java`、`YiyiaddonHeartbeatService.java`、`YiyiaddonTelemetryService.java`、`YiyiaddonPasswordInterceptorService.java`、`YiyiaddonWatermark.java`、`YiyiaddonCommandLogger.java`（仅 `Logger` 日志）。

### 7.14 命令类汇总（模块名前缀 + 播报对应）

| 命令 | 前缀模块名 | 播报见 | 文件 |
| --- | --- | --- | --- |
| `.id 物品/实体/方块` | `ID识别` | 7.10 | `itemid/IdCommand.java` |
| `.stardew` | `星露谷农场` | 下表 | `stardew/command/StardewCommand.java` |
| `.fumo` | `自动附魔` | 7.8 | `enchant/command/FumoCommand.java` |
| `.farm` / `.nongchang` | `自动农场` | 下表 | `autofarm/command/NongChangCommand.java` |
| `.wk` | `自动挖矿` | 下表 | `mining/command/WKCommand.java` |
| `.cunmin` | `自动村民交易` | 下表 | `villager/command/CunminCommand.java` |
| `.autochest` | `自动箱子` | 下表 | `autochest/command/AutoChestCommand.java` |
| `.tp` | `传送` | 7.11 | `teleport/command/TpCommand.java:36-40` |
| `.聊天` | `聊天` | 下表 | `commands/YiyiaddonChatCommand.java` |
| `.回复` | `管理员消息` | 下表 | `commands/ReplyAdminCommand.java` |
| `.yiyiaddon` | `更新管理` | 下表 | `commands/YiyiaddonUpdateCommand.java` |

**`.id` 补充索引（2026-09-14 补记，此前遗漏）**：`.id` 成功回执还含**作物归属三字段** `所属作物` / `cropKey` / `类型`（`itemid/IdCommand.java:128,277-282`）与作物状态 `资源阶段`（`:267`）/ `原作物`（`:269`），由 `stardew.recognition.CropRuntimeStateResolver` 提供。逐字与全部分支见《2026-09-14-旧项目基准-06-指令体系全量清单》附 01 第 1.5 节。

`.stardew`（`stardew/command/StardewCommand.java:227-535`）：标题 `"已设置种植目标"`（字段 `作物` / `农田`，状态 `"已写入农田记忆"`）、`"设置种植目标失败"`（原因 `"请先绑定农田起点 / 终点，且该作物已在「作物选择」中被勾选"`，状态 `"未写入"`）、`"标记成熟失败"`（原因 `"当前方块为枯死作物，不能标记成熟"` / `"当前方块不是已识别的自定义作物"` / `"已识别为自定义作物，但缺少阶段信息（无法确定 stageKey）"` / `"当前没有有效的准星目标"` / `"当前准星指向空气"` / `"无法唯一解析作物「"+x+"」：请使用当前服务器资源里存在的 cropKey，或 TAB 补全后再执行"` / `"当前阶段属于特殊变种，需要独立收割 / 行为规则，不能作为普通成熟阶段人工确认"` / `"当前服务器资源中不存在该阶段"` / `"该作物（"+x+"）不在当前服务器资源索引中：请先在「服务器资源」检测 / 提取资源"` / `"保存失败：请确认当前服务器资源已就绪（需为多人服务器且已完成资源检测）"`）、`"成熟规则已确认"`（状态 `"当前阶段与已有成熟规则一致，未改动任何规则"`）、`"已纠正成熟规则"` / `"已人工确认成熟规则"`（状态 `"已保存"`）、资源闸门原因 `"当前未进入世界"` / `"星露谷资源检测仅支持多人服务器"` / `"当前服务器资源尚未检测"`。

`.farm`（`autofarm/command/NongChangCommand.java:73-347`）：标题 `"锚点绑定状态"`、`"已清空全部锚点"`、`"已绑定"+type.cn()`、`"已解绑"+type.cn()`、`"已扩展农田"`（字段 `方向` / `格数` / `新范围` / `农场点位1` / `农场点位2`，状态 `"已保存"`）；提示与错误 `"六个锚点本来就都没有绑定。"`、`"当前不在游戏世界中，无法绑定锚点"`、`"模块运行中无法修改锚点，请先关闭模块"`、`type.cn()+"已绑定，请先删除旧绑定再重新设置"`、`"§7提示：使用 §e.farm 移除 " + type.cn() + " §7删除"`、`"准星未对准任何方块，请将准星对准要绑定的方块"`、`"该锚点需要指向容器方块（箱子/桶/潜影盒等），但准星对准的不是容器"`、`"无法获取当前维度信息"`、`type.cn()+"本来就没有绑定。"`、`"当前不在游戏世界中，无法扩展农田"`、`"模块运行中无法修改农田范围，请先关闭模块"`、`"农田范围未绑定完整，请先用 .farm 设置 农场点位1 / 农场点位2 框出范围"`、`"农田锚点不在当前维度，无法按面朝方向扩展"`、`"自动农场模块未注册。"`（**2026-09-14 更正**：并非全部带 `§6` 前缀——`farmInfo` 走**无前缀**路径，如 `"§7提示：使用 §e.farm 移除 " + type.cn() + " §7删除"`，`NongChangCommand.java:132,285-287`；其余错误经 `error()` 追加 `"§6"`）。

`.wk`（`mining/command/WKCommand.java:134-594`）：绑定成功标题 `"已设置"+name` / `"已设置挂机修复点"`（字段 `类型` / `视角`=`"偏航角 %.1f° / 俯仰角 %.1f°"`，状态 `"已保存"`）、删除 `"已删除"+name`、清空 `"已清空全部绑定"`（范围 `"矿物箱 + 食物箱 + 挂机修复点"`）、状态 `"坐标绑定状态"`（状态 `DATA_STORE.size() + " / 3 已绑定"`）；提示与错误 `"矿物箱已绑定，请先删除旧绑定再重新设置"`、`"§7提示：使用 §e.wk 移除 矿物箱 §7删除"`、`"准星未对准任何方块"`、`"目标方块不是容器（箱子/桶/潜影盒等）"`、`"无法获取当前维度信息"`、`"跨维度绑定被拒绝！\n§7%s 位于 §e%s§7，但你现在在 §e%s\n§7三个点位必须在同一维度，防止 Baritone 跨维度乱寻路"`、`"%s 与 %s 距离超过 32 格（%.1f 格），三个点位必须互相在 32 格范围内"`、`"该坐标本来就没有绑定"`、`"当前没有任何绑定"`、`"自动挖矿模块未加载"`。

`.cunmin`（`villager/command/CunminCommand.java:107-538`）：设置标题 `"已设置"+displayName`、删除 `"已删除"+displayName`、清空 `"已清空全部绑定"`（范围 `"绿宝石箱 + 成品交易箱"`）、状态 `"坐标绑定状态"`（未绑定 `"§8未绑定"`；状态 `"已绑定"` / `"暂无绑定点位，使用 .cunmin 设置 <目标> 进行绑定"`）；错误 `"§c玩家或世界无效"`、`"§c请将准星对准容器方块"`、`"§c目标方块不是有效的容器（箱子/木桶/潜影盒）"`、`"%s 未绑定"`、`"保存失败：" + PlayerText.failure(e)`、`"§c准星未对准任何方块，请重新设置"`、`"§c目标方块不是容器（箱子/桶/潜影盒等），请重新设置"`、`"§c§l✗ 已删除 " + name + " 绑定"`、`"§c该坐标本来就没有绑定"`。

`.autochest`（`autochest/command/AutoChestCommand.java`）：标题 `"已设置"+type.displayName()` / `"已删除标点"` / `"已清空全部标点"` / `"标点状态"`；失败标题 `"添加标点失败"` / `"删除标点失败"` / `"清空标点失败"` / `"标点状态"`，原因 `"自动箱子模块未加载"` / `"准星未对准任何方块"` / `"当前目标不是可绑定容器"` / `"该坐标已存在标点"` / `"该坐标没有标点"`；状态 `"操作未完成"`；值 `"当前维度没有标点"`、`"§c已处理"` / `"§a未处理"`、`"共 " + points.size() + " 个标点"`。

`.聊天`（`commands/YiyiaddonChatCommand.java:74-174`）：帮助标题 `"聊天指令用法"` / `"聊天系统"`，字段 `在线/帮助/说/私聊/回复/说明`；在线查询 `"在线玩家"`（字段 `人数` / `名单`，无人在线 `"§8当前没有在线玩家"`，状态 `"查询完成"`）；发送 `"消息已发送"` / `"消息发送失败"`（字段 `对象`，状态 `"已发送"` / `"未发送，请稍后重试"`）；错误 `"§c消息不能为空且不能超过 300 个字符"`、`"§c在线列表获取失败"`、`"§c消息发送失败，请检查网络"`。

`.回复`（`commands/ReplyAdminCommand.java:82-103`）：标题 `"回复已发送"`（字段 `对象`=`"管理员"`，状态 `"已发送"`）、`"回复发送失败"`（字段 `原因`=`"服务未接受回复请求"` / `PlayerText.failure(e)`、`技术信息`=`"响应码 " + statusCode`；状态 `"未发送"`）。

`.yiyiaddon`（`commands/YiyiaddonUpdateCommand.java:28-68`）：标题 `"已跳过当前版本更新提示"`（字段 `恢复方式`=`"删除 config/yiyiaddon-skip-version.txt"`，状态 `"已跳过"`）、`"正在检查更新"`（状态 `"请稍候"`）、`"更新管理"`（字段 `跳过/检查/说明`）、`"§c检查失败：" + PlayerText.failure(e)`。

---

## 八、本次未覆盖 / 存疑项

1. `itemid/ItemIdManager`、`EntityIdManager`、`BlockIdManager`、`ItemSnapshotManager`、`BlockSnapshotManager` 未逐行通读（只需文件读写，未发现聊天输出）。
2. `worldblock/BlockResultScreen.java` 只按 `copy/save/saveSnapshot` 三类列出按钮与文案，其它分支文案可能遗漏。
3. `enchant/AutoEnchantBook.java`（约 3200 行）只提取含 `notify/notifyError` 的行并读取报告构造区，`State.cn()` 枚举值未展开。
4. `enchant/report/GearCraftReport`、`enchant/gear/*`、`enchant/vanilla/*`、`enchant/ui/*` 仅关键字检索，未逐个通读。
5. `stardew/CropRuntimeStateResolver` 的 `displayName()/lifecycleLabel()/ruleSourceLabel()/matureStageLabel()` 具体返回串未逐字展开（这些值会进入 `.id` / `.stardew` 卡片字段）。
6. `translations/` 包（翻译注入会在运行时改写第三方界面与聊天文本）未盘其翻译表。
7. `mixin/` 包 39 个 Mixin 未逐个展开（其中 `ModuleToggleMessageMixin` 会改写模块开关提示）。
8. `CommandMessageFormatter.alignedComponent` 依赖的 `yiyiaddon:spacing` 字体资源文件未核对（不在 java 源码内）。
9. `autologin/AutoLoginModule.java`（约 1700 行）个别多行拼接字符串未逐字确认后半段；`AccountChecker.describe(mc)` 返回串未展开。
10. `mining/notification/SoundNotifier` 为声音提示，未确认是否附带文字输出。
