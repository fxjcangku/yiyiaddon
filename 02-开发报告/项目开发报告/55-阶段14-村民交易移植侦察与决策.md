# 55 · 阶段14 · 村民交易移植侦察与决策

> 本文回答三件事：**旧项目 `villager` 里有什么（逐条带行号）**、**落到新项目怎么接（复用什么、新建什么）**、**哪些必须用户拍板**。
> 口径正本：[01-总纲-迁移口径与复刻原则.md](01-总纲-迁移口径与复刻原则.md)（功能全量复刻 + 只换 UI 外壳 + 禁止新增旧项目没有的功能）。
> 上游顺序：[30-移植总览](30-移植总览-已移植与未移植全清单.md)第五节（村民交易 → 图书管理员 → 传送 → 自动登入 → 战术 → 单文件模块）。
> 同批做法参照：[48-阶段13-自动农场移植侦察与决策.md](48-阶段13-自动农场移植侦察与决策.md)。
> **代码未动，等用户拍板 D1~D6 后开工。**

**日期**：2026-09-17　**状态**：侦察完成（**大扫描终版**：16 文件全部全文细读，逐字播报与逐态行为见 [56-阶段14-附录A-逻辑层全量侦察记录.md](56-阶段14-附录A-逻辑层全量侦察记录.md)；§2.0 侦察深度声明已按附录更新）。

---

## 一、结论摘要（一屏看完）

| 项 | 结论 |
|---|---|
| 旧包 | `com.example.addon.villager`，**16 文件 / 4,309 行**（本机逐文件实测，见 2.1） |
| 死代码 | `adapter/` 1 文件 + `recognition/` 2 文件 = **3 文件 / 68 行，零调用**（2.8）→ 建议不搬，**待拍板 D1** |
| 模块形态 | 三模式村民交易所：**LOCAL 原地交易**（不寻路，只自动交易，补给卸货玩家手动）/ **SINGLE_PATH 寻路单点**（Baritone 寻路 + 自动补给卸货）/ **PIPELINE 多任务**（勾选职业组成队列依次执行）；交易走**真实打开村民界面发包**（SelectTrade + QUICK_MOVE，无静默交易）；默认榨干模式 |
| 状态机 | `VillagerTradeFSM` **1,221 行 / 18 态**（2.3），超 1,000 行 → 按第 38 条**必须先拆分再迁移**（拆分方案见 2.3 末） |
| 设置项 | 静态可视 7 项 + 动态 53 项（13 职业 × 物品选择器 / 价格上限 / 购买量隐藏项 / 多任务勾选，另有图书管理员附魔书选择器），分组 `目标物品 / 价格上限 / 多任务职业`（2.4） |
| 指令 | `.cunmin`（**无别名**）：设置 / 移除 / 状态 / 清空，两锚点 `绿宝石箱 / 成品交易箱`，回执走统一排版器（2.5） |
| 持久化 | `config/yiyiaddon/cunmin/<服务器>.json` 按服务器分文件 + **手写逐行 JSON 解析**（2.7）→ 新项目改 `JsonFileStore`（第 45 条已定规范） |
| 播报 | 启动报告整块多行 + FSM 内 35 处 `log()` + 每笔交易成功/失败提示（2.6） |
| 提示音 | 交易成功 `SoundEvents.VILLAGER_TRADE`（FSM `:711`）——旧项目已有，1:1 保留 |
| ESP | `ContainerESP`（82 行）：两箱线框 + 字牌（绿宝石箱绿 / 成品交易箱青）→ **直接复用新项目 `WorldOverlay` + `EspRenderer`**（成熟基建，用户已明确口径） |
| 底座就绪度 | 容器（`ContainerService`/`ContainerAccess`）、发包（`BlockPacketSender`）、导航（`FarmNav` 原语）、持久化（`repository/JsonFileStore` + `MiningPointStore` 按服务器分文件先例）、服务器键（`ResourcePackCache.canonicalKey`）**全部已存在**（第四节） |
| 拍板 | D1~D6 未拍板（第五节），拍板前不开工 |

## 二、旧项目事实（只读扫描，2026-09-17 实测）

### 2.0 侦察深度（2026-09-17 大扫描终版，如实声明）

- **16 文件全部全文细读**（`AutoVillagerTradeModule` 835 / `VillagerTradeFSM` 1,221 / `CunminCommand` 560 / `VillagerProfessionRegistry` 306 / `VillagerNavigationService` 318 / `TradeEngine` 168 / `UnloadService` 187 / `SupplyService` 170 / `EnchantmentMatcher` 99 / `TradeMatcher` 149 / `ContainerESP` 82 / `VillagerTradeTarget` 78 / `PipelineTask` 68 / 死代码 3 文件 68）。
- 逐态行为、全部 38 处 FSM 播报原文（含 `§8[<模式>]§r` 包装前缀机制）、服务层/导航/交易协议判据 → **[56 号附录A](56-阶段14-附录A-逻辑层全量侦察记录.md)**。
- **零调用验证**：`adapter/` + `recognition/` 三文件全项目搜索（2.7）。
- 行号均为本机实测；引用一律以「文件:行号」给出。

### 2.1 文件与规模（逐文件行数实测）

| 文件 | 行数 |
|---|---|
| `AutoVillagerTradeModule.java` | 835 |
| `fsm\VillagerTradeFSM.java` | 1,221 |
| `command\CunminCommand.java` | 560 |
| `data\VillagerProfessionRegistry.java` | 306 |
| `navigation\VillagerNavigationService.java` | 318 |
| `trade\TradeEngine.java` | 168 |
| `trade\TradeMatcher.java` | 149 |
| `logistics\UnloadService.java` | 187 |
| `logistics\SupplyService.java` | 170 |
| `data\VillagerTradeTarget.java` | 78 |
| `render\ContainerESP.java` | 82 |
| `logistics\PipelineTask.java` | 68 |
| `trade\EnchantmentMatcher.java` | 99 |
| `recognition\TradeRecognizer.java` | 20 |
| `recognition\TradeResult.java` | 28 |
| `adapter\VillagerTradeAdapter.java` | 20 |
| **合计** | **4,309**（16 文件） |

### 2.2 模块入口与三种模式（`AutoVillagerTradeModule.java`）

| 项 | 事实 | 行号 |
|---|---|---|
| 声明 | `public class AutoVillagerTradeModule extends YiyiaddonModule` | `:65` |
| 构造标题/描述 | `super(AddonTemplate.CATEGORY_AUTOMATION, "自动村民交易", "自动与村民交易，支持原地和寻路模式。点击按钮查看说明。")` | `:311` |
| 职业枚举 | `ProfessionChoice` 13 值（盔甲匠…武器匠，值 = 注册表 ID，如 `minecraft:armorer`） | `:71-95` |
| 运行模式 | `Mode { LOCAL 原地交易, SINGLE_PATH 寻路单点, PIPELINE 多任务 }`（`toString()` 返回中文） | `:666-681` |
| 事件订阅 | `TickEvent.Pre`（快速停止键 / 异常处理 / `fsm.tick()`）`:457-482`；`Render3DEvent`（ContainerESP）`:435-439`；`OpenScreenEvent`（静默容器，放行背包 + **创造模式背包**，防止 RecipeBook NPE 闪退——注释 `:442-455`） | — |
| 完成自动关闭 | `fsm.setCompleteHandler/setErrorHandler` → 停播报（`chatFeedback=false`）→ `toggle()` → 恢复（`:338-351`） | — |
| 自检 | 6 类缺项：职业 / 目标物品（多任务校验整个队列）/ 绿宝石箱 / 成品交易箱 / 半径内村民存在（原地 3.2 格、寻路按搜索范围）/ Baritone 可用（`:488-554`，逐字文案见 §2.6） | — |
| 启动报告 | `announceStartup`：榨干模式声明、多任务逐任务列职业/目标/价格/总量、单职业五行、原地模式两条 ⚠、挂机循环一条（`:386-428` 逐字全文保留） | — |
| 模块页 | `§e查看使用说明` 按钮 + 多任务说明一行（`§7多任务模式 = 依次执行…`）+ 两张点位卡（绿宝石箱 `§a` / 成品交易箱 `§6`，结构同自动农场六卡片：坐标/维度/`§8暂未绑定`/`§8-`/设置/删除）（`:687-826`） | — |
| 使用说明 | 7 章节：`模块定位 / 三种模式 / 默认榨干 §7(已内置，无需开关) / 使用流程 / 点位设置 §7(三种模式通用) / 状态反馈 / 注意事项`（`:721-765` 逐字全文） | — |

### 2.3 状态机（`VillagerTradeFSM.java`，1,221 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 协议要点（类注释原文） | 服务端只接受 `containerMenu == MerchantMenu` 时的 SelectTrade 包 → 必须真实打开界面；成功信号 = offer uses 变化或绿宝石减少；渲染线程 tick 驱动禁止 sleep | `:40-54` |
| 18 态 | `IDLE / SEARCHING / RESOLVING_WORKSTATION / NAVIGATING / OPENING_MENU / TRADING / CLOSING_MENU / SUPPLY_NAV / SUPPLY_OPEN / SUPPLY_TAKE / UNLOAD_NAV / UNLOAD_OPEN / UNLOAD_TAKE / WAITING_RESTOCK / WAITING_PLAYER / NEXT_TASK / DONE / ERROR` | `:1178-1198` |
| 三模式差异（类注释原文） | LOCAL 只交易不寻路；SINGLE_PATH 寻路+自动补给卸货；PIPELINE 队列逐任务（每任务 ≈ 一次 SINGLE_PATH） | `:40-47` |
| 常量 | `SEARCH_TIMEOUT=600` / `NAV_TIMEOUT=2400` / `OPEN_TIMEOUT=100` / `CLOSE_TIMEOUT=60` / `TRADE_IDLE_TIMEOUT=200` / `CONFIRM_TICKS=20` / `MAX_CONFIRM_FAILS=2` / `INTERACT_RANGE=3.2` | `:123-130` |
| 运行参数 | `sendCooldownTicks=2` / `emeraldThreshold=32` / `supplyStacks=1` / `targetQuantity=64`（榨干不参与退出判定）/ `searchRange=96` / `idleLoop=false` / `restockWaitTicks=2400` | `:76-83` |
| tick 分派 | `switch (currentState)` 逐态一个 `tickXxx()`（18 个方法入口行号：`:332/:435/:484/:573/:744/:804/:833/:853/:884/:913/:933/:975/:1001/:1040` 等） | `:305-323` |
| 交易协议实现 | `TradeEngine.executeTrade`：① `menu.setSelectionHint(offerIndex)` + `tryMoveItems` + `sendSelectTrade`（`ServerboundSelectTradePacket`）② `handleContainerInput(containerId, 2, 0, QUICK_MOVE)` shift 点结果槽循环买满（`TradeEngine.java:77-117` 逐字） | — |
| 提示音 | `mc.player.playSound(SoundEvents.VILLAGER_TRADE, 1.0F, 1.0F)`（每笔成功） | `:711` |
| logger | `setLogger(Consumer<String>)` 透传给补给/卸货服务；全 FSM 35 处 `log()` 播报 | `:139-143` |
| **拆分依据（第 38 条）** | 超 1,000 行，必须先拆再迁。建议拆分：`TradeSession`（交易会话：SELECT/CONFIRM/重试/skip 索引与 uses 快照）+ `SupplyUnloadRunner`（补给/卸货六态衔接，包 `SupplyService`/`UnloadService` 驱动）+ `PipelineQueue`（多任务队列与 NEXT_TASK 推进）+ `VillagerTradeFSM`（状态调度与守卫）。拆分依据是职责数量，拆完语义零变化 | — |

### 2.4 设置项清单（`AutoVillagerTradeModule`，分组逐字）

| 分组（逐字） | 项 | 类型/默认/域 | 行号 |
|---|---|---|---|
| 默认组 | `运行模式` | Enum LOCAL/描述「选择交易模式」 | `:114-119` |
| 默认组 | `绿宝石补给量(组)` | Int 1，域 1~27，无滑条；描述「…补到『底限32个 + 该组数×64』即返回交易」 | `:121-129` |
| 默认组 | `搜索范围(格)` | Int 96，域 8~256，无滑条 | `:131-139` |
| 默认组 | `挂机循环(按钮)` | Bool false | `:141-146` |
| 默认组 | `补货等待(秒)` | Int 120，域 20~600 | `:148-156` |
| 默认组 | `目标职业` | Enum ProfessionChoice（默认图书管理员；`onChanged` 更新可见性；多任务模式隐藏） | `:158-165` |
| 默认组 | `快速停止键` | Keybind（`Keybind.none()` 默认） | `:274-279` |
| `目标物品` | `<职业名>交易` ×13 | ItemListSetting（filter = 该职业可购买白名单；可见性随模式/职业联动） | `:217-230` |
| `目标物品` | `图书管理员附魔书` | EnchantmentListSetting（Meteor 注册表多选弹窗；描述「只选择村民能够刷出的附魔类型，交易时忽略附魔等级」） | `:233-241` |
| `目标物品` | `<职业名> 购买量(组)` ×13 | Int 1，域 1~64，**`.visible(() -> false)` 隐藏**（描述「已改为默认榨干模式，购买量不再生效」） | `:243-254` |
| `价格上限` | `<职业名> 价格上限` ×13 | Int 32，域 1~64，无滑条，可见性联动 | `:256-267` |
| `多任务职业` | `<职业名>` 勾选 ×13 | Bool false，仅多任务模式显示 | `:197-207` |

计数：**可视静态 7 + 动态 53（13 物品选择器 + 13 价格 + 13 隐藏购买量 + 13 多任务勾选 + 1 附魔书）= 60 项**；开工前按第 168 条逐行复核计数。

### 2.5 `.cunmin` 指令（`CunminCommand.java`，560 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 注册 | `super("cunmin", "村民交易点位绑定（绿宝石箱、成品交易箱）")`，**无别名** | `:48-50` |
| 子命令 | 裸指令/`状态`（showStatus）、`设置 <绿宝石箱/成品交易箱>`、`移除 <同>`、`清空` | `:52-86` |
| 绑定校验 | 不在世界 `§c玩家或世界无效`；非方块 `§c请将准星对准容器方块`；非容器 `§c目标方块不是有效的容器（箱子/木桶/潜影盒）` | `:105-125` |
| 成功回执 | `已设置<名>` / `已删除<名>` / `已清空全部绑定`（`范围` = `绿宝石箱 + 成品交易箱`）+ `坐标绑定状态`（未绑定 `§8未绑定`，已绑定 `X/Y/Z §8▸ 维度`）——全部走统一排版器 | `:135-207` |
| 静态 API | `getEmeraldChestPos/UnloadChestPos/…Dimension`（模块自检与卡片用）、`setBinding(key)`（GUI 路径文案：`§c准星未对准任何方块，请重新设置` / `§c目标方块不是容器…请重新设置`）、`removeBinding(key)`（`§c§l✗ 已删除 X 绑定` / `§c该坐标本来就没有绑定`） | `:472-540` |
| 持久化 | `config/yiyiaddon/cunmin/<服务器>.json`（服务器键 = `ip.replaceAll("[^a-zA-Z0-9._-]", "_")`）；**手写逐行 JSON 解析/保存**；维度值只规范化三种（`minecraft:overworld/the_nether/the_end`，`toDimensionId :418-424`） | `:237-347` |

### 2.6 自检缺项文案（逐字，模块 `:488-554`）

`§e目标职业§f·未选择`、`§e多任务§f·未勾选任何职业（请在「多任务职业」组勾选并选好物品）`、`§e目标物品§f·未选择`、`§a绿宝石箱§f·未绑定`、`§b成品交易箱§f·未绑定`、`§e<职业>村民§f·<半径> 格内未找到[，请靠近村民（原地模式需站在村民旁约 3 格）]`、`§cBaritone§f·未安装或未启用`。

### 2.7 死代码（零调用验证）

`adapter\VillagerTradeAdapter`（20）+ `recognition\TradeRecognizer`（20）+ `recognition\TradeResult`（28）= **3 文件 68 行**。全项目搜索仅这三个文件自身命中（与 villager 主逻辑零引用；autofarm 的同形态死代码已按 D4 拍板不搬）。→ **D1 拍板是否沿用同一口径。**

### 2.8 渲染（`ContainerESP.java`，82 行）

两箱线框 + 字牌：绿宝石箱（绿）/ 成品交易箱（青），`ShapeMode.Lines`，仅在模块启用且绑定存在时画（`render :31` → `renderLabel :28`）。→ **D6 落地方案：`WorldOverlay` 注册一层，`EspRenderer.blockBox(Lines) + text` 直接复用，文案逐字。**

## 三、底座就绪度（新项目已具备，全部只复用不重造）

| 旧依赖 | 新项目落点 | 状态 |
|---|---|---|
| `ContainerBroker` / `FarmPacketOps` | `service/container/ContainerService` + `platform/container/ContainerAccess`；`platform/network/BlockPacketSender`（SupplyService/UnloadService 的开箱取放与农场/挖矿同底座） | ✅ 已有（阶段05/13 验证） |
| 交易协议（`MerchantMenu` / `ServerboundSelectTradePacket` / `ContainerInput.QUICK_MOVE`） | 原版 API，Mojang 映射同版本直用；`TradeEngine` 属业务层照搬 | ✅ 无需新基建 |
| Baritone 寻路 + 卡死检测 | `platform/navigation/FarmNav` 原语（goTo/pathing/cancel/arrived）+ 卡死检测属业务逻辑留模块内（`navigation/`） | ✅ 原语已有 |
| `WorldOverlay` / `EspRenderer`（ESP） | `ui/render/world/`（第 147 条既定路线，**用户已明确 ESP 复用新项目成熟基建**） | ✅ 已有 |
| `SelectorScreen`（13 物品选择器 + 附魔书多选） | `ui/screen/SelectorScreen` 多选构造 + `ItemIconCache` 行图标（D9 同口径；候选集 = `VillagerProfessionRegistry` 白名单，D-13-04 同款） | ✅ 已有 |
| `CommandMessageFormatter`（指令/播报排版） | `core/CommandMessageFormatter`（同一排版器，前缀规则第 110-113 条） | ✅ 已有 |
| 控制台 / 点位卡 / 日志页 | `ui/console/ConsoleWidgets` + `PointCardGrid` + 星露谷「日志」页参照口径 | ✅ 已有（第 181 条） |
| 按服务器分文件持久化 | `repository/JsonFileStore`（第 45 条统一抽象）+ `MiningPointStore` 按服务器目录先例 + `ResourcePackCache.canonicalKey`（第 45 条服务器键规范） | ✅ 已有 |
| 模块快捷键（快速停止键） | `core/module/ModuleKeybinds`（模块快捷键体系；**无独立 Keybind 设置控件**） | ⚠ 需拍板 D4 |

## 四、缺口与风险

| # | 风险 | 处置 |
|---|---|---|
| R1 | FSM 1,221 行超限 | 按第 38 条先拆后迁（§2.3 末拆分方案），拆完逐段与旧源码比对 |
| R8 | 导航 `disabled` 语义：旧导航服务任一异常即永久停用，`FarmNav` 是每次独立判断 | 模块内导航服务改用 FarmNav 原语口径（失败返回 false 由 FSM 既有失败路径兜底），登记差异；56 号附录A §四 |
| R2 | 旧维度键只认三维度（自定义维度塌缩主世界，`CunminCommand :418-424`） | 与第 45 条「维度键统一 `dimension().identifier().toString()`」冲突 → **D2 拍板**（旧档兼容 vs 统一口径，禁止静默改） |
| R3 | 13 职业 × 3 类设置 + 模式联动的可见性在自研壳无 `visible()` 原语 | 控制台按 D2 范式组织（页签 + 折叠分组，第 195/196 条），方案随 D3 拍板 |
| R4 | 附魔书 `EnchantmentListSetting`（Meteor 注册表弹窗） | `SelectorScreen` 多选（候选 = `LIBRARIAN_ENCHANTMENTS` 池 + 图标 + 搜索），行为等价 |
| R5 | 旧手写 JSON 逐行解析（脆弱，末地维度注释即为例证） | 迁移到 `JsonFileStore`（第 45 条），**旧存档文件读回兼容**必须保留（键名 `emerald_chest/unload_chest` 不变） |
| R6 | `chatFeedback` 抑制自动关闭时的重复播报 | 新壳已有 `Module.suppressEnableAnnounce()` 与 ModuleManager 开关播报机制，对接方式在施工时按 `AutoMinerModule` 同款处理 |
| R7 | 静默容器需额外放行 `CreativeModeInventoryScreen`（NPE 防护，旧注释 `:442-455`） | 类名等价判定里加白名单（与挖矿同做法），注释原样保留 |

## 五、待拍板（D1~D6，拍板前不开工）

| # | 问题 | 建议 |
|---|---|---|
| **D1** | 死代码 `adapter/` + `recognition/` 3 文件 68 行（零调用）是否沿用 autofarm D4 口径不搬 | 不搬并登记 |
| **D2** | 锚点维度键：沿用旧三值口径（overworld/nether/end，读旧档零迁移）还是按第 45 条统一全维度 ID（需双读迁移） | 沿用旧口径（模块数据文件非设置键，第 45 条约束的是新写盘结构；但旧档兼容优先）——**用户裁定** |
| **D3** | 13 职业设置组织：控制台是否照「逐作物」先例——按职业折叠分组（每组：物品选择器入口行 + 价格上限行 + 多任务勾选行），附魔书单独一行；可见性联动改为「按当前模式显示对应组」 | 照逐作物先例 |
| **D4** | `快速停止键`：新壳无独立键位设置控件——落 `ModuleKeybinds`（模块快捷键体系，与其它模块同款）还是登记留白 | 落 ModuleKeybinds |
| **D5** | 模块页点位卡（绿宝石箱/成品交易箱两张）+ 控制台（第 181 条移植模块一律控制台）——控制台页签建议：概览 / 设置（模式+补给+职业组）/ 点位 / 日志 | 照建议 |
| **D6** | 购买量(组) 13 个隐藏设置（描述已写「不再生效」）不落设置项，FSM 参数按默认 1×64 传入（榨干模式不参与退出判定，行为不变） | 照此留白 |

## 六、施工方案（复用声明 + 批次）

**复用清单（用户已点名「成熟代码直接用」，零自造壳件）：**
ESP → `WorldOverlay` + `EspRenderer`；选择器 → `SelectorScreen`（+`ItemIconCache` 图标）；排版 → `CommandMessageFormatter`；控制台骨架与行构件 → `ConsoleWidgets` + `TooltipLayer`；点位卡 → `PointCardGrid`（含 D9 图标扩展）；容器 → `ContainerService`/`ContainerAccess`；发包 → `BlockPacketSender`；导航原语 → `FarmNav`；持久化 → `JsonFileStore`（+`canonicalKey` 服务器键）；帮助页 → `HelpPanelScreen`；自检弹窗 → 运行时 `ConfirmPanelScreen.notice`（第 176-179 条）；日志页 → 星露谷参照口径（容量 80 / 刷新清空按钮）。**新增文件只允许出现在 `feature/villager/<八类>/` 模块包内。**

- **批次 1**：本侦察报告落库 + D1~D6 拍板 + 附A 开工指令。
- **批次 2 · 逻辑层**：`VillagerProfessionRegistry`（数据表照搬）→ `VillagerTradeTarget`/`PipelineTask`/`TradeMatcher`/`EnchantmentMatcher`/`TradeEngine` → `SupplyService`/`UnloadService`（容器底座换名）→ `VillagerNavigationService`（业务留模块内、原语走 FarmNav）→ FSM 按 §2.3 拆分迁移 → `VillagerBindingStore`（JsonFileStore，键名与旧档兼容 per D2）。
- **批次 3 · 外围**：模块入口（三模式配置/启动报告逐字/静默容器含创造背包放行/提示音）→ `.cunmin` 指令（回执逐字）→ 模块页与两卡 → 控制台（D5 页签）→ 13 职业组（D3）→ ESP 层 → 注册 `AddonModules`（分类 `automation`）。
- **批次 4 · 收口**：编译 + 逐字比对 + 实机（交易所场景：原地/寻路/多任务 + 补给卸货 + 榨干循环）+ 总差异清单报告。

## 七、验收清单（待实机，与旧项目逐条对齐）

1. `.cunmin` 全子命令回执逐字（含 `已设置/已删除/已清空` 与状态两字段）。
2. 自检 7 类缺项文案逐字 + 弹窗（第 176-179 条双通道）。
3. 启动报告逐字（多任务逐任务块 / 原地两条 ⚠ / 挂机循环行）。
4. 三模式行为差异可见：原地只交易（WAITING_PLAYER 提示手动补给）、寻路自动补给卸货、多任务顺序推进（NEXT_TASK）。
5. 交易协议：真实开界面 → SelectTrade → QUICK_MOVE 买满；成功信号与 `CONFIRM_TICKS=20` 重发上限 1 次行为一致；每笔成功播报 + `VILLAGER_TRADE` 提示音。
6. 附魔书忽略等级匹配（`EnchantmentMatcher`）与图书管理员附魔池过滤。
7. 榨干模式：售罄/锁死才收工；挂机循环按 `补货等待(秒)` 倒计时重循环。
8. 静默容器：交易中村民界面不显示；背包与创造背包放行。
9. 绑定存档跨服务器隔离 + 重启生效（第 172 条）。
10. ESP 两箱线框与字牌（绿/青）随绑定显隐。

## 八、证据与命令

```powershell
# 逐文件行数（2.1 表即此命令输出）
$root = 'd:\mcaddon\26.1.2\src\main\java\com\example\addon\villager'
Get-ChildItem $root -Recurse -Filter *.java -File | Sort-Object FullName | ForEach-Object {
  '{0,5}  {1}' -f (Get-Content $_.FullName).Count, $_.FullName.Replace("$root\",'') }

# 死代码零调用（D1 依据）
#   范围：d:\mcaddon\26.1.2\src  关键词：TradeRecognizer | TradeResult | VillagerTradeAdapter
#   结论：仅 villager\adapter 与 villager\recognition 自身命中

# 状态机 18 态与常量：fsm\VillagerTradeFSM.java:1178-1198 / :123-130
# 交易协议：trade\TradeEngine.java:77-117（SelectTrade + QUICK_MOVE 两步）
# 提示音：fsm\VillagerTradeFSM.java:711
```

---

## 项目迁移路线图

已完成：√ 阶段01~07 √ 阶段08 自动箱子 √ 阶段09 星露谷 √ 阶段10 自动挖矿 √ 阶段11 自动附魔 √ 阶段12 管理员检测 √ 阶段13 自动农场（批次1~3 已落位，批次4 实机待执行）
当前：→ 阶段14 村民交易（本报告侦察完成，**D1~D6 待拍板**，拍板后出附A开工）
下一步：→ 阶段14 批次2 逻辑层 → 批次3 外围 → 批次4 收口
未来：→ 图书管理员 → 传送 → 自动登入 → 战术 → 用户统计 → 骨粉 → 断线 → Baritone/Meteor 引导 → 全量实机验收与 Mixin 对账
