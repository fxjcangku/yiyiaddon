# 55 · 附A · 新窗口开工指令（复制粘贴即用）

日期：2026-09-17
用途：**新开对话窗口时，把下面整段发给智能体**。它替代「你上次说过什么」的口头交代——上下文、任务、硬约束、开工前要办的事全在里面。
维护：拍板或范围有变动时**改本文件**，不要靠对话记忆传递。

---

```text
项目：d:\mcaddon\yiyiaddon（Minecraft 26.1.2 Fabric 自研客户端模组，Skija 自研壳，禁止依赖 Meteor 框架）
旧项目：d:\mcaddon\26.1.2（严格只读，任何情况下不得修改）
今天：2026-09-17

【第一步：连接上下文，按顺序读，不要跳】
1. 开发习惯.md（项目根目录，规则正本，最高优先级；含第三十一章第 207/208 条新铁律）
2. 02-开发报告/项目开发报告/00-AI阅读指引.md（导航与口径）
3. 同目录 01-总纲-迁移口径与复刻原则.md
4. 第 1 层六份基准（迁移时逐字对齐的唯一依据，禁止凭印象改写旧项目原文）：
   02-基准-01-状态播报体系 / 03-基准-02-自检启动机制 / 04-基准-03-配置页按钮与独立窗口清单 /
   05-基准-04-关联代码依赖网 / 06-基准-05-逐模块依赖全量清单 / 07-基准-06-指令体系全量清单
5. 30-移植总览-已移植与未移植全清单.md（推进顺序与逐包清点）
6. 阶段14 本轮开工依据（村民交易）：
   55-阶段14-村民交易移植侦察与决策.md（主报告，大扫描终版）+
   56-阶段14-附录A-逻辑层全量侦察记录.md（18 态逐态行为 / 38 处播报逐字 / 服务层 / 导航 / 交易协议判据）
   ——侦察已全部完成，不要重复侦察、不要重写报告
7. 照搬这几套已验证的做法（只换 UI 外壳、功能与文案逐字）：
   42+43（阶段10 挖矿：控制台/ESP/指令）与 45+46（阶段11 附魔：多选择器/逐项设置）；
   52（阶段13 批次2 底座）与 54（批次3 外围，含两节「状态变更」：自造项审计与交付前自查）+
   57（阶段13 批次2与3 代码自查报告——4 处偏差与修复、文案机对 120/120 与 23/23 的方法学；
   新窗口做任何模块交付前必须照此跑一遍文案机对与自造件审计）

【第二步：开工前第一件事】向用户提交 D1~D6 拍板表（55 号第五节），等用户批复：
  D1 死代码 adapter/+recognition/ 3 文件 68 行不搬（建议：照 autofarm D4 口径，不搬并登记）
  D2 锚点维度键沿用旧三值口径 overworld/nether/end（建议：沿用，旧档零迁移，禁止静默改）
  D3 13 职业设置组织照「逐作物」先例：控制台按职业折叠分组（物品选择器入口 + 价格上限 + 多任务勾选）
  D4 快速停止键落 ModuleKeybinds（模块快捷键体系）
  D5 控制台页签：概览 / 设置 / 点位 / 日志（模块页保留旧元素 + 控制台入口，照自动农场先例）
  D6 购买量 13 个隐藏设置不落设置项（FSM 参数按默认 1×64 传入，榨干模式行为不变）
拍板后把结论回改进 55 号第五节与本文件。

【第三步：任务】阶段14 村民交易移植（旧 com.example.addon.villager 16 文件 4,309 行）
- 搬旧项目除死代码 3 文件外的全部（D1 后定稿），模块形态三模式：
  LOCAL 原地交易（只自动交易，补给卸货玩家手动，WAITING_PLAYER 恢复机制）/
  SINGLE_PATH 寻路单点（Baritone 寻路 + 绿宝石箱补给 + 成品箱卸货）/
  PIPELINE 多任务（勾选职业组成队列，NEXT_TASK 顺序推进，可为不同村民）。
- 交易协议（必须逐字保留，56 号附录A §3.1）：
  真实打开村民界面 → menu.setSelectionHint + tryMoveItems + ServerboundSelectTradePacket →
  handleContainerInput QUICK_MOVE 结果槽（服务端循环买满）；
  成功信号只认 offer uses 递增（绿宝石减少是假阳性，SelectTrade 会挪付款物品）；
  CONFIRM_TICKS=20、MAX_CONFIRM_FAILS=2（重发 1 次）、findBestOfferIndex 轮换 =
  已购最少优先 + 同物品最便宜；getEmeraldCost 用 getCostA/getCostB（含 demand 涨价），
  禁止 getBaseCostA；客户端只能读 MerchantMenu offers，禁止 Villager.getOffers()。
- 状态名与常量逐字：18 态（FSM :1178-1198）、SEARCH_TIMEOUT=600 / NAV_TIMEOUT=2400 /
  OPEN_TIMEOUT=100 / CLOSE_TIMEOUT=60 / TRADE_IDLE_TIMEOUT=200 / INTERACT_RANGE=3.2；
  FSM 超 1,000 行，按第 38 条先拆后迁（拆分方案见 55 号 §2.3 末），拆完逐段与旧源码比对。
- FSM 全部 38 处播报逐字（56 号附录A §1.2 内联原文），保留 §8[<模式>]§r 模式前缀包装机制；
  每笔成功播报 + SoundEvents.VILLAGER_TRADE 提示音（FSM :711）照搬。
- 模块层资产逐字：模块名/描述、启动报告 announceStartup 全文、自检 7 类缺项文案、
  使用说明 7 章节、模块页（说明按钮 + 多任务说明行 + 两张点位卡）——行号见 55 号 §2.2/2.6。
- 指令 .cunmin（无别名）：设置/移除/状态/清空，两锚点 绿宝石箱/成品交易箱，回执逐字
  （55 号 §2.5）；GUI 静态路径与指令路径两套文案并存（旧项目现状，禁止合并）。
- 持久化：config/yiyiaddon/cunmin/<服务器>.json 旧档必须能读回（键 emerald_chest/unload_chest
  不变）；新写盘走 JsonFileStore + ResourcePackCache.canonicalKey（第 45 条已定规范）。
- ESP：ContainerESP 两箱线框+字牌 → 直接复用新项目 WorldOverlay + EspRenderer（成熟基建）。

【第四步：三条硬约束】
1. 底座只复用不重造（55 号第三节全表）：
   容器 → service/container/ContainerService + platform/container/ContainerAccess
   发包 → platform/network/BlockPacketSender；导航原语 → platform/navigation/FarmNav
   （模块内导航服务留 feature/villager/navigation/，卡死检测是业务逻辑；
    旧 disabled 永久停用语义改 FarmNav 口径，登记差异 R8）
   选择器 → ui/screen/SelectorScreen（13 物品选择器 + 附魔书多选，每行带图标 D9）
   ESP → ui/render/world/{WorldOverlay,EspRenderer}；排版 → core/CommandMessageFormatter
   控制台 → ui/console/ConsoleWidgets + TooltipLayer；点位卡 → ui/console/PointCardGrid
   帮助页 → ui/screen/HelpPanelScreen；自检弹窗 → 运行时 ConfirmPanelScreen.notice（第 176-179 条）
   持久化 → repository/JsonFileStore；日志页 → 星露谷参照口径（容量 80 / 刷新清空按钮）
   禁止自造壳件（54 号「状态变更」节是前车之鉴）。
2. 死代码不搬（D1 拍板后执行）：adapter/ 1 文件 + recognition/ 2 文件，旧项目零调用。
   但真实判据必须逐行照搬：VillagerProfession 是 Record 的 Identifier 匹配陷阱（FSM :384-393）、
   幼年村民排除、getCostA demand 口径、附魔书忽略等级（EnchantmentMatcher）、
   渔夫收船不卖船等白名单剔除注释（Registry :66-89）。
3. 禁止新增旧项目没有的功能；界面形态、按钮文案、自检文案、使用说明七章节、
   启动报告、38 处 FSM 播报全部逐字。设置项 7 静态可视 + 13 职业×3 类 + 附魔书 + 13 多任务勾选
   逐项落地，一个不许少、不许改名（名称与描述逐字见 55 号 §2.4）。
   新代码一律 feature/villager/<八类>/ 分包 + 详细中文注释（第 208 条，硬性验收项）；
   API 查不到按第 207 条走 01-开发参考库 官网源码 grep，禁止猜。

【第五步：施工顺序】拍板后按 55 号第六节批次推进：
批次 1 ✅ 侦察记录落库（55 主报告 + 56 附录A）；
批次 2 逻辑层：VillagerProfessionRegistry（数据表照搬）→ VillagerTradeTarget/PipelineTask/
       TradeMatcher/EnchantmentMatcher/TradeEngine → SupplyService/UnloadService（容器底座换名）→
       VillagerNavigationService（模块内，原语走 FarmNav）→ FSM 拆分迁移 →
       VillagerBindingStore（JsonFileStore + 旧档兼容，D2 拍板后定稿）；
批次 3 外围：模块入口（三模式/启动报告/静默容器含创造背包放行/提示音）→ .cunmin 指令 →
       模块页与两卡 → 控制台（D5 页签）→ 13 职业组（D3）→ ESP 层 → AddonModules 注册（分类 automation）；
批次 4 收口：编译 + 逐字比对 + 实机（交易所：原地/寻路/多任务 + 补给卸货 + 榨干循环）+ 总差异清单报告。
每批做完先编译（./gradlew compileJava --console=plain -q），再往下走；每批结束向用户汇报并写报告。

【第六步：实机验证环境】固定村民交易所（村民提前手动解锁至大师级）：
三种模式各跑一轮；绿宝石不足/背包满触发补给卸货；榨干后挂机循环补货倒计时；
快速停止键终止；绑定点位重启持久化（第 172 条：改值→关客户端→重开→仍在）。

【铁律速查】只换 UI 不换功能与文案；指令名与回执一个字不改；禁止新增旧项目没有的功能；
一切改动先经用户同意；修复前必须取证（行号 / 存档 / 可复现实验）；同源逻辑两边逐字比对；
代码美观 + 详细中文注释（第 208 条，硬性验收项，与编译通过同级）；
API 三步查不到必须继续 grep 参考库官网源码，禁止猜（第 207 条）；
ESP/选择器/排版/控制台/点位卡/持久化全部复用既有成熟件，禁止自造（54 号审计教训）。
编译验证：./gradlew compileJava --console=plain -q
```

---

## 变动记录

| 日期 | 变动 |
| --- | --- |
| 2026-09-17 | 首版：由 55 号（大扫描终版）+ 56 号附录A 合成；开工前第一件事 = 提交 D1~D6 拍板表；硬约束写入复用清单与第 207/208 条 |
