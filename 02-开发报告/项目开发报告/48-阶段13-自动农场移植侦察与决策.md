# 48 · 阶段13 · 自动农场移植侦察与决策

> 本文回答三件事：**旧项目 `autofarm` 里有什么（逐条带行号）**、**落到新项目怎么接（复用什么、新建什么）**、**哪些必须用户拍板**。
> 口径正本：[01-总纲-迁移口径与复刻原则.md](01-总纲-迁移口径与复刻原则.md)（功能全量复刻 + 只换 UI 外壳 + 禁止新增旧项目没有的功能）。
> 上游依据：[30-移植总览-已移植与未移植全清单.md](30-移植总览-已移植与未移植全清单.md) 第五节给的顺序（星露谷 → 自动附魔 → **自动农场** → 自动挖矿 → 村民交易 → 图书管理员 → 传送 → 自动登入 → 战术 → 其余）。
> 同批做法参照：[37-阶段10-自动挖矿移植侦察与决策.md](37-阶段10-自动挖矿移植侦察与决策.md)、[38-阶段11-自动附魔移植侦察与决策.md](38-阶段11-自动附魔移植侦察与决策.md)。
> 施工入口：[48-附A-新窗口开工指令.md](48-附A-新窗口开工指令.md)。

**日期**：2026-09-16　**状态**：侦察完成，第五节 D1~D8 **已全部拍板**（2026-09-16）。

---

## 一、结论摘要（一屏看完）

| 项 | 结论 |
|---|---|
| 旧包 | `com.example.addon.autofarm`，**42 文件 / 5,454 行**（本机 `(Get-Content).Count` 实测，逐文件见 2.1） |
| 连带并入 | `com.example.addon.water\WaterESPModule`（**1 文件 / 366 行**，见 2.10）—— 用户 2026-09-16 拍板连带搬，自动农场模块页的「打开/关闭水源显示」按钮才有落点 |
| 本阶段实际搬运 | **35 文件 / 5,454 行**（autofarm 42 文件扣除死代码 8 文件 366 行，加 water 1 文件 366 行） |
| 模块形态 | **站位农场**：先在世界里框一个矩形农田（两个点位），模块只在框内「熟一颗收一颗 / 自动补种 / 自动锄地 / 自动拾取」，并跑四个箱子的物流（单作物箱 / 多作物箱 / 种子补货箱 / 杂物箱）。**不寻矿、不跨维度、没有远端传送** |
| 界面面 | 旧项目只有**模块页**：使用说明 + 六个点位卡片（设置 / 删除）+ 一个「打开/关闭水源显示」快捷开关（04-基准-03 第 1.2 节）。**本项目拍板：模块页 + 整屏控制台**（用户 2026-09-16，理由：16 静态 + 15 逐作物设置项模块页塞不下；控制台是本项目自研壳，不算新增功能） |
| 指令 | `.farm`（别名 `nongchang`）：设置 / 移除 / 状态 / 清空 / 扩展，回执逐字见 2.5 |
| 状态机 | 8 个**展示用**状态（观察/收割/补种/锄地/拾取/卸货/补货/杂物处理）+ 13 个任务类；**状态只写不读**，真实行为由「当前任务对象」决定（2.3） |
| 设置项 | 5 分组 + **16 个可视静态项 + 6 个隐藏锚点项** + 15 个逐作物动态项（10 卸货 + 5 补货，运行期生成）= **37 项**（2.7；计数已按源码逐条复核，见变动记录） |
| 死代码 | `adapter/` 2 文件 + `recognition/` 6 文件 = **8 文件 / 366 行，旧项目全量搜索零实例化、零调用**（2.6）→ **已拍板不搬并登记** |
| 底座就绪度 | 容器（`ContainerService` / `ContainerAccess`）、发包（`BlockPacketSender`，含本地置空气 + 预测编号）、Baritone 隔离层（`feature/stardew/navigation/FarmNav`，与旧 `autofarm/navigation/FarmNav` **逐行等价**）**全部已在新项目存在**（第三节） |
| 缺口 | ① 原版作物识别（`CropBlock.AGE` / `NetherWartBlock` / `CocoaBlock` / 竹子甘蔗顶格 / `HoeItem`）在新项目**零命中**，必须新写；② 时运工具选择（`FortuneTool`，读 `DataComponents.ENCHANTMENTS`）需落新壳；③ `BlockPacketSender` 少一个旧 `FarmPacketOps.getRemainingDurability`；④ 逐作物 15 个动态设置项在本项目新壳里的承载方式（已拍板：独立窗口 + 按作物折叠，见第五节 D3） |
| 拍板 | D1~D8 **已全部拍板**（2026-09-16），逐条见第五节 |

---

## 二、旧项目事实（只读扫描，2026-09-16 实测）

### 2.1 文件与规模（逐文件行数，`(Get-Content).Count` 实测）

| 子包 | 文件 | 行数 |
|---|---|---|
| （根）`AutoFarmMatrix.java` | 1 | 854 |
| `adapter/` | 2 | 99 |
| `command/` | 1 | 350 |
| `controller/` | 8 | 1,166 |
| `model/` | 7 | 475 |
| `navigation/` | 1 | 168 |
| `recognition/` | 6 | 267 |
| `render/` | 1 | 67 |
| `resource/` | 1 | 263 |
| `scan/` | 1 | 309 |
| `task/` | 13 | 1,436 |
| **合计** | **42** | **5,454** |

> **与 30 号报告的口径差异（如实登记）**：[30 号](30-移植总览-已移植与未移植全清单.md) 第三节写 `autofarm` = 42 文件 / **4,685 行**。本文按 37 号约定改用 `(Get-Content).Count` 逐文件实测，得 **5,454 行**；差 769 行来自统计方法/时点不同（30 号成文于 2026-09-14）。以本文实测为准。
>
> 逐文件行数：`AutoFarmMatrix` 854、`adapter\DefaultFarmAdapter` 49、`adapter\FarmServerAdapter` 50、`command\NongChangCommand` 350、`controller\BatchHarvestPlan` 36、`BatchPlantPlan` 34、`BatchTillPlan` 33、`FarmController` 539、`FarmDecision` 315、`FarmObserver` 35、`FarmSelfCheck` 103、`FarmVerifier` 73、`model\CropProfile` 213、`FarmSite` 73、`FarmState` 48、`FarmTarget` 35、`HarvestMode` 30、`PlantMode` 36、`SiteType` 40、`navigation\FarmNav` 168、`recognition\CropRecognitionResult` 27、`CropRecognizer` 21、`MaturityRecognizer` 21、`MaturityResult` 25、`SoilRecognizer` 30、`VanillaFarmRecognizer` 143、`render\FarmRenderer` 67、`resource\FarmResourceManager` 263、`scan\FarmScanner` 309、`task\BatchHarvestTask` 164、`BatchPlantTask` 196、`CollectTask` 166、`ContainerTask` 102、`FarmTask` 19、`FortuneTool` 127、`HarvestTask` 114、`PlantTask` 133、`PoisonDumpTask` 51、`RestockTask` 78、`TaskResult` 71、`TillTask` 160、`UnloadTask` 53。

### 2.2 模块形态：站位农场内的「熟一颗收一颗 + 四箱物流」

- 模块入口：`public final class AutoFarmMatrix extends YiyiaddonModule`（`AutoFarmMatrix.java:68`）。
- 构造器标题与描述逐字：`super(AddonTemplate.CATEGORY_AUTOMATION, "自动农场", "熟一颗收一颗，自动补种拾取，单作物箱/种子补货箱/多作物箱与杂物箱物流自动化。点击按钮查看说明。")`（`AutoFarmMatrix.java:133-134`）。
- 农田范围 = 两个点位（`START` / `END`）框出的对角矩形，交给 `FarmScanner.setBounds`：**Y 轴向下多 1 格、向上多 3 格**（`FarmScanner.java:64-65`），分帧扫描 **每 tick 512 格**（`FarmScanner.java:31`、`:205-209`），激活时先跑一次无预算全量扫描 `fullScan()`（`FarmScanner.java:217-234`，调用点 `AutoFarmMatrix.java:333`）。
- 四个箱子各有专属语义（`FarmSelfCheck.java:47-80`）：
  - **单作物箱**：`!needsReplant() || plantItem == harvestItem` 的作物产物（如马铃薯、下界疣）
  - **多作物箱**：双物品作物（种子与产物分离，如小麦、甜菜根）的产物
  - **种子补货箱**：双物品作物的种子来源，补货时从这里取，卸货时始终保留安全库存
  - **杂物箱**：毒马铃薯 + `junk()` 作物（当前仅仙人掌花，`CropProfile.java:113-115`），独立处理、不混进普通箱（`PoisonDumpTask.java:12-13`）
- 事件订阅 4 个：`TickEvent.Post`（`AutoFarmMatrix.java:440-441`）、`Render3DEvent`（`:483-484`）、`Render2DEvent`（`:504-505`）、`GameLeftEvent`（`:522-523`，出世界即 `toggle()`）、`OpenScreenEvent`（`:527-528`，物流任务运行中拦截容器界面，见 `:533-539`）。
- 防踩踏：农田范围内关掉跳跃键（`AutoFarmMatrix.java:445-449`）。

### 2.3 状态与任务：8 个展示状态 + 13 个任务类（状态只写不读）

`FarmState` 8 个取值（逐字）：`OBSERVE 观察`（`FarmState.java:15`）、`HARVEST 收割`（`:18`）、`PLANT 补种`（`:21`）、`TILL 锄地`（`:24`）、`COLLECT 拾取`（`:27`）、`UNLOAD 卸货`（`:30`）、`RESTOCK 补货`（`:33`）、`POISON_DUMP 杂物处理`（`:36`）。

**关键事实**：`FarmController` 内**没有任何 `if/switch (state)` 分支**（state 只经 `setState` 写入 `:512-514`、`state()` 读出 `:109-111`）。状态名只用于状态条播报（`AutoFarmMatrix.java:463` → `broadcastState`），**真实行为完全由 `currentTask` 的类型与 `FarmDecision` 的决策决定**。搬的时候状态名要逐字保留（用户可见），但不必把行为挂到状态上。

任务契约（`FarmTask.java`，接口，3 个方法全抽象）：

| 方法 | 行号 | 语义（逐字注释） |
|---|---|---|
| `TaskResult tick();` | `:12` | 每 tick 推进一次，返回当前结果 |
| `boolean exclusive();` | `:15` | 是否独占任务（卸货/补货/杂物处理期间只观察、不抢任务） |
| `void cancel();` | `:18` | 取消并清理副作用（关容器、取消导航、恢复临时手持） |

`TaskResult` 17 个取值（`TaskResult.java:12-60`）：`IN_PROGRESS` / `SUCCESS` / `WAVE_DONE` / `TARGET_INVALID` / `HARVEST_FAILED` / `PLANT_FAILED` / `RESOURCE_INSUFFICIENT` / `CONTAINER_MISSING` / `CONTAINER_FULL` / `CONTAINER_EMPTY` / `CONTAINER_SYNC_FAILED` / `CONTAINER_OPEN_FAILED` / `NAVIGATION_FAILED` / `POISON_CONTAINER_FULL` / `DIMENSION_MISMATCH` / `FARM_AREA_INVALID` / `CANCELLED`；`done()` = `this != IN_PROGRESS`（`:63-65`）、`ok()` = `this == SUCCESS`（`:68-70`）。
其中 `CONTAINER_SYNC_FAILED`、`CONTAINER_OPEN_FAILED`、`DIMENSION_MISMATCH`、`FARM_AREA_INVALID`、`CANCELLED` **全项目无任何返回点**（`task/` 目录全量搜索），属旧项目预留枚举。

13 个任务类逐个（全部带关键常量）：

| 任务 | 行数 | 关键常量 | 完成判据 | 备注 |
|---|---|---|---|---|
| `ContainerTask`（抽象基类） | 102 | `OPEN_RETRY_INTERVAL = 10`（`:21`） | 抽象 `transfer()`（`:70`） | `tick()` 为 **final**（`:36`）；到达判据 = 切比雪夫邻域 ≤1（`:84-93`）；开箱包每 10 tick 重试一次（`:95-101`）；**注释明确已放弃 `isBoundContainer` 引用相等校验**（`:62-65`） |
| `UnloadTask` | 53 | — | `broker.depositOne(filter)` 循环 ≤ `bpt`（`:34-41`） | 箱满 → `CONTAINER_FULL`，否则 `SUCCESS`（`:49-51`） |
| `RestockTask` | 78 | `syncCooldown = 2`（`:25`） | `countItem(plantItem) >= safetyStock(crop)`（`:50-54`） | 空箱 → `CONTAINER_EMPTY`（`:63-67`）；`countInChest`（`:73-77`） |
| `PoisonDumpTask` | 51 | — | 同 `UnloadTask` | 箱满 → `POISON_CONTAINER_FULL`（`:45-49`） |
| `HarvestTask` | 114 | `WAIT_TICKS = 2`、`MAX_RETRIES = 2`（`:19`/`:21`） | `verifier.harvestSucceeded`（`:83`） | 只在未破坏时校验目标有效性（`:49-52`）；`FortuneTool` 持时运工具（`:63-66`） |
| `BatchHarvestTask` | 164 | `WAIT_TICKS = 2`、`MAX_RETRIES = 2`、**`WAVE_SIZE = 32`**（`:24`/`:26`/`:28`） | 分级 `SUCCESS` / `WAVE_DONE`（`:139`/`:143`） | **「瞬间暴力破坏」**：同一 tick 内最多 32 个目标连续发破坏包（`:109-115`），靠 `FarmPacketOps.breakBlock` 发包后本地立刻置空气（`FarmPacketOps.java:63-72`）；`drainHarvested()` 供上层补种+拾取（`:53-57`） |
| `PlantTask` | 133 | `WAIT_TICKS = 2`、`MAX_RETRIES = 2`（`:22`/`:23`） | `verifier.plantSucceeded`（`:81`） | **播种固定走副手**（`:102`/`:110`/`:114`/`:120`）；材料搜索顺序：副手 → 快捷栏 → 背包（`:103-123`） |
| `BatchPlantTask` | 196 | `WAIT_TICKS = 2`、`MAX_RETRIES = 2`、移动种子节流 `> 8` tick（`:26`/`:28`/`:150-159`） | `planted()` 返回内部引用（`:51-53`） | 按 **同一种子**（`Item` 引用相等）组批（`:82-86`）；无种子时返回 `true` 让后续验证自然失败（`:161-163`） |
| `TillTask` | 160 | `WAIT_TICKS = 2`、`MAX_RETRIES = 2`（`:22`/`:23`） | `verifier.tillSucceeded`（`:90`） | **锄地必须主手持锄头**（`:17`）；用 `InvUtils.swap` 并 `swapped` 标记以便 `cancel()` 还原（`:125-132`、`:107-110`）；失败复用 `PLANT_FAILED`（`:98`） |
| `CollectTask` | 166 | `EMPTY_GRACE = 20`、`MAX_TOTAL_TICKS = 400`、`STUCK_TICKS = 50`、`PICKUP_RADIUS_SQ = 2.25`、`DRY_SEARCH_RADIUS = 4`（`:19`-`:27`） | 「连续 20 tick 附近无掉落物」即 `SUCCESS`（`:53-62`） | 卡住 50 tick / 总时长 400 tick 也返回 `SUCCESS`（`:48-51`、`:69-76`）；掉落物在水里时环形找干燥站位（`:144-160`） |
| `FortuneTool`（不是任务） | 127 | `prepTicks > 8` 重发移动（`:53`） | — | 时运工具阶梯：主手有时运 → 快捷栏最高时运 → 背包（含副手）移到选中槽 → 裸手（`:28-69`）；`restore()` 只在交换过时 `swapBack()`（`:72-77`）；时运等级读 `DataComponents.ENCHANTMENTS` + `Registries.ENCHANTMENT`（`:114-126`） |

完成判据集中器 `FarmVerifier`（4 条，全部基于世界真实状态，不看发包回执）：`harvestSucceeded`（`:22-32`）、`plantSucceeded`（`:37-43`）、`tillSucceeded`（`:48-53`）、`targetStillValid`（`:56-72`）。

### 2.4 决策链优先级与阈值（`FarmDecision` 315 行）

`FarmController.tick()` 每 tick 严格按此行序推进（`:118-276`）：

| 序 | 行号 | 动作 | 命中即 return |
|---|---|---|---|
| 1 | `:120` | 世界未就绪 | 是 |
| 2 | `:121-141` | 玩家死亡 → 清空任务与三个计划 + `FarmNav.cancel()` + 回 `OBSERVE` | 是 |
| 3 | `:144-145` | `scanner.tick()`（512 格） + `broker.tick()` | — |
| 4 | `:148-156` | 当前任务 `tick()`，`done()` 则交 `handleTaskResult` | 是 |
| 5 | `:159-170` | **物流**：`decision.decideLogistics(sites)` | 是 |
| 6 | `:173-186` | 消化批量收割计划 `batchPlan` | 是 |
| 7 | `:189-201` | 消化批量补种计划 `batchPlantPlan` | 是 |
| 8 | `:204-216` | 消化批量锄地计划 `batchTillPlan` | 是 |
| 9 | `:219-244` | 锄地（`tillWorkAvailable()` → `selectTillTargets()`，无锄头给提示） | 是 |
| 10 | `:247-258` | 补种（`selectPlantTargets()`，>1 建批量计划） | 是 |
| 11 | `:261-272` | 收割（`selectHarvestTargets()`，`BATCH` 时上限 `Integer.MAX_VALUE`，否则 1，`:176`） | 是 |
| 12 | `:275` | 归位等待 `updateIdleWait()` | — |

物流内部优先级（`FarmDecision.java:109-164`）：**① 杂物**（`countJunk() >= poisonUnloadThreshold` 且杂物箱已绑，`:111-114`）→ **② 补货**（逐作物 `needsRestock`，双物品作物进种子补货箱、其余进单作物箱，`:120-132`）→ **③ 卸货**（总闸：三类可卸 + `freeInventorySlots() <= 2`，`:136-139`；再按 种子/单/多 三类依次，`:141-160`）。

其它阈值与常量（逐字）：`CENTER_WAIT_TIMEOUT = 200` tick（`FarmController.java:76`）、归位到位半径 `1.5`（`:305`）、批量统一拾取半径 `max(max(dx,dz), 2)/2.0 + 1`（`:496`）、`collectRange` 默认 `4`（`:86`）、自检上限 `MAX_CROPS = 3`（`FarmSelfCheck.java:24`）、单物品作物保留量取 `Math.min(safetyStock, unloadStock)`（`FarmResourceManager.java:70`）。

补种模式三分支（`FarmDecision.java:216-290`）：`SEQUENTIAL 顺序优先`（`:224-232`）/ `ROTATE 均匀轮转`（`:235-250`，`rotateCursor = (index+1) % size`）/ `FOLLOW_NEARBY 就近跟随`（`:256-290`，`FOLLOW_RADIUS = 4`，无邻居回落 `SEQUENTIAL`）。

资源判定（`FarmResourceManager` 263 行）：仅两个常量 `DEFAULT_UNLOAD_GROUPS = 8`（`:24`）、`DEFAULT_RESTOCK_GROUPS = 3`（`:26`）；`safetyStock = restockGroups * 64`（`:54-56`）、`unloadStock = unloadGroups * 64`（`:59-61`）。

扫描器三缓存（`FarmScanner.java`）：成熟 `matureBlocks`（`:43`）、可补种 `plantableBlocks`（`:45`）、可锄地 `tillableBlocks`（`:47`）；`classify` 每格三判（`:255-292`，空气格直接清三缓存 `:258-263`）；对外只给按距离排序的最近列表（`:105-158`，每次 `new ArrayList` + 排序，**无上限** → 大农田上是已知开销点）。

### 2.5 六点位与 `.farm` 指令

六点位（`SiteType.java:16-21`，逐字）：`农场点位1` / `农场点位2` / `单作物箱` / `多作物箱` / `种子补货箱` / `杂物箱`；后四个 `requiresContainer() == true`（`:18-21`）。

点位序列化：`FarmSite.serialize()` = `x,y,z,维度id`（`FarmSite.java:25-27`，例 `120,64,-88,minecraft:overworld`），空串 = 未绑定（`UNBOUND`，`:22`）；`describe()` = `§7X§f… §7Y§f… §7Z§f… §8▸ §b<维度摘要>`（`:69-72`，维度名走 `WorldContextFormatter.dimensionSummary`）。存储载体是模块里 6 个 **隐藏** `StringSetting`（`AutoFarmMatrix.java:276-281`、`:291-298`，`.visible(() -> false)`）。

指令 `.farm`（别名 `nongchang`，`NongChangCommand.java:42`）：裸指令 = 锚点绑定状态（`:69-89`，字段值 `§8未绑定` / `site.describe("§b")`，末行 `N / 6 已绑定`）；`设置 <锚点>`（`:115-163`，四条失败回执逐字：`当前不在游戏世界中，无法绑定锚点` / `模块运行中无法修改锚点，请先关闭模块` / `<锚点>已绑定，请先删除旧绑定再重新设置` / `准星未对准任何方块，请将准星对准要绑定的方块` / `该锚点需要指向容器方块（箱子/桶/潜影盒等），但准星对准的不是容器` / `无法获取当前维度信息`；成功回执字段 `坐标` / `维度` / `类型`，状态 `已保存`）；`移除 <锚点>`（`:165-184`）；`状态`（`:60`）；`清空`（`:91-113`，空绑定回执 `六个锚点本来就都没有绑定。`）；`扩展 <格数>`（`:187-252`，1~128 格，朝向取玩家水平朝向 `西/北/东/南`，只改 X/Z）。

指令 ↔ 模块通信：**经注册表取实例**（`Modules.get().get(AutoFarmMatrix.class)`，`NongChangCommand.java:294-307`），点位读写统一走 `AutoFarmMatrix.site/bindSite/clearSite/clearAllSites`（`:546-572`）→ `rawSite/setRawSite`（`:574-594`）。

### 2.6 死代码（旧项目零调用，建议不搬并登记）

| 文件 | 行数 | 证据 |
|---|---|---|
| `adapter\FarmServerAdapter.java` | 50 | 全项目搜索：仅 `AutoFarmMatrix.java:4` 死 import + 自身声明 `:13`，**零实例化** |
| `adapter\DefaultFarmAdapter.java` | 49 | 仅 `AutoFarmMatrix.java:3` 死 import + 自身声明 `:16`，**零 `new`** |
| `recognition\VanillaFarmRecognizer.java` | 143 | 仅 `AutoFarmMatrix.java:17` 死 import + 自身声明 `:21`，**零 `new`、零调用** |
| `recognition\CropRecognizer.java` | 21 | 仅被死适配器与 `VanillaFarmRecognizer` 引用 |
| `recognition\MaturityRecognizer.java` | 21 | 同上 |
| `recognition\SoilRecognizer.java` | 30 | 同上 |
| `recognition\CropRecognitionResult.java` | 27 | 同上 |
| `recognition\MaturityResult.java` | 25 | 同上 |
| **合计** | **366** | 8 文件 |

**实际生效的识别链路**在别处：`FarmScanner.classify`（`FarmScanner.java:255-292`）→ `CropProfile.isHarvestable`（`CropProfile.java:151-168`）+ `isMaxAge`（`:180-199`，找名为 `age` 的 `IntegerProperty` 取上限比对）+ `isPlantable`（`:173-177`），加 `FarmScanner.isTillable`（`:295-299`，`草方块/泥土 && 上方空气`）。**这四段才是要搬的东西**，`recognition/` 包不搬。

### 2.7 设置项全量（5 分组 / 16 可视静态 + 6 隐藏锚点 + 15 逐作物动态 = 37 项）

> **计数口径（本机逐条复核）**：`sgHelper.add` 2 处、`sgCrops.add` 4 处、`sgPerSeed.add` 2 处（循环内生成 15 项）、`sgLogistics.add` 5 处、`sgRender.add` 5 处 → **可视静态项 16 个**（`.name(` 逐条命中行 138 / 144 / 151 / 158 / 165 / 172 / 213 / 220 / 226 / 233 / 239 / 246 / 252 / 258 / 264 / 270）；`hiddenAnchor(` 6 处调用 → 隐藏锚点 6 项。运行期合计 37 项。

分组（`AutoFarmMatrix.java:84-88`，逐字）：`辅助工具`（默认收起）/ `作物选择`（默认展开）/ `逐作物独立配置`（默认展开）/ `运行参数`（默认收起）/ `渲染显示`（默认收起）。

静态项（19）：

| 变量 | 中文名（逐字） | 控件 | 默认 | 行号 |
|---|---|---|---|---|
| `antiTrample` | 防踩踏 | 开关 | `true` | `:137-141` |
| `autoTill` | 自动锄地 | 开关 | `true` | `:143-147` |
| `cropsDouble` | 双作物 | 方块列表（筛 `isDoubleCrop`） | 空 | `:150-155` |
| `cropsSingle` | 单作物 | 方块列表（筛 `isSingleCrop`） | 空 | `:157-162` |
| `cropsPillar` | 柱状物 | 方块列表（筛 `isPillar`） | 空 | `:164-169` |
| `cropsVine` | 果实 | 方块列表（筛 `isFruit`） | 空 | `:171-176` |
| `harvestMode` | 收割模式 | 枚举（`单个收割`/`批量收割`） | `SINGLE` | `:212-217` |
| `plantMode` | 补种模式 | 枚举（`顺序优先`/`均匀轮转`/`就近跟随`） | `SEQUENTIAL` | `:219-223` |
| `poisonUnloadThreshold` | 杂物卸货 | 整数 1~64 无滑条 | `64` | `:225-230` |
| `bpt` | 发包速率(BPT) | 整数 1~30 无滑条 | `10` | `:232-236` |
| `reachDistance` | 收割距离 | 整数 3~8 无滑条 | `4` | `:238-242` |
| `renderBounds` | 农田边界 | 开关 | `true` | `:245-249` |
| `boundsColor` | 边界框颜色 | 颜色（`renderBounds` 可见） | `255,255,255,50` | `:251-255` |
| `renderTarget` | 目标显示 | 开关 | `true` | `:257-261` |
| `targetColor` | 目标颜色 | 颜色（`renderTarget` 可见） | `0,255,100,75` | `:263-267` |
| `renderLabels` | 点位字牌 | 开关 | `true` | `:269-273` |
| `siteStart` / `siteEnd` / `siteSingle` / `siteMulti` / `siteSeed` / `sitePoison` | `_anchor_start` 等（描述「内部使用：农场点位1」…） | 隐藏文本 | 空 | `:276-281`、`:291-298` |

逐作物动态项（15）：遍历 `CropProfile.values()` 跳过 `junk()`（`AutoFarmMatrix.java:181-183`），共 11 种作物 → 10 个「`<作物名>-卸货数量`」（`IntSetting` 1~36 无滑条，默认 8，`:189-196`）+ 5 个「`<作物名>-补货种子数量`」（`IntSetting` 1~10 无滑条，默认 3，`:200-207`，仅 `needsReplant()` 的作物）。

作物图鉴（`CropProfile` 11 个常量，`CropProfile.java:26-43`）：`WHEAT 小麦`/`CARROT 胡萝卜`/`POTATO 马铃薯`（`extraLoot = 毒马铃薯`，`:29`）/`BEETROOT 甜菜根`/`NETHER_WART 下界疣`（底盘 `SOUL_SAND`）/`BAMBOO 竹子`/`SUGAR_CANE 甘蔗`/`CACTUS 仙人掌`/`CACTUS_FLOWER 仙人掌花`/`PUMPKIN 南瓜`/`MELON 西瓜`。三类 `Kind`：`CROP`（age 成熟、需补种）/`PILLAR`（柱状：下方同种或竹笋且再下方不同种，`:158-164`）/`FRUIT`（挂果恒可收，`:156`）。

### 2.8 与水源显示的耦合（单向、弱）

- `AutoFarmMatrix` 用**全限定名**取模块（无 import）：`Modules.get().get(com.example.addon.water.WaterESPModule.class)`（`AutoFarmMatrix.java:705-707`）。
- 按钮文案随状态切换：激活时 `§a关闭水源显示`，否则 `§b打开水源显示`（`:708`）；点击 `waterModule.toggle(); mc.setScreen(null);`（`:710-713`）。
- `WaterESPModule`（`water/WaterESPModule.java`，**366 行，未移植**）全文不引用农场；`FarmRenderer` 类注释也写明「水源渲染已独立到 WaterESPModule，与农场模块解耦」（`FarmRenderer.java:16`）。
- **风险**：若本轮不搬水源模块，模块页这个按钮就没有落点（按钮本体只有 1 行逻辑，但模块缺失）。

### 2.9 自检与使用说明

- 自检：`FarmSelfCheck.check(enabledCrops, sites)`（`FarmSelfCheck.java:33-91`）返回全部缺项，逐条文案带颜色码，例如 `§a农场点位1§f·未绑定`（模板 `:94`）、`§e农场点位2§f·不在当前维度`（模板 `:100`）、`§c农田范围§f·农场点位1与农场点位2重合了`（`:87`）、`§e作物§f·未启用任何农作物`（`:38`，命中即 return）、`§c作物§f·当前版本最多支持 3 种农作物（当前 N 种）`（`:42`，命中即 return）。
- 失败播报格式由基类统一：`还差 N 项没配好，配完再开：` + `§6  i. §f<条目>`（`YiyiaddonModule.java:156-159`，调用点 `AutoFarmMatrix.java:316`）。
- 使用说明：`§e查看使用说明`（`:700`）→ `new HelpScreen(theme, this, buildHelpContent())`（`:701`），标题 = `自动农场 - 使用说明`（`HelpScreen.java:28`）；七个章节逐字：`准备工作`、`点位设置 §7(指令)`、`作物选择`、`单种子独立配置`、`收割模式`、`工作流程`、`注意事项`（`AutoFarmMatrix.java:805-851`）。
- 模块页六张点位卡片：`§a设置` / `§8设置`（未绑定）、`§c删除`，点「设置」走 `NongChangCommand.setBinding(type)`（`AutoFarmMatrix.java:779-791`），删除走 `removeBinding(type)`；删除失败会给提示（04-基准-03 第六节对照）。

### 2.10 连带并入：水源显示 `WaterESPModule`（1 文件 / 366 行，用户 2026-09-16 拍板）

| 项 | 事实 | 行号 |
|---|---|---|
| 声明与分类 | `public class WaterESPModule extends YiyiaddonModule`；分类 `AddonTemplate.CATEGORY` = **「工具」**（不是自动化） | `WaterESPModule.java:32`、`:78`；`AddonTemplate.java:84` |
| 标题与描述逐字 | `"水源显示"` / `"已放水渲染蓝色 9×9 灌溉范围，在已有水源旁显示红色建议框提示可放水位置。"` | `:78-79` |
| 注册点 | `Modules.get().add(new WaterESPModule())` | `AddonTemplate.java:165` |
| 指令 | **无指令类**（`water` 包只有这 1 个文件，无 `command/`） | 包清点 |
| 外部引用 | 仅 `AutoFarmMatrix.java:706`（模块页那个开关）与注册点；无其它模块引用 | 全项目搜索 |
| 常量 | `BUDGET_PER_TICK = 512`、`SUGGEST_INTERVAL = 40`、`SCAN_DOWN = 12` | `:35`、`:38`、`:41` |
| 设置分组（4，逐字） | 默认组（`settings.getDefaultGroup()`）／`灌溉范围显示`／`建议放水点`／`高级选项` | `:43-46` |
| 设置项（10） | 扫描半径 `12`（4~32 带滑条，`:82-88`）、渲染距离 `16`（4~64 带滑条，`:90-96`）、显示灌溉范围 `true`（`:99-103`）、范围颜色 `30,144,255,60`（`:105-110`）、显示建议点 `true`（`:113-117`）、建议点颜色 `255,0,0,180`（`:119-124`）、建议点样式 `Both`（`:126-131`）、显示水源方块 `false`（`:134-138`）、水源方块颜色 `0,0,139,180`（`:140-144`）、水源方块样式 `Both`（`:146-151`） | — |
| 扫描 | 玩家周围 `半径` 的水平方形 × `Y-12..Y`；512 格/tick 游标推进 `x → z → y`（`:287-297`、`:355-365`）；移动判定阈值 **2 格**（`:240-244`）；移动时**只剪掉越界缓存、保留游标进度**（避免水框闪烁，`:259-278`） | — |
| 过滤 | 只收「静止水源」`state.is(Blocks.WATER) && getFluidState().isSource()`（`:291`、`:301-304`）且**孤立**（东/南/西/北四邻都不是静止水源，`:310-315`）→ 过滤海/湖/河连片水体 | — |
| 灌溉范围框 | 每个水源画 `x-4..x+5, y..y+1, z-4..z+5`（9×9）的 `ShapeMode.Both` 框（`:208-212`） | — |
| 建议点 | 每个水源的 东/西/南/北 **±9 格**（灌溉范围刚好相连不重叠，`:330-335`）；排除已有水源与重复（`:339-342`）；要求该格上方是空气/水/可替换（`:346-349`）；重算时机 = 区域变化立即 + 否则每 40 tick（`:170-177`） | — |
| 事件 | `TickEvent.Post`（`:166-178`）、`Render3DEvent`（`:181-226`） | — |
| 与农场的关系 | 单向弱耦合：农场模块页只放一个开关按钮（`AutoFarmMatrix.java:705-713`）；本模块自身不引用农场（`:30` 注释「独立于自动农场模块，不开农场也能单独用」） | — |

> 落本项目时的分类：旧「工具」→ 本项目 `utility`「工具」（`module\AddonModules.java:83`，权重 10）；与阶段12 的管理员检测同分类。

---

## 三、依赖盘点（旧项目用什么 → 新项目现状 → 处置）

| 旧依赖 | 旧项目落点 | 新项目现状（已核对到方法级） | 处置 |
|---|---|---|---|
| `farm\ContainerBroker`（235 行） | 容器同步 + 搬运 | `service\container\ContainerService.java`（有状态实例，`tick/isReady/depositOne/withdrawOne/countInContainer/hasSpace/close`，`:36-149`）+ `platform\container\ContainerAccess.java`（静态原语 `openMenu/quickMove/closeContainer/...`，`:34-106`）；结果枚举 `model\container\ContainerTransferResult`（`MOVED/CHEST_FULL/NONE/NOT_READY`） | **直接复用**，不新建 |
| `farm\FarmPacketOps`（185 行） | 破坏/放置/交互/锄地发包 | `platform\network\BlockPacketSender.java`：`breakBlock`（`:73-95`，**已含预测编号 + 本地立刻置空气**）、`useOnBlock`（`:104-111`）、`interactBlock`（`:121-129`）、`tillBlock`（`:137-146`） | **直接复用**；缺 `getRemainingDurability`（旧 `FarmPacketOps.java:179-184`）→ 按需补 |
| `autofarm\navigation\FarmNav`（168 行） | Baritone 隔离层 | `feature\stardew\navigation\FarmNav.java` 与旧类**逐行等价**（方法名、行号一一对应） | **两选一**：直接跨包引用，或提升到 `platform`（见 D7） |
| Meteor `InvUtils` / `FindItemResult` | 快捷栏/背包查找、换槽、副手 | `feature\stardew\adapter\StardewAdapter`（`findHotbar/findAny/selectHotbar/swapToHotbar/swapOffhandWith/heldItem`）+ 实现 `DefaultStardewAdapter`（`:92-141`）+ `feature\stardew\service\StardewInventoryService`（计数/找槽/读水量等） | **复用其能力，另外新写农场专用查找**（作物/锄头/时运工具语义不同，落在 `feature/autofarm/` 自己的工具类） |
| Meteor 渲染（`Render2DEvent`/`Render3DEvent`/`Renderer2D`/`NametagUtils`/`ShapeMode`） | `FarmRenderer`（67 行） | `ui\render\world\`：`WorldOverlay`（注册层 + 帧钩子，`register/unregister`）、`EspRenderer`（`box/line/tracer/text/quad` 全套）、`ShapeMode`（`Lines/Sides/Both`）、`EspGlobalSettings`、`EspColor` | **照旧项目行为重写为 `WorldOverlay.Layer`**（星露谷就是这么做的：`StardewRenderState.java:38`、注册 `StardewFarmModule.java:468/557`） |
| 原版作物识别 | `CropProfile` 的 `isMaxAge/isHarvestable/isPlantable` + `FarmScanner.isTillable` | **新项目零命中**（搜 `CropBlock`/`NetherWartBlock`/`CocoaBlock`/`SWEET_BERRY_BUSH`/`HoeItem`/`FARMLAND` 无逻辑命中；星露谷识别层完全是自定义语义身份 + `CropRuntimeStateResolver`） | **必须新写**（这是本阶段最大的新增面） |
| `CommandMessageFormatter` | 指令回执排版 | `core\CommandMessageFormatter.java` 已存在（星露谷指令在用） | **直接复用** |
| 使用说明壳 `ui\HelpScreen` | 七章节帮助 | `ui\screen\HelpPanelScreen`（`HelpPanelScreen(String moduleName, String[] helpContent, Screen parent)`，`:50-54`；`buildHelpContent(HelpSection...)`，`:75-99`） | **直接复用** |
| 六点位卡片（旧为模块页内嵌卡片） | 设置/删除两个动作 | `ui\console\PointCardGrid`（`PointCard` / `Grid`，两列，`:24`/`:30`/`:148`）——由星露谷控制台抽出、星露谷与自动挖矿共用 | **直接复用** |
| 多目标选择器（旧 4 个 `BlockListSetting`） | 双/单/柱状/果实 四类作物 | `ui\screen\SelectorScreen`（左候选 + 右已选 + 两级分组 + 搜索，多选与单选 `pick` 两种构造） | **直接复用**（阶段12 刚收口） |
| 模块基类与播报 | `YiyiaddonModule` | `core\` 模块基类 + `ui\component\ModuleStatusBar` | **直接复用** |
| 模块分类 | `AddonTemplate.CATEGORY_AUTOMATION`「自动化」 | `module\AddonModules.java:85` 已注册 `automation`「自动化」 | **落 `automation`** |
| 水源显示模块（连带并入） | `water\WaterESPModule` 366 行，分类「工具」 | 本项目**零命中**（无 `WaterESP`/`水源显示`），且本项目已有 `ui\render\world\` 世界渲染壳与 ESP 全局设置 | **新建 `feature\water\`**，渲染走 `WorldOverlay.Layer`；分类落 `utility`「工具」 |

**节拍与线程事实**（旧项目侧）：整个 `autofarm/` **零线程、零 Timer、零 sleep**，全部推进依赖主线程 `TickEvent.Post`（`AutoFarmMatrix.java:440-441`）；每 tick 固定工作量 = 512 格扫描（`FarmScanner.java:31`）+ 一次决策链 + 一次任务 `tick()`；`fullScan()` 只在激活时跑一次（`AutoFarmMatrix.java:333`）。新项目事件体系不同，落点改为模块自己的 `onTick`。

---

## 四、风险与不确定项

| # | 风险 | 依据 | 处置建议 |
|---|---|---|---|
| R1 | **原版作物识别全新写**：`age` 上限判定、柱状物顶格判定、果实恒可收、`isTillable` 这四段在新项目无对应物 | `CropProfile.java:151-199`、`FarmScanner.java:295-299`；新项目零命中（第三节） | 逐行照搬判据（不引入语义身份），自成 `feature/autofarm/recognition` |
| R2 | **批量瞬间破坏依赖「发包后本地置空气」** | `FarmPacketOps.java:63-72`；新项目 `BlockPacketSender.breakBlock:88-89` 已有同款 | 已核，无需额外改动；但 `WAVE_SIZE = 32` 的单 tick 包量要实机观察服务端容忍度 |
| R3 | **时运工具读附魔**：`DataComponents.ENCHANTMENTS` + `Registries.ENCHANTMENT` | `FortuneTool.java:114-126` | 新项目附魔模块已有附魔读取能力（阶段11），施工时先核一次，能复用就不新写 |
| R4 | ~~水源显示模块缺席~~ | `AutoFarmMatrix.java:705-713`；`water\WaterESPModule` 366 行 | **已由 D1 拍板收口**：水源模块本阶段连带搬（2.10），按钮落点随之就位 |
| R5 | **逐作物 15 个动态设置项**在新壳的呈现方式 | `AutoFarmMatrix.java:181-207`；本项目没有「按作物动态生成设置行」的先例 | **已由 D3 拍板收口**：整组进独立窗口，窗口内按作物分组折叠（开发习惯第 201 条） |
| R6 | **`FarmScanner` 三缓存排序无上限**：每次都 `new ArrayList` + 全排序 | `FarmScanner.java:112-113`、`:132-133`、`:150-151` | 照旧项目一比一搬；实机若卡再按旧口径优化（不得先改） |
| R7 | 旧模块页是 Meteor `WWidget/WTable` 手搭 | `AutoFarmMatrix.java:690-736` | 新壳是自研页面体系，需按「只换外壳」重搭，行为逐字对齐（阶段10/11/12 同一做法） |

---

## 五、用户拍板（2026-09-16，本阶段施工依据）

| # | 问题 | 拍板结果 | 落地影响 |
|---|---|---|---|
| **D1** | 模块页「打开/关闭水源显示」按钮 | **连带搬水源显示模块**（`WaterESPModule` 1 文件 366 行） | 本阶段范围 +1 文件；新增 `feature/water/`，渲染走 `WorldOverlay.Layer`，分类落 `utility`「工具」；2.10 节为侦察事实 |
| **D2** | 界面形态 | **模块页 + 整屏控制台** | 模块页保留旧项目全部内容（说明 / 六卡片 / 水源开关）+ 控制台入口；控制台是本项目自研壳，内容照旧项目设置分组展开 |
| **D3** | 15 个逐作物动态设置项呈现 | **独立窗口 + 按作物折叠** | 页面上保留作物勾选；「逐作物独立配置」整组（10 卸货 + 5 补货）进独立窗口，窗口内按作物分组折叠（开发习惯第 201 条）；行文案逐字沿用旧项目生成的 `<作物名>-卸货数量` / `<作物名>-补货种子数量` |
| **D4** | 死代码 8 文件 366 行 | **不搬**（登记在差异清单） | `adapter/` 2 + `recognition/` 6 不入库；四段真实生效的判据（`isMaxAge`/`isHarvestable`/`isPlantable`/`isTillable`）另行照搬 |
| **D5** | `.farm` 别名 `nongchang` | **保留**（指令名与别名一个字不改） | — |
| **D6** | 自检上限 `MAX_CROPS = 3` | **照旧 3** | 文案 `§c作物§f·当前版本最多支持 3 种农作物（当前 N 种）` 逐字 |
| **D7** | `FarmNav` 归属（现居 `feature/stardew/navigation/`） | **提升到 `platform/` 共享**，星露谷改为引用 | 纯 Baritone 隔离层（零业务）两模块共用一份；改动星露谷 import 后必须重新编译验证，并在差异清单登记 |
| **D8** | 实机验证环境 | **单机自建农田** | 验收清单按第七节逐条跑；原版作物（小麦/胡萝卜/马铃薯/甜菜根/下界疣/竹蔗/瓜类）在单机可比对旧项目行为 |
| **D9** | 界面质量（用户 2026-09-16 原话：「带图标的选择器 控制台 细节细节记得做好」） | **选择器一律带图标；控制台细节做到位** | ① 作物四类选择器（双/单/柱状/果实）与战利品选择器每行必须有图标，图标来源照本项目现行做法（原版物品贴图，能取实体模型的取实体模型），**不得出现纯文字行**；② 点位卡片沿用 `PointCardGrid`（图标 + 名称 + 坐标 + 状态），不新造样式；③ 控制台照本项目既有四页签范式（概览 / 设置 / 点位 / 日志）并按本模块实际内容划分页签，页内每一行的图标、间距、行高、悬停反馈、tooltip 全部对齐既有页面，不允许出现「半成品页」；④ 交付前逐页截图自查，细节问题在阶段报告里逐条登记 |

---

## 六、施工批次建议（一次性做完整，按此顺序推进）

- **批次 1 · 侦察记录落库**（本阶段先交这个，等你看过再进批次 2）
  - `49-阶段13-附录A-逻辑层侦察记录.md`：`AutoFarmMatrix` / `FarmController` / `FarmDecision` / `FarmScanner` / `FarmResourceManager` / 13 个任务类 / 模型层 / **水源模块扫描与建议点算法**，逐字原文 + 行号
  - `50-阶段13-附录B-界面与指令侦察记录.md`：`.farm` 全子命令与逐字回执、模块页按钮与六卡片、使用说明七章节逐字、状态播报文案、**控制台（本项目新壳）页面划分方案**
  - `51-阶段13-附录C-设置项控件对照表.md`：19 + 15 + 6 项 + 水源模块 10 项逐条 → 本项目自研控件映射
- **批次 2 · 底座与逻辑层**：先做 D7（`FarmNav` 提升到 `platform/`，星露谷改引用并重编译验证）→ 模型层（`CropProfile`/`FarmSite`/`FarmTarget`/`SiteType`/`FarmState`/`HarvestMode`/`PlantMode`）→ 识别与扫描（`FarmScanner` + 原版作物判据）→ 资源（`FarmResourceManager`）→ 任务层 13 类 → 控制器（`FarmController`/`FarmDecision`/`Observer`/`Verifier`/`SelfCheck`/三个计划）
- **批次 3 · 外围**：`.farm` 指令（全子命令 + 逐字回执）、模块页与六卡片、使用说明七章节、自检、状态播报、世界渲染、**整屏控制台（D2）**、**逐作物独立窗口（D3）**、**水源显示模块（D1）与模块页那个开关按钮**
- **批次 4 · 收口**：编译 + 第五节 D1~D8 逐条落地核对 + 实机验证（单机自建农田，D8）+ 生成 `52-阶段13-自动农场移植记录与差异清单.md`（含死代码 8 文件 366 行与所有新增项的登记）

---

## 七、验收清单（待实机，与旧项目逐条对齐）

1. `.farm` 六个子命令 + 五条失败回执 + 四条成功回执逐字一致；`扩展` 四朝向与范围计算一致。
2. 自检缺项文案逐字（含 `最多支持 3 种农作物`、`农场点位1与农场点位2重合了`）。
3. 六点位卡片：设置 / 删除失败提示行为与旧项目一致。
4. 单作物箱（马铃薯/下界疣）、多作物箱（小麦/甜菜根）、种子补货箱、杂物箱（毒马铃薯 + 仙人掌花）四条物流各自触发条件与保留量正确。
5. `单个收割` 与 `批量收割` 差异可见；批量时一 tick 内连续破坏上限 32。
6. 三种补种模式行为差异可见（顺序 / 轮转 / 就近跟随）。
7. 时运工具：主手无时运时会自行置入最高时运工具，任务结束还原。
8. 防踩踏（框内锁跳跃）、自动锄地（无锄头跳过）、掉落物拾取（水里找干燥站位）三条行为一致。
9. 渲染三项：农田边界（只画线）、目标高亮（线 + 面）、六点位字牌（白字）开关生效。
10. 状态播报 8 个状态名逐字出现在状态条上。
11. 出世界自动关闭模块；物流任务运行中容器界面被拦截。

---

## 八、证据与命令

```powershell
# 逐文件行数（本文件 2.1 表即此命令输出，用 (Get-Content).Count，Measure-Object -Line 会偏低）
$root = 'd:\mcaddon\26.1.2\src\main\java\com\example\addon'
Get-ChildItem "$root\autofarm" -Recurse -Filter *.java -File | Sort-Object FullName | ForEach-Object {
  '{0,-58} {1,5}' -f $_.FullName.Replace("$root\", ''), (Get-Content $_.FullName).Count
}

# 死代码零调用核实（adapter 与 recognition 两包）
#   搜索范围：d:\mcaddon\26.1.2\src
#   关键词：FarmServerAdapter | DefaultFarmAdapter | VanillaFarmRecognizer | CropRecognizer(autofarm 包)
#   结论：仅 import/声明命中，零 new、零调用点

# 新项目底座核实（对照第三节）
#   platform\container\ContainerAccess.java          容器原语
#   service\container\ContainerService.java          有状态容器 broker（与旧 ContainerBroker 一一对应）
#   platform\network\BlockPacketSender.java          发包（breakBlock:73-95 已含预测编号 + 本地置空气）
#   feature\stardew\navigation\FarmNav.java          与旧 autofarm\navigation\FarmNav 逐行等价
#   ui\render\world\{WorldOverlay,EspRenderer,ShapeMode}.java   世界渲染壳
#   ui\screen\{HelpPanelScreen,SelectorScreen}.java  帮助页与多目标选择器
#   ui\console\PointCardGrid.java                    点位卡片（两列）
#   module\AddonModules.java:85                      自动化分类

# 新项目无原版作物识别（R1 依据）
#   搜索：CropBlock | NetherWartBlock | CocoaBlock | SWEET_BERRY_BUSH | HoeItem | FARMLAND
#   结论：零逻辑命中（唯一命中是星露谷物流页注释里的字符串）
```

---

## 变动记录

| 日期 | 变动 |
| --- | --- |
| 2026-09-16 | 首版：三路只读侦察（入口与指令层 / 调度与模型层 / 任务与识别层）合成；登记 8 项待拍板 |
| 2026-09-16 | D1~D8 全部拍板（第五节），范围变动：**水源显示模块并入本阶段**（新增 2.10 节，1 文件 366 行）；批次表按 D1/D2/D3/D7/D8 回改；R4/R5 风险关闭 |
