# 37 · 附A · 新窗口开工指令（复制粘贴即用）

日期：2026-09-16
用途：**新开对话窗口时，把下面整段发给智能体**。它替代「你上次说过什么」的口头交代——上下文、任务、硬约束、开工前要问的四件事全在里面。
维护：拍板或范围有变动时**改本文件**，不要靠对话记忆传递。

---

```text
项目：d:\mcaddon\yiyiaddon（Minecraft 26.1.2 Fabric 自研客户端模组，Skija 自研壳，禁止依赖 Meteor 框架）
旧项目：d:\mcaddon\26.1.2（严格只读，任何情况下不得修改）
今天：2026-09-16

【第一步：连接上下文，按顺序读，不要跳】
1. 开发习惯.md（项目根目录，规则正本，最高优先级）
2. 02-开发报告/项目开发报告/00-AI阅读指引.md（导航与口径）
3. 同目录 01-总纲-迁移口径与复刻原则.md
4. 第 1 层六份基准（迁移时逐字对齐的唯一依据，禁止凭印象改写旧项目原文）：
   02-基准-01-状态播报体系 / 03-基准-02-自检启动机制 / 04-基准-03-配置页按钮与独立窗口清单 /
   05-基准-04-关联代码依赖网 / 06-基准-05-逐模块依赖全量清单 / 07-基准-06-指令体系全量清单
5. 30-移植总览-已移植与未移植全清单.md（推进顺序与逐包清点）
6. 37-阶段10-自动挖矿移植侦察与决策.md（本轮开工依据，已侦察完，不要重复侦察）
7. 38-阶段11-自动附魔移植侦察与决策.md（只读 2.8 节：KillAura 是它的公共前置）
8. 32-阶段09-星露谷农场移植记录.md 与 34-复盘-ID三件套差异核对与修复.md（照搬这套做法与复盘口径）

【第二步：任务】阶段10 自动挖矿移植（范围已拍板，见 37 号第五节，不要重新决策）
- 搬旧项目 com.example.addon.mining 全包 16 文件 / 6,532 行 + 必要外围。
- 模块形态：靠服务器传送指令在「矿区 ⇄ 城」往返，不是自己找矿；三个点位 = 矿物箱 / 食物箱 / 挂机修复点。
- 本轮做：FSM 主干（IDLE / GO_WILD / MINING / UNLOADING / SUPPLY / EATING / DEATH_HANDLING / RESPAWN_WAIT）、
  Baritone 寻路、矿石识别与单目标锁定、容器物流与白名单、秒破、KillAura、ESP、`.wk` 指令全集、
  配置页面 + 控制台页 + 帮助页 + 状态播报、设置项 5 组逐项落地。
- 本轮不做（留白并登记，不得夹带）：OrePredictor 种子模式、tactical 整套反检测（4,297 行，不可搬）、
  `.wk 检测假矿`（定性后再定）、音效通知（待用户拍板）。

【第三步：三条用户已拍板、必须执行的硬约束】
1. 杀截 KillAura：**以 Meteor 原始源码为蓝本重写**（参考库 01-开发参考库/Meteor原始源码/.../combat/KillAura.java，
   527 行；行号要点见 37 号 5.2 表），一比一还原（全量设置项与行为）。旧项目 combat/KillAura.java 只作比对副本，
   有出入以 Meteor 那份为准。实现全部落在本项目自己的工具层，**不引入任何 Meteor 依赖**。
2. 秒破：按旧项目 mining/fastbreak/ 一比一还原（控制器 + 发包协议 + mixin 接线），独立开关、默认关。
3. **配置页面与模块是联动的，必须同批交付**（37 号 5.3，用户原话：「我的自动挖矿配置页面是跟自动挖矿有联动的」）：
   - 页面每一行直接读写模块自己的设置字段与点位表，不允许「模块做完再补一张图」；
   - 页面与 `.wk` 指令同源同一份数据，双向一致；
   - 点位绑定 / 移除 / 失效校验只有模块那一处实现，页面与指令都调它；
   - 帮助页文案与 `.wk` 回执逐字同源。

【第四步：施工顺序】按 37 号第六节批次推进，先落批次 1 的**两份侦察记录**（照 28/29 号的做法，
逐字照抄旧项目源码原文 + 行号，分「逻辑层」与「界面与指令」两份），发我看过再进批次 2。

【第五步：开工前必须先问我的四件事（37 号第七节）】
1. 好友系统：新项目无好友体系 → 复刻简版好友名单，还是去掉「不打好友」这条过滤？
2. 音效通知（SoundNotifier 143 行，纯本地零风险）本轮带不带？
3. `.wk 检测假矿` 属识别层还是反检测层（需先读实现再定性）？
4. 实机验证用哪台服务器（RTP 指令 / GUI 关键词 / 冷却时长都要在那台上填）？

【铁律速查】只换 UI 不换功能与文案；指令名与回执一个字不改；禁止新增旧项目没有的功能；
一切改动先经我同意；修复前必须取证（行号 / 存档 / 可复现实验）；同源逻辑两边逐字比对。
编译验证：./gradlew compileJava --console=plain -q
```

---

## 变动记录

| 日期 | 变动 |
| --- | --- |
| 2026-09-16 | 首版：由 37 号第五/六/七节 + 用户当日三条拍板（KillAura 蓝本 / 秒破 1:1 / 配置页联动）合成 |
