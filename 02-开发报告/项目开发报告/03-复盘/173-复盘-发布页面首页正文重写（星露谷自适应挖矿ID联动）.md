日期：2026-09-21

# 173 复盘：发布页面首页正文重写（星露谷自适应 / 自动挖矿 / ID 联动自动箱子）

## 一、目的与范围

用户要求重做发布页面正文，重点介绍四项内容（星露谷农场含服务器自适应、自动挖矿、ID 联动自动箱子、安装步骤），补写前身说明与 Discord 入口，**图片不更换**。

据此确定范围：只改 `06-发布页面/仓库首页/README.md` 正文，`展示资源/` 下四张图（动态封面、静态封面、农场功能图、挖矿功能图）与 `版本说明草稿.md` 的两条版本行、附件名、依赖版本一律不动。模组运行代码未改。

## 二、改动清单

| 文件 | 改动 |
| :--- | :--- |
| `06-发布页面/仓库首页/README.md` | 正文重写：首屏三行定位 + 页内导航 + 下载/反馈入口 + Discord 徽章；新增「这个模组做什么」三行重点表；星露谷、自动挖矿、ID 联动自动箱子各成专章；「全部功能概览」「安装准备」「这个模组的前身」「版本状态」「反馈与社区」「作品与致谢」重排；新增社区段与 Discord 链接 |
| `06-发布页面/版本说明草稿.md` | 补「更新日志」块（9 月 21 日退出卡死修复）与 Discord 入口、首页指引 |
| `06-发布页面/使用说明.md` | 公开提交哈希更新；记录本轮只改正文、图片未动 |
| `06-发布页面/页面预览.html` | 用 `工具/生成预览.mjs` 重新生成 |

## 三、正文结构

首屏（居中）：动态封面 → 一句话定位「种地、挖矿、取货，一条流程跑到底」→ 页内导航六项 → 下载/静态封面/Issues/Discord 四个入口 → Discord 徽章。

主体四个功能章：星露谷农场（自适应点表 5 行 + 六页签 + 能力表 5 行 + 首次使用折叠）、自动挖矿（环节表 8 行 + 典型流程 + 首次使用折叠）、ID 联动自动箱子（联动点表 5 行）、全部功能概览（方向表 + 三段补充）。

收尾：安装准备（环境表 + 四步）、这个模组的前身、版本状态（Beta 口径 + 验证项表）、反馈与社区、作品与致谢。

## 四、内容口径依据

- **服务器自适应**：资源包提取 → 作物/种植盆/种子/工具列表、旧布局资源包（基础物品 + 派发表）、洒水器覆盖范围自学、成熟与季节判定、按服务器隔离存储，均取自既有实现与 136 号前后的服务器适配复盘。
- **自动挖矿**：采掘、战斗、满载卸货、口粮、工具维护、搭路垫脚、自用模式卖矿循环、配置记录按服务器，取自 143 等复盘的实际落地口径。
- **ID 联动自动箱子**：取物目标来自 ID 目标列表、逐物品定量、三种工作方式、七个页签。
- **前身**：明确写「前身是 Meteor Client 的附属插件，现重写为独立 Fabric 客户端模组，不再依赖任何第三方客户端框架，界面重做」，与第四阶段迁移实况一致。
- **Discord**：`https://discord.gg/vwrRCtET`，取自旧项目 README 的社区入口，未新造链接。
- 支持范围沿用规格口径：正文写 26.x，具体范围只写在安装准备表（26.1.2 与 26.2，26.3 及以后暂不支持）。
- 全部未实机验证的能力沿用既有状态标注方式，不在正文新增能力承诺。

## 五、过程中两个技术点

1. **GitHub 锚点规则**：页内导航若指向含全角括号的标题（`## 星露谷农场（服务器自适应）`），GitHub 生成的锚点会剥掉括号变成 `星露谷农场服务器自适应`，链接必失效。改为 `## 星露谷农场` 平标题 + 下方加粗「服务器自适应：…」引导句，锚点与本地预览一致。核对结果：六条页内导航对应标题 `## 星露谷农场`、`## 自动挖矿`、`## ID 联动自动箱子`（slug 为 `id-联动自动箱子`）、`## 全部功能概览`、`## 安装准备`、`## 这个模组的前身` 全部匹配。
2. **marked 版本**：本机 npx 缓存里是 marked 4.3.0，规格要求 12.x。按规格「可从命令行指定现有 marked 模块」，改用 `npm install marked@12` 到临时目录后重跑生成；预览 HTML 的 diff 主要为本次正文改动本身。

## 六、验收

| 项目 | 方式 | 结果 |
| :--- | :--- | :--- |
| 预览生成 | `node 工具/生成预览.mjs <marked 12 esm 路径>` | 成功，输出 `页面预览.html` |
| 结构核对 | 脚本统计 | 标题 9 个、表格 9 个、折叠 3 个，无 Markdown 表格残留、无未渲染管道符 |
| 资源引用 | 正文引用的四张图 | 均在 `展示资源/` 且为公开仓库现有文件 |
| 视觉检查 | Chrome 视口截图 | 1440px 与 390px 两档，首屏、星露谷章、页签行、表格排版正常，窄屏居中无溢出 |
| 远程发布页 | `gh release edit --notes-file` | 已更新，正文含更新日志与 Discord 入口，附件与版本不变 |
| 公开仓库首页 | 独立检出提交并推送 | 公开提交 `761cfbfc957875e23d016625f422adb57e9a8177` |
| 沙箱限制 | 截图需落在工作区内 | 未落盘，改用视口截图回传，无临时文件残留 |

## 七、遗留

- `06-发布页面/首页效果预览.png` 未重新生成（该 PNG 由历史本机渲染产出，生成工具只写 HTML；用户本轮要求图片不动，故保留原图）。
- 公开仓库当前为私有可见性，首页与 Discord 入口仅对授权访问者可见。
- 首页正文的「服务器自适应」等能力仍为代码完成与启动验证状态，需真实服务器环境验证。

## 八、追加（同日）：发布页正文改编号分块式

用户要求发布页文案「做规范点，1.2.3 这种样式的」。据此定稿编号分块格式，并把规格模板一并改成同一形态，避免样本与模板两套写法。

**定稿格式**：开头一行点明阶段，其后每块一律 `## <序号>. <块名>`，顺序固定为 **下载 / 运行环境 / 更新日志 / 使用提醒 / 反馈渠道**；缺哪块整块不写、剩余编号顺延，不留空号。

**改动**：

| 文件 | 改动 |
| :--- | :--- |
| `06-发布页面/版本说明草稿.md` | 改为五块编号式：1 下载（版本表）／2 运行环境（Loader + Java + 解压说明 + 内置寻路组件随包）／3 更新日志（9 月 21 日退出卡死修复）／4 使用提醒（独立实例 + 备份 + 26.3 不支持）／5 反馈渠道（Issues + Discord + 首页指引） |
| `06-发布页面/使用说明.md` | 硬约束第 2 条由「不超过六块」改为「按编号分块、标题格式 `## <序号>. <块名>`、顺序固定、缺块编号顺延」；第 8 条补「回读时确认编号连续、无空块」；两个模板（首测 / 后续版本）同步改为编号式；「首测不写更新日志」放宽为「更新日志只写本次包相对上一份包真实修掉的问题，没有就不写」 |
| 远程 Release | `gh release edit --notes-file` 同步并回读全文核对，编号 1—5 连续、无空块 |

**未动**：附件、版本号、下载表内容、标题、预发布标志、正文中的依赖版本（与 `gradle.properties` 一致）。

## 九、追加（同日）：文案定稿并写入开发习惯第四十章

用户追加两条要求：**「beta2 开始就是更新日志开头了」「文案要高级好看」**。据此定稿措辞标准，并把规范写进 `开发习惯.md`。

**新增铁律**：`开发习惯.md` 第四十章「发布页文案铁律」（2026-09-21 用户指令追加），第 238—241 条：

| 条 | 内容 |
| :--- | :--- |
| 238 | 正文分块编号 `## <序号>. <块名>`；首测顺序 下载 / 运行环境 / 更新日志 / 使用提醒 / 反馈渠道；**后续版本（beta2 起）更新日志开头**，顺序 更新日志 / 下载 / 运行环境 / 使用提醒 / 反馈渠道；缺块整块不写、编号顺延 |
| 239 | 更新日志只写玩家能看见的效果，按 修复 / 新增 / 变更 三类，一条一句话，写成「什么操作会怎样 → 现在怎样」；不写内部实现字样 |
| 240 | 措辞准确、克制、书面；块内用标签式短行承载信息；禁营销词、Emoji、感叹号堆情绪、空泛承诺；依赖版本与支持范围照抄文件；未实机验收的能力不许写成已支持 |
| 241 | 改完必须 `gh release edit` + `gh release view --json body` 回读，确认编号连续、无空块、无内部字样；只有真要换包时才动附件 |

**文案升级点**（同一批事实，换更高档的写法）：

- 定位行由「独立开发的 Minecraft Fabric 客户端模组」改为「面向 Minecraft Java 版 26.x 的独立 Fabric 客户端模组，主打星露谷农场、自动挖矿与 ID 联动自动箱子」
- 「运行环境」由一整段改为五条标签式短行：加载器 / 运行环境 / 安装方式 / 版本对应 / 内置组件
- 「使用提醒」由一句改为三条：独立实例与备份 / 支持范围 / Beta 状态说明
- 「反馈渠道」由一句改为三条：问题反馈（附日志建议）/ 交流社区 / 功能说明
- 更新日志改为书面陈述句式（「…的问题已修复」）

**同步**：`06-发布页面/使用说明.md` 的硬约束与两个模板改成同一形态（后续版本模板改为更新日志开头）；远程 Release 已 `gh release edit` 并回读核对，编号 1—5 连续。

**未动**：附件摘要、版本号、下载表、预发布标志、标题。
