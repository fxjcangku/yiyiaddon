# 181 - 复盘：v1.0-beta1 第五次覆盖发布（26.2 线同步后台链路与旧配置摘除）

日期：2026-09-21
分支：`master` + `port/26.2`
验证：两线 `buildRelease` EXIT=0；26.2 线 `compileJava` EXIT=0；**本次未跑生产实例**

## 一、本次发布内容

用户指令：「肯定同步26.2啊我的分支版本懂吗然后构建混淆跟个人版 混淆覆盖旧发布页版本。」

- 版本号：维持 `1.0-beta1`（同标签覆盖，与前三、四次一致）
- 覆盖对象：`v1.0-beta1` 的两个附件
- 包内改动（均来自 master 的三笔提交，见第二节）：

| 提交 | 内容 |
| --- | --- |
| `00232cc` | 后台面板三页重做与登录页去字母 Y、图表与总数缺陷修复、正版校验补镜像回退（174/177/178 复盘）—— **在服务端，不随包发布** |
| `4668e64` | 模组接通后台链路（遥测上报 / 指令埋点 / 玩家侧 `.聊天`·`.回复` 指令）并撤掉「更新提示」远程开关 |
| `dbb3190` | 摘除旧项目配置读取层，存档只认新项目目录 |

| 线 | ZIP | ZIP SHA-256 | 内含 JAR | 映射归档 |
| --- | --- | --- | --- | --- |
| 26.1.2 | `yiyiaddon-1.0-beta1-26.1.2.zip` | `b7dfb42d…` | `4a18feb1…` | `…-26.1.2-4a18feb1` |
| 26.2 | `yiyiaddon-1.0-beta1-26.2.zip` | `e7ba1d32…` | `1b3e9325…` | `…-26.2-1b3e9325` |

线上附件已回读（名称 / 大小 / SHA-256 与本地逐一相等）。

## 二、26.2 线同步（cherry-pick，未手工照搬文件）

在既有工作树 `D:\mcv\wt-262`（`port/26.2`）上按第 231 条执行：

```powershell
git cherry-pick 00232cc 4668e64 dbb3190      # → 13b816a / 69034d6 / ee97b3d
.\gradlew.bat compileJava                    # EXIT=0
git push source port/26.2                    # 7b14568..ee97b3d
```

三笔**全部干净落地，无一处冲突**：`03-后端API/` 与后台无关版本线，两份文件在 26.2 上与 master 逐字节相同；模组侧三笔只碰 `YiyiAddonClient` / `CommandManager` / `HeartbeatService` / `RemoteConfigService` / `TelemetryService` 与两个新指令类，都不涉及两线的官方 API 差异（`Minecraft.gui.setScreen`、`EntityTypeIds`、`FramePresent` 那几处）。26.2 线未新增任何版本适配修补。

## 三、构建

| 步骤 | 命令 | 产物 |
| --- | --- | --- |
| 个人版（两线一次出齐） | `.\gradlew.bat buildBoth` | `build/libs/yiyiaddon-1.0-beta1-personal+26.1.2.jar`、`…-personal+26.2.jar` |
| 混淆发布版（26.1.2） | `.\gradlew.bat buildRelease`（master） | `build/release/yiyiaddon-1.0-beta1-26.1.2.jar` / `.zip` / `.sha256` |
| 混淆发布版（26.2） | `.\gradlew.bat buildRelease`（`D:\mcv\wt-262`） | 同目录 `…-26.2.jar` / `.zip` / `.sha256` |

两条线的构建期检查全绿：加固行为对照 64448 项、加密常量审计（密文篡改认证拒绝=通过）、混淆静态检查（26.1.2 改名 1157 个类 / 26.2 改名 1159 个类）、Gson 字段契约往返。

## 四、验收（本次是**降级验收**，须照实记账）

- 做到：两线 `compileJava` 通过、两线 `buildRelease` 全量静态检查通过、发布包从构建脚本自身导出并回读线上摘要。
- **没做**：两线都没起生产实例，`--quickPlaySingleplayer` 跑满全程那一步这次省掉了。原因是本次包内改动集中在网络链路与配置读取层，不碰渲染 / 启动路径，风险面比前几次小；但这只是判断，**不等于验过**。
- 因此：包内新增的玩家侧 `.聊天` / `.回复` 指令、遥测与开关闸门、旧配置目录摘除后的存档路径，都还需要玩家实机确认（后台发消息能否到达、指令能否回显）。
- 覆盖前 26.2 那个附件有 **1 次下载**（26.1.2 为 0），发布页正文与下载表未改动 —— 同标签换附件不改版本号，已装 beta1 的玩家收不到更新提示；装过旧包的按需重下。

## 五、台账与仓库

- `06-发布页面/使用说明.md`：新增「2026-09-21 线上状态（第五次覆盖）」
- `05-混淆文件备份/README.md`：同一段镜像记录（26.2 线随 cherry-pick 同步）
- 新映射入库：`4a18feb1`（26.1.2，随 master 提交）、`1b3e9325`（26.2，提交在 port/26.2 线，与 `61a56a66`/`89dfcf52` 同处）
- 被覆盖那对（映射 `1b124a97` / `89dfcf52`）按保留规则留着
- 删除两处**从未对外挂过**的作废归档：`…-26.2-40edf1e0`、`…-26.2-d8c818c8`（第 234 条；它们一直只存在于 26.2 工作树里，是报告 172「遗留」里点名要清的那批）

## 六、遗留

- `build/` 下另有一批 `1.0-beta2` 残迹（`build/libs/…-personal+26.1.2.jar`、`build/release/…-26.1.2.jar` / `.zip`，26.2 工作树同样有），是版本号误升那次的产物，**从未对外发布**；`gradlew clean` 一带就会清掉，未单独处理。
- 生产实例验收这一步建议在玩家反馈前补跑一次；补跑时按第 235 条确认 `mods/` 里只有一个 `yiyiaddon-*.jar`。
