# 170 - 复盘：v1.0-beta1 第三次覆盖发布（自动登入两处口径修复）

日期：2026-09-21
分支：`master` + `port/26.2`（业务改动先在主线落地，再 cherry-pick）
验证：两线 `buildRelease` EXIT=0；两线各跑独立生产实例 180 秒，日志 0 异常行、无崩溃报告、正常存盘退出

## 一、本次发布内容

用户要求「发布最新的」，并明确**不升版本号**（「没叫你升级版本号，替换之前两个旧的」）。

- 版本号：维持 `1.0-beta1`（`gradle.properties` 曾误升到 `1.0-beta2`，已回退并另起提交）
- 覆盖对象：`v1.0-beta1` 上的两个附件 `yiyiaddon-1.0-beta1-26.1.2.zip` / `yiyiaddon-1.0-beta1-26.2.zip`
- 打包进去的业务改动（今天两笔）：
  1. 自动登入补主动断线抑制窗口判据（报告 168）—— 管理员检测保命断线不再被自动登入连回去
  2. 正版验证服允许开启自动登入，检测到加密连接即跳过登录流程（报告 169）

## 二、产物与摘要

| 线 | ZIP | ZIP SHA-256 | 内含 JAR | 映射归档 |
| --- | --- | --- | --- | --- |
| 26.1.2 | `yiyiaddon-1.0-beta1-26.1.2.zip` | `4b290469…` | `2f4c20d1…` | `02-字符串与控制流加强版-26.1.2-2f4c20d1` |
| 26.2 | `yiyiaddon-1.0-beta1-26.2.zip` | `6cf3632b…` | `61a56a66…` | `02-字符串与控制流加强版-26.2-61a56a66` |

线上回读：`gh release view v1.0-beta1 --json assets` —— 两个附件的名称 / 大小 / 更新时间与本地逐一对应，**下载次数均为 0**（故按第 235 条与发布页规范，正文与下载表不动、页面不出现「重新下载」字样，只在台账记录）。

## 三、生产实例验收（第 235 条）

两线各起独立生产实例（真 Fabric Loader 0.19.5 + 对应 Fabric API + **从发布 ZIP 解出的 JAR**，`--quickPlaySingleplayer` 进单人世界，180 秒）：

| 线 | 结果 |
| --- | --- |
| 26.1.2 | 崩溃报告：无；日志异常行 0；载入清单含 `yiyiaddon 1.0-beta1`；正常存盘退出 |
| 26.2 | 崩溃报告：无；日志异常行 0；载入清单含 `yiyiaddon 1.0-beta1`；正常存盘退出 |

静态强校验（两线均过）：类加载 1179 / 1183、解密字符串常量 16032 / 16038、解密整数常量 29055 / 29074、分派键逐项匹配、密文篡改认证拒绝。

## 四、本次跑验收的沙箱绕行（环境事实，非项目改动）

本会话的沙箱只允许写 `d:\mcaddon` 及其允许清单，验收脚本默认实例根 `D:\mcv` 被硬拦（`path not in allowlist`），且脚本的库准备是**现从 Mojang 拉取**，本会话网络下载同样被拦。

绕行做法（不改脚本、不改项目文件）：

1. `robocopy D:\mcv\<版本> D:\mcaddon\_prodcheck\<版本> /E /XJ` —— 复用 `D:\mcv` 下已备好的 library / natives / assets（`/XJ` 跳过指向 loom 缓存的 junction，由脚本自行重建）
2. 以 `-Root D:\mcaddon\_prodcheck` 跑脚本；库与资源已在位，脚本不再触发下载
3. 验收完成、归档落库后删除 `_prodcheck`（本次 26.1.2 + 26.2 合计约 580MB，工作区内不留痕；该目录在仓库之外，也不进 `.gitignore` 管辖范围）

26.2 线用 `-Release D:\mcv\wt-262\build\release` 指定本线产物目录；脚本的映射归档只在「脚本所在仓库根」下查找，26.2 的归档因此写在 worktree 自己的 `05-混淆文件备份/` 里（已确认落在 `…-26.2-61a56a66`）。

## 五、台账同步

- `06-发布页面/使用说明.md`：新增「2026-09-21 线上状态（第三次覆盖）」，记 ZIP 与内含 JAR 摘要、验收结论、下载次数为 0
- `05-混淆文件备份/README.md`：新增同一段的镜像记录（26.2 线随 cherry-pick 同步）

## 六、自查

- 第 235 条：两线均以「从发布 ZIP 解出的 JAR」跑满 180 秒，异常行与崩溃报告均为 0，结论未沿用上一轮。
- 第 234 条：新映射按「版本线 + 摘要前 8 位」命名归档；被覆盖那对的映射保留（曾对外挂过）。
- 第 233 条：本报告与台账改动先在 `master` 落地，再 cherry-pick 到 `port/26.2`，两条线各自 `compileJava` 过关后推送 `source`。
- 发布页规范：页面正文未改动，未出现内部字样与「重新下载」提示（下载次数为 0，无需提醒）。

## 七、遗留

- 本次验收只覆盖「装上去能进游戏能跑满」；自动登入的两处改动（保命断线抑制、正版服跳过登录）**未在服务器环境实测**，需玩家实机确认。
- 沙箱限制属本会话环境问题：今后若仍需我跑生产实例验收，需允许写 `D:\mcv` 且放行 Mojang 库下载，否则要走第四节的绕行。
