# 旧项目基准 03 · 配置页按钮与独立窗口清单

日期：2026-09-14
类别：基准类（旧项目只读盘点，非阶段报告）
调研对象：`D:/mcaddon/26.1.2` 自有包 `com.example.addon`
配套文件：
- 《2026-09-14-旧项目基准-01-状态播报体系.md》
- 《2026-09-14-旧项目基准-02-自检启动机制.md》
- 《2026-09-14-旧项目基准-04-关联代码依赖网.md》
- 《2026-09-14-旧项目基准-05-逐模块依赖全量清单.md》

> 检索方式（交叉覆盖）：`setScreen(new `、`setScreen(`、`Minecraft.getInstance().execute(`、`.action =`、`theme.button(`、`addUniformButton(`、`WButton`、`WMinus`、`WPlus`、`theme.plus()/minus()/triangle()/checkbox()`、`extends WindowScreen`、`ConfirmScreen`、`HelpScreen`、`registerCustomFactory`、空回调 `-> {}`。
> `theme.button(...)` 是第三方框架的 `WButton` 入口；`GuiRenderer.RESET` / `GuiRenderer.EDIT` 是无中文的图标字形按钮，按常量名逐字记录。

---

## 一、配置页按钮主表（按模块分组，穷尽）

### 1.1 自动箱子 `autochest`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `设置箱子点位（对准容器）` | `autochest.AutoChestModule` | `autochest/AutoChestModule.java:387` | `addPointFromCrosshair()` 后 `mc.setScreen(null)` | 否 |
| `清空全部点位` | 同上 | `:409` | 弹 `ConfirmScreen("清空点位")` | 否 |
| `清除当前维度处理记录` | 同上 | `:415` | 弹 `ConfirmScreen("清除记录")` | 否 |
| `清除全部处理记录` | 同上 | `:418` | 弹 `ConfirmScreen("清除记录")` | 否 |
| `§c删除`（点位卡片） | 同上 | `:437` | `pointManager.remove(...)`；通知后 `mc.setScreen(null)` | 否 |
| `选择容器类型` | `autochest.config.ContainerTypeSetting` | `autochest/config/ContainerTypeSetting.java:77` | 弹 `ContainerTypeSelectScreen` | 否 |
| `GuiRenderer.RESET`（图标） | 同上 | `:80` | `setting.reset()` + `refreshCount()` | 否 |
| `选择目标物品` | `itemid.ItemTargetSetting` | `itemid/ItemTargetSetting.java:91` | 弹 `ItemTargetSelectScreen`；**该控件虽定义在 `itemid` 包，但由自动箱子的「目标物品」配置项承载（`autochest/config/AutoChestSettings.java:191-195`），属「ID 三件套 ⇄ 自动箱子」联动链，见《基准-05》§0.8** | 否 |
| `GuiRenderer.RESET`（图标） | 同上 | `:94` | `setting.reset()` + `refreshCount()` | 否 |
| `配置每种物品数量` | `autochest.config.ItemQuantitySetting` | `autochest/config/ItemQuantitySetting.java:119` | 弹 `ItemQuantityScreen` | 否 |
| `GuiRenderer.RESET`（图标） | 同上 | `:122` | `setting.reset()` + `refreshCount()` | 否 |

### 1.2 自动农场 `autofarm`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `§e查看使用说明` | `autofarm.AutoFarmMatrix` | `autofarm/AutoFarmMatrix.java:699`（回调 701） | `mc.setScreen(new HelpScreen(...))` | 否 |
| `§a关闭水源显示` / `§b打开水源显示`（按水源模块状态切换） | 同上 | `:707` | `waterModule.toggle()` 后 `mc.setScreen(null)` | 否 |
| `§a设置` / `§8设置`（六卡片：农场点位1、农场点位2、单作物箱、多作物箱、种子补货箱、杂物箱） | 同上 | `:779`（卡片 `:718-731`） | `NongChangCommand.setBinding(type)` + `mc.setScreen(null)` | 否 |
| `§c删除`（同上六卡片） | 同上 | `:787` | `NongChangCommand.removeBinding(type)` + `mc.setScreen(null)` | 否 |

### 1.3 自动挖矿 `mining`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `§e查看使用说明` | `mining.AutoMinerModule` | `mining/AutoMinerModule.java:1509` | `mc.setScreen(new HelpScreen(...))` | 否 |
| `检测假矿` | 同上 | `:1517` | `checkFakeOres()`（校验种子/目标矿石后扫描） | 否 |
| `§a设置` / `§8设置`（矿物箱 / 食物箱 / 挂机修复点 三卡片） | 同上 | `:1593` | `WKCommand.setBinding(key)` + `mc.setScreen(null)` | 否 |
| `§c删除`（同上三卡片） | 同上 | `:1602` | 若 `isBound` → `WKCommand.removeBinding(key)` + `mc.setScreen(null)` | 否 |

### 1.4 自动附魔 `enchant`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `§e查看使用说明` | `enchant.AutoEnchantBook` | `enchant/AutoEnchantBook.java:606` | `mc.setScreen(new HelpScreen(...))` | 否 |
| `§a设置` / `§8设置`（点位卡片：空白书箱、青金石箱、工具/护甲箱、附魔台、砂轮、铁砧、铁砧箱、挂机点、成品箱） | 同上 | `:700` | `FumoCommand.setPoint(type)` 成功后 `mc.setScreen(null)` | 否 |
| `§c删除`（同上点位卡片） | 同上 | `:711` | 若 `isBound` → `FumoCommand.removePoint(type)` + `mc.setScreen(null)` | 否 |
| `选择附魔`（原版近战/工具/弓/钓竿/三叉戟/弩/通用附魔 + 自定义附魔，多个） | `enchant.ui.EnchantmentSelectSetting` | `enchant/ui/EnchantmentSelectSetting.java:56`（构造 `AutoEnchantBook.java:592-597`） | 弹 `EnchantmentSelectScreen` | 否 |
| `GuiRenderer.RESET`（图标） | 同上 | `:59` | `setting.reset()` + `refreshCount()` | 否 |
| `配置装备附魔` | `enchant.gear.GearEnchantSetting` | `enchant/gear/GearEnchantSetting.java:55`（回调 60-61） | `Minecraft.getInstance().execute(() -> ... setScreen(new GearEnchantScreen(...)))` | **是** |
| `GuiRenderer.RESET`（图标） | 同上 | `:57` | `setting.reset()` + `refreshSummary()` | 否 |

### 1.5 自动村民交易 `villager`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `§e查看使用说明` | `villager.AutoVillagerTradeModule` | `villager/AutoVillagerTradeModule.java:693` | `mc.setScreen(new HelpScreen(...))` | 否 |
| `§a设置` / `§8设置`（绿宝石箱 / 成品交易箱） | 同上 | `:806` | `CunminCommand.setBinding(key)` + `mc.setScreen(null)` | 否 |
| `§c删除`（同上两卡片） | 同上 | `:815` | 若 `isBound` → `CunminCommand.removeBinding(key)` + `mc.setScreen(null)` | 否 |

### 1.6 图书管理员 `librarian`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `§e查看使用说明` | `librarian.AutoLibrarianModule` | `librarian/AutoLibrarianModule.java:81` | `mc.setScreen(new HelpScreen(...))` | 否 |

### 1.7 自动登入 `autologin`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `立即重连` | `autologin.AutoLoginModule` | `autologin/AutoLoginModule.java:200` | 若 `reconnectHandler.isScheduled()` → `reconnectNow()` | 否 |
| `§c停止重连` | 同上 | `:205` | `reset()` + `transitionTo(IDLE)` + 通知 | 否 |
| `§e查看使用说明` | 同上 | `:217` | `mc.setScreen(new HelpScreen(...))` | 否 |

### 1.8 自动断线 `autodisconnect`

| 触发按钮逐字文案 | 出现位置 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- | --- |
| `§c立即断线` | `autodisconnect.CometDisconnectModule` | `autodisconnect/CometDisconnectModule.java:67` | `disconnect("手动触发自动断线")` | 否 |

### 1.9 ID 配置管理 `itemid.IdConfigModule`

主面板：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| `识别物品（主手→副手）` | `itemid/IdConfigModule.java:120` | `identifyItem()`（识别并写入 ID 配置） | 否 |
| `识别准星方块` | `:122` | `identifyBlock()` | 否 |
| `刷新（重读磁盘）` | `:124` | `refresh()` | 否 |
| `更多管理` | `:126` | `openManagementScreen()` → `setScreen(new IdManagementScreen(...))` | 否 |
| `全部` / `物品` / `实体` / `方块`（前缀 `§a✓ ` 或 `§7`） | `:146-149` | `selectType(n)` 重建面板 | 否 |
| `方块·全部` / `方块·已确认` / `方块·未知`（前缀同上） | `:155-160` | 改 `blockStatus` + `rebuildList()` | 否 |
| `§e使用说明` | `:171`（回调 172） | `mc.setScreen(new HelpScreen(...))` | 否 |
| `§7上一页` | `:315` | `page--` + `rebuildList()` | 否 |
| `§7下一页` | `:323` | `page++` + `rebuildList()` | 否 |
| `theme.minus()`（行内删除：物品 / 实体 / 方块） | `:337`、`:345`、`:384` | `removeItem` / `removeEntity` / `removeBlock` | 否 |

子窗口 `IdManagementScreen`（标题 `ID 更多管理`）：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 |
| --- | --- | --- |
| `手动添加物品 ID` | `itemid/IdManagementScreen.java:41` | 回调 `addItem` → `openAddScreen()` → `setScreen(new IdAddScreen(...))`（`IdConfigModule.java:417-419`） |
| `打开物品 ID 目录` | `:42` | `openDirectory(idManager.itemsDirectory())` |
| `打开实体 ID 目录` | `:43` | `openDirectory(entityIdManager.entitiesDirectory())` |
| `打开方块目录` | `:44` | `openDirectory(blockIdManager.blocksDirectory())` |
| `打开 ID 总目录` | `:45` | `openDirectory(autochestDir())` |
| `§c数据清理 >` | `:48` | 回调 `openCleanup` → `openDataCleanScreen()` → `setScreen(new IdDataCleanScreen(...))`（`IdConfigModule.java:434-443`） |
| `§7返回` | `:51` | `mc.setScreen(null)` |

子窗口 `IdDataCleanScreen`（标题 `ID 数据清理`）：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 |
| --- | --- | --- |
| `§c清空全部物品 ID` | `itemid/IdDataCleanScreen.java:43` | → `confirmClearItems()` → `ConfirmScreen`（`IdConfigModule.java:591`） |
| `§c清空全部实体 ID` | `:44` | → `confirmClearEntities()`（`IdConfigModule.java:600`） |
| `§c清空方块稳定记录` | `:45` | → `confirmClearBlocks()`（`IdConfigModule.java:527`） |
| `§c清空方块历史快照` | `:46` | → `confirmClearSnapshots()`（`IdConfigModule.java:536`） |
| `§c清空全部方块数据` | `:47` | → `confirmClearAllBlocks()`（`IdConfigModule.java:546`） |
| `§c§l【最高危险】清空全部 ID 数据` | `:52` | → `confirmClearAllIds()`（`IdConfigModule.java:614`） |
| `§7返回` | `:56` | `openManagementScreen()`（回到「ID 更多管理」） |

### 1.10 用户统计 `userstats`

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| `§a立即刷新`（加载中时为 `§e加载中...`） | `userstats/UserStatsModule.java:340` | `refreshStats()` | 否 |

### 1.11 星露谷农场 `stardew`

「服务器资源」面板 `stardew/selector/StardewResourceSetting`：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| 动态：`§e§l检测 / 提取当前服务器资源包` / `§a§l重新检测 / 更新资源` / `§7执行中… <状态>` / `§8检测 / 提取当前服务器资源包` | 初始 `:179`；文案刷新 `:310-319` / `:321-326` | `ServerResourceService.requestExtract()` + `setting.refresh()` | 否 |
| `§b打开控制台` | `:191` | `setting.module::openConsole`（`StardewFarmModule.java:2025` → `setScreen(new StardewConsoleScreen(...))`） | 否 |
| 动态：`§b§l查看资源包` / `§8查看资源包` | 初始 `:198`；文案刷新 `:328-334` | `setting.openCachedPack()`（定位当前服务器 ZIP 缓存） | 否 |
| `§e查看使用说明` | `:208` | `mc.setScreen(new HelpScreen(theme, setting.module, setting.module.helpContent()))` | 否 |

六类物品选择器 `stardew/selector/StardewTargetSetting`（设置页 `visible(false)`，实际在控制台渲染）：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| `点击选择`（作物 / 种植盆 / 肥料 / 魔法药剂 / 水壶 / 洒水器 六个） | `:160` | 弹 `StardewTargetSelectScreen`（标题 `"选择" + setting.category.title()`） | 否 |
| `GuiRenderer.RESET`（图标） | `:167` | `setting.reset()` + `refreshCount()` + `persist()` | 否 |

渲染对象 `stardew/selector/StardewRenderSetting`：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| `设置` | `:66` | 弹 `StardewRenderObjectScreen` | 否 |

后勤参数 `stardew/selector/StardewCropLogisticsSetting`：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| `GuiRenderer.RESET`（图标，每作物一个） | `:288` | 该作物数量模式与四阈值恢复默认（`StardewCropPlanStore.put` + `StardewLogisticsStore.put`） | 否 |

控制台 `stardew/ui/StardewConsoleScreen`：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| 页签 `概览` / `种植` / `运行` / `后勤` / `点位` / `日志`（激活 `§b§l`，非激活 `§7`） | `:139` | `switchTab(value)` → `mc.execute(this::reload)` | **是** |
| `§c清空全部点位` | `:225` | 弹 `ConfirmScreen("清空全部点位")` | 否 |
| `§7刷新日志` | `:244` | `mc.execute(this::reload)` | **是** |
| `§c清空日志` | `:246` | `module.clearConsoleLog()` + `mc.execute(this::reload)` | **是** |
| `§7刷新` | `:266` | `mc.execute(this::reload)` | **是** |
| `§7关闭` | `:269` | `onClose()` | 否 |

控制台「点位」页卡片（`StardewFarmModule.renderPointCardsInto` → `buildLocationCard` / `buildSprinklerCard`）：

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| `§a设置` / `§8设置`（农田起点、农田终点、种子箱、成品箱、补水点 五卡片） | `StardewFarmModule.java:2313`（按钮构造 `:2351`） | `setPointFromCrosshair(type)` 成功后 `mc.setScreen(null)` | 否 |
| `§c删除`（同上五卡片） | `:2315`（按钮构造 `:2356`） | `removePoint(type)` 成功后 `mc.setScreen(null)` | 否 |
| `§a添加`（洒水器卡片） | `:2325` | `addSprinklerFromCrosshair()` 成功后 `mc.setScreen(null)` | 否 |
| `§c移除`（洒水器卡片） | `:2327` | `removeSprinklerFromCrosshair()` 成功后 `mc.setScreen(null)` | 否 |

### 1.12 界面主题 `theme`

| 触发按钮逐字文案 | 文件:行号 | 点击后行为 |
| --- | --- | --- |
| `樱花粉` | `theme/ThemeModule.java:244` | `select(Palette.SAKURA)` |
| `蜜桃粉` | `:245` | `select(Palette.PEACH)` |
| `玫瑰粉` | `:246` | `select(Palette.ROSE)` |
| `莓果粉` | `:248` | `select(Palette.BERRY)` |
| `粉紫夜` | `:249` | `select(Palette.PINK_NIGHT)` |
| `薄荷灰` | `:250` | `select(Palette.MINT_GRAY)` |
| `雾霾蓝` | `:252` | `select(Palette.MIST_BLUE)` |
| `暖沙灰` | `:253` | `select(Palette.WARM_SAND)` |
| `鼠尾草` | `:254` | `select(Palette.SAGE)` |
| `灰紫夜` | `:256` | `select(Palette.DUSK_LAVENDER)` |
| `森林雾` | `:257` | `select(Palette.FOREST_MIST)` |
| `青碧` | `:258` | `select(Palette.TEAL)` |
| `薰衣草` | `:260` | `select(Palette.LAVENDER)` |
| `珊瑚` | `:261` | `select(Palette.CORAL)` |
| `天蓝` | `:262` | `select(Palette.SKY)` |
| `奶油` | `:264` | `select(Palette.CREAM)` |
| `石墨` | `:265` | `select(Palette.GRAPHITE)` |
| `恢复默认` | `:266` | `this::restoreDefault` |

（按钮构造统一在 `theme/ThemeModule.java:299-303` `addButton`，文案枚举 `:306-322`。）

### 1.13 战术 `tactical`

| 触发按钮逐字文案 | 所属模块 | 文件:行号 | 点击后行为 |
| --- | --- | --- | --- |
| `§e查看使用说明` | 发包防踢 `AntiKickBypass` | `tactical/AntiKickBypass.java:338` | `mc.setScreen(new HelpScreen(...))` |
| `§e查看使用说明` | 飞行绕过 `FlightBypass` | `tactical/FlightBypass.java:180` | 同上 |
| `§e查看使用说明` | 发包秒破 `PacketInstantBreak` | `tactical/packetbreak/PacketInstantBreak.java:310` | 同上 |
| `§e查看使用说明` | 服务器检测 `ServerDetector` | `tactical/ServerDetector.java:149` | 同上 |
| `§b查看下载资源包` | 服务器检测 `ServerDetector` | `:152` | `openResourcePackFolder()` → `SystemFileOpener.openDirectory(ResourcePackDownloader.dir(), ...)` |

### 1.14 独立窗口内部按钮

| 触发按钮逐字文案 | 所属 | 文件:行号 | 点击后行为 |
| --- | --- | --- | --- |
| `关闭` | 通用说明窗 | `ui/HelpScreen.java:51` | `mc.setScreen(null)` |
| `§c§l确认`（默认，可被构造参数覆盖） | 通用确认窗 | `autochest/ui/ConfirmScreen.java:40` | `onConfirm.run()` + `setScreen(null)` |
| `§7取消` | 通用确认窗 | `:45` | `setScreen(null)` |
| `§a✓ <类型名>` / `§c✗ <类型名>`（容器类型开关） | 自动箱子 | `autochest/ui/ContainerTypeSelectScreen.java:41` | `setEnabled(id, !enabled)` + 重建 |
| `theme.plus()` / `theme.minus()`（附魔多选左右栏） | 自动附魔 | `enchant/ui/EnchantmentSelectScreen.java:48`（加）、`:56`（减） | `setSelected(option, true/false)` + 重建 |
| `theme.plus()` / `theme.minus()`（目标物品多选） | 自动箱子 | `itemid/ItemTargetSelectScreen.java:86`（加）、`:72`（减） | `setSelected(key, true/false)` + 重建 |
| `theme.plus()` / `theme.minus()`（星露谷多选） | 星露谷 | `stardew/selector/StardewTargetSelectScreen.java:322`（加）、`:318`（减） | `setSelected(key, true/false)` + `persist()` + 重建 |
| `§b合成附魔详细` | 原版装备附魔 | `enchant/gear/GearEnchantScreen.java:84` | `GearCraftReport::openFolder` |
| `§c删除合成记录` | 原版装备附魔 | `:86` | `GearCraftReport.deleteAll()` + 更新提示标签 |
| `§c已排除` / `§a启用` | 原版装备附魔 | `:212` | `setting.setExcluded(...)` + 重建 |
| `theme.minus()` / `theme.plus()`（附魔等级） | 原版装备附魔 | `:221`（减）、`:229`（加） | `setting.setLevel(...)` + 重建 |
| `GuiRenderer.EDIT`（图标：颜色选择器） | 星露谷渲染对象 | `stardew/selector/StardewRenderObjectScreen.java:58` | `mc.setScreen(new ColorSettingScreen(theme, color))` |
| `wc.checkbox` 显示 / 彩虹（点击开关） | 星露谷渲染对象 | `:47`、`:65` | 写回同一 Setting |
| `§a查找并添加` | ID 手动添加 | `itemid/IdAddScreen.java:60` | `resolveAndAdd()` |
| `§c取消` | ID 手动添加 | `:62` | `setScreen(null)` |
| `§a<名称> §8▸ §7<物品ID>`（候选列表，动态） | ID 手动添加 | `:150` | `buildAndAdd(item)` |
| `复制 Item ID` | ID 识别结果 | `itemid/IdResultScreen.java:78` | 复制剪贴板 + 提示 |
| `复制完整信息` | ID 识别结果 | `:79` | 复制 JSON + 提示 |
| `保存 ID` | ID 识别结果 | `:80` | `saveQuietly()`（窗口保留） |
| `另存快照` | ID 识别结果 | `:81` | `saveSnapshot()` |
| `添加到 ID 配置` | ID 识别结果 | `:82` | `saveAndClose()`（保存后关窗） |
| `复制实体ID` | 实体识别结果 | `itemid/EntityResultScreen.java:49` | 复制 + 提示 |
| `复制完整信息` | 实体识别结果 | `:50` | 复制 JSON + 提示 |
| `保存实体ID` | 实体识别结果 | `:51` | `saveQuietly()` |
| `保存并关闭` | 实体识别结果 | `:52` | `saveAndClose()` |
| `复制方块ID` | 方块识别结果 | `worldblock/BlockResultScreen.java:91` | 复制 + 提示 |
| `复制完整信息` | 方块识别结果 | `:92` | 复制 JSON + 提示 |
| `保存方块` | 方块识别结果 | `:93` | `saveQuietly()` |
| `另存状态快照` | 方块识别结果 | `:94` | `saveSnapshotQuietly()` |
| `保存并关闭` | 方块识别结果 | `:95` | `saveAndClose()` |

---

## 二、独立窗口类清单

| 窗口类名（完整包名） | 标题文案（构造参数原文） | 所属 | 文件:行号 |
| --- | --- | --- | --- |
| `com.example.addon.ui.HelpScreen` | `module.title + " - 使用说明"`（动态拼接） | 通用 | `ui/HelpScreen.java:28` |
| `com.example.addon.autochest.ui.ConfirmScreen` | `title`（调用处传 `"清空点位"` / `"清除记录"` / `"清空全部点位"` / `"清空方块稳定记录"` …） | 通用确认 | `autochest/ui/ConfirmScreen.java:26` |
| `com.example.addon.autochest.ui.ContainerTypeSelectScreen` | `setting.title` → `"容器类型"` | 自动箱子 | `autochest/ui/ContainerTypeSelectScreen.java:26`；名称定义 `autochest/config/AutoChestSettings.java:122-124` |
| `com.example.addon.autochest.ui.ItemQuantityScreen` | `setting.title` → `"每种物品数量"` | 自动箱子 | `autochest/ui/ItemQuantityScreen.java:27`；`AutoChestSettings.java:198-200` |
| `com.example.addon.enchant.ui.EnchantmentSelectScreen` | `setting.title`（`"原版近战附魔"` / `"原版工具附魔"` / `"原版弓附魔"` / `"原版钓竿附魔"` / `"原版三叉戟附魔"` / `"原版弩附魔"` / `"原版通用附魔"` / 自定义附魔组名） | 自动附魔 | `enchant/ui/EnchantmentSelectScreen.java:23`；名称 `AutoEnchantBook.java:555-560,592` |
| `com.example.addon.enchant.gear.GearEnchantScreen` | `"原版装备附魔配置"`（字面量） | 自动附魔 | `enchant/gear/GearEnchantScreen.java:44` |
| `com.example.addon.itemid.ItemTargetSelectScreen` | `setting.title` → `"目标物品"` | ID 配置管理 | `itemid/ItemTargetSelectScreen.java:27`；`AutoChestSettings.java:191-193` |
| `com.example.addon.itemid.IdAddScreen` | `"添加物品"`（字面量） | ID 配置管理 | `itemid/IdAddScreen.java:40` |
| `com.example.addon.itemid.IdManagementScreen` | `"ID 更多管理"`（字面量） | ID 配置管理 | `itemid/IdManagementScreen.java:27` |
| `com.example.addon.itemid.IdDataCleanScreen` | `"ID 数据清理"`（字面量） | ID 配置管理 | `itemid/IdDataCleanScreen.java:29` |
| `com.example.addon.itemid.IdResultScreen` | `"识别结果"`（字面量） | ID 识别 | `itemid/IdResultScreen.java:34` |
| `com.example.addon.itemid.EntityResultScreen` | `"实体识别结果"`（字面量） | ID 识别 | `itemid/EntityResultScreen.java:26` |
| `com.example.addon.worldblock.BlockResultScreen` | `"方块识别结果"`（字面量） | 方块识别 | `worldblock/BlockResultScreen.java:34` |
| `com.example.addon.stardew.ui.StardewConsoleScreen` | `"星露谷农场控制台"`（字面量） | 星露谷 | `stardew/ui/StardewConsoleScreen.java:58` |
| `com.example.addon.stardew.selector.StardewRenderObjectScreen` | `"渲染设置 · " + setting.objectName()`（动态） | 星露谷 | `stardew/selector/StardewRenderObjectScreen.java:37` |
| `com.example.addon.stardew.selector.StardewTargetSelectScreen` | `"选择" + setting.category.title()`（如「选择种植作物 / 种植盆 / 肥料 / 魔法药剂 / 水壶 / 洒水器」） | 星露谷 | `stardew/selector/StardewTargetSelectScreen.java:81`；类别名 `StardewFarmModule.java:269-274` |

补充：`StardewRenderObjectScreen.java:73` 会打开第三方原生 `ColorSettingScreen`（标题由第三方按 ColorSetting 名称生成）。

---

## 三、自研控件类清单

| 控件类（完整包名） | 提供的按钮 / 可点击元素 | 文件:行号 |
| --- | --- | --- |
| `com.example.addon.core.YiyiaddonModule` | 统一入口 `addUniformButton(theme, table, title, action)`（铺满整行 WButton）；说明面板骨架 | `core/YiyiaddonModule.java:280-284` |
| `com.example.addon.ui.HelpScreen` | `关闭` | `ui/HelpScreen.java:51` |
| `com.example.addon.autochest.ui.ConfirmScreen` | 确认（文案参数 `confirmLabel`，默认 `§c§l确认`）+ `§7取消` | `autochest/ui/ConfirmScreen.java:40,45` |
| `com.example.addon.autochest.config.ContainerTypeSetting` | `选择容器类型` + `GuiRenderer.RESET` | `autochest/config/ContainerTypeSetting.java:77,80` |
| `com.example.addon.autochest.config.ItemQuantitySetting` | `配置每种物品数量` + `GuiRenderer.RESET` | `autochest/config/ItemQuantitySetting.java:119,122` |
| `com.example.addon.autochest.config.InfoTextSetting` | 纯展示标签，**无按钮** | `autochest/config/InfoTextSetting.java:59-65` |
| `com.example.addon.itemid.ItemTargetSetting` | `选择目标物品` + `GuiRenderer.RESET` | `itemid/ItemTargetSetting.java:91,94` |
| `com.example.addon.enchant.ui.EnchantmentSelectSetting` | `选择附魔` + `GuiRenderer.RESET` + 附魔书图标 | `enchant/ui/EnchantmentSelectSetting.java:56,59` |
| `com.example.addon.enchant.ui.CustomEnchantSetting` | 无自研按钮（复用第三方 `StringListSetting.fillTable`） | `enchant/ui/CustomEnchantSetting.java:40` |
| `com.example.addon.enchant.gear.GearEnchantSetting` | `配置装备附魔`（execute 延迟弹窗）+ `GuiRenderer.RESET` + 装备图标 | `enchant/gear/GearEnchantSetting.java:55,57` |
| `com.example.addon.stardew.selector.StardewTargetSetting` | `点击选择` + `GuiRenderer.RESET` | `stardew/selector/StardewTargetSetting.java:160,167` |
| `com.example.addon.stardew.selector.StardewRenderSetting` | `WCheckbox` 显示开关 + `设置` | `stardew/selector/StardewRenderSetting.java:62,66` |
| `com.example.addon.stardew.selector.StardewResourceSetting` | 检测/更新资源、`§b打开控制台`、查看资源包、`§e查看使用说明`（前两者文案动态 `set()`） | `stardew/selector/StardewResourceSetting.java:179,191,198,208` |
| `com.example.addon.stardew.selector.StardewCropLogisticsSetting` | `WIntEdit` 数值输入 + 每作物 `GuiRenderer.RESET` + `LeftAlignedSection` 折叠标题 | `stardew/selector/StardewCropLogisticsSetting.java:198,288,345` |
| `com.example.addon.stardew.selector.LeftAlignedSection` | 自研可折叠区块：`theme.triangle()` 三角按钮（点击整行切换） | `stardew/selector/LeftAlignedSection.java:48-50` |
| `com.example.addon.stardew.selector.WFixedCell` | 固定槽位列容器（自身无按钮） | `stardew/selector/WFixedCell.java:20` |
| `com.example.addon.stardew.selector.WValueCell` | 值列容器（按像素换行，自身无按钮） | `stardew/selector/WValueCell.java:46` |
| `com.example.addon.stardew.selector.StardewPreview` | 图标预览静态工厂（无按钮） | `stardew/selector/StardewPreview.java:23` |
| `com.example.addon.stardew.ui.StardewSettingsRenderer` | 把 Setting 画进任意表（复用第三方控件工厂，自身无按钮） | `stardew/ui/StardewSettingsRenderer.java:27-39` |
| `com.example.addon.hud.HudExample` | HUD 元素，仅渲染文字，**无可点击元素** | `hud/HudExample.java:14-51` |

`theme.plus()/minus()`、`theme.checkbox()`、`theme.dropdown()`、`theme.intEdit()` 均为第三方原生控件，但被上表自研控件大量使用（引用点：`enchant/ui/EnchantmentSelectScreen.java:48,56`、`itemid/ItemTargetSelectScreen.java:72,86`、`itemid/IdConfigModule.java:337,345,384`、`stardew/selector/StardewTargetSelectScreen.java:318,322`、`enchant/gear/GearEnchantScreen.java:221,229`、`stardew/selector/StardewRenderSetting.java:62`、`stardew/selector/StardewRenderObjectScreen.java:47,65`、`stardew/selector/LeftAlignedSection.java:48`）。

---

## 四、二次确认窗清单

统一走 `com.example.addon.autochest.ui.ConfirmScreen`（构造 `:20-30`）。确认按钮默认 `§c§l确认`，取消固定 `§7取消`，可覆盖确认文案。

| 前置按钮 | 窗口标题 | 正文（逐字，`\n` 为换行） | 确认按钮 | 文件:行号 |
| --- | --- | --- | --- | --- |
| 自动箱子 `清空全部点位` | `清空点位` | `确定要清空全部标点吗？此操作不可恢复。` | `§c§l确认` | `autochest/AutoChestModule.java:410-411` |
| 自动箱子 `清除当前维度处理记录` | `清除记录` | `确定清除当前维度的已处理记录吗？` | `§c§l确认` | `:416-417` |
| 自动箱子 `清除全部处理记录` | `清除记录` | `确定清除全部服务器的已处理记录吗？` | `§c§l确认` | `:419-420` |
| 星露谷控制台 `§c清空全部点位` | `清空全部点位` | `§f将删除当前服务器已绑定的全部点位\n§7农田起点 · 农田终点 · 种子箱 · 成品箱 · 补水点 · 洒水器\n\n§c此操作不可恢复。` | `§c§l确认` | `stardew/ui/StardewConsoleScreen.java:227-231` |
| ID 清理 `§c清空方块稳定记录` | `清空方块稳定记录` | `§c§l将删除 <n> 条方块稳定记录\n§7目录：AutoChest/blocks/\n§7不包含历史状态快照（block-snapshots/）\n§c此操作不可撤销` | `§c§l确认清空` | `itemid/IdConfigModule.java:523-527` |
| ID 清理 `§c清空方块历史快照` | `清空方块历史快照` | `§c§l将删除 <n> 条方块历史状态快照\n§7目录：AutoChest/block-snapshots/\n§7不包含方块稳定记录（blocks/）\n§c此操作不可撤销` | `§c§l确认清空` | `:532-536` |
| ID 清理 `§c清空全部方块数据` | `清空全部方块数据` | `§c§l将删除 <nb> 条方块稳定记录 + <ns> 条历史快照\n§7目录：AutoChest/blocks/ 和 AutoChest/block-snapshots/\n§7不涉及物品 / 实体 / AutoChest 配置 / 农场点位\n§c此操作不可撤销` | `§c§l确认清空` | `:542-546` |
| ID 清理 `§c清空全部物品 ID` | `清空全部物品 ID` | `§c§l将删除 <n> 条物品 ID\n§7目录：AutoChest/items/\n§7不涉及实体 / 方块 / 快照 / 箱子配置 / 农场点位\n§c此操作不可撤销` | `§c§l确认清空` | `:587-591` |
| ID 清理 `§c清空全部实体 ID` | `清空全部实体 ID` | `§c§l将删除 <n> 条实体 ID\n§7目录：AutoChest/entities/\n§7不涉及物品 / 方块 / 快照 / 箱子配置 / 农场点位\n§c此操作不可撤销` | `§c§l确认清空` | `:596-600` |
| ID 清理 `§c§l【最高危险】清空全部 ID 数据` | `清空全部 ID 数据` | `§c§l【最高危险】将清空全部 ID 数据\n§7物品 <ni> 条 + 实体 <ne> 条 + 方块 <nb> 条 + 快照 <ns> 条\n§7范围：AutoChest/items/ + entities/ + blocks/ + block-snapshots/\n§7不删除：AutoChest 配置/点位、AutoFarm、.fumo、资源包、Profile 等\n§c此操作不可撤销` | `§c§l确认全部清空` | `:609-614` |

---

## 五、死按钮 / 占位按钮排查

**结论：未发现点击回调为空的按钮。** 全包检索 `-> {}`、`-> { }`、`action = () -> {}` 无一命中按钮回调；`IdManagementScreen.java:28-34`、`IdDataCleanScreen.java:30-36` 甚至用 `Objects.requireNonNull(...)` 强制校验每个按钮回调非空。

检索到的空 lambda 均与按钮无关：`enchant/ui/CustomEnchantSetting.java:24` 的 `values -> {}` 是 `StringListSetting` 的 `onChanged` 回调；`stardew/task/StardewCoordinator.java:180`、`stardew/status/StardewStatusReporter.java:46`、`autofarm/controller/FarmController.java:55,100` 均为日志/监听默认实现。

**存在的「条件门控」（回调非空，但特定状态下静默无反馈，建议迁移时补提示）**：

| 按钮 | 条件 | 现象 | 文件:行号 |
| --- | --- | --- | --- |
| 星露谷资源面板 检测/更新 | `!ServerResourceService.resourceActionsAllowed()` 或 `isBusy()` | 静默 return，仅 tooltip 说明 | `stardew/selector/StardewResourceSetting.java:179-185` |
| 自动登入 `立即重连` | `!reconnectHandler.isScheduled()` | 静默 return | `autologin/AutoLoginModule.java:200-203` |
| 自动农场 `§a关闭水源显示 / §b打开水源显示` | `waterModule == null` | 点击无任何反应 | `autofarm/AutoFarmMatrix.java:707-713` |
| 自动挖矿 `§c删除` | 未绑定（`!isBound`） | 静默 return | `mining/AutoMinerModule.java:1602-1608` |
| 自动附魔 `§c删除` | 未绑定（`!isBound`） | 静默 return | `enchant/AutoEnchantBook.java:711-717` |
| 自动村民交易 `§c删除` | 未绑定（`!isBound`） | 静默 return | `villager/AutoVillagerTradeModule.java:815-821` |

对照：自动箱子 `§c删除`（`autochest/AutoChestModule.java:437-446`）与自动农场位置卡片删除（`autofarm/AutoFarmMatrix.java:787-791`）在失败时会给出提示。

---

## 六、与新项目现状的对照

铁律 136 登记的「弹出独立窗口」按钮共 20 行（含 11 处 `§e查看使用说明` 来源模块），当前落地状态：

| 项 | 旧项目 | 新项目现状 |
| --- | --- | --- |
| `更多管理` → `ID 更多管理` | `IdConfigModule.java:126` | 已落地（`IdManagementScreen`） |
| `§e使用说明` → `<模块名> - 使用说明` | `IdConfigModule.java:171` | 已落地（`HelpPanelScreen`） |
| `手动添加物品 ID` → `添加物品` | `IdManagementScreen.java:41` | 已落地（`IdAddScreen`） |
| `§c数据清理 >` → `ID 数据清理` | `IdManagementScreen.java:48` | 已落地（`IdDataCleanScreen`） |
| 6 项清空 → 二次确认窗 | `IdDataCleanScreen.java:43-52` | 已落地（`ConfirmPanelScreen`） |
| 识别结果三窗 | `IdResultScreen` / `EntityResultScreen` / `BlockResultScreen` | 已落地（`IdResultScreen` / `IdEntityResultScreen` / `IdBlockResultScreen`） |
| `选择目标物品` → `目标物品` 窗口 | `itemid/ItemTargetSetting.java:91` → `ItemTargetSelectScreen` | **未落地**：数据层 `config/identity/IdentityTargetConfig` 已齐，缺 UI 入口；旧窗口在新项目**无对应物**。**该入口是「ID 三件套 ⇄ 自动箱子」联动链的必经一环**（旧项目由 `AutoChestSettings.java:191-195` 的「目标物品」配置项承载，消费方自动箱子取物时按选中身份匹配，见《基准-05》§0.8），不是可选项 |
| `配置每种物品数量` / `选择容器类型` / `选择附魔` / `配置装备附魔` / `设置` / `点击选择` | 自动箱子与星露谷、自动附魔 | 触发模块本身未迁移，随模块落地时接入 |
| 通用二次确认文案 | 默认 `§c§l确认` / `§7取消`；清空类 `§c§l确认清空` / `§c§l确认全部清空` | 已对齐（`ConfirmPanelScreen`） |
| 识别结果窗按钮组 | 5 / 4 / 5 个按钮（复制、另存快照、保存并关闭…） | 已落地（文案逐字对齐） |

**迁移硬性要求（据本文件）**：

1. 本文件第一章所有按钮，凡触发模块已迁移的，**逐字对齐按钮文案与目标窗口标题**；
2. 「点击后弹独立窗口」一律弹独立窗口，禁止改成同屏展开、聊天栏输出或内嵌面板（铁律 136）；
3. `配置装备附魔` 走「延迟一 tick 再弹屏」（`Minecraft.getInstance().execute(...)`），迁移时保留该时序；
4. 条件门控按钮（第五章）在新项目应补「不可用原因」提示，不得沿用静默 return；
5. 二次确认正文（第四章）含目录名与「不涉及/不删除」范围声明，迁移时逐字保留，不得简化。

---

## 七、本次未覆盖 / 存疑项

1. 第三方 `meteordevelopment.meteorclient.*` 源码未扫描（其 `WButton`、`StringListSetting.fillTable` 的添加/删除/重置、`ColorSettingScreen` 的确认/取消不属自有包）；其中 `ColorSettingScreen` 由 `StardewRenderObjectScreen.java:73` 打开，已在第二章注明。
2. `mixin` 包只做注入，不新增按钮（`ButtonBuilderTranslationMixin`、`AbstractWidgetMessageTranslationMixin` 等仅翻译文本；已逐一确认无 `setScreen` / 按钮构造）。
3. 命令类未计入按钮表：各 `*Command` 为聊天指令入口，会间接触发窗口（如 `IdCommand.java:141/181/224` 用 `mc.execute` 弹结果屏、`StardewCommand.java:498` 用 `execute` 打开控制台），但不属「配置页按钮」。
4. `IdIdentifyModule` 的结果屏非按钮触发：`itemid/IdIdentifyModule.java:141`（弹 `IdResultScreen`）与 `:163`（弹 `BlockResultScreen`）由模块运行/识别流程触发。
5. `dropdown` / `intEdit` / `slider` 等第三方原生控件的内部点击未计入按钮表。
6. 星露谷季节人工绑定 / 洒水器世界绑定设置（`stardew/season/StardewSeasonManualBinding.java`、`stardew/point/SprinklerWorldBinding.java`）**无自研按钮**。
7. 资源包内语言文件与 `resources/**` 未统计：本文件只覆盖 Java 源码中的按钮文案；界面汉化后的显示文案需另扫资源文件。
8. `translations` 包（大量文本映射）属翻译注入，未纳入。
9. `autofarm/AutoFarmMatrix.java` 的 `addUniformButton` 覆盖已完成（主表列出 `§e查看使用说明` 与水源开关）；其说明面板纯文本段（`:802` 起）不含按钮。
