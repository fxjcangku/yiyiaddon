# 45 · 阶段11 · 附B · 自动附魔（enchant）界面与指令侦察记录

日期：2026-09-16

- 侦察对象：`d:\mcaddon\26.1.2\src\main\java\com\example\addon\` 下 `enchant` 包的**界面层与指令层**，外加其依赖的 `ui/HelpScreen.java`、`core/YiyiaddonModule.java`（播报与自检口）、`core/CommandMessageFormatter.java`（回执排版）、`core/AddonTemplate.java`（注册点）
- 侦察方式：**只读**，逐行照抄原文，未修改旧项目任何文件（旧项目严格只读）
- 路径前缀：`enchant/` = `…\com\example\addon\enchant\`；`ui/` = `…\com\example\addon\ui\`；`core/` = `…\com\example\addon\core\`
- 配套：逻辑层逐条见同批另一窗口产出的 `43-阶段11-附录A-逻辑层侦察记录.md`；决策底本见 [`38-阶段11-自动附魔移植侦察与决策.md`](38-阶段11-自动附魔移植侦察与决策.md)（含 §九 修订记录）与 [`38-附A-新窗口开工指令.md`](38-附A-新窗口开工指令.md)
- 本文**不重复上报**以下已知结论（用户已拍板）：点位 9 类、**无异常装备箱**；`.fumo` 前缀保留；`GearEnchantScreen` / `EnchantmentSelectScreen` **不做一比一交互复刻**，改控制台页
- 本文只写**界面、控件、设置项呈现、指令与回执、帮助/使用说明、播报入口**；状态机、算法、`selfCheck()` 判定与缺项文案由逻辑层那一份负责

---

## §0 更正与新发现

> 与 `38` 号报告的差异、以及本轮新实测到的事实，逐条带行号。**`38` 号已写对的条目不再重复。**

| # | 新事实（逐字） | 证据 |
| --- | --- | --- |
| 1 | **ESP 标点只渲染 6 个点位**：`书本箱 / 青金石箱 / 成品箱 / 附魔台 / 砂轮 / 挂机位`。GEAR 独有的 3 个点位（`EQUIPMENT_STORAGE` / `ANVIL` / `ANVIL_BOX`）**没有 ESP 标签** | `AutoEnchantBook.java:1596-1604`（对照点位读写 `:729-756`） |
| 2 | ESP 里书箱标签逐字是 `§a§l[书本箱]`，而卡片标题逐字是 `空白书箱`（`PointType.title()`）——**两处文案不同** | ESP `:1598`；卡片 `:679`；`point/PointType.java`（`title()`） |
| 3 | 配置页 `getWidget()` 里**只有一个按钮**（`§e查看使用说明`）+ 点位卡片区；`buildInfoWidget(theme, table -> {…})` **没有传任何说明章节** → 配置页正文没有任何说明文字 | `:602-619`（`buildInfoWidget` 重载见 `core/YiyiaddonModule.java:241-268`） |
| 4 | 点位卡片**全部塞进同一个 `WTable row`，无换行**：GEAR 模式 8 张卡排成一行 | `:611-617` |
| 5 | 卡片「设置」按钮：**设置成功才关界面**（`if (FumoCommand.setPoint(type)) mc.setScreen(null);`）——自动挖矿是无条件 `mc.setScreen(null)`，两模块行为不同 | `:700-707`（对照 `mining/AutoEnchantBook` 同族代码 `mining/AutoMinerModule.java:1591-1599`） |
| 6 | 卡片标题配色里 `ANVIL` 与 `OUTPUT_STORAGE` **同为 `§6`** | `:673`、`:676` |
| 7 | 156 个自定义附魔属性 Bool 的等级后缀**不全是 5**：`弓_禁锢6` 的名字逐字是 `禁锢 6`、`其他_催生1` 的名字逐字是 `催生 1` | `:254`、`:337` |
| 8 | `.fumo` 类 javadoc 的节点清单只列 **8** 个（漏 `铁砧箱`），且用的词是 `工具护甲箱`：`<书/青晶石/工具护甲箱/附魔台/砂轮/铁砧/挂机位/成品箱>` | `command/FumoCommand.java:28` |
| 9 | 帮助页「点位设置」把节点写成 `装备箱`，与指令字面量 `工具护甲箱` **不一致** | 帮助 `:793`；指令 `:49` |
| 10 | 帮助页「换服、换维度与指令」写「再重设**六个**点位」，而 GEAR 模式实际需要 **8** 个点位 | 帮助 `:832`；`requiredPoints` `:759-770` |
| 11 | 帮助页「注意」写「模块运行中无法修改点位，先关闭模块再设置」，但 `setPos()` **没有** `isActive()` 拦截（与挖矿同款漂移） | 帮助 `:839`；`FumoCommand.java:85-134` |
| 12 | 隐藏设置只有 **2** 个：`ESP标点`、`返回挂机视角`（都 `visible(() -> false)`，旧页面**无任何入口**、取值恒 `true`）。基础设置里其余项全部可见 | `:164-168` |
| 13 | `成功提示音` / `成功提示音类型` 是**可见**设置，且在同一 `sgBasic` 组内联动（后者 `visible(成功提示音::get)`） | `:170-176` |
| 14 | `记录合成日志` / `合成策略` 挂在 **`sgGear`（原版装备附魔页）**，不在基础设置 | `:451-462`（字段声明 `:449` / 加入组 `:563`） |
| 15 | `RunMode` 枚举显示名逐字是 `挂机循环` / `纯附魔模式`（不是「EXP/纯附魔」）：`EXPERIENCE("挂机循环")`、`DRAIN("纯附魔模式")`，默认 `EXPERIENCE` | `:482-496`、`:152-162` |
| 16 | 三个模式各有一个**独立**的运行模式设置项，各自 `visible` 只在对应模式显示 | `:152-162` |
| 17 | 点位**不是独立 JSON**：全部进模块 NBT（`modules.nbt`），键名逐字见 `toTag`；跨服务器/维度隔离靠 `pointServer` + `pointDimension` 两个键（非按服务器分文件） | `:1621-1659`、`:1681-1694` |
| 18 | `.fumo 移除` 成功后若**已无任何点位**，会同时清空 `pointServer` / `pointDimension` | `FumoCommand.java:161-165` |
| 19 | `.fumo 状态` **不输出维度**，9 个字段只给 X/Y/Z 或 `§c未设置`，末尾一条 `当前世界与点位匹配 / 不匹配` | `FumoCommand.java:202-220` |
| 20 | 切换目标模式后刷新页面走「延到下一 tick」+ 旧壳 `WidgetScreen.reload()`；`getWidget` 每次重建都重新读点位（不缓存） | `:625-629`、`:663` |
| 21 | 帮助页共 **8** 个章节（`首次配置 / 点位设置 §7(两种方式) / 运行流程 / 原版装备附魔 / 参数说明 / 原版附魔分类 / 换服、换维度与指令 / 注意`），全文逐字见 §7.2 | `:773-842` |
| 22 | `.fumo` 没有 TAB 补全（全部 literal 节点，无 `suggests`）；且**裸 `.fumo` 无 executes**（也没有 `.fumo 状态` 之外的根行为） | `FumoCommand.java:42-81` |
| 23 | 旧项目**自检没有任何界面呈现**（无弹窗、无跳转按钮、无窗口标题），全部走聊天 + 自动关模块 | `core/YiyiaddonModule.java:147-161`；调用点 `:857`、`:909` |
| 24 | 编号冲突（登记）：`38-附A` 的批次1 命名是 `43-阶段11-附录A` / `44-阶段11-附录B`，本轮指令要求本文件为 **45** | `38-附A-新窗口开工指令.md:77` |

---

## §1 界面层总览

### 1.1 四个「页面」在哪、怎么打开、由谁 `setScreen`

| 页面 | 类 / 入口 | 窗口标题（逐字） | 怎么打开 | `setScreen` 落点 |
| --- | --- | --- | --- | --- |
| ① 模块配置页 | `AutoEnchantBook.getWidget(theme)` | 由旧壳模块面板决定（模块名 `自动附魔`；注册在 `CATEGORY_AUTOMATION = new Category("§c§lyiyiaddon §e§l自动化", …)`） | 玩家在模块面板打开本模块 | 无（面板自己管） |
| ② 装备附魔设置页 | `gear/GearEnchantScreen` | `原版装备附魔配置` | 配置页里 `GearEnchantSetting` 的自定义控件按钮 `配置装备附魔` | `GearEnchantSetting.java:60-61`（**延到下一 tick**：`Minecraft.getInstance().execute(() -> Minecraft.getInstance().setScreen(new GearEnchantScreen(theme, setting)))`） |
| ③ 附魔选择页 | `ui/EnchantmentSelectScreen` | `setting.title`（构造 `:23`，即选择器名字本身，如 `剑附魔属性`；`Setting.title` = `Utils.nameToTitle(name)`，中文名逐字不变） | 五个「选择附魔」按钮 + 八个原版「选择附魔」按钮（`EnchantmentSelectSetting` 自定义控件） | `EnchantmentSelectSetting.java:60`（**立即** `Minecraft.getInstance().setScreen(new EnchantmentSelectScreen(theme, setting))`） |
| ④ 帮助页 | `ui/HelpScreen` | `module.title + " - 使用说明"` → `自动附魔 - 使用说明` | 配置页第一个按钮 `§e查看使用说明` | `AutoEnchantBook.java:606-607`（**立即** `mc.setScreen(new HelpScreen(theme, this, buildHelpContent()))`） |

模块构造（标题与描述逐字）：`super(AddonTemplate.CATEGORY_AUTOMATION, "自动附魔", "经验获取→定向附魔→极品剔除→洗练仓储全自动闭环。详细参考下面使用说明。")`（`:538-540`）。

### 1.2 配置页构建顺序（代码顺序，逐字骨架）

`AutoEnchantBook.java:602-619`：

```java
@Override
public WWidget getWidget(GuiTheme theme) {
    return buildInfoWidget(theme, table -> {
        // 使用说明按钮（置顶显眼位置）
        WButton helpBtn = theme.button("§e查看使用说明");
        helpBtn.action = () -> mc.setScreen(new HelpScreen(theme, this, buildHelpContent()));
        table.add(helpBtn).expandX().minWidth(200);
        table.row();

        // 点位卡片区：按当前目标模式动态显示对应点位按钮（三种模式互不污染）
        WTable row = theme.table();
        for (PointType type : requiredPoints(目标模式.get())) {
            buildPointCard(theme, row, type);
        }
        table.add(row).expandX();
        table.row();
    });
}
```

即页面纵向顺序固定为：

```text
[按钮] §e查看使用说明            （满宽 minWidth 200）
[一行 N 列的卡片区] requiredPoints(当前模式) 的每个点位一张卡（GEAR=8 张 / BOOK·CUSTOM=6 张）
——以下由旧框架按设置组顺序自动渲染（见 §2.0 顶层分组出现规则）——
基础设置 → （CUSTOM：自动附魔分类 → 自定义附魔 | BOOK：原版附魔分类 | GEAR：原版装备附魔）
```

> 注意：旧项目**没有分区标题文字**、**没有二次确认窗**、**没有「清空全部点位」按钮**；除 `§e查看使用说明` 外配置页没有别的按钮，其余交互全在卡片里。

### 1.3 点位卡片（`buildPointCard` `:660-721`，逐字）

每张卡片是一个垂直 `WTable`，固定 **5 行**（标题 / 坐标 / 维度 / 设置 / 删除），未绑定时用占位符保持等高：

| 行 | 已绑定 | 未绑定 | 行号 |
| --- | --- | --- | --- |
| 标题 | `titleColor + type.title()`，`expandX().center()` | 同左 | `:679-680` |
| 坐标 | `String.format("§7X§f%d §7Y§f%d §7Z§f%d", x, y, z)` | `§8暂未绑定` | `:683-687` / `:692-693` |
| 维度 | `dim != null ? "§7维度 §f" + dim : "§8-"` | `§8-` | `:688-690` / `:694-695` |
| 设置按钮 | `§a设置`（已绑定亮绿） | `§8设置`（未绑定暗灰） | `:699-708` |
| 删除按钮 | `§c删除`（始终存在） | `§c删除`（点击静默 return） | `:711-718` |

标题配色逐字（`:667-677`，与 ESP 标点色系同源）：

```java
String titleColor = switch (type) {
    case BOOK_STORAGE -> "§a";        // 空白书箱：绿色
    case LAPIS_STORAGE -> "§9";       // 青金石箱：蓝色
    case EQUIPMENT_STORAGE -> "§b";   // 工具/护甲箱：青色
    case ENCHANTING_TABLE -> "§d";    // 附魔台：粉色
    case GRINDSTONE -> "§7";          // 砂轮：灰色
    case ANVIL -> "§6";               // 铁砧：金色
    case ANVIL_BOX -> "§e";           // 铁砧箱：黄色
    case AFK -> "§c";                 // 挂机点：红色
    case OUTPUT_STORAGE -> "§6";      // 成品箱：金色
};
```

按钮行为逐字（`:700-718`）：

```java
String setBtnColor = isBound ? "§a" : "§8";
WButton setBtn = theme.button(setBtnColor + "设置");
setBtn.action = () -> {
    // 设置成功才关闭 GUI（失败保留界面让玩家重新对准）
    if (FumoCommand.setPoint(type)) {
        mc.setScreen(null);
    }
};
card.add(setBtn).expandX().center();
card.row();

// 删除按钮（红色）
WButton delBtn = theme.button("§c删除");
delBtn.action = () -> {
    if (isBound) {
        FumoCommand.removePoint(type);
        mc.setScreen(null);
    }
};
```

维度显示名：`WorldContextFormatter.dimensionSummary(pointDimension)`，`pointDimension == null` 时返回 `null`（`:723-726`）。

### 1.4 全部 `setScreen(...)` 调用点（穷尽）

| 触发 | 目标窗口 | 行号 |
| --- | --- | --- |
| 配置页 `§e查看使用说明` | `new HelpScreen(theme, this, buildHelpContent())` | `AutoEnchantBook.java:607` |
| 卡片 `§a设置` / `§8设置`（**成功才关**） | `mc.setScreen(null)` | `:704` |
| 卡片 `§c删除` | `mc.setScreen(null)` | `:715` |
| `GearEnchantSetting` 的 `配置装备附魔` | `new GearEnchantScreen(theme, setting)`（延一 tick） | `GearEnchantSetting.java:60-61` |
| `EnchantmentSelectSetting` 的 `选择附魔` | `new EnchantmentSelectScreen(theme, setting)` | `EnchantmentSelectSetting.java:60` |
| 帮助页 `关闭` | `mc.setScreen(null)` | `ui/HelpScreen.java:51-57` |

模式切换后的页面刷新（不是新窗口）：`刷新模式界面()` `:625-629`

```java
private void 刷新模式界面() {
    mc.execute(() -> {
        if (mc.screen instanceof WidgetScreen screen) screen.reload();
    });
}
```

### 1.5 自定义控件注册点（界面入口的接线）

`core/AddonTemplate.java:156-160`：

```java
// 自动附魔：经验获取→定向附魔→极品剔除→洗练仓储全自动闭环
EnchantmentSelectSetting.register();
GearEnchantSetting.register();
CustomEnchantSetting.register();
Modules.get().add(new AutoEnchantBook());
```

指令注册：`core/AddonTemplate.java:220` — `Commands.add(new FumoCommand());           // 自动附魔坐标管理指令`

---

## §2 全部设置项逐条穷尽（18 个分组）

> 顺序 = 旧源码 `add(...)` 的**代码顺序**，即界面渲染顺序。类型列为旧框架设置类名（复刻时换本项目控件）。

### 2.0 18 个分组（逐字声明）+ 顶层出现规则 + 13 个子组挂载

声明逐字（`AutoEnchantBook.java:94-111`）：

```java
private final SettingGroup sgBasic    = settings.createGroup("基础设置");
private final SettingGroup sgAdvanced = settings.createGroup("自动附魔分类");
private final SettingGroup sgCustom   = settings.createGroup("自定义附魔");
private final SettingGroup sgVanilla  = settings.createGroup("原版附魔分类");
private final SettingGroup sgSword    = settings.createGroup("剑附魔属性");
private final SettingGroup sgAxe      = settings.createGroup("斧头附魔属性");
private final SettingGroup sgBow      = settings.createGroup("弓附魔属性");
private final SettingGroup sgArmor    = settings.createGroup("护甲附魔属性");
private final SettingGroup sgOther    = settings.createGroup("工具与通用附魔属性");
private final SettingGroup sgVanillaArmor    = settings.createGroup("原版防具附魔");
private final SettingGroup sgVanillaMelee    = settings.createGroup("原版近战附魔");
private final SettingGroup sgVanillaTool     = settings.createGroup("原版工具附魔");
private final SettingGroup sgVanillaBow      = settings.createGroup("原版弓附魔");
private final SettingGroup sgVanillaFishing  = settings.createGroup("原版钓竿附魔");
private final SettingGroup sgVanillaTrident  = settings.createGroup("原版三叉戟附魔");
private final SettingGroup sgVanillaCrossbow = settings.createGroup("原版弩附魔");
private final SettingGroup sgVanillaCommon   = settings.createGroup("原版通用附魔");
private final SettingGroup sgGear            = settings.createGroup("原版装备附魔");
```

| # | 分组名（逐字） | 字段 | 层级 | 顶层可见性 |
| --- | --- | --- | --- | --- |
| 1 | `基础设置` | `sgBasic` | 顶层 | **恒在** |
| 2 | `自动附魔分类` | `sgAdvanced` | 顶层 | 仅 CUSTOM |
| 3 | `自定义附魔` | `sgCustom` | 顶层 | 仅 CUSTOM |
| 4 | `原版附魔分类` | `sgVanilla` | 顶层 | 仅 BOOK |
| 5 | `原版装备附魔` | `sgGear` | 顶层 | 仅 GEAR |
| 6 | `剑附魔属性` | `sgSword` | 子组（挂 `sgAdvanced`） | 选择器替身 |
| 7 | `斧头附魔属性` | `sgAxe` | 子组（挂 `sgAdvanced`） | 同上 |
| 8 | `弓附魔属性` | `sgBow` | 子组（挂 `sgAdvanced`） | 同上 |
| 9 | `护甲附魔属性` | `sgArmor` | 子组（挂 `sgAdvanced`） | 同上 |
| 10 | `工具与通用附魔属性` | `sgOther` | 子组（挂 `sgAdvanced`） | 同上 |
| 11 | `原版防具附魔` | `sgVanillaArmor` | 子组（挂 `sgVanilla`） | 同上 |
| 12 | `原版近战附魔` | `sgVanillaMelee` | 子组（挂 `sgVanilla`） | 同上 |
| 13 | `原版工具附魔` | `sgVanillaTool` | 子组（挂 `sgVanilla`） | 同上 |
| 14 | `原版弓附魔` | `sgVanillaBow` | 子组（挂 `sgVanilla`） | 同上 |
| 15 | `原版钓竿附魔` | `sgVanillaFishing` | 子组（挂 `sgVanilla`） | 同上 |
| 16 | `原版三叉戟附魔` | `sgVanillaTrident` | 子组（挂 `sgVanilla`） | 同上 |
| 17 | `原版弩附魔` | `sgVanillaCrossbow` | 子组（挂 `sgVanilla`） | 同上 |
| 18 | `原版通用附魔` | `sgVanillaCommon` | 子组（挂 `sgVanilla`） | 同上 |

**顶层分组出现规则（`同步模式分组()` `:635-649`，逐字）**：

```java
private void 同步模式分组() {
    TargetMode mode = 目标模式.get();
    List<SettingGroup> ordered = new ArrayList<>();
    ordered.add(sgBasic);
    if (mode == TargetMode.CUSTOM) ordered.add(sgAdvanced);
    if (mode == TargetMode.CUSTOM) ordered.add(sgCustom);
    if (mode == TargetMode.BOOK) ordered.add(sgVanilla);
    if (mode == TargetMode.GEAR) ordered.add(sgGear);
    // 幂等保护：顺序一致时不重建列表。
    // 否则目标模式 onChanged 在 Modules.load / Settings.fromTag 遍历 groups 时同步清空重建，
    // 会触发 ConcurrentModificationException 导致启动闪退。
    if (settings.groups.equals(ordered)) return;
    settings.groups.clear();
    settings.groups.addAll(ordered);
}
```

→ 顶层同时只出现 **2~3 个**（`基础设置` + 当前模式的 1~2 个）。初始调用点：`onChanged`（`:116`）、构造末尾（`:585`）、`onActivate`（`:854`）。

**13 个子组挂载（`addSelector` 实参逐字）**：

`AutoEnchantBook.java:541-545`（CUSTOM，5 次，返回值存入字段）：

```java
剑附魔选择 = addSelector("剑附魔属性", sgSword, sgAdvanced, () -> 目标模式.get() == TargetMode.CUSTOM);
斧头附魔选择 = addSelector("斧头附魔属性", sgAxe, sgAdvanced, () -> 目标模式.get() == TargetMode.CUSTOM);
弓附魔选择 = addSelector("弓附魔属性", sgBow, sgAdvanced, () -> 目标模式.get() == TargetMode.CUSTOM);
护甲附魔选择 = addSelector("护甲附魔属性", sgArmor, sgAdvanced, () -> 目标模式.get() == TargetMode.CUSTOM);
工具通用附魔选择 = addSelector("工具与通用附魔属性", sgOther, sgAdvanced, () -> 目标模式.get() == TargetMode.CUSTOM);
```

`AutoEnchantBook.java:554-561`（BOOK，8 次，不存字段）：

```java
addSelector("原版防具附魔", sgVanillaArmor, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
addSelector("原版近战附魔", sgVanillaMelee, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
addSelector("原版工具附魔", sgVanillaTool, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
addSelector("原版弓附魔", sgVanillaBow, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
addSelector("原版钓竿附魔", sgVanillaFishing, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
addSelector("原版三叉戟附魔", sgVanillaTrident, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
addSelector("原版弩附魔", sgVanillaCrossbow, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
addSelector("原版通用附魔", sgVanillaCommon, sgVanilla, () -> 目标模式.get() == TargetMode.BOOK);
```

`addSelector` 实现（`:592-600`，逐字）——**第二参（源组）的每个 `BoolSetting` 被收进一个 `EnchantmentSelectSetting`，选择器再加进第三参（目标组）**：

```java
private EnchantmentSelectSetting addSelector(String name, SettingGroup source, SettingGroup target, IVisible visible) {
    List<BoolSetting> values = new ArrayList<>();
    for (Setting<?> setting : source) if (setting instanceof BoolSetting boolSetting) values.add(boolSetting);
    List<String> names = new ArrayList<>();
    for (BoolSetting setting : values) names.add(setting.name);
    EnchantmentSelectSetting selector = new EnchantmentSelectSetting(name, names, values, visible);
    target.add(selector);
    return selector;
}
```

13 个子组从 `settings.groups` 里被摘掉（`:571-583`，逐字 13 次 `settings.groups.remove(...)`），折叠初始态（`:564-570`）：`sgAdvanced.sectionExpanded = true; sgVanilla.sectionExpanded = false; sgGear.sectionExpanded = true; sgSword/sgAxe/sgBow/sgArmor.sectionExpanded = false;`

设置项总数实测（`Select-String` 计数，见 §11）：`sgBasic 15 / sgCustom 1 / sgSword 54 / sgAxe 5 / sgBow 34 / sgArmor 50 / sgOther 13 / sgGear 3 / 原版 8 组共 33 / 选择器 13` → **221 个设置项**。

### 2.1 基础设置（`sgBasic`，15 项，`AutoEnchantBook.java:113-176`）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 取值域 / 可见性 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `EnumSetting<TargetMode>` | `:114-117` | `目标模式` | `原版装备附魔 / 原版附魔书 / 自定义附魔，三模式互斥切换` | `TargetMode.BOOK` | 三枚举；`onChanged(mode -> { 同步模式分组(); 刷新模式界面(); })` |
| 2 | `IntSetting` | `:119-121` | `单轮抽取次数` | `挂机循环每轮附魔最大次数，纯附魔模式忽略此项` | `10` | min 1 / max 100 / `noSlider` / `visible(目标模式 != GEAR)` |
| 3 | `IntSetting` | `:123-124` | `GUI操作延迟(Tick)` | `所有 GUI 点击之间的等待 Tick 数` | `1` | min 1 / max 10 / `noSlider` |
| 4 | `IntSetting` | `:126-128` | `发包打开距离` | `发包开箱/开附魔台/开砂轮/开铁砧允许的最大距离（格），超出则先寻路靠近` | `6` | min 1 / max 32 / `noSlider` |
| 5 | `IntSetting` | `:130-132` | `书本补给组数` | `每次去书箱抓取的组数（1组=64本）` | `1` | min 1 / max 10 / `noSlider` / `visible(目标模式 != GEAR)` |
| 6 | `IntSetting` | `:134-135` | `青金石补给组数` | `每次去青金石箱抓取的组数（1组=64个）` | `1` | min 1 / max 10 / `noSlider` |
| 7 | `IntSetting` | `:137-139` | `每批取用数量` | `每次任务最多从装备箱取用的目标装备数量，铁砧合并会消耗装备，最终完成数可能小于此值` | `4` | min 1 / max 16 / `noSlider` / `visible(目标模式 == GEAR)` |
| 8 | `IntSetting` | `:141-143` | `极品附魔数量` | `本次运行最终产出的极品装备数量，达到后自动停机` | `1` | min 1 / max 64 / `noSlider` / `visible(目标模式 == GEAR)` |
| 9 | `EnumSetting<RunMode>` | `:152-154` | `装备运行模式` | `原版装备附魔：纯附魔只消耗当前经验，不足则停机；挂机循环前往挂机点刷经验` | `RunMode.EXPERIENCE` | `visible(目标模式 == GEAR)` |
| 10 | `EnumSetting<RunMode>` | `:156-158` | `附魔书运行模式` | `原版附魔书：纯附魔只消耗当前经验，不足则停机；挂机循环前往挂机点刷经验` | `RunMode.EXPERIENCE` | `visible(目标模式 == BOOK)` |
| 11 | `EnumSetting<RunMode>` | `:160-162` | `自定义运行模式` | `自定义附魔：纯附魔只消耗当前经验，不足则停机；挂机循环前往挂机点刷经验` | `RunMode.EXPERIENCE` | `visible(目标模式 == CUSTOM)` |
| 12 | `BoolSetting` | `:164-165` | `ESP标点` | `显示已设置点位的名称` | `true` | `visible(() -> false)` —— **恒隐藏** |
| 13 | `BoolSetting` | `:167-168` | `返回挂机视角` | `到达挂机位后恢复设置该点位时记录的视角` | `true` | `visible(() -> false)` —— **恒隐藏** |
| 14 | `BoolSetting` | `:170-172` | `成功提示音` | `达成目标时播放本地提示音（命中附魔书 / 装备达成极品）` | `true` | — |
| 15 | `EnumSetting<SuccessSound>` | `:174-176` | `成功提示音类型` | `选择达成目标时播放的音效` | `SuccessSound.CHALLENGE_COMPLETE` | `visible(成功提示音::get)` |

三个模式运行模式的可选值（`RunMode` `:482-496` 逐字）：

```java
public enum RunMode {
    DRAIN("纯附魔模式"),
    EXPERIENCE("挂机循环");
```

`成功提示音类型` 的 12 个可选值（`SuccessSound` `:512-524` 逐字）：

```text
挑战完成 / 升级 / 附魔台 / 音符叮 / 钟声 / 烟花 / 经验球 / 村民庆祝 / 三叉戟雷鸣 / 暴击 / 猫叫 / 雷声
```

`目标模式` 的 3 个可选值（`TargetMode` `:465-480` 逐字）：

```text
原版装备附魔 / 原版附魔书 / 自定义附魔
```

### 2.2 自定义附魔（`sgCustom`，1 项，`:145-149`）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 可见性 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `CustomEnchantSetting`（继承 `StringListSetting`） | `:145-149` | `自定义附魔目标` | `每行填写一个附魔名称和等级，例如：打雷 5。支持中文、阿拉伯数字、罗马数字和不带等级的附魔。` | `List.of()` 空列表 | `visible(() -> 目标模式.get() == TargetMode.CUSTOM)` |

### 2.3 自动附魔分类（`sgAdvanced`，5 个选择器）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 可见性 | 源组（被收起） | 项数 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `EnchantmentSelectSetting` | `:541` | `剑附魔属性` | `选择需要收集的附魔属性` | 空（源 Bool 全 `false`） | `目标模式 == CUSTOM` | `sgSword` | 54 |
| 2 | `EnchantmentSelectSetting` | `:542` | `斧头附魔属性` | 同上 | 空 | 同上 | `sgAxe` | 5 |
| 3 | `EnchantmentSelectSetting` | `:543` | `弓附魔属性` | 同上 | 空 | 同上 | `sgBow` | 34 |
| 4 | `EnchantmentSelectSetting` | `:544` | `护甲附魔属性` | 同上 | 空 | 同上 | `sgArmor` | 50 |
| 5 | `EnchantmentSelectSetting` | `:545` | `工具与通用附魔属性` | 同上 | 空 | 同上 | `sgOther` | 13 |

> 描述逐字来源：`ui/EnchantmentSelectSetting.java:35`（`"选择需要收集的附魔属性"`）。

### 2.4 原版附魔分类（`sgVanilla`，8 个选择器 + 33 条源 Bool）

| # | 类型 | 行号 | 设置名（逐字） | 源组 | 项数 | 可见性 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `EnchantmentSelectSetting` | `:554` | `原版防具附魔` | `sgVanillaArmor` | 9 | `目标模式 == BOOK` |
| 2 | `EnchantmentSelectSetting` | `:555` | `原版近战附魔` | `sgVanillaMelee` | 7 | 同上 |
| 3 | `EnchantmentSelectSetting` | `:556` | `原版工具附魔` | `sgVanillaTool` | 3 | 同上 |
| 4 | `EnchantmentSelectSetting` | `:557` | `原版弓附魔` | `sgVanillaBow` | 4 | 同上 |
| 5 | `EnchantmentSelectSetting` | `:558` | `原版钓竿附魔` | `sgVanillaFishing` | 2 | 同上 |
| 6 | `EnchantmentSelectSetting` | `:559` | `原版三叉戟附魔` | `sgVanillaTrident` | 4 | 同上 |
| 7 | `EnchantmentSelectSetting` | `:560` | `原版弩附魔` | `sgVanillaCrossbow` | 3 | 同上 |
| 8 | `EnchantmentSelectSetting` | `:561` | `原版通用附魔` | `sgVanillaCommon` | 1 | 同上 |

源 Bool 由 `addTargets(...)` 批量生成（调用 `:546-553` 逐字，实现 `:588-590`——**循环创建，无逐项行号**）：

```java
addTargets(sgVanillaArmor, "保护 IV", "火焰保护 IV", "摔落缓冲 IV", "爆炸保护 IV", "弹射物保护 IV", "水下呼吸 III", "水下速掘", "荆棘 II", "深海探索者 III");
addTargets(sgVanillaMelee, "锋利 IV", "亡灵杀手 IV", "节肢杀手 IV", "击退 II", "火焰附加 II", "抢夺 III", "横扫之刃 III");
addTargets(sgVanillaTool, "效率 IV", "精准采集", "时运 III");
addTargets(sgVanillaBow, "力量 IV", "冲击 II", "火矢", "无限");
addTargets(sgVanillaFishing, "海之眷顾 III", "饵钓 III");
addTargets(sgVanillaTrident, "忠诚 III", "穿刺 V", "激流 III", "引雷");
addTargets(sgVanillaCrossbow, "多重射击", "快速装填 III", "穿透 IV");
addTargets(sgVanillaCommon, "耐久 III");

private void addTargets(SettingGroup group, String... names) {
    for (String name : names) group.add(new BoolSetting.Builder().name(name).defaultValue(false).build());
}
```

> 33 条源 Bool 全部：`BoolSetting` / 无描述 / 默认 `false` / 无取值域 / 无 `visible` / 属于对应 `sgVanilla*` 子组 / 行号均为 `:588-590`（调用点 `:546-553`）。

### 2.5 原版装备附魔（`sgGear`，3 项）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 取值域 / 可见性 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `BoolSetting` | `:451-455` | `记录合成日志` | `开启后记录原版装备附魔的完整合成过程，生成彩色 HTML 报告到 .minecraft/yiyiaddon-reports/装备附魔合成/` | `false` | — |
| 2 | `EnumSetting<AnvilPlanner.Strategy>` | `:458-462` | `合成策略` | `铁砧装备+装备合并排序策略：简单=贡献优先、节能=低惩罚+低成本优先、快速=提升优先少步骤，用于对比经验消耗` | `AnvilPlanner.Strategy.SAVE_XP` | `简单 / 节能 / 快速` |
| 3 | `GearEnchantSetting`（继承 `StringListSetting`） | 字段声明 `:449`，构造+入组 `:562-563` | `装备附魔配置` | `配置原版装备的极品附魔目标` | 空列表 | `visible(() -> 目标模式.get() == TargetMode.GEAR)` |

`AnvilPlanner.Strategy` 显示名逐字（`gear/AnvilPlanner.java:50-56`）：`SIMPLE("简单")` / `SAVE_XP("节能")` / `FAST("快速")`。

### 2.6 五个自定义附魔属性子组（156 项，全部 `BoolSetting` / 无描述 / 默认 `false` / 无取值域 / 无 `visible`）

> 共同点：类型 `BoolSetting`，`name("…")` + `defaultValue(false)`，无 `description`（旧框架默认空串），无 `min/max`，无 `visible`；**不直接出现在页面上**，由 §2.3 的 5 个选择器聚合。

**（a）`剑附魔属性`（`sgSword`，54 项，`:180-234`）**

| 名称（逐字） | 行号 | | 名称（逐字） | 行号 |
| --- | --- | --- | --- | --- |
| `双刃剑 5` | `:180` | | `涅槃 5` | `:228` |
| `背刺 5` | `:181` | | `心灵感应 5` | `:229` |
| `饕餮 5` | `:182` | | `归一 5` | `:230` |
| `定身 5` | `:183` | | `高傲 5` | `:231` |
| `干扰 5` | `:184` | | `蔑视 5` | `:232` |
| `势破 5` | `:185` | | `焚天 5` | `:233` |
| `折锋 5` | `:186` | | `讨价还价 5` | `:234` |
| `暗影突袭 5` | `:187` | | `丛刃 5` | `:207` |
| `永夜 5` | `:188` | | `决斗 5` | `:208` |
| `灵魂收割 5` | `:189` | | `利刃 5` | `:209` |
| `生死判 5` | `:190` | | `剑气 5` | `:210` |
| `破败 5` | `:191` | | `名刀司命 5` | `:211` |
| `镇魂 5` | `:192` | | `咒刃 5` | `:212` |
| `忍者 5` | `:193` | | `嗜血 5` | `:213` |
| `忍术 5` | `:194` | | `天谴 5` | `:214` |
| `狂热 5` | `:195` | | `天道 5` | `:215` |
| `破釜 5` | `:196` | | `奥术 5` | `:216` |
| `血怒 5` | `:197` | | `对决咒术 5` | `:217` |
| `跃斩 5` | `:198` | | `弑君 5` | `:218` |
| `退散 5` | `:199` | | `弑魔 5` | `:219` |
| `速攻 5` | `:200` | | `惩戒咒术 5` | `:220` |
| `首击 5` | `:201` | | `慈悲 5` | `:221` |
| `骑士 5` | `:202` | | `生灵咒术 5` | `:222` |
| `法术大炮 5` | `:203` | | `终结 5` | `:223` |
| `冷血术 5` | `:204` | | `脉冲刃 5` | `:224` |
| `剑卫 5` | `:205` | | `蛇吻 5` | `:225` |
| `血偿 5` | `:226` | | `钝锋 5` | `:227` |

> 源码顺序：`:180-205`（注释 `// 传说` 在 `:179`）→ `:207-234`（注释 `// 稀世` 在 `:206`）。上表右半部分是源顺序的后半段，**排序请以行号为准**。

**（b）`斧头附魔属性`（`sgAxe`，5 项，`:237-241`）**

```text
:237 定身 5      :238 涅槃 5      :239 心灵感应 5   :240 破釜 5      :241 跃斩 5
```

**（c）`弓附魔属性`（`sgBow`，34 项，`:245-279`）**

```text
:245 势破 5      :246 宣判 5      :247 折锋 5      :248 暗影突袭 5  :249 永夜 5
:250 浮尘 5      :251 灵魂收割 5  :252 生死判 5    :253 破败 5      :254 禁锢 6
:255 镇魂 5      :256 忍士 5      :257 标记 5      :258 狂热 5      :259 猎手 5
:260 穿颅 5      :261 五言 5      :262 空军 5      :263 首射 5      :264 骑射 5
:265 鸣踪 5      :266 玻璃大炮 5  :267 退散 5      :269 天道 5      :270 弑君 5
:271 真三言 5    :272 苍劲 5      :273 苍穹 5      :274 神射手 5    :275 创伤 5
:276 弓魄 5      :277 狂妄 5      :278 焚天 5      :279 夺金 5
```

（`:268` 是注释 `// 稀世`，不是设置项。）

**（d）`护甲附魔属性`（`sgArmor`，50 项，`:283-333`）**

```text
:283 不懈 5      :284 不灭 5      :285 临阵脱逃 5  :286 光环 5      :287 启迪 5
:288 圣愈 5      :289 均衡之法 5  :290 均衡之遁 5  :291 幸运 5      :292 清风引 5
:293 狂潮 5      :294 磐石 5      :295 祭祀 5      :296 虚幻 5      :297 血罡 5
:298 霜滞 5      :299 倔强 5      :300 均衡之御 5  :301 均衡之攻 5  :302 时速 5
:303 金钟罩 5    :304 钢铁胃 5    :306 乾坤 5      :307 力场 5      :308 圣之守护 5
:309 复苏之风 5  :310 奥术壁垒 5  :311 奥术血统 5  :312 招架 5      :313 无畏契约 5
:314 星穹 5      :315 村庄英雄 5  :316 物法皆修 5  :317 狂骨 5      :318 玄煞 5
:319 生命源泉 5  :320 生命潮汐 5  :321 盛宴 5      :322 貔貅 5      :323 逆鳞 5
:324 旺盛 5      :325 捍卫 5      :326 决战 5      :327 混沌 5      :328 燃血 5
:329 祭血 5      :330 重型装甲 5  :331 意志 5      :332 噬灵 5      :333 破格 5
```

（`:305` 是注释 `// 稀世`。）

**（e）`工具与通用附魔属性`（`sgOther`，13 项，`:335-347`）**

```text
:335 龙行 5      :336 海王 5      :337 催生 1      :338 矿脉 5      :339 自我修复 5
:340 血契 5      :341 地质学家 5  :342 破界 5      :343 贪欲 5      :344 立方 5
:345 龙之后裔 5  :346 龙脉 5      :347 挖金 5
```

---

## §3 `gear/GearEnchantScreen.java`（311 行，整页逐段）

> **本轮不做一比一交互复刻**（用户已拍板），但**它的全部文案与设置项是资产，必须逐字登记**（见下）。

### 3.1 类头与构造

- 类：`public final class GearEnchantScreen extends WindowScreen`（`:33`），类 javadoc 说明三级分类「大类（工具 / 武器 / 护甲）→ 类型（镐 / 斧 / 剑 / 头盔…）→ 材质（木 / 石 / 铜 / 铁 / 金 / 钻石 / 下界合金 / 皮革 / 锁链 / 海龟壳）」（`:22-32`）
- **窗口标题逐字**（`:44`）：`super(theme, "原版装备附魔配置");`
- 构造字段：`setting`（`GearEnchantSetting`）、`data = GearEnchantData.get()`、`categoryKey / typeKey / materialKey / table / 报告提示`（`:35-41`）
- 三级分类反推（`:47-51`，逐字）：

```java
// 从当前已选装备反推三级分类（未选则默认 工具→镐→钻石）
VanillaEnchantDatabase.GearCandidateRule current = currentRule();
this.categoryKey = current == null ? "TOOL" : prefix(current.category());
this.typeKey = current == null ? "PICKAXE" : suffix(current.category());
this.materialKey = current == null ? "diamond" : (current.material() == null ? "none" : current.material());
```

- `initWidgets()`（`:54-58`）：建 `table` 后 `rebuild()`

### 3.2 `rebuild()` 逐段（`:61-201`）

| 段 | 内容（逐字） | 行号 |
| --- | --- | --- |
| ① 顶部装备图标 + 名称 | `header.add(gearIcon(rule.itemId()));` `header.add(theme.label("§b§l" + rule.name()));` + `theme.horizontalSeparator()` | `:71-79` |
| ①a 装备变化时同步 setting | `if (rule != null && !rule.itemId().equals(setting.gearId())) { setting.applyGear(rule.itemId()); }` | `:68-70` |
| ② 报告入口行 | 按钮 `§b合成附魔详细` → `GearCraftReport::openFolder`；按钮 `§c删除合成记录` → `GearCraftReport.deleteAll()`，提示 `§a已删除 {n} 份合成报告` 或 `§8暂无合成报告可删除` | `:82-93` |
| ③ 分类表单（独立 `WTable form`，`horizontalSpacing = 4`） | `WTable form = theme.table(); form.horizontalSpacing = 4;` | `:96-97` |
| ④ 第一层：大类 | 标签 `§7类别` + 下拉 `GearCatalog.categories()`，`action` 内重置品质/类型为第一个并 `rebuild()` | `:100-111` |
| ⑤ 第二层：品质（材质） | 标签 `§7品质` + 下拉；材质为空时提前 `return` 并提示 `§c该类别暂无装备` | `:114-130` |
| ⑥ 第三层：类型 | 标签 `§7类型` + 下拉；类型为空时提前 `return` 并提示 `§c该类别暂无装备` | `:133-148` |
| ⑦ 找不到装备 | 提示 `§c未找到对应装备` | `:151-156` |
| ⑧ 无方案 | 分隔线 + 提示 `§7该装备 §c暂无极品方案§7，仅作为官方装备数据收录。` | `:157-165` |
| ⑨ 极品方案 | 标签 `§7极品方案`；多方案→下拉（`action` 调 `setting.applyProfile(...)` + `rebuild()`）；单方案→标签 `§b` + 方案名 | `:167-181` |
| ⑩ 附魔列表 | 分隔线 + 独立 `WTable enchantTable`（`horizontalSpacing = 4`），逐 `profile.targets` 调 `addEnchantRow` | `:188-198` |
| ⑪ 收尾 | `setting.refreshSummary();` | `:200` |

下拉统一 `.minWidth(64)`（`:104`、`:124`、`:143`、`:173`）。

### 3.3 单个附魔行（`addEnchantRow` `:204-236`，逐字）

```java
int rawLevel = setting.levelOf(target.id);
final int level = rawLevel < 1 ? target.level : rawLevel;
boolean excluded = setting.isExcluded(target.id);

t.add(theme.label(excluded ? "§8" + target.name : "§f" + target.name));

if (target.excludable) {
    WButton exclude = t.add(theme.button(excluded ? "§c已排除" : "§a启用")).widget();
    exclude.action = () -> {
        setting.setExcluded(target.id, !excluded);
        rebuild();
    };
} else {
    t.add(theme.label("§8核心"));
}

WMinus minus = t.add(theme.minus()).widget();
minus.action = () -> { setting.setLevel(target.id, level - 1); rebuild(); };

t.add(theme.label("§e" + roman(level)));

WPlus plus = t.add(theme.plus()).widget();
plus.action = () -> { setting.setLevel(target.id, level + 1); rebuild(); };
```

列序 = 名称 | 排除开关（或 `§8核心`）| `[-]` | `§e{罗马等级}` | `[+]`。
罗马数字（`roman` `:300-310`）：1→`I`、2→`II`、3→`III`、4→`IV`、5→`V`，其它回退阿拉伯数字。

### 3.4 其余工具方法

| 方法 | 作用 | 行号 |
| --- | --- | --- |
| `gearIcon(String itemId)` | 按 ID 查原版物品渲染官方图标；查不到/NPE 回退 `theme.item(ItemStack.EMPTY)`（注释：不叠加附魔光效，保持主菜单与游戏内一致） | `:239-251` |
| `currentRule()` | 已选装备对应的 `VanillaEnchantDatabase.GearCandidateRule`，未选返回 `null` | `:254-258` |
| `currentProfile(gear)` | 按 `setting.profileId()` 在 `gear.profiles` 里找方案 | `:260-267` |
| `findCategory / findMaterial / findType` | 按 key 找，找不到取第 0 个 | `:269-288` |
| `prefix / suffix` | `category` 按第一个 `_` 切前半/后半（`TOOL_PICKAXE` → `TOOL` / `PICKAXE`） | `:290-298` |

---

## §4 `gear/GearEnchantSetting.java`（289 行）

### 4.1 定位

- `public final class GearEnchantSetting extends StringListSetting`（`:36`），类 javadoc 明确「只负责『状态序列化 + UI 入口』，不解析 JSON，不做目标判断」（`:34`）
- 构造（`:40-42`，逐字）：

```java
public GearEnchantSetting(String name, IVisible visible) {
    super(name, "配置原版装备的极品附魔目标", new ArrayList<>(), null, null, visible, null, null);
}
```

（模块侧实参：名字 `装备附魔配置`、`visible = () -> 目标模式.get() == TargetMode.GEAR`，见 `AutoEnchantBook.java:562`。）

### 4.2 序列化（键名逐字）

**本类不覆写 `toTag` / `fromTag`**（实测全文 289 行无这两个方法）→ 完全走旧框架链路，键名逐字如下：

```text
本类内容格式（javadoc :26-31 逐字）
  [0] 装备 ID（minecraft:diamond_sword）
  [1] 方案 ID（fortune）
  [2..] 每个目标附魔 "enchantId:level:excludedFlag"（flag 0/1）

落链（旧框架 Meteor 26.1.2 源码，只读参考库）
  StringListSetting.save(tag)  → tag.put("value", ListTag<StringTag>)            [[Meteor原始源码] settings/StringListSetting.java:47-55]
  Setting.toTag()              → tag.putString("name", name) + save(tag)        [settings/Setting.java:118-125]
  SettingGroup.toTag()         → "name" / "sectionExpanded" / "settings"[]      [settings/SettingGroup.java:60-73]
  Settings.toTag()             → "groups"[]                                     [settings/Settings.java:157-167]
  Module.toTag()               → "name" / "keybind" / "toggleOnKeyRelease" / "chatFeedback" / "favorite" / "settings" / "active"  [systems/modules/Module.java:156-168]
  Modules                      → super("modules")                               [systems/modules/Modules.java:66]
  System.<init>                → file = new File(MeteorClient.FOLDER, name + ".nbt")  [systems/System.java:36]
```

→ 实际落盘路径：`<游戏根目录>/meteor-client/modules.nbt`（`MeteorClient.FOLDER`，见 `01-开发参考库/API参考/Meteor客户端/其他.md:13`）内 `settings → groups[] → {name:"原版装备附魔", settings[] → {name:"装备附魔配置", value:[...]}}`。

> **与点位的关系**：点位**不在本设置里**，走模块自己的 `toTag/fromTag`（见 §6.5）。

### 4.3 入口方法逐条

| 方法 | 签名 / 作用 | 行号 |
| --- | --- | --- |
| `register()` | `SettingsWidgetFactory.registerCustomFactory(GearEnchantSetting.class, theme -> (table, setting) -> createWidget(theme, table, (GearEnchantSetting) setting));` | `:45-48` |
| `createWidget(theme, table, setting)` | 横向列表 = `theme.item(currentIconStack())` + 按钮 `配置装备附魔` + 摘要标签 + `GuiRenderer.RESET` 图标按钮 | `:50-70` |
| `createWidget` 内按钮 | `select.action = () -> Minecraft.getInstance().execute(() -> Minecraft.getInstance().setScreen(new GearEnchantScreen(theme, setting)));`（注释：延迟到下一 tick，否则主菜单里点了没反应） | `:58-61` |
| `createWidget` 内重置 | `setting.reset(); setting.refreshSummary();` | `:62-66` |
| `gearId()` | `[0]`，空转 `null` | `:76-79` |
| `profileId()` | `[1]`，长度 <2 或空转 `null` | `:82-85` |
| `applyGear(String gearId)` | 切装备 → 取 `defaultProfile` → 重置附魔列表；无方案写 `[gearId, ""]` | `:88-96` |
| `applyProfile(String profileId)` | 按方案 ID 重置附魔列表 | `:99-110` |
| `applyProfileInternal` | 编码 `[gearId, profile.id, 每个 "id:level:0"]` | `:112-120` |
| `setLevel(String, int)` | clamp `[1, max]`；`maxLevelOf` 返回 -1 时不强制降级 | `:123-130` |
| `setExcluded(String, boolean)` | 写排除位 `1/0` | `:133-135` |
| `levelOf(String)` | 找不到返回 `-1` | `:138-146` |
| `isExcluded(String)` | 读排除位 | `:149-155` |
| `parseTarget(String)` | **从右向左**定位最后两个冒号（附魔 ID 自带命名空间冒号，不能直接 `split(":")`） | `:163-174` |
| `currentProfile()` | → `TargetProfile` 快照（见 4.4） | `:177-203` |
| `refreshSummary()` | 刷新摘要标签与图标 | `:206-209` |
| `currentIconStack()` | 装备 ID → `ItemStack`，非法/空气/NPE → `ItemStack.EMPTY` | `:212-225` |
| `summaryText()` | `"§a" + gearText + " §8▸ §b" + profileText + " §8▸ §e" + active + " 项目标"`；未选装备 → `未选择装备`；未选方案 → `未选择方案` | `:227-236` |
| `mutateTarget(...)` | 就地改一行（等级 / 排除位） | `:238-249` |
| `findProfile / isExcludable / defaultProfile / enchantName` | 方案查找、核心附魔判定、默认方案（`isDefault`，否则第 0 个）、附魔显示名（查不到回退 ID） | `:251-284` |

### 4.4 与 `TargetProfile` 的关系（`:177-203` 逐字）

```java
public TargetProfile currentProfile() {
    String gearId = gearId();
    String profileId = profileId();
    GearEnchantData.GearDefinition gear = GearEnchantData.get().gear(gearId == null ? "" : gearId);
    GearEnchantData.GearProfile profile = findProfile(gear, profileId);
    if (gear == null) return null;

    List<TargetProfile.TargetEnchantment> targets = new ArrayList<>();
    for (int i = 2; i < get().size(); i++) {
        String[] parts = parseTarget(get().get(i));
        if (parts == null) continue;
        int level;
        try { level = Integer.parseInt(parts[1]); } catch (NumberFormatException e) { continue; }
        boolean excluded = "1".equals(parts[2]);
        boolean required = !isExcludable(profile, parts[0]);
        targets.add(new TargetProfile.TargetEnchantment(parts[0], enchantName(parts[0]), level, excluded, required));
    }

    return new TargetProfile(
        gear.id, gear.name, gear.category,
        profileId == null ? "" : profileId,
        profile == null ? "" : profile.name,
        targets,
        profile == null ? List.of() : profile.exclusiveWith,
        profile == null ? List.of() : profile.forbidden
    );
}
```

**唯一消费点**（界面 ↔ 逻辑层的接缝）：`AutoEnchantBook.onActivate()` `:868` — `gearProfile = 装备附魔配置.currentProfile();`，随后 `:869-873` 空则报 `请先在「原版装备附魔」分类选择装备与极品方案！` 并 `toggle()`。

---

## §5 `ui/` 3 文件

### 5.1 `ui/EnchantmentSelectSetting.java`（91 行）

- 类：`public final class EnchantmentSelectSetting extends StringListSetting`（`:25`），含 `public final List<String> options`、`private final List<WLabel> countLabels`（`:26-27`）
- 类 javadoc（`:20-24`）：「附魔目标多选设置控件。底层的 BoolSetting 组被隐藏，本控件把勾选状态聚合成一个可点击的选择器，点击后打开 `EnchantmentSelectScreen` 进行可视化多选。」
- 构造（`:29-37` 逐字）：

```java
public EnchantmentSelectSetting(String name, List<String> options, List<BoolSetting> backing) {
    this(name, options, backing, null);
}

/** 带可见性条件的构造：用于三模式 UI 下按目标模式动态显示/隐藏 */
public EnchantmentSelectSetting(String name, List<String> options, List<BoolSetting> backing, IVisible visible) {
    super(name, "选择需要收集的附魔属性", selected(backing), values -> apply(values, backing), null, visible, null, null);
    this.options = List.copyOf(options);
}
```

- 选中态 ↔ 源 Bool 的双向同步（`:39-47` 逐字）：

```java
private static List<String> selected(List<BoolSetting> backing) {
    List<String> values = new ArrayList<>();
    for (BoolSetting setting : backing) if (Boolean.TRUE.equals(setting.get())) values.add(setting.name);
    return values;
}

private static void apply(List<String> values, List<BoolSetting> backing) {
    for (BoolSetting setting : backing) setting.set(values.contains(setting.name));
}
```

> 以 **`BoolSetting.name`（中文设置名）作为条目 ID**：选择器列表显示的就是设置名本身。

- 控件形态（`createWidget` `:53-65` 逐字）：

```java
WHorizontalList list = table.add(theme.horizontalList()).expandCellX().widget();
list.add(theme.item(enchantedBookIcon()));
WButton select = list.add(theme.button("选择附魔")).expandCellX().widget();
WLabel count = list.add(theme.label(setting.countText())).widget();
setting.countLabels.add(count);
WButton reset = table.add(theme.button(GuiRenderer.RESET)).widget();
select.action = () -> Minecraft.getInstance().setScreen(new EnchantmentSelectScreen(theme, setting));
reset.action = () -> {
    setting.reset();
    setting.refreshCount();
};
```

→ 行内 = `[附魔书图标] [选择附魔 按钮] [已选择 N 项]`；**`RESET` 图标按钮加在外层 `table`（另起一行）**，不在横向列表内。
→ 计数文案（`:88-90`）：`"已选择 " + get().size() + " 项"`；`refreshCount()` 用 `removeIf(label -> { label.set(text); return false; })` 刷全部标签（`:76-82`）。

### 5.2 `ui/EnchantmentSelectScreen.java`（77 行）

- 类：`public final class EnchantmentSelectScreen extends WindowScreen`（`:17`），**窗口标题 = `setting.title`**（`:23`）
- 类 javadoc（`:13-16`）：「附魔目标多选界面。左侧为未选中的词条，右侧为已选中的词条，支持顶部关键词过滤。」
- `initWidgets()`（`:27-37` 逐字）：

```java
WTextBox search = add(theme.textBox("")).minWidth(400).expandX().widget();
search.setFocused(true);
search.action = () -> {
    filter = search.get().trim().toLowerCase();
    rebuild();
};
table = add(theme.table()).expandX().widget();
rebuild();
```

- `rebuild()`（`:39-60`）：双栏 + 中间 `theme.verticalSeparator()`；左栏未选（`WPlus` 加入），右栏已选（`WMinus` 移除）；过滤条件 `filter.isEmpty() || option.toLowerCase().contains(filter)`
- `setSelected(option, selected)`（`:66-76`）：改列表 → `setting.set(values)` → `setting.refreshCount()` → `rebuild()`

### 5.3 `ui/CustomEnchantSetting.java`（51 行）

- 类：`public final class CustomEnchantSetting extends StringListSetting`（`:21`）
- 构造（`:23-25`，逐字）：`super(name, description, defaultValue, values -> {}, null, visible, null, null);`
- 类 javadoc（`:14-20`）：复用旧框架 `StringListSetting#fillTable` 的内联字符串编辑能力（每行一个附魔名 + 删除，底部添加 / 重置），顶部追加附魔书官方图标
- `createWidget`（`:32-41` 逐字）：

```java
WHorizontalList header = table.add(theme.horizontalList()).expandX().widget();
header.spacing = 6;
header.add(theme.item(enchantedBookIcon()));
table.row();
// 内联编辑列表（复用 Meteor 官方 StringListSetting.fillTable）
WTable wtable = table.add(theme.table()).expandX().widget();
StringListSetting.fillTable(theme, wtable, setting);
```

- 内联编辑的按钮文案来自旧框架（`settings/StringListSetting.java:74-119`）：逐行 `WTextBox` + `WMinus`；底部 `Add` 按钮 + `GuiRenderer.RESET` 图标按钮（`reset.tooltip = "Reset"`）。**`Add` / `Reset` 是英文原文。**

---

## §6 指令 `.fumo`（`command/FumoCommand.java`，292 行）

### 6.1 基本信息

| 项 | 逐字 / 值 | 行号 |
| --- | --- | --- |
| 指令名 | `fumo` | `:39` |
| 描述（Brigadier description） | `自动附魔坐标设置指令` | `:39` |
| 别名 | **无**（构造只传 name + description） | `:38-40` |
| 播报模块名常量 | `MODULE_NAME = "自动附魔"` | `:36` |
| 根节点 | **无 executes**（裸 `.fumo` 无输出） | `:42-81` |
| TAB 补全 | **无** `suggests(...)`（全 literal；候选 = `设置 / 移除 / 状态 / 清空` + 9 个点位节点） | `:42-81` |
| 类 javadoc 用法块 | 见下（**只列 8 个节点，漏 `铁砧箱`**） | `:27-31` |
| 注册点 | `core/AddonTemplate.java:220`（`Commands.add(new FumoCommand());`） | — |

类 javadoc 逐字（`:27-31`）：

```text
   .fumo 设置 <书/青晶石/工具护甲箱/附魔台/砂轮/铁砧/挂机位/成品箱>
   .fumo 移除 <同上>
   .fumo 状态
   .fumo 清空
```

### 6.2 完整指令树（逐字，`:42-81`）

```java
// .fumo 设置 <节点>
builder.then(literal("设置")
    .then(literal("书")         .executes(ctx -> { setPos(PointType.BOOK_STORAGE);      return SINGLE_SUCCESS; }))
    .then(literal("青晶石")     .executes(ctx -> { setPos(PointType.LAPIS_STORAGE);     return SINGLE_SUCCESS; }))
    .then(literal("工具护甲箱") .executes(ctx -> { setPos(PointType.EQUIPMENT_STORAGE); return SINGLE_SUCCESS; }))
    .then(literal("附魔台")     .executes(ctx -> { setPos(PointType.ENCHANTING_TABLE);  return SINGLE_SUCCESS; }))
    .then(literal("砂轮")       .executes(ctx -> { setPos(PointType.GRINDSTONE);        return SINGLE_SUCCESS; }))
    .then(literal("铁砧")       .executes(ctx -> { setPos(PointType.ANVIL);             return SINGLE_SUCCESS; }))
    .then(literal("铁砧箱")     .executes(ctx -> { setPos(PointType.ANVIL_BOX);         return SINGLE_SUCCESS; }))
    .then(literal("挂机位")     .executes(ctx -> { setPos(PointType.AFK);               return SINGLE_SUCCESS; }))
    .then(literal("成品箱")     .executes(ctx -> { setPos(PointType.OUTPUT_STORAGE);    return SINGLE_SUCCESS; }))
);

// .fumo 移除 <节点>
builder.then(literal("移除")
    .then(literal("书")         .executes(ctx -> { removePos(PointType.BOOK_STORAGE);      return SINGLE_SUCCESS; }))
    .then(literal("青晶石")     .executes(ctx -> { removePos(PointType.LAPIS_STORAGE);     return SINGLE_SUCCESS; }))
    .then(literal("工具护甲箱") .executes(ctx -> { removePos(PointType.EQUIPMENT_STORAGE); return SINGLE_SUCCESS; }))
    .then(literal("附魔台")     .executes(ctx -> { removePos(PointType.ENCHANTING_TABLE);  return SINGLE_SUCCESS; }))
    .then(literal("砂轮")       .executes(ctx -> { removePos(PointType.GRINDSTONE);        return SINGLE_SUCCESS; }))
    .then(literal("铁砧")       .executes(ctx -> { removePos(PointType.ANVIL);             return SINGLE_SUCCESS; }))
    .then(literal("铁砧箱")     .executes(ctx -> { removePos(PointType.ANVIL_BOX);         return SINGLE_SUCCESS; }))
    .then(literal("挂机位")     .executes(ctx -> { removePos(PointType.AFK);               return SINGLE_SUCCESS; }))
    .then(literal("成品箱")     .executes(ctx -> { removePos(PointType.OUTPUT_STORAGE);    return SINGLE_SUCCESS; }))
);

// .fumo 状态 —— 打印当前所有坐标
builder.then(literal("状态").executes(ctx -> {
    printStatus();
    return SINGLE_SUCCESS;
}));

builder.then(literal("清空").executes(ctx -> {
    clearPoints();
    return SINGLE_SUCCESS;
}));
```

节点字面量与 `PointType` 的对应（9 对 1:1）：

| `.fumo` 节点（逐字） | `PointType` | `title()`（卡片标题） |
| --- | --- | --- |
| `书` | `BOOK_STORAGE` | `空白书箱` |
| `青晶石` | `LAPIS_STORAGE` | `青金石箱` |
| `工具护甲箱` | `EQUIPMENT_STORAGE` | `工具/护甲箱` |
| `附魔台` | `ENCHANTING_TABLE` | `附魔台` |
| `砂轮` | `GRINDSTONE` | `砂轮` |
| `铁砧` | `ANVIL` | `铁砧` |
| `铁砧箱` | `ANVIL_BOX` | `铁砧箱` |
| `挂机位` | `AFK` | `挂机点` |
| `成品箱` | `OUTPUT_STORAGE` | `成品箱` |

> **`青晶石` 是指令节点，`青金石` 是设置项与卡片里的用词**——两处用字不同，逐字保留（`青晶石` 见 `:48/:61`；`青金石箱` 见点位 `title()` 与卡片 `:670`）。

### 6.3 全部回执文案（逐字）

**统一排版入口**：`core/CommandMessageFormatter.of("自动附魔", 标题)` + `.world()` / `.dimension(id)` / `.coord(x,y,z)` / `.field(标签, 值)` / `.status(Level, 明细)` / `.send()`；级别只决定**状态行的颜色**与**无明细时的默认文字**（`SUCCESS("成功","§a")` / `RUNNING("处理中","§e")` / `WARNING("警告","§6")` / `FAILURE("失败","§c")` / `INFO("信息","§7")`），状态行渲染为 `§7状态　§8▸ {级别色}{明细或级别名}`（`CommandMessageFormatter.java:36-63`、`:140-162`）。字段行渲染为 `§7{标签}　§8▸ {值}`。所有回执**只发本地聊天**（`:165-167`、`:188-192`）。下表的「状态」列一律写作渲染后的**明细部分**（前缀固定为 `§7状态　§8▸ `）。

| 触发场景 | 逐字文案 | 行号 |
| --- | --- | --- |
| 设置-模式不匹配 | 标题 `设置点位失败`；字段 `原因 ▸ 当前为 [{模式中文名}] 模式，无需绑定 [{title}] 点位，请切换到对应模式后再设置` | `:89-92` |
| 设置-已存在 | 标题 `设置点位失败`；字段 `原因 ▸ [{title}] 已设置，请先执行 .fumo 移除 {node} 后再重新设置` | `:95-98` |
| 设置-准星未命中（非 AFK） | 标题 `设置点位失败`；字段 `原因 ▸ 请将准星对准目标方块` | `:106-108` |
| 设置-附魔台不对 | 标题 `设置点位失败`；字段 `原因 ▸ 准星指向的方块不是附魔台` | `:232-235` |
| 设置-砂轮不对 | 标题 `设置点位失败`；字段 `原因 ▸ 准星指向的方块不是砂轮` | `:237-240` |
| 设置-铁砧不对 | 标题 `设置点位失败`；字段 `原因 ▸ 准星指向的方块不是铁砧` | `:242-245` |
| 设置-非容器 | 标题 `设置点位失败`；字段 `原因 ▸ [{title}] 必须设置为箱子、木桶或潜影盒容器` | `:247-249` |
| 设置-服务器/维度识别失败 | 标题 `设置点位失败`；字段 `原因 ▸ 无法识别当前服务器或维度，点位未保存` | `:255-257` |
| 设置-旧点位无世界绑定 | 标题 `设置点位失败`；字段 `原因 ▸ 旧版点位没有世界绑定，请先执行 .fumo 清空 后重新设置` | `:264-266` |
| 设置-跨世界 | 标题 `设置点位失败`；字段 `原因 ▸ 现有点位属于其他服务器或维度，请切回原世界，或执行 .fumo 清空 后重设` | `:268-270` |
| **设置-成功** | 标题 `已设置{title}点位`；字段 `服务器/地址（world() 追加）` + `维度` + `坐标 ▸ §7X§f{} §7Y§f{} §7Z§f{}` + `类型 ▸ {title}`；状态明细 `§a已保存` | `:126-132` |
| **移除-成功** | 标题 `已删除{title}点位`；字段 `服务器/地址` + `维度` + `类型 ▸ {title}`（旧点位存在时追加 `坐标`）；状态明细 `§a已删除` | `:153-159` |
| **清空-成功** | 标题 `已清空全部点位`；字段 `服务器/地址` + `范围 ▸ 全部点位 + 挂机视角 + 服务器维度绑定`；状态明细 `§a已清空` | `:173-177` |
| **状态** | 标题 `点位状态`；字段 = 9 个（`{title} ▸ §c未设置` 或 `{title} ▸ §7X§f{} §7Y§f{} §7Z§f{}`）；状态明细 `§a当前世界与点位匹配` 或 `§6当前世界与点位不匹配` | `:206-219` |
| 模块未找到 | 标题 `模块未就绪`；字段 `原因 ▸ 找不到 自动附魔 模块，请确认已注册` | `:224-228` |
| 失败统一格式 | `fail(标题, 原因)` = 标题 + `原因 ▸ {原因}` + 状态明细 `§c操作未完成`（级别 `FAILURE`，只影响颜色） | `:286-291` |

> 回执中**没有**「无点位可清空」「该点位本来就没有设置」这类前置判断：`removePos()` `:136-166` 与 `clearPoints()` `:168-178` 都不检查是否为空，直接删除并播成功。`.fumo 状态` 对未设置项显示 `§c未设置`（`:210-212`）。

### 6.4 静态入口（供配置页卡片调用）

```java
public static boolean setPoint(PointType type) {
    return new FumoCommand().setPos(type);
}

public static void removePoint(PointType type) {
    new FumoCommand().removePos(type);
}
```

（`:182-200`，含逐字 javadoc：`@return true = 设置成功（可关闭 GUI），false = 设置失败（保留 GUI 让玩家重新对准）`。）
配置页调用点：`AutoEnchantBook.java:703`（`setPoint`）、`:714`（`removePoint`）。

### 6.5 点位落盘与跨服务器隔离（键名逐字）

- 写盘点：`setPos` 末尾 `Modules.get().save()`（`:124`）；`removePos` 内两处（`:151`、`:164`）；`clearPoints` 内（`:172`）
- 数据载体：模块 NBT（`AutoEnchantBook.toTag/fromTag` `:1621-1659`），键名逐字：

```java
writePos(tag, "posBook", posBook);
writePos(tag, "posLapis", posLapis);
writePos(tag, "posOutput", posOutput);
writePos(tag, "posEnchant", posEnchant);
writePos(tag, "posGrindstone", posGrindstone);
writePos(tag, "posHangout", posHangout);
writePos(tag, "posAnvil", posAnvil);
writePos(tag, "posAnvilBox", posAnvilBox);
writePos(tag, "posEquipment", posEquipment);
if (posAnvilFacing != null) tag.putString("posAnvilFacing", posAnvilFacing.name());
if (hangoutYaw != null) tag.putFloat("hangoutYaw", hangoutYaw);
if (hangoutPitch != null) tag.putFloat("hangoutPitch", hangoutPitch);
if (pointServer != null) tag.putString("pointServer", pointServer);
if (pointDimension != null) tag.putString("pointDimension", pointDimension);
```

（坐标值为 `tag.putIntArray(key, new int[]{x, y, z})`，`:1661-1663`；读回 `:1665-1679`。）

- **跨服务器/维度隔离不是「按服务器分文件」**，而是两个字段 + 比对（`:1681-1694` 逐字）：

```java
public String currentServer() {
    // 单机世界没有服务器地址，返回固定标识 "singleplayer"（与自动挖矿 WKCommand 一致），
    // 保证单人游戏也能正常标点、保存并校验点位，不再报「无法识别当前服务器或维度」。
    if (mc.getCurrentServer() == null || mc.getCurrentServer().ip == null) return "singleplayer";
    return mc.getCurrentServer().ip.trim().toLowerCase(Locale.ROOT);
}

public String currentDimension() {
    return mc.level == null ? null : mc.level.dimension().identifier().toString();
}

public boolean matchesCurrentPointContext() {
    return Objects.equals(pointServer, currentServer()) && Objects.equals(pointDimension, currentDimension());
}
```

- 挂机视角与铁砧朝向随点位一起写（`FumoCommand.java:115-122`）：`module.hangoutYaw = mc.player.getYRot(); module.hangoutPitch = mc.player.getXRot();`；`module.posAnvilFacing = mc.level.getBlockState(pos).getValue(AnvilBlock.FACING);`
- 移除时同步清（`:143-149`）：`AFK` → 视角清空；`ANVIL` → 朝向清空
- `clearPoints()`（模块侧 `:1702-1717`）清 9 点位 + 朝向 + 视角 + `pointServer` + `pointDimension`

> **旧项目落盘路径**：进 `<游戏根目录>/meteor-client/modules.nbt`（非 `config/yiyiaddon/{server}.json`）——新项目按本项目范式（对照阶段10 自动挖矿的落盘做法）重做，属**实现迁移**，不是文案迁移。

---

## §7 使用说明 / 帮助页 / 自检提示 / 播报入口

### 7.1 帮助页窗口规格（`ui/HelpScreen.java`）

| 项 | 逐字 / 值 | 行号 |
| --- | --- | --- |
| 类 | `public class HelpScreen extends WindowScreen` | `:23` |
| 窗口标题 | `module.title + " - 使用说明"` → `自动附魔 - 使用说明` | `:28` |
| 内容渲染 | 逐行 `list.add(theme.label(line)).expandX();`；**空字符串行渲染为 `theme.horizontalSeparator()`** | `:38-46` |
| 底部 | 一条分隔线 + 满宽按钮 `关闭`，点击 `mc.setScreen(null)` | `:49-57` |
| 标题框（3 行，逐字） | `§8┏━…┓` / `§8┃ §3§l> §b§l使用说明                                      §8┃` / `§8┗━…┛` | `:76-78` |
| 章节标题格式 | `content[index++] = "§3[§b#§3] §f" + section.title;`（章节前插一个空串 → 渲染成分隔线） | `:81-87` |

### 7.2 自动附魔的 8 个章节（逐字，`AutoEnchantBook.java:773-842`）

```text
§3[§b#§3] §f首次配置
  §8├─ §f先在要使用的服务器和维度执行 §e.fumo 清空 §7(清空旧点位)
  §8├─ §f准星对准对应方块，点击下方卡片「设置」按钮依次绑定：
  §8│    §7书 / 青晶石 / 成品箱 / 附魔台 / 砂轮
  §8│    §7原版装备模式：装备箱 / 青晶石 / 附魔台 / 砂轮 / 铁砧 / 铁砧箱 / 成品箱
  §8├─ §f挂机循环模式站在刷怪点调好杀怪视角，再绑定「挂机位」 §7(纯附魔模式不需要)
  §8├─ §f在自动附魔分类或原版附魔分类勾选要收集的目标词条
  §8├─ §f把带「横扫之刃」的剑放背包或快捷栏任意位置
  §8└─ §f书箱放空白书、青金石箱放青金石、成品箱预留空间

§3[§b#§3] §f点位设置 §7(两种方式)
  §b▸ §e方式1 §8- §f配置页面卡片按钮
    §7准星对准方块 §8→ §f点击对应卡片「设置」
    §7书/青晶石/成品箱：必须是箱子/桶/潜影盒
    §7附魔台/砂轮：必须对准对应方块
    §7挂机位：直接站在目标位置即可绑定 §7(含视角)

  §b▸ §e方式2 §8- §f指令系统
    §8> §3.fumo 设置 <节点> §8— §7节点：书/青晶石/成品箱/附魔台/砂轮/挂机位/装备箱/铁砧/铁砧箱
    §8> §3.fumo 移除 <节点> §8— §7删除单个点位
    §8> §3.fumo 状态 §8— §7查看坐标与当前地点匹配
    §8> §3.fumo 清空 §8— §7清空全部点位

§3[§b#§3] §f运行流程
  §a▸ §f物资不足时自动前往对应补给箱取书或青金石
  §a▸ §f经验不足时，挂机循环返回挂机位；纯附魔模式低于 30 级停止
  §a▸ §f横扫之刃剑在主背包时自动换入当前快捷栏并选中
  §a▸ §f达到目标等级后自动前往附魔台执行 30 级附魔
  §a▸ §f未命中目标词条时前往砂轮洗练，再继续下一次附魔
  §a▸ §f命中目标词条时播放提示音并将附魔书存入成品箱

§3[§b#§3] §f原版装备附魔
  §b▸ §f从装备箱取目标装备 → 附魔台随机附魔 → 评分判定
  §b▸ §f零命中/禁止/互斥 → 砂轮洗练后重新附魔
  §b▸ §f部分命中 → 保留，多件互补时铁砧合并升级
  §b▸ §f合并后 100% 达标 → 存入成品箱，达到「极品附魔数量」自动停机
  §b▸ §f铁砧损坏 → 自动去铁砧箱取备用铁砧原位补放

§3[§b#§3] §f参数说明
  §6▸ §e单轮抽取次数 §f— 附魔书/自定义模式每轮计划执行的附魔次数
  §6▸ §eGUI操作延迟 §f— 服务器卡顿或吞点击时适当调大
  §6▸ §e发包打开距离 §f— 发包开箱/开附魔台/开砂轮/开铁砧的最大距离，超出先寻路靠近
  §6▸ §e书本/青金石补给组数 §f— 每次补给希望保有的组数
  §6▸ §e每批取用数量 §f— 原版装备模式每次从装备箱取用的目标装备数量
  §6▸ §e极品附魔数量 §f— 原版装备模式最终产出的极品装备数量，达标自动停机
  §6▸ §e运行模式 §f— 每个目标模式独立开关：纯附魔只消耗当前经验、不足停机；挂机循环自动补经验
  §6▸ §e成功提示音 §f— 命中目标词条时播放所选音效

§3[§b#§3] §f原版附魔分类
  §7· 仅收录普通书通过附魔台随机附魔可获得的词条
  §7· 每种词条只提供附魔台实际能刷出的最高等级
  §7· 锋利/效率/力量等书本附魔最高 IV，不显示无法直接刷出的 V
  §7· 不含经验修补、冰霜行者、灵魂疾行、迅捷潜行和诅咒

§3[§b#§3] §f换服、换维度与指令
  §d▸ §f一套点位只能用于设置它时所在的服务器和维度
  §d▸ §f去其他服务器/维度时旧点位不会运行，也不会乱跑
  §d▸ §f新地点使用：先 §e.fumo 清空§f，再重设六个点位
  §d▸ §f回原服务器/维度时旧点位可直接使用，不必重设

§3[§b#§3] §f注意
  §c⚠ §f单人世界与服务器均可使用
  §c⚠ §f至少勾选一个目标词条，否则模块不启动
  §c⚠ §f成品箱满、书箱/青金石箱空时会提示并自动停机
  §c⚠ §f模块运行中无法修改点位，先关闭模块再设置
```

章节 `new HelpScreen.HelpSection(...)` 起始行号：`首次配置 :775`、`点位设置 §7(两种方式) :785`、`运行流程 :798`、`原版装备附魔 :806`、`参数说明 :813`、`原版附魔分类 :823`、`换服、换维度与指令 :829`、`注意 :835`。

> 与指令对不上/与代码对不上的 3 处已在 §0 第 9/10/11 条登记（`装备箱` vs `工具护甲箱`；「六个点位」；「运行中无法修改点位」）。按铁律**逐字保留原文，不据此改代码**。

### 7.3 自检失败的界面呈现（界面侧）

- **旧项目没有任何界面侧呈现**：无弹窗、无「打开设置」跳转、无窗口标题提示；自检失败**只走聊天**并**自动关闭模块**。
- 呈现链路（基类 `core/YiyiaddonModule.java:147-161` 逐字）：

```java
protected boolean reportSelfCheck(java.util.List<String> missing) {
    if (missing.isEmpty()) return true;

    chatFeedback = false;
    mc.execute(() -> {
        if (isActive()) toggle();
        chatFeedback = true;
    });

    notifyError("还差 " + missing.size() + " 项没配好，配完再开：");
    for (int i = 0; i < missing.size(); i++) {
        notify("§6  " + (i + 1) + ". §f" + missing.get(i));
    }
    return false;
}
```

- 调用点：`AutoEnchantBook.java:857`（GEAR 分支）、`:909`（BOOK / CUSTOM 分支）；缺项清单由 `selfCheck()` `:1760-1772` 生成（**判定与缺项文案属逻辑层那一份，本文不展开**）。
- 触发方式（「按钮」）：模块开关（模块面板 / 按键绑定 / `.fumo` 无关）→ 旧壳 `toggle()` → `onActivate()`；`toggle()` 的开关播报逐字 `§a§l已开启` / `§c§l已关闭`（`core/YiyiaddonModule.java:56-66`）。
- 新项目差异（登记）：本项目自检失败**必须聊天 + 屏幕弹窗**（`ConfirmPanelScreen.notice`，见 `40-阶段10-附录B` §5.5 与 `开发习惯.md` 第 176-179 条）；旧项目只有聊天。

### 7.4 播报入口（前缀与配色，界面侧口径）

| 入口 | 前缀 / 逐字 | 行号 |
| --- | --- | --- |
| 统一格式 | `"§c§l[yiyiaddon]§r§f§l[" + 模块名 + "]§r" + 内容` | `core/YiyiaddonModule.java:294-298` |
| `notify(msg)` | 内容前缀 `§f` | `:117-120` |
| `notifyError(msg)` | 内容前缀 `§6§l` | `:126-129` |
| `warning(msg)` | 内容前缀 `§e§l` | `:99-102` |
| `error(msg)` | 内容前缀 `§c§l` | `:104-107` |
| 开关 | `§a§l已开启` / `§c§l已关闭` | `:56-66` |
| 高亮工具 | `highlightText §a§l…§r§f§l` / `highlightFunction §b§l…` / `highlightNumber §e§l…` / `highlightServer §6§l…` / `highlightLocation §d§l…` / `highlightCommand §e§l…` | `:167-195` |

新项目按 `开发习惯.md` 第 110-113 条**去掉 `[yiyiaddon]` 段**，其余保持。

**附魔模块自身的两块「启动/批次」报告（界面呈现，逐字）**：

`announceGearStartup()` `:2064-2073`：

```text
§a§l✓ 自动附魔 · 原版装备附魔启动
§7运行模式　§8▸ {高亮 挂机循环 | 纯附魔模式}
§7目标装备　§8▸ {高亮 gearName}
§7极品方案　§8▸ {高亮 Function profileName}
§7目标附魔　§8▸ {高亮 Number N 项}
§7极品数量　§8▸ {高亮 Number N 件}
```

`announceGearSummary()` `:2076-2087`：

```text
§a§l✓ 原版装备附魔 · 批次完成
§7处理装备　§8▸ {N 件}
§7完成数量　§8▸ {N 件}
§7失败跳过　§8▸ {N 件}
§7附魔次数　§8▸ {N 次}
§7砂轮次数　§8▸ {N 次}
§7铁砧次数　§8▸ {N 次}
§7挂机次数　§8▸ {N 次}
```

`announceStartup(int totalTasks, int vanillaTasks, int extensionTasks)`（BOOK / CUSTOM）`:964-982`：

```text
§a§l✓ 自动附魔 · 启动报告
§7当前模式　§8▸ {高亮 挂机循环 | 纯附魔模式}
§7目标词条　§8▸ {N 本}
§7原版词条　§8▸ {N 本}
§7扩展词条　§8▸ {N 本}
§7单轮抽取　§8▸ {N 次}        ← 仅 EXPERIENCE（挂机循环）时追加
```

> 标签用**全角空格**对齐（`　`），与 `CommandMessageFormatter` 的字段排版同一口径；上面 `§7X　§8▸` 中的 `　` 是原文全角空格。

**开工/停机提示（界面侧，逐字采样，全部由 `notify` / `notifyError` 出）**：`:849` `§c必须进入世界后才能启动模块。`、`:870` `请先在「原版装备附魔」分类选择装备与极品方案！`、`:877` `目标方案违反 26.1.2 规则：§c{原因}§7，请重新选择！`、`:883` `无法解析目标装备：{名}（技术 ID：{id}）`、`:916` `§7当前为纯附魔模式，忽略挂机位，不会前往挂机区。`、`:926` `任务列表为空，请至少勾选一个附魔书属性！`、`:1040` `检测到服务器或维度发生变化，已停止寻路并关闭模块！`、`:1054` `目标装备不在背包，原版装备极品附魔已暂停。请补充装备后重试。`。
（状态机内的逐条播报属逻辑层范围，本文不重复。）

---

## §8 「页面 ↔ 模块 ↔ 指令」联动点（逐条落 `文件:行号`）

| # | 联动关系 | 旧项目的实际落点 | 证据 |
| --- | --- | --- | --- |
| 1 | 页面每一行直接读写模块设置字段 | 15 个 `sgBasic` 字段 + 3 个 `sgGear` 字段 + 1 个 `sgCustom` 字段 + 13 个选择器，全部是模块字段，经 `settings` 直接序列化 | `:114-176`、`:451-462`、`:145-149`、`:541-561` |
| 2 | 页面与 `.fumo` 指令同源同一份数据 | 卡片按钮走 `FumoCommand.setPoint/removePoint`（静态入口）→ 内部**同一批** `setPos/removePos`；点位字段是模块的 `posXxx`，指令与页面对同一批字段读写 | 页面 `:703`/`:714`；入口 `FumoCommand.java:189-200`；字段 `:350-363`；读写 `:729-756` |
| 3 | 绑定 / 移除 / 失效校验只有一处实现 | 唯一校验链：`preparePointContext()`（服务器/维度）+ `isValidTarget()`（容器/附魔台/砂轮/铁砧）+ `getPointPos()!=null`（已设置保护）；页面与指令共用 | `FumoCommand.java:85-134`、`:230-273` |
| 4 | 切换目标模式 → 页面分组重排 | `目标模式.onChanged` → `同步模式分组()`（清 + 重填 `settings.groups`）+ `刷新模式界面()`（下一 tick `screen.reload()`） | `:116`、`:625-649` |
| 5 | 页面点位卡片数量 ← 模块 `requiredPoints(mode)` | `getWidget` 遍历 `requiredPoints(目标模式.get())` 建卡；指令侧 `setPos` 用同一函数拦截 | 页面 `:613`；指令 `FumoCommand.java:89`；实现 `:759-770` |
| 6 | 卡片读出的是模块点位字段 | `BlockPos pos = getPointPos(type);`（未绑定 = `null` → `§8暂未绑定`） | `:663-664`、`:729-741` |
| 7 | 页面维度名 ← 模块 `pointDimension` | `维度中文名()` = `WorldContextFormatter.dimensionSummary(pointDimension)` | `:688-690`、`:723-726` |
| 8 | 装备附魔配置页 → 逻辑层目标 | `GearEnchantSetting.currentProfile()` 产出 `TargetProfile`，`onActivate` 取用为 `gearProfile` | `GearEnchantSetting.java:177-203`；`AutoEnchantBook.java:868` |
| 9 | 选择器 ↔ 源 Bool 双向同源 | 选择器列表项 **就是** `BoolSetting.name`；勾选写回 `setting.set(...)`，读回 `selected(backing)` | `EnchantmentSelectSetting.java:35`、`:39-47`、`:66-76` |
| 10 | 卡片标题色与 ESP 标点色同源注释 | 卡片配色注释逐字「与 ESP 标点颜色一致」 | `:666-677`；ESP `:1595-1604` |
| 11 | 挂机视角 ↔ 点位持久化 | `.fumo 设置 挂机位` 记 `hangoutYaw/hangoutPitch`；卡片不写视角 | `FumoCommand.java:115-118`；`toTag` `:1634-1635` |
| 12 | 铁砧朝向 ↔ 点位持久化 | `.fumo 设置 铁砧` 记 `posAnvilFacing`（`AnvilBlock.FACING`） | `FumoCommand.java:119-122`；`toTag` `:1633` |

**运行期一致性要求（迁移后必须仍成立）**：页面改设置 → `.fumo 状态` 与状态机立即可见（同一字段）；`.fumo 设置/移除` 改点位 → 重开页面卡片必须同步（旧项目靠「每次 `getWidget` 重新读字段」实现，`:663`；新控制台若缓存控件，必须在重新进入 / 切换页签时重建）；卡片 `设置/删除` 成功后必须**同一帧**关面板（`:704`/`:715`），否则会看到旧状态闪烁。

---

## §9 控制台页签映射草案（**草案 · 待用户确认**）

> 依据：用户 2026-09-16 已拍板的页签划分（`38-附A` 第 64-70 行、`38` §六 补充），本节的**具体页内排布**是草案，供批次5 施工前确认。
> 新项目现成骨架（先例，逐字类名）：`feature/mining/ui/MiningConsoleScreen.java` + `ui/console/{MiningOverviewPage, MiningPointPage, MiningTargetPage, MiningTeleportPage, MiningThresholdPage, MiningBaritonePage}.java` + `MiningRegistry.java`；通用件 `ui/console/ConsoleWidgets`（`Ctl` / `ConsoleRow` / `Note` / `Table` / `FoldSection` / `ButtonStrip`，`:41`、`:62`、`:233`、`:301`、`:377`、`:515-518`）。

### 9.1 页签（沿用旧分组名，切换模式自动增删）

| 当前模式 | 页签（逐字） |
| --- | --- |
| GEAR | `概览` · `点位` · `基础设置` · `原版装备附魔` |
| BOOK | `概览` · `点位` · `基础设置` · `原版附魔分类` |
| CUSTOM | `概览` · `点位` · `基础设置` · `自动附魔分类` · `自定义附魔` |

### 9.2 §2 的 18 组逐组映射

| 旧分组（本文 §2） | 项数 | 落到哪一页 | 形态 |
| --- | --- | --- | --- |
| `基础设置`（`sgBasic`） | 15 | **基础设置**页 | 第 1 行 = `目标模式` 分段控件（沿用旧 `sgBasic` 首位）；其余按旧顺序逐行；`visible` 条件由页面自管（切模式整页重建） |
| `自动附魔分类`（`sgAdvanced`） | 5（选择器） | **自动附魔分类**页 | 5 个 `FoldSection` 折叠段（见 9.3），段头逐字 = 选择器名 |
| `自定义附魔`（`sgCustom`） | 1 | **自定义附魔**页 | 1 行：`自定义附魔目标` 列表编辑（逐行文本框 + 删除 + 添加/重置） |
| `原版附魔分类`（`sgVanilla`） | 8（选择器） | **原版附魔分类**页 | 8 个 `FoldSection` 折叠段 |
| `原版装备附魔`（`sgGear`） | 3 | **原版装备附魔**页 | `记录合成日志`（开关）、`合成策略`（三值分段）、`装备附魔配置`（行 + `配置装备附魔` 入口按钮 + 摘要） |
| （无旧对应） | — | **概览**页 | 新项目新增（挖矿 `MiningOverviewPage` 先例）：状态条 + 统计，**不新增设置项** |
| （无旧对应） | — | **点位**页 | 由配置页卡片区（`:611-617`）迁移：9 类点位按 `requiredPoints(mode)` 过滤（GEAR 8 / BOOK·CUSTOM 6） |

### 9.3 13 个子组 → 页内折叠段（`FoldSection`）

| 所在页 | 折叠段名（逐字 = 旧选择器名） | 源子组 | 项数 | 旧 `sectionExpanded` 取值 |
| --- | --- | --- | --- | --- |
| 自动附魔分类 | `剑附魔属性` | `sgSword` | 54 | `false`（显式 `:567`） |
| 自动附魔分类 | `斧头附魔属性` | `sgAxe` | 5 | `false`（显式 `:568`） |
| 自动附魔分类 | `弓附魔属性` | `sgBow` | 34 | `false`（显式 `:569`） |
| 自动附魔分类 | `护甲附魔属性` | `sgArmor` | 50 | `false`（显式 `:570`） |
| 自动附魔分类 | `工具与通用附魔属性` | `sgOther` | 13 | `true`（未显式赋值，取 `createGroup(name)` 默认值，`Settings.java:97-99`） |
| 原版附魔分类 | `原版防具附魔` | `sgVanillaArmor` | 9 | `true`（未显式赋值，同上） |
| 原版附魔分类 | `原版近战附魔` | `sgVanillaMelee` | 7 | `true`（未显式赋值） |
| 原版附魔分类 | `原版工具附魔` | `sgVanillaTool` | 3 | `true`（未显式赋值） |
| 原版附魔分类 | `原版弓附魔` | `sgVanillaBow` | 4 | `true`（未显式赋值） |
| 原版附魔分类 | `原版钓竿附魔` | `sgVanillaFishing` | 2 | `true`（未显式赋值） |
| 原版附魔分类 | `原版三叉戟附魔` | `sgVanillaTrident` | 4 | `true`（未显式赋值） |
| 原版附魔分类 | `原版弩附魔` | `sgVanillaCrossbow` | 3 | `true`（未显式赋值） |
| 原版附魔分类 | `原版通用附魔` | `sgVanillaCommon` | 1 | `true`（未显式赋值） |

> **注意**：这 13 个子组在旧页面里**根本不渲染**（`:571-583` 把它们从 `settings.groups` 里 remove 掉，内容以选择器形式呈现），所以 `:564-570` 里的 `sectionExpanded` 只有 `sgAdvanced(true)` / `sgVanilla(false)` / `sgGear(true)` 三项对页面有效。上表取值是**代码事实**，新项目要的是**折叠段默认收起**（156 + 33 项平铺要滚多屏）→ 建议 13 段默认全部 `collapsed`，与旧值无关，**待用户确认**。

> 折叠键建议沿用本项目既有命名（挖矿为 `"baritone:toggles"` 形式，`MiningConsoleScreen.java:116`），例如 `"enchant:advanced:sword"`；折叠态必须存在窗口侧（重建会丢控件实例，`MiningConsoleScreen.java:109-116`）。

### 9.4 切模式的页签重排（照抄旧语义）

- 旧：`目标模式.onChanged → 同步模式分组() + 刷新模式界面()`（`:116`、`:625-649`）；**幂等保护不可省**（顺序一致直接 return，避免遍历 `settings.groups` 时 clear+addAll → `ConcurrentModificationException` 启动闪退）。
- 新：走本项目既有做法 —— `switchTab` 延到下一 tick 重建（`MiningConsoleScreen.java:187-195`）、`init` 里 `minecraft.execute(this::reload)`（`:138`）；页签集合本身按当前模式现算，**不得在遍历页签时重建页签列表**。
- 设置项的可见性条件（`visible(...)`）在新项目无等价机制 → 由页面**按值重建**（先例：`AutoChestPage` 的运行模式联动、`MiningTargetControls.afterModeSwitch`）。

---

## §10 待核对 / 存疑清单

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | **文件编号冲突** | `38-附A` 第 77 行说批次1 产出 `43-阶段11-附录A` / `44-阶段11-附录B`；本轮指令要求本文件为 **45**。请确认最终编号（`43` 已被 `43-复盘-自动挖矿移植补修记录.md` 占用） |
| 2 | 帮助页与指令字面量不一致 | 帮助写 `装备箱`，指令字面量是 `工具护甲箱`（§0-9）。**逐字保留原文**，不改成一致；若新项目要「两边同源」，需用户拍板以哪边为准 |
| 3 | 帮助页「重设六个点位」 | GEAR 实际 8 点位（§0-10）。同上，保留原文 |
| 4 | 帮助页「模块运行中无法修改点位」 | 代码无 `isActive()` 拦截（§0-11）。保留原文，不据此加拦截 |
| 5 | ESP 标点缺 GEAR 3 点位 + 标签 `[书本箱]` | §0-1 / §0-2。新项目 ESP 是否补齐 3 点位、标签用 `[书本箱]` 还是 `空白书箱`，求确认 |
| 6 | 2 个隐藏设置（`ESP标点` / `返回挂机视角`） | 旧页面无入口、恒 `true`（§0-12）。新项目**做成可见开关**还是继续隐藏（沿用旧行为）？ |
| 7 | 报告入口两个按钮 | `§b合成附魔详细` / `§c删除合成记录`（`GearEnchantScreen.java:82-93`）依赖 `GearCraftReport`，而该报告**本轮延后不做**。按钮是否保留？保留则点击无目标；删除则丢文案资产。**建议：本轮不建按钮，文案登记在案**（待确认） |
| 8 | 点位页排布 | 旧页面 8 张卡挤一行（§0-4）。新控制台点位页用几列 / 是否 `ListRow` 形式，待定 |
| 9 | 独立选择页是否保留 | `EnchantmentSelectScreen` 本次不做一比一；改成页内折叠段后，**双栏多选交互（左未选/右已选 + 关键词过滤）**是否需要用 `SelectorScreen` 多选复刻，还是折叠段内直接平铺 156 个开关 |
| 10 | `成功提示音类型` 12 种音效 | 界面侧只需保留 12 个下拉项（逐字）；新壳 `SoundEvent` 映射属逻辑层，求确认是否有缺项 |
| 11 | `.fumo 状态` 不含维度 | §0-19。新项目是否补齐「维度」字段？补齐 = 加内容（需拍板）；不补 = 逐字保留 |
| 12 | 自检的界面呈现 | 旧项目纯聊天（§7.3）。新项目按 `开发习惯.md` 第 176-179 条要**聊天 + 弹窗 + 打开设置**；弹窗文案是否沿用「还差 N 项没配好，配完再开：」逐字 |
| 13 | `GearEnchantScreen` 主菜单兜底 | 图标 `getDefaultInstance()` NPE 兜底返回 `ItemStack.EMPTY`（`GearEnchantScreen.java:246-250`、`GearEnchantSetting.java:220-224`）。新壳在**未进世界**时打开装备配置是否同样兜底 |
| 14 | 旧框架英文文案 | `Add` / `Reset`（tooltip `"Reset"`，`settings/StringListSetting.java:104-118`）是英文原文。新项目「自定义附魔目标」的添加/重置按钮文案：照抄英文，还是本项目中文（需拍板） |
| 15 | 选择器的条目 ID | 旧项目用**中文设置名**当条目 ID（`EnchantmentSelectSetting.java:41`、`:46`）。新项目若设置项改英文键名，选择器与设置必须同步改，否则勾选会丢 |

---

## §11 证据与命令

### 11.1 本次实际执行过的只读命令

```powershell
# 18 个设置分组逐组计数 + 关键行数（本文 §2.0 的「221 个设置项」即此命令输出）
$f='d:\mcaddon\26.1.2\src\main\java\com\example\addon\enchant\AutoEnchantBook.java'
foreach($g in 'sgBasic','sgAdvanced','sgCustom','sgSword','sgAxe','sgBow','sgArmor','sgOther','sgVanillaArmor','sgVanillaMelee','sgVanillaTool','sgVanillaBow','sgVanillaFishing','sgVanillaTrident','sgVanillaCrossbow','sgVanillaCommon','sgGear'){
  "{0,-16} {1,3}" -f $g, (Select-String -Path $f -Pattern ("$g\.add\(")).Count
}
"addSelector: " + (Select-String -Path $f -Pattern 'addSelector\(').Count
"addTargets: "  + (Select-String -Path $f -Pattern 'addTargets\(').Count
"lines: "       + (Get-Content $f).Count
```

输出（原文）：

```text
sgBasic           15
sgAdvanced         0
sgCustom           1
sgSword           54
sgAxe              5
sgBow             34
sgArmor           50
sgOther           13
sgVanillaArmor     0
sgVanillaMelee     0
sgVanillaTool      0
sgVanillaBow       0
sgVanillaFishing   0
sgVanillaTrident   0
sgVanillaCrossbow  0
sgVanillaCommon    0
sgGear             3
addSelector: 14      ← 含方法定义 1 行，实际调用 13 次
addTargets: 9        ← 含方法定义 1 行，实际调用 8 次
lines: 3230
```

> `sgAdvanced.add(` / `sgVanilla*.add(` 计数为 0，是因为选择器由 `addSelector` 内部 `target.add(selector)` 挂载、原版 33 项由 `addTargets` 内部 `group.add(...)` 挂载（`:592-600`、`:588-590`）。

### 11.2 本次逐字整读的旧项目文件

```text
enchant/AutoEnchantBook.java          （:1-200 / :200-440 / :440-880 / :879-1078 / :1556-1700 / :1755-1815 / :2040-2097）
enchant/gear/GearEnchantScreen.java    （全读 311 行）
enchant/gear/GearEnchantSetting.java   （全读 289 行）
enchant/ui/EnchantmentSelectSetting.java（全读 91 行）
enchant/ui/EnchantmentSelectScreen.java （全读 77 行）
enchant/ui/CustomEnchantSetting.java    （全读 51 行）
enchant/command/FumoCommand.java        （全读 292 行）
enchant/gear/AnvilPlanner.java          （:50-64，Strategy 显示名）
enchant/gear/TargetProfile.java         （结构引用，经 GearEnchantSetting.currentProfile()）
ui/HelpScreen.java                      （全读 104 行）
core/YiyiaddonModule.java               （:45-305：toggle / notify* / reportSelfCheck / highlight* / buildInfoWidget / formatMessage）
core/CommandMessageFormatter.java        （全读 263 行）
core/AddonTemplate.java                 （:85 / :140-228：分类常量、控件注册、模块与指令注册）
```

对照参考（不逐字抄录，仅用于对齐口径）：

```text
01-开发参考库/Meteor原始源码/settings/StringListSetting.java（:47-67 save/load；:74-119 fillTable）
01-开发参考库/Meteor原始源码/settings/Setting.java（:118-135 toTag/fromTag）
01-开发参考库/Meteor原始源码/settings/SettingGroup.java（:60-73 toTag）
01-开发参考库/Meteor原始源码/settings/Settings.java（:157-183 toTag/fromTag）
01-开发参考库/Meteor原始源码/systems/modules/Module.java（:156-168 toTag）
01-开发参考库/Meteor原始源码/systems/modules/Modules.java（:66 super("modules")）
01-开发参考库/Meteor原始源码/systems/System.java（:36 file = new File(MeteorClient.FOLDER, name + ".nbt")）
01-开发参考库/API参考/Meteor客户端/其他.md（:13 MeteorClient.FOLDER = 游戏根目录/meteor-client）
```

### 11.3 新项目侧核对文件（只读，用于 §9 草案）

```text
feature/mining/ui/MiningConsoleScreen.java（Tab 枚举 :80-97 / 折叠键 :116 / reload :178-185 / switchTab :187-195 / 页装配 :215-229）
feature/mining/ui/MiningRegistry.java（选择器候选与分组标题口径）
feature/mining/ui/console/Mining{Overview,Point,Target,Teleport,Threshold,Baritone}Page.java（页签先例）
ui/console/ConsoleWidgets.java（Ctl :41 / ConsoleRow :62 / Note :233 / Table :301 / FoldSection :377 / ButtonStrip :515-518）
ui/screen/{PanelScreen,HelpPanelScreen,ConfirmPanelScreen}.java
38-阶段11-自动附魔移植侦察与决策.md（§六 补充 + §九）
38-附A-新窗口开工指令.md（批次划分与硬约束）
40-阶段10-附录B-界面与指令侦察记录.md（体裁范本）
41-阶段10-附录C-配置页控件对照表.md
```

### 11.4 本文引用行号总表（复核用）

```text
AutoEnchantBook.java
  :94-111   18 个 SettingGroup 声明
  :113-176  基础设置 15 项
  :145-149  自定义附魔目标
  :180-347  五个自定义属性子组 156 项（sgSword :180-234 / sgAxe :237-241 / sgBow :245-279 / sgArmor :283-333 / sgOther :335-347）
  :350-363  9 个点位字段 + 铁砧朝向 + 挂机视角 + 服务器/维度
  :449/:451-462  sgGear 3 项（记录合成日志 / 合成策略 / 装备附魔配置）
  :465-536  三个枚举（TargetMode / RunMode / SuccessSound）显示名
  :538-585  构造 + 13 次 addSelector + 折叠初始态 + 13 次 groups.remove + 同步模式分组
  :546-553  addTargets 8 次调用（33 条原版 Bool）
  :588-600  addTargets / addSelector 实现
  :602-619  getWidget（1 按钮 + 卡片区）
  :625-649  刷新模式界面 / 同步模式分组（含幂等保护）
  :660-726  buildPointCard（标题配色 / 5 行 / 设置成功才关）
  :729-770  getPointPos / setPointPos / requiredPoints
  :773-842  帮助页 8 章节
  :846-948  onActivate（GEAR 分支 :856-908 / 其余 :909-948）
  :950-956  onDeactivate
  :964-982  announceStartup
  :1621-1659 toTag / fromTag（点位落盘键名）
  :1681-1694 currentServer / currentDimension / matchesCurrentPointContext
  :1702-1717 clearPoints
  :1755-1772 selfCheck（判定属逻辑层）
  :2064-2087 announceGearStartup / announceGearSummary
  :1596-1604 onRender2D（ESP 标点 6 个标签）
GearEnchantScreen.java   :33-58 / :61-201 / :204-236 / :239-267 / :269-310
GearEnchantSetting.java  :26-31（格式）/ :36 / :40-70 / :76-155 / :163-257 / :260-288
EnchantmentSelectSetting.java :20-47 / :49-65 / :67-90
EnchantmentSelectScreen.java  :13-37 / :39-76
CustomEnchantSetting.java     :14-41 / :43-50
FumoCommand.java         :28 / :36-40 / :42-81 / :85-134 / :136-166 / :168-178 / :182-200 / :202-220 / :224-273 / :286-291
ui/HelpScreen.java       :23-58 / :66-103
core/YiyiaddonModule.java :56-66 / :99-129 / :147-161 / :167-195 / :241-284 / :294-298
core/CommandMessageFormatter.java :36-63 / :86-150 / :152-192
core/AddonTemplate.java  :85 / :156-160 / :220
```
