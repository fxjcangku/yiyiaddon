# 30 · 移植总览 · 已移植与未移植全清单

> 本报告回答一个问题：**旧项目的东西，哪些搬完了、哪些还没搬。**
> 口径正本：[01-总纲-迁移口径与复刻原则.md](01-总纲-迁移口径与复刻原则.md)（旧项目功能模块全量复刻 + 只换 UI 外壳，新项目自研脱离旧框架）。
> 数据来源：旧项目 `d:\mcaddon\26.1.2\src\main\java\com\example\addon\` 全量清点；新项目 `d:\mcaddon\yiyiaddon\src\main\java\com\yiyiaddon\` 全量清点（初版 2026-09-14；**2026-09-18 重新清点，第二~五节已按最新现状更新；当日第四批 `reconnect` 落盘后，未移植仍为 0，新项目自有 `.java` 实测 757 / 125,515 行**）。
> 逐模块依赖去查 [06-基准-05-逐模块依赖全量清单.md](06-基准-05-逐模块依赖全量清单.md)；文案去查 [02](02-基准-01-状态播报体系.md)~[07](07-基准-06-指令体系全量清单.md)。

---

## 一、总账

| 项 | 旧项目 | 新项目 |
|---|---|---|
| 自有 `.java` 文件 | **404** | **620** |
| 代码行数（含空行口径） | **78,368** | **118,801** |
| 一级包 | 31（另有 0 文件的 `settings`） | 14 |

新项目行数反而更多，是三件事：① 新项目自带一套自研 UI 基建（`ui/` 107 文件 / 19,727 行：控制台公共件、通用窗口、设置控件、ESP 基建），旧项目这些能力由 Meteor 框架提供、不在自有代码里；② 旧项目把同一套逻辑在多个管理器里各复制一份（如 `JsonFileStore` 类注释记录的「三个管理器各复制约三百行」），新项目收敛为唯一实现，同时把跨模块共用件抽到顶层（`platform/` / `service/` / `model/`）；③ 旧框架（Meteor）的设置/控件/事件体系整层不复刻，死代码与旧框架胶水不搬（清单见 [26-审计](26-审计-自造内容清单与处置.md)）。

---

### 状态变更

> 本节记录本报告成文后发生的变动。按 `开发习惯.md` 第 76 条，正文数字不逐次回改，差异在此登记。

**2026-09-18（重新清点，正文第二~五节已同步）**

- 新项目自有 `.java` 由 323 → **620**，行数由 50,415 → **118,801**（含空行口径）。
- 期间新迁落地：`librarian`（自动图书管理员，53 文件 / 6,220 行，见 [87](87-阶段15-自动图书管理员移植侦察与决策.md)~[91](91-阶段15-批次4-收口报告.md)）、`villager`（村民交易）、`autofarm`（自动农场）、`mining`（自动挖矿，含杀戮光环）、`enchant`（自动附魔）、`water`（水源显示）、`admdetector`（管理员检测）。
- 旧侧行数口径统一为**含空行**（旧 78,368 / 非空约 69,570，与原正文 69,570 的差异即口径差异）。
- **2026-09-18 追加（同一日）**：新增「视觉」分类与「透视」模块（`feature/vision/` 14 文件 / 1,811 行）——
  **用户点名新增，不是迁移**（旧项目没有方块 / 实体透视模块，取证见 [92 号](92-阶段16-透视模块侦察与决策.md) 第二节）；
  自有 `.java` 由 620 → **634**。分类「视觉」`visuals` 权重 70 由第 148 条的预留项转为已注册项。
- **2026-09-18 追加（同日第二批）**：`teleport`（22 文件 / 2,601 行，见 [94](94-阶段17-传送模块侦察与决策.md)/[95](95-阶段17-传送模块落盘记录.md)）、`tactical` 四模块（飞行绕过 / 发包防踢 / 发包秒破 / 服务器检测，见 [96](96-阶段18-战术模块侦察与决策.md)~[101](101-阶段18-批次5-发包秒破落盘记录.md)）、`bonemeal`（自动骨粉，13 文件 / 2,407 行，见 [102](102-阶段19-自动骨粉落盘记录.md)）全部落盘。
- **2026-09-18 用户决策**：`userstats`、`baritoneguide`、`meteorguide` **不做**（移出未移植清单）；`autologin` **暂停**；`autodisconnect` 待定。当日 ESP 总闸新增两层：`INSTANT_BREAK`（发包秒破）、`BONE_MEAL`（自动骨粉）。
- **2026-09-18 追加（同日第三批）**：`autologin`（自动登入）**落盘**，25 文件 / 4,761 行，落点 `feature/autologin/`，模块 ID `autologin`，分类 `automation`，`order()=40`，已注册进 `AddonModules`（模块总数 21），`gradlew build` EXIT=0；见 [103 号](103-阶段20-自动登入落盘记录.md)。**§三未移植清零（0 文件 / 0 行），第三~五节已同步。**
- **2026-09-18 全量重新清点（同日晚）**：新项目自有 `.java` 由 634（第二轮登记值）→ **746**，行数 → **124,041**（含空行口径，实测 `src/main/java/com/yiyiaddon`）；§一总账表沿用成文时快照，差异按第 76 条在此登记。
- **2026-09-18 追加（同日第四批 · 用户点名新增）**：`reconnect`（**自动重连**，11 文件 / 1,482 行，落点 `feature/reconnect/` + `service/reconnect/` + `platform/network/ConnectionCloser`，模块 ID `autoreconnect`，分类 `utility`，`order()=50`）**落盘**，`gradlew build` EXIT=0，见 [104 号](104-阶段21-自动重连模块落盘记录.md)。
  **本模块不是迁移**：旧项目没有独立重连模块（重连原先长在自动登入里），用户 2026-09-18 要求「单独拆一个做全点 + 加测试键」。为此把重连实体逻辑上提为共用件 `service/reconnect/ReconnectEngine`（自动登入的 `ReconnectHandler` 改为委托它的适配层，公开方法与行为一字未变）、断线动作上提为 `platform/network/ConnectionCloser`；`AdminDisconnect` 的「断线前关第三方重连」改为 `ReconnectSuppression` 抑制窗口（保命断线不被自家重连撤销）。模块总数 21 → **22**。
  自有 `.java` 由 746 → **757**，行数 → **125,515**（含空行口径，实测）。
- **2026-09-18 追加（同日第五批 · 对账收口）**：**Mixin 逐条对位完成**，见 [105 号](105-阶段22-Mixin逐条对位报告.md) —— 旧 39 个 Mixin 逐个登记去向（原样搬 6 / 业务原版注入 5 / 口径不搬 1 / 旧框架层 26），销掉 §2.6「需在对应阶段逐条对账」这条待办；§2.6 已同步。
  **疑似缺口已清零**：唯一那条（旧 `StardewCommandUsageMixin`，修 `.stardew` 指令在聊天框提示区的「兄弟分支多余用法行」与「尾随空格报错」）经**用户 2026-09-18 实机验证不复现**，销项为「实机验证后确认无需搬运」——新指令树的节点结构与注入点不触发原版的 `fillNodeUsage` / `showException` 判定（105 号第三节）。**旧 39 个 Mixin 对位全部闭合，无缺口。**

**2026-09-15**

- **统计时点**：本报告全部数字取自 2026-09-14。此后只做行为修复，**未新增或删除任何 `.java` 文件**，故「自有 `.java` 文件数」两栏仍然成立；行数为该时点快照，本轮增删若干行后略有出入，需精确数字时重新清点。
- **2.1 节「ID 三件套 全迁」**：迁移范围仍成立，但当时存在两处行为偏差——识别模式分流判据写反、「ID识别」模块「点击开启即识别」丢失。已于 2026-09-15 修正，逐条取证与判定见 [34-复盘-ID三件套差异核对与修复.md](34-复盘-ID三件套差异核对与修复.md)。
- **2.3 节星露谷农场**：`.stardew 标记成熟` 的中文作物参数在旧项目侧不可解析（整段标红、TAB 会改写输入），已于 2026-09-15 在旧项目修正；「资源检测完成」卡片与档案计数已回退旧口径。见 [33-复盘-补全行为与资源卡片口径修复.md](33-复盘-补全行为与资源卡片口径修复.md)。
- **旧项目侧本轮一并改动**：`itemid/IdConfigModule`、`core/AddonTemplate`、`worldblock/BlockIdentifier`、`itemid/ItemIdentity`、`autochest/AutoChestModule`（口径对齐，属经用户批准的追加修改，非回退）。
- **遗留**：`.id 方块` 在「自动保存」模式下漏写方块状态快照（旧项目为「稳定记录 ＋ 快照」两次落盘与两条播报），待拍板。

---

## 二、已移植（落盘完成 · 全部未实机）

### 2.1 业务模块：ID 三件套 + 方块识别（共 3 + 1 个包、49 个旧文件）

| 旧包 | 旧文件数 | 旧行数 | 状态 | 新项目落点 |
|---|---|---|---|---|
| `itemid` | 19 | 3,445 | **全迁** | `feature/identity/`、`platform/identity/`、`model/identity/`、`repository/identity/`、`service/identity/`、`config/identity/`、`ui/screen/SelectorScreen` |
| `worldblock` | 5 | 992 | **全迁** | 同上（`platform/identity/BlockIdentifier`、`model/identity/BlockIdentity`、`repository/identity/BlockIdentityRepository`、`repository/identity/BlockSnapshotRepository`、`feature/identity/ui/IdBlockResultScreen`） |
| `autochest` | 25 | 3,353 | **全迁** | `feature/autochest/`、`model/autochest/`、`repository/autochest/` |

`itemid` 19 个旧文件逐一对位：

| 旧文件 | 新落点 |
|---|---|
| `IdCommand` | `feature/identity/command/IdentityCommand` |
| `IdIdentifyModule` / `IdConfigModule` | `feature/identity/IdIdentifyModule` / `IdConfigModule` |
| `IdResultScreen` / `EntityResultScreen` | `feature/identity/ui/IdResultScreen` / `IdEntityResultScreen` |
| `IdManagementScreen` / `IdDataCleanScreen` / `IdAddScreen` | `feature/identity/ui/` 同名类 |
| `ItemIdManager` / `EntityIdManager` / `ItemSnapshotManager` | `repository/identity/ItemIdentityRepository` / `EntityIdentityRepository` / `ItemSnapshotRepository` + `service/identity/IdentityService` |
| `ItemIdentity` / `EntityIdentity` / `IdentifyMode` | `model/identity/` 同名类 |
| `ItemIdentifier` / `EntityIdentifier` / `ItemIdentityMatcher` | `platform/identity/` 同名类 |
| `ItemTargetSetting` | `config/identity/IdentityTargetConfig` |
| `ItemTargetSelectScreen` | `ui/screen/SelectorScreen` |

### 2.2 三个模块的联动链（已打通，逐条可查）

```
.id 物品（IdIdentifyModule / IdentityCommand）
   → IdentityService.addItem(...)  → 落盘 <游戏目录>/AutoChest/items/*.json
   → IdentityService 通知监听器
自动箱子「目标物品」（AutoChestPage → SelectorScreen）
   → 数据源 IdentityService.allItems()（= 旧 ItemIdManager）
   → 选中键存 IdentityTargetConfig（= 旧 ItemTargetSetting）
取物（ChestInteractionService）
   → IdentityTargetConfig.selectedItems(IdentityService.shared()) 逐格匹配
ID 删除 → pruneInvalid → 播报「有 N 个目标 ID 已失效，已从选择器移除」
```

与旧项目完全一致：**数据源唯一，自动箱子不建第二套物品库**（旧 `AutoChestModule.java:50-51`）。

### 2.3 业务模块：星露谷农场（81 文件 / 17,576 行，**已落盘 · 未实机**）

| 旧包 | 旧文件数 | 状态 | 新项目落点 | 详情 |
|---|---|---|---|---|
| `stardew` | 67 | **搬 57 ＋ 新增/拆分 23** | `feature/stardew/`（16 个子包） | [32-阶段09-星露谷农场移植记录](32-阶段09-星露谷农场移植记录.md) |
| `autofarm/navigation` | — | 随本批搬运 1 个 | `feature/stardew/navigation/FarmNav` | 同上 |

- **未搬 10 个**：`StardewProfileStore`（死代码）· `EspShapeMode`（新项目已有 `ShapeMode`）· `LeftAlignedSection`｜`StardewSettingsRenderer`（旧框架补丁类）· 四个旧设置壳（`StardewResourceSetting`／`StardewTargetSetting`／`StardewCropLogisticsSetting`／`StardewRenderSetting`，内容已重写进新壳）· 两个旧表格控件（`WFixedCell`／`WValueCell`）。
- **界面形态回归旧项目**：「服务器资源」面板（模块页）＋ 控制台 6 页签（概览/种植/运行/后勤/点位/日志）＋ 三个独立窗口（渲染对象/目标选择/确认）＋ 使用说明窗；文案与交互逐字，壳为新项目自研。
- **指令**：`.stardew`（绑定 / 移除 / 添加洒水器 / 移除洒水器 / 种植 / 标记成熟 / 控制台 / 季节 / 状态 / 清空），无别名。
- **已注册**：模块进模块中心「星露谷」分类；指令已挂 `Module.commands()`。
- **顺带补齐**：全模块播报排版器已升级为旧项目完整版（真实字体测量 ＋ 一像素空白对齐）；新增共享类 `platform/world/PlayerText`、`platform/world/WorldContextFormatter`。

### 2.4 业务模块：自动挖矿（**已落盘 · 待实机**）

| 旧包 | 旧文件数 | 状态 | 新项目落点 | 详情 |
|---|---|---|---|---|
| `mining` | 16 | **搬 16 ＋ 新增控制台 / ESP / 指令等 19 个** | `feature/mining/` | [42-阶段10-自动挖矿移植记录与差异清单](42-阶段10-自动挖矿移植记录与差异清单.md) |
| `combat`（KillAura） | 1 | 以 Meteor 原始源码为蓝本一比一重写 | `feature/combat/` | 同上 §九（功能层 6 文件 + 控制台配置页 7 文件） |

- **已做**：9 态 FSM（含死亡处理 / 复活等待）、Baritone 寻路、矿石识别与单目标锁定、容器物流与白名单、秒破（独立开关、**默认关**）、杀戮光环、ESP（点位 / 岩浆）、`.wk` 指令全集、模块页 + 控制台 7 页签 + 帮助页 + 状态播报、设置项 5 组。
- **未搬**：`OrePredictor` 种子模式（含 5 个种子设置项）、`tactical` 反检测（4,297 行）、`.wk 检测假矿`、音效通知；按用户裁定去掉旧两道校验（跨维度拒绝、三点位 32 格距离）。
- 控制台公共件 `ui/console/`（`ConsoleWidgets` / `ConsoleHost` / `ConsoleMetrics`）即本阶段抽出，后续模块统一复用。

### 2.5 业务模块：自动附魔（45 文件 / 8,890 行，**已落盘 · 待实机**）

| 旧包 | 旧文件数 | 状态 | 新项目落点 | 详情 |
|---|---|---|---|---|
| `enchant` | 33 | **搬 27 ＋ 新增 18** | `feature/enchant/`（9 个子包） | [45-阶段11-自动附魔移植记录与差异清单](45-阶段11-自动附魔移植记录与差异清单.md) |

- **三模式互斥**：GEAR 原版装备附魔 / BOOK 原版附魔书 / CUSTOM 自定义附魔；30 态状态机（通用 12 + GEAR 18）。
- **点位 9 类**、**设置 18 组（174 项，含 189 个多选词条）**、控制台 6 页签、使用说明 8 章、`.fumo` 指令（9 节点 + 状态 + 清空，回执逐字）。
- **资源两套**：`assets/yiyiaddon/gear-enchants.json`（549 KB）+ `enchantment/vanilla/`（79 文件 / 2.18 MB：30 级附魔台候选池 + 附魔 / 装备 / 互斥规则）。
- **未搬**：`report/GearCraftReport`（449 行，日志功能按用户裁定删除，连带删 10 处调用点与设置项 `记录合成日志`）、死代码 5 文件 244 行（`recognition` / `adapter` / `CandidateMatcher` / `CandidatePool`，全项目零调用）。
- **依赖已就位**：杀戮光环（阶段10 落地）、Baritone 封装、发包层、`WorldContextFormatter` 全部复用，未新建第二套。

### 2.6 基建层（按新架构重建，非 1:1 搬迁）

| 旧包 | 旧文件数 | 新项目对应 |
|---|---|---|
| `core` | 7 | `core/`（模块基类、管理器、事件桥、消息排版） |
| `farm` | 2 | `platform/container/` + `platform/network/`（`ContainerAccess`、`BlockPacketSender`） |
| `utils` | 11 | `utils/` + `platform/storage/GamePaths` 等 |
| `commands` | 3 | `command/`（`ClientCommand`、`CommandRegistry`、`CommandManager`） |
| `accessor` | 3 | `mixin/` + accesswidener |
| `translations` | 6 | `integration/baritone/` 的翻译类 |
| `mixin` | 39 | `mixin/` **19 个**（Baritone 翻译 6 + 业务/原版 5 + 自研 8）。旧 39 个已**逐条对位**：[105 号](105-阶段22-Mixin逐条对位报告.md)（原样搬 6 / 业务注入 5 / 口径不搬 1 / 旧框架层 26 / 实机验证后无需搬运 1；**疑似缺口 0，已全部闭合**） |
| `resourcepack` | 3 | `platform/resource/` + `service/resourcepack/`（阶段 07 完成主体） |

### 2.7 不复刻（口径规定，不算缺口）

Meteor 框架本体、明文密码外发、水印、指令名上报、示例 HUD、主题模块、旧项目死代码、旧项目 `run/` 与 `build/` 产物。

### 2.8 其余已迁业务模块（2026-09-18 重新清点）

| 旧包 | 旧文件数 / 行数 | 新落点 | 新规模（文件 / 行） | 状态 |
|---|---|---|---|---|
| `villager` | 16 / 4,320 | `feature/villager/` | 34 / 7,177 | **已落盘 · 待实机** |
| `autofarm` | 42 / 5,495 | `feature/autofarm/` | 46 / 6,525 | **已落盘 · 待实机** |
| `mining` | 16 / 6,547 | `feature/mining/` | 38 / 15,149 | **已落盘 · 待实机** |
| `enchant` | 33 / 7,753 | `feature/enchant/` | 46 / 9,247 | **已落盘 · 待实机** |
| `librarian` | 52 / 4,604 | `feature/librarian/` | 53 / 6,220 | **已落盘 · 待实机**（见 [91 号](91-阶段15-批次4-收口报告.md)） |
| `water` | 1 / 367 | `feature/water/` | 5 / 875 | **已落盘 · 待实机** |
| `admdetector` | 1 / 262（另 `autodisconnect` 1 / 86 合并进来） | `feature/admindetect/` | 12 / 1,903 | **已落盘 · 待实机**；用户 2026-09-16 裁定把旧「自动断线」合并进本模块（`service/AdminDisconnect`） |
| 杀戮光环（`combat`，旧无独立包） | 1（随 `mining`） | `feature/combat/` | 15 / 3,566 | **已落盘 · 待实机** |
| ——（本项目基建） | —— | `feature/respawn/`、`feature/visuals/` | 1 / 75、2 / 336 | 基建 / 测试件 |
| ——（**新增功能，非迁移**：旧项目无对应物） | —— | `feature/vision/` | 14 / 1,811 | **已落盘 · 待实机**（「透视」模块：方块 + 实体两模式，见 [93 号](93-阶段16-透视模块落盘记录.md)） |
| ——（**新增功能，非迁移**：用户点名拆出自动登入的重连能力并做全） | —— | `feature/reconnect/` + `service/reconnect/` + `platform/network/ConnectionCloser` | 11 / 1,482 | **已落盘 · 待实机**（「自动重连」模块：独立重连 + 测试键；重连引擎与自动登入共用一份，见 [104 号](104-阶段21-自动重连模块落盘记录.md)） |
| `teleport` | 20 / 1,846（实测；表内旧记 2,157） | `feature/teleport/` | 22 / 2,601 | **已落盘 · 待实机**（见 [94 号侦察](94-阶段17-传送模块侦察与决策.md) / [95 号落盘](95-阶段17-传送模块落盘记录.md)）；死代码 3 文件不搬 |
| `bonemeal` | 1 / 656 | `feature/bonemeal/` | 13 / 2,407 | **已落盘 · 待实机**（见 [102 号](102-阶段19-自动骨粉落盘记录.md)）；ESP 已接总闸层 `BONE_MEAL` |
| `autologin` | 14 / 3,145 | `feature/autologin/` | 25 / 4,761 | **已落盘 · 待实机**（见 [103 号](103-阶段20-自动登入落盘记录.md)）；第三方 `AutoReconnect` 接管段与 `KeyboardInvoker` mixin 收口为自有 `ReconnectHandler` / `KeyboardSimulator` |

新侧规模大于旧侧，差额是本项目新增的壳件：控制台（含页签页）、模块页、使用说明窗、ESP 基建、指令、自检与播报排版。

---

### 2.9 业务模块：战术四模块（10 文件 / 实测 3,691 行，**已落盘 · 待实机**）

| 旧包 | 旧文件数 | 状态 | 新项目落点 | 详情 |
|---|---|---|---|---|
| `tactical` | 10 | 四个模块全部落盘 + 新增核心底座 | `feature/tactical/`、`feature/packetbreak/`、`core/net/` | [96 侦察](96-阶段18-战术模块侦察与决策.md) · [97 批次1](97-阶段18-批次1-核心通道与协调器落盘记录.md) · [98 批次2](98-阶段18-批次2-飞行绕过逻辑层落盘记录.md) · [99 批次3](99-阶段18-批次3-服务器检测落盘记录.md) · [100 批次4](100-阶段18-批次4-发包防踢落盘记录.md) · [101 批次5](101-阶段18-批次5-发包秒破落盘记录.md) |

- **四个模块**：`FlightBypassModule`（飞行绕过）、`AntiKickBypassModule`（发包防踢）、`PacketInstantBreakModule`（发包秒破，含 ESP 层 `INSTANT_BREAK`）、`ServerDetectorModule`（服务器检测，含资源包三模式接管）。
- **共用底座**：`TacticalCoordinator`（常驻协调器，不依赖模块开关）+ `ServerFingerprints`（四张指纹表 151 条）+ `FlightPolicy`；核心新增发包闸门（`core/net/`：视图 / 处置 / 规则 / 延迟 / 语义直发）、收包只读快照（`SERVER_BLOCK` / `SERVER_CHANNEL` / 玩家距离）、`TickRateMonitor`、`ResourcePackGate`。
- **用户 2026-09-18 决策**：四个模块**各自独立**（不合并）；绕过三件套 → `combat`，服务器检测 → `utility`；`服务器检测` 与 `发包防踢` **默认启用**；秒破渲染**接入 ESP 总闸**（76 号三步）。
- **未搬**：无。`PacketMiningFeedback` / `PacketMiningProtocol` 两旧类的能力已由 `BlockWatchService` + `ClientPacketSender` 覆盖，按第 169 条不重复实现。
- **登记不修**：旧 bug 原样保留（防踢的离服统计不播报、秒破的工具不切回 / `startAcknowledged` 只写不读 / 近路少发 STOP 等）。

---

## 三、未移植（2026-09-18 重新清点）

业务模块 **全部落盘，未移植数为 0**。

| 优先级 | 旧包 | 文件数 | 行数 | 说明 |
|---|---|---|---|---|
| 已落盘（2026-09-18） | `autologin` | 14 | 3,145 | 自动登入 → 新侧 25 文件 / 4,761 行，`feature/autologin/`，模块 ID `autologin`，分类 `automation`，见 [103 号](103-阶段20-自动登入落盘记录.md) |

`autologin` **暂停已解除**：用户 2026-09-18 当日要求「继续移植自动登入」，第三方 `AutoReconnect` 接管段与 `KeyboardInvoker` mixin 按第 169 条收口为自有实现（`ReconnectHandler` / `KeyboardSimulator`），差异逐条登记在 [103 号](103-阶段20-自动登入落盘记录.md) §5。

`autodisconnect`（1 / 86）**早已合并移植**（用户 2026-09-16 裁定与「管理员检测」合并为一个模块，落点 `feature/admindetect/service/AdminDisconnect.java`，`§c立即断线` 按钮在管理检测控制台「概览」页）；本清单此前误列为未移植，2026-09-18 更正。

**用户 2026-09-18 明确不做（非缺口，不再计入）**：`userstats`（1 / 732，用户统计）、`baritoneguide`（1 / 211，Baritone 引导）、`meteorguide`（1 / 130，旧框架引导）。

**未移植合计：0 个文件 / 0 行**。2026-09-18 当日移出：`teleport`（20 / 2,157）、`tactical`（10 / 表内 4,307，**实测 3,691**，差额 616 行为统计口径差异，按第 76 条登记不回改）、`bonemeal`（1 / 656）、`autologin`（14 / 3,145）、`autodisconnect`（1 / 86，**更正：早已合并进管理员检测**）；`userstats` / `baritoneguide` / `meteorguide` 三个包按用户决定移出未移植列。

另有四类**口径规定不搬**（不算缺口）：`convention`（1 / 795，项目约定文档类，迁移时作对账依据、不产生界面）、`theme`（1 / 354，第 43 条）、`hud`（1 / 53，示例 HUD）、`ui`（1 / 105，旧框架 UI 基类，新项目自研）。底座包（`core` / `farm` / `commands` / `utils` / `accessor` / `mixin` / `translations` / `resourcepack`）已按新架构重建，见 2.6 节；`settings` 为空包。

---

## 四、已移植部分的验证状态（2026-09-18）

| 模块 | 编译 / 构建 | 实机 |
|---|---|---|
| ID识别 | 通过 | **未验证** |
| ID配置管理 | 通过 | **未验证**（更多管理 / 数据清理 / 使用说明滚动 / 添加物品 ID） |
| 自动箱子 | 通过 | **未验证**（10 项清单见 [27-阶段08](27-阶段08-自动箱子迁移报告.md) 第四节） |
| 星露谷农场 | 通过 | **未验证**（模块页 / 控制台 6 页 / 三个独立窗口 / `.stardew` 全子命令 / 世界渲染 / 后台开箱屏蔽 / 自动任务流程，清单见 [32-阶段09](32-阶段09-星露谷农场移植记录.md) 第六节） |
| 自动挖矿（含杀戮光环） | 通过 | **未验证**（见 [42-阶段10](42-阶段10-自动挖矿移植记录与差异清单.md)） |
| 自动附魔 | 通过 | **未验证**（见 [45-阶段11](45-阶段11-自动附魔移植记录与差异清单.md)） |
| 自动农场 | 通过 | **未验证** |
| 村民交易 | 通过 | **未验证**（批次4 收口并行待办） |
| 水源显示 | 通过 | **未验证** |
| 管理员检测 | 通过 | **未验证** |
| 自动图书管理员 | 通过（`compileJava` / `build` 均 EXIT=0） | **未验证**（12 项清单见 [91 号](91-阶段15-批次4-收口报告.md) 第七节） |
| 自动登入 | 通过（`gradlew build` EXIT=0） | **未验证**（15 项清单见 [103 号](103-阶段20-自动登入落盘记录.md) §6） |
| 自动重连（**新增非迁移**） | 通过（`gradlew build` EXIT=0） | **未验证**（15 项清单见 [104 号](104-阶段21-自动重连模块落盘记录.md) 第七节；重点：测试键全链路、与自动登入并存不双连） |

**口径提醒（第 19 / 46 条）：至今没有任何模块完成过实机验收**——上表「通过」只代表编译与静态核对（文案机对、依赖清单、自造件审计），**不代表功能验证通过**。旧项目历史失败点（工位几何、静默交易、持久化）必须实机才能证实。

---

## 五、下一步建议顺序

1. **实机验收（先验收再往前加码，避免错误叠加）**：自动图书管理员（12 项见 [91 号](91-阶段15-批次4-收口报告.md) 第七节）→ 村民交易 → 自动农场 → 自动挖矿 + 杀戮光环 → 自动附魔 → ID 三件套 + 自动箱子 → 星露谷农场 → 水源显示 / 管理员检测 → 自动登入（[103 号](103-阶段20-自动登入落盘记录.md) §6）→ **自动重连（[104 号](104-阶段21-自动重连模块落盘记录.md) 第七节，重点测试键与两个重连模块并存）**。
2. **待定项收口**：图书管理员的两条拍板项已落地（[91 号](91-阶段15-批次4-收口报告.md) §6.1）；遗留项见 91 号第八节与其第五节（壳的 tooltip 原语、`StardewStatusCard` 档案计数推导等）。
3. **业务模块移植已清零**（旧项目业务包全部落盘或按用户裁定移出）。后续只剩：全模块实机验收 → 与旧项目逐条对账（含 Mixin 的逐条对位）；`meteorguide` 已按用户决定不做。
4. 收尾：全模块实机验收 + 与旧项目逐条对账（含 Mixin 的逐条对位）。
