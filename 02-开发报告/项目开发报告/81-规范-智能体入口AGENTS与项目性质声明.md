# 81 - 规范：智能体入口钩子 `AGENTS.md`（项目性质 = 移植中，未完成）

- 日期：2026-09-17
- 触发：用户「`开发习惯.md` 这个怎么改成什么名字能让智能体第一件事就是读这个，告诉它我是移植项目、现在还在逐渐完成」
- 结论：**不改名**（`开发习惯.md` 被 64 个文件引用，含 Java 源码注释），改为「**入口钩子 + 规则正本**」两件式
- 落地物：新增 `AGENTS.md`（4.1 KB，一屏）；`开发习惯.md` 开头加醒目项目性质块 + 第零章加防漂移条款

---

## 1. 方案选择（用户裁定）

| 方案 | 判定 |
| --- | --- |
| **A 新增 `AGENTS.md` 当入口钩子，`开发习惯.md` 原名不动** | **采纳**：64 处引用零改动；`AGENTS.md` 是通用约定名，多数智能体 / 工具会优先检索它 |
| B 把 `开发习惯.md` 改名 `AGENTS.md` | 否：30+ 篇历史报告里的旧名会与实况分裂，还得在开头写「原名…」 |
| C 改名 `项目必读-开发习惯.md` | 否：同样要改引用，且不依赖任何工具约定 |

## 2. `AGENTS.md` 内容（一屏，只放指针与速查，不复制规则正文）

1. **第一件事**：完整读 `开发习惯.md`（33 章 / 224 条）；冲突以它为准（第二十三章优先级最高）。
2. **这是什么项目**：性质=移植项目（旧项目 `D:/mcaddon/26.1.2` 逐模块搬入，只换 UI 外壳）；
   状态=**正在进行中，尚未完成**，不要假设某模块已存在 / 已完整；
   附迁移口径正本与**进度清单**（`30-移植总览-已移植与未移植全清单.md`）路径。
3. **必读顺序**：`开发习惯.md` → `02-开发报告/项目开发报告/00-AI阅读指引.md` → `01-开发参考库/README.md`。
4. **硬约束速查**：只列条号（163~165 改动禁区、171 不许夹带新功能、167 先取证、169 不留两份、
   180 页面构建时机、208 中文注释、212 说明时效、218/222/223 埋点修复门槛与清理）。
5. **目录地图**（`src` / `01-开发参考库` / `02-开发报告` / `03-后端API` / `04-埋点修复` / 两个根文件）。
6. **每轮交付标准**五条（先取证、build EXIT=0 且标注是否实机、写报告、中文无 Emoji、推送通道）。

引用路径已逐个核对存在：`开发习惯.md`、`00-AI阅读指引.md`、`01-开发参考库/README.md`、
`01-总纲-迁移口径与复刻原则.md`、`30-移植总览-已移植与未移植全清单.md` → 全部 `True`。

## 3. `开发习惯.md` 的两处改动

1. **开头加醒目块**（原来是两行泛泛的「长期强制规则」）：
   明确写出**项目性质 = 移植项目**、**状态 = 正在逐步迁移中、尚未完成**、不要假设模块已存在 / 已完整、
   给出口径正本与进度清单路径、冲突时以本文件为准；并指向入口钩子 `AGENTS.md`。
2. **第零章加防漂移条款**：`AGENTS.md` 只放「项目性质 + 必读清单 + 条号速查」，
   **规则内容只写在 `开发习惯.md`**；本文件增删章节时必须同步核对 `AGENTS.md` 第三节的速查条号。

## 4. 还需用户做的一件事（工具侧的硬保证）

文件名约定只是「大概率被读到」；**每个新会话百分之百先读它，要在 Trae 规则（user_rules）里加一句**：

```
开始任何工作前，第一件事是完整读取项目根目录的 AGENTS.md 与 开发习惯.md，读完再扫描或修改代码。
```

## 5. Codex 侧同步（用户追加：「codex 也换一下」）

- Codex 的规则入口 = 全局 `~/.codex/AGENTS.md` + 仓库根 `AGENTS.md`（项目那个本文件 §2 已就位）。
- **发现的问题**：全局 `~/.codex/AGENTS.md` 里写的**全是旧项目** `D:/mcaddon/26.1.2` 的环境与规范
  （Meteor Client 架构、Yarn 映射表工具 `03-映射表/工具/查API.js`、产物归档 `08-诊断记录/`、
  `# 项目环境` 一节甚至写明「基于 Meteor Client 架构」）—— 在 yiyiaddon 里照它执行会直接跑偏。
- **改动**（遵守该文件原有铁律「规范注释只增不删」）：仅在文件**最前面新增**一节
  `# 入口（第一件事，2026-09-17 追加）`，写明三件事：
  1. 工作目录为 `D:/mcaddon/yiyiaddon` 时，**第一件事**是完整读仓库根 `AGENTS.md` + `开发习惯.md`
     （规则正本），读完再动手；冲突以 `开发习惯.md` 为准；
  2. yiyiaddon 性质 = **移植项目、尚未完成**，动任何模块前先查 `30-移植总览…全清单.md`；
  3. 下方原章节属**旧项目**、在 yiyiaddon 不适用，并列出 yiyiaddon 自己的口径
     （零第三方框架 / Mojmap / `01-开发参考库/工具/查JARAPI.js` / 报告与埋点落点 / 埋点修复门槛）。
- **验证**：`~/.codex/AGENTS.md` 11661 → 13528 字节；原有 15 个章节标题（`# 核心原则` …
  `# 重要规范文件位置`）全部仍在，未删改任何既有条目。
- **同日追加（用户：「帮我把 codex 目录下替换成我们这个」）**：不再只挂一节，而是**整体替换**为
  本项目 `AGENTS.md` 的副本（多一段「生效范围」头：说明本文件是 yiyiaddon 入口钩子的全局副本、
  相对路径以 `D:/mcaddon/yiyiaddon` 为根、第一件事读 `开发习惯.md`）。
  - 旧项目那套规则**原样备份**到 `~/.codex/AGENTS-旧项目规则-备份-2026-09-17.md`
    （11661 字节 = 旧规则原文，与原文件逐字节等大；未销毁；新文件头部注明「在旧项目里工作时读那份」）。
  - 未把 `开发习惯.md` 正文（125 KB / 224 条）复制进全局：那样每个会话都要注入一份，
    且会与仓库里的正本**双份漂移**（第 169 条）；全局只留入口与速查。
- **顺带发现（未改动，待用户决定）**：`~/.codex/config.toml` 里登记的受信项目是旧路径
  `d:\documents\chatgpt\yiyiaddon`，当前仓库 `d:\mcaddon\yiyiaddon` 未登记；
  另本机有 `~/.claude/`，若也用 Claude Code 需另加全局/项目 `CLAUDE.md`。

## 6. 遗留

- 本次两个文件均**未 commit**。
- `AGENTS.md` 与 `开发习惯.md` 的条号速查靠人工同步（已在第零章立规）；后续若真出现漂移，
  再考虑做一个小脚本按章节标题生成速查（本轮不做）。
