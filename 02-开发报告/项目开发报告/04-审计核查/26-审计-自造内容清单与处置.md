# 26 · 审计 · 自造内容清单与处置

日期：2026-09-14
性质：整改类（对全项目成品代码的一次「是否偏离旧项目」审计）
触发：用户点名 `.resource` 指令「不是我旧项目的东西」，要求排查并清除全部自造内容。
审计范围：`d:\mcaddon\yiyiaddon\src\main\java\com\yiyiaddon\`（221 个 .java）对照旧项目 `d:\mcaddon\26.1.2\src\main\java\com\example\addon\`（404 个 .java）

---

## 一、审计口径

按《开发习惯》第 162 条与 `01-总纲-迁移口径与复刻原则.md`，以下三类「旧项目没有」的东西是**允许**的：

| 类别 | 定义 | 例 |
| --- | --- | --- |
| 框架层自研 | 旧项目由第三方客户端框架提供、本项目必须自研的等价能力 | 模块运行时、事件总线、指令系统骨架、Skija UI/渲染、主题、配置持久化、Mixin 桥接 |
| 复刻实现 | 与旧项目类职责一一对应的迁移实现（类名可不同） | `platform/identity`、`repository`、`service` |
| 明示脚手架 | 源码内声明「这是脚手架、可整体删除」的测试/调试类 | `feature/visuals/EspTestModule` |

**其余一律判为「自造」，须删或改回旧项目原文。**

---

## 二、审计规模与结果

| 维度 | 数据 |
| --- | --- |
| 文件 | 新项目 221 个（旧项目 404 个） |
| 中文串字面量 | 2167 条（分布于 94 个文件） |
| 文本判定 | 合规 2132（旧同串 1923 / 框架层 152 / 日志 19 / 脚手架 14 / 装饰拼接差异 24）；**无出处 20**；存疑 15 |
| 结构判定 | 框架层自研 141 / 复刻实现 75 / 脚手架 2 / **多加的 3** |
| 行为判定 | 差异 13 条（高 0 / 中 4 / 低 9） |

反证（说明审计有效）：`integration/baritone` 三个翻译文件 1108 条中文串**零无出处**；identity 模块 31 条播报中 24 条与旧项目逐字一致。

---

## 三、已删除（本次执行完毕）

| # | 内容 | 路径 | 依据 | 连带处理 |
| --- | --- | --- | --- | --- |
| 1 | 资源指令（152 行） | `command/ResourceCommand.java` | 旧项目 11 条指令中没有它；旧项目的资源检测 / 提取入口是星露谷配置页「服务器资源」**GUI 面板**（按钮逐字见 `04-基准-03` §1.11）。属越权新增（违反开发习惯 98 / 100 / 162 条） | 移除 `module/AddonModules.java` 的 import、`RESOURCE_PROBE` 注释与 `registerAddonCommands()` 整个方法及调用；`ResourceIndexProbe` 保留（资源生命周期服务仍需它做内容探针） |
| 2 | 未注册主题类 3 个 | `ui/theme/vape/VapeTheme.java`、`ui/theme/LightBlueTheme.java`、`ui/theme/DefaultClickGuiTheme.java` | 零引用、未在任何主题管理器注册、未进入用户可见入口（`ClickGuiThemeManager.java:12,17-19` 只注册 4 套） | 无 |

验证：`gradlew.bat compileJava` **通过**（exit 0）。

---

## 四、待处置清单

### 4.1 自造用户可见文本 20 条（全部集中在 ID 模块）

旧项目 grep 实测为空（正则 `已选目标|统计完成|详细输出|数据统计|最近结果|快照与识别目标|身份数据在磁盘上的位置|点击执行对应识别动作|识别目标配置中仍有效的选中项|已保存的物品状态快照数量|未知识别模式|输入后即时过滤当前` → No matches found）。

| # | 文件:行 | 自造文本 | 场景 | 旧项目对应 |
| --- | --- | --- | --- | --- |
| 1 | `feature/identity/IdIdentifyModule.java:268` | `已选目标` | 播报字段标签 | 旧项目无此字段，同类信息走 `§8▸ §7快照 §f` 字段流（`itemid/IdConfigModule.java:221-224`） |
| 2 | `feature/identity/IdIdentifyModule.java:270` | `统计完成` | 播报状态句 | 旧项目该统计只改界面标签、不发状态句（同上 `:219-225`） |
| 3 | `feature/identity/IdConfigModule.java:399` | `已选目标` | 同 #1（重复实现） | 同上 |
| 4 | `feature/identity/IdConfigModule.java:401` | `统计完成` | 同 #2 | 同上 |
| 5 | `feature/identity/service/IdentityActions.java:175` | `" ｜ 已选目标 "` | 一行式统计分隔 | 旧分隔符为 ` §8▸ `（同上） |
| 6 | `feature/identity/command/IdentityCommand.java:71` | `未知子命令：` | 指令回执 | 旧 `.id` 三分支无子命令分派（`itemid/IdCommand.java:83-193`） |
| 7 | `feature/identity/command/IdentityCommand.java:113` | `未知识别模式：` | 指令回执 | 旧项目只读当前模式，无非法模式回执（`itemid/IdCommand.java:110`） |
| 8 | `feature/identity/ui/IdentityModulePage.java:74` | `点击执行对应识别动作` | 设置行描述 | 无 |
| 9 | `feature/identity/ui/IdentityModulePage.java:76` | `开启后额外输出全部识别字段` | 设置行描述 | 无 |
| 10 | `feature/identity/ui/IdentityModulePage.java:76` | `详细输出` | 设置项名 | 旧对应项为 `方块语义调试`（语义不同，`08-规范` §1538 已记待单独迁移） |
| 11 | `feature/identity/config/IdentityModuleConfig.java:20` | `详细输出` | 设置项名（持久化键） | 同上 |
| 12 | `feature/identity/ui/IdentityModulePage.java:80` | `最近结果` | 分区分组名 | 旧仅有 `最近一轮`（语境不同） |
| 13 | `feature/identity/ui/IdentityModulePage.java:81` | `数据统计` | 分区分组名 | 无 |
| 14 | `feature/identity/ui/IdentityModulePage.java:87` | `快照与识别目标` | 折叠区标题 | 无 |
| 15 | `feature/identity/ui/IdentityModulePage.java:89` | `已保存的物品状态快照数量` | 设置行描述 | 无 |
| 16 | `feature/identity/ui/IdentityModulePage.java:91` | `已保存的方块状态快照数量` | 设置行描述 | 无 |
| 17 | `feature/identity/ui/IdentityModulePage.java:93` | `已选识别目标` | 设置项名 | 无 |
| 18 | `feature/identity/ui/IdentityModulePage.java:93` | `识别目标配置中仍有效的选中项` | 设置行描述 | 无 |
| 19 | `feature/identity/ui/IdConfigPage.java:135` | `输入后即时过滤当前 ID 清单` | 设置行描述 | 旧界面文案未迁移（`08-规范` §1539 记「随模块迁移」） |
| 20 | `feature/identity/ui/IdConfigPage.java:157` | `身份数据在磁盘上的位置` | 设置行描述 | 无 |

处置建议：#1~#7 改回旧原文或删除（旧项目没有这些信息位）；#8~#20 属「模块页面自造文案」，**随 ID 三件套收口一并按旧项目界面原文重写**（旧项目该模块界面文案逐字见 `09-规范-旧项目UI资产模板` 第五章）。

### 4.2 结构 / 行为改动（需拍板）

| # | 项 | 旧项目（证据） | 新项目现状（证据） | 性质 |
| --- | --- | --- | --- | --- |
| D1 | 播报前缀 | `§c§l[yiyiaddon]§r§f§l[模块名]§r`（旧 `core/YiyiaddonModule.java:294-298`、`CommandMessageFormatter.java:155`） | `§f§l[模块名]§r§l`（`core/ClientChat.java:9-32`、`CommandMessageFormatter.java:94`） | **规则冲突**：新项目依《开发习惯》第 110 条（明令前缀中禁止 `yiyiaddon`）执行，且该条声明取代第 102 条。需确认这是否仍是你的口径 —— 若是，`04-基准` 与 `02-基准-01` 的「唯一前缀格式」需标注例外 |
| D2 | 模块分类 | 旧 5 类，带 `§c§lyiyiaddon §X§l` 前缀，注册序 工具→自动化→绕过→辅助→星露谷（`core/AddonTemplate.java:84-95,242-249`） | 新 6 类，裸中文，序 自动化→辅助→工具→导航→附魔→星露谷（`module/AddonModules.java:74-79`） | 分类名属第 96 条「中文显示文本」不可改资产；**新增「导航 / 附魔」、缺「绕过」**，且顺序改变。当前 `AddonModules:74-79` 的 javadoc 自称「分类由本项目界面设计决定、禁止按旧项目增删或改名」，此自行授权与第 96 条冲突 |
| D3 | 新增模块 | 旧项目同域为「水源显示」（`water/WaterESPModule.java:78-79`） | 「ESP 测试」模块已进入模块中心（`feature/visuals/EspTestModule.java:73-74`、注册于 `module/AddonModules.java:57`） | 明示脚手架，但**已进入用户可见列表**；建议移出注册或删除 |
| D4 | 框架指令 | 旧项目无（旧项目模块开关、说明均由界面提供） | `.help`（`h`、`帮助`）、`.module`（`m`、`模块`） | 判定为框架能力，**保留**；但需你确认是否接受非旧项目指令存在 |
| D5 | `.id` 扩展 | 旧 `.id` 仅 `物品/实体/方块`（`itemid/IdCommand.java:83-193`） | 新增 `统计` / `模式` 子命令与 TAB 补全（`feature/identity/command/IdentityCommand.java`） | `.id` 名、子命令中文名、无参输出帮助、description **均已核实与旧项目逐字一致**（此前报告的两处怀疑不成立）；新增的 `统计/模式` 属多加，建议删除或改由模块界面承载 |

### 4.3 零消费方 / 未接线（非自造，但需清理决策）

| 项 | 位置 | 现状 |
| --- | --- | --- |
| 后端账号 / 统计 / 聊天链 | 8 个 service + `model/ReportPayload` 等 | 当前**零消费方**，仅彼此互调；`client/YiyiAddonClient` 未挂载 |
| 容器 / 发包底座 | `service/container/ContainerService`、`service/network/PacketService`、`platform/container/ContainerAccess`、`platform/network/BlockPacketSender` | 零外部调用 |
| 选择器 | `ui/screen/SelectorScreen` + `ui/component/SplitPanels` | 零消费方（`21-追加` 已自述待接线） |
| 设置控件 | `SettingColorPicker`、`SettingPasswordBox` | 零消费方 |
| 身份匹配 | `platform/identity/ItemIdentityMatcher` | 零消费方（旧项目由 autochest 消费，模块未迁移） |

---

## 五、存疑（未能定论，需人工裁决）

1. `service/RegisterService.java:39,48` 的 `注册上报失败` / `注册上报异常` —— 旧项目 grep 为空，模块名出处不明（`08-规范` §1541 亦标注未确认）。
2. 旧项目指令前缀「能否由用户修改」源码无法定论（旧项目全部经第三方框架注册；仅翻译表暗示框架内有 `Prefix` 设置）。新项目默认前缀 `.` 与旧项目一致。
3. Meteor 内置 GUI 的开启键是否为 Right Shift，未在旧源码定位；新项目自建默认键 Right Shift。
   > **后续变更（2026-09-17）**：默认开启键已按用户指令改为 **G**（`ModuleKeybindManager.applyDefaultBindings()`）；本条记录的是当时的自建默认值。
4. `统计` / `模式` 两个新子命令与旧 GUI 能力的逐字等价性未核。
5. 文字审计的 15 条存疑项（详见 `26-附A-文本出处核查.md` 附录，主要为装饰前缀差异与框架兼容文本）。
6. `ui/` 外壳实为第三方开源项目派生（`NOTICE:31-92` 有逐文件对照），与「自研」表述存在张力；`NOTICE` 内容已过期（仍提 `TestPage` / `SettingSlider` / `SettingButton`，现状均已不存在），建议改为「派生并改写」并重写 NOTICE。

---

## 六、处置执行记录（2026-09-14，用户逐项拍板）

| 项 | 用户决定 | 执行结果 |
| --- | --- | --- |
| `.resource` 指令 | **删掉** | 已删除 `command/ResourceCommand.java` 及其在 `AddonModules` 的注册点（`ResourceIndexProbe` 保留） |
| 3 个未注册主题类 | **删掉** | 已删除 `ui/theme/vape/VapeTheme.java`、`ui/theme/LightBlueTheme.java`、`ui/theme/DefaultClickGuiTheme.java` |
| `.id` 的 `统计` / `模式` 子命令 | **删掉** | 已删除两个子命令的分支、`handleMode()`、`parseMode()` 及相关 import；`SUBCOMMANDS` 恢复为 `物品 / 实体 / 方块`；补全与 `usage()` 同步收窄。连带消失的自造文本：`未知识别模式：`（原审计 #7） |
| 模块分类 | **改回来**（回归旧项目，且分类名不得出现 `yiyiaddon`） | 已改为旧项目 5 类与旧顺序：`工具 → 自动化 → 绕过 → 辅助 → 星露谷`（对应旧 `AddonTemplate.java:84-95,242-249`），显示名去掉旧项目的 `§c§lyiyiaddon ` 前缀段，只保留分类名；删掉自造的「导航」「附魔」两类；`AddonModules` 原 javadoc 中「禁止按旧项目增删或改名」的自我授权已删除 |
| ESP 测试模块（脚手架） | **不删** | 保持现状（仍注册在 `tools` 分类） |
| 播报前缀 | **不删**（不带 `yiyiaddon`，用模块名） | 保持现状 `§f§l[模块名]§r§l`；《开发习惯》第十七章第 110~113 条维持有效，我此前对该章的误改已撤销 |

编译验证：`gradlew.bat compileJava` 通过（exit 0）。

**本次新增铁律**：《开发习惯》第二十三章第 163~166 条 —— 项目完成前禁止擅自新增；非必要不得擅自修改；一切改动须先经用户同意；违规处置办法。

**遗留（待用户指示，未擅动）**
- 已按用户指示删除 `IdIdentifyModule.reportStats()` 与 `IdConfigModule.reportStats()`（原为 `.id 统计` 子命令服务，删除子命令后成孤儿），并清理随之失去用途的 import（`IdentityTargetConfig`、`CommandMessageFormatter`）。
- 删除后新增孤儿：`IdentityTargetConfig.countText(IdentityService)`（`:131`，原调用方即被删的 `reportStats()`）。其文案 `未选择目标（共 N 项）` / `已选 X / N 项` 属旧项目原文，建议保留待 ID 收口使用。
- 删除前即存在的孤儿链：`IdIdentifyModule.statsText()`（`:318`）→ `IdentityActions.statsText()`（`:170`）。
- 审计 §4.1 的 20 条自造文案中，除 `未知识别模式：`（随代码删除）与 `统计完成` / `已选目标` / `数据统计` 相关（随 `reportStats()` 删除）之外，其余仍在（集中在 ID 模块页面）。

### 6.1 ID 模块页面文字对齐（第二轮，2026-09-14，用户批准「全部改回来」）

**口径**：严格按《开发习惯》第 126 / 127 / 128 条 —— **壳不动，只换文字与交互**。页面外壳（本项目紧凑面板、控件、主题、按钮尺寸）一律保持现状；只把字段标签与说明改回旧项目逐字原文，并删除旧项目没有的元素。

**第一轮的判定更正**：第一轮曾把「20 条自造文案」理解为需要按旧项目按钮结构重做页面 —— 这是错的，违反第 126 条（界面工作只有换文字、对齐交互两件事）与第 128 条（按钮尺寸与排布由本项目适配，不参照旧项目）。实际核对后确认 `IdConfigPage` 的按钮本就已逐字落地，真正的问题只在 `IdentityModulePage` 的文字。

| 位置 | 原（自造） | 现（旧项目原文） | 依据 |
| --- | --- | --- | --- |
| `IdentityModulePage` `识别模式` 描述 | 拆分为三条的简写（`describe(mode)`） | `聊天复制/显示：识别手持物品后弹出结果屏幕；自动保存：识别手持物品后直接写入 ID 配置；准星方块识别：识别准星真实命中的方块。` | 旧 `IdIdentifyModule.java:63` |
| `IdentityModulePage` `当前模式` 行 | **缺失** | 标签 `当前模式`，描述 `当前选中的识别模式（实时显示）。`，值 `§a§l` + 模式名 | 旧 `IdIdentifyModule.java:66-69` |
| `IdentityModulePage` `详细输出` | 自造开关（作用于聊天逐行输出） | 标签 `方块语义调试` + 描述逐字原文；开关接到 `BlockStateModelResolver.setVerbose`（旧项目 `BlockStateModelResolver.DEBUG` 的等价物），载入时同步一次 | 旧 `IdIdentifyModule.java:73-80` |
| `IdentityModulePage` `点击执行对应识别动作` | 自造 | **已删**（旧项目以模块开关即识别，无此控件） | 旧 `IdIdentifyModule.java:54` 描述「点击开启即识别」 |
| `IdentityModulePage` `最近结果` / `数据统计` / `高级设置 › 快照与识别目标 › 物品快照 / 方块快照 / 已选识别目标` | 自造 | **已删**（旧项目该模块无这些设置项；身份库统计在 `ID配置管理` 页汇总行，已落地） | 旧 `IdIdentifyModule.java:60-80` 仅三个设置项 |
| `IdIdentifyModule.report()` 详细输出逐行回执 | 自造行为 | **已删**（旧项目无此回执） | 旧 `IdIdentifyModule` 无对应逻辑 |
| 持久化键 `详细输出` | 自造 | 改为 `方块语义调试` | 旧设置项名 |
| `IdConfigPage` 搜索行描述 `输入后即时过滤当前 ID 清单` | 自造 | **已删**（旧项目该行只有标签与输入框） | 旧 `IdConfigModule.java:133-135` |
| `IdConfigPage` 底部 `数据目录` / `身份数据在磁盘上的位置` | 自造 | **已删**（旧项目无此行；数据目录入口在 `更多管理` 内的 `打开 ID 总目录`） | 旧 `IdConfigPage` 对应 `IdManagementScreen` |

编译验证：`gradlew.bat compileJava` 通过（exit 0）。

**本轮遗留（未擅动，待指示）**
- 已按用户指示删除 `IdIdentifyModule.setModeIndex()` 的**模式切换聊天回执卡**（旧项目改模式只有 GUI 设置、无聊天回执），并删除随之失去用途的 `IdIdentifyModule.latestText()`、`IdIdentifyModule.statsText()`、`IdentityActions.statsText()`、`IdentityActions.selectedTargetCount()`、`IdentityModuleConfig.describe()`、`IdentityTargetConfig.countText()`，以及只为该回执卡存在的 `latest` 字段与其全部赋值点（含 `onDisable` 覆写）。
- **删除后新出现的孤儿（未处理，待指示）**：
  - `IdentitySummary.statusText()`（`:55`，原仅被 `latestText()` 调用）
  - `IdentityActions.itemSnapshotCount()`（`:157`）、`IdentityActions.blockSnapshotCount()`（`:161`，原仅被 `statsText()` / 页面统计行调用）
  - `IdentityTargetConfig.selectedItemKeys()`（`:83`）现仅被同文件 `:114` 内部调用
  - `IdConfigModule.countsText()`（`:396`）、`IdConfigModule.typeCounts()`（`:400`）—— 本轮之前即为孤儿

---

## 七、结论

1. **规模可控**：221 个类中「多加的」只有 3 个（已删），自造用户可见文本 20 条（全部集中在 ID 模块），行为差异 13 条。
2. **问题集中**：自造基本集中在**为 ID 模块自建的界面与播报**——与该模块「本来就不是以前那一套」的既有判断一致。
3. **已清 4 项**：资源指令 1 个 + 未注册主题类 3 个，编译通过。
4. **必须补的**：星露谷配置页「服务器资源」GUI 面板（旧项目真实入口，目前新项目缺失）。
