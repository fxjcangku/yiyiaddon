# 38 · 附A · 新窗口开工指令（复制粘贴即用）

日期：2026-09-16
用途：**新开对话窗口时，把下面整段发给智能体**。它替代「你上次说过什么」的口头交代——上下文、任务、已拍板范围、施工顺序、铁律全在里面。
维护：拍板或范围有变动时**改本文件**，不要靠对话记忆传递。本文件与 `38-阶段11-自动附魔移植侦察与决策.md`（含 §九 修订记录）必须保持一致。

---

```text
项目：d:\mcaddon\yiyiaddon（Minecraft 26.1.2 Fabric 自研客户端模组，Skija 自研壳，禁止依赖 Meteor 框架）
旧项目：d:\mcaddon\26.1.2（严格只读，任何情况下不得修改）
今天：2026-09-16

【第一步：连接上下文，按顺序读，不要跳】
1. 开发习惯.md（项目根目录，规则正本，最高优先级）
   —— 重点：第 180 条（模块页面构建必须推迟到打开时，禁止初始化阶段碰注册表）
      + §二十七 模块控制台铁律 181 / 182 / 183（移植模块一律做整屏分页控制台；「查看使用说明」必须排在「打开控制台」下面；旧独立设置页不做一比一交互复刻）
2. 02-开发报告/项目开发报告/00-AI阅读指引.md（导航与口径）
3. 同目录 01-总纲-迁移口径与复刻原则.md
4. 第 1 层六份基准（迁移时逐字对齐的唯一依据，禁止凭印象改写旧项目原文）：
   02-基准-01-状态播报体系 / 03-基准-02-自检启动机制 / 04-基准-03-配置页按钮与独立窗口清单 /
   05-基准-04-关联代码依赖网 / 06-基准-05-逐模块依赖全量清单 / 07-基准-06-指令体系全量清单
5. 30-移植总览-已移植与未移植全清单.md
6. 38-阶段11-自动附魔移植侦察与决策.md（本轮开工依据，已侦察完 + 已按旧代码逐条更正，**含 §九 修订记录，不要重复侦察**）
7. 37-阶段10-自动挖矿移植侦察与决策.md + 42-阶段10-自动挖矿移植记录与差异清单.md
   （照搬挖矿的施工做法、控制台框架、点位落盘范式、差异登记口径；KillAura 已在阶段10 全量落地）
8. 32-阶段09-星露谷农场移植记录.md 与 34-复盘-ID三件套差异核对与修复.md（照搬这套做法与复盘口径）
9. 旧项目现成附魔文档（迁移依据，只读，施工时逐字核对）：
   d:\mcaddon\26.1.2\07-开发报告\自动附魔\2026-09-01-自动附魔-最终报告.md（343 行，主依据）
   d:\mcaddon\26.1.2\07-开发报告\装备附魔\ 下 5 份（规则数据库 / 覆盖 / 验收全流程）

【第二步：任务】阶段11 自动附魔移植（范围已拍板，见 38 号第五 / 六节 + §九，不要重新决策）
- 搬旧项目 com.example.addon.enchant 全包 33 文件 / 7,726 行 + 资源文件
  gear-enchants.json（7,945 行 / 561,731 字节，原样搬，落点 src/main/resources/assets/yiyiaddon/）。
- 三模式互斥切换，只显示对应配置页、业务流程隔离：GEAR（原版装备极品附魔）/ BOOK（原版附魔书）/ CUSTOM（自定义附魔）。
- 本轮做（主干 + 界面一次做完整）：
  · 三模式骨架 + GEAR 全状态机（**18 态**，`State` 枚举共 30 = 通用 12 + GEAR 18；复用 WALK_TO_FARM / FARMING 做挂机刷经验）
  · 点位 **9 类**（point/PointType.java 实为 9 常量，**没有异常装备箱**）+ 跨服务器隔离 + 必填项自检 + 铁砧朝向持久化
  · 附魔数据层（VanillaEnchantDatabase / TargetMatcher / CandidateMatcher / CandidatePool /
    VanillaEnchantRuleValidator / EnchantPlanningEngine / EnchantEvaluationService / AnvilPlanner /
    GearCatalog / GearEnchantData / TargetProfile / GearEnchantTask / GearTaskQueue / AnvilPlan / AnvilStep /
    XpPlanner / GearSafetyGuard / RecoveryValidator / AcceptanceStrategy / TaskErrorReason / 识别适配层 3 文件）
  · 发包业务（附魔台 / 砂轮 / 铁砧 / 箱子，复用本项目 platform/network 发包层，不新建第二套）
  · 挂机刷经验：按 tooltip 含「横扫之刃」找剑 + 切手持 + toggle 本项目 KillAura + 到目标等级回落 gearReturnState
  · `.fumo` 指令全集（9 个点位节点 + 状态 + 清空，前缀与回执逐字不改）
  · 控制台（整屏分页）+ 使用说明页 + 状态播报 + 启动自检 + 18 组设置逐项落地
- 本轮不做（留白并登记，不得夹带）：
  · report/GearCraftReport.java（449 行，产出报告）—— 用户拍板**延后**，本轮不建 `report/` 包
  · 异常装备箱 —— **旧项目代码里已没有这个点位 / 箱子 / 状态**，不补指令、不补点位、不补界面
  · tactical 整套反检测（与附魔无关）、Meteor / 旧壳任何依赖

【第三步：用户已拍板、必须执行的硬约束】
1. 范围 = 主干 + 界面一次做完整；控制台与模块**同批交付**（同 37 号 5.3）：
   - 页面每一行直接读写模块自己的设置字段与点位表，不允许「模块做完再补一张图」；
   - 页面与 `.fumo` 指令同源同一份数据，双向一致；
   - 点位绑定 / 移除 / 失效校验只有模块那一处实现，页面与指令都调它；
   - 使用说明文案与 `.fumo` 回执逐字同源。
2. 代码结构 = `AutoEnchantBook.java`（3,230 行）**按职责拆分 Java**（状态机 / 设置 / 控制台三块），
   行为与判据逐字对齐，只挪位置不改语义，拆分差异登记进收口报告。
3. 界面 = **一律控制台样式（参照星露谷农场）**，`GearEnchantScreen`(311 行) / `EnchantmentSelectScreen`(77 行)
   **不做一比一交互复刻**（开发习惯.md 第 183 条）。构件统一用 `ui/console/ConsoleWidgets`
   （ConsoleRow / Ctl / Note / Table / FoldSection / ButtonStrip），骨架统一 `PanelScreen` + `ConsoleHost`，
   禁止另写分页壳。**设置项名称、描述、默认值、取值域、可见性条件、按钮与回执文案逐字照抄**。
   页签划分（沿用旧分组名与顺序，见 38 号 §六 补充）：
     GEAR   → 概览 · 点位 · 基础设置 · 原版装备附魔
     BOOK   → 概览 · 点位 · 基础设置 · 原版附魔分类
     CUSTOM → 概览 · 点位 · 基础设置 · 自动附魔分类 · 自定义附魔
   13 个子组在页内用 `FoldSection` 折叠；切换目标模式后**页签立即重排**（整页重建），
   并保留旧 `同步模式分组()`（:635-649）的**幂等保护语义**（避免遍历页签时重建页签集合 → ConcurrentModificationException 启动闪退）。
   「查看使用说明」按钮**必须排在「打开控制台」下面**（第 182 条）。
4. 挂机刷经验一比一还原（见 38 号 2.8），KillAura 已落地，本轮只做接线，不重复复刻。
5. `.fumo` 前缀与全部指令回执一个字不改。
6. 依赖前置已就位：KillAura（阶段10 已全量落地）、Baritone 封装、发包层、WorldContextFormatter 均复用，不新建。

【第四步：施工顺序】按批次推进，**先落批次 1 的两份侦察记录发我过目，再进批次 2**
（照 28/29、39/40 号的做法：逐字照抄旧项目源码原文 + 行号，分「逻辑层」与「界面与指令」两份）：
  批次1 → 43-阶段11-附录A-逻辑层侦察记录.md / 44-阶段11-附录B-界面与指令侦察记录.md
  批次2 → 骨架与数据（三模式枚举 / 18 组设置 / 9 类点位落盘 / gear-enchants.json 搬迁 + 原校验）
  批次3 → 附魔数据层（原版数据库 / 目标匹配 / 评分 / 铁砧规划 / 装备目录 / 断点恢复 / 耐久保护）
  批次4 → GEAR 18 态状态机 + 发包序列 + 挂机刷经验接 KillAura
  批次5 → 控制台（分页 + 18 组设置 + 使用说明）+ `.fumo` 指令 + 状态播报 + 启动自检
  批次6 → 收口报告（差异清单 / 留白登记）+ 编译验证

【第五步：开工前要问的事】本轮 6 个待定项 2026-09-16 **已全部拍板**（范围 / 界面 / 拆类 / 前缀 / 产出报告 / 异常箱），
见 38 号第五、六节与 §九。施工中若发现**代码与文档冲突，一律以代码为准**并登记，不要按文档补东西。已知漂移：
  (1) 旧文档列了 gear/RetryGuard.java，实际不存在 —— 重试逻辑已内联进 AutoEnchantBook；
  (2) 旧文档说 10 类点位含异常装备箱，实际代码 9 类、没这个箱子 —— 不要补点位 / 指令 / 状态。
任何本轮范围外的改动或新增，先问我再动。

【铁律速查】只换 UI 不换功能与文案；指令名与回执一个字不改；禁止新增旧项目没有的功能；
一切改动先经我同意；修复前必须取证（行号 / 存档 / 可复现实验）；同源逻辑两边逐字比对。
编译验证：./gradlew compileJava --console=plain -q
```

---

## 变动记录

| 日期 | 变动 |
| --- | --- |
| 2026-09-16 | 首版：由 38 号第五/六节 + 用户当日六条拍板（范围=主干+界面 / 控制台样式 / 拆分 Java / 挂机一比一 / 保留 .fumo / 产出报告延后 / 异常箱不存在）合成 |
| 2026-09-16 | 与 38 号 §九 修订对齐：点位 10→9 类、GEAR 17→18 态、异常装备箱改「不存在」、补开发习惯 181/182/183 与页签划分表 |
