日期：2026-09-19

# 测试版附件命名与发布说明精简

用户要求公开页面不显示混淆字样，附件名改为 `yiyiaddon-1.0-beta1.zip`；随后要求 Release 只写首个测试版与简短提示。

已将同一已发布 JAR 原样装入 ZIP，内部文件名为 `yiyiaddon-1.0-beta1.jar`。解压流 SHA-256 与原测试包一致：`10ae6ac89943e888635b97a1602656b082a5481c901750b414b4d32c36e3b6e3`，代码保护不变。ZIP 摘要为 `cdaa30d35ff5fdb6af5a3348e676fa628b22aecf4e2b5b15d1d5d82abef4c8c7`，已与 GitHub 上传附件 digest 比对一致。

Release 标题改为 `yiyiaddon 1.0 · 首个测试版`，正文仅三段：发布说明、环境与解压安装方式、问题反馈。保留 v1.0-beta1 标签和 Pre-release 标记。确认新 ZIP 上传完成后，删除旧 obfuscated.jar 及其校验附件；现在只有一个用户上传的 ZIP 附件，GitHub 自动生成的 Source code 附件由平台提供。

仓库首页同步去掉混淆字样并改为 ZIP 安装说明，公开提交 `df38c41`。本地版本正文与 HTML 预览同步。没有改业务代码、源码历史或内部映射；新项目仍为独立 Fabric 模组，旧项目未修改。

验证：普通 `./gradlew build --console=plain -q` EXIT=0；ZIP 内 JAR 摘要验证与 GitHub 回读通过。本次未实机，沿用同一 JAR 在报告 125 的启动验证边界。

## 下一阶段开发规划

### 一、下一阶段目标

收集首个 Beta 的使用反馈。

### 二、执行原因

完整游戏功能与长期性能仍需测试。

### 三、前置条件检查

ZIP 已公开，内容与此前测试 JAR 相同，内部映射保留。

### 四、任务拆分

1. 目标：定位反馈问题；涉及目录：测试实例；影响范围：测试数据；预计修改内容：取得复现证据后再定点修复。

### 五、验收标准

本轮构建、附件校验和发布信息回读通过，报告完成，无新增旧框架依赖；游戏内完整功能待实机验收。

### 六、禁止事项

不公开映射，不擅自改业务功能，不把附件更名说成新功能验收。

## 项目迁移路线图

已完成：ZIP 命名规范化与 Release 简化。

当前：Beta 公开测试。

下一步：按反馈取证。

未来：业务迁移沿用 30 号总览。
