# 56 · 阶段14 · 附录A · 逻辑层全量侦察记录（村民交易）

> 本文是 [55-阶段14-村民交易移植侦察与决策.md](55-阶段14-村民交易移植侦察与决策.md) 的**全量深扫记录**：16 文件全部全文细读（2026-09-17），供施工逐条对账。
> 口径：**只抄旧项目原文与行号，不做概括、不给建议**；行号为本机实测。
> 路径前缀：`OLD = d:\mcaddon\26.1.2\src\main\java\com\example\addon\villager\`。

---

## 一、状态机 `fsm\VillagerTradeFSM.java`（1,221 行）逐态行为

### 1.1 字段与常量（`:61-130`）

| 项 | 事实 | 行号 |
|---|---|---|
| 组件 | `navigation`（VillagerNavigationService）、`supplyService`、`unloadService`、`logger`/`completeHandler`/`errorHandler`（Consumer<String>） | `:61-67` |
| 任务配置 | `targetProfession` / `targets` / `maxPrice=64` / `sendCooldownTicks=2`（注释：交易发包节流 2 tick，实现「一次买满」连续交易）/ `emeraldThreshold=32` / `supplyStacks=1` / `targetQuantity=64`（默认榨干下不参与退出判定）/ `searchRange=96` / `idleLoop=false` / `restockWaitTicks=2400` | `:73-83` |
| Pipeline | `pipelineTasks` / `currentTaskIndex` | `:84-85` |
| 运行态 | `mode` / `currentState` / `stateTicks` / `currentVillager` / `currentWorkstation` / `exhaustedVillagers`（已榨干集合） | `:91-97` |
| 交易会话 | `tradePhase`（SELECT/CONFIRM）/ `skipOfferIndexes` / `offerIndex` / `usesSnapshot` / `confirmTicks` / `failStreak` / `sendCooldown` / `purchasedCount` / `tradeCount` / `perItemPurchased`（按物品轮换）/ `lastProgressAt` / `interactTries` / `openRetries` | `:99-112` |
| 去向 | `afterCloseRoute` / `stopReason` / `waitReason`（SUPPLY/UNLOAD，原地模式用） | `:114-117` |
| 常量 | `SEARCH_TIMEOUT=600`(30s) / `NAV_TIMEOUT=2400`(120s) / `OPEN_TIMEOUT=100`(5s) / `CLOSE_TIMEOUT=60`(3s) / `TRADE_IDLE_TIMEOUT=200`(10s 无进展换村民) / `CONFIRM_TICKS=20`(1s，避免同步延迟误判重发导致重复购买) / `MAX_CONFIRM_FAILS=2`(最多重发 1 次) / `INTERACT_RANGE=3.2`(生存交互 3.0+容差) | `:123-130` |
| 启动守卫 | `start/startPipeline` 非 IDLE → `§c当前任务未处于空闲状态，无法启动`；空目标 → `§c未配置职业或目标物品`；空队列 → `§c无 Pipeline 任务` | `:201-239` |
| `stop()` | navigation.stop + 两服务 reset + `ContainerBroker.closeContainer()` + resetSession + IDLE；wasRunning 才播 `§c✗ 状态机已停止` | `:244-253` |
| `handleException` | `§e⚠ 检测到异常 §8▸ <原因>` → stop()（断线/死亡/世界切换由模块调用） | `:258-261` |
| `loadTask` | Pipeline 换任务：加载职业/目标/价格/总量并清会话计数 | `:284-297` |
| **播报包装** | `log()` 统一加 `§8[<模式前缀>]§r ` 前缀，模式前缀 = `原地交易/寻路单点/多任务`（`modePrefix :1138-1144`） | `:1131-1135` |

### 1.2 逐态行为（tick 分派 `:303-326`）

| 态 | 行为要点（判据与行号） |
|---|---|
| SEARCHING `:332-376` | 玩家/世界无效 → fail；`stateTicks > 600`：PIPELINE 跳任务（`§e⚠ 搜索超时…跳过此任务`→NEXT_TASK）/ idleLoop → WAITING_RESTOCK / 其余 `§d⚠ <职业>已榨干…`→DONE。搜索半径：LOCAL 固定 16.0 格、寻路模式用 searchRange（`:354`）。取最近村民；锁定播报 `§a✓ 锁定村民 §8▸ <职业>`。LOCAL→OPENING_MENU，寻路→RESOLVING_WORKSTATION |
| `isValidTarget :378-400` | 排除死亡/幼年（`isBaby`）/已榨干；职业匹配必须用注册表 Identifier（`VillagerProfession` 是 Record，`value().equals()` 会误匹配傻子/失业，注释 `:384-385`）；LOCAL 额外要求 `distanceTo <= 3.2` |
| RESOLVING `:406-433` | 工作方块只搜村民脚下与紧邻水平一格（range=1，注释：避免搜到隔壁村民的工作方块把站位带偏）；找不到 → `§e⚠ <职业>村民附近未找到工作站 §8▸ 换下一个` + 加入榨干集合；找到播 `§a✓ 工作站 §8▸ <坐标>` |
| NAVIGATING `:435-469` | 首 tick 发起寻路；超时/卡死 → fail；路径被取消每 40 tick 重发；到达判据：有精确站位时必须 `hasArrived(standTarget, 0.5)`（玩家-工作方块-村民共线，站侧面不算）；无站位按 3.2 格 |
| OPENING_MENU `:484-534` | `containerMenu instanceof MerchantMenu` → prepareTradeSession → TRADING；超时或超距：寻路模式首次回 NAVIGATING 重新站位（openRetries==0），再失败换村民（`§e⚠ 无法与<职业>村民交互…`）；每 15 tick 重试交互（转视角 faceVillager `:539-554` + `gameMode.interact` 主手），`interactTries > 4` 换村民 |
| TRADING `:573-614` | 界面被关：**`mc.screen instanceof InventoryScreen` 才算玩家手动干预**（`containerMenu == inventoryMenu` 只是默认态不能误判，注释 `:581-587`）→ fail；否则 `§e⚠ <职业>交易界面已关闭` 换村民；僵局：`stateTicks - lastProgressAt > 200` → `§e⚠ <职业>长时间无进展 §8▸ 换下一个`；子相 SELECT/CONFIRM |
| SELECT `:616-680` | 退出①背包满 `TradeEngine.hasSpace()`：LOCAL → `waitReason=UNLOAD` + WAIT_PLAYER；寻路 → `§e⚠ 背包已满 §8▸ <职业>交易暂停，前往卸货` + Route.UNLOAD。取 offers 只能从 MerchantMenu（`Villager.getOffers()` 客户端抛异常，注释 `:631`）；`findBestOfferIndex` 无匹配：区分「售罄/超价」（`§e⚠ <目标>已售罄或超价…`）与「未刷出」（`§e⚠ <职业>未刷出目标物品 §8▸ <清单>`）；绿宝石 < 本单成本：LOCAL → WAIT_PLAYER(SUPPLY)；寻路 → `§e⚠ 绿宝石不足 §8▸ 无法购买<目标>，前往补给` + Route.SUPPLY。发包：记录 `usesSnapshot`、`failStreak=0`（换新 offer 重开连败计数 `:674`）、`TradeEngine.executeTrade(index)`、`sendCooldown=2`、CONFIRM |
| CONFIRM `:682-733` | **成功信号只认 offer uses 递增**（`SelectTrade` 会把绿宝石挪进付款槽，「绿宝石减少」是假阳性，注释 `:688-690`）；成功：`tradeCount++`、本笔获得 = uses 差值 × `safeResultCount`、`perItemPurchased` 累计（轮换用）、播 `§a✓ 第 N 笔成功 §7(+G <物品名>)` + `playSound(VILLAGER_TRADE)`（`:710-711`）；超 20 tick 未确认：`failStreak >= 2` → `§e该交易连续 N 次确认失败，跳过`（skip 该 offer、复位连败）；否则 `§e✗ 第 N+1 笔未确认，重发 x/2` + 刷新快照重发同一笔 |
| CLOSING_MENU `:744-765` | 静默模式没有 Screen，`containerMenu != inventoryMenu` 是唯一权威判断；每 5 tick `ContainerBroker.closeContainer()`，超 CLOSE_TIMEOUT 用 `player.closeContainer()` 兜底；关完 `dispatchRoute` |
| 路由 `:770-798` | SEARCH_NEXT / BACK_TO_VILLAGER（村民与工作站都在→NAVIGATING）/ SUPPLY / UNLOAD（`hasTaskItems` 才进卸货；PIPELINE 无货→NEXT_TASK，否则 DONE）/ FINISH / NEXT_TASK / WAIT_PLAYER |
| SUPPLY_NAV `:804-831` | 未绑定 `fail("未绑定绿宝石箱")`；维度不符 `fail("绿宝石箱在其他维度")`（`isSameDimension :1099-1105` 用 contains 兼容裸 ID 与旧 ResourceKey 格式）；首 tick 与每 60 tick 重发 `navigation.pathToContainer(box)`；`hasArrived(box, 3.0)` → SUPPLY_OPEN |
| SUPPLY_OPEN `:833-851` | 超 100 tick fail(`无法打开绿宝石箱`)；`containerId != 0` → SUPPLY_TAKE；每 10 tick `FarmPacketOps.interactBlock(MAIN_HAND, box, UP)` |
| SUPPLY_TAKE `:853-878` | 首 tick `supplyService.start(emeraldThreshold + supplyStacks * 64)`；COMPLETED 后校验仍低于阈值 → `fail("绿宝石箱已空 §8▸ 无法补给，任务停止")`（防「不足→空箱补给」死循环），否则 `§a✓ 补给完成 §8▸ 背包 N 绿宝石` → BACK_TO_VILLAGER；ERROR 时不足则停、否则 `§e⚠ 补给异常 §8▸ 有多少算多少继续交易` |
| UNLOAD_NAV/OPEN/TAKE `:884-954` | 对称补给（成品交易箱）；TAKE 首 tick `unloadService.start(targets)`；`UnloadService.State.FULL` → `fail("成品交易箱已满 §8▸ 请清空后重试")`（不能跳过否则永远失败，注释 `:939`）；COMPLETED/ERROR → `§e⚠ 卸货异常 §8▸ 跳过`（仅失败时）→ BACK_TO_VILLAGER（榨干：卸完回去继续榨） |
| WAITING_RESTOCK `:975-996` | 首 tick `§d⏳ 全部村民已榨干 §8▸ 等待补货 N 秒后循环`；每 100 tick `§7等待村民补货 §8▸ 剩余 N 秒`；结束清榨干记录 → `§a✓ 补货等待结束 §8▸ 开始新一轮交易`（注释：村民补货冷却 `allowedToRestock()` 2400 tick） |
| WAITING_PLAYER `:1001-1034` | 提示节流首帧 + 每 60 tick：`§e⚠ 绿宝石不足 §8▸ 请手动补给绿宝石后继续` / `§e⚠ 背包已满 §8▸ 请手动卸货后继续`；恢复条件 = 绿宝石 ≥ 32 或有空位；村民仍在 6.0 格内 → OPENING_MENU，否则 SEARCHING |
| NEXT_TASK `:1040-1057` | `§a✓ 任务 i/N 完成 §8▸ <职业> · 购入 X 件`；队列尽 → `§a§l✓ Pipeline 全部任务完成！` → DONE；否则 loadTask + `§b开始 Pipeline 任务 i/N §8▸ <职业>` → SEARCHING |
| DONE `:1059-1074` | 一次性：`§a✓ 交易完成 §8▸ <职业> · 共执行 N 笔 · 购入 X 件 §7(榨干模式)` 或 `§e⚠ 交易结束 §8▸ <职业>未购买到任何目标物品`；调 completeHandler → IDLE |
| ERROR `:1076-1085` | 一次性：navigation.stop + `§c✗ 运行出错 §8▸ <原因>` + errorHandler → IDLE |

### 1.3 FSM 全部播报原文清单（38 处，含包装前缀 `§8[<模式>]§r `）

`start/startPipeline` 守卫 2 条、`stop` 1 条、`handleException` 1 条、SEARCHING 3 条、RESOLVING 2 条、OPENING_MENU 1 条、TRADING 5 条、SELECT 4 条、CONFIRM 3 条、SUPPLY 5 条（含 fail 文案：未绑定/其他维度/超时卡死/无法发起/打开失败/交互失败）、UNLOAD 5 条、WAITING 4 条、NEXT_TASK/DONE/ERROR 6 条——**全部逐字见 §1.2 各行内引用与源文件行号，迁移时逐字照抄，模式前缀机制原样保留。**

## 二、服务层

### 2.1 `logistics\SupplyService.java`（170 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 职责边界（类注释） | 只负责「箱子已开后按节流搬绿宝石进背包」，开箱/关闭/寻路由状态机负责 | `:12-20` |
| 常量 | `OP_INTERVAL=2` / `READY_TIMEOUT=100` / `EMPTY_STREAK_LIMIT=8` | `:33-35` |
| `start(targetEmeralds)` | `§b开始搬运绿宝石，目标 N 个`；broker.reset + RUNNING | `:51-59` |
| `tick()` | broker.tick 先于就绪判定；同步超时 `§c容器同步超时`；达标 `§a补给达标（N 个）`；背包满 `§e背包已满，补给提前结束`（COMPLETED）；`withdrawOne(EMERALD)` 失败连 8 次 → 箱空（`§e绿宝石箱已空，补给提前结束` COMPLETED）否则 `§c补给搬运异常` ERROR | `:64-119` |
| 计数 | `countEmeralds`：全背包 + **副手**（注释：漏计导致补给量误判） | `:127-140` |
| 状态 | `IDLE/RUNNING/COMPLETED/ERROR` | `:164-169` |

### 2.2 `logistics\UnloadService.java`（187 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 核心原则（类注释） | 只卸当前任务目标物品，不碰私人物品；附魔书精准匹配类型；槽位走 ContainerBroker | `:14-25` |
| 常量 | `OP_INTERVAL=3` / `READY_TIMEOUT=100` / `DONE_STREAK_LIMIT=6` | `:38-40` |
| `start(targets)` | `§b开始卸货` | `:56-64` |
| `tick()` | `depositOne(isTaskTarget)`：MOVED 清连败；NOT_READY 等待；**CHEST_FULL → State.FULL**（注释：必须停机提示不能误判完成，提示语由 FSM fail() 统一输出）；连 6 次搬不动 → `§a卸货完成` COMPLETED；同步超时 `§c容器同步超时` | `:69-114` |
| `hasTaskItems` | 背包 0~35 命中白名单（附魔书走 EnchantmentMatcher） | `:119-132` |
| 状态 | `IDLE/RUNNING/COMPLETED/ERROR/FULL` | `:180-186` |

## 三、交易层

### 3.1 `trade\TradeEngine.java`（168 行，全静态工具）

| 方法 | 要点 | 行号 |
|---|---|---|
| `findBestOfferIndex` | 单次遍历；过滤 skip/售罄/超价/不匹配；**轮换策略：优先「已购数量最少」的物品，同物品内部选最便宜**（勾选多目标轮流买入）；客户端只读 MerchantMenu offers，禁止 `Villager.getOffers()`（抛 IllegalStateException，注释 `:41-42`） | `:48-72` |
| `sendSelectTrade` | `ServerboundSelectTradePacket(offerIndex)`（不依赖挥手） | `:77-85` |
| `executeTrade` | 两步缺一不可：① `setSelectionHint` + `tryMoveItems` + SelectTrade；② `handleContainerInput(containerId, 2, 0, QUICK_MOVE)`——`doClick` 对 QUICK_MOVE 进 while 循环逐次成交直到绿宝石尽/背包满/售罄（「按住 shift 快速买满」，注释 `:86-101`） | `:103-117` |
| `countItem` | 全背包 + 副手（漏计导致差额误判） | `:121-137` |
| `hasSpace` | 主背包 0-35 有空位 | `:149-157` |
| `safeResultCount` | 防御 offers 刷新越界 | `:162-167` |

### 3.2 `trade\TradeMatcher.java`（149 行）

- `matches`：非售罄 + 实际绿宝石成本 ≤ 上限 + 结果命中白名单（`:32-58`）。
- `matchesItemOnly`：忽略售罄与价格（区分「未刷出」与「售罄/超价」提示用，`:63-71`）。
- **`getEmeraldCost` 关键判据**：用 `getCostA()/getCostB()`（已叠加 demand 与 specialPriceDiff），不能用 `getBaseCostA()`——村民被大量购买后 demand 涨价，用基础价会漏买超价物品（注释 `:92-100`）。
- `getOfferDetails`：调试用，物品名走 `getHoverName()`。

### 3.3 `trade\EnchantmentMatcher.java`（99 行）

- 匹配逻辑：ENCHANTED_BOOK + `DataComponents.STORED_ENCHANTMENTS` + 附魔 **ID** 相等即匹配（**不比较等级**）；`ResourceKey.toString()` 是 `ResourceKey[...]` 格式，必须 `unwrapKey().identifier()`（注释 `:48`）；目标无 enchantmentId 时接受所有附魔书。
- `getEnchantmentDetails`：调试串 `id Lv.N`。

## 四、导航 `navigation\VillagerNavigationService.java`（318 行）

| 项 | 事实 | 行号 |
|---|---|---|
| `pathToContainer` | 前方可站位（北>南>西>东优先）→ `GoalBlock`，退化直接容器坐标 | `:49-56` |
| `pathToVillager` | **精确站位 = 工作方块前一格**（村民→工作方块方向延伸一格，玩家-工作方块-村民共线，隔工作方块正面交互）；正前方不可站立回退 `GoalNear(villager, 2)`；站位存 `villagerStandTarget` | `:69-79`、`:96-116` |
| `isStandable` | 脚下 `isCollisionShapeFullBlock`（1.21.5+ 替代 blocksMotion，注释 `:187`）+ 脚部/头部可通过 | `:184-200` |
| 卡死检测 | **寻路状态下每 tick 计数，每 3600 tick（3 分钟）检查一次，位移 < 5 格判卡死** | `:36-37`、`:255-287` |
| `stop` | `onLostControl` + `cancelEverything` + 清站位 | `:235-250` |
| disabled | 任何 Throwable → 永久 disabled（**旧口径**；与 FarmNav「每次独立判断」不同，登记差异：村民模块沿用旧语义还是换 FarmNav 口径——施工时按 D7 先例处理，见 55 号 §6 批次 2） | `:127-147` 等 |

## 五、数据表 `data\VillagerProfessionRegistry.java`（306 行）

- 13 职业注册（`registerProfession`：注册表 ID / 工作站方块 / 中文名 / 可购买白名单）：盔甲匠(高炉, 15 项铁链钻装备+盾+钟)、屠夫(烟熏炉, 6 熟食)、制图师(制图台, 地图/旗等)、牧师(酿造台, 5 项)、农民(堆肥桶, 8 项)、**渔夫(木桶, 5 项——注释 `:66-67`：渔夫是收船不是卖船，空桶也不卖)**、**制箭师(制箭台, 4 项——注释 `:72-73`：燧石与光灵箭是收的或没有，已剔除)**、皮匠(炼药锅, 6 项)、图书管理员(讲台, 6 项 + **附魔书单独处理不进普通白名单** `:83`)、石匠(切石机, 40 项——注释 `:88-89`：砖块方块/泥砖/泥坯没有，带釉陶瓦已补全)、牧羊人(织布机, 78 项——专家级卖旗帜)、工具匠(锻造台, 13 项)、武器匠(砂轮, 5 项)。
- **图书管理员附魔池 36 条**（`registerLibrarianEnchantedBooks :159-214`）：含中文注释逐条对照；**剔除灵魂疾行与迅捷潜行**（村民永远不卖，注释 `:160-161`）。
- 反查 API：`getWorkstation` / `getProfessionByWorkstation` / `getDisplayName`（null → `未知职业`）/ `getProfessionByDisplayName` / `getAllDisplayNames` / `isValidProfession`。

## 六、模型

| 类 | 事实 | 行号 |
|---|---|---|
| `VillagerTradeTarget`（78） | 字段 `item / displayName / enchantmentId`；`isEnchantedBook()`；`equals/hashCode` 按 item+enchantmentId | 全文 |
| `PipelineTask`（68） | `profession / targets / maxPrice / targetQuantity / completed`；构造钳制 `max(1)`；`toString` 调试串 | 全文 |

## 七、新项目底座映射核对（施工对照）

| 旧调用 | 新落点 | 差异处置 |
|---|---|---|
| `ContainerBroker.withdrawOne/depositOne/tick/isReady/reset/openMenu/countInChest/closeContainer` | `ContainerService`（实例）/ `ContainerAccess`（静态） | 同名同义；`DepositResult`→`ContainerTransferResult` 同值；`closeContainer()+reset()`→`close()` |
| `FarmPacketOps.interactBlock` | `BlockPacketSender.interactBlock` | 同签名 |
| `BaritoneAPI` 直连 + `disabled` 永久停用 | `FarmNav` 原语（每次独立判断） | 导航服务留模块内；**disabled 语义差异**：改用 FarmNav 口径（失败返回 false 由 FSM 重试/失败路径兜底），登记差异 |
| `ContainerBroker` 的 FSM `closeMenuAndRoute` 关界面 | `ContainerAccess.closeContainer()` | 同义 |
| `CommandMessageFormatter` / `HelpScreen` / `WorldContextFormatter` / `PlayerText` | 同名同位（`core/`、`ui/screen/`、`platform/world/`） | 直接换 import |

## 八、证据命令

```powershell
$root = 'd:\mcaddon\26.1.2\src\main\java\com\example\addon\villager'
# 全文行数复核
Get-ChildItem $root -Recurse -Filter *.java | ForEach-Object {
  '{0,5}  {1}' -f (Get-Content $_.FullName).Count, $_.FullName.Replace("$root\",'') }
# 播报全清单（本附录 §1.3 即此命令逐条核对结果）
Select-String -Path "$root\fsm\VillagerTradeFSM.java" -Pattern 'log\('
# 死代码零调用
Select-String -Path (Get-ChildItem 'd:\mcaddon\26.1.2\src' -Recurse -Filter *.java).FullName `
  -Pattern 'TradeRecognizer|VillagerTradeAdapter' | Select-Object Path
```
