# 48 · 附A · 新窗口开工指令（复制粘贴即用）

日期：2026-09-16
用途：**新开对话窗口时，把下面整段发给智能体**。它替代「你上次说过什么」的口头交代——上下文、任务、硬约束、开工前要问的事全在里面。
维护：拍板或范围有变动时**改本文件**，不要靠对话记忆传递。

---

```text
项目：d:\mcaddon\yiyiaddon（Minecraft 26.1.2 Fabric 自研客户端模组，Skija 自研壳，禁止依赖 Meteor 框架）
旧项目：d:\mcaddon\26.1.2（严格只读，任何情况下不得修改）
今天：2026-09-16

【第一步：连接上下文，按顺序读，不要跳】
1. 开发习惯.md（项目根目录，规则正本，最高优先级）
2. 02-开发报告/项目开发报告/00-AI阅读指引.md（导航与口径）
3. 同目录 01-总纲-迁移口径与复刻原则.md
4. 第 1 层六份基准（迁移时逐字对齐的唯一依据，禁止凭印象改写旧项目原文）：
   02-基准-01-状态播报体系 / 03-基准-02-自检启动机制 / 04-基准-03-配置页按钮与独立窗口清单 /
   05-基准-04-关联代码依赖网 / 06-基准-05-逐模块依赖全量清单 / 07-基准-06-指令体系全量清单
5. 30-移植总览-已移植与未移植全清单.md（推进顺序与逐包清点）
6. 48-阶段13-自动农场移植侦察与决策.md + 49/50/51 三份附录 + 52-阶段13-批次2-底座与逻辑层迁移记录 +
   54-阶段13-批次3-外围移植记录
   （本轮开工依据，侦察与批次 1~3 已全部完成，不要重复侦察、不要重写附录；
   逻辑层与外围均已落位，只调用不重排）
7. 47-阶段12-管理员检测移植记录与差异清单.md 与 32-阶段09-星露谷农场移植记录.md（照搬这两套做法：
   只换 UI 外壳、功能与文案逐字、点位/选择器/帮助页共用同一套壳）

【第二步：任务】阶段13 自动农场移植（D1~D9 已全部拍板，见 48 号第五节，不要重新决策）
- 从**批次 4** 开工（批次 1~3 已全部落库，见第一步第 6 条与 52/54 号）。
- 搬旧项目 com.example.addon.autofarm（42 文件 5,454 行）中**除死代码 8 文件 366 行**之外的全部，
  并**连带搬 `com.example.addon.water\WaterESPModule`（1 文件 366 行）**——D1 拍板，见 48 号 2.10。
  合计 35 文件 / 5,454 行。
- 模块形态：站位农场。两个点位框出矩形农田，框内「熟一颗收一颗 / 自动补种 / 自动锄地 / 自动拾取」，
  四箱物流：单作物箱、多作物箱、种子补货箱、杂物箱。不寻矿、不跨维度、无远端传送。
- 状态名逐字保留 8 个：观察 / 收割 / 补种 / 锄地 / 拾取 / 卸货 / 补货 / 杂物处理。
  注意旧项目里状态**只写不读**（FarmController 无任何 state 分支），行为由「当前任务对象」决定，
  状态名只用于状态条播报——照抄这个结构，不要自作聪明把行为挂到状态上。
- 指令 .farm（别名 nongchang）：设置 / 移除 / 状态 / 清空 / 扩展，回执逐字，见 48 号 2.5 与 50 号第四节。
- 界面：**模块页 + 整屏控制台**（D2）；15 个逐作物设置项进**独立窗口、按作物折叠**（D3）。
- 界面质量（D9，用户原话「带图标的选择器 控制台 细节细节记得做好」）：
  四类作物选择器与逐作物窗口每行**必须有图标**，不得出现纯文字行；点位卡片带图标；
  控制台行高/间距/悬停/tooltip 对齐既有页面，不允许半成品页；交付前逐页截图自查。
- 水源显示模块：分类落「工具」（utility），渲染走 WorldOverlay.Layer，10 个设置项逐项落地。

【第三步：三条硬约束】
1. 死代码不搬（48 号 2.6，D4 拍板）：adapter/ 两文件 + recognition/ 六文件，旧项目零实例化零调用。
   但 CropProfile 里的 isMaxAge / isHarvestable / isPlantable 与 FarmScanner.isTillable 这四段判据
   **是真实生效的识别逻辑，必须逐行照搬**（本项目没有任何原版作物识别，见 48 号 R1）。
2. 底座只复用不重造（48 号第三节）：
   容器 → service/container/ContainerService + platform/container/ContainerAccess
   发包 → platform/network/BlockPacketSender（含预测编号 + 本地置空气，批量瞬间破坏靠它）
   导航 → FarmNav（Baritone 隔离层，与旧类逐行等价；D7 拍板先把它提升到 platform/ 共享，
          星露谷改为引用，改完必须重新编译验证并在差异清单登记）
   渲染 → ui/render/world/{WorldOverlay,EspRenderer,ShapeMode}
   帮助页 → ui/screen/HelpPanelScreen；多目标选择器 → ui/screen/SelectorScreen；点位卡片 → ui/console/PointCardGrid
   指令排版 → core/CommandMessageFormatter
3. 禁止新增旧项目没有的功能；界面形态、按钮文案、自检文案、使用说明七章节全部逐字。
   设置项 16 可视静态 + 6 隐藏锚点 + 15 逐作物（外加水源模块 10 项）逐项落地，一个不许少、不许改名。
   自检上限 MAX_CROPS = 3 照旧（D6）；.farm 别名 nongchang 保留（D5）。

【第四步：施工顺序】按 48 号第六节批次推进（**批次 1、批次 2 已完成，从批次 3 开始**）：
批次 1 ✅ 三份侦察记录已落库（49 逻辑层 / 50 界面与指令 / 51 设置项控件对照表），若有疑问先读，不要重写；
批次 2 ✅ 底座与逻辑层已落位（D7：FarmNav 提升到 platform/navigation 并改星露谷引用重编译通过；
       模型 7 类 / FarmScanner / FarmResourceManager / 任务 13 类 + InventoryOps / 控制器层 8 类，
       编译通过，零旧框架依赖，逐文件对位见 52 号第二节，只调用不重排）；
批次 3 ✅ 外围已落位（模块入口 / `.farm` 指令回执逐字 / 模块页与六卡片 / 整屏控制台五页签 /
       逐作物独立窗口 / 帮助七章节 / 自检与播报 / 世界渲染三项 / 水源显示模块并已在 AddonModules 注册；
       编译通过，逐文件对位与差异登记见 54 号）；
批次 4 收口（实机验收 + D1~D9 核对 + 总差异清单）——任务拆分见 54 号第十节；
批次 4 收口（编译 + D1~D9 逐条核对 + 单机自建农田实机 + 生成总差异清单报告）。
每批做完先编译（./gradlew compileJava --console=plain -q），再往下走；每批结束向用户汇报并写报告。

【第五步：实机验证环境】单机自建农田（D8）：框一块田，种小麦/胡萝卜/马铃薯/甜菜根/下界疣/竹蔗/瓜类，
逐条跑 48 号第七节 11 项验收清单；水源显示模块用 9×9 灌溉范围与建议点实测。

【铁律速查】只换 UI 不换功能与文案；指令名与回执一个字不改；禁止新增旧项目没有的功能；
一切改动先经用户同意；修复前必须取证（行号 / 存档 / 可复现实验）；同源逻辑两边逐字比对；
代码美观 + 详细中文注释（第 208 条，硬性验收项，与编译通过同级）；
API 三步查不到必须继续 grep 参考库官网源码，禁止猜（第 207 条）。
编译验证：./gradlew compileJava --console=plain -q
```

---

## 变动记录

| 日期 | 变动 |
| --- | --- |
| 2026-09-16 | 首版：由 48 号第二/三/五/六节合成，待用户拍板 D1~D8 后回改第三节范围 |
| 2026-09-16 | D1~D8 拍板回改：范围加入水源显示模块（D1）、界面定为模块页 + 控制台（D2）、逐作物项进独立窗口（D3）、FarmNav 提升到 platform（D7）、验证环境改单机（D8）；「开工前先问」整节替换为「实机验证环境」 |
| 2026-09-16 | 批次 1 完成后回改：读文件清单加入 49/50/51；「从批次 2 开工」；新增 D9 界面质量（选择器带图标 / 控制台细节）；设置项计数纠正为 **16 可视静态 + 6 隐藏锚点 + 15 逐作物**（依据：`AutoFarmMatrix.java` 逐条 `add(` 计数，见 51 号第二节） |
| 2026-09-17 | 批次 3 完成后回改：读文件清单加入 54 号；「从批次 4 开工」；批次 3 标注已落位（17 新文件 + AddonModules/PointCardGrid 两处报备改动）；批次 4 拆分指向 54 号第十节 |
| 2026-09-17 | 批次 2 完成后回改：读文件清单加入 52 号；「从批次 3 开工」；施工顺序标注批次 2 已落位（FarmNav 提升与逻辑层 28 文件，对位表见 52 号）；批次 4 产物改名「总差异清单报告」（52 号已被批次 2 迁移记录占用）；铁律速查加入第 207/208 条（API 兜底走参考库官网源码 / 代码美观 + 详细中文注释） |
