# 50 · 阶段13 · 附录B · 界面与指令侦察记录（自动农场）

> 本文是 [48-阶段13-自动农场移植侦察与决策.md](48-阶段13-自动农场移植侦察与决策.md) 的**界面与指令层**逐行侦察记录。
> 口径：**用户可见文案一律逐字抄录（含 `§` 颜色码），不做改写、不补标点**；行号为本机实测（2026-09-16）。
> 路径前缀：`OLD = d:\mcaddon\26.1.2\src\main\java\com\example\addon\`
> 关联：[49-阶段13-附录A-逻辑层侦察记录.md](49-阶段13-附录A-逻辑层侦察记录.md)、[51-阶段13-附录C-设置项控件对照表.md](51-阶段13-附录C-设置项控件对照表.md)。

---

## 一、旧项目界面资产总览

| 资产 | 载体 | 事实 | 行号 |
|---|---|---|---|
| 模块页 | `AutoFarmMatrix.getWidget(GuiTheme)` | 一屏搞定：标题 + 状态摘要 + 使用说明按钮 + 水源显示开关 + 六张点位卡片（两列三行） | `AutoFarmMatrix.java:690-736` |
| 使用说明窗口 | `new HelpScreen(theme, this, buildHelpContent())` | 标题 = `自动农场 - 使用说明`（`HelpScreen.java:28`）；七个章节 | `AutoFarmMatrix.java:701`、`:803-853` |
| 独立窗口 / 控制台 | — | **旧项目没有**（04-基准-03 第 1.2 节只登记按钮） | — |
| 指令 | `.farm`（别名 `nongchang`） | 五个子命令 | `NongChangCommand.java:42`、`:46-67` |
| 状态播报 | 状态条 + `statusSummary()` | 8 状态名 + 一行摘要 | `AutoFarmMatrix.java:463`、`:739-754` |
| 水源模块界面 | `WaterESPModule` 设置分组 | 4 组 10 项，无指令、无独立页 | `WaterESPModule.java:43-151` |

---

## 二、模块页逐行（逐字）

`AutoFarmMatrix.getWidget` 组装顺序（`:690-736`）：

| 序 | 逐字内容 | 行号 |
|---|---|---|
| 1 | 标题标签 `"§b§l自动农场 §r§8▸ §f全自动农业系统"` | `:693` |
| 2 | 状态摘要标签 = `statusSummary()` | `:695` |
| 3 | 空行 `" "` | `:697` |
| 4 | 按钮 `"§e查看使用说明"` → `mc.setScreen(new HelpScreen(theme, this, buildHelpContent()))` | `:700-701` |
| 5 | 水源开关按钮：`waterActive ? "§a关闭水源显示" : "§b打开水源显示"` → `waterModule.toggle(); mc.setScreen(null);` | `:705-714` |
| 6 | 卡片行 1：`农场点位1`（`§a`）+ `农场点位2`（`§e`） | `:718-721` |
| 7 | 卡片行 2：`单作物箱`（`§6`）+ `多作物箱`（`§d`） | `:724-727` |
| 8 | 卡片行 3：`种子补货箱`（`§b`）+ `杂物箱`（`§c`） | `:730-733` |

### 2.1 状态摘要 `statusSummary()`（逐字拼装，`:739-754`）

```
"§7作物 §8▸ " + (enabled.isEmpty() ? "§8未选择" : "<作物名用 §f、 连接，每个名字走高亮>)"
+ " §8│ §7锄地 §8▸ " + (autoTill ? "§a开" : "§c关")
+ " §8│ §7收割 §8▸ " + (BATCH ? 高亮("批量") : 高亮("单个"))
```

### 2.2 点位卡片 `buildLocationCard(...)`（逐字，`:757-796`）

| 片段 | 逐字 | 行号 |
|---|---|---|
| 卡片标题 | `<color>` + 标题（`农场点位1` / `农场点位2` / `单作物箱` / `多作物箱` / `种子补货箱` / `杂物箱`），居中 | `:763` |
| 已绑定 · 坐标 | `String.format("§7X§f%d §7Y§f%d §7Z§f%d", x, y, z)` 居中 | `:767-769` |
| 已绑定 · 维度 | `"§7" + dimensionName(dimension)` 居中 | `:771` |
| 未绑定 · 第一行 | `"§8暂未绑定"` 居中 | `:774` |
| 未绑定 · 第二行 | `"§8-"` 居中 | `:776` |
| 设置按钮 | `(isBound ? "§a" : "§8") + "设置"` → `NongChangCommand.setBinding(type); mc.setScreen(null);` | `:780-784` |
| 删除按钮 | `"§c删除"` → `NongChangCommand.removeBinding(type); mc.setScreen(null);` | `:788-792` |

---

## 三、使用说明七章节（逐字全文，`AutoFarmMatrix.java:803-853`）

```
【准备工作】
  §8├─ §f建好农田 §7(耕地或对应底盘)
  §8├─ §f用 .farm 设置 农场点位1 / 农场点位2 框出矩形范围
  §8├─ §f智能绑定单作物箱/种子补货箱/多作物箱 §7(单物品→单箱，双物品→种子+多箱)
  §8└─ §f绑定杂物箱 §7(独立处理毒马铃薯与仙人掌花)

【点位设置 §7(指令)】
  §8> §3.farm 设置 农场点位1 §8— §7准星对准农田对角起点
  §8> §3.farm 设置 农场点位2 §8— §7准星对准农田对角终点
  §8> §3.farm 设置 单作物箱 §8— §7准星对准箱子(单物品作物)
  §8> §3.farm 设置 种子补货箱 §8— §7准星对准箱子(双物品种子)
  §8> §3.farm 设置 多作物箱 §8— §7准星对准箱子(双物品成熟掉落物)
  §8> §3.farm 设置 杂物箱 §8— §7准星对准箱子
  §8> §3.farm 扩展 4 §8— §7面朝方向把农田向外扩 4 格(东/南/西/北)

【作物选择】
  §a▸ §f最多同时启用 3 种作物
  §a▸ §f普通作物 §8- §7小麦、胡萝卜、马铃薯、甜菜根、下界疣
  §a▸ §f柱状物 §8- §7竹子、甘蔗、仙人掌
  §a▸ §f果实 §8- §7南瓜、西瓜
  §c▸ §f不支持 §7海带、甜浆果

【单种子独立配置】
  §a▸ §f启用几种作物 §8- §7面板就自动显示几种独立配置
  §a▸ §f卸货数量 §8- §7该作物产物超过此组数才卸入作物箱
  §a▸ §f补货种子数量 §8- §7种植材料低于此组数自动补货
  §e▸ §f双物品成熟掉落物卸入多作物箱 §8· §7其余卸入单作物箱

【收割模式】
  §a▸ §f单个收割 §8- §7每次只收一个成熟目标，收完补种拾取后再观察
  §a▸ §f批量收割 §8- §7一次性锁定全部成熟目标，瞬间暴力破坏
  §e▸ §f批量收割无数量上限 §8- §7默认无限量智能收割智能补种

【工作流程】
  §a[0] §f锄地 §8→ §7范围内草方块/泥土自动锄成耕地
  §a[1] §f观察 §8→ §7分帧扫描农场，发现成熟目标
  §a[2] §f收割 §8→ §7熟一颗收一颗，验证后接补种/拾取
  §a[3] §f补种 §8→ §7需要补种的作物自动播种
  §a[4] §f拾取 §8→ §7就近等待掉落物进入背包
  §a[5] §f物流 §8→ §7卸货/补货/杂物独立处理

【注意事项】
  §c⚠ §f模块运行中无法修改点位，必须先关闭模块
  §c⚠ §f已绑定点位不允许覆盖，必须先删除再重新设置
  §c⚠ §f自动寻路依赖 Baritone，不可用时移动任务会失败并重新规划
  §c⚠ §f关闭模块立即停止，重新开启会重新观察
```

章节标题逐字：`准备工作`、`点位设置 §7(指令)`、`作物选择`、`单种子独立配置`、`收割模式`、`工作流程`、`注意事项`（`:805-851`）。
壳层渲染：标题框三行在 `HelpScreen.java:76-78`；章节标头格式 `"§3[§b#§3] §f" + section.title`（`HelpScreen.java:83`）。

---

## 四、`.farm` 指令（逐字）

### 4.1 类 javadoc 帮助（逐字，`NongChangCommand.java:22-31`）

```
.farm                               查看六个锚点的绑定情况
.farm 设置 农场点位1/农场点位2       绑定农田范围对角
.farm 设置 单作物箱/种子补货箱/多作物箱  绑定对应作物箱（必须是容器）
.farm 设置 杂物箱                绑定杂物独立处理箱（必须是容器）
.farm 移除 任意锚点               解绑
.farm 状态                        显示详细信息
.farm 清空                         清空全部锚点
.farm 扩展 格数                   面朝方向把农田向外扩 n 格（东/南/西/北）
```

注册：`super("farm", "绑定自动农场的农田范围与物流箱子。", "nongchang")`（`:42`）；播报名常量 `MODULE_NAME = "自动农场"`（`:39`）。

### 4.2 子命令树（`:46-67`）

| 子命令 | 参数 | 行号 |
|---|---|---|
| 裸 `.farm` | — | `:48` |
| `设置 <六个锚点>` | 每个 `SiteType.cn()` 一个 literal | `:50`、`:53-54`、`:58` |
| `移除 <六个锚点>` | 同上 | `:51`、`:53`、`:55`、`:59` |
| `状态` | — | `:60` |
| `清空` | — | `:61` |
| `扩展 <格数>` | `IntegerArgumentType.integer(1, 128)` | `:62-66` |

### 4.3 回执逐字（成功 / 失败全表）

**裸指令 / `状态`（`showStatus` `:69-89`）**

| 场景 | 逐字 |
|---|---|
| 标题 | `CommandMessageFormatter.of("自动农场", "锚点绑定状态").world()` |
| 未绑定字段 | 字段名 `type.cn()`，值 `"§8未绑定"` |
| 已绑定字段 | 字段名 `type.cn()`，值 `site.describe("§b")` |
| 状态行 | `Level.INFO`，`bound + " / " + SiteType.values().length + " 已绑定"`（即 `N / 6 已绑定`） |

**`清空`（`clearAll` `:91-113`）**

| 场景 | 逐字 | 行号 |
|---|---|---|
| 本来没绑定 | `六个锚点本来就都没有绑定。` | `:101` |
| 成功标题 | `已清空全部锚点` | `:106` |
| 成功字段 | `数量` = `bound + " 个"`；`提示` = `农田范围同时重置` | `:108-109` |
| 成功状态 | `Level.SUCCESS`，`已清空` | `:110` |

**`设置 <锚点>`（`bind` `:115-163`）**

| 场景 | 逐字 | 行号 |
|---|---|---|
| 不在世界 | `当前不在游戏世界中，无法绑定锚点` | `:120` |
| 模块运行中 | `模块运行中无法修改锚点，请先关闭模块` | `:125` |
| 已绑定 | `<锚点>已绑定，请先删除旧绑定再重新设置` | `:131` |
| 覆盖提示 | `§7提示：使用 §e.farm 移除 <锚点> §7删除` | `:132` |
| 准星无方块 | `准星未对准任何方块，请将准星对准要绑定的方块` | `:138` |
| 非容器 | `该锚点需要指向容器方块（箱子/桶/潜影盒等），但准星对准的不是容器` | `:143` |
| 取不到维度 | `无法获取当前维度信息` | `:149` |
| 成功标题 | `已绑定<锚点>` | `:155` |
| 成功字段 | `坐标`（coord）+ `维度` = `dimension().identifier().toString()` + `类型` = `type.cn()` | `:157-159` |
| 成功状态 | `Level.SUCCESS`，`已保存` | `:160` |

**`移除 <锚点>`（`unbind` `:165-184`）**

| 场景 | 逐字 | 行号 |
|---|---|---|
| 未绑定 | `<锚点>本来就没有绑定。` | `:171` |
| 成功标题 | `已解绑<锚点>` | `:176` |
| 成功字段 | `坐标` + `维度` + `类型` | `:178-180` |
| 成功状态 | `Level.SUCCESS`，`已删除` | `:181` |

**`扩展 <格数>`（`expandFarm` `:187-252`）**

| 场景 | 逐字 | 行号 |
|---|---|---|
| 不在世界 | `当前不在游戏世界中，无法扩展农田` | `:192` |
| 模块运行中 | `模块运行中无法修改农田范围，请先关闭模块` | `:197` |
| 范围不全 | `农田范围未绑定完整，请先用 .farm 设置 农场点位1 / 农场点位2 框出范围` | `:204` |
| 跨维度 | `农田锚点不在当前维度，无法按面朝方向扩展` | `:209` |
| 成功标题 | `已扩展农田` | `:243` |
| 成功字段 | `方向` = `facing.cn()`；`格数` = `n + " 格"`；`新范围` = `rangeX + "×" + rangeZ`；`农场点位1` = `ns.describe("§a")`；`农场点位2` = `ne.describe("§e")` | `:244-248` |
| 成功状态 | `Level.SUCCESS`，`已保存` | `:249` |

朝向换算（`facingOf` `:255-261`，Minecraft 0° 朝南、90° 朝西）：`西(-1,0)` / `北(0,-1)` / `东(1,0)` / `南(0,1)`（`record Facing(int, int, String)` `:268`）。扩展只改 X/Z、Y 保持锚点原值（`:219-233`，注释 `:186`）。

### 4.4 静态入口（供模块页卡片调用）

| 方法 | 行为 | 行号 |
|---|---|---|
| `hasBinding(SiteType)` | `module.site(type) != null` | `:304-307` |
| `setBinding(SiteType)` | 复用实例方法做校验（运行中 / 已绑定 / 准星 / 容器）后 `bind(type)` | `:309-336` |
| `removeBinding(SiteType)` | 未绑定则 `farmError(type.cn() + "本来就没有绑定")`；否则 `clearSite` + `farmInfo("§c§l✗ 已删除 " + type.cn() + " 绑定")` | `:338-349` |

---

## 五、自检文案逐字（`FarmSelfCheck.check` `:33-91`）

| # | 触发 | 逐字 | 行号 |
|---|---|---|---|
| 1 | 未启用作物 | `§e作物§f·未启用任何农作物`（随后立即 return） | `:38` |
| 2 | 超过 3 种 | `§c作物§f·当前版本最多支持 3 种农作物（当前 N 种）`（随后立即 return） | `:42` |
| 3 | 点位 1 未绑 | `§a农场点位1§f·未绑定` | `:47` → `:94` |
| 4 | 点位 2 未绑 | `§e农场点位2§f·未绑定` | `:48` → `:94` |
| 5 | 单作物箱未绑（`!needsReplant() \|\| plantItem == harvestItem`） | `§6单作物箱§f·未绑定` | `:58-60` |
| 6 | 种子补货箱未绑（= 需要双作物） | `§b种子补货箱§f·未绑定` | `:61-63` |
| 7 | 多作物箱未绑（同上） | `§d多作物箱§f·未绑定` | `:64-66` |
| 8 | 杂物箱未绑（`extraLoot` 非空或 `junk()`） | `§c杂物箱§f·未绑定` | `:68-72` |
| 9-14 | 对应锚点不在当前维度 | `<同色标签>§f·不在当前维度` | `:75-80` → `:100` |
| 15 | 两对角重合 | `§c农田范围§f·农场点位1与农场点位2重合了` | `:87` |

失败播报格式（基类 `YiyiaddonModule`）：头行 `还差 N 项没配好，配完再开：`（`YiyiaddonModule.java:156`）；逐项 `§6  i. §f<条目>`（`:157-159`）；农场调用点 `AutoFarmMatrix.java:316`。
不在世界启动：`必须在进入世界后才能启动模块。`（`AutoFarmMatrix.java:307`）。

---

## 六、状态播报

8 个状态名逐字（`FarmState.java:15/18/21/24/27/30/33/36`）：`观察`、`收割`、`补种`、`锄地`、`拾取`、`卸货`、`补货`、`杂物处理`。
播报入口：`AutoFarmMatrix.java:463`（`broadcastState(controller.state())`）→ `broadcastState` `:466-476`、`isLogisticsState` `:479-481`。

---

## 七、本项目落地形态（2026-09-16 拍板 D2 / D3 / D9）

### 7.1 模块页（保留旧项目全部内容）

| 旧项目元素 | 本项目落点 |
|---|---|
| 标题 + 状态摘要 | `ui/component/ModuleStatusBar` + 模块页标题（沿用既有页面风格） |
| `§e查看使用说明` | `ui/screen/HelpPanelScreen`（`HelpPanelScreen(String moduleName, String[] helpContent, Screen parent)`），正文逐字用第三节七章节 |
| `§a关闭水源显示` / `§b打开水源显示` | 同文案，行为 = 切水源模块（D1 连带搬） |
| 六张点位卡片 | `ui/console/PointCardGrid`（两列 `PointCard`：图标 + 名称 + 坐标 + 维度 + 状态 + 设置/删除按钮） |
| 控制台入口 | 本项目新增（D2）：模块页放一个进入整屏控制台的入口 |

### 7.2 整屏控制台页签划分（D2，按本项目既有四页签范式）

| 页签 | 内容 | 对应旧项目 |
|---|---|---|
| 概览 | 只读实况：当前状态、启用作物、锄地开关、收割模式、六个点位状态、背包空格；按钮 `查看使用说明` | 模块页状态摘要 `:739-754` |
| 设置 | 19 个静态项的 5 个分组（`辅助工具` / `作物选择` / `运行参数`（收割模式、补种模式、杂物卸货、发包速率、收割距离）/ `渲染显示`） | `:84-88`、`:136-273` |
| 逐作物 | 「逐作物独立配置」入口 → **独立窗口**（D3），窗口内按作物分组折叠，每组 1~2 行（卸货数量 / 补货种子数量），文案逐字沿用 `<作物名>-卸货数量`、`<作物名>-补货种子数量` | `:177-208` |
| 点位 | 六张点位卡片（`PointCardGrid`），与模块页同一份数据 | `:717-734` |
| 日志 | 状态播报流水（8 个状态名 + 任务播报） | `:463` |

### 7.3 图标与细节要求（D9，用户原话「带图标的选择器 控制台 细节细节记得做好」）

| 位置 | 要求 |
|---|---|
| 四类作物选择器（双作物 / 单作物 / 柱状物 / 果实） | 每行必须有图标（原版物品贴图，取作物方块对应物品），**不得出现纯文字行**；候选与已选两栏都要有 |
| 逐作物独立窗口 | 每个作物分组头带图标；行内数字控件与既有页面同款 |
| 六张点位卡片 | 卡片头带图标（方块/箱子图标）；未绑定时也要有图标位，不留空洞 |
| 控制台每页 | 行高、间距、悬停反馈、tooltip 对齐既有控制台页面；不允许出现「半成品页」 |
| 交付前 | 逐页截图自查，细节问题在阶段报告里逐条登记 |

---

## 八、水源模块（`WaterESPModule`）界面资产

| 分组（逐字） | 项（逐字 / 类型 / 默认） | 行号 |
|---|---|---|
| 默认组 | `扫描半径`（整数 4~32 带滑条，`12`） | `:43`、`:82-88` |
| 默认组 | `渲染距离`（整数 4~64 带滑条，`16`） | `:90-96` |
| `灌溉范围显示` | `显示灌溉范围`（开关，`true`） | `:44`、`:99-103` |
| `灌溉范围显示` | `范围颜色`（颜色，`30,144,255,60`） | `:105-110` |
| `建议放水点` | `显示建议点`（开关，`true`） | `:45`、`:113-117` |
| `建议放水点` | `建议点颜色`（颜色，`255,0,0,180`） | `:119-124` |
| `建议放水点` | `建议点样式`（枚举，`Lines 线框 / Sides 面 / Both 两者`，默认 `Both`） | `:126-131` |
| `高级选项` | `显示水源方块`（开关，`false`） | `:46`、`:134-138` |
| `高级选项` | `水源方块颜色`（颜色，`0,0,139,180`） | `:140-144` |
| `高级选项` | `水源方块样式`（枚举，`Lines 线框 / Sides 面 / Both 两者`，默认 `Both`） | `:146-151` |

模块标题/描述逐字：`水源显示` / `已放水渲染蓝色 9×9 灌溉范围，在已有水源旁显示红色建议框提示可放水位置。`（`:78-79`）；旧分类「工具」→ 本项目 `utility`。
