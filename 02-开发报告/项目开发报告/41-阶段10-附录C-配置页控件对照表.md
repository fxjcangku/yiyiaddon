# 41 · 阶段10 · 附C · 自动挖矿配置页控件对照表（批次 5 施工前置）

日期：2026-09-16

- 用途：批次 5 写配置页之前，先把「设置项 → 本项目控件」定死，避免页面返工。
- 依据：旧项目设置项原文见 [`40-阶段10-附录B-界面与指令侦察记录.md`](40-阶段10-附录B-界面与指令侦察记录.md) 第 2 节（54 项）；本项目控件以真实文件为准（下述 `文件:行号`）。
- 结论先行：**不需要新建任何控件**。原先 40 号 §9.1 第 2 条「列表类设置项需新建自研列表选择器」的判断**作废**——本项目已有通用双栏选择器。

---

## 1. 可用控件清单（全部为本项目现有实现）

| 用途 | 控件 | 构造签名 | 证据 |
| --- | --- | --- | --- |
| 布尔开关 | `SettingToggle` | `(Supplier<Boolean> getter, Consumer<Boolean> setter)` | `ui/widget/SettingToggle.java:32` |
| 数值输入框 | `SettingNumberBox` | `(double min, double max, double step, String format, …)` | `ui/widget/SettingNumberBox.java:59` |
| 单行文本 | `SettingTextBox` | `(Supplier<String>, Consumer<String>, int maxLength)` | `ui/widget/SettingTextBox.java:45` |
| 分段选择（2~N 段） | `SettingSegmented` | `(List<String> labels, Supplier<Integer> selected, Consumer<Integer> onPick)` | `ui/widget/SettingSegmented.java:62` |
| 少选项循环 | `SettingCycle` | `(List<String> options, Supplier<Integer>, Consumer<Integer>)` | `ui/widget/SettingCycle.java:25` |
| 颜色（带调色板窗口） | `SettingColorPicker` | `(String title, EspColor color, Runnable onChange)` | `ui/widget/SettingColorPicker.java:47` |
| **列表设置入口** | `SettingLink` + `SelectorScreen` | `SettingLink(Supplier<String> label, Runnable action)`；`new SelectorScreen(标题, parent, entries, readSelected, onAdd, onRemove)` | `ui/widget/SettingLink.java:29`、`ui/screen/SelectorScreen.java:116` |
| 自由文本名单（无登记表可挑时） | `BaritoneTextListScreen` | `(Screen parent, String title, Supplier<List<String>> read, Consumer<List<String>> write)` | `integration/baritone/BaritoneTextListScreen.java:48` |
| 只读文本 / 按钮 | `SettingText` / `Button` / `IconButton` | `SettingText(Supplier<String>)` | `ui/widget/SettingText.java:30` |
| 行与分组容器 | `CompactStack` / `CompactRow` / `ListRow` / `CollapsibleSection` / `ButtonRow` | — | `ui/component/` |
| 页面基类 | `CompactModulePage` / `ModulePage` | — | `ui/page/` |

`SelectorScreen` 已具备：左栏候选 + 右栏已选、搜索过滤、分组标题、物品/方块贴图、增删聊天回执（`SelectorScreen.java:22-38`）。先例：`feature/autochest/ui/AutoChestPage.java:432`（物品多选）、`integration/baritone/BaritoneSettingsPage.java:246`（列表设置）。

---

## 2. 54 项设置逐项映射

### 2.1 目标选择（4 项，默认展开）

| 设置名 | 本项目控件 | 说明 |
| --- | --- | --- |
| 采集模式 | `SettingSegmented`（2 段：`精准采集` / `时运`） | 切换后目标选择器的候选集合与已选值需同步（旧 `onChanged → syncTargetsOnModeSwitch`） |
| 主世界矿石 | `SettingLink` + `SelectorScreen`（物品候选） | 候选按当前模式过滤：时运列掉落物、精准列原矿 |
| 下界矿石 | 同上 | 同上 |
| 普通方块 | `SettingLink` + `SelectorScreen`（方块候选） | — |

### 2.2 传送指令（9 项）

| 设置名 | 控件 |
| --- | --- |
| 前往挖矿指令 / 返回卸货指令 / 前往补给指令 / 前往修复指令 / 死亡返回指令 | `SettingTextBox`（5 条，maxLength 与旧输入框一致待定） |
| RTP需要GUI选择 | `SettingToggle` |
| GUI按钮关键词 | `SettingTextBox`（可见性：`RTP需要GUI选择` 为真） |
| 传送等待时长 | `SettingNumberBox`（1~120，整数，**无滑块**：旧为 `noSlider`） |
| RTP冷却时长 | `SettingNumberBox`（1~3600，整数，无滑块） |

### 2.3 触发条件（4 项）

| 设置名 | 控件 |
| --- | --- |
| 满载组数 | `SettingNumberBox`（1~36，整数，无滑块） |
| 食物阈值 | `SettingNumberBox`（1~64，整数，无滑块） |
| 耐久阈值 | `SettingNumberBox`（1~3000，整数，无滑块） |
| 潜影盒打包机 | `SettingToggle` |

### 2.4 物品管理（3 项）

| 设置名 | 控件 | 说明 |
| --- | --- | --- |
| 保留白名单 | `SettingLink` + `SelectorScreen`（物品候选） | 旧为 `ItemListSetting`，候选含任意物品 |
| 食物白名单 | `SettingLink` + `SelectorScreen`（物品候选，只列带 `FOOD` 组件的物品） | 旧 filter 同义；存盘需 `bypassFilterWhenSavingAndLoading` 等价行为（加载时不剔除） |
| 搭路方块白名单 | `SettingLink` + `SelectorScreen`（方块候选） | 改动后立即调用 `baritone().updatePlaceBlocks(...)` |

### 2.5 秒破（3 项）

| 设置名 | 控件 | 默认 |
| --- | --- | --- |
| 快速破坏（秒破） | `SettingToggle` | **关**（用户 2026-09-16 拍板；旧源码为开） |
| 绕过反作弊 | `SettingToggle` | 关 |
| 秒破间隔（tick） | `SettingNumberBox`（0~20，整数，无滑块） | 2 |

### 2.6 Baritone调优 · 开关类（19 项）

全部 `SettingToggle`：`破坏阻挡方块 / 寻路物流破坏方块 / 放置方块 / 自动整理物品栏 / 自动切换工具 / 避开岩浆 / 岩浆透视 / 怪物规避 / 掉落方块暂停 / 疾跑上坡 / 允许跑酷 / 跑酷搭桥 / 对角线上升 / 对角线下降 / 仅挖暴露矿石 / 失败目标暂时跳过 / 合法挖掘模式 / 合法挖掘检测对角矿石`（18 项），另 `岩浆透视范围` 见下表数值类。

> 除 `寻路物流破坏方块`（只由状态机在物流态压 `allowBreak`）与 `岩浆透视` / `岩浆透视范围`（模块自用）外，改动即时下调 `baritone().updateSetting(key, value)`，与旧项目逐项同键。

### 2.7 Baritone调优 · 数值类（9 项）

全部 `SettingNumberBox`（整数、无滑块）：

| 设置名 | 范围 |
| --- | --- |
| 岩浆透视范围 | 2~16 |
| 矿点刷新间隔 | 1~100 |
| 矿点缓存数量 | 1~256 |
| 怪物规避半径 | 1~16（可见性：`怪物规避` 为真） |
| 最大坠落高度 | 0~20 |
| 暴露矿石检测距离 | 1~8（可见性：`仅挖暴露矿石` 为真） |
| 最低挖掘高度 | -64~320 |
| 最高挖掘高度 | -64~320 |
| 合法挖掘高度 | -64~320（可见性：`合法挖掘模式` 为真） |

### 2.8 ESP（4 项，**不出现在页面**）

`字号(_esp_scale_internal)` / `矿石箱颜色` / `食物箱颜色` / `挂机修复点颜色`：旧项目为 `_xxx_internal` 隐藏项，**一比一不上页面**，但值仍落盘（`AutoMinerModule.syncColorsFromSettings/syncColorsToSettings` 已实现双向同步，供后续如需开放时直接接调色板）。

---

## 3. 页面骨架（与旧项目逐条对齐）

| 位置 | 内容 | 证据 |
| --- | --- | --- |
| 顶部 | 按钮 `§e查看使用说明` → `HelpPanelScreen`（标题 `自动挖矿 - 使用说明`） | 旧 `AutoMinerModule.java:1509`、`ui/HelpScreen.java:28` |
| 顶部 | ~~按钮 `检测假矿`~~ **留白**（随种子模式） | 用户裁定 |
| 中部 | 三张点位卡片：`矿物箱` / `食物箱` / `挂机修复点`，每张含标题、坐标 `§7X§f{x} §7Y§f{y} §7Z§f{z}`、维度、`§a设置`/`§8设置`、`§c删除` | 旧 `:1553-1613` |
| 下部 | 5 个设置分组，`目标选择` 默认展开、其余折叠 | 旧 `:99-103` |

三张卡片在本项目建议用 `ListRow`（名称 + 明细 + 尾部按钮）三行呈现——**本项目控件没有「一行三等分卡片」**；六行文案与按钮文字一字不改（用户 2026-09-16 已确认按此方向，未反对）。

---

## 4. 待用户确认

1. 隐藏的 4 项 ESP 是否维持「不上页面」（一比一）—— 当前按一比一。
2. 数值项是否需要滑块 —— 旧项目全部 `noSlider`，当前按「无滑块」。
3. 三张点位卡片的排布（三行 `ListRow` vs 其它）—— 文案与按钮不变，仅排布方式自定。
4. `SelectorScreen` 的窗口标题：旧项目 `ItemListSetting` 弹窗标题逐字为设置名（`保留白名单` / `食物白名单` / `搭路方块白名单`）—— 当前按设置名。
