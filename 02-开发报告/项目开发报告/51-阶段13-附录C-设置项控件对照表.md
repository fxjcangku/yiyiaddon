# 51 · 阶段13 · 附录C · 设置项控件对照表（自动农场 + 水源显示）

> 本文是 [48-阶段13-自动农场移植侦察与决策.md](48-阶段13-自动农场移植侦察与决策.md) 的**设置项**逐条对照表：旧项目每一项 → 本项目哪个控件、落在哪一页。
> 口径：**设置项名称、描述、默认值、上下限逐字保留**（旧原文见 [49 号](49-阶段13-附录A-逻辑层侦察记录.md) 与旧源码行号）；**只换控件外壳**。
> 旧源码：`OLD = d:\mcaddon\26.1.2\src\main\java\com\example\addon\`
> 本项目控件：`NEW = d:\mcaddon\yiyiaddon\src\main\java\com\yiyiaddon\ui\widget\`

---

## 一、控件映射总表（旧类型 → 本项目控件）

| 旧项目控件 | 本项目控件 | 宽度 | 依据 |
|---|---|---|---|
| `BoolSetting` | `SettingToggle`（`SettingToggle.java`） | 44 | 本项目现行设置页范式 |
| `IntSetting`（`.sliderRange(...)`） | **`SettingNumberBox`**（`SettingNumberBox.java`） | 100 | **本项目铁律**：`SettingNumberBox.java:17` 明确「设置项一律使用本控件，禁止使用滑块」；旧滑条形态不保留（登记差异 D-13-01） |
| `IntSetting`（`.noSlider()`） | `SettingNumberBox` | 100 | 同上 |
| `EnumSetting` | `SettingCycle`（`SettingCycle.java`） | 92 | 本项目枚举循环控件；两项枚举亦可用 `SettingSegmented` |
| `ShapeMode`（`Lines/Sides/Both` 三值） | `SettingCycle` | 92 | 本项目 `ui/render/world/ShapeMode` 已存在同名同值枚举 |
| `ColorSetting` | `SettingColorPicker`（`SettingColorPicker.java`）+ `ColorPickerScreen` | 100 | 本项目颜色控件；颜色块预览走 `SettingColorPreview` |
| `StringSetting`（隐藏锚点） | **不落成设置项**：落本项目点位数据 + 点位卡片 | — | 见第四节 D-13-02 |
| `StringSetting`（可见文本，本模块无） | `SettingTextBox` / `SettingPasswordBox` | 220 | 备用 |
| `BlockListSetting` / `ItemListSetting` | `SelectorScreen`（多选构造） | 全屏窗口 | 本项目多目标选择器；**每行必须带图标**（D9） |
| 只读值展示 | `SettingText` | — | 本项目只读文本控件 |
| 点位卡片（旧 `WTable` 手搭） | `PointCardGrid`（`PointCard` / `Grid`） | 两列 | `PointCardGrid.java:15-22` 声明由星露谷控制台抽出、两模块共用 |

---

## 二、自动农场 · 19 个静态设置项

分组逐字（旧 `:84-88`）：`辅助工具`（默认收起）／`作物选择`（默认展开）／`逐作物独立配置`（默认展开）／`运行参数`（默认收起）／`渲染显示`（默认收起）。

| # | 名称（逐字） | 描述（逐字） | 旧控件 / 默认 / 范围 | 本项目控件 | 落点 |
|---|---|---|---|---|---|
| 1 | `防踩踏` | `农田范围内拦截跳跃键，避免踩坏耕地` | Bool / `true` | `SettingToggle` | 控制台 · 辅助工具 |
| 2 | `自动锄地` | `农田范围内发现草方块/泥土时，自动拿锄头锄成耕地；背包无锄头则跳过` | Bool / `true` | `SettingToggle` | 控制台 · 辅助工具 |
| 3 | `双作物` | `种子与产物分离：小麦、甜菜根` | BlockList（筛 `isDoubleCrop`）/ 空 | `SelectorScreen`（多选，带图标） | 控制台 · 作物选择 |
| 4 | `单作物` | `产物即种子：胡萝卜、马铃薯、下界疣` | BlockList（筛 `isSingleCrop`）/ 空 | `SelectorScreen`（多选，带图标） | 控制台 · 作物选择 |
| 5 | `柱状物` | `切根部上方：竹子、甘蔗、仙人掌` | BlockList（筛 `isPillar`）/ 空 | `SelectorScreen`（多选，带图标） | 控制台 · 作物选择 |
| 6 | `果实` | `只砍果实：南瓜、西瓜` | BlockList（筛 `isFruit`）/ 空 | `SelectorScreen`（多选，带图标） | 控制台 · 作物选择 |
| 7 | `收割模式` | `单个收割：每次只处理一个成熟目标；批量收割：一次性锁定全部成熟目标瞬间暴力破坏` | Enum（`单个收割`/`批量收割`）/ `SINGLE`，`onChanged` 联动 | `SettingCycle` | 控制台 · 运行参数 |
| 8 | `补种模式` | `顺序优先：按作物枚举顺序种满一种再种下一种；均匀轮转：启用作物轮流种保持均衡；就近跟随：空耕地种回周围已有作物的同类，保持混种分区` | Enum（`顺序优先`/`均匀轮转`/`就近跟随`）/ `SEQUENTIAL` | `SettingCycle` | 控制台 · 运行参数 |
| 9 | `杂物卸货` | `杂物（毒马铃薯 + 仙人掌花）攒够多少个才卸货一次，避免捡一个就跑一次` | Int 1~64 无滑条 / `64`，可见性 = 有杂物类作物 | `SettingNumberBox` | 控制台 · 运行参数 |
| 10 | `发包速率(BPT)` | `每 tick 最多发送多少个破坏/播种/容器操作包` | Int 1~30 无滑条 / `10` | `SettingNumberBox` | 控制台 · 运行参数 |
| 11 | `收割距离` | `能操作多远的方块，原版上限约 4.5 格，低于 3 会导致寻路卡住` | Int 3~8 无滑条 / `4` | `SettingNumberBox` | 控制台 · 运行参数 |
| 12 | `农田边界` | `只渲染农场范围的外框一圈（不填面），大农场也不卡` | Bool / `true` | `SettingToggle` | 控制台 · 渲染显示 |
| 13 | `边界框颜色` | （无描述） | Color / `255,255,255,50`，可见性 = 农田边界 | `SettingColorPicker` | 控制台 · 渲染显示 |
| 14 | `目标显示` | `高亮当前正在作业的目标方块` | Bool / `true` | `SettingToggle` | 控制台 · 渲染显示 |
| 15 | `目标颜色` | （无描述） | Color / `0,255,100,75`，可见性 = 目标显示 | `SettingColorPicker` | 控制台 · 渲染显示 |
| 16 | `点位字牌` | `各绑定箱头顶显示防呆标签` | Bool / `true` | `SettingToggle` | 控制台 · 渲染显示 |
| 17-19 | 见第四节（六隐藏锚点占 6 项，此处 19 项中其余为 16 项 + 三个逐作物组外项已计入） | — | — | — | — |

> 计数口径：旧项目「19 个静态项」= 16 个可视项（上表 1~16）+ 3 个隐藏锚点字段（`_anchor_start` / `_anchor_end` / `_anchor_single`）之外的另 3 个（`_anchor_multi` / `_anchor_seed` / `_anchor_poison`）计入第六节隐藏项。施工时以旧源码 `AutoFarmMatrix.java:136-280` 逐行清点为准，**一项不许少**。

---

## 三、自动农场 · 15 个逐作物动态项（独立窗口，按作物折叠）

生成规则（旧 `:180-207`）：遍历 `CropProfile.values()`，跳过 `junk()`（仅仙人掌花）；每种作物 1 个「卸货数量」，`needsReplant()` 的作物再加 1 个「补货种子数量」。

| 项名生成式（逐字） | 描述（逐字） | 旧控件 / 默认 / 范围 | 本项目控件 |
|---|---|---|---|
| `<作物名>-卸货数量`（双物品作物） | `该作物成熟掉落物超过此组数才卸入 §d多作物箱§r（种子的盈余仍卸入种子补货箱）` | Int 1~36 无滑条 / `8` | `SettingNumberBox` |
| `<作物名>-卸货数量`（其余作物） | `该作物产物超过此组数才卸入 §6单作物箱` | Int 1~36 无滑条 / `8` | `SettingNumberBox` |
| `<作物名>-补货种子数量` | `该作物种植材料低于多少组时自动去作物箱补货，卸货时始终保留这批材料` | Int 1~10 无滑条 / `3` | `SettingNumberBox` |

逐作物清单（作物名逐字来自 `CropProfile.displayName()`）：

| 作物 | 卸货数量项 | 补货种子数量项 |
|---|---|---|
| 小麦（WHEAT） | `小麦-卸货数量` | `小麦-补货种子数量` |
| 胡萝卜（CARROT） | `胡萝卜-卸货数量` | `胡萝卜-补货种子数量` |
| 马铃薯（POTATO） | `马铃薯-卸货数量` | `马铃薯-补货种子数量` |
| 甜菜根（BEETROOT） | `甜菜根-卸货数量` | `甜菜根-补货种子数量` |
| 下界疣（NETHER_WART） | `下界疣-卸货数量` | `下界疣-补货种子数量` |
| 竹子（BAMBOO） | `竹子-卸货数量` | —（柱状物不补种） |
| 甘蔗（SUGAR_CANE） | `甘蔗-卸货数量` | — |
| 仙人掌（CACTUS） | `仙人掌-卸货数量` | — |
| 南瓜（PUMPKIN） | `南瓜-卸货数量` | — |
| 西瓜（MELON） | `西瓜-卸货数量` | — |
| 仙人掌花（CACTUS_FLOWER） | —（`junk()` 跳过，走全局 `杂物卸货` 阈值） | — |

**合计 10 个卸货项 + 5 个补货项 = 15 项**，与旧项目一致。
可见性口径（旧 `:193`、`:204`）：仅当该作物在「作物选择」中被启用时显示对应行 → 本项目改为**独立窗口内按作物分组折叠**（D3），组头显示作物图标与名称，组内行数 = 该作物实际拥有的项数。

---

## 四、六个锚点（旧为隐藏 `StringSetting`）与登记差异

| 锚点（逐字） | 旧载体 | 本项目落点 | 依据 |
|---|---|---|---|
| `农场点位1` / `农场点位2` / `单作物箱` / `多作物箱` / `种子补货箱` / `杂物箱` | 6 个 `.visible(() -> false)` 的 `StringSetting`（`_anchor_start` 等，`:276-281`、`:291-298`） | **点位数据 + `PointCardGrid` 卡片**（图标 + 名称 + 坐标 + 维度 + 状态 + 设置/删除） | 本项目点位一律走数据 + 卡片，不落隐藏设置项 |

| 差异编号 | 内容 | 原因 | 是否影响用户可见行为 |
|---|---|---|---|
| **D-13-01** | 旧 `IntSetting` 的 `sliderRange` 滑条 → 本项目 `SettingNumberBox` 数字框 | 本项目铁律：数字设置一律数字框，禁滑块（`SettingNumberBox.java:17`） | 否（值域、默认值、步进逐字保留） |
| **D-13-02** | 六个隐藏 `StringSetting` → 本项目点位数据 + 卡片 | 本项目点位体系不靠隐藏设置项承载 | 否（`.farm` 指令回执逐字不变） |
| **D-13-03** | 旧模块页为 Meteor `WTable` 手搭；本项目改为「模块页 + 整屏控制台」（D2） | 壳层自研 | 否（页面内容与文案逐字保留） |
| **D-13-04** | 旧 `BlockListSetting` 的 `filter(...)` 白名单 → 本项目选择器候选集直接给定 | 本项目选择器自带分组与图标 | 否（四类作物的可选范围逐字一致） |

---

## 五、水源显示 · 10 个设置项

| # | 分组（逐字） | 名称（逐字） | 描述（逐字） | 旧控件 / 默认 / 范围 | 本项目控件 |
|---|---|---|---|---|---|
| 1 | 默认组 | `扫描半径` | `扫描玩家周围多少格内的水源` | Int 4~32 滑条 / `12` | `SettingNumberBox`（D-13-01） |
| 2 | 默认组 | `渲染距离` | `只渲染玩家周围多少格内的框（可以比扫描半径大）` | Int 4~64 滑条 / `16` | `SettingNumberBox`（D-13-01） |
| 3 | `灌溉范围显示` | `显示灌溉范围` | `显示每桶水能覆盖的 9×9 耕地范围（蓝色大框）` | Bool / `true` | `SettingToggle` |
| 4 | `灌溉范围显示` | `范围颜色` | `灌溉范围的颜色` | Color / `30,144,255,60` | `SettingColorPicker` |
| 5 | `建议放水点` | `显示建议点` | `在已有水源的上下左右显示可放水位置（红色框）` | Bool / `true` | `SettingToggle` |
| 6 | `建议放水点` | `建议点颜色` | `建议放水点的颜色` | Color / `255,0,0,180` | `SettingColorPicker` |
| 7 | `建议放水点` | `建议点样式` | `Lines 线框 / Sides 面 / Both 两者` | Enum / `Both` | `SettingCycle` |
| 8 | `高级选项` | `显示水源方块` | `用小框标出水源方块本身（通常不需要，主要看灌溉范围即可）` | Bool / `false` | `SettingToggle` |
| 9 | `高级选项` | `水源方块颜色` | （无描述） | Color / `0,0,139,180` | `SettingColorPicker` |
| 10 | `高级选项` | `水源方块样式` | `Lines 线框 / Sides 面 / Both 两者` | Enum / `Both` | `SettingCycle` |

模块标题/描述逐字：`水源显示` / `已放水渲染蓝色 9×9 灌溉范围，在已有水源旁显示红色建议框提示可放水位置。`（`WaterESPModule.java:78-79`）。
渲染走本项目 `ui/render/world/`：三处框（灌溉范围 / 建议点 / 水源方块）对应 `EspRenderer.box(...)`，样式枚举用本项目 `ShapeMode`。

---

## 六、逐项落地核对清单（施工后逐条打勾）

| # | 核对项 | 通过判据 |
|---|---|---|
| 1 | 静态项 16 个可视 + 6 锚点 | 与 `AutoFarmMatrix.java:136-280` 逐行对照，名称/描述/默认值/范围/可见性全等 |
| 2 | 逐作物 15 项 | 与 `:180-207` 生成式对照，项名拼接一致（含 `-` 与中文） |
| 3 | 水源 10 项 | 与 `WaterESPModule.java:82-151` 逐行对照 |
| 4 | 选择器图标 | 四类作物选择器每行都有图标，无纯文字行（D9） |
| 5 | 点位卡片 | 六张卡片含图标、坐标、维度、状态、设置/删除按钮 |
| 6 | 控制台细节 | 行高 / 间距 / 悬停 / tooltip 与既有控制台页面一致，逐页截图自查（D9） |
| 7 | 差异登记 | D-13-01 ~ D-13-04 全部写入阶段报告 |
