# 45 · 阶段11 · 自动附魔移植记录与差异清单

日期：2026-09-16
状态：**代码全部落地；编译 `EXIT=0`；待实机验证**
依据：`38-阶段11`（含 §九 修订）+ `38-附A-新窗口开工指令` + `43/44 号侦察记录`
旧项目（严格只读）：`d:\mcaddon\26.1.2\src\main\java\com\example\addon\enchant\`（33 文件 / 7,726 行）

---

## 一、结论

| 项 | 结果 |
| --- | --- |
| 新代码规模 | `feature/enchant/` **45 文件 / 8,890 行** |
| 旧代码规模 | 33 文件 / 7,726 行（其中 **27 文件 / 7,033 行**实际搬入；449 行日志 + 244 行死代码按裁定不搬） |
| 禁止依赖 | 全目录 `meteordevelopment` **0 命中**、`com.example.addon` **0 命中** |
| 资源 | `gear-enchants.json` 561,731 B（与源字节一致）+ `enchantment/vanilla/` 79 文件 2,395,691 B（与源一致） |
| 编译 | `./gradlew compileJava --console=plain -q` → **EXIT=0** |
| 功能面 | 三模式（GEAR/BOOK/CUSTOM）+ 30 态状态机（通用 12 + GEAR 18）+ 9 类点位 + 18 组设置（174 项）+ 13 个多选组（189 词条）+ 控制台 6 页签 + 使用说明 8 章 + `.fumo` 指令全集 + 两套资源原校验 + 挂机刷经验接线杀戮光环 |

**范围 100% 落实**：38 号第五节「必做」全做完；「待定」三项里界面已按控制台形态交付、产出报告按用户裁定删除、识别/适配层按用户裁定不搬。

---

## 二、交付清单（45 文件 / 8,890 行）

### 数据与模型（批次2）
| 文件 | 说明 |
| --- | --- |
| `model/EnchantTargetMode` | 三模式（旧 `TargetMode:465-480` 逐字） |
| `model/EnchantRunMode` | 纯附魔 / 挂机循环（旧 `:482-496`） |
| `model/EnchantSuccessSound` | 12 项音效（旧 `:512-536`） |
| `model/EnchantPointType` | **9 类点位**：`title` / `node` / `posKey` / ESP 标签与颜色 / `requiredFor(mode)` |
| `model/EnchantPoint` | 坐标 record |
| `gear/AnvilStrategy` | 简单 / 节能 / 快速（旧 `AnvilPlanner.Strategy` 外提） |
| `config/EnchantSettings` | 全部设置项 + 189 词条 + `save/load` |
| `repository/EnchantPointStore` | 9 点位 + 挂机视角 + 铁砧朝向 + 归属世界，单文件落盘 |
| `EnchantModule` | 模块主体（已注册进 `AddonModules`） |

### 数据层（批次3，20 文件）
`vanilla/`：`VanillaEnchantDatabase`(398) / `TargetMatcher`(234) / `VanillaEnchantRuleValidator`(174) / `EnchantPlanningEngine`(113)
`gear/`：`TargetProfile`(106) / `AcceptanceStrategy`(32) / `TaskErrorReason`(39) / `EnchantEvaluationResult`(50) / `EnchantEvaluationService`(147) / `GearSafetyGuard`(49) / `XpPlanner`(54) / `RecoveryValidator`(73) / `GearEnchantTask`(189) / `GearTaskQueue`(78) / `AnvilStep`(89) / `AnvilPlan`(67) / `AnvilPlanner`(304) / `GearCatalog`(174) / `GearEnchantData`(261) / `GearEnchantConfig`(230，抽自 `GearEnchantSetting` 的纯编解码)

### 运行时（批次4）
`fsm/EnchantState`(41) / `fsm/EnchantStateMachine`(2,352) / `service/EnchantContainer`(229) / `navigation/EnchantPathing`(125) / `render/EnchantPointRenderer`(72)

### 界面与指令（批次5）
`ui/EnchantConsoleScreen`(528) / `ui/EnchantPage`(179) / `ui/console/`：`EnchantOverviewPage`(95) / `EnchantPointPage`(82) / `EnchantBasicPage`(174) / `EnchantGearPage`(297) / `EnchantSelectPage`(139) / `EnchantCustomPage`(79) / `EnchantConsoleText`(26) / `service/EnchantBindingService`(222) / `command/FumoCommand`(192)

---

## 三、逐字复核（本次用的三道机器核对）

1. **文案集合差分**：旧包全部 `.java` 与 `feature/enchant` 全部 `.java` 的双引号含中文文案字面量做集合比对 → 旧 667 条 / 新 645 条；「旧有新无」70 条已逐条归类（见 §四），**无一条属于「功能文案被改写」**（详见 §四的误报说明）。
2. **关键判据逐段比对**：`AnvilPlanner.readCost` 仍为 `return menu.getCost();`；`MAX_ANVIL_COST = 39`、`DEFAULT_MAX_OPERATIONS = 6`；`isAcceptable` 三档 switch 穷尽无 `default`；`GearEnchantData.validateTarget` 五项校验「只记日志不剔除」的原语义不变；12 项成功提示音映射与旧逐字一致；`AnvilStep step = gearAnvilPlan.next();` 仍在 `switch (gearAnvilPhase)` 之前。
3. **禁用引用扫描**：`meteordevelopment` / `com.example.addon` 全目录 0 命中。

---

## 四、文案差分的归类结果（旧有 70 条的去向）

| 类别 | 条目数 | 处置 |
| --- | --- | --- |
| 日志/报告的 HTML、表头、统计标签（`<h1>原版装备附魔 · 合成报告</h1>`、`合成报告-`、`对比汇总.json`、`浪费工具`、`铁砧经验`、`半成品/成品`、`跳过`…） | ~37 | **用户 2026-09-16 裁定删除日志功能** → 预期缺失 |
| 旧壳界面控件与空状态（`§7类别`/`§7品质`/`§7类型`/`§7极品方案`/`配置装备附魔`/`§c该类别暂无装备`/`§c未找到对应装备`/`§8暂无合成报告可删除`/`§b合成附魔详细`/`§c删除合成记录`） | 10 | 旧独立配置页按第 183 条**不做一比一交互复刻**；两条空状态在改造后的选择器结构里无触发点（登记 §五 D5） |
| 被删或被隐藏的设置项（`记录合成日志` + 其描述；`返回挂机视角` 的描述 `到达挂机位后恢复设置该点位时记录的视角`） | 3 | `记录合成日志` = D1；后两项因旧项目本身 `.visible(() -> false)`（界面从不显示）而不以字符串出现（登记 D11） |
| 死代码里的 detail 文案（`合法 30 级候选` 等） | 1 | 随 `CandidateMatcher` 不搬（D2） |
| **差分工具误报**（旧 `§7维度 §f` 与新 `"  §7维度 §f"` 因前导空格不等价、`把`/`件</td>` 等表格碎片） | ~19 | 实际显示文字一致，非差异 |
| **确认为真实差异** | **0** | — |

> 差分的实现局限：按**字符串字面量精确相等**比对，前缀空格、拼接拆分（`"A" + x + "B"`）都会计入不等价，故仍需人工归类——上表即为归类结论。

**「新有旧无」48 条**全部属本项目控制台新增文案（`打开控制台`、`点击选择`、`概览`、`§7刷新`、`§7模块：`、`§7状态机：`、`§7运行模式：`、`§7点位世界：`、`§7维度：§f`、`§a自检通过`、`§e自检缺 ` 等），其中 `§7维度：§f` 与 `§8暂未绑定  §8-` 的写法与自动箱子、挖矿控制台**逐字同款**（`AutoChestConsoleScreen:268`、`MiningConsoleScreen:279`），属项目控制台口径。

---

## 五、有意差异清单（**请逐条审**）

| # | 差异 | 原因 | 影响 |
| --- | --- | --- | --- |
| **D1** | **日志功能整体删除**：`report/GearCraftReport.java`(449 行) 不搬；`AutoEnchantBook` 里 10 处调用（`begin/disable/flush/recordSkip/recordEnchant/recordAnvil/recordGrind/recordTooExpensive/recordComplete`）删除；设置项 `记录合成日志` 及其描述一并删除 | 用户 2026-09-16 裁定「附魔日志功能不需要了」 | 「原版装备附魔」组设置项由 3 项变 2 项；不再生成 `.minecraft/yiyiaddon-reports/装备附魔合成/`；`合成策略` **保留**（影响铁砧合并排序，是真实功能） |
| **D2** | **死代码不搬**：`recognition/EnchantmentRecognizer`+`EnchantmentResult`、`adapter/EnchantmentAdapter`、`vanilla/CandidateMatcher`、`vanilla/CandidatePool`（共 5 文件 / 244 行） | 用户 2026-09-16「死代码删掉」；实测全项目零调用、零实现 | 无行为影响 |
| **D3** | **点位不做旧档自动迁移** | 服务器键口径不同：旧 `currentServer()` 取 `ip` 小写（单人写死 `"singleplayer"`），本项目统一 `WorldIdentity.server()`（`host:port` / `singleplayer:<存档名>`，第 45 条禁止另建第二套） | 旧项目附魔点位需重新绑定（`.fumo` 或控制台） |
| **D4** | **ESP 渲染通道**：旧为 2D 屏幕投影（`NametagUtils.to2D` + `TextRenderer.begin(0.6)`），本项目走 `WorldOverlay` 世界字牌（字号 2.0，与挖矿 ESP 同量级） | 项目 ESP 语言差异 | 标签文字、ARGB 颜色、落点（中心 +1.5）、6 个点位与门控（`ESP标点 && matchesCurrentContext()`）逐字不变；字幕大小与渲染通道不同 |
| **D5** | **装备配置交互形态**：旧「大类→品质→类型」三级下拉 + `配置装备附魔` 按钮 → 本项目可搜索选择器（按钮 `点击选择`）。旧两条空状态文案**已按用户裁定补回且可达**：`§c该类别暂无装备` 作为选择器内不可选的占位行（某大类无候选时出现）、`§c未找到对应装备` 作为静态库为空时的页面提示 | 开发习惯第 183 条（独立设置页不复刻交互）；2026-09-16 用户「2 修」 | **功能齐全**：`极品方案` 选择、逐条附魔的「等级 ±」「排除 / 启用 / §8核心」、`暂无极品方案` 提示、摘要 `未选择装备` / `未选择方案` / `§a…§8▸ §b…§8▸ §eN 项目标` 全部保留；两条空状态文案逐字且在有意义的位置触发 |
| **D6** | 成功提示音用 `SoundEvents` 12 项映射直接播放，**未复用**挖矿的 `SoundNotifier` | `SoundNotifier` 只支持固定音效，无法表达 12 项可配置 | 行为与旧一致 |
| **D7** | 启动拦截的停机方式：旧 `toggle()` → 本项目 `ModuleManager.setEnabledSilently` + `active` 门控 | 避免 `onEnable` 栈内重入 | 最终启用状态一致；当帧会先写一次 `enabled=true` 再由任务改回 false |
| **D8** | `closeContainer()` 增加「当前菜单非玩家背包」判断 | 与 `MiningContainer` 同做法，防误关玩家背包 | 正常路径行为不变 |
| **D9** | `gearItemOf` 走 `BuiltInRegistries.ITEM`（旧走 `mc.level.registryAccess()`） | 物品是静态注册表 | 取值等价 |
| **D10** | **已按用户裁定修正（有意偏离旧项目）**：`rebuildActiveTasks`（`EnchantStateMachine`）补入「工具与通用附魔属性」组（旧 `:1723-1727` 的数组漏了该组，而该组在 `:545` 是真实存在的多选控件 → 旧项目表现为「勾了不干活」）；补入位置与 CUSTOM 页控件顺序一致（护甲之后、原版组之前） | 用户 2026-09-16「修」 | 与旧项目行为**不同**：CUSTOM 模式勾「工具与通用附魔属性」的词条现在会被识别为任务 |
| **D11** | `ESP标点`、`返回挂机视角` 仍保持旧项目 `.visible(() -> false)` 语义 | 旧项目这两个设置项界面永不显示 | 控制台不显示这两项；**值照旧读写与落盘** |
| **D12** | `.fumo` 裸指令（只打 `.fumo`）给框架统一用法提示 | 旧为 Brigadier 解析错误（库文案） | 未新增模块自有文案；`.fumo 设置` / `.fumo 移除` 单独输入同理 |
| **D13** | 新增 `EnchantPointStore.clearHangoutView()` / `clearAnvilFacing()` | 为还原旧 `FumoCommand:143-149`「移除挂机位 / 铁砧时连带清附加数据」 | 不是新功能，是旧行为的必要支撑 |
| **D14** | 挂机位绑定时 `player == null` 静默拒绝 | 旧项目此处直接取 `player` 位置（不可达即 NPE）；**不新增文案** | 不可达路径，无用户可见影响 |
| **D15** | 控制台概览/点位页的系统行（`§7模块：`/`§7状态机：`/`§7运行模式：`/`§7点位世界：`/`§7维度：§f`/`§a自检通过` 等）为本项目新增 | 控制台承载结构由本项目决定（第 181/183 条） | 与自动箱子、挖矿控制台逐字同款；不含任何设置项改名或新增设置项 |

---

## 六、留白登记（本轮不做，无夹带）

1. `report/` 包（产出报告）—— D1，按用户裁定删除。
2. `recognition/` `adapter/` 与 `CandidateMatcher` / `CandidatePool` —— D2，按用户裁定不搬。
3. 附魔模块**图标码点**：`icon()` 暂用基类默认（留空）→ 模块卡片显示默认样式；待给一个**已验证**码点（开发习惯第 140 条）。
4. 旧壳 `GearEnchantScreen` / `EnchantmentSelectScreen` 的像素级交互——按第 183 条不复刻。
5. 旧项目 `AnvilStep` 注释写「右槽：附魔书」而实现是「装备+装备」——**按实现搬，注释原样保留**。

---

## 七、实机验证清单（待用户环境恢复后走一遍）

- [ ] 模块中心出现「自动附魔（enchant）」；开关一次能看到自检缺项（聊天 + 弹窗，第 176/178/179 条）
- [ ] 设置落盘：改 `目标模式` / `GUI操作延迟` / 勾几个词条 → **完全关闭客户端再开** → 值仍在（第 172/173 条）
- [ ] 控制台页签：GEAR / BOOK / CUSTOM 三模式各显示对应页签，切模式后页签立即重排（第 181/183 条）
- [ ] 多选控件：`点击选择` → 双栏加减 → `已选择 N 项` 与 `↻` 清空；写回后刷新不丢
- [ ] 点位页 + `.fumo`：9 个节点的 `设置 / 移除`、`状态`（9 行 + 匹配行）、`清空`；模式门禁与已绑定回执逐字
- [ ] 换服务器 / 换维度：点位不生效但不丢失；切回原世界可直接用（D3 后需重新绑一次）
- [ ] GEAR：装备箱按类型过滤取件 → 附魔台发包（槽0 装备 + 槽1 青金石 + 按钮 index 2）→ 评估 → 砂轮洗练重试 / 铁砧合并（`getCost`、>39 放弃）→ 成品箱卸货前复验
- [ ] GEAR：铁砧损坏 → 去铁砧箱取 → **回原位按朝向放置** → 恢复任务
- [ ] 挂机刷经验：横扫之刃剑自动换入 + 杀戮光环开/关 + 到目标等级回原状态（`30 + 3×(单轮-1)`）
- [ ] 耐久 <20% 停止、断点恢复、防死循环（重试上限 3）
- [ ] `极品附魔数量` 达标 → 批次汇总播报 + 自动停机
- [ ] 日志功能已无：跑完一轮不产生 `.minecraft/yiyiaddon-reports/装备附魔合成/`

---

## 八、遗留建议

1. ~~`D10`（工具与通用组不被识别）~~ → **已按用户 2026-09-16 裁定修正**（见 §五 D10）。
2. ~~`D5` 两条空状态文案~~ → **已补回且在有意义的位置触发**（见 §五 D5）。
3. **附魔模块图标码点待给**（见 §六）：现状 `icon()` 用基类默认，模块卡片显示默认样式（开发习惯第 140 条要求已验证码点）。
4. **附魔书图标（已按用户 2026-09-16 要求加上）**：旧项目在**多选控件入口行**（`EnchantmentSelectSetting:55`）与**9 张点位卡片**上画 `Items.ENCHANTED_BOOK`；**旧的多选窗口本身只有文字 + 加/减号，没有物品图片**。本项目为此给控制台公共件 `ConsoleWidgets.ConsoleRow` **新增链式可选图标位** `icon(Supplier<ItemStack>)`（走 `ItemIconCache`，`SkiaScreen` 已全局驱动「抽帧 + 截取」链路）：**不传图标时布局与悬停命中原样不变**，四个模块既有的控制台调用点零改动。附魔侧已在 **13 个多选行 + 9 个点位行**挂上附魔书图标。首帧不显示、下一帧起出现（`ItemIconCache` 既有机制）。
5. `EnchantConsoleScreen` 概览页每秒 `selfCheck()` → `pointStore.reload()`，与挖矿控制台同一口径；点位文件极小、写入即落盘，实测无风险；若日后点位变多可加缓存。
