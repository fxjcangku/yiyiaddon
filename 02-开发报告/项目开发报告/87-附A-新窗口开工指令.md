# 87 · 附A · 新窗口开工指令（复制粘贴即用）

日期：2026-09-18
用途：**新开对话窗口时，把下面整段发给智能体**。它替代「你上次说过什么」的口头交代——上下文、任务、硬约束、开工前要办的事全在里面。
维护：拍板或范围有变动时**改本文件**，不要靠对话记忆传递。

---

```text
项目：d:\mcaddon\yiyiaddon（Minecraft 26.1.2 Fabric 自研客户端模组，Skija 自研壳，禁止依赖任何第三方客户端框架）
旧项目：d:\mcaddon\26.1.2（严格只读，任何情况下不得修改）
今天：2026-09-18

【第一步：连接上下文，按顺序读，不要跳】
1. 开发习惯.md（项目根目录，规则正本，最高优先级；重点：第 96-105 用户交互资产、第 110-113 前缀与颜色、
   第 163-171 改动禁区与不得夹带新功能、第 167-170 取证与二分铁律、第 172-175 设置持久化、
   第 176-179 自检双通道与弹窗、第 180 页面构建推迟、第 181-183 控制台铁律、
   第 195-201 选择器与独立窗口、第 208 代码美观+中文注释、第 209-217 界面承载与说明时效）
2. 02-开发报告/项目开发报告/00-AI阅读指引.md（导航与口径）
3. 同目录 01-总纲-迁移口径与复刻原则.md
4. 第 1 层六份基准（迁移逐字依据，禁止凭印象改写旧项目原文）：
   02-基准-01-状态播报体系 / 03-基准-02-自检启动机制 / 04-基准-03-配置页按钮与独立窗口清单 /
   05-基准-04-关联代码依赖网 / 06-基准-05-逐模块依赖全量清单（附 04 · §02 图书管理员）/ 07-基准-06-指令体系全量清单
5. 30-移植总览-已移植与未移植全清单.md（推进顺序与逐包清点）
6. 阶段15 本轮开工依据（自动图书管理员）：
   87-阶段15-自动图书管理员移植侦察与决策.md（主报告：结论摘要 / 旧项目事实 / 底座 / 缺口 / D1~D9 / 分类对照表 / 批次）
   88-阶段15-附录A-逻辑层全量侦察记录.md（27 态逐态行为、全部判据与阈值、逐字播报与说明、死代码取证）
   ——侦察已全部完成，不要重复侦察、不要重写报告
7. 照搬这几套已验证的做法（只换 UI 外壳、功能与文案逐字）：
   42+43（阶段10 挖矿：控制台/ESP/指令）、45+46（阶段11 附魔：多选择器/逐项设置）、
   52+54+57（阶段13 农场：底座复用、控制台外围、交付前文案机对 120/120 与自造件审计方法学）、
   55+56+58+59（阶段14 村民交易：同属村民系，交易协议与导航先例、D 表拍板与差异登记口径）

【第二步：开工前第一件事】向用户提交 D1~D9 拍板表（87 号第五节），等用户批复：
  D1 零调用死件是否不搬：LecternService(21) / NoOpMovementService(41) / calculateMarkerBlockPosition /
     EnchantmentService 三个默认方法 / setServices+getServices / softReset+restart
     （建议：照村民 D1「不搬并登记」）；VillagerTarget 富状态 API + 3 枚举建议整份照搬
  D2 暂停键语义（旧项目自身不一致，第 168 条强制二分）：实现=「按下→§c§l已强制结束→关模块」，
     描述=「切换暂停/继续，保留进度」；paused 恒 false
     （建议：照实现 A，键位落 ModuleKeybinds，描述照旧留 tooltip）
  D3 目标附魔候选集：旧 = 注册表全部附魔（不是村民 35 项白名单）
     （建议：照旧全量，按第 195/197 条分类，复用 VillagerSelectors 的 8 家族表 + 兜底「其他」）
  D4 控制台页签：旧项目无控制台、无点位、无 ESP
     （建议：概览 / 设置 / 日志 三页签；设置页内 4 折叠组 目标/行为/通知/调试，快捷键行置页末）
  D5 播报口径：7 条状态播报 + 三通道全部经 clean() 抹标点后交旧 formatMessage
     （建议：抹标点行为逐字保留；出口走 CommandMessageFormatter，前缀按 110-113 改 [自动图书管理员]）
  D6 交易发包落点：村民模块已有 SelectTrade + QUICK_MOVE；本模块另有 MerchantMenu 读取、sameOffer 比对、
     库存计数验证（第 169 条禁止同源两份）
     （建议：抽 platform/container/MerchantTradeAccess 一处，两模块共用）
  D7 编排器是否拆：729 行 / 26 handler，未超第 38 条阈值（建议：单文件照搬）
  D8 音效只认 SUCCESS 态（建议照搬并登记现状）
  D9 分类与注册：旧 CATEGORY_AUTOMATION → 本项目 automation（权重 30），AddonModules 追加一行
拍板后把结论回改进 87 号第五节与本文件。

【第三步：任务】阶段15 自动图书管理员移植（旧 com.example.addon.librarian 52 文件 4,552 行）
- 搬旧项目除死代码外的全部（D1 定稿后）：单点自动刷附魔书，无模式、无点位、无指令、无落盘。
- 流程与判据必须逐条照搬（88 号附录A §1 逐态表 + §3 判据表）：
  搜索失业村民（200 tick 空转提示）→ 移动到村民（半径 3）→ 工位探测（yOffsets {0,-1}、两遍：先讲台位空闲、
  再取离玩家最近的可清障候选；60 tick 超时）→ 移动到站位（半径 1）→ 清障（200 tick 超时，状态机 0/1/2）→
  放置讲台（放置前等 15 tick 停稳、先同步朝向、验证朝向）→ 等职业（检测到后等 10 tick 让服务端生成交易）→
  打开交易（前 3 tick 等 AI，之后每 2 tick 重试；超时 100）→ 读第一笔附魔书报价 → 命中判定（六连：
  tradable/!invalid/输出为附魔书/ID 相等/等级相等/该附魔最高等级==目标等级/绿宝石≤上限）→
  未命中则拆讲台刷新（等失业 + 刷新延迟 10 tick）→ 命中则选交易（sameOffer：containerId + 全等快照）→
  等同步（selectedIndex + 结果槽 2 非空）→ 取出（QUICK_MOVE 槽 2）→ 验证（购买前后库存计数增加）→
  完成（找到后移除目标可配）→ 结束村民周期 / 全部完成。
- 静默交易三件套必须逐字保留：gameMode.interact 真实开界面 + OpenScreenEvent 取消容器屏
  （放行 InventoryScreen 与 CreativeModeInventoryScreen）+ 只读 MerchantMenu.getOffers()
  （客户端读 Villager.getOffers() 会抛异常，类注释已写明）。
- 移动：allowBreak=false（只走通路）+ GoalNear(目标, 半径) + 30 tick 宽限期 + 8 个状态枚举。
- 讲台：选最快工具（热键栏 0~8 → 背包 9~35 移槽 0，速度阈值 > 1f）、start/continueDestroyBlock、
  useItemOn 命中面取 lecternFacing、方报名用本地化名不用 Block.toString()。
- 模块层资产逐字：模块名与描述、启动报告 4 行（全角空格 + §8▸）、自检 5 类缺项原文、
  使用说明 5 章（准备/自动流程/静默交易/场地要求/注意，含 §8├─ §8└─ §a[1] §7▸ §6▸ §c⚠）、
  7 条状态播报、error 2000 ms 节流、22 个音效中文名与 SoundEvents 映射。
- 设置 12 项 / 5 组逐字（名称、默认、域、描述），含 4 项不可配硬编码值（到达半径 3 / 交易界面超时 100 /
  交易同步超时 100 / 移动超时 1200）。

【第四步：三条硬约束】
1. 底座只复用不重造（87 号第三节全表）：
   寻路原语 → platform/navigation/FarmNav（模块内导航服务照 feature/villager/navigation 先例）
   控制台 → ui/console/{ConsoleWidgets,ConsoleHost,ConsoleMetrics,ConsoleHeaderBar}
   模块页 → ui/page/CompactModulePage（无顶部状态行）；帮助内嵌 → HelpPanelScreen#inlineContent
   选择器 → ui/screen/SelectorScreen（多选）+ VillagerSelectors 分组先例
   设置控件 → SettingNumberBox（禁滑块）/ SettingToggle / SettingCycle / SettingKeybind + AddonKeybind
   模块快捷键 → core/module/ModuleKeybinds + ModuleKeybindManager
   自检弹窗 → ConfirmPanelScreen.notice（第 176-179 条：聊天 + 弹窗 + 打开设置，条目 §c§l）
   播报 → core/CommandMessageFormatter（前缀 [自动图书管理员]，不出现 yiyiaddon）
   事件 → core/event ClientEventType.SCREEN_OPEN（对齐旧 OpenScreenEvent）
   发包/容器 → service/container/ContainerService、platform/container/ContainerAccess、platform/network/BlockPacketSender
   禁止自造壳件（54 号「状态变更」节是前车之鉴）。
2. 死代码不搬（第 43 条已覆盖）：adapter/ 1 + recognition/ 2 = 3 文件 68 行；D1 定稿的其余零调用死件一并登记。
3. 禁止新增旧项目没有的功能：无指令（不新增 .xxx）、无点位库、无 ESP、无模式切换、无落盘业务文件；
   界面形态、按钮文案、自检文案、使用说明 5 章、启动报告、7 条状态播报、12 项设置全部逐字。
   新代码一律 feature/librarian/<职责>/ 分包 + 详细中文注释（第 208 条，硬性验收项）；
   API 查不到按第 207 条走 01-开发参考库 官网源码 grep，禁止猜、禁止 Yarn 旧名。

【第五步：施工顺序】拍板后按 87 号第六节批次推进：
批次 1 ✅ 侦察记录落库（87 主报告 + 88 附录A + 本开工指令）；
批次 2 逻辑层：model/ 14 → fsm/ 3 → service/ 契约与结果类型 → platform/ 与 integration/ 实现 →
       service/LibrarianOrchestrator；每步与旧源码逐段比对；
批次 3 外围：模块入口（自检/启动报告/静默容器/音效/快捷键）→ 模块页 + 内嵌说明（5 章逐字）→
       控制台（D4 页签）→ 目标附魔选择器（D3）→ AddonModules 注册（automation）；
批次 4 收口：编译 + 文案机对（旧 52 文件全部含中文/§ 字面量 → 新包检索，方法学照 57 号）+
       自造件审计 + 实机验收（87 号第七节 12 项）+ 总差异清单报告。
每批做完先编译（.\gradlew compileJava --console=plain），再往下走；每批结束向用户汇报并写报告。

【第六步：实机验证环境】单机世界 + 已搭建「岩浆块 + 相邻空地」工位阵列：
背包备讲台/书/绿宝石；失业村民在场；勾 2~3 个目标附魔跑完整循环（命中购买 + 未命中刷新各一轮）；
清障流程专门造一次（讲台位预放障碍方块）；静默容器验两条（交易/箱子屏被取消，按 E 背包放行）；
设置改值后完全重启客户端仍在（第 172 条）。

【铁律速查】只换 UI 不换功能与文案；指令名与回执一个字不改（本模块无指令，禁止新增）；
禁止新增旧项目没有的功能；一切改动先经用户同意；修复前必须取证（行号 / 存档 / 可复现实验）；
每项差异必须二分（真移植偏差 / 旧项目本来就一样），后者不得静默修掉，必须提请拍板；
同源逻辑两边逐字比对（交易发包与村民模块的关系见 D6）；代码美观 + 详细中文注释（第 208 条）；
ESP/选择器/排版/控制台/容器/持久化全部复用既有成熟件，禁止自造；
编译验证：.\gradlew compileJava --console=plain
```

---

## 变动记录

| 日期 | 变动 |
| --- | --- |
| 2026-09-18 | 首版：由 87 号（全量细读终版）+ 88 号附录A 合成；开工前第一件事 = 提交 D1~D9 拍板表；硬约束写入复用清单与第 43/169/208 条 |
