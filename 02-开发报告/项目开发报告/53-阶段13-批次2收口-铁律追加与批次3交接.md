# 53 · 阶段13 · 批次2 收口 · 铁律追加与批次3 交接

日期：2026-09-17
性质：**阶段13 批次 2 的完整收口报告 + 新窗口交接正本**。迁移细节逐文件对位见 [52-阶段13-批次2-底座与逻辑层迁移记录.md](52-阶段13-批次2-底座与逻辑层迁移记录.md)；本轮开工依据见 [48-阶段13-自动农场移植侦察与决策.md](48-阶段13-自动农场移植侦察与决策.md) 与 [48-附A-新窗口开工指令.md](48-附A-新窗口开工指令.md)。
状态：**批次 2 全部完成，编译通过；铁律已追加（第 207/208 条）；附A 已改「从批次 3 开工」。**

---

## 一、本轮全部产出（2026-09-17，按时间序）

| # | 产出 | 落点 | 状态 |
|---|---|---|---|
| 1 | D7：`FarmNav` 提升到 `platform/` 共享 | `feature/stardew/navigation/FarmNav.java` → `platform/navigation/FarmNav.java`（git mv，仅 package 声明变更，类体逐行零改动）；`DefaultStardewAdapter` import 改写 1 行 | 搬迁后单独编译通过 |
| 2 | 逻辑层全量迁移 28 文件 | `feature/autofarm/`（model 7 / scan 1 / resource 1 / task 13+1 / controller 8），逐文件对位表见 52 号第二节 | 编译通过，零 `meteordevelopment` 命中 |
| 3 | 全项目中文注释补齐扫描 | 全项目 499 个 `.java` 扫描，7 个缺中文注释文件补类级 javadoc（`SearchResultsPage` / `SkiaGlState` / `ClickGuiTheme` / `ClickGuiThemeMetrics` / `ClickGuiThemePalette` / `SettingColorPreview` / `SettingPasswordBox`），**只加注释不改代码** | 复扫 MISSING = 0，编译通过 |
| 4 | 铁律追加（用户 2026-09-17 指令） | `开发习惯.md` 新增第三十一章（第 207、208 条），内容摘要见本报告第三节 | 已落库 |
| 5 | 新窗口交接文件更新 | `48-附A`：读文件清单加入 52 号；「从批次 2 开工」→「从批次 3 开工」；施工顺序标注批次 2 已落位；铁律速查加入 207/208；变动记录 +1 行 | 已落库 |
| 6 | 批次 2 迁移记录报告 | 52 号（九节齐全 + 批次 3 规划六节 + 路线图） | 已落库 |
| 7 | 本收口报告 | 53 号 | 已落库 |

## 二、验证状态汇总（如实，第 19/46/58 条口径）

| 项 | 真实结果 |
|---|---|
| 编译 | `./gradlew compileJava --console=plain -q` 最终 **EXIT=0**（过程错误：task 层 5 类漏 import `FarmVerifier` 共 10 错，补 import 后通过，全程记录于 52 号第七节） |
| 零旧框架依赖 | `feature/autofarm/` 与 `platform/navigation/` 下 `meteordevelopment` 命中 **0** |
| 中文注释覆盖 | 499 文件扫描：补齐前缺 7，补齐后缺 **0** |
| 客户端启动验证 | **未做**（本批次产物为纯逻辑层，无运行时入口；批次 3 落地模块入口后一并执行，第 180 条） |
| 实际功能验证 | **未做**（批次 4 收口时按 48 号第七节 11 项清单 + 单机自建农田 D8 统一执行） |
| 当前状态定性 | **代码完成 + 编译通过**；不使用「功能已验证」表述 |

## 三、铁律追加内容（第三十一章，2026-09-17 用户指令）

**第 207 条 · API 查不到的兜底路径**：`01-开发参考库/` 收录 26.1.2 官网原版源码参考。第 159 条查询顺序（快速索引 → API参考 → 开发机制 → 原始源码）不变；前三步查不到时**必须继续在参考库官网源码全文 grep 确认**，两边都查不到才能判定 API 不存在或换实现。禁止因「查不到」就按旧版本经验猜 API、猜参数顺序或改用 Yarn 旧名。

**第 208 条 · 代码美观 + 详细中文注释（硬性验收项，与编译通过同级）**：
- 代码美观：命名清晰、职责单一、工整、禁止超长方法与魔法数、与周边风格一致、能复用底座不另造第二份；
- 详细中文注释：每个类有中文 javadoc（职责 + 关键设计）；每个非平凡方法说明做什么/为什么/关键判据；状态机、发包、容器同步、线程、坐标口径等易错点必须点明；注释与实现一致（第 158 条），禁止注释自夸；
- 范围：新开发、移植、修复、重构一律适用；
- 补存量注释只加注释不改逻辑（第 164 条），补完必须重新编译验证。

## 四、新窗口开工指引（复制此节 + 48-附A 即可开工）

1. **第一步**：读 `开发习惯.md`（新增第三十一章必须读）→ `00-AI阅读指引` → `01-总纲` → 六份基准 → `30-移植总览` → **48 号 + 49/50/51 三附录 + 52 号** → `47`（阶段12 做法）/ `32`（阶段09 做法）。
2. **任务**：阶段 13 **批次 3 外围**，任务拆分七项、每项目录/影响/预计文件数见 **52 号第十节**（不要重新拆分、不要重新决策 D1~D9）：
   ① 模块入口与注册（`AddonModules` 唯一引导）→ ② `.farm` 指令（回执逐字，50 号第四节全表）→ ③ 模块页与六卡片 + 控制台入口 → ④ 整屏控制台五页签（D2）→ ⑤ 逐作物独立窗口按作物折叠（D3）→ ⑥ 使用说明七章节 + 自检聊天与弹窗双通道 + 8 状态播报 + 世界渲染三项 → ⑦ 水源显示模块（D1，分类 `utility` 三处同步第 148 条）。
3. **硬约束**（附A第三步不变 + 本轮新增）：死代码 8 文件不搬但四段判据已照搬在 `CropProfile`/`FarmScanner`（勿重写第二份）；底座只复用不重造（`ContainerService`/`BlockPacketSender`/`platform.navigation.FarmNav`/`WorldOverlay`/`HelpPanelScreen`/`SelectorScreen`/`PointCardGrid`/`CommandMessageFormatter`）；设置项 16 可视 + 6 锚点 + 15 逐作物 + 水源 10 项一项不少；`MAX_CROPS = 3`；别名 `nongchang` 保留。
4. **本轮新增验收口径**：每个新文件交付时按第 208 条自检「类 javadoc + 非平凡方法注释 + 无魔法数」；API 三步查不到走参考库官网源码 grep（第 207 条）。
5. **收尾**：批次 3 完成后编译 + `runClient` 启动验证（第 180 条：进世界、确认 `yiyiaddon initialised`、无新崩溃、页面能打开）→ 写批次 3 迁移记录报告（53+ 号顺延）→ 更新路线图。

## 五、遗留问题（承接 52 号第九节，批次 3/4 处理）

1. 播报接线：`FarmController` 已随类迁入 11 处播报文案，状态条播报与自检弹窗双通道在批次 3 接线时按《基准-01》《基准-02》对齐。
2. `InventoryOps.moveToSelectedHotbar` 未实现护甲槽（36~39）映射：任务层只会传 9~35 与 40，当前无影响；出现新调用域先补映射再调用。
3. 死代码 8 文件 366 行（D4）与 `InventoryOps` 等所有新增项需在批次 4 总差异清单汇总登记。
4. 实机验收（11 项清单 + 水源模块 9×9 实测）整体压到批次 4。

## 六、项目迁移路线图

已完成：√ 阶段01~07（初始化 / 规范 / 扫描 / 基建 / 模块中心 / 运行时 / 资源包闭环）√ 阶段08 自动箱子 √ 阶段09 星露谷 √ 阶段10 自动挖矿 √ 阶段11 自动附魔 √ 阶段12 管理员检测 √ 阶段13 批次1（三份附录）√ 阶段13 批次2（底座与逻辑层，52 号，本报告收口）
当前：→ 阶段13 批次3（自动农场外围：入口 / 指令 / 模块页 / 控制台 / 逐作物窗口 / 渲染 / 播报 / 水源模块）
下一步：→ 阶段13 批次4（编译 + D1~D9 核对 + 单机实机 + 总差异清单）
未来：→ 村民交易 → 图书管理员 → 传送 → 自动登入 → 战术 → 用户统计 → 骨粉 → 断线 → Baritone/Meteor 引导 → 全量实机验收与 Mixin 对账
