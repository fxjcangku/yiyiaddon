# 87 · 阶段15 · 自动图书管理员移植侦察与决策

> 本文回答三件事：**旧项目 `librarian` 里有什么（逐条带行号）**、**落到新项目怎么接（复用什么、新建什么）**、**哪些必须用户拍板**。
> 口径正本：[01-总纲-迁移口径与复刻原则.md](../00-总纲与基准/01-总纲-迁移口径与复刻原则.md)（功能全量复刻 + 只换 UI 外壳 + 禁止新增旧项目没有的功能）。
> 上游顺序：[30-移植总览](../00-总纲与基准/30-移植总览-已移植与未移植全清单.md)第五节（… → 村民交易 → **图书管理员** → 传送 → 自动登入 → 战术 → 单文件模块）。
> 同批做法参照：[55-阶段14-村民交易移植侦察与决策.md](55-阶段14-村民交易移植侦察与决策.md)（同属「村民系」五模块）、[48-阶段13-自动农场移植侦察与决策.md](48-阶段13-自动农场移植侦察与决策.md)。
> 逐态行为与全部播报原文：[88-阶段15-附录A-逻辑层全量侦察记录.md](88-阶段15-附录A-逻辑层全量侦察记录.md)。
> **代码未动，等用户拍板 D1~D9 后开工。**

**日期**：2026-09-18　**状态**：侦察完成（**全量细读终版**：52 文件逐个全文读取，事实与行号一律本机实测）。

---

## 一、结论摘要（一屏看完）

| 项 | 结论 |
|---|---|
| 旧包 | `com.example.addon.librarian`，**52 文件 / 4,552 行**（本机逐文件实测，见 2.1；与 [30 号](../00-总纲与基准/30-移植总览-已移植与未移植全清单.md) 记的 4,083 有差异，见第九节） |
| 模块形态 | **单点自动刷附魔书**：搜索失业村民 → 寻路到村民 → 探测「岩浆块 + 讲台位」工位 → 放讲台让村民转职图书管理员 → **静默读报价**（不显示交易界面）→ 命中目标附魔则发包购买 → 未命中则拆讲台刷新交易 → 循环。**无模式、无点位库、无指令、无落盘** |
| 状态机 | `AutoLibrarianState` **27 态**（26 态有处理器，`IDLE` 无）+ `AutoLibrarianStateMachine` 156 行（合法转换表 + 异常转 `ERROR`） |
| 编排器 | `AutoLibrarianOrchestrator` **729 行**（状态→动作映射，26 个 handler）。**未超 1,000 行**，第 38 条不强制拆；拆分建议见第四节 |
| 设置项 | **12 项 / 5 组**（目标 / 行为 / 通知 / 调试 / 快捷键），逐字见 2.4；其中 4 项超时/半径由 `AutoLibrarianConfig.defaults()` 硬编码，**不可配、须照旧** |
| 指令 | **无**。全包无 `command` 子包、无指令注册、无指令文案（2.5）→ 不新增（第 100/163 条） |
| 自检 | `selfCheck()` 5 类缺项逐字，世界就绪预检单独一条（2.6） |
| 播报 | 7 条状态播报 + `info` / `debug` / `error` 三通道，全部经 `clean()` **抹掉全部中英标点**后交旧 `formatMessage`（2.7）；`error` 有 2000 ms 同文案节流 |
| 音效 | **22 个**「附魔成功音效」中文枚举（默认 `成就完成`）+ 提示音开关（默认开）；**只在 `SUCCESS` 态发音**（2.8；本项为 2026-09-18 批次2 取证订正，初版误记 21） |
| 静默交易 | `gameMode.interact` 真实开界面 → `OpenScreenEvent` 取消容器屏显示（放行背包与创造背包）→ 读 `MerchantMenu.getOffers()` → `ServerboundSelectTradePacket` + `handleContainerInput(..., 2, 0, QUICK_MOVE)` → **以库存计数增加判成交**（2.9） |
| 工位几何 | 村民朝向 → 岩浆块（村民前方 1 格）→ 讲台（岩浆块上方）→ 玩家站位（岩浆块沿同向再 1 格）；讲台阅读面 = 村民朝向反向（`VillagerStation.create` 强校验） |
| 死代码 | `adapter/` 1 + `recognition/` 2 = 3 文件 68 行零调用（**第 43 条例外清单已覆盖，不搬**）；另有 7 处零调用死件/死枝见 2.10，**待拍板 D1** |
| 底座就绪度 | 寻路（`FarmNav`）、控制台（`ConsoleWidgets`/`ConsoleHost`/`ConsoleHeaderBar`）、选择器（`SelectorScreen` 多选 + `VillagerSelectors` 8 家族分组先例）、快捷键（`AddonKeybind`/`SettingKeybind`/`ModuleKeybinds`）、协助窗（`ConfirmPanelScreen.notice`）、帮助内嵌（`HelpPanelScreen#inlineContent`）、排版（`CommandMessageFormatter`）**全部已存在**（第三节） |
| 拍板 | **D1~D9 未拍板**（第五节），拍板前不开工 |

---

## 二、旧项目事实（只读扫描，2026-09-18 实测）

### 2.0 侦察深度（如实声明）

- **52 文件全部全文细读**（含 445 行模块入口、729 行编排器、295 行讲台服务、192 行交易服务、全部 20 个 service 契约与 14 个 model）。
- 逐态行为、判据、阈值、全部播报原文 → **[88 号附录A](88-阶段15-附录A-逻辑层全量侦察记录.md)**（本报告的 2.3/2.6/2.7/2.9 为摘要，细节以附录为准）。
- 零调用验证与死代码判定用的 grep 命令与结果见第八节。
- 行号均为本机实测（`Get-Content` 行数）；引用一律以「文件:行号」给出。

### 2.1 文件与规模（逐文件行数实测）

| 子包 | 文件数 | 行数 | 说明 |
|---|---|---|---|
| 根（模块入口） | 1 | 445 | `AutoLibrarianModule` |
| `orchestrator/` | 1 | 729 | `AutoLibrarianOrchestrator` |
| `integration/` | 7 | 999 | 7 个 `Fabric*` 实现 |
| `fsm/` | 3 | 264 | 状态 / 状态机 / 转换记录 |
| `model/` | 14 | 1,229 | 上下文 / 快照 / 值对象 / 枚举 |
| `service/` | 20 | 547 | 契约 + 结果类型 + 校验器 + NoOp |
| `config/` | 3 | 271 | 设置 / 配置记录 / 音效枚举 |
| `recognition/` + `adapter/` | 3 | 68 | **零调用死代码（第 43 条覆盖）** |
| **合计** | **52** | **4,552** | |

逐文件行数（实测，按子包）：

```
AutoLibrarianModule.java                       445
orchestrator/AutoLibrarianOrchestrator.java    729
integration/FabricLecternPlacementService.java 295
integration/FabricTradeService.java            192
integration/FabricVillagerStationService.java  173
integration/FabricBaritoneMovementService.java 140
integration/FabricVillagerSearchService.java    99
integration/FabricInventoryService.java         75
integration/FabricEnchantmentService.java       25
config/AutoLibrarianSettings.java              144
config/AutoLibrarianConfig.java                 80
config/SuccessSound.java                        47
fsm/AutoLibrarianState.java                     80
fsm/AutoLibrarianStateMachine.java             156
fsm/StateTransition.java                        28
model/AutoLibrarianContext.java                224
model/VillagerTarget.java                      182
model/TradeVerificationContext.java            203
model/EnchantmentTarget.java                   115
model/TradeOfferSnapshot.java                  108
model/VillagerStation.java                      97
model/TargetProgress.java                       84
model/InventorySnapshot.java                    56
model/HorizontalDirection.java                  44
model/BlockPosition.java                        32
model/VillagerTradeStatus.java                  25
model/LecternStatus.java                        23
model/VillagerTargetStatus.java                 19
model/StationValidationStatus.java              17
service/ActionResult.java                       42
service/MovementStatus.java                     40
service/TradeService.java                       36
service/EnchantmentService.java                 35
service/MovementStartResult.java                34
service/ActionStatus.java                       32
service/MarkerBlockValidation.java              31
service/DebugSoundEvent.java                    29
service/AutoLibrarianServices.java              28
service/MarkerBlockValidator.java               27
service/LecternPlacementService.java            24
service/MovementService.java                    24
service/VillagerStationService.java             22
service/LecternService.java                     21  ← 零调用
service/VillagerSearchService.java              20
service/DebugLoggerService.java                 19
service/MarkerBlockAccess.java                  19
service/InventoryService.java                   16
service/DebugSoundService.java                   7
service/NoOpMovementService.java                41  ← 零使用
recognition/LibrarianTradeResult.java            29  ← 零调用
recognition/LibrarianTradeRecognizer.java        19  ← 零调用
adapter/LibrarianAdapter.java                    20  ← 零调用
```

### 2.2 模块入口与「静默交易」（`AutoLibrarianModule.java`，445 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 模块名 | `自动图书管理员` | `:71` |
| 描述 | `自动寻路失业村民、放置讲台刷新交易、命中目标附魔自动购买，未命中自动拆台循环。详细参考下面使用说明。` | `:72` |
| 分类 | 旧 `AddonTemplate.CATEGORY_AUTOMATION` | `:71` |
| 聊天反馈默认 | `chatFeedback = false`（模块基类字段，模块内再读设置项开关） | `:73` |
| 世界就绪预检 | 未进世界 → `必须在进入世界后才能启动模块。` + `mc.execute(this::toggle)` 自关 | `:122-127` |
| 自检入口 | `reportSelfCheck(selfCheck())`，缺项则不自启 | `:130` |
| 运行时装配 | 每次启用新建 `AutoLibrarianContext` + 7 个服务 + `AutoLibrarianOrchestrator` | `:137-173` |
| tick | 快捷键按下 → `已强制结束` + `toggle()`；否则 `orchestrator.tick()` | `:272-285` |
| 静默容器 | 启用中取消 `AbstractContainerScreen` 显示，**放行 `InventoryScreen` 与 `CreativeModeInventoryScreen`** | `:287-299` |
| 状态串 | `paused ? "已暂停" : (orchestrator == null ? "未初始化" : 状态中文名)` | `:265-270` |
| 关闭 | `orchestrator.stop()` + `已关闭` | `:253-263` |
| 使用说明 | `buildInfoWidget` 内一个 `§e查看使用说明` 按钮 → 独立说明窗（5 章） | `:77-118` |

### 2.3 状态机（27 态，`fsm/` 264 行）

- 27 个状态全部自带中文名（`AutoLibrarianState:14-79`），供 `getInfoString()` 与调试播报共用一份映射。
- `AutoLibrarianStateMachine`：合法转换表 `ALLOWED_TRANSITIONS`（`:100-131`，任意态均可转 `ERROR`，`IDLE → START` 起步）、重复进入当前态抛异常（`:60`）、处理器抛 `RuntimeException` 统一转 `ERROR` 并回调错误处理器（`:92-97`）。
- `IDLE` 不接受处理器（`:46`）；`FINISH` 处理器为空实现（编排器 `:156-157`）。
- 详尽逐态行为见 [88 号附录A](88-阶段15-附录A-逻辑层全量侦察记录.md) §1。

### 2.4 设置项清单（12 项 / 5 组，逐字）

| 组 | 设置名 | 类型 | 默认 | 域 | 描述（逐字） |
|---|---|---|---|---|---|
| 目标 | `目标附魔` | 附魔多选 | `minecraft:mending` | 注册表全部附魔 | `选择附魔类型，模块自动使用该附魔的最高交易等级。` |
| 行为 | `村民搜索半径` | 整数 | 32 | min 1 | `固定交易站村民搜索半径（格）。` |
| 行为 | `最高绿宝石价格` | 整数 | 64 | min 1 | `允许购买的单本附魔书最高绿宝石成本。` |
| 行为 | `职业等待超时` | 整数 | 200 | min 20 | `等待村民职业同步的最大 Tick 数。` |
| 行为 | `动作延迟` | 整数 | 2 | min 1 | `普通业务动作之间的 Tick 间隔。` |
| 行为 | `刷新延迟` | 整数 | 10 | min 1 | `拆除与重新放置讲台之间的最小 Tick 间隔。` |
| 行为 | `找到后移除目标` | 布尔 | false | — | `成交验证成功后，从本次运行目标集合移除已完成目标。` |
| 通知 | `提示音` | 布尔 | true | — | `找到目标和完成目标时播放提示音。` |
| 通知 | `附魔成功音效` | 枚举（22 项） | `成就完成` | 仅提示音为真时可见 | `找到目标附魔时播放的音效。` |
| 调试 | `调试模式` | 布尔 | false | — | `输出状态与移动诊断信息。` |
| 调试 | `聊天反馈` | 布尔 | true | — | `在聊天栏输出业务反馈。` |
| 快捷键 | `暂停快捷键` | 键位 | none | — | `按下后切换暂停/继续状态，保留当前村民和附魔进度。`（**与实现不一致，见 D2**） |

不可配但必须照旧的四项（`AutoLibrarianConfig.defaults():58-74`）：移动到达半径 **3**、交易界面超时 **100** tick、交易同步超时 **100** tick、移动超时 **1200** tick。

### 2.5 指令：无

- 全包无 `command/` 子包、无 `ClientCommand` 实现、无指令名与回执文案（第八节 grep 证据）。
- 结论：**不新增任何指令**（第 100 条禁止自造指令、第 163 条禁止擅自新增）。

### 2.6 自检缺项（逐字，`selfCheck():183-209`）

```
§d目标附魔§f·未勾选                  （目标附魔集合为空时）
§bBaritone§f·未安装或未启用          （BaritoneAPI 取不到主实例，异常也视为不可用）
§a讲台§f·背包没有                    （0~35 格累计数量为 0）
§a书§f·背包没有                      （同上）
§a绿宝石§f·背包没有                  （同上）
```

世界就绪单列前置：`必须在进入世界后才能启动模块。`（`:124`）。自检一次性收齐全部缺项，不遇错即返回（`:177-182` 注释即此意）。

### 2.7 播报：7 条状态播报 + 三通道，全部抹标点

状态播报（仅 7 态，其余态静默；同一条不重复播，`lastNotifiedState` 去重 `:341/366-368`）：

| 状态 | 文案（逐字） |
|---|---|
| `SEARCH_VILLAGER` | `§7正在搜索失业村民...` |
| `FIND_LECTERN_POSITION` | `§7正在检测工位...` |
| `BREAK_OBSTACLE` | `§7正在清除障碍方块...` |
| `PLACE_LECTERN` | `§7正在放置讲台...` |
| `WAIT_PROFESSION` | `§7等待村民成为图书管理员...` |
| `BREAK_LECTERN` | `§7正在拆除讲台...` |
| `RESET` | `§6⚠ 未命中目标 §8▸ 准备刷新交易` |

三通道：`info` → `§f§l` + 正文（受 `聊天反馈`）；`debug` → `§7[调试] §f`（受 `调试模式`）；`error` → `§c✗ `（**2000 ms 同文案节流** `:382-388`）。另有 `state()` 通道：调试模式输出 `§7[调试] 状态 <中文名> 移动 <中文名>`。

**全部出口经 `clean()` 抹掉中英标点**（`:397-399`，正则 `[\p{Punct}，。！？：；、（）【】「」《》“”‘’…]` 替换为空），再交旧 `formatMessage("自动图书管理员", ...)`（`:390-395`）。

### 2.8 音效（22 项中文枚举）

`SuccessSound:8-35`：钟声 / 暴击 / 猫叫 / 雷鸣 / 经验球 / 成就完成（默认）/ 玩家升级 / 音符盒叮 / 宝箱打开 / 烟花爆炸 / 村民欢庆 / 村民治愈 / 山羊嚎叫 / 恶魂嚎叫 / 悦灵鸣叫 / 附魔台使用 / 三叉戟雷鸣 / 熊猫打喷嚏 / 监守者咆哮 / 末影龙龙吼 / 末地传送门 / 远古守卫者诅咒。

播放条件（`ModuleSound:411-444`）：`提示音` 开关为真 **且** 事件为 `SUCCESS`，才按枚举取 `SoundEvents` 播放（音量 1.0 / 音高 1.0）。`SUCCESS` 事件只由 `COMPLETE_TARGET` / `FINISH` 两个状态映射而来（编排器 `:724`）。

### 2.9 静默交易与购买协议（判据逐条）

| 环节 | 判据 | 证据 |
|---|---|---|
| 打开 | `gameMode.interact(...)` 打实体，先 `lookAt` 同步视角（`ServerboundMovePlayerPacket.Rot`）；每 2 tick 重试 | `FabricTradeService:43-68`、`174-191` |
| 就绪 | `mc.player.containerMenu instanceof MerchantMenu` | `:70-73`、`:168-172` |
| 读报价 | 只读 `MerchantMenu.getOffers()`；**客户端禁止读 `Villager.getOffers()`**（会抛异常闪退，类注释 `:34-37`） | `:81-92` |
| 报价字段 | 只取每笔的**第一个**附魔词条（循环内 `return`）；输出必须是 `minecraft:enchanted_book` | `:129-158` |
| 命中 | `tradable && !invalid && 输出为附魔书 && 附魔 ID 相等 && 等级 == 目标等级 && 该附魔最高等级 == 目标等级 && 绿宝石 ≤ 上限` | `FabricEnchantmentService:16-24` |
| 选中 | 先 `sameOffer`（`containerId` 字符串 + 重新快照全等）再发 `ServerboundSelectTradePacket(index)` | `:95-103`、`:160-166` |
| 同步 | `selectedIndex == index && sameOffer && 结果槽 2 非空` | `:106-109` |
| 取出 | 结果槽非空 → `handleContainerInput(containerId, 2, 0, QUICK_MOVE)` | `:112-119` |
| 成交 | **购买前后库存计数增加**（非报价 uses） | `:593-604`、`FabricInventoryService:40-62` |
| 支付能力 | 绿宝石 ≥ 绿宝石成本 **且**（第二成本为空 或 第二成本非 `minecraft:book` 或 书 ≥ 第二成本数） | `FabricInventoryService:23-29` |
| 关界面 | 仅当本模块自己开的界面才 `closeContainer()` | `:121-127` |

### 2.10 死代码与死枝（零调用实测，第八节命令）

| 对象 | 事实 | 行数/位置 | 判定 |
|---|---|---|---|
| `adapter/LibrarianAdapter` + `recognition/LibrarianTradeRecognizer` + `LibrarianTradeResult` | 仅三者互引，全项目零调用 | 68 行 | **第 43 条明确列为「严禁迁移的全部 recognition/adapter 死代码层」，不搬** |
| `service/LecternService` | 无实现类、零调用 | 21 行 | 待拍板 D1（建议不搬并登记） |
| `service/NoOpMovementService` | 零使用（模块注入的是 Baritone 实现） | 41 行 | 同上 |
| `service/VillagerStationService#calculateMarkerBlockPosition` | 默认方法，零调用 | `:19-21` | 同上 |
| `EnchantmentService#getMaximumTradeLevel` / `isLibrarianTradeable` / `findMatch` | 三个默认方法零调用（编排器自己 stream filter） | `:14-34` | 同上 |
| `AutoLibrarianModule#setServices` / `getServices` | 零外部调用（编排器构造已直接注入同批服务） | `:301-311` | 同上 |
| `AutoLibrarianOrchestrator#softReset` / `restart` | 零调用 | `:112-121` | 同上 |
| `AutoLibrarianModule.paused` | **恒为 false**（无处置 true → `:68/259/267/283`，「已暂停」分支不可达） | — | **待拍板 D2**（与快捷键描述冲突） |
| `VillagerTarget` 富状态 API（`withPosition` / `withResolvedEntity` / `asUnresolved` / `withStatus` / `withLectern` / `withStation` / `withTradeStatus`）+ `VillagerTargetStatus` / `LecternStatus` / `VillagerTradeStatus` | 服务层只用 3 参构造 `(uuid, entityId, position)`，其余字段恒为默认值 | `VillagerTarget:96-181` + 3 枚举 67 行 | 待拍板 D1（建议整份照搬，最小改动） |

### 2.11 使用说明（5 章，逐字，`buildHelpContent():89-118`）

```
准备     背包携带：讲台 × N、书 × N、绿宝石 × 足够数量 / 在「目标附魔」中设置想要的附魔类型 /
         站在已搭建好的岩浆块工位阵列附近 / 开启模块即自动运行
自动流程 [1] 在搜索半径内寻找失业村民 / [2] 识别岩浆块工位，清除障碍方块 /
         [3] 放置讲台，等待村民接受图书管理员职业 / [4] 静默读取交易列表（不打开界面），命中目标附魔则自动发包购买 /
         [5] 未命中则拆除讲台，刷新村民交易，重复循环
静默交易 交易全程不打开交易界面，发包静默完成 / 命中目标附魔后自动选中并领取附魔书
场地要求 每个村民工位：岩浆块 + 相邻空地（用于放置讲台）/ 支持活版门卡位场地（村民无法逃跑）/
         安装 Baritone 可自动寻路移动到村民位置
注意     背包缺少讲台/书/绿宝石时模块会报错并自动关闭 / 绿宝石价格超过「最大价格」上限的交易会跳过
```

原文含 `§` 颜色码与树形符号（`§8├─` / `§8└─` / `§a[1]` / `§7▸` / `§6▸` / `§c⚠`），**逐字**见 [88 号附录A](88-阶段15-附录A-逻辑层全量侦察记录.md) §5。

---

## 三、底座就绪度（新项目已具备，全部只复用不重造）

| 需求 | 新项目既有件 | 本模块怎么用 |
|---|---|---|
| 寻路 | `platform/navigation/FarmNav`（Baritone 隔离层）+ 模块内导航服务先例 `feature/villager/navigation/VillagerNavigationService` | 移动适配层落在模块内，原语走 `FarmNav`；`allowBreak=false` 与 30 tick 宽限期语义照搬（差异登记） |
| 控制台 | `ui/console/ConsoleWidgets`（`ConsoleRow`/`Ctl`/`Note`/`FoldSection`/`ButtonStrip`）、`ConsoleHost`、`ConsoleMetrics`、`ConsoleHeaderBar`（第 216 条：状态 + 快捷键徽章 + 开关压进控制台顶栏） | 新建本模块控制台，骨架与顶栏全部复用 |
| 模块页 | `ui/page/CompactModulePage`（第 209 条：信息块 → 打开控制台 → 使用说明内嵌；无顶部状态行） | 模块页只留「打开控制台 + 内嵌说明」 |
| 使用说明内嵌 | `ui/screen/HelpPanelScreen#inlineContent`（第 210 条：去窗框三行，章节与正文逐字） | 5 章正文原样内嵌 |
| 附魔多选 | `ui/screen/SelectorScreen` 多选构造 + `feature/villager/ui/VillagerSelectors`（分组 + 折叠 + 每行附魔书图标 + 行尾实时计数 + 写回即落盘先例） | 目标附魔选择器按同款接线（候选集与分组见 D3） |
| 设置控件 | `SettingNumberBox`（第 123 条禁滑块）、`SettingToggle`、`SettingCycle`、`SettingKeybind` + `AddonKeybind` + `KeyInputs` | 12 项设置逐个对位，快捷键行末置 + 「§c清空」按钮（第 214 条） |
| 模块快捷键 | `core/module/ModuleKeybinds` + `ModuleKeybindManager` | 暂停键落模块快捷键体系（D2 定稿后） |
| 自检弹窗 | `ConfirmPanelScreen.notice(title, headline, items, parent, settingsTarget)`（第 176-179 条：聊天 + 屏幕中央弹窗，条目红色加粗，带「打开设置」） | 5 类缺项走运行时统一入口，模块不自造弹窗 |
| 播报排版 | `core/CommandMessageFormatter` + 第 110-113 条前缀规则（`[模块名]` 白色加粗、整条加粗、不出现 `yiyiaddon`） | 播报出口统一（D5 定稿后） |
| 声音 | `mc.player.playSound(SoundEvent, 1, 1)`（原版 API） | 22 项枚举照搬，`SoundEvents` 映射逐条照抄 |
| 事件 | `core/event/ClientEventType.SCREEN_OPEN` + `subscribedEvents()` | 静默容器由 `SCREEN_OPEN` 取消实现（对齐旧 `OpenScreenEvent`） |
| 容器/发包 | `service/container/ContainerService`、`platform/container/ContainerAccess`、`platform/network/BlockPacketSender`、`feature/villager/trade/TradeEngine`（已含 SelectTrade + QUICK_MOVE 两步） | 本模块交易协议需 `ServerboundSelectTradePacket` + `handleContainerInput`，复用/抽公共见 D6 |
| 落盘 | 本模块**无业务落盘**；设置走模块设置载体（`ModuleManager.saveSettings`，第 172/173 条） | 只做 `loadSettings`/`saveSettings` 两端键名对称 |

---

## 四、缺口与风险

| # | 项 | 说明 | 处置 |
|---|---|---|---|
| R1 | 交易协议与新项目村民模块**不同源** | 村民是「真实开界面 + `setSelectionHint`/`tryMoveItems`」；图书管理员是「静默 + `ServerboundSelectTradePacket` + 结果槽 QUICK_MOVE + 库存计数验证」。两者都有 SelectTrade 与 QUICK_MOVE 调用 → 存在同源逻辑两份的风险（第 169 条） | 待拍板 D6：抽公共交易访问层，或模块自持并登记 |
| R2 | Baritone 硬依赖 | 旧项目 `NoOpMovementService` 从未使用 → **无降级路径**，缺 Baritone 直接被自检拦下 | 照搬（自检拦 + 不启动）；不新增降级（第 163 条） |
| R3 | 工位几何强校验 | `VillagerStation` 构造器对「岩浆块 / 讲台 / 站位 / 阅读面」四项几何关系强校验，任一处错即抛异常 | 照搬；坐标口径（村民脚下 y、`dy ∈ {0,-1}` 双扫）逐条保留 |
| R4 | 探测两遍策略 | 第一遍只接受「讲台位已空闲」的岩浆块；第二遍 fallback 取**离玩家最近**的可清障岩浆块 | 照搬（顺序与距离判据都影响行为） |
| R5 | 报价只取第一个附魔词条 | `snapshot()` 在词条循环内 `return` → 多附魔书只报第一项 | 照搬（1:1，不"修正"） |
| R6 | 编排器 729 行 26 handler | 未超第 38 条阈值，但单文件职责偏重（搜索/工位/交易三阶段） | 待拍板 D7：单文件照搬（建议）或按三阶段拆 |
| R7 | `clean()` 抹标点 | 用户可见文本被改写（去标点）属旧行为，但新项目排版器另有格式 | 待拍板 D5：是否逐字保留抹标点行为 |
| R8 | 快捷键描述与实现冲突 | 描述「切换暂停/继续」；实现「强制结束并关模块」 | 待拍板 D2 |
| R9 | 30 号报告行数差异 | 30 号记 4,083 行，本机实测 4,552 行 | 按第 76 条在第九节登记，不回改历史报告 |

---

## 五、待拍板（D1~D9，拍板前不开工）

| 编号 | 事项 | 旧项目事实（证据） | 建议 |
|---|---|---|---|
| **D1** | 零调用死件是否不搬 | `LecternService`(21) / `NoOpMovementService`(41) / `calculateMarkerBlockPosition` / `EnchantmentService` 三个默认方法 / `setServices`+`getServices` / `softReset`+`restart` / `VillagerTarget` 富状态 API + 3 枚举（2.10） | 参照村民 D1「死代码不搬」口径：**不搬并列清单**；`VillagerTarget` 与 3 枚举**整份照搬**（最小改动） |
| **D2** | 暂停键语义二选一（旧项目自身不一致，第 168 条强制二分） | 实现「按下 → `§c§l已强制结束` → 关模块」`:276-282`；描述「切换暂停/继续，保留进度」`:138-142`；`paused` 恒 false | **照实现（A 方案）**：强制结束 + 文案逐字，键位落 `ModuleKeybinds`（`module.librarian.stop`）；描述文本照旧留在 tooltip |
| **D3** | 目标附魔候选集与分组 | 旧 `EnchantmentListSetting` 候选 = **注册表全部附魔**（`getIdentifierSuggestions` = `keySet()`），默认 `minecraft:mending` | **照旧取全部附魔**；按第 195/197 条分类：复用 `VillagerSelectors` 的 8 家族表 + 兜底「其他」（不得改用村民 35 项白名单，那会缩小旧取值域） |
| **D4** | 控制台页签组合 | 旧项目无控制台；旧设置分组为 5 组（目标/行为/通知/调试/快捷键）；本模块**无点位库、无 ESP** | 建议 **概览 / 设置 / 日志** 三页签；设置页内用 4 个折叠组（目标/行为/通知/调试），快捷键行置页末（第 214 条）。**不设点位页**（旧项目无点位） |
| **D5** | 播报口径 | 7 条状态播报 + 三通道，全部 `clean()` 抹标点后交旧 `formatMessage`（2.7） | ① 抹标点行为**逐字保留**（属旧行为，第 105 条）；② 出口走 `CommandMessageFormatter`，前缀按第 110-113 条改 `[自动图书管理员]`（唯一允许变的是前缀段） |
| **D6** | 交易发包落点 | 村民模块已有 SelectTrade + QUICK_MOVE；本模块另有 `MerchantMenu` 读取、`sameOffer` 比对、库存计数验证 | 建议**抽公共**：交易界面访问（开/读/选/取/关）收敛到 `platform/container/MerchantTradeAccess` 一处，村民与图书管理员共用，避免第 169 条两份判据 |
| **D7** | 编排器是否拆 | `AutoLibrarianOrchestrator` 729 行 / 26 handler | 建议**单文件照搬**（未超第 38 条阈值）；拆分留作后续可选 |
| **D8** | 音效播放条件 | 只在 `SUCCESS`（`COMPLETE_TARGET`/`FINISH`）发音，受「提示音」开关控制（2.8） | 照搬（含「找到目标附魔时」实际不发音的现状） |
| **D9** | 分类与注册 | 旧 `CATEGORY_AUTOMATION` | 落本项目 `automation`（权重 30，与农场/挖矿/箱子/村民一致），`AddonModules.createModules()` 追加一行 |

---

## 六、施工方案（分类对照表 + 批次）

### 6.1 分类对照表（第 52 条：迁移前列旧类 → 新类 → 目录 → 是否拆分）

| # | 旧路径（`com.example.addon.librarian`） | 新路径（`com.yiyiaddon.feature.librarian`） | 目录归属 | 类型 |
|---|---|---|---|---|
| 1 | `AutoLibrarianModule` | `AutoLibrarianModule` | 模块根 | 重写外壳（入口/自检/播报/静默容器） |
| 2 | `orchestrator/AutoLibrarianOrchestrator` | `service/LibrarianOrchestrator` | `service/` | 直接迁移（D7） |
| 3 | `fsm/AutoLibrarianState` | `fsm/LibrarianState` | `fsm/` | 直接迁移（含中文名） |
| 4 | `fsm/AutoLibrarianStateMachine` | `fsm/LibrarianStateMachine` | `fsm/` | 直接迁移 |
| 5 | `fsm/StateTransition` | `fsm/StateTransition` | `fsm/` | 直接迁移 |
| 6 | `config/AutoLibrarianSettings` | `config/LibrarianSettings` | `config/` | 设置载体（键名逐字） |
| 7 | `config/AutoLibrarianConfig` | `config/LibrarianConfig` | `config/` | 直接迁移 |
| 8 | `config/SuccessSound` | `config/SuccessSound` | `config/` | 直接迁移 |
| 9 | `model/AutoLibrarianContext` | `model/LibrarianContext` | `model/` | 直接迁移 |
| 10 | `model/VillagerTarget` | `model/VillagerTarget` | `model/` | 直接迁移（D1） |
| 11 | `model/VillagerStation` | `model/VillagerStation` | `model/` | 直接迁移 |
| 12 | `model/TargetProgress` | `model/TargetProgress` | `model/` | 直接迁移 |
| 13 | `model/EnchantmentTarget` | `model/EnchantmentTarget` | `model/` | 直接迁移 |
| 14 | `model/TradeOfferSnapshot` | `model/TradeOfferSnapshot` | `model/` | 直接迁移 |
| 15 | `model/TradeVerificationContext` | `model/TradeVerificationContext` | `model/` | 直接迁移 |
| 16 | `model/InventorySnapshot` | `model/InventorySnapshot` | `model/` | 直接迁移 |
| 17 | `model/BlockPosition` | `model/BlockPosition` | `model/` | 直接迁移 |
| 18 | `model/HorizontalDirection` | `model/HorizontalDirection` | `model/` | 直接迁移 |
| 19-21 | `model/LecternStatus` / `VillagerTargetStatus` / `VillagerTradeStatus` / `StationValidationStatus` | 同名 | `model/` | 直接迁移（D1） |
| 22 | `service/VillagerSearchService`（契约） | `service/VillagerSearchService` | `service/` | 契约直接迁移 |
| 23 | `service/VillagerStationService` | `service/VillagerStationService` | `service/` | 契约直接迁移（去死默认方法） |
| 24 | `service/LecternPlacementService` | `service/LecternPlacementService` | `service/` | 契约直接迁移 |
| 25 | `service/TradeService` | `service/TradeService` | `service/` | 契约直接迁移 |
| 26 | `service/InventoryService` | `service/InventoryService` | `service/` | 契约直接迁移 |
| 27 | `service/EnchantmentService` | `service/EnchantmentService` | `service/` | 契约直接迁移（去死默认方法） |
| 28 | `service/MovementService` | `service/MovementService` | `service/` | 契约直接迁移 |
| 29 | `service/MarkerBlockAccess` | `service/MarkerBlockAccess` | `service/` | 契约直接迁移 |
| 30 | `service/MarkerBlockValidator` + `MarkerBlockValidation` | 同名 | `service/` | 直接迁移 |
| 31 | `service/ActionResult` + `ActionStatus` | 同名 | `service/` | 直接迁移 |
| 32-34 | `service/MovementStartResult` / `MovementStatus` / `DebugSoundEvent` | 同名 | `service/`（或 `model/`，见下） | 直接迁移（含中文名） |
| 35 | `service/AutoLibrarianServices` | `service/LibrarianServices` | `service/` | 直接迁移 |
| 36-38 | `service/DebugLoggerService` / `DebugSoundService` | 同名 | `service/` | 契约直接迁移 |
| 39 | `integration/FabricVillagerSearchService` | `platform/VillagerSearchProbe` | `platform/` | Minecraft 适配（去 `Fabric` 前缀命名） |
| 40 | `integration/FabricVillagerStationService` | `platform/StationProbe`（实现 `MarkerBlockAccess`） | `platform/` | 同上 |
| 41 | `integration/FabricLecternPlacementService` | `platform/LecternOps` | `platform/` | 同上（`InvUtils` → 本项目换手工具位置逻辑，见 §6.3） |
| 42 | `integration/FabricTradeService` | `platform/MerchantTradeOps` | `platform/` | 同上（D6 若抽公共则改为调用公共层） |
| 43 | `integration/FabricInventoryService` | `platform/InventoryAccess` | `platform/` | 同上 |
| 44 | `integration/FabricEnchantmentService` | `service/EnchantmentMatcher` | `service/` | 纯逻辑匹配（无 MC 调用），归 service |
| 45 | `integration/FabricBaritoneMovementService` | `integration/BaritoneMoveService` | `integration/` | 第三方兼容（Baritone） |
| 46 | `ui/`（旧项目无模块界面，只有使用说明按钮） | `ui/LibrarianPage` + `ui/LibrarianHelpContent` + `ui/LibrarianEnchantSelector` + `ui/console/…` | `ui/`、`ui/console/` | 本项目壳件（第 181/210 条） |
| 47 | `adapter/LibrarianAdapter`、`recognition/LibrarianTradeRecognizer`、`recognition/LibrarianTradeResult` | — | — | **不搬**（第 43 条） |
| 48 | `service/LecternService`、`service/NoOpMovementService` | — | — | **待拍板 D1**（建议不搬并登记） |

### 6.2 批次划分（照村民/农场先例）

- **批次 1（本报告）**：侦察落库（87 主报告 + 88 附录A + 87-附A 开工指令）。**已完成**。
- **批次 2 · 逻辑层**：`model/` 14 → `fsm/` 3 → `service/` 契约与结果类型 → `platform/` 与 `integration/` 5+1 实现 → `service/LibrarianOrchestrator`；每步与旧源码逐段比对。
- **批次 3 · 外围**：模块入口（自检/启动报告/静默容器/音效/快捷键）→ 模块页 + 内嵌说明（5 章逐字）→ 控制台（D4 页签）→ 目标附魔选择器（D3）→ `AddonModules` 注册（`automation`）。
- **批次 4 · 收口**：编译 + 文案机对（旧 52 文件全部含中文/`§` 字面量 → 新包检索）+ 自造件审计 + 实机验收 + 总差异清单。

### 6.3 硬约束（写入附A）

1. **文案逐字**：模块名/描述、启动报告 4 行、5 类自检缺项、5 章使用说明、7 条状态播报、错误节流阈值、22 个音效中文名、12 项设置名与描述，全部逐字（第 96-105 条）。
2. **壳件只复用不重造**：控制台/选择器/设置控件/帮助内嵌/自检弹窗/排版器/快捷键，一律调既有件（第 200 条同款教训：禁止第二套）。
3. **不复刻框架层**：旧框架的 `Settings`/`EnchantmentListSetting`/`KeybindSetting`/`WWidget`/`InvUtils`/`EventHandler` 一律换本项目自研件；`InvUtils.findInHotbar` / `move().from().toHotbar()` / `swap()` 落本项目既有容器访问层（`ContainerAccess`/`ContainerService`，第 34 条）。
4. **禁止新增**：无指令、无点位库、无落盘业务文件、无 ESP、无模式切换——旧项目没有的一律不做（第 163/171 条）。
5. **代码美观 + 详细中文注释**（第 208 条，硬性验收项）。

---

## 七、验收清单（待实机）

| # | 项 | 验收方式 |
|---|---|---|
| 1 | 编译 | `./gradlew build --console=plain -q` → EXIT=0 |
| 2 | 文案机对 | 旧 52 文件含中文/`§` 字面量逐条在新包检索，缺口逐条给结论（方法学照 57 号） |
| 3 | 自造件审计 | 逐条给「旧项目对应物 / 既有先例」（照 54 号「状态变更」节） |
| 4 | 自检双通道 | 造缺项（清空目标附魔 / 无 Baritone / 无讲台 / 无书 / 无绿宝石）→ 聊天 + 弹窗 + 「打开设置」直达 |
| 5 | 单村民单轮 | 失业村民 + 岩浆块工位 → 放讲台 → 转职 → 命中附魔 → 买到手（库存 +1 验证） |
| 6 | 未命中刷新循环 | 未命中 → 拆讲台 → 村民失业 → 重新检测工位 → 再放（≥3 轮不漏） |
| 7 | 清障流程 | 讲台位预放障碍方块 → `已清除障碍方块，恢复运行` → 继续放置 |
| 8 | 多目标 | 勾 2~3 个附魔 → 逐个完成，剩余目标播报与「找到后移除目标」两种口径各验一次 |
| 9 | 静默容器 | 运行中打开交易/箱子界面被取消；玩家按 E 开背包与创造背包**放行** |
| 10 | 设置持久化 | 改值 → 完全关客户端 → 重开仍在（第 172 条） |
| 11 | 状态串 | 模块列表状态显示中文状态名，不出现英文枚举名 |
| 12 | 使用说明时效 | 说明里指向的入口/页签名/设置名与实现一致（第 212 条） |

---

## 八、证据与命令

```powershell
# 文件数与总行数（2.1 表即此命令输出，52 / 4552）
$f = Get-ChildItem -Path 'd:\mcaddon\26.1.2\src\main\java\com\example\addon\librarian' -Recurse -Filter *.java
($f | Measure-Object).Count
($f | ForEach-Object { (Get-Content -LiteralPath $_.FullName).Count } | Measure-Object -Sum).Sum

# 旧框架依赖点（仅 3 处：模块事件/GUI、设置定义、InvUtils）
#   范围：…\librarian  关键词：meteordevelopment
#   结果：AutoLibrarianModule:28-34、AutoLibrarianSettings:4-12、FabricLecternPlacementService:9-10

# 跨业务包依赖（farm / itemid / resourcepack / villager / core.world / Mixin）
#   范围：…\librarian  关键词：com\.example\.addon\.(farm|itemid|resourcepack|villager|core\.world)|Mixin
#   结果：零命中 → librarian 是五模块中耦合最低的一支

# 死代码零调用（2.10 依据）
#   范围：d:\mcaddon\26.1.2\src\main\java
#   关键词：LibrarianAdapter|LibrarianTradeRecognizer|LibrarianTradeResult|LecternService|NoOpMovementService|
#          softReset|getServices\(\)|setServices\(|calculateMarkerBlockPosition
#   结果：仅声明处自身命中，无外部调用点

# paused 恒 false 与 restart 零调用
#   范围：…\librarian  关键词：restart\(|paused
#   结果：paused 仅 :68/:259/:267/:283；restart 仅 :119 声明

# 指令：无
#   范围：…\librarian  关键词：ClientCommand|CommandRegistry|\.register\(|command
#   结果：无 command 子包、无指令注册
```

---

## 九、状态变更（第 76 条：正文数字不逐次回改，差异在此登记）

| 项 | [30 号](../00-总纲与基准/30-移植总览-已移植与未移植全清单.md) 原记 | 本机实测（2026-09-18） | 说明 |
|---|---|---|---|
| `librarian` 行数 | 4,083 | **4,552** | 30 号的数字统计时点为 2026-09-14 且未附逐文件明细；本次以 `Get-Content` 逐文件行数汇总为准（52 文件数两处一致） |
| 文件数 | 52 | 52 | 一致 |
| 30 号「下一步建议顺序」 | 村民交易之后为图书管理员 | 一致 | 村民交易已于阶段14 批次1~3 落位 |
| 未落项提醒 | — | 阶段14 批次4 收口（村民交易实机三模式验收）**仍未做** | 与本批并行，不阻塞本批侦察 |

[00-AI阅读指引.md](../00-总纲与基准/00-AI阅读指引.md) 第 3 层表格目前只编到 38 号（39~86 号未登记），属既存欠账；本批不擅自改动该文件（第 164 条），一并登记待用户裁定。

---

## 项目迁移路线图

已完成：√ 阶段01~12 √ 阶段13 批次1~3（含自查修复）√ 阶段14 批次1~3（村民交易逻辑层 + 外围）
当前：→ **阶段15 批次1（自动图书管理员侦察，本报告 87 + 附录A 88 + 附A 开工指令）**；阶段14 批次4 收口（村民交易实机验收）并行待办
下一步：→ 阶段15 批次2（逻辑层）→ 批次3（外围）→ 批次4（收口）
未来：→ 传送 → 自动登入 → 战术 → 单文件模块（自动骨粉 / 水源显示已完成 → 管理员检测已完成 → 用户统计 / 自动断线 / Baritone 引导）→ 全量实机验收与 Mixin 对账
