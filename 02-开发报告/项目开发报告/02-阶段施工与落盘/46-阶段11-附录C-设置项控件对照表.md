# 46 · 阶段11 · 附C · 自动附魔设置项控件对照表（批次 2 / 批次 5 施工前置）

日期：2026-09-16
类别：对照表（施工时逐项对默认值用，不写 Java）
依据（全部实测，旧项目严格只读）：`d:\mcaddon\26.1.2\src\main\java\com\example\addon\enchant\AutoEnchantBook.java`（3,230 行）、`ui\CustomEnchantSetting.java`、`ui\EnchantmentSelectSetting.java`、`gear\GearEnchantSetting.java`、`gear\AnvilPlanner.java`、`01-开发参考库\Meteor原始源码\meteordevelopment\meteorclient\settings\`（`Setting.java` / `SettingGroup.java` / `Settings.java` / `StringListSetting.java`）。
底本：`38-阶段11-自动附魔移植侦察与决策.md`（含 §九）+ `38-附A-新窗口开工指令.md` + `阶段11-开工待确认项.md`；**凡底本与源码冲突，一律以本表源码实测为准**。

## 0. 登记口径

**共登记 221 项设置项，分属 18 个 `SettingGroup`。** 明细：

| 分组（字段） | 项数 | 分组（字段） | 项数 |
| --- | --- | --- | --- |
| 基础设置（`sgBasic`） | 15 | 斧头附魔属性（`sgAxe`） | 5 |
| 自定义附魔（`sgCustom`） | 1 | 弓附魔属性（`sgBow`） | 34 |
| 自动附魔分类（`sgAdvanced`） | 5（选择器） | 护甲附魔属性（`sgArmor`） | 50 |
| 原版附魔分类（`sgVanilla`） | 8（选择器） | 工具与通用附魔属性（`sgOther`） | 13 |
| 原版装备附魔（`sgGear`） | 3 | 原版防具 / 近战 / 工具 / 弓 / 钓竿 / 三叉戟 / 弩 / 通用附魔（`sgVanilla*` 8 组） | 9 / 7 / 3 / 4 / 2 / 4 / 3 / 1 = 33 |
| 剑附魔属性（`sgSword`） | 54 | **合计** | **221** |

### 0.1 列的读法

| 列 | 口径 |
| --- | --- |
| 设置项名称（逐字） | 旧项目 `.name("…")` 原文一字不改（实测 221 项**均不含** `§` 颜色码 / 全角空格 / 全角括号，见 §0.4） |
| 旧控件类型 | 旧项目真实类名（`BoolSetting` / `IntSetting` / `EnumSetting<…>` / `StringListSetting` 子类），`noSlider()` 等修饰照写 |
| 默认值 | 逐字照抄 `.defaultValue(…)`；布尔写 `false` / `true`，枚举写 `常量（中文显示名）` |
| 取值范围 / 取值域 | 整数项写 `.min()`~`.max()`（如 `1~100`）；枚举项列全常量；取不到写「源码未给（行号）」 |
| 可见性条件 | 逐字照抄 `.visible(…)` 表达式 + 行号；无 `visible()` 写「无 `visible()`」 |
| 所属分组 | 旧分组中文名（字段名） |
| 源码行号 | `AutoEnchantBook.java` 行号（未另注文件名即指本文件） |
| 新项目落点 | 控制台页（拟名，按 38-附A §3 页签划分）+ 拟用控件；**页类名/控件排布尚未开工，属拟用** |

### 0.2 本项目控件记号（本表用词对照真实类）

本表「拟用控件」列写的是本项目 `ui/widget/` 的真实类，**没有** `Ctl.Number` 这类封装；每行控件都要照挖矿先例包一层 `new Ctl(widget[, hint])`（`ui/console/ConsoleWidgets.java:41`）后再进 `ConsoleRow`。

| 本表写法 | 真实类（落点） | 用途 |
| --- | --- | --- |
| `Ctl(SettingToggle)` | `ui/widget/SettingToggle.java` | 布尔开关 |
| `Ctl(SettingNumberBox)` | `ui/widget/SettingNumberBox.java:59` | 整数/小数输入（本表全部整数、步进 1、`%.0f`、**无滑块**） |
| `Ctl(SettingSegmented)` | `ui/widget/SettingSegmented.java:62` | 2~N 段枚举选择（选择型） |
| `Ctl(SettingCycle)` | `ui/widget/SettingCycle.java:25` | 少选项逐次循环（段数过多时替代分段） |
| `Ctl(SettingTextBox)` | `ui/widget/SettingTextBox.java:45` | 单行文本 |
| `Ctl(SettingText)` | `ui/widget/SettingText.java:30` | 只读文字（摘要 / 计数） |
| `Ctl(Button)` / `Ctl(IconButton)` | `ui/widget/Button.java` / `IconButton.java` | 按钮 / 图标按钮（点击选择、重置） |
| `Note(...)` | `ui/console/ConsoleWidgets.java:233` | 只读说明行 |
| `FoldSection(...)` | `ui/console/ConsoleWidgets.java:377` | 页内折叠子组（13 个子组的容器） |
| `SelectorScreen` / `BaritoneTextListScreen` | `ui/screen/SelectorScreen.java:89` / `integration/baritone/BaritoneTextListScreen.java:48` | 现成的双栏选择器 / 自由文本名单，**非标准控件承载的候选**（见附表②） |

### 0.3 旧项目落盘口径（复核用）

旧项目设置序列化：`groups[] → {name: 分组名, sectionExpanded: …, settings[] → {name: 设置名, …}}`，**键就是设置名**（`Settings.java:157-167` → `SettingGroup.java:60-73` / `:76-88` → `Setting.java:121`）；列表值存在 `value` 里（`StringListSetting.java:47-52` / `:58-64`）。
**注意**：`Settings.toTag()` 只遍历**当前 `settings.groups`**，本模块的 13 个子组已被 `settings.groups.remove(...)` 摘除（`:571-583`），故子组里那 **189 个 `BoolSetting` 自身不落盘**，其勾选状态只以「选择器名字 → 被勾选项名字列表」的形式落盘。

### 0.4 逐字保留核查（只读命令见 §5）

- 94-350 行（设置声明区）内 `§` 命中 **0**、全角空格（U+3000）命中 **0**、全角括号（U+FF08）命中 **0**。
- 全文件 U+3000 共 **17** 处，全在状态播报字符串（969-2085 行），**不在任何设置名里**。
- 唯一带括号的设置名是 `GUI操作延迟(Tick)`（半角括号），逐字保留。
- 189 个子组布尔名里的空格为**半角空格**（如 `定身 5`），逐字保留。

---

## 1. 逐项对照表（221 项）

### 1.1 基础设置（`sgBasic`，15 项）

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 目标模式 | `EnumSetting<TargetMode>`（下拉） | `TargetMode.BOOK`（`原版附魔书`） | `GEAR`（原版装备附魔）/ `BOOK`（原版附魔书）/ `CUSTOM`（自定义附魔） | 无 `visible()`，恒可见；`.onChanged` 调 `同步模式分组()` + `刷新模式界面()` | 基础设置（`sgBasic`） | 114-117 | 「基础设置」页首行 `Ctl(SettingSegmented)` 3 段；切换后整页重建页签并保留幂等保护（`:646-648`） |
| 单轮抽取次数 | `IntSetting`（`.noSlider()`） | `10` | `1~100` | `() -> 目标模式.get() != TargetMode.GEAR`（:121） | 基础设置（`sgBasic`） | 119-121 | 「基础设置」页 `Ctl(SettingNumberBox)`（1~100，步进 1，无滑块） |
| GUI操作延迟(Tick) | `IntSetting`（`.noSlider()`） | `1` | `1~10` | 无 `visible()`，恒可见 | 基础设置（`sgBasic`） | 123-124 | 「基础设置」页 `Ctl(SettingNumberBox)`（1~10） |
| 发包打开距离 | `IntSetting`（`.noSlider()`） | `6` | `1~32` | 无 `visible()`，恒可见 | 基础设置（`sgBasic`） | 126-128 | 「基础设置」页 `Ctl(SettingNumberBox)`（1~32） |
| 书本补给组数 | `IntSetting`（`.noSlider()`） | `1` | `1~10` | `() -> 目标模式.get() != TargetMode.GEAR`（:132） | 基础设置（`sgBasic`） | 130-132 | 「基础设置」页 `Ctl(SettingNumberBox)`（1~10） |
| 青金石补给组数 | `IntSetting`（`.noSlider()`） | `1` | `1~10` | 无 `visible()`，恒可见 | 基础设置（`sgBasic`） | 134-135 | 「基础设置」页 `Ctl(SettingNumberBox)`（1~10） |
| 每批取用数量 | `IntSetting`（`.noSlider()`） | `4` | `1~16` | `() -> 目标模式.get() == TargetMode.GEAR`（:139） | 基础设置（`sgBasic`） | 137-139 | 「基础设置」页 `Ctl(SettingNumberBox)`（1~16） |
| 极品附魔数量 | `IntSetting`（`.noSlider()`） | `1` | `1~64` | `() -> 目标模式.get() == TargetMode.GEAR`（:143） | 基础设置（`sgBasic`） | 141-143 | 「基础设置」页 `Ctl(SettingNumberBox)`（1~64） |
| 装备运行模式 | `EnumSetting<RunMode>`（下拉） | `RunMode.EXPERIENCE`（`挂机循环`） | `DRAIN`（纯附魔模式）/ `EXPERIENCE`（挂机循环） | `() -> 目标模式.get() == TargetMode.GEAR`（:154） | 基础设置（`sgBasic`） | 152-154 | 「基础设置」页 `Ctl(SettingSegmented)` 2 段 |
| 附魔书运行模式 | `EnumSetting<RunMode>`（下拉） | `RunMode.EXPERIENCE`（`挂机循环`） | `DRAIN`（纯附魔模式）/ `EXPERIENCE`（挂机循环） | `() -> 目标模式.get() == TargetMode.BOOK`（:158） | 基础设置（`sgBasic`） | 156-158 | 「基础设置」页 `Ctl(SettingSegmented)` 2 段 |
| 自定义运行模式 | `EnumSetting<RunMode>`（下拉） | `RunMode.EXPERIENCE`（`挂机循环`） | `DRAIN`（纯附魔模式）/ `EXPERIENCE`（挂机循环） | `() -> 目标模式.get() == TargetMode.CUSTOM`（:162） | 基础设置（`sgBasic`） | 160-162 | 「基础设置」页 `Ctl(SettingSegmented)` 2 段 |
| ESP标点 | `BoolSetting` | `true` | `true` / `false` | `() -> false`（:165，**旧项目恒隐藏**） | 基础设置（`sgBasic`） | 164-165 | 存疑·是否一比一不上页面（见 §4-3） |
| 返回挂机视角 | `BoolSetting` | `true` | `true` / `false` | `() -> false`（:168，**旧项目恒隐藏**） | 基础设置（`sgBasic`） | 167-168 | 存疑·同上（见 §4-3） |
| 成功提示音 | `BoolSetting` | `true` | `true` / `false` | 无 `visible()`，恒可见 | 基础设置（`sgBasic`） | 170-172 | 「基础设置」页 `Ctl(SettingToggle)` |
| 成功提示音类型 | `EnumSetting<SuccessSound>`（下拉） | `SuccessSound.CHALLENGE_COMPLETE`（`挑战完成`） | 12 项：`CHALLENGE_COMPLETE`（挑战完成）/ `LEVEL_UP`（升级）/ `ENCHANTMENT_TABLE`（附魔台）/ `NOTE_PLING`（音符叮）/ `BELL`（钟声）/ `FIREWORK`（烟花）/ `EXPERIENCE`（经验球）/ `VILLAGER`（村民庆祝）/ `TRIDENT_THUNDER`（三叉戟雷鸣）/ `ATTACK_CRIT`（暴击）/ `CAT`（猫叫）/ `THUNDER`（雷声） | `() -> 成功提示音.get()`（:176） | 基础设置（`sgBasic`） | 174-176（枚举定义 512-536） | 「基础设置」页 `Ctl(SettingCycle)`（12 选项分段过宽，拟改循环，见 §4-6） |

### 1.2 自定义附魔（`sgCustom`，1 项）

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 自定义附魔目标 | `CustomEnchantSetting extends StringListSetting`（附魔书图标 + 内联列表：每行一个 + 删除，底部添加 / 重置） | `List.of()`（**空列表**） | 每行一个「附魔名称 + 等级」，如 `打雷 5`；描述原文：`每行填写一个附魔名称和等级，例如：打雷 5。支持中文、阿拉伯数字、罗马数字和不带等级的附魔。` | `() -> 目标模式.get() == TargetMode.CUSTOM`（:149） | 自定义附魔（`sgCustom`） | 145-149（控件 `ui/CustomEnchantSetting.java:23-41`） | 「自定义附魔」页；承载方式**待用户确认**（见附表②第 1 行） |

### 1.3 自动附魔分类（`sgAdvanced`，5 项）

本组是 CUSTOM 模式的顶层页；**组内 5 项全部是选择器**（非标准控件），每项的底层是一整组 `BoolSetting`（见 1.4-1.8）。

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 剑附魔属性 | `EnchantmentSelectSetting extends StringListSetting`（附魔书图标 + 「选择附魔」按钮 + 「已选择 N 项」+ 重置；点击开 `EnchantmentSelectScreen`） | `selected(backing)` = 54 个布尔**全为 `false`** → 空选中 | 可勾选项 = `sgSword` 的 54 个布尔设置名（见 1.4） | `() -> 目标模式.get() == TargetMode.CUSTOM`（:541） | 自动附魔分类（`sgAdvanced`） | 541（构造 `ui/EnchantmentSelectSetting.java:34-37`） | **待用户确认**（见附表②第 3 行） |
| 斧头附魔属性 | 同上 | 空选中（`sgAxe` 5 项全 `false`） | 可勾选项 = `sgAxe` 的 5 项（见 1.5） | `() -> 目标模式.get() == TargetMode.CUSTOM`（:542） | 自动附魔分类（`sgAdvanced`） | 542 | **待用户确认**（见附表②第 4 行） |
| 弓附魔属性 | 同上 | 空选中（`sgBow` 34 项全 `false`） | 可勾选项 = `sgBow` 的 34 项（见 1.6） | `() -> 目标模式.get() == TargetMode.CUSTOM`（:543） | 自动附魔分类（`sgAdvanced`） | 543 | **待用户确认**（见附表②第 5 行） |
| 护甲附魔属性 | 同上 | 空选中（`sgArmor` 50 项全 `false`） | 可勾选项 = `sgArmor` 的 50 项（见 1.7） | `() -> 目标模式.get() == TargetMode.CUSTOM`（:544） | 自动附魔分类（`sgAdvanced`） | 544 | **待用户确认**（见附表②第 6 行） |
| 工具与通用附魔属性 | 同上 | 空选中（`sgOther` 13 项全 `false`） | 可勾选项 = `sgOther` 的 13 项（见 1.8） | `() -> 目标模式.get() == TargetMode.CUSTOM`（:545） | 自动附魔分类（`sgAdvanced`） | 545 | **待用户确认**（见附表②第 7 行） |

### 1.4 剑附魔属性（`sgSword`，54 项）

本节 54 项**自身无 `visible()`**，实际可见性由选择器「剑附魔属性」决定（只随 CUSTOM 模式出现，`:541`）；旧项目该组已从 `settings.groups` 摘除（`:571`），故不单独渲染、也不单独落盘。落点排布（页内折叠 vs 选择页）**待用户确认**（见 §4-1）；下表拟用「自动附魔分类页 · `FoldSection`『剑附魔属性』内 `Ctl(SettingToggle)`」。

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 双刃剑 5 | `BoolSetting`（单行声明，无独立字段名） | `false` | `true` / `false` | 无 `visible()`（随选择器 :541，CUSTOM） | 剑附魔属性（`sgSword`） | 180 | 自动附魔分类页 · `FoldSection`「剑附魔属性」内 `Ctl(SettingToggle)` |
| 背刺 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 181 | 同上 |
| 饕餮 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 182 | 同上 |
| 定身 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 183 | 同上 |
| 干扰 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 184 | 同上 |
| 势破 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 185 | 同上 |
| 折锋 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 186 | 同上 |
| 暗影突袭 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 187 | 同上 |
| 永夜 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 188 | 同上 |
| 灵魂收割 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 189 | 同上 |
| 生死判 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 190 | 同上 |
| 破败 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 191 | 同上 |
| 镇魂 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 192 | 同上 |
| 忍者 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 193 | 同上 |
| 忍术 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 194 | 同上 |
| 狂热 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 195 | 同上 |
| 破釜 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 196 | 同上 |
| 血怒 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 197 | 同上 |
| 跃斩 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 198 | 同上 |
| 退散 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 199 | 同上 |
| 速攻 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 200 | 同上 |
| 首击 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 201 | 同上 |
| 骑士 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 202 | 同上 |
| 法术大炮 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 203 | 同上 |
| 冷血术 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 204 | 同上 |
| 剑卫 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 205 | 同上 |
| 丛刃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 207 | 同上 |
| 决斗 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 208 | 同上 |
| 利刃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 209 | 同上 |
| 剑气 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 210 | 同上 |
| 名刀司命 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 211 | 同上 |
| 咒刃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 212 | 同上 |
| 嗜血 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 213 | 同上 |
| 天谴 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 214 | 同上 |
| 天道 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 215 | 同上 |
| 奥术 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 216 | 同上 |
| 对决咒术 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 217 | 同上 |
| 弑君 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 218 | 同上 |
| 弑魔 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 219 | 同上 |
| 惩戒咒术 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 220 | 同上 |
| 慈悲 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 221 | 同上 |
| 生灵咒术 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 222 | 同上 |
| 终结 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 223 | 同上 |
| 脉冲刃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 224 | 同上 |
| 蛇吻 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 225 | 同上 |
| 血偿 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 226 | 同上 |
| 钝锋 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 227 | 同上 |
| 涅槃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 228 | 同上 |
| 心灵感应 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 229 | 同上 |
| 归一 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 230 | 同上 |
| 高傲 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 231 | 同上 |
| 蔑视 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 232 | 同上 |
| 焚天 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 233 | 同上 |
| 讨价还价 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :541） | 剑附魔属性（`sgSword`） | 234 | 同上 |

### 1.5 斧头附魔属性（`sgAxe`，5 项）

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 定身 5 | `BoolSetting`（单行声明，无独立字段名） | `false` | `true` / `false` | 无 `visible()`（随选择器 :542，CUSTOM） | 斧头附魔属性（`sgAxe`） | 237 | 自动附魔分类页 · `FoldSection`「斧头附魔属性」内 `Ctl(SettingToggle)` |
| 涅槃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :542） | 斧头附魔属性（`sgAxe`） | 238 | 同上 |
| 心灵感应 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :542） | 斧头附魔属性（`sgAxe`） | 239 | 同上 |
| 破釜 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :542） | 斧头附魔属性（`sgAxe`） | 240 | 同上 |
| 跃斩 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :542） | 斧头附魔属性（`sgAxe`） | 241 | 同上 |

### 1.6 弓附魔属性（`sgBow`，34 项）

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 势破 5 | `BoolSetting`（单行声明，无独立字段名） | `false` | `true` / `false` | 无 `visible()`（随选择器 :543，CUSTOM） | 弓附魔属性（`sgBow`） | 245 | 自动附魔分类页 · `FoldSection`「弓附魔属性」内 `Ctl(SettingToggle)` |
| 宣判 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 246 | 同上 |
| 折锋 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 247 | 同上 |
| 暗影突袭 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 248 | 同上 |
| 永夜 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 249 | 同上 |
| 浮尘 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 250 | 同上 |
| 灵魂收割 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 251 | 同上 |
| 生死判 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 252 | 同上 |
| 破败 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 253 | 同上 |
| 禁锢 6 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 254 | 同上（等级为 **6**，逐字保留） |
| 镇魂 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 255 | 同上 |
| 忍士 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 256 | 同上 |
| 标记 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 257 | 同上 |
| 狂热 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 258 | 同上 |
| 猎手 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 259 | 同上 |
| 穿颅 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 260 | 同上 |
| 五言 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 261 | 同上 |
| 空军 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 262 | 同上 |
| 首射 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 263 | 同上 |
| 骑射 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 264 | 同上 |
| 鸣踪 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 265 | 同上 |
| 玻璃大炮 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 266 | 同上 |
| 退散 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 267 | 同上 |
| 天道 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 269 | 同上 |
| 弑君 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 270 | 同上 |
| 真三言 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 271 | 同上 |
| 苍劲 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 272 | 同上 |
| 苍穹 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 273 | 同上 |
| 神射手 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 274 | 同上 |
| 创伤 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 275 | 同上 |
| 弓魄 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 276 | 同上 |
| 狂妄 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 277 | 同上 |
| 焚天 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 278 | 同上 |
| 夺金 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :543） | 弓附魔属性（`sgBow`） | 279 | 同上 |

### 1.7 护甲附魔属性（`sgArmor`，50 项）

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 不懈 5 | `BoolSetting`（单行声明，无独立字段名） | `false` | `true` / `false` | 无 `visible()`（随选择器 :544，CUSTOM） | 护甲附魔属性（`sgArmor`） | 283 | 自动附魔分类页 · `FoldSection`「护甲附魔属性」内 `Ctl(SettingToggle)` |
| 不灭 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 284 | 同上 |
| 临阵脱逃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 285 | 同上 |
| 光环 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 286 | 同上 |
| 启迪 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 287 | 同上 |
| 圣愈 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 288 | 同上 |
| 均衡之法 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 289 | 同上 |
| 均衡之遁 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 290 | 同上 |
| 幸运 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 291 | 同上 |
| 清风引 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 292 | 同上 |
| 狂潮 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 293 | 同上 |
| 磐石 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 294 | 同上 |
| 祭祀 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 295 | 同上 |
| 虚幻 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 296 | 同上 |
| 血罡 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 297 | 同上 |
| 霜滞 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 298 | 同上 |
| 倔强 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 299 | 同上 |
| 均衡之御 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 300 | 同上 |
| 均衡之攻 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 301 | 同上 |
| 时速 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 302 | 同上 |
| 金钟罩 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 303 | 同上 |
| 钢铁胃 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 304 | 同上 |
| 乾坤 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 306 | 同上 |
| 力场 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 307 | 同上 |
| 圣之守护 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 308 | 同上 |
| 复苏之风 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 309 | 同上 |
| 奥术壁垒 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 310 | 同上 |
| 奥术血统 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 311 | 同上 |
| 招架 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 312 | 同上 |
| 无畏契约 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 313 | 同上 |
| 星穹 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 314 | 同上 |
| 村庄英雄 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 315 | 同上 |
| 物法皆修 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 316 | 同上 |
| 狂骨 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 317 | 同上 |
| 玄煞 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 318 | 同上 |
| 生命源泉 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 319 | 同上 |
| 生命潮汐 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 320 | 同上 |
| 盛宴 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 321 | 同上 |
| 貔貅 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 322 | 同上 |
| 逆鳞 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 323 | 同上 |
| 旺盛 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 324 | 同上 |
| 捍卫 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 325 | 同上 |
| 决战 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 326 | 同上 |
| 混沌 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 327 | 同上 |
| 燃血 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 328 | 同上 |
| 祭血 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 329 | 同上 |
| 重型装甲 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 330 | 同上 |
| 意志 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 331 | 同上 |
| 噬灵 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 332 | 同上 |
| 破格 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :544） | 护甲附魔属性（`sgArmor`） | 333 | 同上 |

### 1.8 工具与通用附魔属性（`sgOther`，13 项）

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 龙行 5 | `BoolSetting`（单行声明，无独立字段名） | `false` | `true` / `false` | 无 `visible()`（随选择器 :545，CUSTOM） | 工具与通用附魔属性（`sgOther`） | 335 | 自动附魔分类页 · `FoldSection`「工具与通用附魔属性」内 `Ctl(SettingToggle)` |
| 海王 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 336 | 同上 |
| 催生 1 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 337 | 同上（等级为 **1**，逐字保留） |
| 矿脉 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 338 | 同上 |
| 自我修复 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 339 | 同上 |
| 血契 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 340 | 同上 |
| 地质学家 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 341 | 同上 |
| 破界 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 342 | 同上 |
| 贪欲 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 343 | 同上 |
| 立方 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 344 | 同上 |
| 龙之后裔 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 345 | 同上 |
| 龙脉 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 346 | 同上 |
| 挖金 5 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随选择器 :545） | 工具与通用附魔属性（`sgOther`） | 347 | 同上 |

### 1.9 原版附魔分类（`sgVanilla`，8 项）

BOOK 模式的顶层页；**8 项全部是选择器**（非标准控件）。

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 原版防具附魔 | `EnchantmentSelectSetting extends StringListSetting` | 空选中（`sgVanillaArmor` 9 项全 `false`） | 可勾选项 = 9 项（见 1.10-1） | `() -> 目标模式.get() == TargetMode.BOOK`（:554） | 原版附魔分类（`sgVanilla`） | 554 | **待用户确认**（见附表②第 8 行） |
| 原版近战附魔 | 同上 | 空选中（7 项全 `false`） | 可勾选项 = 7 项（见 1.10-2） | `== TargetMode.BOOK`（:555） | 原版附魔分类（`sgVanilla`） | 555 | **待用户确认**（见附表②第 9 行） |
| 原版工具附魔 | 同上 | 空选中（3 项全 `false`） | 可勾选项 = 3 项（见 1.10-3） | `== TargetMode.BOOK`（:556） | 原版附魔分类（`sgVanilla`） | 556 | **待用户确认**（见附表②第 10 行） |
| 原版弓附魔 | 同上 | 空选中（4 项全 `false`） | 可勾选项 = 4 项（见 1.10-4） | `== TargetMode.BOOK`（:557） | 原版附魔分类（`sgVanilla`） | 557 | **待用户确认**（见附表②第 11 行） |
| 原版钓竿附魔 | 同上 | 空选中（2 项全 `false`） | 可勾选项 = 2 项（见 1.10-5） | `== TargetMode.BOOK`（:558） | 原版附魔分类（`sgVanilla`） | 558 | **待用户确认**（见附表②第 12 行） |
| 原版三叉戟附魔 | 同上 | 空选中（4 项全 `false`） | 可勾选项 = 4 项（见 1.10-6） | `== TargetMode.BOOK`（:559） | 原版附魔分类（`sgVanilla`） | 559 | **待用户确认**（见附表②第 13 行） |
| 原版弩附魔 | 同上 | 空选中（3 项全 `false`） | 可勾选项 = 3 项（见 1.10-7） | `== TargetMode.BOOK`（:560） | 原版附魔分类（`sgVanilla`） | 560 | **待用户确认**（见附表②第 14 行） |
| 原版通用附魔 | 同上 | 空选中（1 项 `false`） | 可勾选项 = 1 项（见 1.10-8） | `== TargetMode.BOOK`（:561） | 原版附魔分类（`sgVanilla`） | 561 | **待用户确认**（见附表②第 15 行） |

### 1.10 原版附魔分类 · 8 个子组（33 项）

33 项全部由 `addTargets(sgVanillaXxx, "名字"[, …])` 在构造器中批量创建（调用 `:546-553`，创建处 `:588-590`），**无独立字段名、无描述、无 `visible()`**；旧项目该 8 组已从 `settings.groups` 摘除（`:576-583`），不单独渲染、不单独落盘。落点排布**待用户确认**（见 §4-1），下表拟用「原版附魔分类页 · `FoldSection`『组名』内 `Ctl(SettingToggle)`」。

**1.10-1 原版防具附魔（`sgVanillaArmor`，9 项；`addTargets` 调用行 546）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 保护 IV | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554，BOOK） | 原版防具附魔（`sgVanillaArmor`） | 546（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版防具附魔」内 `Ctl(SettingToggle)` |
| 火焰保护 IV | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上 |
| 摔落缓冲 IV | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上 |
| 爆炸保护 IV | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上 |
| 弹射物保护 IV | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上 |
| 水下呼吸 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上 |
| 水下速掘 | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上（无等级后缀，逐字保留） |
| 荆棘 II | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上 |
| 深海探索者 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :554） | 原版防具附魔（`sgVanillaArmor`） | 546 | 同上 |

**1.10-2 原版近战附魔（`sgVanillaMelee`，7 项；`addTargets` 调用行 547）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 锋利 IV | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :555，BOOK） | 原版近战附魔（`sgVanillaMelee`） | 547（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版近战附魔」内 `Ctl(SettingToggle)` |
| 亡灵杀手 IV | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :555） | 原版近战附魔（`sgVanillaMelee`） | 547 | 同上 |
| 节肢杀手 IV | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :555） | 原版近战附魔（`sgVanillaMelee`） | 547 | 同上 |
| 击退 II | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :555） | 原版近战附魔（`sgVanillaMelee`） | 547 | 同上 |
| 火焰附加 II | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :555） | 原版近战附魔（`sgVanillaMelee`） | 547 | 同上 |
| 抢夺 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :555） | 原版近战附魔（`sgVanillaMelee`） | 547 | 同上 |
| 横扫之刃 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :555） | 原版近战附魔（`sgVanillaMelee`） | 547 | 同上（挂机刷经验按 tooltip 含「横扫之刃」找剑，与本项无关，勿混） |

**1.10-3 原版工具附魔（`sgVanillaTool`，3 项；`addTargets` 调用行 548）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 效率 IV | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :556，BOOK） | 原版工具附魔（`sgVanillaTool`） | 548（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版工具附魔」内 `Ctl(SettingToggle)` |
| 精准采集 | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :556） | 原版工具附魔（`sgVanillaTool`） | 548 | 同上（无等级后缀，逐字保留） |
| 时运 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :556） | 原版工具附魔（`sgVanillaTool`） | 548 | 同上 |

**1.10-4 原版弓附魔（`sgVanillaBow`，4 项；`addTargets` 调用行 549）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 力量 IV | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :557，BOOK） | 原版弓附魔（`sgVanillaBow`） | 549（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版弓附魔」内 `Ctl(SettingToggle)` |
| 冲击 II | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :557） | 原版弓附魔（`sgVanillaBow`） | 549 | 同上 |
| 火矢 | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :557） | 原版弓附魔（`sgVanillaBow`） | 549 | 同上（无等级后缀，逐字保留） |
| 无限 | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :557） | 原版弓附魔（`sgVanillaBow`） | 549 | 同上（无等级后缀，逐字保留） |

**1.10-5 原版钓竿附魔（`sgVanillaFishing`，2 项；`addTargets` 调用行 550）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 海之眷顾 III | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :558，BOOK） | 原版钓竿附魔（`sgVanillaFishing`） | 550（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版钓竿附魔」内 `Ctl(SettingToggle)` |
| 饵钓 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :558） | 原版钓竿附魔（`sgVanillaFishing`） | 550 | 同上 |

**1.10-6 原版三叉戟附魔（`sgVanillaTrident`，4 项；`addTargets` 调用行 551）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 忠诚 III | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :559，BOOK） | 原版三叉戟附魔（`sgVanillaTrident`） | 551（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版三叉戟附魔」内 `Ctl(SettingToggle)` |
| 穿刺 V | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :559） | 原版三叉戟附魔（`sgVanillaTrident`） | 551 | 同上（等级 V，逐字保留） |
| 激流 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :559） | 原版三叉戟附魔（`sgVanillaTrident`） | 551 | 同上 |
| 引雷 | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :559） | 原版三叉戟附魔（`sgVanillaTrident`） | 551 | 同上（无等级后缀，逐字保留） |

**1.10-7 原版弩附魔（`sgVanillaCrossbow`，3 项；`addTargets` 调用行 552）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 多重射击 | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :560，BOOK） | 原版弩附魔（`sgVanillaCrossbow`） | 552（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版弩附魔」内 `Ctl(SettingToggle)` |
| 快速装填 III | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :560） | 原版弩附魔（`sgVanillaCrossbow`） | 552 | 同上 |
| 穿透 IV | `BoolSetting`（`addTargets()`） | `false` | `true` / `false` | 无 `visible()`（随选择器 :560） | 原版弩附魔（`sgVanillaCrossbow`） | 552 | 同上 |

**1.10-8 原版通用附魔（`sgVanillaCommon`，1 项；`addTargets` 调用行 553）**

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 耐久 III | `BoolSetting`（`addTargets()` 批量创建） | `false` | `true` / `false` | 无 `visible()`（随选择器 :561，BOOK） | 原版通用附魔（`sgVanillaCommon`） | 553（创建于 588-590） | 原版附魔分类页 · `FoldSection`「原版通用附魔」内 `Ctl(SettingToggle)` |

### 1.11 原版装备附魔（`sgGear`，3 项）

| 设置项名称（逐字） | 旧控件类型 | 默认值 | 取值范围 / 取值域 | 可见性条件 | 所属分组 | 源码行号 | 新项目落点（控制台页 + 拟用控件） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 记录合成日志 | `BoolSetting` | `false` | `true` / `false` | 无 `visible()`（随 `sgGear`，仅 GEAR 模式页） | 原版装备附魔（`sgGear`） | 451-455 | 「原版装备附魔」页 `Ctl(SettingToggle)`（描述含产出目录 `.minecraft/yiyiaddon-reports/装备附魔合成/`，逐字保留；产出报告本轮延后，开关先照旧落位） |
| 合成策略 | `EnumSetting<AnvilPlanner.Strategy>`（下拉） | `AnvilPlanner.Strategy.SAVE_XP`（`节能`） | `SIMPLE`（简单）/ `SAVE_XP`（节能）/ `FAST`（快速）（`gear/AnvilPlanner.java:50-56`） | 无 `visible()`（随 `sgGear`） | 原版装备附魔（`sgGear`） | 458-462 | 「原版装备附魔」页 `Ctl(SettingSegmented)` 3 段 |
| 装备附魔配置 | `GearEnchantSetting extends StringListSetting`（装备图标 + 「配置装备附魔」按钮 + 摘要 + 重置；点击开 `GearEnchantScreen`） | `new ArrayList<>()`（**空列表** = 未选择装备） | 约定格式字符串列表：`[0]` 装备 ID（如 `minecraft:diamond_sword`）/ `[1]` 方案 ID（如 `fortune`）/ `[2..]` 每个目标附魔 `enchantId:level:excludedFlag`（flag `0`/`1`） | `() -> 目标模式.get() == TargetMode.GEAR`（:562） | 原版装备附魔（`sgGear`） | 562-563（控件 `gear/GearEnchantSetting.java:40-70`） | 「原版装备附魔」页；承载方式**待用户确认**（见附表②第 2 行） |

---

## 2. 附表①：模式 × 分组可见性矩阵

依据 `同步模式分组()` `AutoEnchantBook.java:635-649`（`ordered` 顺序）+ 构造器 `:564-585`（`sectionExpanded` 与 `settings.groups.remove(...)`）。

图例：**✓** = 作为顶层分组出现在设置树；**✗** = 完全不在设置树；**(✓)** = 本组不在设置树，但其全部设置项由所在选择器承载，选择器可见时其项即可见。
`sectionExpanded` 列 = 旧项目初始展开态（未显式设置的取 `Settings.createGroup(name)` 默认 `true`，`Settings.java:97-99`）。

| # | 分组（字段） | GEAR | BOOK | CUSTOM | `ordered` 顺序 | `sectionExpanded` | 依据行号 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 基础设置（`sgBasic`） | ✓ | ✓ | ✓ | 第 1 位（恒在） | `true`（默认） | `:638` |
| 2 | 自动附魔分类（`sgAdvanced`） | ✗ | ✗ | ✓ | CUSTOM 第 2 位 | `true`（`:564`） | `:639` |
| 3 | 自定义附魔（`sgCustom`） | ✗ | ✗ | ✓ | CUSTOM 第 3 位 | `true`（默认） | `:640` |
| 4 | 原版附魔分类（`sgVanilla`） | ✗ | ✓ | ✗ | BOOK 第 2 位 | `false`（`:565`） | `:641` |
| 5 | 原版装备附魔（`sgGear`） | ✓ | ✗ | ✗ | GEAR 第 2 位 | `true`（`:566`） | `:642` |
| 6 | 剑附魔属性（`sgSword`） | ✗ | ✗ | (✓) | —（子组，挂 `sgAdvanced`） | `false`（`:567`） | `:571` 摘除；选择器 `:541` |
| 7 | 斧头附魔属性（`sgAxe`） | ✗ | ✗ | (✓) | — | `false`（`:568`） | `:572` 摘除；选择器 `:542` |
| 8 | 弓附魔属性（`sgBow`） | ✗ | ✗ | (✓) | — | `false`（`:569`） | `:573` 摘除；选择器 `:543` |
| 9 | 护甲附魔属性（`sgArmor`） | ✗ | ✗ | (✓) | — | `false`（`:570`） | `:574` 摘除；选择器 `:544` |
| 10 | 工具与通用附魔属性（`sgOther`） | ✗ | ✗ | (✓) | — | `true`（默认） | `:575` 摘除；选择器 `:545` |
| 11 | 原版防具附魔（`sgVanillaArmor`） | ✗ | (✓) | ✗ | —（子组，挂 `sgVanilla`） | `true`（默认） | `:576` 摘除；选择器 `:554` |
| 12 | 原版近战附魔（`sgVanillaMelee`） | ✗ | (✓) | ✗ | — | `true`（默认） | `:577` 摘除；选择器 `:555` |
| 13 | 原版工具附魔（`sgVanillaTool`） | ✗ | (✓) | ✗ | — | `true`（默认） | `:578` 摘除；选择器 `:556` |
| 14 | 原版弓附魔（`sgVanillaBow`） | ✗ | (✓) | ✗ | — | `true`（默认） | `:579` 摘除；选择器 `:557` |
| 15 | 原版钓竿附魔（`sgVanillaFishing`） | ✗ | (✓) | ✗ | — | `true`（默认） | `:580` 摘除；选择器 `:558` |
| 16 | 原版三叉戟附魔（`sgVanillaTrident`） | ✗ | (✓) | ✗ | — | `true`（默认） | `:581` 摘除；选择器 `:559` |
| 17 | 原版弩附魔（`sgVanillaCrossbow`） | ✗ | (✓) | ✗ | — | `true`（默认） | `:582` 摘除；选择器 `:560` |
| 18 | 原版通用附魔（`sgVanillaCommon`） | ✗ | (✓) | ✗ | — | `true`（默认） | `:583` 摘除；选择器 `:561` |

**顶层只同时出现 2~3 个**：GEAR = 基础设置 + 原版装备附魔；BOOK = 基础设置 + 原版附魔分类；CUSTOM = 基础设置 + 自动附魔分类 + 自定义附魔。
**幂等保护必须照抄**（`:643-648`）：`ordered` 与现有 `settings.groups` 相等时直接 `return`，否则在 `目标模式.onChanged` → `Modules.load / Settings.fromTag` 遍历 `settings.groups` 期间 `clear + addAll` 会抛 `ConcurrentModificationException` 导致启动闪退。新项目换成页签后，同样禁止「遍历页签的同时重建页签集合」（38-附A §3 已列为硬约束）。

**对应控制台页签（38-附A §3，已确认）**

| 当前模式 | 页签（逐字沿用旧分组名与 `ordered` 顺序） |
| --- | --- |
| GEAR | 概览 · 点位 · 基础设置 · 原版装备附魔 |
| BOOK | 概览 · 点位 · 基础设置 · 原版附魔分类 |
| CUSTOM | 概览 · 点位 · 基础设置 · 自动附魔分类 · 自定义附魔 |

---

## 3. 附表②：非标准控件的设置项清单（15 项）

15 项 = 1（`CustomEnchantSetting`）+ 1（`GearEnchantSetting`）+ 13（`EnchantmentSelectSetting`）。
「序列化键名（旧）」= 旧项目 Meteor 落盘键（键即设置名，见 §0.3）；「值格式」= 该键下真正写的内容。

| # | 设置项名称（逐字） | 旧控件（类 + 行号） | 序列化键名（旧） | 值格式 | 源码行号 | 控制台里准备怎么承载（**待用户确认**） |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 自定义附魔目标 | `CustomEnchantSetting extends StringListSetting`（`ui/CustomEnchantSetting.java:21`，构造 `:23-25`，控件 `:32-41`） | `自定义附魔目标` | `value` = ListTag（每项 StringTag 一行）；默认空列表 `List.of()`；行内容例 `打雷 5` | `AutoEnchantBook.java:145-149` | 候选 A：页内 `FoldSection` + `Ctl(SettingTextBox)` 逐行编辑（照 `BaritoneTextListScreen` 先例）；候选 B：`Ctl(Button)` 打开自由文本名单窗口。旧控件为「附魔书图标 + 内联列表（添加/删除/重置）」 |
| 2 | 装备附魔配置 | `GearEnchantSetting extends StringListSetting`（`gear/GearEnchantSetting.java:36`，构造 `:40-42`，控件 `:50-70`，encode `:112-120`，parse `:163-174`，摘要 `:227-236`；旧弹窗 `GearEnchantScreen`（311 行）由 `:60-61` 延迟一 tick 打开） | `装备附魔配置` | `value` = ListTag；`[0]` 装备 ID、`[1]` 方案 ID、`[2..]` `enchantId:level:excludedFlag`；默认空 `ArrayList`（摘要文案 `§a<装备> §8▸ §b<方案> §8▸ §e<N 项目标>`，未选装备时 `未选择装备`） | `AutoEnchantBook.java:562-563`（声明 `:449`） | 38 号待确认项第 6 条已定「放进控制台『原版装备附魔』页里直接操作，**不另开独立窗口**」；但「类别 → 装备 → 方案」三级选择 + 每项等级加减 + 排除开关，用哪些现有控件拼（`SettingCycle` / `ButtonStrip` / `Table` / 逐项 `SettingNumberBox`）**待用户确认** |
| 3 | 剑附魔属性 | `EnchantmentSelectSetting extends StringListSetting`（`ui/EnchantmentSelectSetting.java:25`，构造 `:34-37`，勾选映射 `:39-47`，控件 `:53-65`；旧弹窗 `ui/EnchantmentSelectScreen.java`（77 行），标题 = `setting.title`（`:23`），打开处 `EnchantmentSelectSetting.java:60`） | `剑附魔属性` | `value` = ListTag，内容 = 被勾选 `BoolSetting` 的**名字**（如 `双刃剑 5`）；默认空；底层 54 个 `BoolSetting` **不单独落盘**（`:571` 摘除分组 + `Settings.java:157-167` 只遍历 `groups`） | `AutoEnchantBook.java:541` | 候选 A：页内 `FoldSection` + 54 行 `Ctl(SettingToggle)`（38-附A §3 原话）；候选 B：一行 `Ctl(Button)`「选择附魔」+ `Ctl(SettingText)`「已选择 N 项」+ 重置按钮，打开 `SelectorScreen` 双栏选择页（41 号挖矿先例）。**待用户确认** |
| 4 | 斧头附魔属性 | 同上 | `斧头附魔属性` | 同上；底层 5 项 | `:542` | 同上（候选 A/B） |
| 5 | 弓附魔属性 | 同上 | `弓附魔属性` | 同上；底层 34 项 | `:543` | 同上（候选 A/B） |
| 6 | 护甲附魔属性 | 同上 | `护甲附魔属性` | 同上；底层 50 项 | `:544` | 同上（候选 A/B） |
| 7 | 工具与通用附魔属性 | 同上 | `工具与通用附魔属性` | 同上；底层 13 项 | `:545` | 同上（候选 A/B） |
| 8 | 原版防具附魔 | 同上 | `原版防具附魔` | 同上；底层 9 项 | `:554` | 同上（候选 A/B），页面落在「原版附魔分类」页 |
| 9 | 原版近战附魔 | 同上 | `原版近战附魔` | 同上；底层 7 项 | `:555` | 同上 |
| 10 | 原版工具附魔 | 同上 | `原版工具附魔` | 同上；底层 3 项 | `:556` | 同上 |
| 11 | 原版弓附魔 | 同上 | `原版弓附魔` | 同上；底层 4 项 | `:557` | 同上 |
| 12 | 原版钓竿附魔 | 同上 | `原版钓竿附魔` | 同上；底层 2 项 | `:558` | 同上 |
| 13 | 原版三叉戟附魔 | 同上 | `原版三叉戟附魔` | 同上；底层 4 项 | `:559` | 同上 |
| 14 | 原版弩附魔 | 同上 | `原版弩附魔` | 同上；底层 3 项 | `:560` | 同上 |
| 15 | 原版通用附魔 | 同上 | `原版通用附魔` | 同上；底层 1 项 | `:561` | 同上 |

> 13 个选择器的**描述文案全部相同**：`选择需要收集的附魔属性`（`ui/EnchantmentSelectSetting.java:35`），逐字保留。

---

## 4. 存疑清单（6 条）

1. **189 项附魔勾选的页内承载**：38-附A §3 写「13 个子组在页内用 `FoldSection` 折叠」，而挖矿 41 号先例走的是 `SettingLink + SelectorScreen` 双栏选择页。两者只能选一：全塞页内＝「自动附魔分类」页 156 行、BOOK 页 33 行；走选择页＝每子组一行按钮 + 计数。**待用户确认**（附表②已按此列候选 A/B）。
2. **选择器摘要与重置**：旧控件是「附魔书图标 + 选择附魔按钮 + `已选择 N 项` + 重置」（`EnchantmentSelectSetting.java:53-65`）。若改走页内折叠，是否保留「已选择 N 项」计数与重置按钮 —— **存疑**。
3. **两项恒隐藏开关**：`ESP标点`（`:164-165`）与 `返回挂机视角`（`:167-168`）的 `visible` 为 `() -> false`，旧项目**永不上界面**但值仍落盘（默认 `true`）。是否一比一不上页面（挖矿 41 号 §2.8 先例是「不上页面、值仍落盘」）—— **待用户确认**。
4. **切模式后的落盘范围**：旧 `Settings.toTag()` 只遍历当前 `groups`（`Settings.java:157-167`），非当前模式的分组在存档时**不写入**（旧项目靠内存不回滚，实机未暴露）。新项目是否必须保留该行为 —— **存疑**。
5. **装备附魔配置的控件拼装**：见附表②第 2 行 —— **待用户确认**。
6. **`成功提示音类型`（12 选项）的控件选择**：`SettingSegmented` 12 段会远超行宽（项目里分段控件最小段宽 64px），拟改 `Ctl(SettingCycle)` 逐次点击循环 —— **存疑，待用户拍**。

---

## 5. 证据与命令（只读，抄录）

```powershell
$f='d:\mcaddon\26.1.2\src\main\java\com\example\addon\enchant\AutoEnchantBook.java'

# 各分组设置项数（本表 §0 表即此命令输出）
foreach($x in 'sgBasic','sgCustom','sgSword','sgAxe','sgBow','sgArmor','sgOther','sgAdvanced','sgVanilla','sgGear'){
  "{0,-12} .add( 次数: {1}" -f $x, (Select-String -Path $f -Pattern ([regex]::Escape($x) + '\.add\(') -AllMatches).Matches.Count
}
# → sgBasic 15 / sgCustom 1 / sgSword 54 / sgAxe 5 / sgBow 34 / sgArmor 50 / sgOther 13 / sgAdvanced 0 / sgVanilla 0 / sgGear 3
# （sgAdvanced、sgVanilla 的项经 addSelector() 内的 target.add(selector) 加入，故直接 .add( 计数为 0）

# 设置项声明总数、Setting.name 总数（= 174 - 1：自定义附魔目标的名字走构造器参数）
(Select-String -Path $f -Pattern 'private final Setting<' -AllMatches).Matches.Count   # 174
(Select-String -Path $f -Pattern '\.name\("' -AllMatches).Matches.Count                # 173

# 18 个分组声明
Select-String -Path $f -Pattern 'settings\.createGroup\(' | ForEach-Object { "{0}: {1}" -f $_.LineNumber, $_.Line.Trim() }   # 94-111

# 逐字核查：设置声明区（94-350）内不得出现 §、全角空格 U+3000、全角括号 U+FF08
"94-350 §: "      + (Select-String -Path $f -Pattern '§'            | Where-Object { $_.LineNumber -ge 94 -and $_.LineNumber -le 350 }).Count   # 0
"94-350 U+3000: " + (Select-String -Path $f -Pattern ([char]0x3000) | Where-Object { $_.LineNumber -ge 94 -and $_.LineNumber -le 350 }).Count   # 0
"94-350 U+FF08: " + (Select-String -Path $f -Pattern ([char]0xFF08) | Where-Object { $_.LineNumber -ge 94 -and $_.LineNumber -le 350 }).Count   # 0
(Get-Content $f -Raw).ToCharArray() | Where-Object { $_ -eq [char]0x3000 } | Measure-Object | Select-Object -Expand Count   # 17（全在 969-2085 行播报文案）

# 模式可见性（同步模式分组 幂等保护）
Select-String -Path $f -Pattern '同步模式分组' | ForEach-Object { "{0}: {1}" -f $_.LineNumber, $_.Line.Trim() }   # 116 / 585 / 635 / 649 / 854

# 子组摘除点（13 处 remove）
Select-String -Path $f -Pattern 'settings\.groups\.remove\(' | ForEach-Object { $_.LineNumber }   # 571-583

# 两个自定义枚举的取值域
Select-String -Path 'd:\mcaddon\26.1.2\src\main\java\com\example\addon\enchant\gear\AnvilPlanner.java' -Pattern 'SIMPLE\("|SAVE_XP\("|FAST\("'   # 52/54/56
Select-String -Path 'd:\mcaddon\26.1.2\src\main\java\com\example\addon\enchant\AutoEnchantBook.java' -Pattern 'SuccessSound\('                     # 513-524（12 项）
```

**旧项目落盘键证据**：`01-开发参考库\Meteor原始源码\meteordevelopment\meteorclient\settings\Settings.java:157-167`（遍历 `groups`）、`SettingGroup.java:60-73 / 76-88`（`name` / `settings` / `getStringOr("name")`）、`Setting.java:121`（`tag.putString("name", name)`）、`StringListSetting.java:47-52 / 58-64`（`value` ListTag）。

---

## 6. 施工提示（照本表逐项对默认值时）

1. 本表 221 行就是批次 2（`EnchantSettings` 18 组声明）与批次 5（控制台分页）的**验收基线**：新项目里每一项的名称、默认值、取值域必须与「设置项名称（逐字）」「默认值」「取值范围 / 取值域」三列逐字相同。
2. 旧项目**全部整数项都是 `noSlider()`**（13 项，见 §1.1 与 1.11 的 `min/max`），新项目一律用无滑块的 `SettingNumberBox`。
3. 189 个子组布尔项的**归属**以本表「所属分组」列为准，不要按名字前缀（`剑_` / `斧_` / `弓_` / `甲_` / `其他_` 只是旧字段名前缀）自行归类；两者一致，但以分组为准。
4. `.fumo` 指令、点位、播报不属于本表范围（见 44/45 号侦察记录）。
