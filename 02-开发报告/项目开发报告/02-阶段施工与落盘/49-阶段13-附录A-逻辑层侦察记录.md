# 49 · 阶段13 · 附录A · 逻辑层侦察记录（自动农场）

> 本文是 [48-阶段13-自动农场移植侦察与决策.md](48-阶段13-自动农场移植侦察与决策.md) 的**逻辑层**逐行侦察记录，供施工逐条对账。
> 口径：**只抄旧项目原文与行号，不做概括、不给建议**；行号为本机实测（2026-09-16）。
> 路径前缀：`OLD = d:\mcaddon\26.1.2\src\main\java\com\example\addon\`
> 关联：[50-阶段13-附录B-界面与指令侦察记录.md](50-阶段13-附录B-界面与指令侦察记录.md)、[51-阶段13-附录C-设置项控件对照表.md](51-阶段13-附录C-设置项控件对照表.md)。

---

## 一、模块入口 `autofarm\AutoFarmMatrix.java`（854 行）

### 1.1 类与成员

| 项 | 事实 | 行号 |
|---|---|---|
| 声明 | `public final class AutoFarmMatrix extends YiyiaddonModule` | `:68` |
| 构造标题/描述 | `super(AddonTemplate.CATEGORY_AUTOMATION, "自动农场", "熟一颗收一颗，自动补种拾取，单作物箱/种子补货箱/多作物箱与杂物箱物流自动化。点击按钮查看说明。")` | `:132-133` |
| 构造中 new 的对象 | `scanner`（FarmScanner）`:71`、`broker`（ContainerBroker）`:72`、`resources`（FarmResourceManager）`:73`、`observer`（FarmObserver）`:74`、`verifier`（FarmVerifier）`:75`、`selfCheck`（FarmSelfCheck）`:76`、`decision`（FarmDecision）`:284`、`controller`（FarmController）`:285` | — |
| 日志注入 | `controller.setLogger(this::notify)` | `:286` |
| 其它字段 | `lastNotifiedState` `:127`、`prevPauseOnLostFocus`（启动时记录「失焦暂停」原值，关闭还原）`:128-129` | — |

### 1.2 事件订阅

| 事件 | 处理 | 行号 |
|---|---|---|
| `TickEvent.Post` | `onTick`，世界为空 return；防踩踏；四次 `update`；`resources.configure`；`controller.configure`；`controller.tick()`；`broadcastState` | `:440-464` |
| `Render3DEvent` | `onRender3D`（边界 / 目标） | `:483-502` |
| `Render2DEvent` | `onRender2D`（六点位字牌） | `:504-513` |
| `GameLeftEvent` | `if (isActive()) toggle();` | `:522-525` |
| `OpenScreenEvent` | 物流任务运行中拦截容器界面（`currentTask.exclusive()`） | `:527-540` |

### 1.3 生命周期

| 方法 | 关键动作（行号顺序） |
|---|---|
| `onActivate()` `:304-345` | 世界校验（不在世界 → `notifyError("必须在进入世界后才能启动模块。")` `:307` 并 `mc.execute(this::toggle)` `:308`）→ 清理脏数据 → `selfCheck.check(getEnabledCrops(), getSitesMap())` `:316`（失败即 `reportSelfCheck` 中止）→ 关失焦暂停 `:318-325` → 配置扫描器 `:325-333`（含 `fullScan()` `:333`）→ `decision.setSuppress.../resources.configure/controller.configure` `:334-340` → 启动播报 `:341-345` |
| `onDeactivate()` `:414-425` | `controller.reset()`、`ContainerBroker.closeContainer()`、还原失焦暂停原值 |

### 1.4 对外 API（供指令与页面调用）

| 方法 | 行号 |
|---|---|
| `site(SiteType)` | `:546-548`（内部 `FarmSite.parse(rawSite(type))`） |
| `bindSite(SiteType, FarmSite)` | `:550-561`（START/END 时同步 `scanner.setBounds`） |
| `clearSite(SiteType)` | `:563-565` |
| `clearAllSites()` | `:567-572`（全清后 `scanner.reset()`） |
| `getSitesMap()` | `:677-684`（遍历 `SiteType.values()` 组装 Map） |
| 私有 `rawSite` / `setRawSite` | `:574-583` / `:585-594`（switch 六分支读写六个隐藏 StringSetting） |
| 私有辅助 | `reportStartupInfo` `:348-412`、`onHarvestModeChanged` `:428-434`、`broadcastState` `:466-476`、`isLogisticsState` `:479-481`、`renderLabel` `:515-520`、`hiddenAnchor` `:291-298`、`getEnabledCrops` `:601-612`、`addCrops` `:615-621`、`isDoubleCrop/isSingleCrop/isPillar/isFruit` `:624-647`、`sanitizeCropSelectors/sanitize` `:650-660`、`buildUnloadGroups/buildRestockGroups` `:663-674`、`statusSummary` `:739-754`、`buildLocationCard` `:757-796`、`dimensionName` `:798-800`、`buildHelpContent` `:803-853`、`getWidget` `:690-736` |

---

## 二、控制器 `autofarm\controller\FarmController.java`（539 行）

### 2.1 公开 API

| 签名 | 行号 |
|---|---|
| `FarmController(FarmScanner, FarmResourceManager, FarmObserver, FarmVerifier, ContainerBroker, FarmDecision)` | `:88-96` |
| `void setLogger(Consumer<String>)` | `:99-101` |
| `void configure(Map<SiteType,FarmSite> sites, double collectRange)` | `:104-107` |
| `FarmState state()` | `:109-111` |
| `FarmTask currentTask()` | `:113-115` |
| `void tick()` | `:118-276` |
| `void reset()` | `:517-538` |

私有：`farmCenter()` `:279-288`、`updateIdleWait()` `:294-344`、`handleTaskResult(...)` `:347-462`、`taskName(...)` `:465-470`、`startBatchReplant(...)` `:473-489`、`startBatchCollect()` `:492-499`、`stateFor(...)` `:501-510`、`setState(...)` `:512-514`。

### 2.2 `tick()` 调度顺序（严格行序）

| 序 | 行号 | 动作 | 命中即 return |
|---|---|---|---|
| 1 | `:120` | `!observer.worldReady()` | 是 |
| 2 | `:121-141` | 玩家死亡 → 清 `currentTask`/`interleavedHarvest`/三个计划、`FarmNav.cancel()`、`OBSERVE` | 是 |
| 3 | `:144` | `scanner.tick()` | — |
| 4 | `:145` | `broker.tick()` | — |
| 5 | `:148-156` | 当前任务 `tick()`；`done()` → `handleTaskResult` | 是 |
| 6 | `:159-170` | 物流 `decision.decideLogistics(sites)` | 是 |
| 7 | `:173-186` | 批量收割计划 `batchPlan` | 是 |
| 8 | `:189-201` | 批量补种计划 `batchPlantPlan` | 是 |
| 9 | `:204-216` | 批量锄地计划 `batchTillPlan` | 是 |
| 10 | `:219-244` | `decision.tillWorkAvailable()` + `selectTillTargets()` | 是 |
| 11 | `:247-258` | `decision.selectPlantTargets()` | 是 |
| 12 | `:261-272` | `decision.selectHarvestTargets()` | 是 |
| 13 | `:275` | `updateIdleWait()` | — |

### 2.3 状态写入点（**状态只写不读**，全类无 `switch(state)`）

| # | 触发 | 目标状态 | 行号 |
|---|---|---|---|
| 0 | 构造初值 | `OBSERVE` | `:52` |
| 1 | 玩家死亡 | `OBSERVE` | `:139` |
| 2-8 | 物流任务映射 | `PLANT`/`TILL`/`COLLECT`/`UNLOAD`/`RESTOCK`/`POISON_DUMP` | `:503-508` |
| 9 | 批量收割有目标 | `HARVEST` | `:182` |
| 11 | 批量补种有目标 | `PLANT` | `:198` |
| 12 | 批量锄地有目标 | `TILL` | `:209` |
| 15 | 锄地有目标 | `TILL` | `:238` |
| 16/17 | 补种（批量/单个） | `PLANT` | `:252`、`:255` |
| 18/19 | 收割（批量/单个） | `HARVEST` | `:266`、`:269` |
| 20-24 | 归位等待 | `OBSERVE` | `:298`、`:312`、`:320`、`:332`、`:343` |
| 25 | 收割成功且需补种且有种子 | `PLANT` | `:359` |
| 26 | 收割成功无需补种 | `COLLECT` | `:362` |
| 27 | 收割失败 | `OBSERVE` | `:368` |
| 28/29 | 批量收割结束 | `PLANT` / `COLLECT` | `:484`、`:498` |
| 30/31 | 批量补种完成 | `COLLECT` / `OBSERVE` | `:389`、`:391` |
| 32/33 | 单个补种完成 | `OBSERVE` / `COLLECT` | `:402`、`:406` |
| 34 | 锄地完成 | `OBSERVE` | `:428` |
| 36/37 | 拾取完成 | `HARVEST` / `OBSERVE` | `:437`、`:439` |
| 38 | 独占物流任务完成 | `OBSERVE` | `:457` |
| 39 | 兜底 | `OBSERVE` | `:461` |
| 40 | `reset()` | `OBSERVE` | `:537` |

### 2.4 常量与阈值

| 常量 / 值 | 行号 |
|---|---|
| `CENTER_WAIT_TIMEOUT = 200`（归位寻路超时 tick） | `:76` |
| 归位到位半径 `1.5` | `:305` |
| `collectRange` 默认 `4` | `:86` |
| 批量统一拾取半径 `Math.max(Math.max(dx, dz), 2) / 2.0 + 1` | `:496` |
| `reset()` 全量清理项 | `:518-537`（cancel task、interleavedHarvest、三计划、两个 notified 标记、idleReturnTicks、lastIdleNotify、harvestReplantPhase、`FarmNav.cancel()`、`broker.reset()`、`scanner.reset()`、状态回 `OBSERVE`） |

---

## 三、决策 `autofarm\controller\FarmDecision.java`（315 行）

### 3.1 输入与配置注入

| 项 | 行号 |
|---|---|
| 构造依赖 `FarmScanner`/`FarmResourceManager`/`FarmObserver`/`FarmVerifier`/`ContainerBroker` | `:42-46`、`:68-72` |
| `update(int poisonUnloadThreshold, int bpt, double reachDistance)` | `:79-83` |
| `updateMode(HarvestMode)` | `:86-88` |
| `updatePlantMode(PlantMode)` | `:91-93` |
| `updateTill(boolean)` | `:96-98` |
| `suppressRestock(CropProfile)` | `:167-169`（配 `restockSuppressed` `:63`） |
| `rotateCursor`（轮转游标） | `:59` |
| `tillEnabled` 默认 `true` | `:61` |

### 3.2 物流判定（`:109-164`）

| 序 | 条件 | 行号 |
|---|---|---|
| ① 杂物 | `resources.countJunk() >= poisonUnloadThreshold` 且 `validSite(sites, POISON_STORAGE) != null` → `new PoisonDumpTask(junk.pos(), broker, reachDistance, bpt, resources::shouldDepositJunk)` | `:111-114` |
| ② 补货 | 遍历 `scanner.enabledCrops()`：`needsRestock` 为假则解除抑制并 continue `:120-124`；被抑制则 continue `:125`；箱型 = `crop.plantItem() != crop.harvestItem() ? SEED_STORAGE : SINGLE_STORAGE` `:127-128`；有效则 `new RestockTask(..., crop)` `:129-132` | — |
| ③ 卸货总闸 | `hasDepositableSeed() \|\| hasDepositableSingle() \|\| hasDepositableDual() \|\| observer.freeInventorySlots() <= 2` | `:136-139` |
| ③-1/2/3 | `SEED_STORAGE` + `shouldUnloadSeed` `:141-146`；`SINGLE_STORAGE` + `shouldUnloadSingle` `:148-153`；`MULTI_STORAGE` + `shouldUnloadDual` `:155-160` | — |

### 3.3 目标选择与阈值

| 方法 | 判据 | 行号 |
|---|---|---|
| `selectHarvestTargets()` | `max = mode == BATCH ? Integer.MAX_VALUE : 1` | `:175-178`（`:176`） |
| `selectPlantTargets()` | 遍历 `scanner.nearestPlantablePositions(Integer.MAX_VALUE)`，且 `countItem(plantItem) > 0` | `:181-190` |
| `selectTillTargets()` | `!tillEnabled \|\| !needsTill() \|\| !hasHoe()` → 空 | `:193-200` |
| `tillWorkAvailable()` | `tillEnabled && needsTill() && scanner.hasTillable()` | `:203-205` |
| `hasHoe()` | `InvUtils.find(stack -> stack.getItem() instanceof HoeItem).found()` | `:208-212` |
| `needsTill()` | 任一启用作物 `crop.soil() == Blocks.FARMLAND` | `:311` |
| `validSite(type, sites)` | null → null；`!inCurrentDimension()` → null；`requiresContainer()` 时要求 `getBlockEntity instanceof Container` | `:293-306` |
| 补种模式分派 | `switch (plantMode)` `:216-220` → `selectSequential` `:224-232`、`selectRotate`（`rotateCursor = (index + 1) % list.size()` `:245`）`:235-250`、`selectFollowNearby`（`FOLLOW_RADIUS = 4` `:253`，`dx,dz ∈ [-4,4]` 跳过 `(0,0)` `:262-264`，同种计数 `:267`，取最多 `:278`，校验 `isPlantable` `:283`，无邻居回落 `selectSequential` `:289`）`:256-290` | — |

---

## 四、扫描器 `autofarm\scan\FarmScanner.java`（309 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 预算 | `BUDGET_PER_TICK = 512` | `:31` |
| 边界 | `min`/`max`/`bounded` | `:33-35` |
| 游标 | `cursorX`/`cursorY`/`cursorZ` | `:38-40` |
| 三缓存 | `matureBlocks` `:43`、`plantableBlocks` `:45`、`tillableBlocks` `:47` | — |
| 启用作物 | `EnumSet<CropProfile> enabled` | `:49` |
| `setBounds(a, b)` | 对角归一 `:53-60`；**min.Y-1** `:64`、**max.Y+3** `:65`；`bounded = true` `:66`；`restart()` `:67` | `:52-68` |
| `setEnabledCrops(Set)` | 相同即 return，否则 rebuild + `restart()` | `:71-76` |
| 查询 API | `enabledCrops()` `:79-81`、`bounded()` `:83-85`、`min()` `:87-89`、`max()` `:91-93`、`volume()` `:96-102`、`contains(pos)` `:166-171` | — |
| 排序查询 | `nearestHarvests(int)` `:105-122`（`new ArrayList` + 按距离排序 `:112-113`，逐个 `CropProfile.byBlock` 二次确认 `:117-118`）、`nearestPlantablePositions(int)` `:125-140`、`nearestTillablePositions(int)` `:143-158` | — |
| 失效与重置 | `invalidate(pos)` `:174-178`、`reset()` `:180-186`、`restart()` `:189-193` | — |
| 推进 | `tick()` `:196-210`（`!bounded \|\| enabled.isEmpty()` 直接 return `:197`；循环 512 格 `:205-209`）、`advance()` x→z→y `:237-252` | — |
| 分类 | `classify(ClientLevel, BlockPos)` `:255-292`：空气清三缓存 `:258-263`；成熟 `:266-270`；可补种（遍历 enabled + `isPlantable`）`:273-284`；可锄地 `:286-291` | — |
| 全量 | `fullScan()` 三重循环 `:217-234`（注释声明超大农田可能单帧卡顿 `:213`） | — |
| 静态度量 | `isTillable(level, pos)` = `(GRASS_BLOCK \|\| DIRT) && 上方 isAir()` | `:295-299` |

---

## 五、资源 `autofarm\resource\FarmResourceManager.java`（263 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 常量 | `DEFAULT_UNLOAD_GROUPS = 8` `:24`、`DEFAULT_RESTOCK_GROUPS = 3` `:26`（全类仅此两个常量） | — |
| 字段 | `enabled` `:28`、`unloadGroups` `:31`、`restockGroups` `:33` | — |
| `configure(...)` | clear + addAll + 两 Map 赋值 | `:36-41` |
| 组数取值 | `unloadGroups(crop)` `:44-46`、`restockGroups(crop)` `:49-51` | — |
| 库存换算 | `safetyStock = restockGroups * 64` `:54-56`；`unloadStock = unloadGroups * 64` `:59-61` | — |
| `reserve(crop)` | `plantItem == harvestItem` → `Math.min(safetyStock, unloadStock)` `:70`；否则 `safetyStock` `:72` | `:68-73` |
| `countItem(Item)` | 非装备区累加 `:82-84` + 副手 `:85` | `:76-87` |
| `needsRestock(crop)` | `needsReplant() && countItem(plantItem) < reserve(crop)` | `:90-92` |
| `anyNeedsRestock()` / `firstNeedsRestock()` | `:95-100` / `:103-108` | — |
| 杂物判定 | `isJunkItem(item)`：`POISONOUS_POTATO` `:112`；任一启用作物 `junk() && item == harvestItem` `:113-115` | `:111-117` |
| 杂物计数 | `countJunk()` = 毒马铃薯 + 所有 `junk()` 作物收获物 | `:120-126` |
| 存入谓词 | `shouldDepositJunk` `:129-131`、`shouldDepositSeed` `:137-148`、`shouldDepositSingle` `:155-167`（先排毒马铃薯 `:157`、跳过 `junk()` 作物 `:159`）、`shouldDepositDual` `:174-187`（`POISONOUS_POTATO` 直接 false `:176`） | — |
| 卸入谓词 | `shouldUnloadSeed` `:193-204`、`shouldUnloadSingle` `:211-221`（`POISONOUS_POTATO` false `:213`）、`shouldUnloadDual` `:224-236`（`:226`） | — |
| 可用性汇总 | `hasDepositableSeed/Single/Dual` `:239-251`、私有 `hasDepositable(Predicate)` `:254-262` | — |
| 与容器关系 | 本类**不引用 `ContainerBroker`**；只产出 `Predicate<ItemStack>` 与计数，由 `FarmDecision` 塞进任务构造 | 全类 |

---

## 六、任务层 `autofarm\task\`（13 文件 / 1,436 行）

### 6.1 契约

| 项 | 事实 | 行号 |
|---|---|---|
| `FarmTask`（接口） | `TaskResult tick();` `:12`、`boolean exclusive();` `:15`、`void cancel();` `:18`；类注释状态机约定 `ACT → WAIT_FOR_UPDATE → VERIFY → RESULT` `:6-7` | — |
| `TaskResult`（枚举） | 17 值 `:12-60`：`IN_PROGRESS` `:12`、`SUCCESS` `:15`、`WAVE_DONE` `:18`、`TARGET_INVALID` `:21`、`HARVEST_FAILED` `:24`、`PLANT_FAILED` `:27`、`RESOURCE_INSUFFICIENT` `:30`、`CONTAINER_MISSING` `:33`、`CONTAINER_FULL` `:36`、`CONTAINER_EMPTY` `:39`、`CONTAINER_SYNC_FAILED` `:42`、`CONTAINER_OPEN_FAILED` `:45`、`NAVIGATION_FAILED` `:48`、`POISON_CONTAINER_FULL` `:51`、`DIMENSION_MISMATCH` `:54`、`FARM_AREA_INVALID` `:57`、`CANCELLED` `:60`；`done() = this != IN_PROGRESS` `:63-65`；`ok() = this == SUCCESS` `:68-70` | — |
| 零返回点枚举 | `CONTAINER_SYNC_FAILED`、`CONTAINER_OPEN_FAILED`、`DIMENSION_MISMATCH`、`FARM_AREA_INVALID`、`CANCELLED` 在 `task\` 内无任何 `return` | 全目录搜索 |

### 6.2 逐个任务

| 任务 | 要点（行号） |
|---|---|
| `ContainerTask`（抽象，102） | `OPEN_RETRY_INTERVAL = 10` `:21`；字段 `boxPos`/`broker`/`reachDistance`/`openRetry` `:23-27`；`tick()` **final** `:36`；流程：世界空 → `CONTAINER_MISSING` `:38`；非容器方块实体 → `CONTAINER_MISSING` `:41-43`；未到达 → `available()` 假则 `NAVIGATION_FAILED` `:47`，`!pathing()` 则 `goTo(boxPos, 1)` `:48`；到达后 `FarmNav.cancel()` `:51`；`openMenu() == null` → `tryOpen()` `:54-57`；`!isReady()` `:60`；`return transfer()` `:66`；抽象 `transfer()` `:70`；`exclusive() = true` `:73-75`；`cancel()` = `FarmNav.cancel() + closeContainer() + broker.reset()` `:78-82`；`arrived()` 切比雪夫 ≤1 `:84-93`；`tryOpen()` 每 10 tick 发 `interactBlock(MAIN_HAND, boxPos, Direction.UP)` `:95-101`；注释声明放弃 `isBoundContainer` 校验 `:62-65` |
| `UnloadTask`（53） | 字段 `bpt`/`depositFilter` `:17-18`；循环 ≤ bpt 次 `depositOne`，非 MOVED 即 break `:34-41`；`moved` → `IN_PROGRESS` `:44`；`NOT_READY` → `IN_PROGRESS` `:46`；否则关箱 + reset → `CHEST_FULL ? CONTAINER_FULL : SUCCESS` `:49-51` |
| `RestockTask`（78） | 字段 `resources`/`crop`/`syncCooldown` `:21-25`；`crop()` `:35-37`；冷却递减 `:44-47`；`countItem(plantItem) >= safetyStock` → 关箱 + `SUCCESS` `:50-54`；`withdrawOne(plantItem)` 成功 → `syncCooldown = 2` `:57-60`；箱内该物 0 → `CONTAINER_EMPTY` `:63-67`；其余 `IN_PROGRESS` `:69`；`countInChest` `:73-77` |
| `PoisonDumpTask`（51） | 结构同 `UnloadTask` `:17-25`；循环 `:33-40`；`moved`/`NOT_READY` → `IN_PROGRESS` `:42-43`；否则 `CHEST_FULL → POISON_CONTAINER_FULL`，其余 `SUCCESS` `:45-49`；注释说明杂物不进普通箱 `:12-13` |
| `HarvestTask`（114） | `WAIT_TICKS = 2` `:19`、`MAX_RETRIES = 2` `:21`；字段 `target`/`verifier`/`reachDistance`/`acted`/`waitTicks`/`retries`/`fortune`/`toolPrepared` `:23-34`；`target()` `:43-45`；流程：未动手时校验有效性 → `TARGET_INVALID` `:52`；未在范围内导航 `:56-58`；`FarmNav.cancel()` `:60`；`fortune.ensure()` `:63-66`；`FarmPacketOps.breakBlock(pos, Direction.UP)` `:70`；等 2 tick `:77-80`；`harvestSucceeded` → `SUCCESS` `:83`；重试 `:86-91`；超限 `HARVEST_FAILED` `:92`；`exclusive() = false` `:96-98`；`cancel()` = `FarmNav.cancel() + fortune.restore()` `:101-104`；`inReach()` 眼位到 `pos+0.5` `:106-113` |
| `BatchHarvestTask`（164） | `WAIT_TICKS = 2` `:24`、`MAX_RETRIES = 2` `:26`、`WAVE_SIZE = 32` `:28`；字段 `remaining`/`harvested`/`pending`/`acted`/`waitTicks`/`retries` `:30-44`；`drainHarvested()` 拷贝后清空 `:53-57`；流程：`acted` 分支等 2 tick 后 `flushPending()` `:65-71`；剔除失效目标 `:75`；空则 `pending.isEmpty() ? SUCCESS : flushPending()` `:77-79`；选最近并在范围内收集 `:82-92`；空则导航 `:95-97`；`fortune.ensure()` `:103-106`；**同 tick 连发最多 32 个破坏包**（`budget--<=0 break`）`:109-115`；`flushPending()` `:122-144`（成功入 `harvested`，失败回填 `remaining`，`retries < 2` 重试；空则 `SUCCESS`，否则 `WAVE_DONE` `:139`/`:143`） |
| `PlantTask`（133） | `WAIT_TICKS = 2` `:22`、`MAX_RETRIES = 2` `:23`；字段 `target`/`verifier`/`reachDistance`/`hand`/`acted` `:25-32`；流程：非可种 → `TARGET_INVALID` `:50`；导航 `:54-56`；`preparePlantingItem` 失败 → `RESOURCE_INSUFFICIENT` `:61-63`；`FarmPacketOps.useOnBlock(hand, target.pos())` `:68`；等 2 tick `:75-78`；`plantSucceeded` → `SUCCESS` `:81`；重试 `:83-88`；超限 `PLANT_FAILED` `:89`；播种顺序 副手 → 快捷栏 → 背包并 `move().toOffhand()` `:103-123`（`:108`/`:111-114`/`:117-120`），**固定副手播种** |
| `BatchPlantTask`（196） | `WAIT_TICKS = 2` `:26`、`MAX_RETRIES = 2` `:28`；`planted()` 返回内部引用（不拷贝不清空）`:51-53`；按**同 `Item` 引用相等**组批且距离达标 `:82-86`；`ensureOffhand(item)` `:134-164`（副手有 → true `:136-140`；快捷栏 → `move().toOffhand()` `:142-148`；背包 → 每 `prepTicks > 8` 重发 `:150-159`；无种子 → 返回 true 由验证自然失败 `:161-163`）；同 tick 连发播种 `:101`；`flushPending()` `:111-131`（失败重试 `:117-124`，空则 `SUCCESS`，否则 `IN_PROGRESS` `:130`） |
| `TillTask`（160） | `WAIT_TICKS = 2` `:22`、`MAX_RETRIES = 2` `:23`；流程：非有效 → `TARGET_INVALID` `:56`；导航 `:59-64`；`prepareHoe()` 未就绪 `:67-72`（`preparing` 时 `IN_PROGRESS`，否则 `RESOURCE_INSUFFICIENT`）；`FarmPacketOps.tillBlock(hand, target.pos())` `:77`；等 2 tick `:84-87`；`tillSucceeded` → `SUCCESS` `:90`；重试 `:92-97`；超限**复用 `PLANT_FAILED`** `:98`；`cancel()` = `FarmNav.cancel() + if (swapped) InvUtils.swapBack()` `:107-110`；`prepareHoe()` `:113-150`（主手是锄 → MAIN_HAND `:118-122`；快捷栏锄 → `InvUtils.swap(slot, true)` + `swapped` `:125-132`；背包锄 → `prepTicks > 8` 重发移动 `:135-145`；无锄 → null `:147-149`）；注释声明锄地必须主手 `:17` |
| `CollectTask`（166） | `EMPTY_GRACE = 20` `:19`、`MAX_TOTAL_TICKS = 400` `:21`、`STUCK_TICKS = 50` `:23`、`PICKUP_RADIUS_SQ = 2.25` `:25`、`DRY_SEARCH_RADIUS = 4` `:27`；字段 `ref`/`collectRange`/`waited`/`emptyTicks`/`stuckTicks`/`targetId` `:29-34`；流程：`waited++` `:43`；400 tick 兜底 → `SUCCESS` `:48-51`；最近掉落物为 null → 计数并在 20 tick 后 `SUCCESS` `:53-62`；距离 ≤ 2.25 → `stuckTicks++`，50 tick 后 `SUCCESS` `:69-76`；换目标重置卡顿 `:79-84`；不可用寻路不报错 `:91`；`navTarget` 为 null 则取消并计卡顿 `:94-102`；`!pathing()` 则 `goTo(nav, 1)` `:104`；`nearestItem` `:119-138`；`navTarget` 水中环形找干燥站位 `:144-160`；`isLiquid` `:163-165`；**全程不调用发包与验证器** |
| `FortuneTool`（127，非任务） | 字段 `swapped` `:20`、`preparing` `:23`、`prepTicks` `:25`；`ensure()` `:28-69`（主手有时运 → true `:33-37`；快捷栏最高时运 → `InvUtils.swap(hotbar, true)` `:40-47`；背包（含副手）→ 每 `prepTicks > 8` 发移动并返回 false `:50-63`；无时运 → true 裸手 `:65-68`）；`restore()` `:72-77`；`bestFortuneHotbarSlot()` `:80-93`；`findBestFortune()` `:96-111`；`fortuneLevel(stack)` 读 `DataComponents.ENCHANTMENTS` + `Registries.ENCHANTMENT` + `Enchantments.FORTUNE` `:114-126` |

---

## 七、模型层 `autofarm\model\`（7 文件 / 475 行）

### 7.1 `CropProfile`（213 行）

11 个常量（`displayName` / `Kind` / `block` / `plantItem` / `harvestItem` / `soil` / `extraLoot`）：

| 常量 | 行号 | 名称 | Kind | 方块 | 种植材料 | 收获物 | 底盘 | extraLoot |
|---|---|---|---|---|---|---|---|---|
| `WHEAT` | `:26` | 小麦 | CROP | `Blocks.WHEAT` | `WHEAT_SEEDS` | `WHEAT` | `FARMLAND` | 空 |
| `CARROT` | `:27` | 胡萝卜 | CROP | `Blocks.CARROTS` | `CARROT` | `CARROT` | `FARMLAND` | 空 |
| `POTATO` | `:29` | 马铃薯 | CROP | `Blocks.POTATOES` | `POTATO` | `POTATO` | `FARMLAND` | `POISONOUS_POTATO` |
| `BEETROOT` | `:30` | 甜菜根 | CROP | `Blocks.BEETROOTS` | `BEETROOT_SEEDS` | `BEETROOT` | `FARMLAND` | 空 |
| `NETHER_WART` | `:31` | 下界疣 | CROP | `Blocks.NETHER_WART` | `NETHER_WART` | `NETHER_WART` | `SOUL_SAND` | 空 |
| `BAMBOO` | `:34` | 竹子 | PILLAR | `Blocks.BAMBOO` | `null` | `BAMBOO` | `null` | 空 |
| `SUGAR_CANE` | `:35` | 甘蔗 | PILLAR | `Blocks.SUGAR_CANE` | `null` | `SUGAR_CANE` | `null` | 空 |
| `CACTUS` | `:36` | 仙人掌 | PILLAR | `Blocks.CACTUS` | `null` | `CACTUS` | `null` | 空 |
| `CACTUS_FLOWER` | `:39` | 仙人掌花 | FRUIT | `Blocks.CACTUS_FLOWER` | `null` | `CACTUS_FLOWER` | `null` | 空 |
| `PUMPKIN` | `:42` | 南瓜 | FRUIT | `Blocks.PUMPKIN` | `null` | `PUMPKIN` | `null` | 空 |
| `MELON` | `:43` | 西瓜 | FRUIT | `Blocks.MELON` | `null` | `MELON_SLICE` | `null` | 空 |

`Kind` 内嵌枚举 `:46-53`：`CROP` `:48`、`PILLAR` `:50`、`FRUIT` `:52`。
字段声明 `displayName` `:55`、`kind` `:56`、`block` `:58`、`plantItem` `:60`、`harvestItem` `:62`、`soil` `:64`、`extraLoot` `:66`（**无 `cn`/`seeds`/`drops`/`junk` 字段**）。
构造器 `:68-80`（6 参 / 7 参）。方法：`displayName` `:82-84`、`kind` `:86-88`、`block` `:90-92`、`plantItem` `:95-97`、`harvestItem` `:99-101`、`soil` `:103-105`、`extraLoot` `:108-110`、`junk() = this == CACTUS_FLOWER` `:113-115`、`toString()` `:119-121`、`needsReplant() = kind == CROP` `:124-126`、`retainItem() = needsReplant() ? plantItem : null` `:129-131`、`produceItems()` `:136-142`、**`isHarvestable(state, level, pos)` `:151-168`**（FRUIT → true `:156`；PILLAR → 下方同种或竹笋且再下方不同种 `:158-164`；CROP → `isMaxAge` `:166`）、**`isPlantable(level, soilPos)` `:173-177`**、**`isMaxAge(state)` `:180-189`**（`findAgeProperty` 找 `"age"` 的 `IntegerProperty` `:191-199`）、`byBlock(Block)` `:202-207`、`isPillarRoot(state) = state.is(Blocks.BAMBOO_SAPLING)` `:210-212`。

### 7.2 其余模型

| 类 | 事实 | 行号 |
|---|---|---|
| `FarmSite`（record） | `FarmSite(BlockPos pos, ResourceKey<Level> dimension)` `:19`；`UNBOUND = ""` `:22`；`serialize()` = `x + "," + y + "," + z + "," + dimension.identifier()` `:25-27`；javadoc 格式示例 `120,64,-88,minecraft:overworld` `:16-17`；`parse(String)` `:30-48`（null/blank → null `:31`；非 4 段 → null `:33-34`；整数解析 `:37-39`；`Identifier.tryParse` `:41-42`；`ResourceKey.create(Registries.DIMENSION, id)` `:44`；异常 → null `:45-47`）；`here(BlockPos)` `:51-55`；`inCurrentDimension()` `:58-62`；`describe()` `:65-67`；`describe(String)` = `"§7X§f"+x+" §7Y§f"+y+" §7Z§f"+z+" §8▸ "+dimColor+dimName+"§r"` `:69-72`（维度名走全限定 `WorldContextFormatter.dimensionSummary` `:70`） |
| `FarmTarget`（record） | `FarmTarget(TargetType, CropProfile, BlockPos)` `:10`；`TargetType` `:13-20`（`HARVEST` `:15`、`PLANT` `:17`、`TILL` `:19`）；工厂 `harvest` `:23-25`、`plant`（pos 为底盘）`:27-29`、`till`（profile 为 null）`:32-34` |
| `SiteType` | `START 农场点位1` `:16`、`END 农场点位2` `:17`、`SINGLE_STORAGE 单作物箱` `:18`、`MULTI_STORAGE 多作物箱` `:19`、`SEED_STORAGE 种子补货箱` `:20`、`POISON_STORAGE 杂物箱` `:21`；`cn` `:23`、`requiresContainer` `:24`（四箱 true）；`cn()` `:32-34`、`requiresContainer()` `:37-39`（**无 parse / 无序列化方法**） |
| `FarmState` | 8 值：`OBSERVE 观察` `:15`、`HARVEST 收割` `:18`、`PLANT 补种` `:21`、`TILL 锄地` `:24`、`COLLECT 拾取` `:27`、`UNLOAD 卸货` `:30`、`RESTOCK 补货` `:33`、`POISON_DUMP 杂物处理` `:36`；`cn` 字段 `:38`、构造 `:40-42`、`cn()` `:45-47`；类头声明 DECIDE / RESOURCE_CHECK 不单列状态 `:6-9` |
| `HarvestMode` | `SINGLE 单个收割` `:15`、`BATCH 批量收割` `:18`；`displayName` `:20`；`toString()` `:27-29` |
| `PlantMode` | `SEQUENTIAL 顺序优先` `:18`、`ROTATE 均匀轮转` `:21`、`FOLLOW_NEARBY 就近跟随` `:24`；`cn` `:26`；`toString()` `:33-35` |

---

## 八、观察 / 验证 / 自检

| 类 | 方法 | 判据 | 行号 |
|---|---|---|---|
| `FarmObserver`（35） | `worldReady()` | `player != null && level != null && connection != null` | `:13-16` |
| | `playerAlive()` | `player != null && !isDeadOrDying()` | `:19-22` |
| | `freeInventorySlots()` | 0..35 主背包区空格计数 | `:25-34` |
| `FarmVerifier`（73） | `harvestSucceeded(t)` | 空气 → true；非收录作物 → true；否则 `!profile.isHarvestable(...)` | `:22-32` |
| | `plantSucceeded(t)` | `pos.above()` 已是 `profile.block()` | `:37-43` |
| | `tillSucceeded(t)` | `pos` 已是 `Blocks.FARMLAND` | `:48-53` |
| | `targetStillValid(t)` | HARVEST → 仍 `isHarvestable`；TILL → `FarmScanner.isTillable`；PLANT → 仍 `isPlantable` | `:56-72` |
| `FarmSelfCheck`（103） | `MAX_CROPS = 3` | — | `:24` |
| | `check(enabledCrops, sites)` | 逐项文案（逐字）见 [50 号第五节](50-阶段13-附录B-界面与指令侦察记录.md) | `:33-91` |
| | `requireSite(...)` / `checkDimension(...)` | `label + "§f·未绑定"` `:94` / `label + "§f·不在当前维度"` `:100` | `:93-102` |

三个批量计划同构（`List<FarmTarget> targets` + `int cursor`；`hasNext()` / `next()`）：`BatchHarvestPlan` `:19-35`、`BatchPlantPlan` `:17-33`、`BatchTillPlan` `:16-32`。生成点：`FarmController` `:265`（收割 >1）、`:251`（补种 >1）、`:235`（锄地 `subList(1, size)`）；丢弃点 `:179`、`:195`、`:213`、`:415`、`:456`、`:526-528`。

---

## 九、水源模块 `water\WaterESPModule.java`（366 行）

事实已逐条列在 [48 号 2.10 节](48-阶段13-自动农场移植侦察与决策.md#L203-L224)（类声明/分类/注册点/常量/10 个设置项/扫描/过滤/灌溉框/建议点/事件），此处只补算法细节：

| 项 | 实现 | 行号 |
|---|---|---|
| 移动判定 | `lastCenter == null \|\| lastRadius != r \|\| 任一轴位移 >= 2` | `:240-244` |
| 新范围 | `X/Z ∈ [c-r, c+r]`、`Y ∈ [c-12, c]` | `:250-257` |
| 缓存剪裁 | `waterBlocks.removeIf(越界)`，**不清空** | `:260-265` |
| 游标钳制 | 移动时把游标 clamp 回新范围（保留进度） | `:276-278` |
| 区域标记 | `regionChanged = true`（触发建议点立即重算） | `:281` |
| 单片判定（内联） | `state.is(Blocks.WATER) && state.getFluidState().isSource() && isIsolatedSource(...)`，否则从缓存移除 | `:291-295` |
| 孤立判定 | 东/南/西/北四邻都不是静止水源 | `:310-315` |
| 建议点 | 四个候选 `offset(±9, 0, 0)/(0, 0, ±9)`；排除已在水缓存与已建议；要求上方 `isAir \|\| WATER \|\| canBeReplaced` | `:330-350` |
| 游标推进 | `x → z → y` 回卷 | `:355-365` |

---

## 十、节拍与线程事实（旧项目侧）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 线程 / Timer / 线程池 / sleep | `autofarm\` 与 `water\` **零命中** | 全目录搜索 |
| 唯一推进来源 | 主线程 `TickEvent.Post` | `AutoFarmMatrix.java:440-441`、`WaterESPModule.java:166` |
| 每 tick 固定工作量 | `FarmScanner` 512 格 `:31`；`WaterESPModule` 512 格 `:35`；一次决策链；一次任务 `tick()` | — |
| 一次性全量 | `scanner.fullScan()` 仅激活时一次（`AutoFarmMatrix.java:333`） | — |
| 低频重算 | 建议点 `SUGGEST_INTERVAL = 40`（`WaterESPModule.java:38`、`:172`） | — |
| 异步只出现在基类 | 自检失败时 `mc.execute(() -> toggle())`（`YiyiaddonModule.java:151`） | — |

---

## 十一、证据命令

```powershell
$root = 'd:\mcaddon\26.1.2\src\main\java\com\example\addon'

# 逐文件行数（本附录口径）
Get-ChildItem "$root\autofarm" -Recurse -Filter *.java -File | ForEach-Object {
  '{0,-58} {1,5}' -f $_.FullName.Replace("$root\",''), (Get-Content $_.FullName).Count }

# 死代码零调用（D4 依据）
#   关键词：FarmServerAdapter | DefaultFarmAdapter | VanillaFarmRecognizer
#   结论：仅 import / 声明命中，零 new、零调用

# FarmTask 接口方法（契约原文）
#   autofarm\task\FarmTask.java:12/15/18

# TaskResult 零返回点
#   范围：autofarm\task\  关键词：CONTAINER_SYNC_FAILED | CONTAINER_OPEN_FAILED | DIMENSION_MISMATCH | FARM_AREA_INVALID | CANCELLED
#   结论：无 return 命中
```
