《238 · 种子目标（本轮：钻石矿）→ AutoMiner 正式接入 · 目标接入回归结果》
装置：单人夹具世界（固定种子，与预测种子同一颗）+ 现有自动挖矿模块（未另造第二套）
测试世界：seedpoc-known-seed；种子 20260922；覆盖半径 3 区块
装置新鲜度：建世界前已删除上次实验存档并重建（全新 world / region / poi / entities）
指令通道：玩家发包入口（单人存档自带指令权限）—— give / spreadplayers / forceload / fill / setblock / tp
真值来源：集成服务端 ServerLevel#getBlockState（不读客户端镜像）

一、装置环境
  夹具世界：seedpoc-known-seed（种子 20260922，与「预测用的种子」同一颗 ⇒ 真值可比）
  覆盖半径：3 区块（出厂默认 3，本装置不改默认值）
  本轮追的矿物：钻石矿（DIAMOND；-Dyiyiaddon.seedpoc.target.ore=<矿物> 可换，默认钻石）
  复位：种子挖矿关闭 / 显示预测钻石关闭 / 种子原文清空 / 种子目标模式关闭
  落地点（回家点）：(-4,73,-6)
  装备到账：镐=1,剑=1,食物=8,钻石=0

二、A · 种子关闭时的原自动挖矿回归
  目标提供者：普通模式 / 扫描方式：视野内所有目标矿（应为「普通模式 / 视野内所有目标矿」）（通过）
  目标方块：minecraft:diamond_ore（应为钻石矿家族）
  类型扫描 mine 是否真的下发了：true（通过）
  种子运行时是否完全没被牵动：true（计算器运行 false / 缓存区块 0 / 运行时身份 未建立 / 覆盖已预测 0 / 排队 0）（通过）

三、B · 种子目标模式打开但闸门未开
  显示预测钻石：false（本装置全程关着 —— 预测仍应照跑，因为自动挖矿是第二个消费者）
  验证闸门 mayUseForAutomatedMining：false（应为 false）（通过）
  阻止启动的原因：种子验证未通过（当前：未验证） §8▸ 不启动种子自动挖矿（通过）
  模块是否被启动：false（应为 false）（通过）
  是否偷偷下发过挖掘目标：类型 mine 无 / 精确寻路无（通过）
  种子运行时是否在替验证攒证据（闸门未开也必须预测）：运行时身份 世界 singleplayer:seedpoc-known-seed / 种子 20260922 / 维度 minecraft:overworld / Minecraft 26.1.2 / 会话 #2 / 缓存区块 0 / 覆盖已预测 0/49（通过）
  验证读数：验证「未验证」：有效证据单元 0（曾经确认 0 个 / 覆盖 0 个目标区块 / 已观察 0 个 / 解释比例 —）；有效样本区块 0；当前已确认候选 0 / 缺失 0；证据 0 条，身份 世界 singleplayer:seedpoc-known-seed / 种子 20260922 / 维度 minecraft:overworld / Minecraft 26.1.2 / 会话 #2，锁存 未锁定
  …… 等待验证（0 秒）状态「未验证」缓存区块 0 覆盖已预测 0/49 正在预测 钻石(-18,0)
  …… 等待验证（10 秒）状态「收集中」缓存区块 1 覆盖已预测 1/49 正在预测 钻石(-19,0)

  验证通过：状态「已验证」，用时 10 秒；验证「已验证」：有效证据单元 8（曾经确认 8 个 / 覆盖 7 个目标区块 / 已观察 8 个 / 解释比例 100.00%）；有效样本区块 7；当前已确认候选 179 / 缺失 0；证据 179 条，身份 世界 singleplayer:seedpoc-known-seed / 种子 20260922 / 维度 minecraft:overworld / Minecraft 26.1.2 / 会话 #2，锁存 已锁定

四、C · 正确种子已验证后自动选择真实 Diamond Seed Target
  目标提供者：种子模式 / 扫描方式：种子预测坐标（精确到方块）（通过）
  锁定目标：(200,-4,-88)（玩家 (201,69,-96)，区块距离 0）
  该坐标属于正式预测集里的钻石：true（通过）
  落在当前覆盖方框内：true（通过）
  期间是否出现过「按矿物类型 mine」（不许偷偷 fallback）：false（通过）
  是否按精确坐标工作（自定义目标 / 已在交互距离内）：true（通过）

五、D · 目标未加载（远端 Seed 目标）
  锁定目标：(200,-4,-88)（玩家 (201,69,-96)，区块距离 0）
  把玩家挪到 (600,69,-488)（与目标水平相距 400 格，落脚点已挖成空气口袋 ⇒ 目标区块应在客户端卸载）
  目标区块是否已卸载：true（客户端 isLoaded=false，玩家 (565,62,-452)）（通过）
  提供者是否仍持有同一颗目标：true（当前 (200,-4,-88)）（通过）
  是否按精确坐标继续导航（自定义目标进程活跃）：true（类型 mine 出现过：false）（通过）
  目标 (200,-4,-88)：已请求强制加载其区块（否则读到的只是虚空）
  目标真值：minecraft:deepslate_diamond_ore（(200,-4,-88)）；已挖出 3×3×3 空气口袋并把这颗矿原样放回（保证秒破的方向与交互距离都是真实几何）
  服务端真值（目标格）：minecraft:air（通过）
  秒破通道是否被用过：true（通过）
  背包读数（服务端）：镐=1,剑=1,食物=8,钻石=0

六、L · 挖掉一颗后换下一颗
  已挖掉：(200,-4,-88)（真值 minecraft:air）→ 新目标：(201,-6,-87)
  锁定序列（本装置观测）：(200,-4,-88) → (201,-6,-87)

七、F · 加载后实际 MISSING（/setblock air）
  目标：(200,-10,-87)（区块已加载：true）→ 服务端已置为空气
  观察层对该格的结论：缺失（期望「当前缺失」）（通过）
  是否立刻换了下一颗：true（当前 (199,-11,-88)）（通过）
  被放弃的坐标是否被反复重选：1 次（通过）
  锁定序列：(200,-4,-88) → (201,-6,-87) → (200,-10,-87)

八、G · 手动 /setblock air 不得卡死
  目标：(199,-11,-88)
  模块是否仍在运行：true / 状态机：MINING（通过）
  是否换了下一颗：true（当前 (196,-14,-86)）（通过）
  被置空气的坐标是否被反复重选：1 次（通过）
  锁定序列：(200,-4,-88) → (201,-6,-87) → (200,-10,-87) → (199,-11,-88)
  装置在「同一坐标」上取消掉的寻路次数：0（提供者上限 3 次重发）（通过）
  是否换下一颗：true（当前 (196,-14,-86)）（通过）
  该坐标是否被反复重选：1 次（通过）
  锁定序列：(200,-4,-88) → (201,-6,-87) → (200,-10,-87) → (199,-11,-88)

十、H · 关闭「显示预测钻石」（ESP）后自动挖矿是否继续
  当前目标：(196,-14,-86)；缓存区块 122
  ESP 开关：false（应关）（通过）
  渲染快照是否已空（世界里不画框）：true（通过）
  预测缓存是否仍在（不许被 ESP 关掉）：true / 覆盖是否仍在工作：true（已预测 49/49，排队 0）（通过）
  自动挖矿是否照常（模块在跑 + 状态机在挖 + 手上仍有一颗预测目标）：true / MINING / (195,-22,-92)（通过）
    这一窗口里目标是否推进过：true（关 ESP 前 (196,-14,-86) ⇒ 关掉后 (195,-22,-92)；推进说明它仍在按预测坐标继续挖，不是停摆）

十一、I · 重新打开 ESP
  ESP 开关：true（通过）
  渲染快照是否已重建（世界里重新有框）：true（3097 条）（通过）
  当前挖矿是否被打断（模块在跑 + 状态机在挖 + 手上仍有一颗预测目标）：true / MINING / (195,-22,-92)（通过）

十二、J · 换维度（主世界 → 下界 → 主世界）
  切换前：运行时身份 世界 singleplayer:seedpoc-known-seed / 种子 20260922 / 维度 minecraft:overworld / Minecraft 26.1.2 / 会话 #2 / 当前目标 (195,-22,-92)
  下界：维度 下界 / 运行时身份 世界 singleplayer:seedpoc-known-seed / 种子 20260922 / 维度 minecraft:the_nether / Minecraft 26.1.2 / 会话 #3（通过）
  闸门 mayUseForAutomatedMining：false（应关）（通过）
  当前目标是否清空：true / 预测缓存里是否还有主世界条目：false（缓存 9 个区块，全部属于下界 （通过））
  模块是否被 fail-closed 停掉：true；停机原因：下界专属验证尚未建立（当前证据维度：minecraft:the_nether） §8▸ 不启动种子自动挖矿
    （口径：换维度 ⇒ 旧身份整批作废 ⇒ 验证随之清空 ⇒ 提供者报「未验证」并停机，绝不退回按矿物类型扫描；236 起下界也是受支持维度，因此预测 / 观察 / ESP 会以「下界身份」重新开始，而自动挖矿仍恒为 fail-closed）
  返回主世界：运行时身份 世界 singleplayer:seedpoc-known-seed / 种子 20260922 / 维度 minecraft:overworld / Minecraft 26.1.2 / 会话 #4（通过）
  是否把旧世界的目标带了过来：（无）（通过）
  闸门 mayUseForAutomatedMining：false（本装置不判它：验证锁存绑定运行时身份，换维度会把证据与锁存一起清空并从零重收 ——回到同一世界+同一种子后若又收满阈值，闸门重新打开正是 234 定案的既定语义）
  自动挖矿是否仍需要种子预测（需求源）：true（通过）

十三、判定汇总
【判定】A 种子关闭原自动挖矿回归：普通模式 （通过） / 类型扫描 mine 下发 （通过） / 种子运行时零牵动 （通过）
【判定】B 未验证不得开始：闸门关 （通过） / 自检拦住 （通过） / 未下发任何目标 （通过） / 运行时仍在预测 （通过）
【判定】闸门打开（已验证）：通过（用时 10 秒）
【判定】C 选定真实种子目标：种子模式 （通过） / 目标在预测集内 （通过） / 覆盖内 （通过） / 无类型扫描 （通过）
【判定】D 目标未加载仍能导航：区块已卸载 （通过） / 目标保持 （通过） / 精确坐标寻路 （通过） / 无类型扫描 （通过）
【判定】E 加载后实际钻石矿走现有挖矿链：目标被挖掉 （通过） / 秒破通道 （通过）
【判定】L 挖掉一颗换下一颗：通过（新目标 (201,-6,-87)）
【判定】F MISSING 立刻放弃换颗：观察为缺失 （通过） / 已换目标 （通过） / 未反复重选 （通过）
【判定】G 手动置空气不卡死：模块仍在跑 （通过） / 换下一颗 （通过） / 未重选同一点 （通过）
【判定】K 寻路失败不无限重试：重试有上界 （通过） / 已换目标 （通过） / 未重选同一点 （通过）
【判定】H 关 ESP 不影响：ESP 关 （通过） / 缓存保留 （通过） / 覆盖继续 （通过） / 挖矿继续 （通过）
【判定】I 重开 ESP 恢复：渲染层恢复 （通过） / 挖矿未受影响 （通过）
【判定】J 换维度：身份换成下界（主世界身份作废）（通过） / 闸门关闭 （通过） / 目标清空 （通过） / 无主世界残留条目 （通过） / fail-closed 停机 （通过）
【判定】J 返回主世界：身份重建 （通过） / 未带旧目标 （通过） / 需求源在位 （通过）
全部判定：通过
