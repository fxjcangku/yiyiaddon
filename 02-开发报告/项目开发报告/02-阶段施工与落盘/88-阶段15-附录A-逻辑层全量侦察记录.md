# 88 · 阶段15 · 附录A · 逻辑层全量侦察记录

> 本文是 [87-阶段15-自动图书管理员移植侦察与决策.md](87-阶段15-自动图书管理员移植侦察与决策.md) 的逐条证据册：**逐态行为、逐条判据、逐字文案、逐项契约**，全部来自旧项目源码只读扫描（`d:\mcaddon\26.1.2\src\main\java\com\example\addon\librarian`，52 文件 / 4,552 行全文细读）。
> 用途：批次2（逻辑层）与批次3（外围）施工时的逐字依据。**本文不写方案、不写建议**，只写旧项目事实。
> 行号均为本机实测；引用一律「文件:行号」。

**日期**：2026-09-18

---

## 0. 侦察范围与深度声明

| 项 | 内容 |
|---|---|
| 覆盖 | `librarian` 包全部 52 文件（根 1 / orchestrator 1 / fsm 3 / integration 7 / model 14 / service 20 / config 3 / recognition 2 / adapter 1） |
| 深度 | 逐个全文读取；状态机、编排器、交易与工位实现、全部 service 契约与 model 均逐行核对 |
| 不含 | 旧框架自身源码（`meteordevelopment.*`）只在需要判语义时读参考库中的 `EnchantmentListSetting` / `Keybind` 原始源码，其余不追 |
| 零调用验证 | 见 §7，命令与结果同录 |

---

## 1. 状态机逐态行为（`AutoLibrarianState` 27 态，编排器 handler 逐个）

> 表内「阈值」均为旧项目实测常量；`ticks` 一律指 `stateMachine.getTicksInCurrentState()`。

### 1.1 状态清单（取值与顺序照 `fsm/AutoLibrarianState.java:14-68`，中文名在 `:15-68`）

```
IDLE(空闲) · START(启动) · SEARCH_VILLAGER(搜索村民) · SELECT_TARGET_VILLAGER(选择目标村民) ·
MOVE_TO_VILLAGER(移动到村民) · FIND_LECTERN_POSITION(检测工位) · MOVE_TO_STAND_POSITION(移动到放置位) ·
BREAK_OBSTACLE(清除障碍方块) · PLACE_LECTERN(放置讲台) · WAIT_PROFESSION(等待成为图书管理员) ·
OPEN_TRADE(打开交易界面) · WAIT_TRADE_SCREEN(等待交易界面) · READ_TRADES(读取交易) ·
CHECK_ENCHANTMENT(检查附魔) · RESET(准备刷新) · BREAK_LECTERN(拆除讲台) ·
WAIT_UNEMPLOYED(等待村民失业) · SUCCESS_FOUND(找到目标附魔) · TRADE_PROCESS(处理交易) ·
SELECT_TRADE(选择交易) · WAIT_TRADE_SYNC(等待交易同步) · TAKE_TRADE_OUTPUT(领取物品) ·
VERIFY_PURCHASE(验证购买) · COMPLETE_TARGET(完成目标) · END_VILLAGER_CYCLE(结束村民周期) ·
FINISH(全部完成) · ERROR(错误)
```

> `IDLE` 不接受处理器（`AutoLibrarianStateMachine:46`）；`FINISH` 处理器为空实现（编排器 `:156-157`）。共 26 个有处理器的状态。

### 1.2 逐态行为

| 状态 | handler 行号 | 行为与判据（逐条） |
|---|---|---|
| `START` | `:161-171` | ① 目标全完成 → `FINISH`（原因串 `所有附魔目标均已完成`）；② `movementService.isAvailable()` 为假 → `fail("Baritone 不可用，无法启动自动移动")`；③ 否则 → `SEARCH_VILLAGER`（`开始搜索未绑定职业村民`） |
| `SEARCH_VILLAGER` | `:173-185` | 调 `findNearestUnemployedVillager(搜索半径)`；空结果时在 `ticks == 1` 或 `ticks % 200 == 0` 播 `info("附近没有失业村民，等待中...")` 并原地等待；有结果则写入上下文 → `SELECT_TARGET_VILLAGER`（`发现并保存当前目标村民`） |
| `SELECT_TARGET_VILLAGER` | `:187-194` | 目标失效或已有职业 → `END_VILLAGER_CYCLE`（`候选村民失效或已有职业`）；否则 → `MOVE_TO_VILLAGER`（`候选村民验证通过`） |
| `MOVE_TO_VILLAGER` | `:196-230` | 村民失效 → `stop()` + `END_VILLAGER_CYCLE`；`hasArrived()` → `stop()` + `FIND_LECTERN_POSITION`（`已到达村民附近`）；`ticks == 1` 时以**村民当前坐标**为 `GoalNear` 起点（目标半径 = 移动到达半径 3），启动结果不是 `STARTED/ALREADY_RUNNING` → `fail("Baritone 移动启动失败：" + 结果中文名)`；`ticks > 移动超时(1200)` → `fail("Baritone 移动超时")`；状态为 `FAILED/CANCELED/TIMED_OUT` → `fail("Baritone 移动失败：" + 中文名)`。注释明确：**移动阶段不做空间模型检测**，工位检测推迟到到达后 |
| `FIND_LECTERN_POSITION` | `:232-257` | 未建立工位时：`ticks > 60` → `fail("工位检测超时：村民相邻4格内始终没有岩浆块，请检查场地布置")`；否则每 tick 调 `detect()`，探测到即写入上下文（`StationValidationStatus.UNVALIDATED`）并播 `debug("[工位检测] 发现岩浆块，讲台位置: " + 位置)`。随后用 `validate(station)` 静态校验（不重新 detect，避免村民临时转身误判 `:645-655`）；无效 → `fail("[工位检测] 验证失败: " + 原因)`；有效 → 写入讲台位置 → `MOVE_TO_STAND_POSITION`（`固定讲台位置验证通过，移动到放置位`） |
| `MOVE_TO_STAND_POSITION` | `:259-296` | 村民失效 → `stop()` + `END_VILLAGER_CYCLE`；到达后 `hasObstacle` 为真 → `BREAK_OBSTACLE`（`讲台位有障碍，清除后放置`，并播 `info("讲台位有障碍方块，进入清除流程")`）；否则 → `PLACE_LECTERN`（`已到达讲台放置站位`）；`ticks == 1` 时以**玩家站位**为目标、半径 **1** 启动寻路（`debug("[移动到站位] 目标: " + 站位)`）；超时/失败判据同上（文案改「移动到讲台站位…」） |
| `BREAK_OBSTACLE` | `:298-320` | 村民失效 → `END_VILLAGER_CYCLE`；`ticks > 200` → `fail("清除障碍超时（200tick）")`；调 `breakObstacle()`，`SUCCESS` → 播 `info("已清除障碍方块，恢复运行")` + `PLACE_LECTERN`（`障碍已清除，开始放置讲台`）；`FAILED` → `fail("清除障碍失败: " + 原因)`；`WAITING/RETRY` → 下一 tick 继续。每 tick 播 `debug("清除障碍状态 tick=… 位置=…")` 与结果 debug |
| `PLACE_LECTERN` | `:322-359` | 先 `validateCurrentStation("放置讲台前")`；**`ticks < 15` 直接返回**（等玩家停稳，防移动惯性导致放置方向偏差，`:328-329` 注释）；放置前再查障碍，仍有 → 回 `BREAK_OBSTACLE`（`讲台位仍有障碍，重新清除`，播 `info("放置讲台前检测到障碍仍存在，重新清除")`）；`actionSubmitted` 阶段只轮询 `validatePlacement`，通过 → `WAIT_PROFESSION`（`讲台精准放置及朝向验证成功`），超 `职业等待超时` → `fail("讲台放置结果确认超时")`；未提交时等 `动作延迟` 后调 `place()`：`SUCCESS` 且 `validatePlacement` 通过 → `WAIT_PROFESSION`（同文案），`validatePlacement` 失败 → `fail("讲台放置验证失败：位置或阅读面朝向不正确。")`；`WAITING` → 置 `actionSubmitted`；`FAILED` → `fail("讲台放置失败: " + 原因)`；重试超时 → `fail("讲台放置重试超时: " + 原因)` |
| `WAIT_PROFESSION` | `:364-390` | 村民失效 → 清 `librarianDetectedTick` + `END_VILLAGER_CYCLE`；先 `validateCurrentStation("职业确认前")`；`isLibrarian()` 为真时：首次检测记 `librarianDetectedTick = ticks` 并播 `debug("[职业] 检测到图书管理员，等待交易数据初始化...")`，**再等 10 tick**（保证服务端生成交易列表 `:379`）后 → `OPEN_TRADE`（`村民已获得图书管理员职业，交易数据就绪`）；`ticks > 职业等待超时` → `RESET`（`等待图书管理员职业超时`） |
| `OPEN_TRADE` | `:392-410` | 先 `validateCurrentStation("交易前")`；已就绪 → `WAIT_TRADE_SCREEN`（`交易界面已打开`）；`ticks > 交易界面超时(100)` → `fail("交易界面打开超时")`；**前 3 tick 等待村民 AI 稳定，之后每 2 tick 重试一次**（`ticks < 3` 返回；`(ticks - 3) % 2 != 0` 返回）调 `open()`，`FAILED` → `fail("打开交易失败: " + 原因)` |
| `WAIT_TRADE_SCREEN` | `:412-418` | 就绪 → `READ_TRADES`（`交易界面已同步`）；超 `交易界面超时(100)` → `RESET`（`交易界面打开超时`） |
| `READ_TRADES` | `:420-452` | 先 `validateCurrentStation("读取交易前")`；`调试模式` 时逐条 `debug("[交易扫描] index=… 附魔=… 等级=…/… 绿宝石=… 书=… 可交易=…")`，无交易时 `debug("[交易扫描] 当前村民没有任何附魔书交易")`；取第一笔附魔书交易：空 → `RESET`（`首轮交易没有附魔书`）；否则播 `debug("[交易] 附魔=… 等级=… 绿宝石x… 书x…")`、写入上下文 → `CHECK_ENCHANTMENT`（`已读取第一本附魔书交易`） |
| `CHECK_ENCHANTMENT` | `:454-468` | 在**未完成目标**里找第一个 `matches(...)` 命中；无 → `RESET`（`附魔书不属于未完成目标`）；有 → 播 `info("§a✓ 命中目标附魔 §8▸ <名> Lv.<级> §8▸ 绿宝石x<价> §8▸ 书x<数>")`、写入命中目标 → `SUCCESS_FOUND`（`命中目标附魔，保留讲台、职业和交易`） |
| `RESET` | `:470-475` | `movementService.stop()` + `tradeService.close()` + `context.resetRefreshAttempt()` → `BREAK_LECTERN`（`本轮刷新数据已清理，继续使用当前村民`） |
| `BREAK_LECTERN` | `:477-507` | 村民失效 → `END_VILLAGER_CYCLE`；先 `tradeService.close()`；`actionSubmitted` 阶段每 tick 继续攻击，`validateRemoval` 通过 → `WAIT_UNEMPLOYED`（`讲台已拆除，等待村民解除职业`），超 `职业等待超时` → `fail("讲台拆除结果确认超时")`；未提交时等 `动作延迟` 调 `breakLectern()`：`SUCCESS` → `WAIT_UNEMPLOYED`（同文案）、`WAITING` → 置 `actionSubmitted`、`FAILED` → `fail("讲台拆除失败: " + 原因)`、重试超时 → `fail("讲台拆除重试超时: " + 原因)` |
| `WAIT_UNEMPLOYED` | `:509-524` | 村民失效 → `END_VILLAGER_CYCLE`；已失业 **且** `ticks > 刷新延迟(10)` → `context.completeLecternRemoval()` + `FIND_LECTERN_POSITION`（`当前村民已解除职业，重新寻找讲台位置`）；超 `职业等待超时` → `fail("等待村民解除职业超时")` |
| `SUCCESS_FOUND` | `:526-529` | `context.beginTradeProcess()`（校验上下文完整）→ `TRADE_PROCESS`（`目标交易状态已锁定，进入交易流程`） |
| `TRADE_PROCESS` | `:531-540` | `offer.soldOut() || !canAfford(offer) || !hasOutputCapacity()` → `fail("目标交易不可购买或库存空间不足")`；否则记录**购买前**目标附魔书数量 → `SELECT_TRADE`（`购买条件已确认，准备选择目标交易`） |
| `SELECT_TRADE` | `:542-565` | 先 `validateCurrentStation("选择交易前")`；`isSelectedTradeSynchronized(offer)` → `WAIT_TRADE_SYNC`（`目标交易已选择`）；`actionSubmitted` 阶段超 `交易同步超时(100)` → `fail("选择交易结果确认超时")`；否则等 `动作延迟` 调 `select(offer)`：`SUCCESS/WAITING` → 置 `actionSubmitted` 并转 `WAIT_TRADE_SYNC`（`已选择目标交易`）、`FAILED` → `fail("选择交易失败: " + 原因)`、重试超时 → `fail("选择交易重试超时: " + 原因)` |
| `WAIT_TRADE_SYNC` | `:567-573` | 已同步 → `TAKE_TRADE_OUTPUT`（`目标交易已同步`）；超 `交易同步超时(100)` → `fail("目标交易同步超时")` |
| `TAKE_TRADE_OUTPUT` | `:575-591` | 先 `validateCurrentStation("领取交易前")`；`actionSubmitted` 阶段超时 → `fail("领取交易输出结果确认超时")`；否则等 `动作延迟` 调 `takeOutput()`：`SUCCESS` → 置 `actionSubmitted` + `VERIFY_PURCHASE`（`已领取交易输出`）、`FAILED` → `fail("领取交易输出失败: " + 原因)`、等待超时 → `fail("领取交易输出等待超时: " + 原因)` |
| `VERIFY_PURCHASE` | `:593-604` | 当前数量 > 购买前数量 **且** `verifyCurrentPurchase(当前数量)` → `COMPLETE_TARGET`（`库存已确认新增目标附魔书`）；超 `交易同步超时(100)` → 补一次 `verifyCurrentPurchase(当前数量)` 后 `fail("库存未确认目标附魔书，购买验证失败")` |
| `COMPLETE_TARGET` | `:606-623` | `completeVerifiedCurrentTarget(找到后移除目标)` + `tradeService.close()`；播 `info("§a✓ 目标完成 §8▸ <名> Lv.<级>" + (移除 ? "（已移除）" : ""))`；全完成 → 播 `info("§a✓ 全部目标附魔已完成")` + `FINISH`（`全部目标附魔购买完成，保留讲台、职业和交易结果`）；否则播 `info("§7剩余目标 §8▸ <列表，逗号分隔>")`（空则 `无`）+ `END_VILLAGER_CYCLE`（`当前附魔目标已完成，结束当前村民周期`） |
| `END_VILLAGER_CYCLE` | `:625-630` | `movementService.stop()` + `tradeService.close()` + `context.clearVillagerCycle()` → `SEARCH_VILLAGER`（`当前村民周期已安全结束`） |
| `FINISH` | `:156-157` | 空实现（终态，保留讲台/职业/交易结果） |
| `ERROR` | `:632-635` | `movementService.stop()` + `tradeService.close()`（终态） |

### 1.3 编排器公共件

| 项 | 事实 | 行号 |
|---|---|---|
| `actionSubmitted` | 每 tick 动作是否已提交（防重复发包）；**任何 `transition` 都会重置为 false** | `:65-66`、`:671-675` |
| `actionDelayElapsed()` | `ticks > 动作延迟(2)` | `:637-639` |
| `actionTimedOut(t)` | `ticks > t` | `:641-643` |
| `fail(reason)` | 写入 `lastFailureReason` + `logger.error` + 转 `ERROR` | `:665-669` |
| `transition(state, reason)` | 先 `validateContextFor(state)` 再转 | `:671-675` |
| `validateContextFor` | ① `SEARCH_VILLAGER` 要求上下文已清干净；② 一长串状态要求 `requireVillager()`；③ `FINISH` 要求目标全完成；④ `RESET/BREAK_LECTERN` 要求存在待处理讲台；⑤ 交易类状态要求 `tradeProcessActive()` | `:677-703` |
| `onTransition` | 调试模式播 `logger.state(...)`；`debugSound` 为真时按状态映射 `DebugSoundEvent` 播放 | `:705-708`、`:715-728` |
| `onStateFailure` | 写失败原因 + `logger.error("状态执行异常: " + 消息)` | `:710-713` |
| `start/stop/tick` | `start`：清村民周期 + `stateMachine.start()`；`stop`：停移动 + 关交易 + 清周期 + `reset("模块停止")`；`tick`：透传 | `:96-103` |
| 状态→音效映射 | `START→START`；`SEARCH_VILLAGER/SELECT_TARGET_VILLAGER/FIND_LECTERN_POSITION→SEARCH`；两个 MOVE→`MOVE`；`PLACE_LECTERN/BREAK_OBSTACLE→PLACE`；`WAIT_PROFESSION/BREAK_LECTERN/WAIT_UNEMPLOYED→REFRESH`；交易组 9 态→`TRADE`；`COMPLETE_TARGET/FINISH→SUCCESS`；`ERROR→ERROR`；`IDLE/RESET/END_VILLAGER_CYCLE→RESET` | `:715-728` |

### 1.4 状态机转换表（`AutoLibrarianStateMachine:100-131` 逐条）

```
IDLE → START
START → SEARCH_VILLAGER | FINISH
SEARCH_VILLAGER → SELECT_TARGET_VILLAGER
SELECT_TARGET_VILLAGER → MOVE_TO_VILLAGER | END_VILLAGER_CYCLE
MOVE_TO_VILLAGER → FIND_LECTERN_POSITION | END_VILLAGER_CYCLE
FIND_LECTERN_POSITION → MOVE_TO_STAND_POSITION | END_VILLAGER_CYCLE
MOVE_TO_STAND_POSITION → BREAK_OBSTACLE | PLACE_LECTERN | END_VILLAGER_CYCLE
BREAK_OBSTACLE → PLACE_LECTERN | END_VILLAGER_CYCLE
PLACE_LECTERN → BREAK_OBSTACLE | WAIT_PROFESSION | END_VILLAGER_CYCLE
WAIT_PROFESSION → OPEN_TRADE | RESET | END_VILLAGER_CYCLE
OPEN_TRADE → WAIT_TRADE_SCREEN | RESET | END_VILLAGER_CYCLE
WAIT_TRADE_SCREEN → READ_TRADES | RESET | END_VILLAGER_CYCLE
READ_TRADES → CHECK_ENCHANTMENT | RESET | END_VILLAGER_CYCLE
CHECK_ENCHANTMENT → RESET | SUCCESS_FOUND | END_VILLAGER_CYCLE
RESET → BREAK_LECTERN | END_VILLAGER_CYCLE
BREAK_LECTERN → WAIT_UNEMPLOYED | END_VILLAGER_CYCLE
WAIT_UNEMPLOYED → FIND_LECTERN_POSITION | END_VILLAGER_CYCLE
SUCCESS_FOUND → TRADE_PROCESS | END_VILLAGER_CYCLE
TRADE_PROCESS → SELECT_TRADE | END_VILLAGER_CYCLE
SELECT_TRADE → WAIT_TRADE_SYNC | END_VILLAGER_CYCLE
WAIT_TRADE_SYNC → TAKE_TRADE_OUTPUT | END_VILLAGER_CYCLE
TAKE_TRADE_OUTPUT → VERIFY_PURCHASE | END_VILLAGER_CYCLE
VERIFY_PURCHASE → COMPLETE_TARGET | END_VILLAGER_CYCLE
COMPLETE_TARGET → END_VILLAGER_CYCLE | FINISH
END_VILLAGER_CYCLE → SEARCH_VILLAGER
（任意状态 → ERROR）
```

约束：重复进入当前态抛 `IllegalStateException`（`:60`）；非法转换抛 `IllegalStateException("非法状态转换: X -> Y")`（`:61-63`）；`ERROR` 的转换不做表校验（`:61` 条件里排除 `IDLE`，`ERROR` 在表内已默认可达）。

---

## 2. 服务契约全量（`service/` 20 文件）

| 文件 | 类型 | 方法 / 取值 | 语义要点 |
|---|---|---|---|
| `VillagerSearchService` | 接口 | `findNearestUnemployedVillager(radius)`、`isValid(target)`、`isUnemployed(target)`、`isLibrarian(target)` | 搜索 + 三重判定 |
| `VillagerStationService` | 接口 | `detect(target)`、`validate(station)`、默认 `calculateMarkerBlockPosition`（**零调用**） | 工位探测 + 校验 |
| `LecternPlacementService` | 接口 | `place`、`breakLectern`、`hasObstacle`、`breakObstacle`、`validatePlacement`、`validateRemoval` | 讲台全生命周期 |
| `TradeService` | 接口 | `open`、`isTradeScreenReady`、`readFirstEnchantedBookTrade`、默认 `scanTrades`（被 `FabricTradeService` 覆写）、`select`、`isSelectedTradeSynchronized`、`takeOutput`、`close` | 交易全流程 |
| `InventoryService` | 接口 | `canAfford`、`hasOutputCapacity`、`countMatchingBooks` | 背包三问 |
| `EnchantmentService` | 接口 | `matches`（唯一实装）；默认 `getMaximumTradeLevel`（抛 `UnsupportedOperationException`）、`isLibrarianTradeable`（恒 true）、`findMatch` —— **三个默认方法零调用** | 命中判定 |
| `MovementService` | 接口 | `gotoPosition`、`stop`、`isPathing`、`hasArrived`、`getStatus`、`isAvailable` | 移动六问 |
| `MarkerBlockAccess` | 接口 | `isMagmaBlock`、`isAir`、`isLecternFacing`、`canPlaceLectern` | 方块查询抽象（供校验器） |
| `MarkerBlockValidator` | 类 | `validate(station)` | ① 非岩浆块 → `固定交易位无效：未检测到岩浆块。`；② **`isLecternFacing` 命中与未命中都返回 `success()`**（`:21-25`，实际空转，见 §7） |
| `MarkerBlockValidation` | record | `valid` / `reason` + `success()` / `invalid(reason)` | 校验结论 |
| `ActionResult` | record | `status` / `reason` + `success()` / `waiting()` / `retry(reason)` / `failed(reason)` | 统一动作结果 |
| `ActionStatus` | 枚举（中文名） | `SUCCESS(成功)` / `WAITING(等待响应)` / `RETRY(需要重试)` / `FAILED(失败)` | 进调试播报 |
| `MovementStartResult` | 枚举（中文名） | `STARTED(已启动移动)` / `ALREADY_RUNNING(已有移动任务在运行)` / `UNAVAILABLE(移动服务不可用)` / `REJECTED(移动请求被拒绝)` / `FAILED(移动启动失败)` | 拼进失败播报 |
| `MovementStatus` | 枚举（中文名） | `IDLE(空闲)` / `STARTING(启动中)` / `PATHING(寻路中)` / `ARRIVED(已到达)` / `FAILED(移动失败)` / `CANCELED(移动已取消)` / `TIMED_OUT(移动超时)` / `UNAVAILABLE(移动服务不可用)` | 同上 |
| `DebugLoggerService` | 接口 | `state(state, context, movementStatus)` / `info` / `debug` / `error` | 播报四通道 |
| `DebugSoundService` | 接口 | `play(DebugSoundEvent)` | 音效出口 |
| `DebugSoundEvent` | 枚举 | `START / SEARCH / MOVE / PLACE / REFRESH / TRADE / SUCCESS / ERROR / RESET`（无中文名，内部用） | 状态→音效分组 |
| `AutoLibrarianServices` | record | 4 项：工位 / 讲台 / 附魔 / 交易 | **零消费者死件**：模块只在字段里持有一份，编排器收到的是逐个服务而非该记录；`getServices()` 零调用（2026-09-18 批次2 取证订正，初版误记为「传编排器」） |
| `LecternService` | 接口 | `findPlacement` / `place` / `breakLectern` / `isLecternPresent` | **无实现、零调用**（死件） |
| `NoOpMovementService` | 类 | 六个方法返回不可用 / 未到达 | **零使用**（死件） |

---

## 3. 实现层判据全集（`integration/` 7 文件）

### 3.1 `FabricVillagerSearchService`（99 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 搜索 | 玩家包围盒 `inflate(半径)` → `getEntitiesOfClass(Villager.class, box, usable)` → 过滤 `unemployed` → 取距玩家最近 | `:35-44` |
| `usable` | `isAlive() && !isRemoved()` | `:77-79` |
| `unemployed` | 职业键 == `VillagerProfession.NONE` | `:81-83` |
| 职业判定 | `villager.getVillagerData().profession().unwrapKey().map(k -> k.equals(key))`——**不能用 `==` 比 value**（`:85` 注释：26.1.2 里 `LIBRARIAN/NONE` 是 `ResourceKey`） | `:86-90` |
| 实体解析 | 先 `level.getEntity(entityId)` 且 UUID 相等；失败则用玩家 `inflate(64)` 扫 UUID 兜底（chunk 重载后 entity ID 会变） | `:61-75` |
| 快照 | `new VillagerTarget(uuid, entityId, new BlockPosition(x,y,z))`（**3 参构造**） | `:92-98` |

### 3.2 `FabricVillagerStationService`（173 行，实现 `VillagerStationService, MarkerBlockAccess`）

| 项 | 事实 | 行号 |
|---|---|---|
| 村民解析 | 同 3.1 的 entityId → UUID 兜底 | `:38-49` |
| 扫描层 | `yOffsets = {0, -1}`（岩浆块在地面时村民站在上方） | `:53-54` |
| 方向枚举 | `HorizontalDirection.values()` = NORTH / EAST / SOUTH / WEST（枚举定义顺序即扫描顺序） | `:10-18` |
| 几何 | `adjacent = 扫描点 + 方向`；`rawStand = adjacent + 方向`；**`standPos.y = villagerPos.y()`**（强制同层） | `:60-63` |
| 第一遍（优先） | 岩浆块 **且** 玩家站位可站立 **且** `canPlaceLectern(adjacent.up())`（讲台位已空闲，无需挖）→ 立即返回 | `:58-74` |
| 第二遍（fallback） | 只要求岩浆块 + 可站立；收集全部候选，**取讲台位到玩家最近的**（避免方向枚举顺序导致选中背后被埋的） | `:76-112` |
| `effectiveVillagerPos` | `dy == 0` 用原坐标；`dy == -1` 时 y 减 1 | `:67-68`、`:107-108` |
| `isPlayerStandable` | 脚 + 头两格都**不是完整碰撞形状方块**（空气 / 铁链 / 活板门均可） | `:161-168` |
| `canPlaceLectern` | `(空气 或 TrapDoorBlock) 且 下方是岩浆块`（活板门视为可放置位） | `:141-150` |
| `isLecternFacing` | 方块是 `LECTERN` 且 `LecternBlock.FACING` == 目标方向 | `:133-139` |

### 3.3 `FabricLecternPlacementService`（295 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 障碍判定 | 讲台位 `Y+1` 才能算障碍；空气或已是讲台 → 无障碍；**只查这一格**（`Y+2` 活板门卡位不动，`:36` 注释） | `:31-56` |
| 挖掘状态机 | `breakState`：0 = 未初始化 / 1 = 等待工具到位 / 2 = 正在挖；`breakWaitTick`；`activeBreakTarget`；`breaking` | `:43-46` |
| 选工具 | 先热键栏 0~8 找 `getDestroySpeed > 1f` 最快者（`> bestSpeed` 严格大于）；没有则在背包 9~35 找并 `InvUtils.move().from(best).toHotbar(0)`，进状态 1；都没有则选槽 0 | `:76-101`、`:217-237` |
| 状态 1 | `breakWaitTick >= 2` 后选槽 0 并进状态 2 | `:94-101` |
| 挖掘 | 方块状态变化则 `resetBreaking` 重来；`startDestroyBlock(pos, hitFace)` → `continueDestroyBlock(...)` 返回 false 时重新 start | `:103-116` |
| `hitFace` | 取 **`station.lecternFacing()`** 方向 | `:109` |
| 放置 | 已是讲台 → success；非空气 → `failed("讲台目标位置已被占用：" + 方块本地化名)`；下方非岩浆块 → `failed("讲台下方未检测到岩浆块，下方是：" + 名)`；找讲台（先热键栏后背包，背包则移到槽 0）→ `failed("背包中没有讲台。")` / `retry("讲台移至热键栏失败，下次重试。")`；记录原槽 → `InvUtils.swap` → **先 `applyFacing` 同步朝向给服务端** → `useItemOn(MAIN_HAND, 命中结果)` → 还原槽；`InteractionResult.FAIL` → `retry("讲台放置交互被拒绝。")`，否则 `waiting()` | `:119-158` |
| `applyFacing` | `SOUTH→0` / `WEST→90` / `NORTH→180` / `EAST→-90`，发 `ServerboundMovePlayerPacket.Rot` | `:252-268` |
| 拆讲台 | 空气 → success；非讲台 → `failed("固定讲台位置存在非讲台方块，拒绝拆除。")`；其余同清障状态机 | `:160-215` |
| 校验 | `validatePlacement` = 该位置是讲台；`validateRemoval` = 该位置是空气 | `:239-250` |
| 方块名 | `block.getName().getString()`（**不用 `Block.toString()`**，`:131-132` 注释） | `:283-286` |

### 3.4 `FabricTradeService`（192 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 打开 | 已就绪 → 直接 success；村民解析（entityId → UUID 兜底）失败 → `failed("当前村民实体不存在（entity ID 和 UUID 均未找到）。")`；`lookAt(眼位)` → `gameMode.interact(..., EntityHitResult, MAIN_HAND)` → `waiting()` | `:42-68` |
| `lookAt` | 用 `atan2` 算 yaw/pitch，`setYRot/setXRot` 后发 `ServerboundMovePlayerPacket.Rot` | `:174-191` |
| 就绪 | `mc.player.containerMenu instanceof MerchantMenu` | `:70-73`、`:168-172` |
| 扫描 | 逐 index 建快照；**非附魔书或无 `STORED_ENCHANTMENTS` 直接跳过** | `:81-92`、`:129-133` |
| 快照字段 | `tradeIndex`、`synchronizationId = Integer.toString(containerId)`、输出 `minecraft:enchanted_book`、`output.getCount()`、附魔 ID（`holder.unwrapKey().identifier().toString()`）、等级、该附魔 `getMaxLevel()`、`getBaseCostA().getCount()`（绿宝石数）、第二成本为空 → `null`/0 否则物品 ID 与数量、`!offer.isOutOfStock()`、`invalid = false`；**词条循环内 `return`，只取第一项** | `:134-157` |
| 选中 | `sameOffer` 失败 → `failed("交易报价身份已变化。")`；成功发 `ServerboundSelectTradePacket(tradeIndex)` 并记 `selectedIndex` | `:94-103` |
| `sameOffer` | `containerId` 字符串相等 **且** 重新快照与传入快照 `equals` | `:160-166` |
| 同步 | `selectedIndex == index && sameOffer && 结果槽(2) 非空` | `:105-109` |
| 取出 | 界面或玩家不可用 → `retry("交易界面不可用。")`；槽 2 空 → `waiting()`；否则 `handleContainerInput(containerId, 2, 0, ContainerInput.QUICK_MOVE, player)` → success | `:111-119` |
| 关闭 | 清 `selectedIndex`；仅 `ownedByPlugin && 界面在` 才 `player.closeContainer()`；`ownedByPlugin = false` | `:121-127` |

### 3.5 `FabricInventoryService`（75 行）

| 项 | 事实 | 行号 |
|---|---|---|
| `canAfford` | 绿宝石数 ≥ 报价绿宝石成本 **且**（第二成本为空 **或** 第二成本不是 `minecraft:book` **或** 书数 ≥ 第二成本数） | `:23-29` |
| `hasOutputCapacity` | 0~35 格存在空格即真 | `:31-37` |
| `countMatchingBooks` | 附魔书 + `STORED_ENCHANTMENTS` 中存在 `id 相等 && 等级相等` 的条目 → 累加 `getCount()` | `:39-62` |
| `count(item)` | 0~35 格累加同类物品数量 | `:64-74` |

### 3.6 `FabricEnchantmentService`（25 行）

`matches` 六连判据（`:16-24`）：`offer.tradable()` + `!offer.invalid()` + 输出为 `minecraft:enchanted_book` + 附魔 ID 相等 + 附魔等级 == 目标等级 + **该附魔最高等级 == 目标等级** + 绿宝石成本 ≤ 上限。

### 3.7 `FabricBaritoneMovementService`（140 行）

| 项 | 事实 | 行号 |
|---|---|---|
| 宽限期 | `MOVEMENT_GRACE_TICKS = 30`（≈1.5 s，Baritone 算路径期间不判失败） | `:22-23`、`:102-106` |
| 启动 | 参数非法 → `REJECTED`；玩家/世界空 → `UNAVAILABLE` + `error("Baritone 移动启动失败：游戏世界尚未就绪。")`；主实例空 → `UNAVAILABLE` + `error("Baritone 移动启动失败：当前没有可用的 Baritone 实例。")`；目标与当前相同且仍在跑 → `ALREADY_RUNNING` | `:36-55` |
| 关键设置 | **`BaritoneAPI.getSettings().allowBreak.value = false`**（只走已有通路，禁止挖方块寻路） | `:61-62` |
| 目标 | `setGoalAndPath(new GoalNear(目标, 半径))` | `:63` |
| 到达 | 玩家 `blockPosition().distSqr(target) <= 半径²` → `ARRIVED` | `:77-84` |
| 失败降级 | `LinkageError` → `UNAVAILABLE`（文案含「LinkageError，Baritone API 未正确加载」）；`RuntimeException` → `FAILED`（文案含异常类简名） | `:66-74` |
| 停止 | `pathingBehavior().cancelEverything()` 后清目标、置 `CANCELED`；异常静默 | `:111-124` |
| 可用 | 主实例非空（`LinkageError/RuntimeException` 视为不可用） | `:126-135` |

---

## 4. 数据模型（`model/` 14 文件）

| 文件 | 关键约束（构造期校验，逐条） |
|---|---|
| `AutoLibrarianContext` | 三个内部生命周期（全局任务 / 村民周期 / 刷新尝试）；`beginTradeProcess` 要求村民 + 讲台 + 报价 + 命中目标四者齐备；`setVillagerStation` 要求 `station.villagerUuid == 当前村民`；`completeVerifiedCurrentTarget` 要求验证状态为 `SUCCEEDED`；交易验证期间禁止覆盖报价 / 目标；库存采样序号单调递增（`:176-178`） |
| `VillagerTarget` | `entityId >= -1`；未解析 ⇔ `entityId == -1` 双向强校验；讲台位置与讲台状态互锁（`LOCATED/PLACING/PLACED/REMOVING` 必须有位置；`UNLOCATED` 必须无位置）；绑定交易位时要求 UUID、朝向、讲台位置三者一致（`:37-67`）。富状态 API（`withPosition` / `withResolvedEntity` / `asUnresolved` / `withStatus` / `withLectern` / `withStation` / `withTradeStatus`）**全部零调用** |
| `VillagerStation` | 四项几何强校验：岩浆块 = 村民 + 朝向；讲台 = 岩浆块上；玩家站位 = 岩浆块 + 同向；阅读面 = 村民朝向反向（`:40-52`）；`create(...)` 由村民坐标与朝向推导其余坐标（`:54-72`） |
| `TargetProgress` | 构造时至少一个目标（`至少需要一个目标附魔`）；`complete(target, remove)` 支持「标记完成」或「直接移除」；`completedCount` = 已移除数 + 已标记完成数（`:69-72`）；按 ID + 等级查索引，找不到抛 `目标不在配置列表中` |
| `EnchantmentTarget` | 附魔 ID 与图标 ID 必须匹配 `[a-z0-9_.-]+:[a-z0-9_./-]+`；`level >= 1`；锁定报价必须与目标 ID + 等级一致；`resolved(...)` 工厂 = 要求最高等级 + 中文显示名 + 图标 |
| `TradeOfferSnapshot` | `tradeIndex >= 0`；`synchronizationId` 非空；三个 ID 归一化校验；`outputCount >= 1`；`maximumEnchantmentLevel >= enchantmentLevel`；第二成本与数量互锁（无 ID → 数量必须 0；有 ID → 数量 ≥ 1）；**`invalid && tradable` 非法**；另有「legacy 7 参构造」把 `bookCost` 归一为第二成本（`:64-88`） |
| `TradeVerificationContext` | 三段枚举（`RequestStatus` / `SynchronizationStatus` / `VerificationStatus`）严格串行：购买前快照只能记一次、`SUBMITTED → ACCEPTED` 单向、开始等待需「已接受 + 有快照」、记录同步快照需处于等待中、`verifyPurchase` 要求两端快照齐全且同步完成；`markRequestFailed` 在已接受后禁止（`:75-150`） |
| `InventorySnapshot` | 附魔 ID 格式校验、等级 ≥ 1、数量 ≥ 0、采样号 ≥ 0；`hasIncreaseFrom` 要求同一目标且数量更大（`:52-55`） |
| `BlockPosition` | `offset(direction)` / `down()` / `up()`（纯值对象，不依赖 MC） |
| `HorizontalDirection` | NORTH(0,-1) / EAST(1,0) / SOUTH(0,1) / WEST(-1,0)；`opposite()` = 序号 +2 取模 |
| `LecternStatus` | `UNLOCATED / LOCATED / PLACING / PLACED / REMOVING / REMOVED`（无中文名，**零调用**） |
| `VillagerTargetStatus` | `SELECTED / AVAILABLE / UNRESOLVED / UNUSABLE`（**零外部调用**） |
| `VillagerTradeStatus` | `NOT_OPENED / OPENING / READY / SCANNED / MATCHED / PROCESSING / VERIFIED`（**零外部调用**） |
| `StationValidationStatus` | `UNVALIDATED / VALID / INVALID`（仅 `UNVALIDATED` 被使用） |

---

## 5. 模块层资产逐字（`AutoLibrarianModule.java`）

### 5.1 模块名与描述（`:71-72`）

```
自动图书管理员
自动寻路失业村民、放置讲台刷新交易、命中目标附魔自动购买，未命中自动拆台循环。详细参考下面使用说明。
```

### 5.2 启动报告（`reportStartupInfo():226-251`，逐字）

```
§a§l✓ 自动图书管理员 · 启动报告
§7目标附魔　§8▸ <前 3 个：名 Lv.级，§f、 连接；超过 3 个追加 §f 等 §f<数量>§f 种>§r
§7搜索半径　§8▸ <数值> 格§r
§7最高价格　§8▸ <数值> 绿宝石§r
§7命中策略　§8▸ 找到后移除 / 找到后保留§r
```

要点：标签与值之间是**全角空格 + `§8▸ `**（`目标附魔　§8▸`）；数值与关键词走高亮函数（`highlightText` / `highlightNumber` / `highlightFunction`）；整块经 `notify(...)` 一次带模块前缀发出。

### 5.3 自检缺项（`selfCheck():183-209`，逐字）

```
§d目标附魔§f·未勾选
§bBaritone§f·未安装或未启用
§a讲台§f·背包没有
§a书§f·背包没有
§a绿宝石§f·背包没有
```

背包统计口径：槽 0~35，按 `Items.LECTERN` / `Items.BOOK` / `Items.EMERALD` 累计 `getCount()`，为 0 才记缺项（`:196-206`）。Baritone 判定：`BaritoneAPI.getProvider().getPrimaryBaritone() != null`，任何异常视为不可用（`:211-218`）。

### 5.4 状态播报与三通道（`ModuleLogger:337-409`，逐字）

```
SEARCH_VILLAGER         §7正在搜索失业村民...
FIND_LECTERN_POSITION   §7正在检测工位...
BREAK_OBSTACLE          §7正在清除障碍方块...
PLACE_LECTERN           §7正在放置讲台...
WAIT_PROFESSION         §7等待村民成为图书管理员...
BREAK_LECTERN           §7正在拆除讲台...
RESET                   §6⚠ 未命中目标 §8▸ 准备刷新交易
```

- 状态播报只在以上 7 态触发（`switch` 默认 `null` 不播）；`message.equals(lastNotifiedState)` 去重（`:366-368`）。
- `info`：受 `聊天反馈` 控制，格式 `§f§l` + 消息（`:371-374`）。
- `debug`：受 `调试模式` 控制，格式 `§7[调试] §f` + 消息（`:376-379`）。
- `error`：格式 `§c✗ ` + 消息；**同文案 2000 ms 节流**（`ERROR_THROTTLE_MILLIS = 2000L`，`:338`、`:381-388`）。
- `state(...)`：调试模式下播 `§7[调试] 状态 <中文名> 移动 <中文名>`（`:344-350`）。
- 出口：`mc.player.sendSystemMessage(Component.literal(YiyiaddonModule.formatMessage("自动图书管理员", clean(message))))`（`:390-395`）。
- `clean(...)`：`message.replaceAll("[\\p{Punct}，。！？：；、（）【】「」《》“”‘’…]", "")`（`:397-399`）。
- 状态 / 移动中文名：`state.displayName()` / `status.displayName()`，空则 `未初始化`（`:401-408`）。

### 5.5 使用说明 5 章（`buildHelpContent():89-118`，逐字含 `§` 与树形符号）

```
准备
  §8├─ §f背包携带：讲台 × N、书 × N、绿宝石 × 足够数量
  §8├─ §f在「目标附魔」中设置想要的附魔类型
  §8├─ §f站在已搭建好的岩浆块工位阵列附近
  §8└─ §f开启模块即自动运行
自动流程
  §a[1] §f在搜索半径内寻找失业村民
  §a[2] §f识别岩浆块工位，清除障碍方块
  §a[3] §f放置讲台，等待村民接受图书管理员职业
  §a[4] §f静默读取交易列表（不打开界面），命中目标附魔则自动发包购买
  §a[5] §f未命中则拆除讲台，刷新村民交易，重复循环
静默交易
  §7▸ §f交易全程不打开交易界面，发包静默完成
  §7▸ §f命中目标附魔后自动选中并领取附魔书
场地要求
  §6▸ §f每个村民工位：岩浆块 + 相邻空地（用于放置讲台）
  §6▸ §f支持活版门卡位场地（村民无法逃跑）
  §6▸ §f安装 Baritone 可自动寻路移动到村民位置
注意
  §c⚠ §f背包缺少讲台/书/绿宝石时模块会报错并自动关闭
  §c⚠ §f绿宝石价格超过「最大价格」上限的交易会跳过
```

说明窗入口：`§e查看使用说明` 按钮（`:81`），窗口标题由旧 `HelpScreen` 生成（`模块名 + " - 使用说明"`）。

### 5.6 其他模块层事实

| 项 | 事实 | 行号 |
|---|---|---|
| 音效播放 | **22 项**枚举 → `SoundEvents` 映射逐条（`BELL→BELL_BLOCK`、`ATTACK_CRIT→PLAYER_ATTACK_CRIT`、`CAT→CAT_AMBIENT_BABY.value()`、`THUNDER→LIGHTNING_BOLT_THUNDER`、`EXPERIENCE_ORB→EXPERIENCE_ORB_PICKUP`、`CHALLENGE_COMPLETE→UI_TOAST_CHALLENGE_COMPLETE`、`PLAYER_LEVELUP→PLAYER_LEVELUP`、`NOTE_PLING→NOTE_BLOCK_PLING.value()`、`CHEST_OPEN→CHEST_OPEN`、`FIREWORK_BLAST→FIREWORK_ROCKET_BLAST`、`VILLAGER_CELEBRATE→VILLAGER_CELEBRATE`、`ZOMBIE_VILLAGER_CURE→ZOMBIE_VILLAGER_CURE`、`GOAT_SCREAM→GOAT_SCREAMING_AMBIENT`、`GHAST_SCREAM→GHAST_SCREAM`、`ALLAY_AMBIENT→ALLAY_AMBIENT_WITHOUT_ITEM`、`ENCHANTMENT_TABLE→ENCHANTMENT_TABLE_USE`、`TRIDENT_THUNDER→TRIDENT_THUNDER.value()`、`PANDA_SNEEZE→PANDA_SNEEZE`、`WARDEN_ROAR→WARDEN_ROAR`、`DRAGON_GROWL→ENDER_DRAGON_GROWL`、`END_PORTAL→END_PORTAL_SPAWN`、`ELDER_GUARDIAN_CURSE→ELDER_GUARDIAN_CURSE`），音量 1.0 / 音高 1.0 | `:411-444` |
| 静默容器 | `isActive() && screen instanceof AbstractContainerScreen && !(InventoryScreen) && !(CreativeModeInventoryScreen)` → 取消显示；注释说明：交易界面数据仍由 `containerMenu` 同步，`SelectTrade`/取成品照常发包 | `:287-299` |
| 关闭 | `orchestrator.stop()`、字段清空、`paused = false`、播 `§c§l已关闭` | `:253-263` |
| 快捷键 | 按下 → 播 `§c§l已强制结束` → `toggle()` 关模块（**不是暂停**） | `:276-282` |
| 目标解析 | `createTarget`：注册表取 `getMaxLevel()` 作等级（取不到抛 `无法解析目标附魔: <id>`）；显示名取 `description().getString()`，回退到 ID 字符串；图标固定 `minecraft:enchanted_book` | `:317-335` |
| `chatFeedback` | 模块构造里置 `false`（基类字段），业务侧改读设置项 `聊天反馈` | `:73` |

---

## 6. 常量与阈值总表

| 常量 | 值 | 出处 |
|---|---|---|
| 移动到达半径 | 3 | `AutoLibrarianConfig.defaults():61` |
| 交易界面超时 | 100 tick | `defaults():65` |
| 交易同步超时 | 100 tick | `defaults():66` |
| 移动超时 | 1200 tick | `defaults():67` |
| 搜索半径（可配） | 32 | `AutoLibrarianSettings:64-70` |
| 最高绿宝石价格（可配） | 64 | `:72-78` |
| 职业等待超时（可配） | 200 tick | `:80-86` |
| 动作延迟（可配） | 2 tick | `:88-94` |
| 刷新延迟（可配） | 10 tick | `:96-102` |
| 搜索空转提示间隔 | 200 tick | 编排器 `:178` |
| 工位探测超时 | 60 tick | 编排器 `:237` |
| 清障超时 | 200 tick | 编排器 `:307` |
| 放置前停稳等待 | 15 tick | 编排器 `:329` |
| 职业确认后等交易数据 | 10 tick | 编排器 `:380-383` |
| 打开交易前等待 | 3 tick | 编排器 `:403-404` |
| 打开交易重试间隔 | 2 tick | 编排器 `:405` |
| 移动宽限期 | 30 tick（≈1.5 s） | `FabricBaritoneMovementService:22-23` |
| 挖掘工具就位等待 | 2 tick | `FabricLecternPlacementService:96` |
| 错误节流 | 2000 ms | `AutoLibrarianModule:338` |
| 村民兜底扫描半径 | 64 格 | `FabricVillagerSearchService:71`、`FabricVillagerStationService:44`、`FabricTradeService:56` |
| 背包槽范围 | 0~35 | `FabricInventoryService`、自检 |
| 热键栏槽范围 | 0~8 | `FabricLecternPlacementService:221` |
| 工具速度阈值 | `> 1f` | `:220`、`:231` |

---

## 7. 死代码与死枝（零调用实测）

命令（范围 `d:\mcaddon\26.1.2\src\main\java`）：

```
关键词：LibrarianAdapter|LibrarianTradeRecognizer|LibrarianTradeResult|LecternService|NoOpMovementService|
        softReset|getServices\(\)|setServices\(|calculateMarkerBlockPosition
结果：仅声明处自身命中，无任何外部调用点
关键词：restart\(|paused   （范围限 librarian）
结果：restart 仅 AutoLibrarianOrchestrator:119 声明；paused 仅 AutoLibrarianModule:68/259/267/283
关键词：VillagerTargetStatus|LecternStatus\.|VillagerTradeStatus\.
结果：命中全部落在 VillagerTarget 自身的构造与校验内，服务层只用 3 参构造
```

| 对象 | 判定 |
|---|---|
| `adapter/LibrarianAdapter`（20） | 零调用；**第 43 条「全部 recognition/adapter 死代码层」明确不迁移** |
| `recognition/LibrarianTradeRecognizer`（19）+ `LibrarianTradeResult`（29） | 同上 |
| `service/LecternService`（21） | 无实现类、零调用 |
| `service/NoOpMovementService`（41） | 零使用（模块注入 Baritone 实现） |
| `VillagerStationService#calculateMarkerBlockPosition`（`:19-21`） | 默认方法零调用 |
| `EnchantmentService#getMaximumTradeLevel` / `isLibrarianTradeable` / `findMatch`（`:14-34`） | 三个默认方法零调用（`getMaximumTradeLevel` 直接抛 `UnsupportedOperationException`） |
| `MarkerBlockValidator#validate` 第二分支（`:21-25`） | `isLecternFacing` 命中与未命中**都返回 success** → 该判据实际空转（非崩溃） |
| `AutoLibrarianModule#setServices` / `getServices`（`:301-315`） | 零外部调用；`AutoLibrarianServices` 记录本身也只作装配容器 |
| `AutoLibrarianOrchestrator#softReset`（`:112-117`）/ `restart`（`:119-121`） | 零调用 |
| `AutoLibrarianModule.paused`（`:68/259/267/283`） | 恒 false → `getInfoString()` 的 `已暂停` 分支不可达；与 `pauseKeybind` 描述（「切换暂停/继续，保留进度」，`:138-142`）**冲突** |
| `VillagerTarget` 富状态 API（`:96-181`）+ `VillagerTargetStatus` / `LecternStatus` / `VillagerTradeStatus` | 服务层只用 `(uuid, entityId, position)`；其余字段恒为默认值 |

---

## 8. 存疑与待用户裁定（不在本文下结论）

| # | 事项 | 事实 | 关联决策 |
|---|---|---|---|
| 1 | 快捷键语义 | 实现「强制结束并关模块」vs 描述「切换暂停/继续」 | 87 号 D2 |
| 2 | 目标附魔候选集 | 旧为注册表**全部附魔**（Meteor `EnchantmentListSetting#getIdentifierSuggestions` = `registryAccess.lookup(ENCHANTMENT).keySet()`）；村民模块的图书管理员附魔池是 **35 项白名单** | 87 号 D3 |
| 3 | 交易协议同源 | 村民模块已有 SelectTrade + QUICK_MOVE 两步；本模块另有 `MerchantMenu` 读取与库存计数验证 | 87 号 D6 |
| 4 | 播报抹标点 | `clean()` 改写用户可见文本，属旧行为 | 87 号 D5 |
| 5 | 编排器体量 | 729 行 / 26 handler，未超第 38 条阈值 | 87 号 D7 |
| 6 | 音效只认 `SUCCESS` | 描述为「找到目标和完成目标时播放」，实际只在 `COMPLETE_TARGET`/`FINISH` 播放 | 87 号 D8（照搬即可，登记现状） |
