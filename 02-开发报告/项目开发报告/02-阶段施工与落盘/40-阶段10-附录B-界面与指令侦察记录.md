# 40 · 阶段10 · 附B · 自动挖矿（mining）界面与指令侦察记录

日期：2026-09-16

- 侦察对象：`d:\mcaddon\26.1.2\src\main\java\com\example\addon\` 下 `mining` 包的**界面层与指令层**，外加其依赖的 `ui/HelpScreen.java`、`core/YiyiaddonModule.java`（播报与自检口）、`core/CommandMessageFormatter.java`（回执排版）
- 侦察方式：**只读**，逐行照抄原文，未修改任何旧项目文件
- 路径前缀：`mining/` = `…\com\example\addon\mining\`；`ui/` = `…\com\example\addon\ui\`；`core/` = `…\com\example\addon\core\`
- 配套：逻辑层逐条见 [`39-阶段10-附录A-逻辑层侦察记录.md`](39-阶段10-附录A-逻辑层侦察记录.md)
- 本轮已拍板：好友过滤去掉、音效通知带上、`.wk 检测假矿` 随种子模式留白（故本文第 4 节对该子命令只作事实登记，不作施工依据）

> 用户 2026-09-16 拍板原话：「我的自动挖矿配置页面是跟自动挖矿有联动的」。因此本文第 8 节把「页面 ↔ 模块 ↔ 指令」的联动点逐条落到 `文件:行号`，作为同批交付的验收依据。

---

## 1. 配置页整体结构

### 1.1 页面构建顺序（代码顺序）

`mining/AutoMinerModule.java:1503-1542`（逐字骨架）：

```java
@Override
public WWidget getWidget(GuiTheme theme) {
    return buildInfoWidget(theme, table -> {
        WButton helpBtn = theme.button("§e查看使用说明");
        helpBtn.action = () -> mc.setScreen(new HelpScreen(theme, this, buildHelpContent()));
        table.add(helpBtn).expandX().minWidth(200);
        table.row();

        WButton checkFakeBtn = theme.button("检测假矿");
        checkFakeBtn.action = this::checkFakeOres;
        table.add(checkFakeBtn).expandX().minWidth(200);
        table.row();

        WTable cardRow = theme.table();
        buildLocationCard(theme, cardRow, "矿物箱", "mineral");
        buildLocationCard(theme, cardRow, "食物箱", "food");
        buildLocationCard(theme, cardRow, "挂机修复点", "afk");
        table.add(cardRow).expandX();
        table.row();
    });
}
```

即页面纵向顺序固定为：

```
[按钮] §e查看使用说明        （满宽 minWidth 200）
[按钮] 检测假矿              （满宽 minWidth 200）
[一行三列的卡片区] 矿物箱 | 食物箱 | 挂机修复点
——以下由旧框架按设置组顺序自动渲染——
分组1 目标选择（默认展开）→ 分组2 传送指令 → 分组3 触发条件 → 分组4 物品管理 → 分组5 Baritone调优
```

> 注意：旧项目**没有「无点位卡片」空态**、**没有分区标题文字**、**没有二次确认窗**；三张卡片之外，页面按钮只有 2 个。

### 1.2 三张点位卡片（`buildLocationCard` `:1553-1613`，逐字）

每个卡片是一个垂直 `WTable`，固定 **5 行**（标题 / 坐标 / 维度 / 设置按钮 / 删除按钮），未绑定时用占位符保持等高：

| 行 | 已绑定 | 未绑定 | 行号 |
| --- | --- | --- | --- |
| 标题 | `titleColor + title`（`mineral→§6`、`food→§2`、`afk→§d`、其它 `§f`），`expandX().center()` | 同左 | `:1562-1571` |
| 坐标 | `String.format("§7X§f%d §7Y§f%d §7Z§f%d", x, y, z)` | `§8暂未绑定` | `:1574-1585` |
| 维度 | `"§7" + data.dimensionName()` | `§8-`（占位） | `:1580-1587` |
| 设置按钮 | `§a设置`（已绑定亮绿） | `§8设置`（未绑定暗灰） | `:1591-1599` |
| 删除按钮 | `§c删除`（始终存在） | `§c删除`（点击静默 return） | `:1601-1609` |

按钮行为逐字（`:1593-1608`）：

```java
String setBtnColor = isBound ? "§a" : "§8";
WButton setBtn = theme.button(setBtnColor + "设置");
setBtn.action = () -> {
    WKCommand.setBinding(key);
    mc.setScreen(null);
};
card.add(setBtn).expandX().center();
card.row();

WButton delBtn = theme.button("§c删除");
delBtn.action = () -> {
    if (isBound) {
        WKCommand.removeBinding(key);
        mc.setScreen(null);
    }
};
```

> 复刻要点：`§c删除` 是**无条件显示**的，条件门控在**点击回调内**（`if (isBound)`），按钮本身没有 visible 条件。这一点与《基准-03》§五登记的「未绑定 → 静默 return」一致。

---

## 2. 全部设置项（5 组 / 55 项，逐条穷尽）

> 顺序 = `sgXxx.add(...)` 的**代码顺序**，即界面渲染顺序。类型列为旧框架设置类名（复刻时换成本项目控件，见第 9 节）。

### 2.1 分组名（逐字）

`mining/AutoMinerModule.java:99-103`：

```java
private final SettingGroup sgTarget = settings.createGroup("目标选择", true);
private final SettingGroup sgCommand = settings.createGroup("传送指令", false);
private final SettingGroup sgThreshold = settings.createGroup("触发条件", false);
private final SettingGroup sgItems = settings.createGroup("物品管理", false);
private final SettingGroup sgBaritone = settings.createGroup("Baritone调优", false);
```

### 2.2 目标选择（4 项）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 取值域 / 可见性 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `EnumSetting<LootMode>` | `:218-223` | `采集模式` | `精准采集：目标选择器显示原矿；时运：目标选择器显示掉落物（粗铁/粗金/粗铜等）。切换模式时自动同步目标` | `LootMode.FORTUNE` | 两枚举值；`onChanged(this::syncTargetsOnModeSwitch)` |
| 2 | `ItemSetting` | `:225-230` | `主世界矿石` | `时运模式选掉落物（粗铁/粗金/粗铜等），精准采集选原矿（铁矿石等）` | `Items.AIR` | `filter(this::isOverworldTargetItem)` |
| 3 | `ItemSetting` | `:232-237` | `下界矿石` | `时运模式选掉落物（下界残骸/金粒/石英），精准采集选原矿（下界残骸等）` | `Items.AIR` | `filter(this::isNetherTargetItem)` |
| 4 | `BlockSetting` | `:239-243` | `普通方块` | `选择普通方块（石头、泥土、原木等）` | `Blocks.AIR` | — |

枚举显示名（`LootMode.toString()`，`:72-86`）：`SILK_TOUCH = "精准采集"`、`FORTUNE = "时运"`。

三个目标的**互斥**不在控件层，而在自检与 `getTargetBlock()` 的取值优先级里（`39 号` 2.3 / 自检第 1-2 条）。

### 2.3 传送指令（9 项）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 取值域 / 可见性 |
| --- | --- | --- | --- | --- | --- | --- |
| 5 | `StringSetting` | `:249-253` | `前往挖矿指令` | `传送到挖矿区域的指令（支持带/或不带/）` | `""` | — |
| 6 | `BoolSetting` | `:255-259` | `RTP需要GUI选择` | `指令后自动扫描GUI点击匹配按钮` | `false` | — |
| 7 | `StringSetting` | `:261-266` | `GUI按钮关键词` | `输入纯文本（如'主世界'会匹配'§a主 §e世 §b界'），自动忽略颜色和空格` | `"主世界"` | `visible(rtpGuiEnabled::get)` |
| 8 | `StringSetting` | `:268-272` | `返回卸货指令` | `传送到卸货箱的指令` | `""` | — |
| 9 | `StringSetting` | `:274-278` | `前往补给指令` | `传送到食物箱的指令` | `""` | — |
| 10 | `StringSetting` | `:280-284` | `前往修复指令` | `传送到挂机修补点` | `""` | — |
| 11 | `StringSetting` | `:286-290` | `死亡返回指令` | `复活后返回挂机点` | `""` | — |
| 12 | `IntSetting` | `:292-299` | `传送等待时长` | `执行传送指令后等待秒数` | `8` | min 1 / max 120 / `noSlider` |
| 13 | `IntSetting` | `:301-308` | `RTP冷却时长` | `服务器 RTP 传送冷却秒数：传送失败后等这么久再重试，避免冷却期空发指令` | `60` | min 1 / max 3600 / `noSlider` |

### 2.4 触发条件（4 项）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 取值域 |
| --- | --- | --- | --- | --- | --- | --- |
| 14 | `IntSetting` | `:314-321` | `满载组数` | `背包矿物达到多少组时触发卸货` | `20` | min 1 / max 36 / `noSlider` |
| 15 | `IntSetting` | `:323-330` | `食物阈值` | `背包食物少于此数量时触发补给` | `32` | min 1 / max 64 / `noSlider` |
| 16 | `IntSetting` | `:332-339` | `耐久阈值` | `工具剩余耐久低于此值时前往挂机点修补（下界合金镐耐久 2031，上限已放宽）` | `100` | min 1 / max 3000 / `noSlider` |
| 17 | `BoolSetting` | `:341-345` | `潜影盒打包机` | `卸货时把矿物箱(潜影盒)填满，检测到满后等红石推盒换新盒，自动重开箱继续放，直到背包目标矿放完才RTP。给搭配潜影盒打包机的挂机用户使用。` | `false` | — |

### 2.5 物品管理（3 项）

| # | 类型 | 行号 | 设置名（逐字） | 描述（逐字） | 默认 | 过滤 / 回调 |
| --- | --- | --- | --- | --- | --- | --- |
| 18 | `ItemListSetting` | `:351-355` | `保留白名单` | `默认保留任意品质工具、白名单食物、目标矿物；此名单内的额外物品/方块也不会被丢弃` | 空列表 | — |
| 19 | `ItemListSetting` | `:357-368` | `食物白名单` | `从食物箱只拿选中的食物（只显示能吃的食物，默认常用食物，可自由增删）` | `COOKED_BEEF` / `COOKED_PORKCHOP` / `GOLDEN_CARROT` / `BREAD` | `filter(item -> item.getDefaultInstance().has(DataComponents.FOOD))`；`bypassFilterWhenSavingAndLoading()` |
| 20 | `BlockListSetting` | `:370-378` | `搭路方块白名单` | `Baritone搭桥/填坑时使用这些方块，且只保留各一组（多余自动丢弃）` | `COBBLESTONE` / `NETHERRACK` | `onChanged(blocks -> baritone.updatePlaceBlocks(blocks))` |

> `bypassFilterWhenSavingAndLoading()`（`:367`）是必须项：否则读档时白名单里的非食物物品会被 filter 剔除。

### 2.6 Baritone调优（35 项，按界面顺序）

**（a）种子挖矿组 5 项**（本轮留白范围，见 39 号第 11 节）

| # | 类型 | 行号 | 设置名 | 描述 | 默认 | 可见性 / 回调 |
| --- | --- | --- | --- | --- | --- | --- |
| 21 | `BoolSetting` | `:385-398` | `启用种子挖矿` | `实测扫描模式：只挖服务器已下发区块中真实存在的目标矿（逐块寻路+合法破坏），不依赖种子推算` | `false` | `onChanged`：开 → `updateOrePredictor()`；关 → `orePredictor.invalidateCache()`；两者都执行 `baritone.stop()` |
| 22 | `StringSetting` | `:400-405` | `世界种子` | `选填·仅记录用。客户端无法凭种子复刻服务器矿物分布，挖矿采用实测扫描，该字段不参与坐标计算` | `""` | `visible(seedMiningEnabled::get)` |
| 23 | `IntSetting` | `:407-415` | `渲染范围` | `渲染玩家周围多少格内的实测矿点（真实存在于服务器上的目标矿）` | `128` | min 32 / max 256 / `visible(seedMiningEnabled::get)` |
| 24 | `ColorSetting` | `:417-422` | `矿石渲染颜色` | `实测矿点方块框线的颜色` | `SettingColor(255, 215, 0, 180)` | 同上 |
| 25 | `BoolSetting` | `:424-429` | `脉冲效果` | `矿石框线启用呼吸灯效果` | `true` | 同上 |

**（b）秒破组 3 项**

| # | 类型 | 行号 | 设置名 | 描述 | 默认 | 取值域 / 回调 |
| --- | --- | --- | --- | --- | --- | --- |
| 26 | `BoolSetting` | `:433-437` | `快速破坏（秒破）` | `使用 START→服务端 0.7 最早阈值→STOP 的真实发包流程加速破坏；硬方块会等待服务端所需 tick，不提前制造客户端空气墙` | 旧 `true` → **落地 `false`**（用户拍板默认关） | 被 `mixin` 经 `getFastBreak()` 读取 |
| 27 | `BoolSetting` | `:439-443` | `绕过反作弊` | `兼容旧配置：STOP 后对相邻位置补发一次 ABORT；不能保证绕过服务器反作弊，异常时请关闭` | `false` | 被控制器经 `getBypassAnticheat()` 读取 |
| 28 | `IntSetting` | `:445-452` | `秒破间隔（tick）` | `服务端确认方块变化后，开始下一块前的最小等待 tick；不会用于提前重复发送 STOP` | `2` | min 0 / max 20 / 被 `getBreakInterval()` 读取 |

> **默认值与 37 号不一致，已由用户裁定**：旧源码为 `defaultValue(true)`（秒破默认开），用户硬约束要求「独立开关、默认关」→ 落地取 `false`，属有意改旧默认值，进阶段报告差异清单。（记录位置：39 号 §7.4 / §14 第 3 条。）

**（c）开关类 19 项**（全部 `onChanged → baritone.updateSetting(key, value)`，除 `logisticsBreakBlocks`、`lavaEsp`、`lavaEspRange`、`avoidLava` 外）

| # | 类型 | 行号 | 设置名 | 描述 | 默认 | Baritone setting key |
| --- | --- | --- | --- | --- | --- | --- |
| 29 | Bool | `:455-460` | `破坏阻挡方块` | `挖掘时允许破坏阻挡路径的方块（石头、泥土等）` | `true` | `allowBreak` |
| 30 | Bool | `:462-466` | `寻路物流破坏方块` | `前往矿物箱/食物箱/挂机点寻路时，是否允许破坏阻挡方块抄近路（关闭后旁边有路就绕行，不再挖墙）` | `false` | **不直写 Baritone**，由 FSM 在物流态压 `allowBreak`（`MinerFSM.java:228-230`） |
| 31 | Bool | `:468-473` | `放置方块` | `允许搭桥或填坑（需要背包里有方块）` | `true` | `allowPlace` |
| 32 | Bool | `:475-480` | `自动整理物品栏` | `允许Baritone自动将物品从背包移到快捷栏（工具、方块等）` | `true` | `allowInventory` |
| 33 | Bool | `:482-487` | `自动切换工具` | `挖掘时自动选择最佳工具（镐子挖石头、铲子挖土等）` | `true` | `autoTool` |
| 34 | Bool | `:489-494` | `避开岩浆` | `禁止 Baritone 将岩浆作为正常寻路路径` | `true` | `avoidLava`（映射到 `blocksToAvoid`） |
| 35 | Bool | `:496-500` | `岩浆透视` | `高亮显示附近岩浆方块，挖矿时更直观看到岩浆位置` | `true` | 模块自用（FSM 下界自动开启，`MinerFSM.java:237-240`） |
| 36 | Int | `:502-509` | `岩浆透视范围` | `透视岩浆的扫描半径（格）` | `8` | min 2 / max 16 / 模块自用 |
| 37 | Bool | `:511-516` | `怪物规避` | `提高怪物附近路径代价，尽量绕开危险区域` | `true` | `avoidance` |
| 38 | Bool | `:518-523` | `掉落方块暂停` | `遇到沙子、沙砾等掉落方块时暂停挖掘。关闭后不掉方块不暂停，挖矿更流畅（会塌方区域建议手动开启）` | `false` | `pauseMiningForFallingBlocks` |
| 39 | Bool | `:525-530` | `疾跑上坡` | `上坡时提前一格疾跑+跳跃，提升速度` | `true` | `sprintAscends` |
| 40 | Bool | `:532-537` | `允许跑酷` | `允许跨越1-4格的跑酷跳跃（有一定风险）` | `false` | `allowParkour` |
| 41 | Bool | `:539-544` | `跑酷搭桥` | `跑酷跳跃中途放置方块来延长距离（需开启放置方块）` | `false` | `allowParkourPlace` |
| 42 | Bool | `:546-551` | `对角线上升` | `允许斜向上跳跃，速度更快但消耗更多饥饿值` | `false` | `allowDiagonalAscend` |
| 43 | Bool | `:553-558` | `对角线下降` | `允许斜向下降，速度更快但有一定风险（地狱慎用）` | `false` | `allowDiagonalDescend` |
| 44 | Bool | `:560-565` | `仅挖暴露矿石` | `只挖掘能从指定距离看到的矿石，减少无效挖掘` | `false` | `allowOnlyExposedOres` |
| 45 | Bool | `:567-572` | `失败目标暂时跳过` | `矿点无法到达时跳过最近目标，避免反复卡住` | `true` | `blacklistClosestOnFailure` |
| 46 | Bool | `:574-579` | `合法挖掘模式` | `启用合法挖掘限制（关闭可提启效率但可能被检测）` | `false` | `legitMine` |
| 47 | Bool | `:581-586` | `合法挖掘检测对角矿石` | `合法挖掘时检测与已发现矿石对角相邻的矿石` | `false` | `legitMineIncludeDiagonals` |

**（d）数值类 8 项**（旧项目注释写作「滑块类设置」，实际全部 `.noSlider()`）

| # | 类型 | 行号 | 设置名 | 描述 | 默认 | 范围 / 可见性 |
| --- | --- | --- | --- | --- | --- | --- |
| 48 | Int | `:589-597` | `矿点刷新间隔` | `每隔多少tick重新扫描矿点（值越小越优先挖近矿；过小会导致寻路线乱闪、人物频繁停顿，40tick约2秒最稳定）` | `40` | 1~100 / `noSlider` → `mineGoalUpdateInterval` |
| 49 | Int | `:599-607` | `矿点缓存数量` | `Baritone一次缓存的最大矿点数量。太少会找不到矿（寻路失败），太多会路闪。64 缓存充足且稳定` | `64` | 1~256 / `noSlider` → `mineMaxOreLocationsCount` |
| 50 | Int | `:609-618` | `怪物规避半径` | `计算怪物危险区域的半径` | `8` | 1~16 / `noSlider` / `visible(mobAvoidance::get)` → `mobAvoidanceRadius` |
| 51 | Int | `:620-628` | `最大坠落高度` | `允许从多高的地方跳下（超过会绕路）` | `3` | 0~20 / `noSlider` → `maxFallHeightNoWater` |
| 52 | Int | `:630-639` | `暴露矿石检测距离` | `判断矿石是否暴露时使用的检测距离` | `1` | 1~8 / `noSlider` / `visible(allowOnlyExposedOres::get)` → `allowOnlyExposedOresDistance` |
| 53 | Int | `:641-649` | `最低挖掘高度` | `Baritone 挖矿时不会低于此高度` | `-64` | -64~320 / `noSlider` → `minYLevelWhileMining` |
| 54 | Int | `:651-659` | `最高挖掘高度` | `Baritone 挖矿时不会高于此高度` | `320` | -64~320 / `noSlider` → `maxYLevelWhileMining` |
| 55 | Int | `:661-670` | `合法挖掘高度` | `合法挖掘模式进行条带探索时使用的高度` | `12` | -64~320 / `noSlider` / `visible(legitMine::get)` → `legitMineYLevel` |

### 2.7 4 个隐藏设置（不出现在页面）

`_esp_scale_internal`（`2.0`，`:681-686`）、`_mineral_color_internal`（`255,215,0`，`:689-694`）、`_food_color_internal`（`100,255,100`，`:696-701`）、`_afk_color_internal`（`255,100,255`，`:703-708`），全部 `visible(() -> false)`，加在 `settings.getDefaultGroup()`。

> 复刻口径：这 4 项**必须存在**（ESP 缩放与三色是卡片/标签渲染的取值来源，`AutoMinerModule.java:1099-1111`、`:1127`、`:1140-1145`），但**不显示**。

---

## 3. 全部按钮与交互（穷尽）

### 3.1 页面按钮（共 2 个 + 卡片 6 个）

| 触发按钮（逐字） | 行号 | 点击后行为 | execute 延迟 |
| --- | --- | --- | --- |
| `§e查看使用说明` | `:1509-1512` | `mc.setScreen(new HelpScreen(theme, this, buildHelpContent()))` | 否 |
| `检测假矿` | `:1517-1520` | `this::checkFakeOres`（本轮留白） | 否 |
| `§a设置` / `§8设置` ×3 | `:1591-1598` | `WKCommand.setBinding(key); mc.setScreen(null);` | 否 |
| `§c删除` ×3 | `:1601-1609` | `if (isBound) { WKCommand.removeBinding(key); mc.setScreen(null); }` | 否 |

按钮统一样式由基类决定（`core/YiyiaddonModule.java:280-284`）：`expandX().minWidth(200)`，一行一个。

> 与《基准-03》§1.3 一致性：4 行按钮全部命中，**无多余、无缺失**；`检测假矿` 本轮留白（用户已拍板）。

### 3.2 无点位卡片空态

**没有空态文案**。未绑定时卡片用 `§8暂未绑定` + `§8-` 两行占位（`:1585-1588`），保持三卡等高。

---

## 4. `.wk` 指令（`mining/command/WKCommand.java` 773 行）

### 4.1 基本信息

| 项 | 逐字 / 值 | 行号 |
| --- | --- | --- |
| 指令名 | `wk` | `:82` |
| 描述（Brigadier description） | `挖矿坐标绑定（矿物箱、食物箱、挂机修复点）` | `:82` |
| 别名 | **无**（构造只传 name + description） | `:82` |
| 播报模块名常量 | `MODULE_NAME = "自动挖矿"` | `:52` |
| 根节点 | **有 executes**（裸 `.wk` 走 `showStatus()`） | `:88-91` |
| 注册点 | `core/AddonTemplate.java:215`（`Commands.add(new WKCommand());`） | — |

### 4.2 完整子命令树（5 顶层 / 10 叶子）

| 节点 | 类型 | 执行体 | 行号 |
| --- | --- | --- | --- |
| （根） | executes | `showStatus()` | `:88-91` |
| `状态` | literal | `showStatus()` | `:93-96` |
| `设置` | literal（无自身 executes） | — | `:99`、挂载 `:105` |
| ├ `矿物箱` | literal | `bindMineralChest()` | `:101`、`:131-170` |
| ├ `食物箱` | literal | `bindFoodChest()` | `:102`、`:176-215` |
| └ `挂机修复点` | literal | `bindAFKPoint()` | `:103`、`:222-266` |
| `移除` | literal（无自身 executes） | — | `:108`、挂载 `:114` |
| ├ `矿物箱` | literal | `unbind("mineral")` | `:110`、`:276-301` |
| ├ `食物箱` | literal | `unbind("food")` | `:111` |
| └ `挂机修复点` | literal | `unbind("afk")` | `:112` |
| `清空` | literal | `clearAll()` | `:117`、`:303-320` |
| `检测假矿` | literal | `checkFakeOres()`（委派模块，**本轮留白**） | `:120`、`:587-599` |

### 4.3 TAB 补全

- **无自定义 `SuggestionProvider`**（全为 literal 节点）；候选集合 = 「`状态` / `设置` / `移除` / `清空` / `检测假矿`」+ 「`矿物箱` / `食物箱` / `挂机修复点`」。
- 数据键 `"mineral"` / `"food"` / `"afk"` 仅内部 key，**不参与补全**。
- 与《基准-06》§0.4 第 2 条一致：旧项目 11 条指令全部无 `suggests(...)`；新项目补全属本项目能力（`ClientCommand.complete()`）。

### 4.4 回执全量（逐字，含排版入口）

排版统一走 `core/CommandMessageFormatter`：`of(模块名, 标题)` + `.world()` / `.dimension(id)` / `.coord(x,y,z)` / `.field(标签, 值)` / `.status(级别, 明细)` / `.send()`；`wkInfo` = `sendLine(模块名, 原文)`（`:480-482`），`wkError` = `sendLine(模块名, "§6" + 原文)`（`:487-489`）。

| 触发场景 | 逐字文案 | 行号 |
| --- | --- | --- |
| 绑定矿物箱-已绑定 | `§6矿物箱已绑定，请先删除旧绑定再重新设置`；随后 `§7提示：使用 §e.wk 移除 矿物箱 §7删除` | `:134-135` |
| 绑定矿物箱-未命中 | `§6准星未对准任何方块` | `:141` |
| 绑定矿物箱-非容器 | `§6目标方块不是容器（箱子/桶/潜影盒等）` | `:146` |
| 绑定矿物箱-维度失败 | `§6无法获取当前维度信息` | `:152` |
| 绑定矿物箱-校验失败 | `wkError(distanceError)`；随后 `§7提示：靠近其他点位后再设置，或删除旧点位重新规划` | `:159-160` |
| 绑定矿物箱-成功 | 标题 `已设置矿物箱`；字段 `类型 ▸ 矿物箱`；状态 `成功 ▸ 已保存`（排版见 `:470-478`） | `:167` |
| 绑定食物箱 | 同上，名称换 `食物箱`（`:179-180`、`:186`、`:191`、`:197`、`:204-205`、`:212`） | — |
| 绑定挂机修复点-已绑定 | `§6挂机修复点已绑定，请先删除旧绑定再重新设置`；随后 `§7提示：使用 §e.wk 移除 挂机修复点 §7删除` | `:225-226` |
| 绑定挂机修复点-玩家不存在 | `§6玩家不存在` | `:231` |
| 绑定挂机修复点-成功 | 标题 `已设置挂机修复点`；字段 `世界/维度/坐标` + `类型 ▸ 挂机修复点` + `视角 ▸ 偏航角 %.1f° / 俯仰角 %.1f°`；状态 `成功 ▸ 已保存` | `:256-263` |
| 解绑-未绑定 | `§6该坐标本来就没有绑定` | `:279` |
| 解绑-成功 | 标题 `已删除矿物箱` / `已删除食物箱` / `已删除挂机修复点`；字段 `类型`；状态 `成功 ▸ 已删除` | `:293-299` |
| 清空-无绑定 | `§6当前没有任何绑定` | `:306` |
| 清空-成功 | 标题 `已清空全部绑定`；字段 `范围 ▸ 矿物箱 + 食物箱 + 挂机修复点`、`数量 ▸ {count} 个`；状态 `成功 ▸ 已清空` | `:313-318` |
| 状态 | 标题 `坐标绑定状态`；每点位字段值 = `§7X§f{x} §7Y§f{y} §7Z§f{z} §8▸ §b{维度名}`（有玩家时追加 ` §8▸ §e{%.0f}m`）；未绑定 = `§8未绑定`；状态 `信息 ▸ {size} / 3 已绑定` | `:327-335`、`:349-365` |
| 跨维度绑定被拒 | `§c跨维度绑定被拒绝！\n§7%s 位于 §e%s§7，但你现在在 §e%s\n§7三个点位必须在同一维度，防止 Baritone 跨维度乱寻路` | `:400-401` |
| 距离超 32 格 | `%s 与 %s 距离超过 32 格（%.1f 格），三个点位必须互相在 32 格范围内` | `:440-441` |
| 玩家/世界未加载 | `玩家或世界未加载`（作为 `validateBinding` 的返回值） | `:379` |
| 检测假矿-模块缺失 | `§6自动挖矿模块未加载` | `:593` |

**GUI 静态入口回执（与指令不同源，逐字）**：

| 触发场景 | 逐字文案 | 行号 |
| --- | --- | --- |
| GUI-矿物箱/食物箱 未命中 | `§6准星未对准任何方块，请重新设置` | `:525`、`:538` |
| GUI-矿物箱/食物箱 非容器 | `§6目标方块不是容器（箱子/桶/潜影盒等），请重新设置` | `:529`、`:542` |
| GUI-解绑成功 | `§c§l✗ 已删除 {矿物箱\|食物箱\|挂机修复点} 绑定` | `:573` |
| GUI-解绑无绑定 | `§6该坐标本来就没有绑定` | `:575` |

> 复刻要点：**指令路径与 GUI 路径的成功/失败文案不是同一套**（GUI 版多了「请重新设置」、解绑回执走 `wkInfo` 且带 `§c§l✗`）。这是旧项目现状，按第 97 条**逐字保留**，不要「统一」。

### 4.5 静态入口（供配置页调用，`:500-577`）

| 方法 | 行号 | 说明 |
| --- | --- | --- |
| `hasBinding(String key)` | `:500-502` | `DATA_STORE.containsKey` |
| `getBinding(String key)` | `:509-511` | 取 `WKData` 或 null |
| `setBinding(String key)` | `:518-554` | 三 key 分支：`mineral`/`food` 先校验准星与容器再调 `bindXxx()`；`afk` 直接 `bindAFKPoint()`；返回 true 表示已处理（调用方随后关界面） |
| `removeBinding(String key)` | `:560-577` | `DATA_STORE.remove` 成功 → `saveData()` + `wkInfo("§c§l✗ 已删除 " + name + " 绑定")`；失败 → `wkError("该坐标本来就没有绑定")` |
| `reloadForCurrentServer()` | `:669-671` | `loadData()`（模块 `onActivate` 调用） |
| `getMineralChest()` / `getFoodChest()` / `getAFKPoint()` | `:707-717` | 三个点位取用入口（自检、FSM、渲染共用） |

> **注意**：`setBinding` 内部 `new WKCommand()`（`:519`、`:561`）只是借用实例方法，不动静态数据；`bindMineralChest()` 内部**仍然会读取准星**（GUI 线程里读的是关界面前的准星状态，与旧项目行为一致）。

### 4.6 帮助文本的来源

- 指令侧唯一「说明性文本」是类 javadoc（`:29-34`）：

```text
· 绑定点位：.wk 设置 矿物箱 / 食物箱 / 挂机修复点
· 删除点位：.wk 移除 <目标>
· 清空全部：.wk 清空
· 查看状态：.wk 状态
· 检测假矿：.wk 检测假矿（委托给 AutoMinerModule）
```

- **裸 `.wk` 不打印帮助**，直接打印绑定状态（`:88-91`）。
- 面向玩家的完整说明写在**模块帮助页**里（`buildHelpContent()`，见第 6 节）——这正是 `37-附A` 第 3 节第 4 条「帮助页文案与 `.wk` 回执逐字同源」的落点：

| 帮助页原文 | 对应的 `.wk` 子命令 |
| --- | --- |
| `§8> §3.wk 设置 矿物箱 §8— …` | `.wk 设置 矿物箱` |
| `§8> §3.wk 设置 食物箱 §8— …` | `.wk 设置 食物箱` |
| `§8> §3.wk 设置 挂机修复点 §8— …` | `.wk 设置 挂机修复点` |
| `§8> §3.wk 状态 §8— §7查看绑定状态 §7(含坐标、维度、视角)` | `.wk 状态` |
| `§8> §3.wk 检测假矿 §8— …` | `.wk 检测假矿` |
| `§8> §3.wk 移除 §c<目标> §8— §7解绑单个坐标` | `.wk 移除 <目标>` |
| `§8> §3.wk 清空 §8— §7清空所有绑定` | `.wk 清空` |

---

## 5. 全部用户可见文本（穷尽清单）

### 5.1 按钮文字

```text
§e查看使用说明                                                mining/AutoMinerModule.java:1509
检测假矿                                                      mining/AutoMinerModule.java:1517
§a设置 / §8设置                                               mining/AutoMinerModule.java:1592-1593
§c删除                                                        mining/AutoMinerModule.java:1602
关闭                                                          ui/HelpScreen.java:51
```

### 5.2 标签 / 字段名

- 设置组名：`目标选择` / `传送指令` / `触发条件` / `物品管理` / `Baritone调优`（`:99-103`）
- 设置项名：见第 2 节 55 项（逐字）
- 卡片标签：`矿物箱` / `食物箱` / `挂机修复点`（`:1530-1536`）、`§8暂未绑定`（`:1585`）、`§8-`（`:1587`）
- 枚举显示名：`精准采集` / `时运`（`LootMode.toString()`，`:72-86`）
- 隐藏设置名：`_esp_scale_internal` / `_mineral_color_internal` / `_food_color_internal` / `_afk_color_internal`

### 5.3 播报（前缀与配色）

**前缀格式**（旧项目，`core/YiyiaddonModule.java`）：

```java
formatMessage： "§c§l[yiyiaddon]§r§f§l[" + 模块名 + "]§r" + 内容     // :294-298
notify(msg)      → 内容前缀 "§f"                                    // :117-120
notifyError(msg) → 内容前缀 "§6§l"                                  // :126-129
warning(msg)     → 内容前缀 "§e§l"                                  // :99-102
```

新项目按 `开发习惯.md` 第 110-113 条**去掉 `[yiyiaddon]` 段**，其余（`[模块名]` 白色加粗、整条加粗）保持。

**开启/关闭**（基类 `toggle()`，`core/YiyiaddonModule.java:56-66`）：`§a§l已开启` / `§c§l已关闭`。

**FSM 播报共 40+ 条**，逐条行号与逐字文案已见 [`39 号`](39-阶段10-附录A-逻辑层侦察记录.md) 3.4 / 3.5 / 3.6，此处不重复；本节只登记**状态机以外的**播报：

| 来源 | 逐字文案 | 行号 |
| --- | --- | --- |
| 启动报告 | 见 5.4（整块多行） | `AutoMinerModule.java:772-836` |
| 种子格式错误 | `种子格式错误（选填·仅记录）：不是Long数字，已按无种子继续实测扫描` | `:921` |
| 假矿检测 7 条 | `无法检测，玩家未加载` / `无法检测假矿！请先启用「种子挖矿」功能` / `无法检测假矿！请先在「种子挖矿」设置中填入世界种子` / `种子格式错误！必须是Long类型数字（支持负数）` / `未选择目标矿石，请先在「目标选择」中选择矿物` / `§e正在扫描周围矿石...` / `§c周围矿石样本不足（需要至少3个），无法验证种子有效性` + `§7提示：靠近矿脉或使用 X-Ray 找到更多矿石后再检测` / `§c检测到假矿！种子错误或服务器使用了假矿` / `§a扫描 {n} 个矿石，全部匹配预测位置，种子验证通过` | `:1417-1495` |
| Baritone 启动 | `§aBaritone 已启动挖掘：{名称}` / `未选择目标矿石，无法启动挖矿` / `Baritone 未加载` / `Baritone 调用失败：{msg}` | `BaritoneExecutor.java:49/56/70/80` |
| 传送超时（指令层） | `§c传送等待超时` | `CommandManager.java:144` |
| 自检失败 | 首行 `还差 {n} 项没配好，配完再开：`（`notifyError`）+ 逐条 `§6  {i}. §f{缺项}`（`notify`） | `core/YiyiaddonModule.java:156-159` |

### 5.4 启动报告（整块多行，逐字）

`AutoMinerModule.java:805-835`：

```text
§a§l✓ 自动挖矿 · 启动报告
§7当前维度　§8▸ {高亮 维度中文名}
§7目标矿物　§8▸ {高亮 目标显示名}
§7采集模式　§8▸ {高亮 精准采集 | 时运}
§7挖矿模式　§8▸ {高亮 种子模式 | 普通模式}
§7扫描方式　§8▸ §f实测扫描（只挖服务器上真实存在的目标矿）· 渲染范围 {高亮 N 格}
§7扫描方式　§8▸ §f视野内所有目标矿
§7触发阈值　§8▸ §f满载 {高亮 N 组}§f · 饥饿 {高亮 N}§f · 耐久 {高亮 N}
§c⚠ 丢弃规则：除保留项外全部自动丢弃！想留下的物品请先加进「保留白名单」
§e⚠ 镐子无经验修补附魔：耐久低时将无法自动修复，建议换有经验修补的镐子
```

（`§7扫描方式` 两行按种子模式二选一；末行仅 `!hasMendingPickaxe()` 时追加。）

高亮工具（基类，`core/YiyiaddonModule.java:167-195`）：`highlightText` = `§a§l…§r§f§l`、`highlightFunction` = `§b§l…`、`highlightNumber` = `§e§l…`、`highlightServer` = `§6§l…`、`highlightLocation` = `§d§l…`、`highlightCommand` = `§e§l…`。启动报告全部用 `highlightText`（即亮绿）。

维度中文名取法（`:863-871`）：含 `minecraft:overworld` → `主世界`；含 `minecraft:the_nether` → `下界`；含 `minecraft:the_end` → `末地`；否则 `自定义维度`。

### 5.5 自检缺项（16 条，逐字）

`AutoMinerModule.selfCheck()` `:969-1070`：

```text
§e目标§f·未选择                                                              :983
§e目标§f·选了{n}个（只能选1个）                                              :985
§6矿物箱§f·未绑定                                                            :989
§2食物箱§f·未绑定                                                            :990
§d挂机点§f·未绑定                                                            :991
§b前往挖矿指令§f·未填写                                                      :994
§b返回卸货指令§f·未填写                                                      :995
§b前往补给指令§f·未填写                                                      :996
§b前往修复指令§f·未填写                                                      :997
§b死亡返回指令§f·未填写                                                      :998
§7镐子§f·背包里没有                                                          :1038
§7武器§f·背包里没有                                                          :1039
§7食物§f·白名单只有{n}个（低于阈值{阈值}）                                    :1040
§d精准采集镐§f·背包里没有（当前是精准采集模式，请换成精准采集镐）              :1046
§d时运镐§f·背包里没有（当前是时运模式，请换成时运镐或普通镐）                  :1051
§e种子挖矿§f·种子格式错误（需Long数字，或留空）                                :1064
```

判据要点（复刻必抄的四个）：
- 目标单选：三选一计数 `selectedCount == 0` 报未选择，`> 1` 报「只能选1个」（`:977-986`）。
- 装备检测**含副手**（`:1025-1036`），且**精准/时运模式与镐子不符直接阻断**（`:1043-1054`；「时运模式」下普通镐视为兼容，`hasFortunePickaxe || hasPlainPickaxe`）。
- 食物计数**只算食物白名单内且带 `FOOD` 组件的物品数量合计**，与 `hungerThreshold` 比（`:1022`、`:1035`）。
- 种子格式仅在 `seedMiningEnabled` 为真时校验（`:1057-1067`）。

> 新项目差异（必须处理）：本项目自检失败**必须聊天 + 屏幕弹窗**（`ConfirmPanelScreen.notice`，`开发习惯.md` 第 176-179 条），旧项目只有聊天。文案集合不变。

---

## 6. 帮助页（`ui/HelpScreen.java` + `buildHelpContent()`）

### 6.1 窗口规格

- 类：`ui/HelpScreen.java`，继承旧框架 `WindowScreen`（`:23`）
- 窗口标题（逐字，`:28`）：`module.title + " - 使用说明"` → 自动挖矿即 `自动挖矿 - 使用说明`
- 内容渲染：逐行 `theme.label(line)`；**空字符串行渲染为分隔线** `theme.horizontalSeparator()`（`:38-46`）
- 底部：一条分隔线 + 满宽按钮 `关闭`（`:49-57`），点击 `mc.setScreen(null)`
- 标题框与章节格式（`buildHelpContent`，`:66-90`）：

```java
content[index++] = "§8┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓";
content[index++] = "§8┃ §3§l> §b§l使用说明                                      §8┃";
content[index++] = "§8┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛";
...
content[index++] = "";
content[index++] = "§3[§b#§3] §f" + section.title;
```

> 与 `开发习惯.md` 第 137 条完全一致（框线 + `§3[§b#§3]` 章节）；新项目对应 `ui/screen/HelpPanelScreen`，格式禁止改动。

### 6.2 自动挖矿的 8 个章节（逐字，`AutoMinerModule.java:1619-1684`）

```text
【准备工作】
  §8├─ §f准备好挖矿工具 §7(推荐附魔耐久、效率)
  §8├─ §f准备好武器 §7(修补耐久时用)
  §8├─ §f放置矿物箱、食物箱 §7(装满食物)
  §8├─ §f选好挂机修复点 §7(安全区域，怪物可到达)
  §8└─ §f配置页面顶部点击卡片按钮设置三个点位

【点位设置 §7(两种方式)】
  §b▸ §e方式1 §8- §f配置页面按钮
    §7准星对准箱子 §8→ §f点击卡片中的设置按钮
    §7箱子类型：矿物箱、食物箱自动检测容器
    §7挂机修复点：直接站在目标位置即可绑定

  §b▸ §e方式2 §8- §f指令系统
    §8> §3.wk 设置 矿物箱 §8— §7准星对准箱子，绑定矿物贮箱
    §8> §3.wk 设置 食物箱 §8— §7准星对准箱子，绑定食物补给箱
    §8> §3.wk 设置 挂机修复点 §8— §7站在目标位置后自动绑定 §7(含视角)

  §7§o容器检测：箱子类点位会自动检测目标方块是否为容器
  §7§o不是容器 §8→ §7自动拒绝并提示重新设置，避免卡死

【指令系统】
  §8> §3.wk 状态 §8— §7查看绑定状态 §7(含坐标、维度、视角)
  §8> §3.wk 检测假矿 §8— §7检测周围假矿 §7(需启用种子挖矿)
  §8> §3.wk 移除 §c<目标> §8— §7解绑单个坐标
  §8> §3.wk 清空 §8— §7清空所有绑定

【状态机流程】
  §a[1] §f前往挖矿 §8→ §7发送挖矿指令，等区块加载完成
  §a[2] §f采掘 §8→ §7Baritone自动挖矿，满载/饿/耐久触发转换
  §a[3] §f卸货循环 §8→ §7传送到矿物箱，卸货，返回野外
  §a[4] §f补给循环 §8→ §7传送到箱，拿食物，吃饱，返回

【物品管理 §7(默认全丢)】
  §c▸ §f丢弃逻辑：除保留项外，背包其余物品全部自动丢弃
  §a▸ §f默认保留：任意品质工具 §7(镐/铲/斧/剑/锄)§f、白名单食物、目标矿物
  §a▸ §f搭路方块 §7(圆石/地狱岩) §f只保留各一组，多余自动丢弃
  §e▸ §f保留白名单：不想被扔的物品/方块加进去就不会丢
  §6⚠ §f启动前记得把想留的东西加进「保留白名单」

【参数建议】
  §6▸ §f满载组数 §8= §e36 §7(标准背包容量)
  §6▸ §f食物阈值 §8= §e14 §7(7格肉约14饱食度)
  §6▸ §f耐久阈值 §8= §e50 §7(低于50时自动修复)
  §6▸ §f传送等待 §8= §e10秒 §7(RTP加载缓冲)

【种子挖矿 §7(可选)】
  §d▸ §f启用后状态机切换采集流程 §7(两种模式)
    §7普通模式：Baritone mine 挖视野内所有目标矿
    §7种子模式：逐块寻路到预测真矿，原版合法破坏，无视假矿
  §d▸ §f预测位置无矿自动跳过，附近挖完自动重新RTP换区
  §d▸ §f检测假矿：对准可疑方块 §8→ §f点击「检测假矿」按钮
  §d▸ §f假矿判定：预测无矿但显示有矿 §8= §c假矿
  §d▸ §f适用场景：防止挖到管理员放置的诱饵矿

【注意事项】
  §c⚠ §f模块运行中无法修改点位，必须先关闭模块
  §c⚠ §f已绑定点位不允许覆盖，必须先删除再重新设置
  §c⚠ §f传送指令需服务器支持，否则无法自动返回
  §c⚠ §f挂机修复点会记录视角，用于精准对准修补工作台
  §c⚠ §f默认全丢垃圾！想留下的物品务必先加进「保留白名单」
```

> 注意事项第 1 条「模块运行中无法修改点位，必须先关闭模块」与代码现状**不完全一致**（`setBinding` 里没有 `isActive()` 拦截，只有「已绑定不允许覆盖」）。按第 97 条**不加不减、照抄原文**，不据此改代码。

---

## 7. 独立窗口与跳转

### 7.1 全部 `setScreen(...)` 调用点

| 触发 | 目标窗口 | 行号 |
| --- | --- | --- |
| `§e查看使用说明` | `new HelpScreen(theme, this, buildHelpContent())` | `AutoMinerModule.java:1510` |
| 卡片 `设置` | `mc.setScreen(null)`（关面板，回游戏） | `:1596` |
| 卡片 `§c删除` | `mc.setScreen(null)` | `:1606` |
| 帮助页 `关闭` | `mc.setScreen(null)` | `HelpScreen.java:55` |

### 7.2 缺失的窗口（与其它模块对比后确认）

- **无二次确认窗**：`.wk 清空` 走指令直接清空（`WKCommand.java:303-320`），配置页也没有「清空全部点位」按钮（`AutoChestModule` 才有）。→ 本模块不涉及 `ConfirmPanelScreen` 的「清空」用例。
- **无选择器窗口**：三个点位靠准星/GUI 卡片绑定，没有 `SelectorScreen` 类窗口。
- **无控制台/日志页**：页签式控制台是星露谷的做法，旧项目挖矿**没有**；挖矿的界面面只有「配置页 + 三张卡片 + 帮助页」。
- 按 `开发习惯.md` 第 136 条的表格：挖矿只需落地 **`§e查看使用说明`** 这一行（已登记在表中，见 `AutoMinerModule.java:1509`）。

---

## 8. 页面 ↔ 模块 ↔ 指令的联动点（用户拍板要求逐条落地）

| # | 用户约束（`37-附A` 第 3 节第 3 条） | 旧项目的实际落点 | 证据 |
| --- | --- | --- | --- |
| 1 | 页面每一行直接读写模块设置字段与点位表 | 卡片直接调 `WKCommand.hasBinding/getBinding` 读、`setBinding/removeBinding` 写；设置项全部是模块字段（`sgXxx` 加入 `settings`） | `:1558-1559`、`:1595`、`:1605` |
| 2 | 页面与 `.wk` 指令同源同一份数据 | 两者都只碰 `WKCommand.DATA_STORE`（静态 Map）；**没有第二份缓存** | `:55`（Map）、`:500-511`（读）、`:518-577`（写） |
| 3 | 绑定 / 移除 / 失效校验只有模块那一处实现 | 校验函数唯一：`validateBinding`（跨维度 + 32 格）；GUI 与指令都经 `bindMineralChest/bindFoodChest/bindAFKPoint/unbind` 同一批方法（GUI 只是先多一道准星预检） | `:378-446`、GUI 入口 `:518-577` |
| 4 | 帮助页文案与 `.wk` 回执逐字同源 | 帮助页「指令系统」章节 4 条 `.wk 用法` + 「点位设置」章节 3 条 `.wk 设置 …`，与指令树字面量一致 | `:1635-1646` vs `WKCommand.java:93-120` |

**额外联动点（自检与页面同源）**：自检读的也是 `WKCommand.getMineralChest/getFoodChest/getAFKPoint`（`:989-991`），与页面卡片读同一批静态访问器。

**运行期的一致性要求**（迁移后必须仍成立）：
- 页面设置改值 → `.wk 状态` 与状态机立即可见（同一份字段）；
- `.wk 设置/移除` 改点位 → 重开页面卡片必须同步（旧项目靠「每次 `getWidget` 重新读静态 Map」实现，`:1558-1559`；新项目页面若缓存元素，必须在重新进入页面时重建）；
- 三张卡片的 `设置/删除` 按钮必须在**同一帧内**关面板（`mc.setScreen(null)`），否则会看到空态闪烁。

---

## 9. 新项目落地映射（旧控件 → 本项目控件）

| 旧界面元素（`文件:行号`） | 新项目应采用 | 备注 |
| --- | --- | --- |
| 模块配置页整体（`AutoMinerModule.java:1503-1542`） | `ui/page/CompactModulePage` + `ModulePage`（先例：`feature/stardew/ui` 的控制台、`feature/autochest/ui/AutoChestPage`） | 一个模块一个页面类，在 `ModuleEntry.page` 注册 |
| 满宽按钮（`core/YiyiaddonModule.java:280-284`，`expandX().minWidth(200)`） | `ui/component/ButtonRow` + `ui/widget/Button` | 尺寸由本项目控件自动适配（第 128 条），不参照旧固定宽度 |
| `§e查看使用说明` → `HelpScreen` | `ui/screen/HelpPanelScreen`（标题 `自动挖矿 - 使用说明`，正文框线与 `[#]` 章节格式逐字） | 新项目「关闭」为内置按钮，`exitToGame` 语义一致 |
| 三张点位卡片（`:1553-1613`） | `ui/component/ListRow`（名称 + `detail` 坐标/维度 + `action` 按钮）或 `CompactRow` + `Button` 组合 | 旧卡是「三列等高」；新项目若不支持三列，须保证**六行文案与按钮一字不差**，排布可自适 |
| 卡片内 `§a设置` / `§8设置` / `§c删除` | `ui/widget/Button`（`selected(Supplier)` 表达「已绑定亮绿」） | 文案与点击作用逐字照旧 |
| 设置项 55 项 | `SettingSegmented`（双值枚举 `精准采集/时运`）、`SettingTextBox`（字符串）、`SettingNumberBox`（整数，**禁止滑块**，第 123 条）、`SettingToggle`（布尔）、`SettingColorPicker`（颜色）、列表类（保留/食物/搭路白名单） | 旧 `noSlider()` 对应新项目的数值输入框；`ItemListSetting` / `BlockListSetting` 属旧框架控件，须按本项目做法重写（见下条） |
| 旧框架 `ItemListSetting` / `BlockListSetting`（`:351-378`） | 本项目**无等价控件**：需用现有控件组合（如 `SelectorScreen` 双栏 + `ListRow`）或新建 | 需报备（第 163 条）：旧项目对应物 = 这两个设置类；影响面 = 配置页 3 项 |
| `visible(...)` 可见性（`:265`、`:404`、`:414`、`:421`、`:428`、`:617`、`:638`、`:669`） | 本项目设置控件无 `visible` 机制 → 页面按值重建或返回空元素 | 需在页面内自管（先例：`AutoChestPage` 的运行模式联动） |
| 旧框架 `theme.label/button/table/horizontalSeparator` | 本项目 `ui/component/{TextLine,ButtonRow,CompactRow,ListRow}` + `PanelScreen.addDivider()` | — |
| 指令 `.wk`（旧框架 `Command` 子类） | `command/ClientCommand` + `ClientCommand.aliases()/complete()` | 指令名/子命令/回执**逐字保留**（第 100 / 101 条） |
| 回执排版 `CommandMessageFormatter` | 本项目 `core/CommandMessageFormatter`（已含 `of/field/world/dimension/coord/key/highlight/raw/status/send/sendLine/lineOf/line`） | 星露谷批次已升级为旧项目完整版；挖矿回执可直接照抄 |
| 播报前缀 `§c§l[yiyiaddon]§r§f§l[模块名]§r` | 本项目前缀 `[模块名] `（白 `§f` 加粗，不含 `yiyiaddon`，第 110-113 条） | 其余文案与配色一字不改 |
| 自检（旧为纯聊天） | `Module.selfCheck()` 返回缺项 → `ModuleManager` 统一**聊天 + 弹窗**（`ConfirmPanelScreen.notice`，含「打开设置」） | 16 条缺项文案逐字保留（第 176-179 条） |
| 隐藏设置 4 项（`_esp_scale_internal` 等） | 本项目**可改为常量或私有字段**（不出现在页面即可） | 但取值必须与旧默认一致（2.0 / 三色） |
| ESP 渲染（`AutoMinerModule_ESP`） | `ui/render/world/WorldOverlay.register(模块ID, renderer -> …)` + `EspRenderer`（`box` 先填充后描边，`text` 画标签） | 渲染时机由 `mixin/client/GuiRendererMixin` 注入（第 144 条） |

### 9.1 需要拍板的界面差异（登记）

1. **三列卡片**：新项目现有控件里没有「一行三等分卡片」，落地形态待定（保留三列 / 改为三行 `ListRow`）。
2. **列表类设置项**（保留白名单 / 食物白名单 / 搭路方块白名单）：本项目无现成控件，需新建或复用 `SelectorScreen`，属「新增控件」范畴，须先报备。
3. **可见性联动**（`RTP需要GUI选择 → GUI按钮关键词`、`启用种子挖矿 → 后续 4 项`、`怪物规避 → 半径`、`仅挖暴露矿石 → 距离`、`合法挖掘模式 → 高度`）：新项目页面需自管，交互对齐但实现不同。

---

## 附：本次侦察覆盖文件清单

```text
mining/AutoMinerModule.java（1-700 / 700-1280 / 1280-1685）
mining/command/WKCommand.java（全读）
ui/HelpScreen.java（全读）
core/YiyiaddonModule.java（toggle / info / warning / error / notify / notifyError / reportSelfCheck / highlight* / buildInfoWidget 相关区段）
core/AddonTemplate.java（分类常量、模块与指令注册点相关行）
```

对照参考（不逐字抄录，仅用于对齐口径）：`02-基准-01-状态播报体系.md` §7.5 与 `.wk` 段、`03-基准-02-自检启动机制.md` §3.5、`04-基准-03-配置页按钮与独立窗口清单.md` §1.3/§五、`07-基准-06-指令体系全量清单.md` §4。

新项目侧核对文件（只读）：

```text
ui/screen/PanelScreen.java、HelpPanelScreen.java、ConfirmPanelScreen.java
ui/page/CompactModulePage.java、ModulePage.java
ui/component/{ButtonRow,ListRow,CompactRow,TextLine,CompactStack}.java
ui/widget/{Button,SettingTextBox,SettingNumberBox,SettingSegmented,SettingToggle,SettingColorPicker}.java
ui/render/world/{WorldOverlay,EspRenderer,ShapeMode,EspColor}.java
command/{ClientCommand,CommandRegistry,CommandManager}.java
core/CommandMessageFormatter.java、core/module/{Module,ModuleManager}.java
module/AddonModules.java
feature/autochest/ui/AutoChestPage.java（页面写法先例）
```
