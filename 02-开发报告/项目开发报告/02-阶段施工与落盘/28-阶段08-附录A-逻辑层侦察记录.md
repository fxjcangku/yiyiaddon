# _recon_autochest_A · 自动箱子（autochest）逻辑层侦察报告

- 侦察对象：`d:\mcaddon\26.1.2\src\main\java\com\example\addon\autochest\`
- 侦察方式：只读，逐文件整读，未修改任何文件
- 用途：供新项目 `d:\mcaddon\yiyiaddon` 按原样复刻
- 所有结论均带 `文件:行号`，逐字文案用代码块包裹

> **开篇关键更正（与任务清单不符，必须先说清）**：
> `autochest` 包内**不存在** `task/` 子包。任务清单点名的 `ContainerTask / UnloadTask / RestockTask / PoisonDumpTask` **全部属于 `autofarm/task/`**，与自动箱子无关（证据：`autofarm/task/ContainerTask.java`、`autofarm/task/UnloadTask.java`、`autofarm/task/RestockTask.java`、`autofarm/task/PoisonDumpTask.java`）。自动箱子的取货逻辑全部在 `service/ChestInteractionService.java` + `fsm/AutoChestStateMachine.java` 内。详见第 5 节。

---

## 1. 文件清单

| 文件路径（`autochest\` 下） | 行数 | 一句话职责 | 是否被引用（引用点，均指旧项目 `26.1.2`） |
|---|---|---|---|
| `AutoChestModule.java` | 522 | 模块本体：设置装配、生命周期、扫描周期、视角同步、标点/记录 GUI 按钮、启动自检 | 是：`core/AddonTemplate.java:35`(import)、`:179`(实例化)；`command/AutoChestCommand.java:3`(import)、`:75`,`:109`(`Modules.get().get`)；`fsm/AutoChestStateMachine.java:3`；`render/AutoChestRenderer.java:3` |
| `config/AutoChestSettings.java` | 278 | 全部设置项定义与分组 | 是：`AutoChestModule.java:3`、`:96`；`service/ChestInteractionService.java:3` |
| `config/ContainerTypeSetting.java` | 87 | 「容器类型」多选设置（持久化类型 id 列表） | 是：`core/AddonTemplate.java:36`(import)、`:175`(register)；`AutoChestModule.java:122` |
| `config/InfoTextSetting.java` | 67 | 纯展示设置（不参与持久化） | 是：`core/AddonTemplate.java:37`、`:174`；`itemid/IdIdentifyModule.java:3` |
| `config/ItemQuantitySetting.java` | 129 | 「每种物品数量」设置（身份键→数量映射） | 是：`core/AddonTemplate.java:38`、`:176`；`AutoChestModule.java:198` |
| `fsm/AutoChestStateMachine.java` | 653 | 全流程状态机（扫描→选目标→锁→取物→关箱→记录） | 是：`AutoChestModule.java:4`、`:104` |
| `model/ChestTarget.java` | 72 | 容器/点位不可变快照（坐标+维度+服务器+类型+时间） | 是：`AutoChestModule.java:5`；`scan/ContainerScanner.java:3`；`service/ChestPointManager.java:4`；`fsm/AutoChestStateMachine.java:4`；`render/AutoChestRenderer.java:4` |
| `model/ContainerRecord.java` | 121 | 已处理容器记录（服务器+维度+坐标+类型+状态+时间+版本） | 是：`service/ContainerRecordManager.java:4` |
| `model/ContainerType.java` | 61 | 容器类型（id + 中文名 + Block 匹配谓词） | 是：`model/ContainerTypeRegistry.java:3`；`config/ContainerTypeSetting.java:3`；`scan/ContainerScanner.java:4`；`command/AutoChestCommand.java:5` |
| `model/ContainerTypeRegistry.java` | 100 | 容器类型注册表（内置 5 类） | 是：`AutoChestModule.java:7`；`config/ContainerTypeSetting.java:4`；`scan/ContainerScanner.java:6`；`command/AutoChestCommand.java:6`；`ui/ContainerTypeSelectScreen.java:5` |
| `model/ScanMode.java` | 34 | 三种运行模式枚举 | 是：`AutoChestModule.java:8`；`config/AutoChestSettings.java:3`；`fsm/AutoChestStateMachine.java:5` |
| `model/WithdrawMode.java` | 33 | 三种取物模式枚举 | 是：`AutoChestModule.java:9`；`config/AutoChestSettings.java:4`；`service/ChestInteractionService.java:4` |
| `model/WorldIdentity.java` | 118 | 服务器/维度/数据版本身份工具（隔离核心） | 是：`AutoChestModule.java:10`；`scan/ContainerScanner.java:3`；`service/ChestPointManager.java:3`；`service/ContainerRecordManager.java:3`；`fsm/AutoChestStateMachine.java:6`；**跨模块**：`itemid/IdConfigModule.java:3`、`worldblock/BlockIdentifier.java:4` |
| `render/AutoChestRenderer.java` | 73 | 三态 ESP 渲染（未处理/已处理/处理中） | 是：`AutoChestModule.java:11`、`:105` |
| `scan/ContainerScanner.java` | 158 | 分帧容器扫描器（预算 512/帧 + 稳定快照） | 是：`AutoChestModule.java:12`、`:98` |
| `scan/ContainerSelector.java` | 42 | 候选选择器（维度隔离+去已处理+最近优先） | 是：`AutoChestModule.java:13`、`:99`；`fsm/AutoChestStateMachine.java:262`、`:279` |
| `service/ChestInteractionService.java` | 406 | 开箱/读槽/三模式取物/关箱（核心） | 是：`AutoChestModule.java:14`、`:102`；`fsm/AutoChestStateMachine.java:7` |
| `service/ChestPointManager.java` | 175 | 标点管理器（内存+磁盘，按服务器隔离） | 是：`AutoChestModule.java:15`、`:101`；`command/AutoChestCommand.java:94`、`:120`、`:139`、`:140`、`:160` |
| `service/ContainerRecordManager.java` | 220 | 已处理记录管理器（JSON 落盘，按服务器隔离） | 是：`AutoChestModule.java:16`、`:100`；`fsm/AutoChestStateMachine.java`（经 `module.recordManager`）；`scan/ContainerSelector.java:4`；`render/AutoChestRenderer.java:56` |
| `service/PathingService.java` | 131 | Baritone 寻路服务（算安全站位） | 是：`AutoChestModule.java:17`、`:103`；`fsm/AutoChestStateMachine.java`（经 `module.pathingService`） |
| `adapter/AutoChestAdapter.java` | 31 | 容器识别适配器**接口** | **否**。全仓仅自引用（`AutoChestAdapter.java:12`），无任何实现类与调用点 → **死代码** |
| `command/AutoChestCommand.java` | 211 | 标点管理指令 `.autochest 添加/移除/清空/状态` | 是：`core/AddonTemplate.java:34`、`:180` |
| `ui/ConfirmScreen.java` | 48 | 通用确认弹窗 | 是：`AutoChestModule.java:18`；**跨模块**：`stardew/StardewFarmModule.java:3`、`stardew/ui/StardewConsoleScreen.java:3`、`itemid/IdConfigModule.java:4` |
| `ui/ContainerTypeSelectScreen.java` | 58 | 容器类型多选界面 | 是：`config/ContainerTypeSetting.java:5`、`:81` |
| `ui/ItemQuantityScreen.java` | 55 | 每物品数量配置界面 | 是：`config/ItemQuantitySetting.java:3`、`:123` |

---

## 2. 模块本体（`AutoChestModule.java`）

### 2.1 `super(...)` 完整参数（逐字）

`AutoChestModule.java:93-94`：

```java
public AutoChestModule(ItemIdManager idManager) {
    super(AddonTemplate.CATEGORY_ASSIST, "自动箱子", "扫描并自动处理附近容器，取走ID配置中的目标物品。详细参考下面使用说明。");
```

分类常量 `CATEGORY_ASSIST` 逐字（`core/AddonTemplate.java:87`）：

```java
public static final Category CATEGORY_ASSIST = new Category("§c§lyiyiaddon §d§l辅助", () -> DisplayItemUtils.toStack(Items.CHEST));
```

> `YiyiaddonModule` 基类构造（`core/YiyiaddonModule.java:29-32`）额外设置 `toggleOnBindRelease = false;`。

### 2.2 设置项分组（`settings.createGroup` 逐字）

`config/AutoChestSettings.java:80-87`：

```java
grpMode      = settings.createGroup("运行模式");
grpPlayer    = settings.createGroup("玩家控制模式");
grpMarker    = settings.createGroup("标点模式");
grpContainer = settings.createGroup("容器");
grpProtect   = settings.createGroup("保护");
grpTarget    = settings.createGroup("目标物品");
grpWithdraw  = settings.createGroup("取物");
grpRender    = settings.createGroup("渲染");
```

### 2.3 全部设置项（类名 / 行号 / 名称 / 描述 / 默认值 / 取值域）

| 设置项字段 | 类型 | 定义行 | 设置名（逐字） | 描述（逐字） | 默认值 | 取值域 / 可见性 |
|---|---|---|---|---|---|---|
| `scanMode` | `EnumSetting<ScanMode>` | `AutoChestSettings.java:90-95` | `运行模式` | `玩家控制模式：玩家自己走位，进入触发距离自动处理；寻路模式：自动扫描并寻路；标点模式：只处理保存点位。` | `ScanMode.PLAYER_CONTROL` | 三枚举值；`onChanged(mode -> 刷新界面())`（`:94`） |
| `currentMode` | `InfoTextSetting` | `:98-101` | `当前模式` | `当前选中的运行模式（实时显示）。` | — | 展示 `"§a§l" + scanMode.get().toString()` |
| `triggerDistance` | `IntSetting` | `:104-112` | `触发距离` | `玩家控制模式下，距离容器多少格内自动处理（受开箱可达距离约 4.5 格限制，上限 4）。` | `4` | min 1 / max 4 / `noSlider` / `visible(() -> scanMode.get() == ScanMode.PLAYER_CONTROL)` |
| `markerHint` | `InfoTextSetting` | `:115-119` | `标点管理` | `标点通过说明面板按钮或指令管理，模块只处理已保存的点位。` | — | 展示 `§7面板按钮或 §e.autochest 添加/移除/清空/状态§7 管理点位；目标物品用 §e.id 物品§7 识别`；`visible(() -> scanMode.get() == ScanMode.MARKER)` |
| `containerTypes` | `ContainerTypeSetting` | `:122-124` | `容器类型` | `选择要识别的合法容器类型（箱子/陷阱箱/16色潜影盒/木桶/铜箱）。` | 全部已注册类型（`ContainerTypeSetting.java:36-40`） | — |
| `scanRadius` | `IntSetting` | `:126-132` | `检测范围` | `扫描附近合法容器的半径（格），负责发现容器。` | `16` | min 4 / `noSlider` |
| `scanInterval` | `IntSetting` | `:134-140` | `扫描周期` | `每多少 Tick 推进一轮扫描（分帧扫描，不整世界全扫）。` | `20` | min 1 / `noSlider` |
| `recordExpireMinutes` | `IntSetting` | `:142-148` | `已处理记录过期` | `已处理容器记录多少分钟后失效，可被再次处理。` | `30` | min 0 / `noSlider` |
| `multiplayerProtect` | `BoolSetting` | `:151-155` | `多人保护` | `检测到其他玩家正在使用目标容器时，不抢箱，暂时跳过并进入冷却。` | `true` | — |
| `playerDetectDistance` | `IntSetting` | `:157-164` | `玩家检测距离` | `其他玩家距离容器多少格内视为正在使用，触发多人保护。` | `3` | min 1 / `noSlider` / `visible(multiplayerProtect::get)` |
| `maxRetries` | `IntSetting` | `:166-172` | `最大重试次数` | `开箱/寻路/交互失败后最多重试几次，超过则本轮跳过该容器。` | `3` | min 1 / `noSlider` |
| `cooldownTicks` | `IntSetting` | `:174-180` | `临时冷却` | `连续失败或多人保护后，容器进入暂时不可用的冷却时长（Tick）。` | `100` | min 20 / `noSlider` |
| `withdrawMode` | `EnumSetting<WithdrawMode>` | `:183-187` | `取物模式` | `按目标数量取：每种目标物品单独配置数量；目标物品拿空：只拿空目标列表物品；全部拿空：忽略目标列表取走所有合法物品。` | `WithdrawMode.TARGET_COUNT` | 三枚举值 |
| `targetItems` | `ItemTargetSetting` | `:191-195` | `目标物品` | `选择 AutoChest 要取的目标物品（数据源唯一来自 ID 配置管理）。` | 空列表 | `visible(() -> withdrawMode.get() != WithdrawMode.TAKE_ALL)` |
| `itemQuantities` | `ItemQuantitySetting` | `:198-202` | `每种物品数量` | `按目标数量取模式下，为每种目标物品单独配置目标数量。` | 空 map | `visible(() -> withdrawMode.get() == WithdrawMode.TARGET_COUNT)` |
| `actionDelay` | `IntSetting` | `:204-210` | `动作延迟` | `两次槽位操作之间的 Tick 间隔。` | `2` | min 1 / `noSlider` |
| `renderEsp` | `BoolSetting` | `:213-217` | `ESP高亮` | `高亮显示已发现但未处理的容器。` | `true` | — |
| `espStyle` | `EnumSetting<EspStyle>` | `:219-224` | `ESP框样式` | `容器的 ESP 渲染样式：仅线条 / 仅面 / 线+面。` | `EspStyle.BOTH` | `visible(renderEsp::get)` |
| `unprocessedColor` | `ColorSetting` | `:226-231` | `未处理颜色` | `未处理容器的 ESP 颜色。` | `new SettingColor(0, 255, 0, 80)` | `visible(renderEsp::get)` |
| `processedColor` | `ColorSetting` | `:233-238` | `已处理颜色` | `已处理容器的 ESP 颜色。` | `new SettingColor(255, 0, 0, 80)` | `visible(renderEsp::get)` |
| `processingColor` | `ColorSetting` | `:240-245` | `处理中颜色` | `正在处理容器的 ESP 颜色（处理中不显示为已处理红色）。` | `new SettingColor(255, 200, 0, 80)` | `visible(renderEsp::get)` |

`EspStyle` 枚举逐字（`AutoChestSettings.java:249-266`）：

```java
public enum EspStyle {
    LINES("仅线条", ShapeMode.Lines),
    SIDES("仅面", ShapeMode.Sides),
    BOTH("线+面", ShapeMode.Both);
```

### 2.4 监听器注册与回调

- 注册点 `AutoChestModule.java:108`：

```java
idManager.addListener(this::onIdManagerChanged);
```

- 回调 `AutoChestModule.java:117-123`：

```java
private void onIdManagerChanged() {
    int removed = moduleSettings.targetItems.pruneInvalid();
    moduleSettings.targetItems.refreshCount();
    if (removed > 0) {
        notifyError("有 " + removed + " 个目标 ID 已失效，已从选择器移除");
    }
}
```

- 底层监听机制在 `itemid/ItemIdManager.java:37`（`listeners` 列表）、`:54-56`（`addListener`）、`:270-278`（`notifyChanged`，单个监听器异常不阻断其余）。

### 2.5 生命周期逐条

`onActivate()` `AutoChestModule.java:150-177`：
1. `:153-161` 世界就绪检查：`mc.player == null || mc.level == null || mc.gameMode == null` 时报 `"必须在进入世界后才能启动模块。"`（`:154`），`chatFeedback = false`，`mc.execute(...)` 延后 toggle 关闭并恢复 `chatFeedback = true`。
2. `:164-166` 装载数据：`idManager.reload(); recordManager.reload(); pointManager.reload();`
3. `:169` 启动自检：`if (!reportSelfCheck(selfCheck())) return;`
4. `:172` `scanner.setRadius(moduleSettings.scanRadius.get());`
5. `:173` `lastSeenContainers.clear();`
6. `:175-176` `stateMachine.reset(); scanner.reset();`

`onDeactivate()` `AutoChestModule.java:230-237`：
1. `:233` `pathingService.stop();`
2. `:234` `interactionService.close();`
3. `:235` `stateMachine.reset();`
4. `:236` `lastSeenContainers.clear();`

`onTick(TickEvent.Pre)` `AutoChestModule.java:239-264`：
1. `:241-242` `!isActive()` 或玩家/世界为空直接 return。
2. `:245` `stateMachine.tick();`
3. `:248` `syncViewToTarget();`（视角同步）
4. `:253-256` 扫描周期计数：`if (++scanTick >= moduleSettings.scanInterval.get()) { scanTick = 0; scanner.requestScan(playerPos); }`
5. `:258-260` `if (scanner.tick(playerPos)) { reconcileDestroyed(playerPos); }`
6. `:263` `interactionService.tick();`

`onRender3D(Render3DEvent)` `AutoChestModule.java:369-373`：`if (!isActive()) return; renderer.render(event);`

`getWidget(GuiTheme)` `:375-379`：保存 `theme`，返回 `buildInfoWidget(theme, this::buildHeader, buildSections())`。

`stopAutomation(String reason)` `:141-148`：

```java
notifyStatus("§c✗ " + reason + "，已停止自动箱子");
chatFeedback = false;
mc.execute(() -> {
    if (isActive()) toggle();
    chatFeedback = true;
});
```

`processingTarget()` `:126-128` → `stateMachine.processingTarget()`；`notifyStatus(String)` `:131-133` → `notify(message)`。

### 2.6 模块内部可变字段与流转

- `scanTick`（`:87`）：`onTick` 累加，达到 `scanInterval` 归零并 `requestScan`。
- `lastSeenContainers`（`Set<BlockPos>`，`:88`）：`onActivate`/`reconcileDestroyed`/`onDeactivate` 清空；每轮扫描完成后替换为本轮 `seen`（`AI:273-287`）。
- `theme`（`:91`）：`getWidget` 时赋值，供确认窗复用。
- 视角同步常量：`VIEW_TURN_STEP = 15.0f`（`:58`）、`VIEW_SYNC_RANGE = 6.0`（`:61`）、`VIEW_ALIGN_TOLERANCE = 25.0f`（`:64`）。

**状态机全部可变字段（`fsm/AutoChestStateMachine.java`）：**
`state`(`:111`)、`stateTick`(`:112`)、`lockedTarget`(`:115`)、`retryCount`(`:118`)、`retryToState`(`:121`)、`failWait`(`:124`)、`cooldownWait`(`:127`)、`idleWait`(`:130`)、`openTimeout`(`:133`)、`pathTimeout`(`:136`)、`alignWait`(`:139`)、`cooldowns`(`Map<String,Long>`，`:142`)、`lastMode`(`:145`)、`lastDimension`(`:148`)。
常量：`OPEN_TIMEOUT_TICKS = 60`(`:86`)、`MOVE_TIMEOUT_TICKS = 200`(`:89`)、`NEXT_COOLDOWN = 5`(`:92`)、`FAIL_WAIT_TICKS = 10`(`:95`)、`IDLE_SCAN_WAIT = 5`(`:98`)、`ALIGN_WAIT_TICKS = 20`(`:106`)。
状态枚举 `State` `:53-83`：`IDLE/SCAN/SELECT_TARGET/LOCK_TARGET/WAIT_DISTANCE/CALCULATE_STAND_POSITION/PATHING/ARRIVED/OPENING/READING_SLOTS/MATCHING/TAKING/VERIFYING/CLOSING/COMPLETED/FAILED/COOLDOWN/STOPPED`，其中文名逐字见 `:54-71`（如 `IDLE("空闲")`、`OPENING("打开容器")`）。
状态推进主体 `advance(ScanMode)` `:293-512`；`transitionTo(State)` `:632-637`；播报 `broadcast(State)` `:640-652` 仅播 `OPENING/TAKING/COMPLETED`。

---

## 3. 点位管理（`service/ChestPointManager.java`）

### 3.1 数据文件路径与读写时机

- 目录常量 `ChestPointManager.java:25`：

```java
private static final Path CONFIG_DIR = Paths.get("config", "yiyiaddon", "autochest", "points");
```

- 文件名 `ChestPointManager.java:38-40`：

```java
private Path dataFile() {
    return CONFIG_DIR.resolve(WorldIdentity.fileSafeServer(mc) + ".json");
}
```

- 读：`reload()` `:43-53`（先 `points.clear()`，`migrateLegacyFile(file)`，不存在返回；`parse(Files.readString(file))`，读取失败视为无标点）。
- 写：`save()` `:124-145`，在 `add`(`:78`)、`remove`(`:85`)、`clear`(`:92`) 内**立即落盘**（内存与磁盘同步）。
- 旧文件迁移：`migrateLegacyFile` `:56-69`，仅复制不移动（`:64-65` 注释「只复制不移动：旧文件是升级回滚依据」）。

### 3.2 点位字段

`ChestTarget`（`model/ChestTarget.java:15-28`）字段：`pos`(BlockPos)、`dimension`(String)、`server`(String)、`containerType`(String)、`discoveredAt`(long)。点位 JSON 落盘键（`ChestPointManager.java:131-136`）：

```java
sb.append("  {\"x\":").append(p.pos().getX())
  .append(",\"y\":").append(p.pos().getY())
  .append(",\"z\":").append(p.pos().getZ())
  .append(",\"dim\":\"").append(esc(p.dimension()))
  .append("\",\"type\":\"").append(esc(p.containerType()))
  .append("\"}");
```

> **注意**：点位 JSON **不落盘 server 字段**，`server` 在解析时由当前会话回填（`ChestPointManager.java:148`：`String server = serverIdentifier();`，`:168`）。隔离靠**文件路径**保证。

### 3.3 增删清接口与被调用方

| 接口 | 行号 | 被谁调用 |
|---|---|---|
| `reload()` | `:43` | `AutoChestModule.java:166` |
| `add(pos, dim, type)` | `:72` | `AutoChestModule.java:484`；`AutoChestCommand.java:94` |
| `remove(pos, dim)` | `:83` | `AutoChestModule.java:440`；`AutoChestCommand.java:120` |
| `clear()` | `:90` | `AutoChestModule.java:496`；`AutoChestCommand.java:140` |
| `find(pos, dim)` | `:96` | 未被 autochest 内其它文件调用（仅本类） |
| `pointsInCurrentDimension()` | `:104` | `AutoChestModule.java:395`；`AutoChestCommand.java:160`；`fsm/AutoChestStateMachine.java:262` |
| `hasPoints()` | `:114` | `AutoChestModule.java:200` |
| `size()` | `:118` | `AutoChestModule.java:495`；`AutoChestCommand.java:139` |

### 3.4 跨服隔离（逐字证据）

- 服务器键来源 `ChestPointManager.java:34-36`：

```java
private String serverIdentifier() {
    return WorldIdentity.server(mc);
}
```

- `WorldIdentity.server(...)` `model/WorldIdentity.java:27-39`：

```java
public static String server(Minecraft mc) {
    if (mc != null && mc.getCurrentServer() != null) {
        return canonicalServerKey(mc.getCurrentServer().ip);
    }
    if (mc != null && mc.getSingleplayerServer() != null) {
        // 存档目录名由 Meteor 从实际 LevelStorageSource 提取，避免同名展示标题或不同单人存档串数据
        String worldName = Utils.getFileWorldName();
        if (worldName != null && !worldName.isBlank() && !worldName.startsWith("FAILED_")) {
            return "singleplayer:" + worldName;
        }
    }
    return "singleplayer:unknown";
}
```

- 文件名安全化 `WorldIdentity.java:42-56`（`fileSafeServer` → `fileSafeKey`，多人前缀 `server_`、单人前缀 `singleplayer_`，剔除 `[\\/:*?"<>|\x00-\x1F]`）。
- 结论：**是，跨服隔离存在**，且以「逻辑服务器键 → 文件安全键 → `points/{key}.json`」一层文件隔离实现；`WorldIdentity` 就是判据唯一来源，无第二套。

---

## 4. 配置持久化

自动箱子存在**两套持久化**，必须区分：

### 4.1 设置项 → Meteor 框架 NBT（不是自建 JSON）

- `AutoChestSettings` 全部设置项经 `settings.createGroup(...).add(...)` 挂到 Meteor `Settings` 上（`config/AutoChestSettings.java:80-245`），随 Meteor 的 `modules.nbt` 由框架读写。
- 自定义设置类**自行覆写框架的 `save/load`**：
  - `ItemQuantitySetting`（`config/ItemQuantitySetting.java:86-103`）落盘键名逐字为 `"value"`，结构为「身份键 → int」：

```java
protected CompoundTag save(CompoundTag tag) {
    CompoundTag valueTag = new CompoundTag();
    for (Map.Entry<String, Integer> entry : get().entrySet()) {
        valueTag.putInt(entry.getKey(), entry.getValue());
    }
    tag.put("value", valueTag);
    return tag;
}
```
  - `InfoTextSetting` **不落盘**（`config/InfoTextSetting.java:48-55`：`save` 原样返回 tag、`load` 返回 `""`）。
  - `ContainerTypeSetting extends StringListSetting`（`:26`）与 `ItemTargetSetting extends StringListSetting` 走 Meteor `StringListSetting` 的列表持久化。
- 相关自定义控件工厂注册（`AddonTemplate.java:173-176`）：`ItemTargetSetting.register(); InfoTextSetting.register(); ContainerTypeSetting.register(); ItemQuantitySetting.register();`

### 4.2 点位 / 处理记录 → 自建极简 JSON（手写，非框架 NBT）

- 标点：`config/yiyiaddon/autochest/points/{fileSafeServer}.json`，键 `x/y/z/dim/type`（`ChestPointManager.java:131-136`）。
- 处理记录：`config/yiyiaddon/autochest/{fileSafeServer}.json`，键 `x/y/z/dim/type/t/ver`（`ContainerRecordManager.java:170-177`）。

---

## 5. 容器交互与取货逻辑

> 本节是复刻核心。取货逻辑**不在任何 task 类**（autochest 无 task 包），全部在 `service/ChestInteractionService.java`，由 `fsm/AutoChestStateMachine.java` 逐状态驱动。

### 5.1 底层调用点（逐条 `文件:行号`）

**开箱发包（`FarmPacketOps.interactBlock`）**
- 调用点唯一：`ChestInteractionService.java:126`：

```java
return FarmPacketOps.interactBlock(InteractionHand.MAIN_HAND, pos, Direction.UP);
```
- 实现：`farm/FarmPacketOps.java:122-145`（内部 `startPredicting → retainKnownServerState → currentSequence → ServerboundUseItemOnPacket`，`:137-141`）。注意 `face = Direction.UP`（`ChestInteractionService.java:126`）。

**容器同步层（`ContainerBroker`）**
- 字段持有：`ChestInteractionService.java:70` `private final ContainerBroker broker = new ContainerBroker();`
- `broker.tick()`：`ChestInteractionService.java:88`
- `ContainerBroker.openMenu()`：`ChestInteractionService.java:112`、`:131`、`:181`、`:221`
- `ContainerBroker.closeContainer()`：`ChestInteractionService.java:141`
- `broker.isReady()`：`ChestInteractionService.java:136`
- 实现：`farm/ContainerBroker.java:87-96`（`openMenu`，`containerId == 0` 视为未开箱）、`:62-76`（`tick` 观测 stateId 稳定，需连续 3 tick，`:32`）、`:226-234`（`closeContainer`）。

**槽位操作（`handleContainerInput`）**
- 整叠 shift：`ChestInteractionService.java:319`（`ContainerInput.QUICK_MOVE`，button 0）
- 精确取物拿整叠：`ChestInteractionService.java:349`（`ContainerInput.PICKUP`，button 0）
- 精确取物逐格放 1：`ChestInteractionService.java:371`（`ContainerInput.PICKUP`，button 1）
- 精确取物放回剩余：`ChestInteractionService.java:386`（`ContainerInput.PICKUP`，button 0）

**Baritone（仅 `service/PathingService.java`）**
- `PathingService.java:69` `baritone.getCustomGoalProcess().setGoalAndPath(new GoalBlock(stand));`
- `PathingService.java:72` `baritone.getCustomGoalProcess().setGoalAndPath(new GoalTwoBlocks(chestPos));`
- `PathingService.java:85` `baritone.getPathingBehavior().isPathing()`
- `PathingService.java:96-97` `cancelEverything()` + `setGoal(null)`
- `PathingService.java:126` `BaritoneAPI.getProvider().getPrimaryBaritone()`
- 站位计算 `computeStandPosition` `:39-54`（同层→下一层→上一层，四水平方向），可站立判据 `isStandable` `:111-122`。

**KillAura：无。** 全 `autochest` 包无任何 KillAura 相关调用点。

### 5.2 取货主流程（逐步拆解）

1. **扫描附近容器**：`ContainerScanner` 分帧扫描（预算 `BUDGET_PER_TICK = 512`，`scan/ContainerScanner.java:29`），按 Block 命中注册类型（`:152-157`），产出稳定快照 `results()`（`:115-117`）。由 `AutoChestModule.onTick` 按 `scanInterval` 触发 `requestScan`（`:253-256`）+ 每 tick `tick`（`:258`）。
2. **选目标**：`AutoChestStateMachine.selectTarget(mode)` `:255-280`；标点模式用 `pointManager.pointsInCurrentDimension()`（`:262`），玩家控制模式按触发距离取最近（`:267-276`），寻路模式用 `selector.select(...)`（`:279`）。`ContainerSelector.select` `scan/ContainerSelector.java:32-41`（过滤非当前维度 + `records.isProcessed` + 最近优先）。
3. **锁目标与校验**：`SELECT_TARGET` `:305-321` → `LOCK_TARGET` `:323-331`；`validLocked()` `:232-252`（维度/存在性/已处理/多人保护）。
4. **移动/等待**：玩家控制 `WAIT_DISTANCE` `:333-351`（等 `distSqr <= trigger²`）；寻路/标点 `CALCULATE_STAND_POSITION` `:353-370`（`pathingService.pathTo`）→ `PATHING` `:372-389`（`isAdjacent` 判达）。
5. **开箱**：`ARRIVED` `:391-399`（等视角 `viewAlignedTo` 或超时）→ `OPENING` `:401-413`（`interactionService.isOpen()` / `open(pos)`，超时 `fail`）→ `READING_SLOTS` `:415-426`（等 `isSynced`）。
6. **判定目标物品**：`MATCHING` `:428-442` 调 `interactionService.matchOnce(withdrawMode)`；`findTarget` `ChestInteractionService.java:243-274` 遍历容器侧真实槽位，`ItemIdentityMatcher.matchTarget`（`:254`）命中后按模式算 `takeCount`（`:258-264`）；背包放不下标记 `full[0]`（`:267-270`）。
7. **取出**：`TAKING` `:444-460` 调 `interactionService.withdrawOnce(withdrawMode)`：
   - 精确会话优先 `tickPrecise`（`ChestInteractionService.java:186-188`、`:354-382`）；
   - 未同步/冷却中返回 `WITHDREW`（`:191-192`）；
   - 正常：`findTarget` → 整叠不超量走 `quickMove`（`:202-203`），单叠超量走 `startPrecise`（`:205`）。
8. **校验**：`VERIFYING` `:462-476`（容器已关→`CLOSING`；仍开则 `matchOnce` 复核）。
9. **关箱 → 记录**：`CLOSING` `:478-481`（`interactionService.close()`）→ `COMPLETED` `:483-491`（`recordManager.markProcessed(...)` → 释放锁 → `COOLDOWN`）。
10. **卸载/补货**：autochest **不做**卸载/补货；这些属于 `autofarm/task/{UnloadTask,RestockTask}.java`，与本模块无关。

### 5.3 取物三模式（`withdrawMode`）

`findTarget` `ChestInteractionService.java:243-274` 逐字关键分支：
- `TAKE_ALL`：`targets = null`，不匹配（`:244-245`、`:253-255`），`takeCount = stack.getCount()`（`:258`）。
- `TARGET_COUNT`：`settings.itemQuantities.quantityOf(hit.identityKey())` 与 `countPlayerHas` 求差额（`:259-263`），`need <= 0` 跳过。
- `TARGET_EMPTY`（其它模式）：命中即整叠。
- 结果枚举 `Result` `:38-45`（`WITHDREW/FINISHED/INVENTORY_FULL`）；`MatchResult` `:48-55`（`TAKABLE/DONE/INVENTORY_FULL`）。

精确取物会话 `PreciseWithdraw` `:325-338`（字段 `sourceSlot/need/placed/carried/phase`，`Phase { PLACE, RETURN }`），`startPrecise` `:341-351`，`tickPrecise` `:354-382`，`returnRemainder` `:385-389`，`findPlaceSlot` `:392-405`。

背包空间判定 `canFit` `:287-301`（含关键注释：**不要用 `Inventory#getSlotWithRemainingSpace`**，空槽会被判成 -1）；`countPlayerHas` `:304-315`（含副手，`mc.player.getOffhandItem()`）。

### 5.4 与 ID 数据源的对接点

| 对接点 | 行号 | 说明 |
|---|---|---|
| `targetItems.selectedIdentities()` | `ChestInteractionService.java:244-245` | 取目标身份集合（TAKE_ALL 时为 null） |
| `ItemIdentityMatcher.matchTarget(stack, targets)` | `ChestInteractionService.java:254` | 命中身份（实现 `itemid/ItemIdentityMatcher.java:32-38`） |
| `hit.identityKey()` | `ChestInteractionService.java:260` | 回查数量键 |
| `identity.matches(stack)` | `ChestInteractionService.java:309` | 背包/副手计数（实现 `itemid/ItemIdentity.java:201-207`） |
| `ItemIdentity` 类型 | `ChestInteractionService.java:58`、`:244`、`:253`、`:304` | `TakeTarget` record 持有身份 |
| `ItemQuantityScreen` 消费身份 | `ui/ItemQuantityScreen.java:40`、`:47` | 数量配置界面 |
| 注册监听 | `AutoChestModule.java:108` | ID 变更联动 |

> 无 `ItemIdManager` 直调；`ChestInteractionService` 只经 `settings.targetItems`（`ItemTargetSetting`）间接回查 `ItemIdManager.findByKey`（`itemid/ItemTargetSetting.java:66-73`）。

---

## 6. 处理记录与落盘

### 6.1 处理记录（`service/ContainerRecordManager.java`）

- 目录常量 `:25`：`private static final Path CONFIG_DIR = Paths.get("config", "yiyiaddon", "autochest");`
- 数据文件 `:39-41`：`CONFIG_DIR.resolve(WorldIdentity.fileSafeServer(mc) + ".json")`
- 记录字段（`model/ContainerRecord.java:33-52`）：`server`、`dimension`、`pos`、`containerType`、`status`（枚举 `PROCESSED("已处理")`，`:18-31`）、`processedAt`、`dataVersion`。
- 落盘 JSON 键（`ContainerRecordManager.java:170-177`）逐字：

```java
sb.append("  {\"x\":").append(r.pos().getX())
  .append(",\"y\":").append(r.pos().getY())
  .append(",\"z\":").append(r.pos().getZ())
  .append(",\"dim\":\"").append(esc(r.dimension()))
  .append("\",\"type\":\"").append(esc(r.containerType()))
  .append("\",\"t\":").append(r.processedAt())
  .append(",\"ver\":").append(r.dataVersion())
  .append('}');
```

- 接口：`reload`(`:44`)、`isProcessed`(`:78`)、`markProcessed`(`:93`，先 `remove` 再 `add` 后 `save`)、`find`(`:101`)、`invalidate`(`:111`)、`clear`(`:122`)、`clearDimension`(`:128`)、`clearAllServers`(`:137`)、`size`(`:157`)。
- 过期判定 `model/ContainerRecord.java:99-102`：`expireMs <= 0` 立即过期；类型不符则失效旧记录（`ContainerRecordManager.java:85-88`）。

### 6.2 清除接口与调用方

| 清除接口 | 行号 | 调用方 |
|---|---|---|
| `clearDimension(dim)` | `:128` | `AutoChestModule.java:503`（GUI「清除当前维度处理记录」） |
| `clearAllServers()` | `:137` | `AutoChestModule.java:509`（GUI「清除全部处理记录」） |
| `invalidate(pos, dim)` | `:111` | `AutoChestModule.java:283`；`fsm/AutoChestStateMachine.java:235`、`:336`、`:356` |

### 6.3 `yiyiaddon-reports/`

**本模块无 `yiyiaddon-reports/` 落盘。** 全仓 `yiyiaddon-reports` 仅出现在自动附魔模块：`enchant/AutoEnchantBook.java:453`、`enchant/report/GearCraftReport.java:26/203/207`。自动箱子只写 `config/yiyiaddon/autochest/`（记录）与 `config/yiyiaddon/autochest/points/`（标点）。

---

## 7. 自检（`selfCheck()`）

- 存在，为 `AutoChestModule` 的**私有方法**：`AutoChestModule.java:185` `private List<String> selfCheck()`。
- 调用点 `:169`：`if (!reportSelfCheck(selfCheck())) return;`（`reportSelfCheck` 在基类 `core/YiyiaddonModule.java:147-161`：非空则 `notifyError("还差 " + missing.size() + " 项没配好，配完再开：")` 并逐条输出，同时 `mc.execute` 延后关闭模块）。
- 判定条件与缺项文案（逐字，`AutoChestModule.java`）：
  - `:189-191` 容器类型为空：

```java
missing.add("§a容器类型§f·未选择任何可识别的容器");
```
  - `:194-197` 非 TAKE_ALL 且目标为空：

```java
missing.add("§a目标物品§f·当前取物模式需要目标物品，但未选择任何目标");
```
  - `:200-202` 标点模式且无点位：

```java
missing.add("§a标点模式§f·尚未保存任何点位（用 .autochest 添加 指令添加）");
```
  - `:205-208` 背包无容量：

```java
missing.add("§a背包容量§f·36 格全满且无未满堆叠，取物会立刻停机（先清出空位）");
```
- 辅助方法 `hasInventoryRoom()` `:219-228`（只统计下标 0~35，空槽或未满可堆叠即 true）。
- 实现方式：**私有方法 + `List<String>` 收集**（非独立类 / 非枚举驱动），逐条 `if` 判定，一次性返回全部缺项。

---

## 8. 外部依赖清单

### 8.1 对本项目其余模块的依赖（逐条 `文件:行号`）

| 依赖目标 | 引用点 |
|---|---|
| `itemid.ItemIdManager` | `AutoChestModule.java:21`(import)、`:70`(字段)、`:93`(构造参数)、`:95`、`:108`、`:164` |
| `itemid.ItemTargetSetting` | `config/AutoChestSettings.java:6`、`:62`、`:191-195`；`config/ItemQuantitySetting.java:4`、`:31` |
| `itemid.ItemIdentity` | `service/ChestInteractionService.java:7`、`:58`、`:244`、`:253`、`:304`；`ui/ItemQuantityScreen.java:4`、`:40`、`:47` |
| `itemid.ItemIdentityMatcher` | `service/ChestInteractionService.java:8`、`:254` |
| `farm.ContainerBroker` | `service/ChestInteractionService.java:5`、`:70`、`:88`、`:112`、`:131`、`:141`、`:181`、`:221` |
| `farm.FarmPacketOps` | `service/ChestInteractionService.java:6`、`:126` |
| `core.AddonTemplate` | `AutoChestModule.java:19`、`:94`（`CATEGORY_ASSIST`） |
| `core.YiyiaddonModule` | `AutoChestModule.java:20`（`extends`）、`:441`/`:485`（`formatCoords`）；`fsm/AutoChestStateMachine.java:8`、`:642` |
| `core.world.WorldContextFormatter` | `model/WorldIdentity.java:92`（`serverName`）、`:107`（`dimensionDisplayName`→`dimensionSummary`） |
| `resourcepack.ServerResourceService` | `command/AutoChestCommand.java:9`——**仅 import，文件内无任何使用 → 死 import** |
| `core.CommandMessageFormatter` | `command/AutoChestCommand.java:8`（仅指令层使用） |

反向被依赖（别人用 autochest）：
- `ui/ConfirmScreen`：`stardew/StardewFarmModule.java:3`、`stardew/ui/StardewConsoleScreen.java:3`、`itemid/IdConfigModule.java:4`
- `config/InfoTextSetting`：`itemid/IdIdentifyModule.java:3`
- `model/WorldIdentity`：`itemid/IdConfigModule.java:3`、`worldblock/BlockIdentifier.java:4`

### 8.2 第三方框架 API（`meteordevelopment.*`，仅记录不复刻）

`systems.modules.Module/Category/Modules`、`settings.{Settings,SettingGroup,Setting,EnumSetting,IntSetting,BoolSetting,ColorSetting,StringListSetting,IVisible}`、`gui.{GuiTheme,WidgetScreen,WindowScreen,renderer.GuiRenderer,utils.SettingsWidgetFactory,widgets.*}`、`events.world.TickEvent`、`events.render.Render3DEvent`、`renderer.ShapeMode`、`utils.render.color.{Color,SettingColor}`、`orbit.EventHandler`、`utils.Utils.getFileWorldName`、`commands.Command`、`addons.MeteorAddon`。
（引用点分布在 `AutoChestModule.java:22-28`、`config/*.java`、`render/AutoChestRenderer.java:5-6`、`fsm/AutoChestStateMachine.java:28`、`command/AutoChestCommand.java:10-11`、`core/AddonTemplate.java:63-70`。）

### 8.3 静态可变状态 / 单例 / 缓存

- `model/ContainerTypeRegistry.java:26`：`private static final List<ContainerType> TYPES = new ArrayList<>();`（静态注册表，`static {}` 块注册 5 类，`:28-50`；`register` 重复 id 忽略 `:57-61`）。跨线程只读消费。
- `service/ChestInteractionService.java:70`：`private final ContainerBroker broker`（实例级状态 `lastStateId/stableTicks`，`ContainerBroker.java:49-50`）。
- `AutoChestStateMachine.cooldowns`（`:142`）：实例级 `HashMap` 临时冷却表。
- 无全局单例；`ItemIdManager` 由 `AddonTemplate.java:168` 单实例注入。

---

## 9. 复刻要点与坑

### 9.1 必须原样保留的关联

1. **服务器/维度隔离口径**：逻辑键格式（多人 `host:port` 小写、单人 `singleplayer:<存档目录名>`）、文件名前缀（`server_` / `singleplayer_`）、落盘路径 `config/yiyiaddon/autochest/{fileSafeServer}.json` 与 `.../points/{fileSafeServer}.json`，以及 JSON 键 `x/y/z/dim/type/t/ver` 必须逐字一致，否则老档无法读取。证据：`WorldIdentity.java:27-88`、`ChestPointManager.java:25/38-40/131-136`、`ContainerRecordManager.java:25/39-41/170-177`。
2. **容器同步三重校验**：`containerId != 0` + `stateId` 连续稳定 3 tick（`ContainerBroker.java:32/79-96`）——抢跑发包会被服务端判幽灵物品回滚。
3. **开箱发包细节**：`FarmPacketOps.interactBlock(MAIN_HAND, pos, Direction.UP)`（`ChestInteractionService.java:126`），sequence 取号流程不可省（`FarmPacketOps.java:122-145`）。
4. **取物三模式语义与差额算法**：`TARGET_COUNT` 按 `quantityOf(identityKey) - countPlayerHas`（含副手）；`canFit` 不能用 `Inventory#getSlotWithRemainingSpace`（`ChestInteractionService.java:277-301` 注释是血泪坑）。
5. **槽位归属判据**：`slot.container != inventory` 判容器侧，`Inventory.isHotbarSlot` 分快捷栏/主背包（`ChestInteractionService.java:164-167`），禁止写死槽位号。
6. **状态机常量与冷却/有限重试**：`OPEN_TIMEOUT_TICKS=60`、`MOVE_TIMEOUT_TICKS=200`、`maxRetries`、`cooldownTicks`、多人保护不写「已处理」（`AutoChestStateMachine.java:86-106`、`:232-252`、`:515-544`）。
7. **启动自检 4 条缺项文案**（第 7 节）与「一次列全」机制（`AutoChestModule.java:185-211`）。
8. **三态 ESP 着色**：处理中黄色优先于已处理红色（`AutoChestRenderer.java:52-61`）。

### 9.2 死代码 / 未接线

- `adapter/AutoChestAdapter.java`（31 行）：接口无实现、无调用，全仓仅 `AutoChestAdapter.java:12` 自引用 → **死代码**，可复刻时丢弃或保留占位。
- `command/AutoChestCommand.java:9` `import ...resourcepack.ServerResourceService;`：**未使用 import**（死 import）。
- `PathingService.isContainerAt`（`:104-108`）：未被本类以外调用（状态机用的是自己的 `isContainerAt`），属冗余 API。

### 9.3 依赖新项目当前**还没有**的能力（会卡住迁移）

> 新项目 `d:\mcaddon\yiyiaddon` 已具备部分等价物，但下列能力缺失或形态不同：

| 旧依赖 | 新项目现状（证据） | 影响 |
|---|---|---|
| **Baritone 寻路服务**（`PathingService`：`GoalBlock`/`GoalTwoBlocks`/`setGoalAndPath`/`isPathing`） | 新项目对 baritone 的引用**只在指令翻译与 Mixin**（`integration/baritone/BaritoneChatTranslations.java:285` 等、`mixin/baritone/*.java`），全仓无 `baritone.api.pathing` / `customGoalProcess` / `IBaritone` 使用（grep `import baritone` 命中均为 command 包） | **卡住**：`寻路模式(PATHING)` 与 `标点模式(MARKER)` 的自动导航无法复刻，直到新增 Baritone API 寻路封装。玩家控制模式不受影响 |
| **Meteor 设置框架**（`Settings`/`SettingGroup`/`SettingsWidgetFactory`/`StringListSetting`） | 新项目 `core/module/Module.java` 走 `loadSettings(JsonObject)/saveSettings(JsonObject)`（`:142-147`）+ 自研 `ui/widget/Setting*.java`；无 `Settings.createGroup` | **形态不同**：`AutoChestSettings` 的分组/可见性/自定义控件逻辑需改写为新框架；`ItemQuantitySetting` 的 NBT `"value"` 键不再适用（新项目用 JSON，可参考 `IdentityTargetConfig` 的 `yiyiaddon-identity-targets.json`） |
| **Meteor `Render3DEvent` + `ShapeMode` + `SettingColor`** | 新项目有自研 `ui/render/world/{EspRenderer,ShapeMode,EspColor}.java` | **形态不同**：ESP 渲染需改接新渲染管线 |
| `meteordevelopment...WidgetScreen/WindowScreen` + `ui/ConfirmScreen` | 新项目有 `ui/screen/ConfirmPanelScreen.java`、`ui/page/*` | **形态不同**：确认窗/选择屏需沿用新 UI 体系 |
| `worldblock.BlockIdentifier` 反依赖 `WorldIdentity` | 新项目 `platform/world/WorldIdentity.java` 已存在（含 `server()`/`fileSafeKey()`/`dimension()`/`dataVersion()`，`:36-129`） | **已具备**，可直接用 |
| `farm.ContainerBroker`（stateId 同步 + openMenu/closeContainer/quickMove） | 新项目 `platform/container/ContainerAccess.java`（`openMenu/closeContainer/quickMove/countInContainer`）+ `service/container/ContainerService.java`（`tick/isReady/depositOne/withdrawOne`） | **已具备**（等价，STABLE_TICKS_REQUIRED=3 一致） |
| `farm.FarmPacketOps.interactBlock` | 新项目 `platform/network/BlockPacketSender.java:121-129` `interactBlock(hand,pos,face)` | **已具备**（等价） |
| `itemid.ItemIdManager`（实例 + `addListener/findByKey/reload`） | 新项目 `service/identity/IdentityService.java`（**单例** `shared()` `:48-60`，`addListener` `:93`，`findItem` `:107`，`reload` `:87`） | **已具备**（注意：旧为实例注入，新为单例，接线方式需调整） |
| `itemid.ItemTargetSetting`（`selectedIdentities/pruneInvalid`） | 新项目 `config/identity/IdentityTargetConfig.java`（`selectedItems(service)` `:113`、`pruneInvalid(service)` `:120`，全部 static） | **已具备**（注意 static 与实例差异） |
| `itemid.ItemIdentity.matches` / `ItemIdentityMatcher.matchTarget` | 新项目 `model/identity/ItemIdentity.java:174`（`identityKey`）、`platform/identity/ItemIdentityMatcher.java:30-44`（`matchTarget`/`matches`） | **已具备**（注意新 `matchTarget` 用 `ItemIdentifier.coreKeyOf` 比较，旧 `ItemIdentity.matches` 比较 `stack.getItem()` 引用 + coreKey，语义需核对） |
| `core.YiyiaddonModule.selfCheck/reportSelfCheck` 机制 | 新项目 `core/module/Module.java:119` `public List<String> selfCheck()`（返回缺项中文列表，运行时统一播报） | **已具备**（框架级，直接返回 List 即可） |

**唯一硬性卡点：Baritone 运行期寻路 API 尚未在新项目接线**（构建层有 `maven-repo/baritone/baritone-fabric/1.18.0`，但无运行时代码使用 `baritone.api.pathing.*`）。复刻 `PathingService` 前必须先补齐该能力，否则寻路/标点两模式无法工作。
