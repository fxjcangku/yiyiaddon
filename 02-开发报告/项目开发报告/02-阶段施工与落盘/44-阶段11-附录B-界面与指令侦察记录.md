# 44 · 阶段11 · 附录B · 界面与指令侦察记录

日期：2026-09-16
状态：**批次1 产物之二；逐字取自旧项目源码，供批次2~5 施工直接引用**
配套：`43-阶段11-附录A-逻辑层侦察记录.md`（逻辑层）

> 取证口径：带 `文件:行号`；标「**亲核**」的由主施工亲读源核实，其余为子代理取证，施工时逐字复核。
> 铁律：**文案、指令名、回执一个字不改**；旧壳界面**不做一比一交互复刻**，改控制台（开发习惯 181/182/183）。

---

## 一、`.fumo` 指令树（`command/FumoCommand.java`，292 行）

### 1.1 注册结构（亲核）

```
super("fumo", "自动附魔坐标设置指令")          // :36
MODULE_NAME = "自动附魔"                        // :39

.fumo 设置 <节点>     // :46 容器，本身无 executes
.fumo 移除 <节点>     // :59 容器，本身无 executes
.fumo 状态            // :72-75 → printStatus()
.fumo 清空            // :77-80 → clearPoints()
```
**全树无 `ArgumentType` 参数节点**，全是硬编码 literal。

### 1.2 9 个点位节点 → PointType（设置 :47-55 / 移除 :60-68 完全对称）

| 节点（逐字） | PointType |
| --- | --- |
| `书` | `BOOK_STORAGE` |
| `青晶石` | `LAPIS_STORAGE` |
| `工具护甲箱` | `EQUIPMENT_STORAGE` |
| `附魔台` | `ENCHANTING_TABLE` |
| `砂轮` | `GRINDSTONE` |
| `铁砧` | `ANVIL` |
| `铁砧箱` | `ANVIL_BOX` |
| `挂机位` | `AFK` |
| `成品箱` | `OUTPUT_STORAGE` |

**注意三处不一致，一律照抄、不许「顺手统一」**：
- 节点是 `工具护甲箱`，而点位显示名是 `工具/护甲箱`；使用说明正文写的是「装备箱」（`AutoEnchantBook.java:793`）。
- 节点是 `青晶石`，显示名是 `青金石箱`。
- 节点是 `挂机位`，显示名是 `挂机点`。

### 1.3 `设置` 分支回执（逐字，经 `CommandMessageFormatter`）

成功（`:126-132`）：标题 `已设置<点位title>点位`（例 `已设置空白书箱点位`），字段顺序 `world()` 环境字段 → `维度` → `坐标` → `类型`，状态 `成功 ▸ 已保存`。
副产物：挂机位额外记 `hangoutYaw = player.getYRot(); hangoutPitch = player.getXRot();`（`:115-118`）；铁砧额外记 `posAnvilFacing = level.getBlockState(pos).getValue(AnvilBlock.FACING)`（`:119-122`）；成功后 `Modules.get().save()`（`:124`）。

失败分支全部标题 `设置点位失败`，`原因` 逐字：

| 行号 | 原因文案 |
| --- | --- |
| `:89-92` | `当前为 [<模式>] 模式，无需绑定 [<点位title>] 点位，请切换到对应模式后再设置` |
| `:95-98` | `[<点位title>] 已设置，请先执行 .fumo 移除 <节点> 后再重新设置` |
| `:106-108` | `请将准星对准目标方块` |
| `:234` | `准星指向的方块不是附魔台` |
| `:239` | `准星指向的方块不是砂轮` |
| `:244` | `准星指向的方块不是铁砧` |
| `:248` | `[<点位title>] 必须设置为箱子、木桶或潜影盒容器` |
| `:256` | `无法识别当前服务器或维度，点位未保存` |
| `:265` | `旧版点位没有世界绑定，请先执行 .fumo 清空 后重新设置` |
| `:269` | `现有点位属于其他服务器或维度，请切回原世界，或执行 .fumo 清空 后重设` |
| `:226` | 标题 `模块未就绪`，原因 `找不到 自动附魔 模块，请确认已注册` |

### 1.4 `移除` 分支（`:136-166`）

标题 `已删除<点位title>点位`（例 `已删除挂机点位`），状态 `成功 ▸ 已删除`；`coord` 仅当原点位非 null 才追加。**无失败回执**（模块为 null 直接 return；删不存在的点位同样报成功）。挂机位连带清 `hangoutYaw/hangoutPitch`（`:143-146`），铁砧连带清 `posAnvilFacing`（`:147-149`）；删光后清 `pointServer/pointDimension` 并二次存档（`:161-165`）。

### 1.5 `状态`（`:202-220`）

标题 `点位状态`，`.world()`，然后**遍历全部 9 个点位**（不是只遍历当前模式所需）：

```java
formatter.field(type.title(), pos == null
    ? "§c未设置"
    : "§7X§f" + pos.getX() + " §7Y§f" + pos.getY() + " §7Z§f" + pos.getZ());
```
末行状态二选一（`:214-218`）：`当前世界与点位匹配`（SUCCESS）/ `当前世界与点位不匹配`（WARNING）。
判据（`AutoEnchantBook:1691-1693`）：`Objects.equals(pointServer, currentServer()) && Objects.equals(pointDimension, currentDimension())`。

### 1.6 `清空`（`:168-178` → `AutoEnchantBook:1701-1716`）

标题 `已清空全部点位`，字段 `范围` → `全部点位 + 挂机视角 + 服务器维度绑定`，状态 `成功 ▸ 已清空`（**不调 `.dimension()`**）。
实际清 14 个字段：`posBook / posLapis / posOutput / posEnchant / posGrindstone / posHangout / posAnvil / posAnvilBox / posAnvilFacing / posEquipment / hangoutYaw / hangoutPitch / pointServer / pointDimension`。

### 1.7 GUI 复用入口

`FumoCommand.setPoint(PointType)` / `removePoint(PointType)`（`:189-200`）**与指令共用同一套校验**——新项目控制台按钮必须调同一处实现（点位绑定/移除/校验唯一实现）。

---

## 二、点位体系（`point/PointType.java`，70 行）—— 亲核

```java
BOOK_STORAGE("空白书箱", "书"),
LAPIS_STORAGE("青金石箱", "青晶石"),
EQUIPMENT_STORAGE("工具/护甲箱", "工具护甲箱"),
ENCHANTING_TABLE("附魔台", "附魔台"),
GRINDSTONE("砂轮", "砂轮"),
ANVIL("铁砧", "铁砧"),
ANVIL_BOX("铁砧箱", "铁砧箱"),
AFK("挂机点", "挂机位"),
OUTPUT_STORAGE("成品箱", "成品箱");
```
字段仅 `title` / `node`；方法 `title()`（44-46）、`node()`（49-51）、`toString()=title`（53-56）、`fromNode(String)`（59-64）、`all()`（67-69）。**无 tooltip / 图标 / 排序字段**。
图标与配色在 `AutoEnchantBook.buildPointCard` 的 `titleColor`（`:667-677`）：书箱`§a` / 青金石`§9` / 工具护甲`§b` / 附魔台`§d` / 砂轮`§7` / 铁砧`§6` / 铁砧箱`§e` / 挂机`§c` / 成品箱`§6`；卡片图标统一 `Items.ENCHANTED_BOOK`。

### 2.1 `requiredPoints(mode)`（`AutoEnchantBook:759-770`，唯一实现）

```java
case GEAR -> List.of(EQUIPMENT_STORAGE, LAPIS_STORAGE, ENCHANTING_TABLE, GRINDSTONE,
                     ANVIL, ANVIL_BOX, AFK, OUTPUT_STORAGE);            // 8 个
case BOOK, CUSTOM -> List.of(BOOK_STORAGE, LAPIS_STORAGE, OUTPUT_STORAGE,
                             ENCHANTING_TABLE, GRINDSTONE, AFK);        // 6 个
```

三处过滤必须一致：指令门禁（`FumoCommand:89-92`）、启动自检（`AutoEnchantBook:1760-1771`）、控制台点位页（`AutoEnchantBook:613`）。

### 2.2 启动自检（`:1760-1772`，逐字）

```java
List<String> missing = new ArrayList<>();
TargetMode mode = 目标模式.get();
for (PointType type : requiredPoints(mode)) {
    if (type == PointType.AFK && 当前运行模式() == RunMode.DRAIN) continue;
    if (getPointPos(type) == null) missing.add("§6" + type.title() + "§f·未绑定");
}
```
播报（`core/YiyiaddonModule.reportSelfCheck:147-161`）：`还差 N 项没配好，配完再开：` + 每项 `§6  i. §f<项>`；同时 `mc.execute` 延后 `toggle()` 关模块。
**颜色码叠加（`§f§6`）是原文如此，照抄不要去重。**

### 2.3 落盘字段（`:350-363`，14 个 public，键名见 §3.1）

`posBook / posLapis / posOutput / posEnchant / posGrindstone / posHangout / posAnvil / posAnvilBox / posAnvilFacing(Direction) / posEquipment / hangoutYaw / hangoutPitch / pointServer / pointDimension`
落盘键（`toTag:1621-1639` / `fromTag:1641-1659`，**键名逐字**）：`posBook, posLapis, posOutput, posEnchant, posGrindstone, posHangout, posAnvil, posAnvilBox, posAnvilFacing, posEquipment, hangoutYaw, hangoutPitch, pointServer, pointDimension`（坐标存 `int[3]`，朝向存 `Direction.name()`）。

---

## 三、设置体系（18 组 / 174 个 Setting）

### 3.1 18 个分组（`:94-111`，逐字）

| # | 字段 | 分组名 | # | 字段 | 分组名 |
| --- | --- | --- | --- | --- | --- |
| 1 | sgBasic | 基础设置 | 10 | sgVanillaArmor | 原版防具附魔 |
| 2 | sgAdvanced | 自动附魔分类 | 11 | sgVanillaMelee | 原版近战附魔 |
| 3 | sgCustom | 自定义附魔 | 12 | sgVanillaTool | 原版工具附魔 |
| 4 | sgVanilla | 原版附魔分类 | 13 | sgVanillaBow | 原版弓附魔 |
| 5 | sgSword | 剑附魔属性 | 14 | sgVanillaFishing | 原版钓竿附魔 |
| 6 | sgAxe | 斧头附魔属性 | 15 | sgVanillaTrident | 原版三叉戟附魔 |
| 7 | sgBow | 弓附魔属性 | 16 | sgVanillaCrossbow | 原版弩附魔 |
| 8 | sgArmor | 护甲附魔属性 | 17 | sgVanillaCommon | 原版通用附魔 |
| 9 | sgOther | 工具与通用附魔属性 | 18 | sgGear | 原版装备附魔 |

### 3.2 分组可见性与顺序（`:564-585`、`:634-649`，逐字核心）

- 构造期 `settings.groups.remove(...)` **13 次**（`:571-583`）：移除 `sgSword / sgAxe / sgBow / sgArmor / sgOther / sgVanillaArmor / sgVanillaMelee / sgVanillaTool / sgVanillaBow / sgVanillaFishing / sgVanillaTrident / sgVanillaCrossbow / sgVanillaCommon` —— 这些组**只作 selector 的 backing 数据源**，不在顶层显示。
- `sectionExpanded`：`sgAdvanced=true; sgVanilla=false; sgGear=true; sgSword/sgAxe/sgBow/sgArmor=false`（`:564-570`）。
- **幂等保护**（`:643-648`，逐字，必须保留）：

```java
if (settings.groups.equals(ordered)) return;
settings.groups.clear();
settings.groups.addAll(ordered);
```
> 注释原文：顺序一致时不重建列表；否则 `目标模式.onChanged` 在 `Modules.load / Settings.fromTag` 遍历 `groups` 期间同步清空重建会抛 `ConcurrentModificationException`，**导致启动闪退**。

- `刷新模式界面()`（`:624-628`）：`mc.execute(() -> { if (mc.screen instanceof WidgetScreen s) s.reload(); })`。

| 模式 | 顶层顺序 | 页内多选控件（selector） |
| --- | --- | --- |
| CUSTOM | 基础设置 → 自动附魔分类 → 自定义附魔 | 剑/斧/弓/护甲/工具与通用 附魔属性（5 个，分别 backing 54/5/34/50/13 个 BoolSetting） |
| BOOK | 基础设置 → 原版附魔分类 | 原版防具/近战/工具/弓/钓竿/三叉戟/弩/通用 附魔（8 个，backing 9/7/3/4/2/4/3/1） |
| GEAR | 基础设置 → 原版装备附魔 | 记录合成日志 / 合成策略 / 装备附魔配置 |

### 3.3 基础设置 16 项（`:114-176`，逐字）

| 名称 | 类型 | 默认 | 范围/候选 | 可见条件（逐字） | 描述（逐字） |
| --- | --- | --- | --- | --- | --- |
| `目标模式` | Enum | `BOOK` | `GEAR("原版装备附魔")`/`BOOK("原版附魔书")`/`CUSTOM("自定义附魔")`，`onChanged(m -> {同步模式分组(); 刷新模式界面();})` | 无条件 | 原版装备附魔 / 原版附魔书 / 自定义附魔，三模式互斥切换 |
| `单轮抽取次数` | Int | 10 | 1~100 noSlider | `目标模式.get() != TargetMode.GEAR` | 挂机循环每轮附魔最大次数，纯附魔模式忽略此项 |
| `GUI操作延迟(Tick)` | Int | 1 | 1~10 noSlider | 无条件 | 所有 GUI 点击之间的等待 Tick 数 |
| `发包打开距离` | Int | 6 | 1~32 noSlider | 无条件 | 发包开箱/开附魔台/开砂轮/开铁砧允许的最大距离（格），超出则先寻路靠近 |
| `书本补给组数` | Int | 1 | 1~10 noSlider | `目标模式.get() != TargetMode.GEAR` | 每次去书箱抓取的组数（1组=64本） |
| `青金石补给组数` | Int | 1 | 1~10 noSlider | 无条件 | 每次去青金石箱抓取的组数（1组=64个） |
| `每批取用数量` | Int | 4 | 1~16 noSlider | `目标模式.get() == TargetMode.GEAR` | 每次任务最多从装备箱取用的目标装备数量，铁砧合并会消耗装备，最终完成数可能小于此值 |
| `极品附魔数量` | Int | 1 | 1~64 noSlider | `目标模式.get() == TargetMode.GEAR` | 本次运行最终产出的极品装备数量，达到后自动停机 |
| `装备运行模式` | Enum`RunMode` | `EXPERIENCE` | `DRAIN("纯附魔模式")`/`EXPERIENCE("挂机循环")` | `目标模式.get() == TargetMode.GEAR` | 原版装备附魔：纯附魔只消耗当前经验，不足则停机；挂机循环前往挂机点刷经验 |
| `附魔书运行模式` | Enum`RunMode` | `EXPERIENCE` | 同上 | `目标模式.get() == TargetMode.BOOK` | 原版附魔书：纯附魔只消耗当前经验，不足则停机；挂机循环前往挂机点刷经验 |
| `自定义运行模式` | Enum`RunMode` | `EXPERIENCE` | 同上 | `目标模式.get() == TargetMode.CUSTOM` | 自定义附魔：纯附魔只消耗当前经验，不足则停机；挂机循环前往挂机点刷经验 |
| `ESP标点` | Bool | true | — | `() -> false` | 显示已设置点位的名称 |
| `返回挂机视角` | Bool | true | — | `() -> false` | 到达挂机位后恢复设置该点位时记录的视角 |
| `成功提示音` | Bool | true | — | 无条件 | 达成目标时播放本地提示音（命中附魔书 / 装备达成极品） |
| `成功提示音类型` | Enum`SuccessSound` | `CHALLENGE_COMPLETE` | 12 项（见 §3.4） | `成功提示音.get()` | 选择达成目标时播放的音效 |

### 3.4 `SuccessSound` 12 项（`:512-536` 逐字）

`CHALLENGE_COMPLETE("挑战完成")`、`LEVEL_UP("升级")`、`ENCHANTMENT_TABLE("附魔台")`、`NOTE_PLING("音符叮")`、`BELL("钟声")`、`FIREWORK("烟花")`、`EXPERIENCE("经验球")`、`VILLAGER("村民庆祝")`、`TRIDENT_THUNDER("三叉戟雷鸣")`、`ATTACK_CRIT("暴击")`、`CAT("猫叫")`、`THUNDER("雷声")`。

### 3.5 `原版装备附魔` 组 3 项（`:451-462`、`:449/562-563`）

| 名称 | 类型 | 默认 | 描述（逐字） |
| --- | --- | --- | --- |
| `记录合成日志` | Bool | false | 开启后记录原版装备附魔的完整合成过程，生成彩色 HTML 报告到 .minecraft/yiyiaddon-reports/装备附魔合成/ |
| `合成策略` | Enum`AnvilPlanner.Strategy` | `SAVE_XP` | 铁砧装备+装备合并排序策略：简单=贡献优先、节能=低惩罚+低成本优先、快速=提升优先少步骤，用于对比经验消耗 |
| `装备附魔配置` | `GearEnchantSetting` | 空列表 | 配置原版装备的极品附魔目标（`.visible(目标模式==GEAR)`） |

### 3.6 五大属性组 156 项 BoolSetting（全部 `defaultValue(false)`、无描述、无可见条件）

**剑附魔属性（54，`:180-234`）**：
双刃剑 5 / 背刺 5 / 饕餮 5 / 定身 5 / 干扰 5 / 势破 5 / 折锋 5 / 暗影突袭 5 / 永夜 5 / 灵魂收割 5 / 生死判 5 / 破败 5 / 镇魂 5 / 忍者 5 / 忍术 5 / 狂热 5 / 破釜 5 / 血怒 5 / 跃斩 5 / 退散 5 / 速攻 5 / 首击 5 / 骑士 5 / 法术大炮 5 / 冷血术 5 / 剑卫 5 ｜`// 稀世`（`:206`）｜ 丛刃 5 / 决斗 5 / 利刃 5 / 剑气 5 / 名刀司命 5 / 咒刃 5 / 嗜血 5 / 天谴 5 / 天道 5 / 奥术 5 / 对决咒术 5 / 弑君 5 / 弑魔 5 / 惩戒咒术 5 / 慈悲 5 / 生灵咒术 5 / 终结 5 / 脉冲刃 5 / 蛇吻 5 / 血偿 5 / 钝锋 5 / 涅槃 5 / 心灵感应 5 / 归一 5 / 高傲 5 / 蔑视 5 / 焚天 5 / 讨价还价 5

**斧头附魔属性（5，`:237-241`）**：定身 5 / 涅槃 5 / 心灵感应 5 / 破釜 5 / 跃斩 5

**弓附魔属性（34，`:245-279`）**：
势破 5 / 宣判 5 / 折锋 5 / 暗影突袭 5 / 永夜 5 / 浮尘 5 / 灵魂收割 5 / 生死判 5 / 破败 5 / 禁锢 6 / 镇魂 5 / 忍士 5 / 标记 5 / 狂热 5 / 猎手 5 / 穿颅 5 / 五言 5 / 空军 5 / 首射 5 / 骑射 5 / 鸣踪 5 / 玻璃大炮 5 / 退散 5 ｜`// 稀世`（`:268`）｜ 天道 5 / 弑君 5 / 真三言 5 / 苍劲 5 / 苍穹 5 / 神射手 5 / 创伤 5 / 弓魄 5 / 狂妄 5 / 焚天 5 / 夺金 5

**护甲附魔属性（50，`:283-333`）**：
不懈 5 / 不灭 5 / 临阵脱逃 5 / 光环 5 / 启迪 5 / 圣愈 5 / 均衡之法 5 / 均衡之遁 5 / 幸运 5 / 清风引 5 / 狂潮 5 / 磐石 5 / 祭祀 5 / 虚幻 5 / 血罡 5 / 霜滞 5 / 倔强 5 / 均衡之御 5 / 均衡之攻 5 / 时速 5 / 金钟罩 5 / 钢铁胃 5 ｜`// 稀世`（`:305`）｜ 乾坤 5 / 力场 5 / 圣之守护 5 / 复苏之风 5 / 奥术壁垒 5 / 奥术血统 5 / 招架 5 / 无畏契约 5 / 星穹 5 / 村庄英雄 5 / 物法皆修 5 / 狂骨 5 / 玄煞 5 / 生命源泉 5 / 生命潮汐 5 / 盛宴 5 / 貔貅 5 / 逆鳞 5 / 旺盛 5 / 捍卫 5 / 决战 5 / 混沌 5 / 燃血 5 / 祭血 5 / 重型装甲 5 / 意志 5 / 噬灵 5 / 破格 5

**工具与通用附魔属性（13，`:335-347`）**：
龙行 5 / 海王 5 / 催生 1 / 矿脉 5 / 自我修复 5 / 血契 5 / 地质学家 5 / 破界 5 / 贪欲 5 / 立方 5 / 龙之后裔 5 / 龙脉 5 / 挖金 5

> 名称中的数字是**名称的一部分**（如「定身 5」= 目标等级 5），半角空格分隔；`催生 1`、`禁锢 6` 为真实原文。

### 3.7 动态生成的 33 个原版附魔条目（`:546-553` + `:588-591`）

```java
private void addTargets(SettingGroup group, String... names) {
    for (String name : names) group.add(new BoolSetting.Builder().name(name).defaultValue(false).build());
}
```

| 组 | 条目（逐字，按序） | 数量 |
| --- | --- | --- |
| 原版防具附魔 | 保护 IV / 火焰保护 IV / 摔落缓冲 IV / 爆炸保护 IV / 弹射物保护 IV / 水下呼吸 III / 水下速掘 / 荆棘 II / 深海探索者 III | 9 |
| 原版近战附魔 | 锋利 IV / 亡灵杀手 IV / 节肢杀手 IV / 击退 II / 火焰附加 II / 抢夺 III / 横扫之刃 III | 7 |
| 原版工具附魔 | 效率 IV / 精准采集 / 时运 III | 3 |
| 原版弓附魔 | 力量 IV / 冲击 II / 火矢 / 无限 | 4 |
| 原版钓竿附魔 | 海之眷顾 III / 饵钓 III | 2 |
| 原版三叉戟附魔 | 忠诚 III / 穿刺 V / 激流 III / 引雷 | 4 |
| 原版弩附魔 | 多重射击 / 快速装填 III / 穿透 IV | 3 |
| 原版通用附魔 | 耐久 III | 1 |

> **这些条目无字段引用**，`name` 字符串就是落盘键（`EnchantmentSelectSetting.apply` 用 `name` 写回，`EnchantmentSelectSetting.java:46`）→ **改名=丢配置**。

### 3.8 selector 生成器（`:592-600`，逐字）

```java
private EnchantmentSelectSetting addSelector(String name, SettingGroup source, SettingGroup target, IVisible visible) {
    List<BoolSetting> values = new ArrayList<>();
    for (Setting<?> setting : source) if (setting instanceof BoolSetting boolSetting) values.add(boolSetting);
    List<String> names = new ArrayList<>();
    for (BoolSetting setting : values) names.add(setting.name);
    EnchantmentSelectSetting selector = new EnchantmentSelectSetting(name, names, values, visible);
    target.add(selector);
    return selector;
}
```
**顺序约束**：必须先 `addTargets`（546-553）再 `addSelector`（554-561），否则候选项为空。

### 3.9 15 个多选/自定义设置

13 个 `EnchantmentSelectSetting`：`剑附魔属性`(:541) / `斧头附魔属性`(:542) / `弓附魔属性`(:543) / `护甲附魔属性`(:544) / `工具与通用附魔属性`(:545) / `原版防具附魔`(:554) / `原版近战附魔`(:555) / `原版工具附魔`(:556) / `原版弓附魔`(:557) / `原版钓竿附魔`(:558) / `原版三叉戟附魔`(:559) / `原版弩附魔`(:560) / `原版通用附魔`(:561)；description 统一 `选择需要收集的附魔属性`。前 5 个 `.visible(目标模式==CUSTOM)`，后 8 个 `.visible(目标模式==BOOK)`。

另 2 个：`自定义附魔目标`（`CustomEnchantSetting`，`:145-149`，默认空，`.visible(目标模式==CUSTOM)`，描述 `每行填写一个附魔名称和等级，例如：打雷 5。支持中文、阿拉伯数字、罗马数字和不带等级的附魔。`）、`装备附魔配置`（`GearEnchantSetting`，见 §3.5）。

---

## 四、旧界面三件（**不做一比一复刻，仅取文案**）

### 4.1 `ui/EnchantmentSelectSetting`（91 行，多选控件）

| 文案（逐字） | 控件 | 行号 |
| --- | --- | --- |
| `选择附魔` | 按钮（打开多选窗口） | :56 |
| `已选择 N 项` | 计数标签（`countText()`：`"已选择 " + get().size() + " 项"`） | :57、:88-90 |
| 附魔书图标 + `↻`（`GuiRenderer.RESET`，**非文字**） | 重置按钮 | :59 |

### 4.2 `ui/EnchantmentSelectScreen`（77 行，多选窗口）—— **无额外文案**

- 标题 = 传入设置项 name（`:23`）
- 顶部过滤框 `theme.textBox("")`，**占位符是空串**（`:29`），打开即聚焦（`:30`）
- 左列 = 未选中项 + `＋`（`:45-51`）；右列 = 已选中项 + `－`（`:53-59`）；中间竖分隔线（`:42`）
- **没有**左右列标题、空状态提示、确定/取消按钮、搜索占位符（新项目控制台若要这些文案 = **新增**，须先问）

### 4.3 `gear/GearEnchantScreen`（311 行，装备附魔配置页）

窗口标题 `原版装备附魔配置`（`:44`）。逐字文案：

| 文案（逐字） | 行号 |
| --- | --- |
| `§b§l<装备名>` | :75 |
| `§b合成附魔详细` / `§c删除合成记录` | :84 / :86 |
| `§a已删除 N 份合成报告` / `§8暂无合成报告可删除` | :92 |
| `§7类别` / `§7品质` / `§7类型` / `§7极品方案` | :102 / :122 / :141 / :168 |
| `§c该类别暂无装备` | :118、:137 |
| `§c未找到对应装备` | :154 |
| `§7该装备 §c暂无极品方案§7，仅作为官方装备数据收录。` | :163 |
| `§c已排除` / `§a启用`（`excludable` 时） / `§8核心`（不可排除时） | :211 / :218 |
| `§e` + 罗马数字（`roman()`：1→I…5→V，其余阿拉伯数字） | :227、:301-309 |

三级分类：大类 `工具 / 武器 / 护甲`（`GearCatalog:62-64`）；材质中文 `木/石/铜/铁/金/钻石/下界合金/皮革/锁链/海龟壳`+`特殊`（`GearCatalog:143-156`、`:76`）；类型中文 14 项（`镐/斧/锹/锄/剑/矛/弓/弩/三叉戟/重锤/头盔/胸甲/护腿/靴子`，`GearCatalog:124-141`）。

### 4.4 `ui/CustomEnchantSetting`（51 行）

**零硬编码用户可见文本**（名称/描述由 `AutoEnchantBook` 传入）；用 `StringListSetting.fillTable`（`:39-40`）。

---

## 五、控制台页签划分（照 38 号 §六 补充，已确认）

| 模式 | 页签（顺序） |
| --- | --- |
| GEAR | 概览 · 点位 · 基础设置 · **原版装备附魔** |
| BOOK | 概览 · 点位 · 基础设置 · **原版附魔分类** |
| CUSTOM | 概览 · 点位 · 基础设置 · **自动附魔分类** · **自定义附魔** |

- 页签名**逐字沿用**旧分组名，顺序照 `同步模式分组()` 的 `ordered`
- 13 个 backing 子组在页内用 `FoldSection` 折叠（与挖矿 Baritone 页同做法）
- 「目标模式」分段放「基础设置」页第一行（沿用旧 `sgBasic` 位置）；切模式后**页签立即重排**（整页重建），保留幂等保护语义
- 点位页按 `requiredPoints(mode)` 过滤；切模式不污染、不删除他模式点位
- 「查看使用说明」**必须排在「打开控制台」下面**（开发习惯 182）

---

## 六、使用说明全文（`AutoEnchantBook.buildHelpContent()` `:773-847`，逐字）

窗口标题 `自动附魔 - 使用说明`，底部按钮 `关闭`，空串行渲染为水平分隔线（`ui/HelpScreen.java:28/51/39-42`）。

```text
[标题框]
§8┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
§8┃ §3§l> §b§l使用说明                                      §8┃
§8┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
```

**章节 1｜首次配置**（`:775-783`）
```text
§3[§b#§3] §f首次配置
  §8├─ §f先在要使用的服务器和维度执行 §e.fumo 清空 §7(清空旧点位)
  §8├─ §f准星对准对应方块，点击下方卡片「设置」按钮依次绑定：
  §8│    §7书 / 青晶石 / 成品箱 / 附魔台 / 砂轮
  §8│    §7原版装备模式：装备箱 / 青晶石 / 附魔台 / 砂轮 / 铁砧 / 铁砧箱 / 成品箱
  §8├─ §f挂机循环模式站在刷怪点调好杀怪视角，再绑定「挂机位」 §7(纯附魔模式不需要)
  §8├─ §f在自动附魔分类或原版附魔分类勾选要收集的目标词条
  §8├─ §f把带「横扫之刃」的剑放背包或快捷栏任意位置
  §8└─ §f书箱放空白书、青金石箱放青金石、成品箱预留空间
```

**章节 2｜点位设置 §7(两种方式)**（`:785-796`）
```text
§3[§b#§3] §f点位设置 §7(两种方式)
  §b▸ §e方式1 §8- §f配置页面卡片按钮
    §7准星对准方块 §8→ §f点击对应卡片「设置」
    §7书/青晶石/成品箱：必须是箱子/桶/潜影盒
    §7附魔台/砂轮：必须对准对应方块
    §7挂机位：直接站在目标位置即可绑定 §7(含视角)

  §b▸ §e方式2 §8- §f指令系统
    §8> §3.fumo 设置 <节点> §8— §7节点：书/青晶石/成品箱/附魔台/砂轮/挂机位/装备箱/铁砧/铁砧箱
    §8> §3.fumo 移除 <节点> §8— §7删除单个点位
    §8> §3.fumo 状态 §8— §7查看坐标与当前地点匹配
    §8> §3.fumo 清空 §8— §7清空全部点位
```

**章节 3｜运行流程**（`:798-804`）
```text
§3[§b#§3] §f运行流程
  §a▸ §f物资不足时自动前往对应补给箱取书或青金石
  §a▸ §f经验不足时，挂机循环返回挂机位；纯附魔模式低于 30 级停止
  §a▸ §f横扫之刃剑在主背包时自动换入当前快捷栏并选中
  §a▸ §f达到目标等级后自动前往附魔台执行 30 级附魔
  §a▸ §f未命中目标词条时前往砂轮洗练，再继续下一次附魔
  §a▸ §f命中目标词条时播放提示音并将附魔书存入成品箱
```

**章节 4｜原版装备附魔**（`:806-811`）
```text
§3[§b#§3] §f原版装备附魔
  §b▸ §f从装备箱取目标装备 → 附魔台随机附魔 → 评分判定
  §b▸ §f零命中/禁止/互斥 → 砂轮洗练后重新附魔
  §b▸ §f部分命中 → 保留，多件互补时铁砧合并升级
  §b▸ §f合并后 100% 达标 → 存入成品箱，达到「极品附魔数量」自动停机
  §b▸ §f铁砧损坏 → 自动去铁砧箱取备用铁砧原位补放
```

**章节 5｜参数说明**（`:813-821`）
```text
§3[§b#§3] §f参数说明
  §6▸ §e单轮抽取次数 §f— 附魔书/自定义模式每轮计划执行的附魔次数
  §6▸ §eGUI操作延迟 §f— 服务器卡顿或吞点击时适当调大
  §6▸ §e发包打开距离 §f— 发包开箱/开附魔台/开砂轮/开铁砧的最大距离，超出先寻路靠近
  §6▸ §e书本/青金石补给组数 §f— 每次补给希望保有的组数
  §6▸ §e每批取用数量 §f— 原版装备模式每次从装备箱取用的目标装备数量
  §6▸ §e极品附魔数量 §f— 原版装备模式最终产出的极品装备数量，达标自动停机
  §6▸ §e运行模式 §f— 每个目标模式独立开关：纯附魔只消耗当前经验、不足停机；挂机循环自动补经验
  §6▸ §e成功提示音 §f— 命中目标词条时播放所选音效
```

**章节 6｜原版附魔分类**（`:823-827`）
```text
§3[§b#§3] §f原版附魔分类
  §7· 仅收录普通书通过附魔台随机附魔可获得的词条
  §7· 每种词条只提供附魔台实际能刷出的最高等级
  §7· 锋利/效率/力量等书本附魔最高 IV，不显示无法直接刷出的 V
  §7· 不含经验修补、冰霜行者、灵魂疾行、迅捷潜行和诅咒
```

**章节 7｜换服、换维度与指令**（`:829-833`）
```text
§3[§b#§3] §f换服、换维度与指令
  §d▸ §f一套点位只能用于设置它时所在的服务器和维度
  §d▸ §f去其他服务器/维度时旧点位不会运行，也不会乱跑
  §d▸ §f新地点使用：先 §e.fumo 清空§f，再重设六个点位
  §d▸ §f回原服务器/维度时旧点位可直接使用，不必重设
```

**章节 8｜注意**（`:835-839`）
```text
§3[§b#§3] §f注意
  §c⚠ §f单人世界与服务器均可使用
  §c⚠ §f至少勾选一个目标词条，否则模块不启动
  §c⚠ §f成品箱满、书箱/青金石箱空时会提示并自动停机
  §c⚠ §f模块运行中无法修改点位，先关闭模块再设置
```

> **说明与实现不一致（照抄原文，不改）**：793/779 行写「装备箱」，实际节点是 `工具护甲箱`；832 行写「重设六个点位」，GEAR 实为 8 个、BOOK/CUSTOM 为 6 个；793 行节点顺序与指令注册顺序不同。

---

## 七、播报文案全量（GEAR / 通用）

前缀恒为 `§c§l[yiyiaddon]§r§f§l[自动附魔]§r` + （`notify`：`§f` / `notifyError`：`§6§l`）+ 正文。

### 7.1 三段结构化报告（多行一条消息）

**BOOK/CUSTOM 启动**（`announceStartup` `:964-982`，标签后用**全角空格 U+3000**）：
```text
§a§l✓ 自动附魔 · 启动报告
§7当前模式　§8▸ <§b§l纯附魔模式>
§7目标词条　§8▸ <§e§lN 本>
§7原版词条　§8▸ <§e§lN 本>
§7扩展词条　§8▸ <§e§lN 本>
§7单轮抽取　§8▸ <§e§lN 次>      // 仅 EXPERIENCE
```

**GEAR 启动**（`announceGearStartup` `:2064-2073`，标签后同为**全角空格 U+3000**）：
```text
§a§l✓ 自动附魔 · 原版装备附魔启动
§7运行模式　§8▸ <运行模式>
§7目标装备　§8▸ <装备名>
§7极品方案　§8▸ <方案名>
§7目标附魔　§8▸ <N 项>
§7极品数量　§8▸ <N 件>
```

**GEAR 批次完成**（`announceGearSummary` `:2076-2087`，**7 行一条多行消息**，标签后全角空格）：
```text
§a§l✓ 原版装备附魔 · 批次完成
§7处理装备　§8▸ <N 件>
§7完成数量　§8▸ <N 件>
§7失败跳过　§8▸ <N 件>
§7附魔次数　§8▸ <N 次>
§7砂轮次数　§8▸ <N 次>
§7铁砧次数　§8▸ <N 次>
§7挂机次数　§8▸ <N 次>
```
（以上三段已按旧 `:964-982` / `:2064-2073` / `:2076-2087` 实测复核：**三段一律全角空格 U+3000**，无半角写法；批次汇总是 7 行而非一行。）

**GEAR 三段明细**（`:2499-2561`）：附魔完成（`§a✓ 附魔完成 §8▸ <大类> · <装备>` + `获得词条/目标进度/还缺词条`）、铁砧合并（`§a✓ 铁砧合并 §8▸ <装备>` + `主装备/材料/产物/费用/进度`）、砂轮清除（`§a✓ 砂轮清除 §8▸ <装备>` + `去除词条`）。`大类中文()`：`WEAPON*`→武器 / `ARMOR*`→护甲 / `TOOL*`→工具 / 其它→装备。

### 7.2 通用流程提示（逐字）

```
§c✗ 停机 §8▸ 纯附魔经验低于 <§e§l30 级>§7，切换挂机循环后重新启动        // :1111
附魔台不存在或已被挖掉，自动化已停止。请重新设置附魔台点位。               // :1158, :1180
背包无空白书，切至补给！                                                  // :1214
青金石不足 3 个，取消本次附魔并补给！                                      // :1235
附魔台青金石未达到 3 个，取消本次附魔并补给！                              // :1253
§a✓ 命中目标附魔书 §8▸ <词条>                                            // :1298
成品箱已满！自动停机。                                                    // :1472
§a已存入：§e<词条>§f，剩余任务：<N>                                      // :1489
所有极品任务已完成！自动停机。                                            // :1493
背包已满，无法继续补给！自动停机。                                        // :1529
§e⚠ <青金石|空白书>补给箱暂时没拿到，正在重试（第 N 次）...                 // :1582
<青金石|空白书>补给箱已空！自动停机。                                      // :1588
§7正在<状态中文>...                                                       // :2044（setState）
§7发包附魔中（不打开界面）...                                             // :2038（一次性）
检测到服务器或维度发生变化，已停止寻路并关闭模块！                          // :1040
目标装备不在背包，原版装备极品附魔已暂停。请补充装备后重试。                 // :1054
```

### 7.3 GEAR 流程提示（逐字）

```
装备处理失败 → <原因>，已跳过该件。                                       // :2111（gearFail）
装备箱中没有可用的<目标装备名>，原版装备极品附魔已暂停。                     // :2256
背包已满，无法继续取装备！自动停机。                                      // :2312
附魔台不存在或已被挖掉！自动停机。                                        // :2362, :2404
§c✗ 停机 §8▸ 纯附魔经验不足 <§e§l30 级>§7，切换挂机循环后重新启动            // :2371
青金石不足 3 个，无法附魔！自动停机。                                     // :2446
附魔台青金石未达到 3 个，取消附魔并停机。                                  // :2464
背包已满，无法继续取青金石！自动停机。                                    // :2603
青金石箱已空，无法附魔！原版装备极品附魔已暂停。                            // :2635
§e⚠ 铁砧已损坏或消失，正在自动更换...                                     // :2774, :2991
背包已满，无法继续取铁砧！自动停机。                                      // :2837, :2879
铁砧已损坏，铁砧箱没有备用铁砧，原版装备极品附魔已暂停。                     // :2863, :2942
§a✓ 铁砧已更换完成，继续原任务。                                          // :2936
§e⚠ 铁砧费用过高 §8▸ 无法继续合并，送砂轮清零重新培养                       // :3091
§c✗ 停机 §8▸ 纯附魔经验不足铁砧费用 <§e§lN 级>§7，切换挂机循环后重新启动      // :3101
成品箱空间不足，原版装备极品附魔已暂停。                                   // :3181
§a✓ 装备已达成极品目标，存入成品箱 §8▸ <装备名>                            // :3216
```

### 7.4 ESP 渲染（`onRender2D` `:1594-1617`）

门控 `!ESP标点.get() || !matchesCurrentPointContext()` → return。6 条标签（**GEAR 专属点位不渲染**）：

| 点位 | 文案（逐字） | 颜色 RGBA |
| --- | --- | --- |
| posBook | `§a§l[书本箱]` | (80,230,160,200) |
| posLapis | `§9§l[青金石箱]` | (70,130,255,200) |
| posOutput | `§6§l[成品箱]` | (255,200,50,200) |
| posEnchant | `§d§l[附魔台]` | (200,100,255,200) |
| posGrindstone | `§7§l[砂轮]` | (160,160,160,200) |
| posHangout | `§c§l[挂机位]` | (255,80,80,200) |

渲染：屏幕点 `(x+0.5, y+1.5, z+0.5)` → `NametagUtils.to2D(screen, 0.6)` → `renderer.begin(0.6)` → 居中 `render(label, -width/2, 0, color, true)`。

---

## 八、待核项

1. `EnchantmentSelectSetting` 的 4 参入口与 `StringListSetting` super 调用的实际参数表（批次4 移植前逐字复核）。
2. `ui/CustomEnchantSetting` 依赖的 `StringListSetting.fillTable` 内部文案（属旧壳库文案）→ 新项目控制台一律自建，不引用。
3. 使用说明第 2 章的「方式1 卡片按钮」在新项目控制台中的等价入口（「设置」按钮文案逐字 = `设置`，颜色 `§a` 已绑定 / `§8` 未绑定；删除按钮 `§c删除`；未绑定两行 `§8暂未绑定` / `§8-`；已绑定坐标 `§7X§f%d §7Y§f%d §7Z§f%d`、维度 `§7维度 §f<名>`）—— 卡片形态改控制台行，但**文案逐字保留**。
