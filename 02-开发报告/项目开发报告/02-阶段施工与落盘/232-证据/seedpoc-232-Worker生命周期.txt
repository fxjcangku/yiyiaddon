《232 · 种子挖矿正式化第四阶段 · 计算器进程生命周期与崩溃恢复结果》
装置：从客户端线程调用 SeedMiningService 公开 API（与界面按钮同一条路径），
      崩溃注入方式 = 从本进程的子进程里 destroyForcibly() 掉计算器。
世界夹具：seedpoc-known-seed（建世界前已删除上次实验存档并重建（全新 world / region / poi / entities））

零、环境
  世界夹具：seedpoc-known-seed（建世界前已删除上次实验存档并重建（全新 world / region / poi / entities））
  固定用例：Seed 20260922 区块 (0,0) 必须 45 个候选
  说明：本轮的宿主是本机隔离的计算器进程，崩溃与恢复都是它的进程事件；
        世界生成仍然只走离线管线（宿主 ChunkMap 查询必须为 0）。

一、基线（未崩溃前）
  基线结果：候选 45（期望 45）（通过）；宿主 ChunkMap 查询 0
  基线进程：PID=35288，累计启动次数=1；状态「预测完成」

二、崩溃（按正式层自报的 PID 强杀计算器）
  正式层自报 PID 35288 → 系统中该 PID 存在且在运行（通过）
  该进程命令行：（本平台不可读）
  本客户端进程 34408 的直接子进程：25144, 35288
  该进程的父进程 PID：34408（预期就是本客户端进程）
  强制结束该进程：已发出
  崩溃后状态：「本地世界生成计算器异常」（期望「本地世界生成计算器异常」，等待 30 秒）（通过）
  失败原因：本地世界生成计算器已停止（退出码 1）
  计算器进程还在跑？false（期望 false）（通过）
  客户端是否仍然存活？true（本装置还在推进刻，本身就是证据）（通过）

三、恢复（再点一次预测，允许自动重启一次）
  恢复结果：候选 45（期望 45）（通过）；宿主 ChunkMap 查询 0
  进程：PID=29520（基线 35288，必须不同）（通过）；累计启动次数=2（基线 1 + 1）（通过）
  被强杀的旧 PID 35288 是否确已消失：true（通过）

四、退出世界（断开连接）
  已回到标题界面：状态「等待进入世界」（期望「等待进入世界）」（通过）
  运行时结果已清：（通过）；计算器已停止：（通过）；重启出来的进程 29520 也已退出：true（通过）

五、判定汇总
【判定】基线预测：（通过）
【判定】崩溃后自动进入失败态：（通过） / 进程已回收 （通过） / 客户端未受影响 （通过）
【判定】崩溃后自动重启一次：（通过） / 候选 45（通过） / PID 已更换 （通过） / 启动次数 +1 （通过） / 新进程存活 （通过）
【判定】退出世界清理：状态回「等待进入世界」（通过） / 结果已清 （通过） / 无残留进程 （通过）

六、计算器诊断
  状态=未启动；PID=无；会话=无；启动次数=2；预测次数=2；最近启动耗时=4412 ms；最近预测耗时=2238 ms；宿主 ChunkMap 查询=0
全部判定：通过
