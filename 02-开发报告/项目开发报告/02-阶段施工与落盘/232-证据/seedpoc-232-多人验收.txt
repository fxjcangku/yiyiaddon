《232 · 种子挖矿正式化第四阶段 · 真正多人服务器验收结果》
环境：Minecraft 26.1.2 + 本地专用服务器（level-seed=20260922）；
客户端：真实客户端进程，由 --quickPlayMultiplayer 自动连接，
       预测时客户端没有任何集成服务端（Minecraft#getSingleplayerServer() == null）。

零、环境
  世界维度：minecraft:overworld
  Minecraft#getSingleplayerServer()：null（必须为 null 才是真正的多人环境）（通过）
  玩家位置区块：0,0
  本地世界生成计算器：未启动（本阶段宿主由它提供）

一、近端目标（口径第五十一节：Seed 20260922 区块 0,0 必须 45）
  目标区块 (0,0)：玩家在 (0,0)；客户端是否已有该区块：有（近端目标，通常已加载）
  结果：成功=true；候选 45（期望 45）；调度敏感 0；未解析 45；确定性 0；耗时 2184 ms；缓存 529；宿主 ChunkMap 查询 0（必须为 0）
  目标区块一致性：0,0（期望 0,0）（通过）

二、远端未加载区块（口径第四十八节：预测与「服务端有没有发过这个区块」无关）
  目标区块 (-400,380)：玩家在 (0,0)；客户端是否已有该区块：没有（远端目标，必须没有）
  结果：成功=true；候选 37；调度敏感 0；未解析 37；确定性 0；耗时 1176 ms；缓存 1058；宿主 ChunkMap 查询 0（必须为 0）
  目标区块一致性：-400,380（期望 -400,380）（通过）
  远端目标在客户端是否仍未加载：仍未加载（正确）（通过）

三、判定汇总
【判定】多人环境 getSingleplayerServer() == null：（通过）
【判定】多人目标 (0,0)：成功 （通过） / 候选 45（通过） / 目标一致 （通过） / 宿主查询为 0 （通过）
【判定】远端未加载区块可预测：（通过）
【判定】多人目标 (-400,380)：成功 （通过） / 候选 37（通过） / 目标一致 （通过） / 宿主查询为 0 （通过）

四、计算器诊断
  状态=已就绪；PID=19292；会话=s1-135283a；启动次数=1；预测次数=2；最近启动耗时=5424 ms；最近预测耗时=1184 ms；宿主 ChunkMap 查询=0
全部判定：通过
