《230 · 种子挖矿正式化第二阶段 · 服务层回归结果》
驱动方式：客户端线程调用 SeedMiningService 公开 API（与界面按钮同一条路径）；
世界生成在服务层后台单线程执行，渲染线程只做提交与读数。
数字基线：报告 229（正式 Predictor 迁移回归）与报告 228（FEEDBACK 调度定案）。
测试世界：seedpoc-known-seed（每轮新建，种子 20260922；注意预测用的是手动填写的种子，与世界种子无关）
装置新鲜度：建世界前已删除上次实验存档并重建（全新 world / region / poi / entities）

一、未进入世界（标题界面）
  关闭状态下状态：已关闭；期望「已关闭」→ （通过）
  开启但未进入世界：等待进入世界；期望「等待进入世界」→ （通过）
  进入世界：minecraft:overworld；世界生成宿主=可用

二、界面装配烟测（控制台「种子挖矿」页）
  个人模式关：控制台整窗装配成功；单独装配「种子挖矿」页成功，总高 750 像素、是否为空：false
  个人模式开：控制台整窗装配成功（种子页签走隐藏路径，不抛异常）

三、种子输入解析（服务层公开 API）
  输入「（空串）」→ 解析 null / 输入状态「未填写」/ 运行时状态「等待填写服务器种子」
  输入「0」→ 解析 0 / 输入状态「已填写」/ 运行时状态「已就绪」
  输入「12345」→ 解析 12345 / 输入状态「已填写」/ 运行时状态「已就绪」
  输入「-7777」→ 解析 -7777 / 输入状态「已填写」/ 运行时状态「已就绪」
  输入「20260922」→ 解析 20260922 / 输入状态「已填写」/ 运行时状态「已就绪」
  输入「9223372036854775807」→ 解析 9223372036854775807 / 输入状态「已填写」/ 运行时状态「已就绪」
  输入「-9223372036854775808」→ 解析 -9223372036854775808 / 输入状态「已填写」/ 运行时状态「已就绪」
  输入「abc」→ 解析 null / 输入状态「格式无效」/ 运行时状态「种子格式无效」
  输入「12.3」→ 解析 null / 输入状态「格式无效」/ 运行时状态「种子格式无效」
  输入「--」→ 解析 null / 输入状态「格式无效」/ 运行时状态「种子格式无效」
  非法种子下是否允许预测：禁止（正确）

四、服务层「测试当前区块预测」（后台单线程执行）
  用例 种子 20260922 区块 (0,0)：229 固定集首个目标（冷启动）｜提交后状态「正在预测当前区块…」，客户端线程提交耗时 718 微秒（世界生成不在渲染线程）
    状态：预测完成；目标区块 (0,0)（期望 (0,0)）（通过）
    候选钻石 45（期望 45）（通过）；调度敏感 0（期望 0）（通过）
    未解析 45；确定性 0（本阶段恒 0）（通过）；未解析=候选-敏感：（通过）
    预测耗时 2539 ms；会话持有离线区块 529 个；宿主 ChunkMap 查询增量 0（必须为 0）（通过）
  用例 种子 2 区块 (-400,380)：228 受控顺序样本：争议格 (-6385,-59,6085) 必须仍是调度敏感｜提交后状态「正在预测当前区块…」，客户端线程提交耗时 59 微秒（世界生成不在渲染线程）
    状态：预测完成；目标区块 (-400,380)（期望 (-400,380)）（通过）
    候选钻石 23（期望 23）（通过）；调度敏感 1（期望 1）（通过）
    未解析 22；确定性 0（本阶段恒 0）（通过）；未解析=候选-敏感：（通过）
    预测耗时 1820 ms；会话持有离线区块 529 个；宿主 ChunkMap 查询增量 0（必须为 0）（通过）
    已知争议位置 (-6385,-59,6085)：调度敏感；期望「调度敏感」（通过）
  用例 种子 12345 区块 (0,0)：229 Seed 12345 回归｜提交后状态「正在预测当前区块…」，客户端线程提交耗时 44 微秒（世界生成不在渲染线程）
    状态：预测完成；目标区块 (0,0)（期望 (0,0)）（通过）
    候选钻石 31（期望 31）（通过）；调度敏感 0（期望 0）（通过）
    未解析 31；确定性 0（本阶段恒 0）（通过）；未解析=候选-敏感：（通过）
    预测耗时 1756 ms；会话持有离线区块 529 个；宿主 ChunkMap 查询增量 0（必须为 0）（通过）
  用例 种子 12345 区块 (-1,-1)：228「真值不可重复」区块：正式分析给出 7 个调度敏感｜提交后状态「正在预测当前区块…」，客户端线程提交耗时 151 微秒（世界生成不在渲染线程）
    状态：预测完成；目标区块 (-1,-1)（期望 (-1,-1)）（通过）
    候选钻石 29（期望 29）（通过）；调度敏感 7（期望 7）（通过）
    未解析 22；确定性 0（本阶段恒 0）（通过）；未解析=候选-敏感：（通过）
    预测耗时 274 ms；会话持有离线区块 574 个；宿主 ChunkMap 查询增量 0（必须为 0）（通过）
  用例 种子 12345 区块 (-25,17)：228 顺序无关目标｜提交后状态「正在预测当前区块…」，客户端线程提交耗时 278 微秒（世界生成不在渲染线程）
    状态：预测完成；目标区块 (-25,17)（期望 (-25,17)）（通过）
    候选钻石 24（期望 24）（通过）；调度敏感 0（期望 0）（通过）
    未解析 24；确定性 0（本阶段恒 0）（通过）；未解析=候选-敏感：（通过）
    预测耗时 1040 ms；会话持有离线区块 1103 个；宿主 ChunkMap 查询增量 0（必须为 0）（通过）
  用例 种子 12345 区块 (120,-130)：228 顺序无关目标｜提交后状态「正在预测当前区块…」，客户端线程提交耗时 138 微秒（世界生成不在渲染线程）
    状态：预测完成；目标区块 (120,-130)（期望 (120,-130)）（通过）
    候选钻石 26（期望 26）（通过）；调度敏感 0（期望 0）（通过）
    未解析 26；确定性 0（本阶段恒 0）（通过）；未解析=候选-敏感：（通过）
    预测耗时 1121 ms；会话持有离线区块 1632 个；宿主 ChunkMap 查询增量 0（必须为 0）（通过）

五、服务结果 vs 直连正式预测器（逐 BlockPos，只对第一个用例做，避免重复世界生成）
  直连正式 Predictor（独立实例、独立后台线程）预测 种子 20260922 区块 (0,0)……
  服务层 45 个 / 直连 45 个 → 服务有直连没有 0 / 直连有服务没有 0 → 逐 BlockPos 完全一致

六、维度切换（实机切换：主世界 → 下界 → 主世界）
  切换前：状态「预测完成」，上一次结果 26 个，当前维度 主世界
  下界：维度显示「下界」/ 支持状态「当前版本暂未支持下界矿物预测」/ 运行时状态「当前维度暂不支持」（通过）
    旧维度结果是否已清除：已清除（通过）
    下界是否允许预测：禁止（正确）（通过）
  返回主世界：维度显示「主世界」/ 支持状态「支持：钻石预测」/ 运行时状态「已就绪」（通过）
    返回后结果仍为空（旧维度的结果不会复活）：（通过）

七、回主世界后补一次预测（让「退出世界确实持有预测器」可被观测）
  补预测结果：20 个；是否持有预测器：true（通过）

八、退出世界（断开连接）
  断线前：状态「预测完成」，是否持有预测器：true，上一次结果：有
  已回到标题界面：状态「等待进入世界」（通过）
    运行时结果已清：（通过）；预测器已释放：（通过）
    配置保留：启用=true，种子原文=12345（种子与开关属于配置，按口径不清）

九、判定汇总
【判定】界面装配烟测：通过（两种模式整窗装配成功 + 种子页可构建 750 像素）
【判定】种子输入解析矩阵：（通过）；非法种子禁止启动预测：（通过）
【判定】用例 种子 20260922 区块 (0,0)：目标区块 （通过） / 候选 45（通过） / 调度敏感 0（通过） / 确定性为零 （通过） / 未解析关系 （通过）
【判定】用例 种子 2 区块 (-400,380)：目标区块 （通过） / 候选 23（通过） / 调度敏感 1（通过） / 确定性为零 （通过） / 未解析关系 （通过） / 争议格调度敏感 （通过）
【判定】用例 种子 12345 区块 (0,0)：目标区块 （通过） / 候选 31（通过） / 调度敏感 0（通过） / 确定性为零 （通过） / 未解析关系 （通过）
【判定】用例 种子 12345 区块 (-1,-1)：目标区块 （通过） / 候选 29（通过） / 调度敏感 7（通过） / 确定性为零 （通过） / 未解析关系 （通过）
【判定】用例 种子 12345 区块 (-25,17)：目标区块 （通过） / 候选 24（通过） / 调度敏感 0（通过） / 确定性为零 （通过） / 未解析关系 （通过）
【判定】用例 种子 12345 区块 (120,-130)：目标区块 （通过） / 候选 26（通过） / 调度敏感 0（通过） / 确定性为零 （通过） / 未解析关系 （通过）
【判定】服务层结果 vs 直连正式 Predictor 逐 BlockPos 一致：（通过）
【判定】下界状态与旧结果清理：状态 （通过） / 结果已清 （通过） / 禁止预测 （通过）
【判定】返回主世界回到就绪：（通过） / 结果未被旧维度复活：（通过）
【判定】退出世界清理：状态回「等待进入世界」（通过） / 结果已清 （通过） / 预测器已释放 （通过）
全部判定：通过
