日期：2026-09-19

# 混淆备份目录中文命名

按用户授权，将 `05-混淆文件备份/1.0-beta1/` 下的两个纯摘要目录改为：

- `01-基础混淆版-21413309`
- `02-字符串与控制流加强版-61168863`

目录内部文件保持原样，完整 SHA-256 继续保存在产物信息中。发布构建的备份规则同步使用 `02-字符串与控制流加强版-<摘要前8位>`，原有逐文件比较保留；即使摘要前缀相同，只要已有文件内容不同便拒绝覆盖。同步更新备份首页与 111—113 号报告中匹配的目录链接。

实际执行 `.\gradlew build buildRelease --console=plain -q`，EXIT=0；最终包常量审计和 64,448 项行为对照通过。构建复用了已改名的加强版目录，没有再次生成纯摘要目录。本次未实机，不修改业务源码与发布 JAR 内容，不提交或推送。新项目仍是独立 Fabric 模组，旧项目未改动。

## 下一阶段开发规划

### 一、下一阶段目标

沿用报告 112 的实际功能与性能验收计划。

### 二、执行原因

目录汉化便于查阅，不代替游戏功能验收。

### 三、前置条件检查

目录整理、备份规则及构建检查完成；实际场景仍需测试环境。

### 四、任务拆分

1. 目标：实际功能验收；涉及目录：独立测试实例；影响范围：测试数据；预计修改内容：先记录测试结果，发现明确问题再定点修正。

### 五、验收标准

本轮构建通过、报告完成、没有新增旧框架依赖；此前客户端启动证据见报告 112，完整功能待真实环境验证。

### 六、禁止事项

不公开映射，不混用不同产物的映射，不将目录整理当成游戏功能验证。

## 项目迁移路线图

已完成：混淆资料归档与外层目录中文命名。

当前：目录可直接查阅，自动备份规则已同步。

下一步：按报告 112 完成功能与性能验收。

未来：业务迁移仍以 30 号总览为准。
