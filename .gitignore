# Gradle
.gradle/
build/
# 发布加固工具的 Java 包名为 build，这里是源码，不能被构建目录规则忽略。
!gradle/hardening/src/com/yiyiaddon/build/
out/
bin/
loom-cache/

# Fabric / 运行期（运行目录按版本分流：run-26.1.2/ 与 run-26.2/，见 开发习惯.md 第 232 条）
/run/
/run-*/
logs/
crash-reports/
.fabric/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.code-workspace
.settings/
.project
.classpath

# 编译产物与二进制中间物
*.class
*.jar
*.log
*.env

# OS
.DS_Store
Thumbs.db

# 版本库内必须保留的构件（覆盖上方 *.jar 规则）
!gradle/wrapper/gradle-wrapper.jar
!maven-repo/**/*.jar

# 开发参考库：本地只读参考资料（Minecraft 26.1.2 官方源码、Baritone 源码、映射索引）
# 不参与正式编译，不进入 yiyiaddon.jar，不作为运行时依赖，不进版本库
01-开发参考库/

# 后端 API 的本地工具缓存（wrangler 账号缓存等），属机器本地状态，不进版本库
03-后端API/.wrangler/

# 埋点修复：原始事件日志是本机取证数据（可能几十 MB），不进版本库；
# 案例档（README.md）、案例模板、工具脚本一律保留（见 开发习惯.md 第三十三章）
04-埋点修复/**/日志-*.ndjson
04-埋点修复/**/*.bak-*
